---
name: "ArchResearch"
description: "问题启动、图像优先的建筑研究工作台，证据始终贴近图纸。"
colors:
  studio-canvas: "#EEF0ED"
  paper: "#FFFFFF"
  surface-subtle: "#F6F7F5"
  graphite: "#171A18"
  ink-secondary: "#5E6661"
  ink-tertiary: "#626A65"
  ink-disabled: "#8B938E"
  line: "#D5DAD6"
  line-strong: "#ADB5B0"
  blueprint: "#2F5BFF"
  blueprint-hover: "#2449D8"
  blueprint-soft: "#EDF1FF"
  blueprint-line: "#B6C3EF"
  marker: "#FFD84D"
  marker-ink: "#413500"
  focus: "#2F5BFF"
  evidence: "#1F7A5A"
  evidence-soft: "#E7F3ED"
  restriction: "#D6533C"
  restriction-ink: "#A63827"
  restriction-soft: "#FFF0EC"
  restriction-line: "#E6B4AA"
  image-well: "#E5E8E5"
  overlay: "#171A1847"
typography:
  display:
    fontFamily: '"Segoe UI", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif'
    fontSize: "2rem"
    fontWeight: 700
    lineHeight: "2.5rem"
    letterSpacing: "-0.035em"
  headline:
    fontFamily: '"Segoe UI", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif'
    fontSize: "1.5rem"
    fontWeight: 700
    lineHeight: "2rem"
    letterSpacing: "-0.015em"
  title:
    fontFamily: '"Segoe UI", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif'
    fontSize: "1.25rem"
    fontWeight: 600
    lineHeight: "1.75rem"
    letterSpacing: "-0.01em"
  subtitle:
    fontFamily: '"Segoe UI", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif'
    fontSize: "1rem"
    fontWeight: 600
    lineHeight: "1.5rem"
    letterSpacing: "normal"
  body:
    fontFamily: '"Segoe UI", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif'
    fontSize: "0.875rem"
    fontWeight: 400
    lineHeight: "1.375rem"
    letterSpacing: "normal"
  body-compact:
    fontFamily: '"Segoe UI", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif'
    fontSize: "0.8125rem"
    fontWeight: 400
    lineHeight: "1.25rem"
    letterSpacing: "normal"
  label:
    fontFamily: '"Segoe UI", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif'
    fontSize: "0.75rem"
    fontWeight: 600
    lineHeight: "1rem"
    letterSpacing: "normal"
  caption:
    fontFamily: '"Segoe UI", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif'
    fontSize: "0.6875rem"
    fontWeight: 500
    lineHeight: "1rem"
    letterSpacing: "normal"
  metadata:
    fontFamily: '"Cascadia Mono", Consolas, monospace'
    fontSize: "0.6875rem"
    fontWeight: 500
    lineHeight: "1rem"
    letterSpacing: "normal"
rounded:
  sheet: "12px"
  control: "8px"
  popover: "12px"
  dialog: "16px"
  pill: "999px"
spacing:
  "0": "0"
  "4": "4px"
  "8": "8px"
  "12": "12px"
  "16": "16px"
  "24": "24px"
  "32": "32px"
  "40": "40px"
  "48": "48px"
  "56": "56px"
  "64": "64px"
  "80": "80px"
layout:
  shellMax: "1760px"
  stageMax: "1600px"
  studioGrid: "128px"
  researchPromptDesktop: "152px"
  researchPromptTablet: "132px"
  researchPromptMobile: "108px"
components:
  button-primary:
    backgroundColor: "{colors.blueprint}"
    textColor: "{colors.paper}"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "8px 16px"
    height: "36px"
  button-secondary:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.graphite}"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "8px 12px"
    height: "36px"
  research-bar:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.graphite}"
    typography: "{typography.body}"
    rounded: "{rounded.popover}"
    padding: "12px 16px"
    height: "48px"
  drawing-sheet:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.graphite}"
    typography: "{typography.body-compact}"
    rounded: "{rounded.sheet}"
    padding: "0"
  evidence-drawer:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.graphite}"
    typography: "{typography.body}"
    rounded: "{rounded.sheet}"
    padding: "24px"
    width: "500px"
---

# Design System: ArchResearch

## Overview

**Creative North Star: “轻快的数字建筑评图桌”**

ArchResearch 是建筑学生和青年设计师使用的研究工作面。整个视口是一张浅灰制图桌，而不是承载居中应用卡片的空背景：低对比定位网格、裁切线稿与注册角把宽屏余量也纳入工作面。首页像刚铺开的蓝图桌，问题入口醒目、语气轻快、起点容易尝试；进入结果后则回到克制的评图桌。建筑设计按“任务 → 研究结论 → 问题章节 → 案例答案”展开，图纸灵感按“任务 → 风格方向 → 图纸类型 → 图片观察”展开。视觉记忆点来自全局制图场、蓝图任务面、少量标记纸与真实图纸比例，不是渐变、玻璃或指标面板。

**产品原则**

- **图像先于控制器：** 默认视图把空间留给图纸；筛选、进度和导出不得形成永久仪表盘，Trace 只保留为内部诊断数据。
- **双结构组织结果：** 建筑设计直接按 3–6 个问题章节显示完整案例答案，不再先重复一遍问题拆解；图纸灵感把宽泛提问扩展为若干互斥风格方向，再按方向组织图片。用户不需要先把视觉需求写成完整设计问题。
- **案例答案承载方法：** 后台项目研究包继续保留正文来源、URL 与引文，前台每个项目只显示名称、逐题机制、最多三条做法、一条适用条件和最多一张识别图。图片不是机制证据，也不是案例准入条件。
- **方案案例与制图灵感分工：** 项目官网与多家可信建筑媒体进入项目档案，回答方案如何成立；小红书按线稿、拼贴、材质渲染、氛围叙事等方向进入独立灵感板，回答图可以怎样表达。视觉线索不计作方案项目，也不因画面相似获得项目事实。
- **答案先于过程：** 建筑结果和建筑收藏每案例保留一个安静的出处链接（出处 · 域名），但不显示来源钉、核验状态、逐字引文、研究备注或过程入口；其余字段继续留在 durable data 与导出边界。
- **默认完整阅读：** 每个有可用结果的章节先显示一次问题结论，随后按排序显示全部合格案例；不使用 `<details>`、代表案例摘要表或第二套完整档案。
- **入口轻快，证据克制：** 首页允许一个明确的蓝图任务面和一个标记黄提示；结果、证据和权利状态仍采用短标签、细结构线与功能性色彩。
- **熟悉的工具行为：** 使用标准按钮、表单、菜单、抽屉与键盘顺序；一致性高于新奇交互。
- **问题先于结果：** 默认首页以问题输入为主，不直接展开研究结果；允许显示研究方式、问题示例、资料入口和任务级历史摘要，帮助首次开始或继续任务。研究是后台进程：打开应用永远落在主页，未结束的运行不劫持视图，只作为最近研究中的实时状态行；进入任何进度或结果视图都需要明确操作。本次会话内用户亲自发起的研究仍立即进入进度视图。
- **关联分析不串题：** 同一项目支持多个建筑子问题、或同一图纸支持多个灵感方向时，每个关联分别保存与展示分析；不得把第一个分支的结论复制到其他章节。
- **建筑案例对照产生判断：** 比较视图固定比较解决对象、可借鉴方法、图中看到和适用条件，不显示来源或证据状态；禁止只把两张卡片并排。对照入口与对话框统一命名“对照案例策略”。图纸灵感不提供比较视图。

**全局布局规则**

- 4px 是最小单位，8px 是主要节奏。组件内部优先使用 8/12/16，组件之间优先使用 16/24/32，页面区段使用 32/48。新尺寸必须先成为语义 token，不在单个组件里临时发明。
- 全局外壳最大宽度 1760px，主要工作台最大宽度 1600px，正文段落最大 72ch。页面不得产生横向滚动；仅对比表、阶段轨等局部区域允许显式横向滚动。
- 断点固定为：单列 `≤620px`、双列 `621–860px`、三列 `861–1180px`、四列 `>1180px`。页面边距依次为 12/16/24px，图纸间距为 12/16px。
- 桌面可显示 56px 瘦工具轨；中屏及以下并入顶部工具菜单。来源和完整筛选器永远不常驻在工具轨内；Trace 不进入用户界面。
- 项目档案按子问题和相关度保持 DOM/键盘顺序；档案内图组保持资源原始长宽比，不使用 CSS columns 或 `order` 制造与阅读顺序不一致的布局。
- 全视口使用 128px 静态定位网格；`>1380px` 每个主视图最多显示一个被画面裁切的平面或剖面线稿。首页背景强度高于结果页，`≤860px` 降低网格，`≤620px` 关闭全部背景纹样。

## Colors

颜色采用“首页一个 committed 蓝图任务面，结果页保持中性”的分区策略。首页的 `blueprint` 可占首屏约 12–20%，只承载当前研究启动器；结果、证据、对比和导出视图中高饱和色面积仍不得超过 10%。色彩只表达任务焦点、动作、选择、证据或限制。

| 语义 token | 用法 |
| --- | --- |
| `studio-canvas` / `paper` / `surface-subtle` | 应用底、图纸与抽屉、低优先级分区。`studio-canvas` 可承载全局静态定位网格；图像查看面始终使用不透明 `paper`。 |
| `graphite` / `ink-secondary` / `ink-disabled` | 主文字、补充文字、不可交互文字。正文不得降到 `ink-disabled`。 |
| `line` / `line-strong` | 分隔与静态边界、可交互控件边界。禁止用阴影代替所有结构线。 |
| `blueprint` / `blueprint-hover` / `blueprint-soft` | 主动作、当前选择与选择底色；仅首页任务启动器可作大面积背景。 |
| `marker` / `marker-ink` | 首页“还不知道怎么描述”提示纸及其文字；单屏不超过 2%，永不表达系统状态、证据或权利。 |
| `focus` | 所有键盘焦点的 3px 外轮廓，偏移 2px；焦点不得只靠颜色变化。 |
| `evidence` / `evidence-soft` | 已核验证据及其低强度底色，只表示核验，不表示收藏或版权。 |
| `restriction` / `restriction-ink` / `restriction-soft` | 权利限制、拒绝、冲突和错误。小字必须使用对比度更高的 `restriction-ink`。 |
| `warning-soft` / `warning-line` / `warning-ink` | 需要用户注意但不是错误的状态（如备份记录过期）。文字必须使用深色 `warning-ink`（白底对比度 ≥4.5:1），且颜色之外必须有文字说明。 |
| `overlay` | 抽屉与对话框遮罩；不得叠加模糊玻璃效果。 |

**证据与权利分离规则。** `verified/partial/visual_lead` 对外固定显示为“来源与内容已核对 / 已有依据，仍需核对 / 只作视觉参考”。后两者使用中性图标加文字；不得用限制红表达低可信度，也不得用核验绿表达可公开分享。

**蓝色稀缺规则。** 结果和证据区域只允许一个蓝色主动作。首页任务启动器是唯一的大面积例外；启动器内部蓝色仍只负责外层焦点、主动作和明确选择，不把普通说明、每个图标和每条边界都染蓝。

## Typography

全产品只使用系统无衬线；等宽字体只服务于证据编号、时间和尺寸。字号固定，不使用 `clamp()`；中文正文按 14/22px 起步，任何可操作标签不得小于 12/16px。

| 层级 | 角色 |
| --- | --- |
| `display` | 页面唯一主标题或大型空状态；每屏最多一次。 |
| `result question` | 结果页当前任务；桌面 / 平板 / 手机固定为 40 / 34 / 26px，每屏一次，不使用流体字号。 |
| `headline` | 抽屉、对比视图或主要区段标题。 |
| `title` | 研究问题、结果组标题。 |
| `subtitle` | 图纸标题、项目标题、面板小标题。 |
| `body` | 事实、观察、推断、说明文本；长文控制在 65–72ch。 |
| `body-compact` | 图纸方法句、菜单说明和密集列表。不得承载关键错误说明。 |
| `label` | 按钮、字段、筛选、状态；使用句式大小写，不默认全大写。 |
| `caption` | 次级时间、计数与辅助说明。 |
| `metadata` | ID、页码、尺寸、耗时、阶段事件；不得用于正文或按钮。 |

**项目档案层级规则。** 项目标题使用 `subtitle`，项目条件、设计机制、可迁移策略和适用边界使用 `body/body-compact` 的固定标题；单张图纸只使用 `label/caption` 标记类型与证据层级。不得为每张图纸重复项目标题和整段方法结论。

**字重规则。** 正文 400，强调和标签 600，一级标题 700。禁止用 800/900 或连续三种加粗层级制造层次。

## Elevation

系统默认是平的：底色、1px 结构线和局部遮罩负责分层。阴影只在当前图纸、浮层和抽屉离开工作面时出现；静态图纸不得全部漂浮。

| 层级 | 阴影 | 用法 |
| --- | --- | --- |
| Flat | `none` | 画布、静态图纸、内联分区。 |
| Task island | `0 5px 0 rgba(23, 26, 24, 0.12)` | 仅首页蓝图任务启动器，形成一张落在桌面的工作纸。 |
| Marker note | `3px 3px 0 rgba(23, 26, 24, 0.16)` | 仅首页一个标记黄问题提示，不复制到普通卡片。 |
| Sheet active | `0 8px 24px rgba(23, 26, 24, 0.08)` | 仅 hover、键盘聚焦或选中的当前图纸。 |
| Popover | `0 12px 32px rgba(23, 26, 24, 0.14)` | 菜单、项目创建浮层、比较停靠条。 |
| Drawer | `-16px 0 42px rgba(23, 26, 24, 0.16)` | 右侧证据抽屉。 |
| Modal | `0 24px 64px rgba(23, 26, 24, 0.22)` | 必须阻断当前任务的对话框；优先使用内联或抽屉。 |

**语义 z-index**

`base 0` → `sheet-active 10` → `sticky 100` → `dropdown 200` → `comparison-dock 300` → `backdrop 400` → `drawer 500` → `modal 600` → `toast 700` → `tooltip 800`。禁止使用 999/9999 或组件私有的任意层级。

**浮起规则。** 图纸 hover 只允许 `translateY(-2px)` 配合 Sheet active 阴影；图像本身不缩放，周围图纸不移动。

## Components

### App shell 与研究栏

- 顶栏最小高度 60px，保持稳定定位；只放品牌与当前任务动作。研究项目选择与新建属于首页“最近研究”的父级上下文，结果页不显示。
- `StudioBackdrop` 在全局 App shell 只挂载一次，使用 `aria-hidden`、`pointer-events: none` 和自身裁切层；不得给工作区增加会改变抽屉/浮层包含块的 transform、filter 或 containment。
- 首页只显示一个裁切平面片段和一个黄色注册点；结果页替换为半强度剖面片段且不出现黄色装饰。来源抽屉、正式事实、权利判断和图像井一律保持不透明纯色。
- 首页、个人收藏页与结果页互斥显示：首页可以显示任务级历史摘要，但不得显示图纸缩略图、证据卡或旧结果墙；收藏与结果页都使用唯一“返回主页”回到问题输入。收藏页顶部只有建筑方案 / 图纸灵感原位切换；建筑方案内部允许“问题目录 → 单个子问题详情”的轻量层级，详情使用明确的“返回问题目录”，不新增弹窗、第三类收藏或全页路由。
- 首页工作面桌面最大宽度 1600px。蓝图任务启动器由蓝色标题区和内嵌白色输入纸组成；主输入、“建筑设计研究 / 图纸灵感”双入口、当前场景资料和一个主动作仍属于同一表单，不另建设置面板。
- 只有建筑设计研究常显紧凑“研究方式”。概览/标准/深入只先显示“快速找方向 / 形成方案依据 / 做跨案例论证”，当前选择再显示一句具体差异；同一说明明确案例会轮换多家建筑媒体且只保留正文证据完整的项目。图纸灵感隐藏全部深度控件并自动使用小红书，在最近研究中省略深度标签。
- 主问题输入区按桌面 / 平板 / 手机使用 152 / 132 / 108px 最小高度，并继续允许用户垂直拉伸。它是主要思考工作面，不按普通单行研究栏的 48px 高度处理。
- 任务启动器下方采用“常见问题起点 + 最近研究”两栏：`>1380px` 四个问题起点排成一条横向启动带，最近研究约占四分之一；常规桌面为 2×2 起点，`≤860px` 单列。问题起点点击只填入并聚焦问题；最近研究跨项目直接平铺全部 Run，以当前 Run 问题派生的短记录名作为主标题，再显示方式、状态和可用图纸数，建筑设计研究另显示深度。完整列表位于最大 320px / 45dvh 的具名、可聚焦原生滚动窗口，默认只露出约三条记录而不随历史数量拉长主页；标题说明与“新建项目”使用同一紧凑双列头部。记录名不得复用项目名，不得改写完整问题，也不得为命名增加模型调用；无法拆分的句式以问题开头截断至 28 个字符。主页不显示项目选择器、“当前项目 / 历史归档”分类、独立归档区或完整历史弹层；点击记录后才加载完整结果，并只在后台同步其所属项目。“新建项目”是最近研究区唯一显式项目动作。历史 Run 默认保留 14 天，用户可单条设为永久保留。
- 首页分区使用 1px 结构线和 16/24/32px 间距，不使用指标卡、模板商城、资讯流或营销型功能卡。只有任务启动器与一个标记提示可使用已定义的硬阴影；最近研究保持 Flat。
- 运行状态是一条紧凑状态带，直接保留取消/重试与当前阶段，不显示内部 Trace，也不使用指标卡片。
- “研究环境”只在图纸灵感中常显为中性紧凑状态行，表达小红书与可选页面读取状态；建筑案例研究不显示技术预检，也不因可选 Chrome 图纸读取权限被阻塞。不得把供应商余额、保留额度或模型能力做成产品状态、仪表或设置。

### Case answer、Visual drawing sheet 与来源钉

- 建筑设计结果页固定按“任务 → 研究结论 → 问题章节 → 案例答案 → 继续使用工具”展开；图纸灵感固定按“任务 → 灵感方向 → 制图灵感 → 继续使用工具”展开。演示数据必须显示“演示任务”，真实运行显示“本次研究任务”。建筑设计工作台固定为“对照案例策略 / 导出策略矩阵 / 生成可分享结果板”；图纸灵感固定为“编辑图纸表达规范 / 查看个人收藏 / 生成可分享来源板”。完成的建筑结果不显示研究进度、过程入口或 Trace 抽屉。
- 研究结论标题只承载一条用户可直接行动的设计判断。简短正常 answer 原样显示；`deterministic_fallback`、`【本地证据汇总】` 前缀或异常超长 answer 从既有 causal mechanism 投影为不超过 84 字的标题，不能补写事实。建筑结果的优先做法去掉“转译步骤 / 转译建议”包装与来源审计尾句，按文本去重并最多展示三条；适用条件取第一条非审计边界。完成态建筑结果不显示状态条、证据计数、raw answer 或审计折叠层。
- 每个子问题形成一个连续案例章节。章节标题后直接显示一次题目级结论；随后按原项目顺序显示全部合格案例，每个案例依次展示去除 ArchDaily 等发布媒体后缀的项目身份、逐题机制、最多三条可执行做法、第一条非审计适用条件和最多一张识别图。
- 建筑案例章节不显示项目条件台账、逐字引文、来源、核验/权利状态、相关度、图面观察或来源操作，也不使用原生 `<details>`。无可用结果时显示诚实短提示；待归组线索不进入答案页。相关完整数据继续保留在后台研究包。
- 同一项目支持多个子问题时，可以出现在多个章节，但每个章节读取自己的关联分析；不得只按首个子问题归类，也不得沿用另一章节的机制、观察或边界。
- 案例答案是一个平面结构区段，标题、正文和识别图由分隔线与留白组织；禁止用“项目卡片里再嵌图纸卡片”的层层圆角容器，也禁止退化为同尺寸图纸卡墙。
- 档案分析使用 112px 标签轨加全宽正文的四行台账；中文正文不得沿用西文 `ch` 窄栏。`>860px` 的长转译列表可按 DOM 顺序分为两栏且单条不可跨栏，`≤860px` 恢复单栏。项目预览只有一项时铺满 gallery 并采用左图右文，`≤860px` 图上文下；两项以上才使用图纸网格。多图时，共同推断、相同观察和共同缺省说明以一条平面式“共同图面说明”置于图组上方且各只出现一次；没有独有观察的图片不渲染空 caption。单图继续显示原标题和观察。
- 图纸保持原始长宽比，`object-fit: contain`；白色图像井使用 12px 圆角和 1px 结构线。图组按互补性和相关度排序，筛选只改变可见图纸，不改写项目分析。
- 建筑案例的单张识别图只显示图纸类型，不显示证据层级或来源操作。图纸灵感的单张图纸可保留图纸类型和既有来源交互；操作收在图纸边缘，hover、focus 或选择后才显示次要动作。
- 来源钉只属于图纸灵感，不得出现在建筑结果、建筑收藏或建筑对照中。图纸灵感沿用 16px 图标和文字/可读名称，不得只显示彩色圆点。
- 加载使用保持预期比例的骨架屏；预览失败保留纸张尺寸并给出“重试/打开来源”，不以中央 spinner 替代内容。

### Visual inspiration board

- 小红书 `visual_lead` 不进入 Project dossier。它们按“灵感方向 → 帖子 → 帖子内图片”形成独立的制图灵感板；帖子统一显示平台、标题、一次整体观察、原笔记链接、自然语言相关度与权利未知边界，图片只保留逐图类型和独立选择。界面不显示“问题匹配 3/4”类内部分数。
- 图纸灵感允许“我想出一张轴测图，帮我找风格”“效果图怎么出”这类宽泛输入。用户点名一种图纸时，planner 的所有短方向都固定该类型、只变化表达风格；只有未指定类型时才选择合适图纸类型。不同方向必须在可见语言上互斥，rationale 只描述线宽、色彩、纹理、构图、光影和注释等可搜索特征，不生成项目条件、功能或流线问题。
- 图纸灵感使用固定的视觉配置，不向用户暴露概览/标准/深入。每个方向执行一次视觉平台排序搜索，最多依 rank 尝试四篇笔记，累计三篇产生可用资产的帖子后停止；每帖最多四图。视觉平台已有候选时不并行检查同题通用网页；OpenCLI 搜索失败或为空时才进入扩展后备路径，两条小红书路径都不可用时诚实终止而不降级通用网页。所有路径共享 48 个逐图检查槽位 / 48 MiB 总预算；extension fallback 达到预算且仍有方向不足三篇 usable 帖子时诚实保留 partial，并显示为图纸检查容量已用完。
- 建筑设计研究按查询顺序轮换 ArchDaily、Designboom、Dezeen、Divisare、ArchDaily China，并从可信文章继续核对项目官网；图纸灵感只检查小红书。来源优先级随任务意图变化，不使用 publication tier 作为跨用途的唯一排序轴，也不为凑站点数降低正文证据门。
- 每个灵感方向先用紧凑类型索引汇总平面、剖面、分析图、轴测、效果图等数量，再按帖子排列图片组。禁止为每张图重复帖子标题、观察、原帖链接和权利说明，也禁止在每种类型只有一张图时建立占满整行的稀疏分区。
- 帖子组只显示一次帖子标题、首个唯一画面观察、自然语言相关度、来源/权利和原笔记；组内每张图只显示图纸类型，并保留查看证据与独立选择导出。不得显示空的“项目条件/空间机制”，也不得把视觉图片计入项目数量。
- `>1180px` 帖子组最多两列，`≤1180px` 单列；帖子内图片保持两列紧凑网格，紧凑屏的选择目标不小于 44px。图片保持原图比例并使用 `object-fit: contain`，页面本身不得横向滚动。

### Evidence drawer

- 来源检视器只由图纸灵感的图片入口打开，建筑结果、建筑收藏和建筑对照均不可到达。桌面使用 1040px 图面/证据双区，紧凑屏占满视口并改为图面在前的单列；头部与关闭动作粘滞。抽屉打开后，焦点进入标题/关闭按钮，关闭后返回图纸灵感触发入口。
- 检视器的唯一主任务是回答“这条判断由原文哪句话支持”。右栏在项目身份后立即显示“逐字原文证据”，每条按 statement、逐字引文、PDF 页码或 URL 定位链接排列；发布来源、项目身份、图片归属和权利状态随后常显，不折叠。
- 项目条件、空间机制、转译步骤和适用边界继续由完整项目档案承载，图中观察继续贴在单图证据条上；检视器不得再次复制这些分析。左栏“打开原始来源”、收藏/拒绝和研究备注保持可用，抽屉内禁止嵌套卡片墙。

### Controls

- 默认控件高 36px，研究主输入高 48px；紧凑屏触摸目标不得小于 44×44px。主按钮只承载当前区域最重要动作。建筑结果页不提供图纸类型筛选器（M143 移除）。
- 按钮、输入、选择器、标签与菜单必须覆盖 default、hover、focus-visible、active、disabled、loading、error。disabled 同时取消指针动作并保留可读标签。
- 输入框使用 `paper`、`line-strong` 和全局 `control` 8px 圆角；error 同时显示图标、边界和具体修复文字。placeholder 按正文对比度处理。
- 筛选器使用一行紧凑选择/分段控件，溢出项进入“更多筛选”；不把每个资产类型做成同等抢眼的按钮。
- 建筑研究在项目档案标题处按案例选择一次，不在案例图组里逐张勾选图纸；图纸灵感继续逐图选择。选择达到 1 项后出现底部停靠条；建筑案例 2–6 项可打开比较。停靠条不得覆盖最后一行图纸，页面底部预留 80px。
- 建筑设计比较入口命名为“对照案例策略”。对照表按结果排序保留列顺序，首列固定共享维度；窄屏只允许表格容器横向滚动，并显示明确的滑动提示，页面本身不得横向溢出。图纸灵感不显示该入口、dock 或 dialog。

### Icons, motion 与响应

个人收藏不是导出：建筑案例与图纸灵感都通过结果页底部选择条明确加入，主页入口进入非弹窗的完整收藏页。页面默认显示建筑收藏，并常驻建筑方案 / 图纸灵感双向切换。建筑模式默认是“问题目录”，目录按案例子问题建立平铺分隔行并显示原研究题目和案例数；点入后只显示当前子问题，不再重复原研究题目。每个项目依次显示去除 ArchDaily 等发布媒体后缀的项目名、逐题 `design_mechanism`、最多三条去重后的 `transfer_strategy`、第一条不含原文/来源审计措辞的适用条件和最多三张识别图；建筑收藏不显示来源入口，只保留删除。宽屏文字/图片采用 `1.05fr / 0.95fr`，≤1180px 单列，首图跨满图片列、后两图并排。图纸灵感收藏的高清入口、原帖来源与删除保持不变。目录、返回和删除在移动端命中区不小于 44px。建筑选择、批量替换、保存后清空、Run 过期后的 durable snapshot 和两类导出数据契约保持不变。

- 统一使用同一套 1.75px 线性图标：行内 16px、控件 20px、主要导航/空状态 24px。32px 及以上只用于说明性空状态；禁止混用实心、表情和线性图标。
- 无文字的图标按钮必须有可访问名称和 tooltip；tooltip 不能替代关键标签。桌面图标按钮视觉框 36px，移动端命中区 44px。
- 快速反馈 120ms、常规状态 160ms、抽屉/浮层 220ms，统一使用 `cubic-bezier(0.22, 1, 0.36, 1)`。只动画 `opacity`、`transform`、颜色和阴影，不动画布局尺寸。
- “开始研究”主动作允许一次 300ms 的点击火花作为任务已启动的轻量反馈；实现改编自 React Bits `ClickSpark`，只在完整指针点击后运行、结束即停，不创建持续动画循环。
- 资料输入按研究场景隔离，不使用混合格式的通用上传框。建筑设计只显示“添加任务书或案例页（可选）”，展开时允许一次 8px→0、220ms 的低位移揭示，并以两列承载项目任务书 PDF 与指定案例/项目 URL。选中 PDF 后仍使用普通“开始研究”：提交后只把按钮短暂改为“正在准备研究…”，后台完成任务书读取和 3/4/6 问题拆解后直接进入既有研究结果流。不得把内部摘要、边界或子问题变成新的启动器页面、modal 或确认步骤；文件名由原生控件显示一次，外层只保留文件数量状态。读取失败使用既有页面错误反馈并保留输入，不提供绕过任务书约束的额外功能面。案例 URL 只提供参考线索。图纸灵感不显示资料区。每个字段以 `aria-describedby` 绑定说明，≤860px 折为单列、输入命中区至少 44px；切换场景清空未提交资料。动效借鉴 React Bits `AnimatedContent` 的状态连续性，但只用原生 CSS，不引入 GSAP/滚动触发依赖。
- 禁止页面入场编排、弹跳、弹性和环境动效。`prefers-reduced-motion: reduce` 下取消位移与缩放，持续时间降至 1ms，并保留即时状态反馈。

## Do's and Don'ts

### Do

- **Do** 先让用户看到保持原比例的建筑图纸，再提供筛选、证据与高级工具。
- **Do** 严格从 4/8 间距、固定字阶、语义色与 z-index 标尺取值；新数值必须先进入全局 token。
- **Do** 在图纸灵感内部让来源入口、图纸和权利边界保持可追踪，并用图标加文字表达状态；建筑结果和建筑收藏只呈现研究结果。
- **Do** 用子问题建立案例章节，以项目档案连接逐字正文证据、条件、机制、迁移策略和边界；图片只作可选预览。
- **Do** 用短风格方向组织图纸灵感，允许用户从宽泛图纸类型开始，再比较线稿、拼贴、渲染与氛围等可见表达。
- **Do** 在 390、640、1024、1440px 验证无页面级横向溢出、完整键盘操作和合理图纸列数。
- **Do** 用骨架、空状态和具体错误文案告诉用户下一步，而不是只显示“暂无数据”或 spinner。
- **Do** 把活力集中在首页启动任务和选择问题的瞬间；进入研究与证据核验后让真实图纸重新成为视觉主角。
- **Do** 用低对比制图网格、注册角与每视图一个裁切线稿组织宽屏空白；所有装饰必须可从建筑制图语汇解释，并在结果页自动降强度。

### Don't

- **Don't** 做聊天优先界面、永久完整仪表盘、指标磁贴、暗色外壳或常驻证据侧栏。
- **Don't** 使用通用圆角 SaaS 卡片、嵌套卡片、装饰性渐变、玻璃拟态、渐变文字或彩色侧条。
- **Don't** 把结果做成相同图纸卡片铺满页面的证据墙，或用单张图纸和一句话代替项目分析。
- **Don't** 把图纸裁成统一 4:3、在每张静态图纸上加重阴影，或让 hover 缩放图像并扰动图纸墙。
- **Don't** 把来源可信度、事实归属、收藏状态和版权/权利状态混成一个分数、一种颜色或一个徽章。
- **Don't** 用全大写等宽小字作为视觉主题，或在按钮、标签和数据中引入展示字体。
- **Don't** 用装饰性动效、非标准表单、定制滚动条或模态框重新发明成熟工具行为。
- **Don't** 使用持续运行的粒子、鼠标尾迹、磁吸按钮、页面入场编排或 React Bits 背景特效干扰输入和图纸阅读。
- **Don't** 随机堆放圆形、光斑、漂浮卡片、伪轴号、伪尺寸或每区不同线稿；静态背景不是第二套研究结果，也不得进入来源与权利区域。
- **Don't** 使用任意间距、任意圆角、999 层级或只靠颜色表达焦点和状态。
