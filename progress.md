# Progress Log

## 2026-07-11

- Read the approved V2.1 plan and confirmed it satisfies the brainstorming design gate.
- Read required skill instructions for persistent planning, TDD, coding restraint, and frontend design.
- Inspected the workspace: empty, no Git repository, no existing implementation.
- Created persistent plan, findings, progress, and design specification files.
- Completed Phase 0: initialized Git and committed the approved design baseline (`39f3a7f`).
- Started Phase 1: M0 test contracts and vertical protocol spike.
- Completed M0-M5 vertical implementation: local API/SQLite workflow, OpenAI/TinEye providers, paired MV3 browser bridge, evidence governance, and responsive reference board.
- Closed the extension security audit findings and added API-side DNS resolution, session-bound permissions, safe capture checks, and sensitive-page exclusion.
- Added deterministic browser inspection with cropped nine-class visual analysis, partial-result preservation, TTL cleanup, and restart recovery.
- Current verification baseline: API 64 tests; extension 103 tests; board 21 tests. API lint/type checks and board lint/type/build are green.
- M6 remains in progress: clean-start smoke, persistent-Chrome E2E, final interaction persistence, and packaged demo evidence.
- Completed the secure relay configuration milestone: hidden PowerShell input, live Responses/web-search capability probe, transactional Windows Credential Manager storage, startup loading for both `gpt-5.5` clients, redacted health reporting, and deterministic Mock fallback.
- Verified the milestone with 79 API tests, 103 extension tests, 21 board tests, Python lint/type checks, PowerShell security-contract tests, dependency checks, and both production builds. The live relay probe remains intentionally pending until the user enters their own Key.
- Fixed the provider command's Windows PowerShell 5.1 encoding/parser failure reported from the real terminal, and added a cross-version parse regression test. The command now reaches its hidden Key prompt under the user's exact invocation.
- Removed the provider command's accidental `pnpm` dependency, verified its Python-only runtime resolution under PowerShell 7, and launched the user's exact command through the hidden Key prompt without entering a credential.
- Confirmed the user's real relay setup completed successfully: the live capability gate passed, the non-secret provider configuration is present, and the Windows credential entry exists. The API was not running during verification and will load the relay on its next start.
- Resumed M6 acceptance after interruption: restored the Board to 23/23 tests, zero lint/type errors and a successful production build; added complete styles for workspace creation, cancel/retry, errors, partial coverage, loading/empty states, missing previews and evidence locators.
- Started the real local API and Board on ports 8000/5173 and captured 1440/1024/390 screenshots with system Chrome. The layout has no page-level horizontal overflow; missing Demo drawing assets are the remaining visual blocker.
- Closed M5 Board acceptance: added two inspected CC0 drawings and five deterministic project-owned replay assets, truthful source/rights metadata, a local source manifest, full-image previews, responsive two/single-column layouts and automatic first-result inspection. Final 1440/1024/390 screenshots have zero console errors and no document-level overflow; Board remains green at 23 tests plus lint/type/build.
- Resumed M6 from the persisted plan after interruption. Closed the live-run restoration boundary: legacy asset descriptions now normalize to the nine-class enum, API results expose whether a local crop exists, the Board avoids nonexistent crop URLs, and terminal runs display their real status. API is green at 102 tests; Board is green at 25 tests plus lint/type checks.
- Expanded the final delivery gate so one command also validates PowerShell security/process contracts, deterministic evaluation fixtures and the packaged Chrome extension E2E. README now uses PowerShell 7 and links the architecture, failure cases, three demo flows and versioned evaluation datasets.
- Started the last browser-loop acceptance item: a deterministic full-stack replay using the real FastAPI WebSocket, packaged MV3 extension, fixed page inspection and persisted crop delivery. This remains in progress until its E2E passes.
- Closed the browser-loop acceptance item: a separate persistent Chrome profile pairs to a real temporary FastAPI server, inspects a fixed architecture page, captures a PNG, persists it to SQLite/workspace storage, serves it through the content API and revokes host access at terminal state. Uvicorn now declares its WebSocket runtime dependency.
- Completed the repository-wide delivery gate. `scripts/verify.ps1` passes 105 API tests, 25 Board tests, 111 extension tests, 6 packaged Chrome E2E cases, Python/TypeScript lint and type checks, both production builds, PowerShell security/process tests, and deterministic validation of 30 research tasks plus 108 classification samples.
- Added a versioned gpt-5.5 live smoke report with explicit source-recall results, duplicate/coverage counts, historical time-budget observation and the browser-unavailable limitation. No credential or private page data is recorded.
- Restarted the real local services after verification. API health reports the stored 梭子蟹 provider with `gpt-5.5`; the Board is available on port 5173 and historical legacy assets now restore through the normalized enum without nonexistent crop requests.
- Started M7 usability simplification after first-user screenshot feedback. The accepted direction preserves all research/evidence capabilities but changes the default interface to one primary prompt-and-results workflow with progressive disclosure for workspaces, evidence, comparison, StyleProfile, exports and Trace.
- Completed M7 usability simplification: removed permanent workspace/evidence/stage rails, reduced the result controls to one asset-type select, converted evidence and Trace to drawers, collapsed advanced research inputs, and exposed compare/export/style actions only when relevant. Cancel/retry remain directly available from the compact run status.
- Accepted the simplified Board at 1440/1024/390 px with four/three/one-column grids, no horizontal overflow, a full-width mobile evidence drawer, one non-duplicated analysis-diagram filter, and zero browser console warnings/errors.
- Re-ran the repository-wide delivery gate after the redesign: 105 API tests, 26 Board tests, 111 extension tests, 6 packaged Chrome E2E cases, all lint/type/build checks, PowerShell safety/process tests, and all 30/108 evaluation fixtures pass.

## 2026-07-12

- Recovered the local runtime after the user saw `ERR_CONNECTION_REFUSED`: confirmed both ports were inactive despite a stale launcher state file, restarted through `scripts/start.ps1`, and verified Board HTTP 200 plus API health on the stored 梭子蟹 `gpt-5.5` provider.
- Started M8 after the user rejected the simplified Board as visually rigid. The new goal is a mature architectural asset-browser language informed by proven collection and research products, while preserving the completed evidence workflow and progressive disclosure.

## 2026-07-13

- Implemented the M8 digital pin-up wall: a compact branded shell, command-like research composer, ordered responsive drawing wall, intrinsic image ratios, tier-specific evidence pins, Lucide action language, contextual modal drawers and mobile 44px targets.
- Added root `DESIGN.md` as the single global UI authority and removed the obsolete Board-local design file. The system now defines semantic colors, type/line-height tiers, spacing, radii, elevation, z-index, icon, motion and breakpoint rules.
- Completed M8 interaction hardening: evidence levels now have distinct icons/colors, saved/rejected states do not reuse evidence semantics, dialogs trap focus, Escape closes overlays, trigger focus returns, first-screen images load eagerly and later images remain lazy.
- Accepted M8 at 1440/1024/390 px with four/three/one ordered columns, no horizontal overflow, zero console warnings/errors and zero design-system detector findings.
- Re-ran the full delivery gate after the redesign: 105 API tests, 26 Board tests, 111 extension tests, 6 packaged Chrome E2E cases, lint/type/build, PowerShell contracts and all evaluation fixtures pass.
- Completed M9 problem-first navigation: the default homepage remains on the design-question composer after completed-run hydration, previous results require an explicit transition, active runs still resume, and the result page returns through “发起新研究”. Board verification is green at 27 tests plus lint/type/build; live `/` acceptance confirmed both directions with zero console warnings/errors.
- Started M10 after the problem-first homepage proved too empty in first-user review. The bounded audit keeps the composer primary but adds capability discovery, varied architecture problem starters and compact recent-work recall instead of restoring the full result wall.
- Completed M10 as a useful research-workbench home informed by Perplexity, NotebookLM, Figma, Notion and Linear: visible research paths, architecture problem starters and lazy-loaded recent runs now fill the home without exposing result cards. Desktop/mid/mobile browser acceptance and the complete offline repository gate pass.
- Closed M10 hardening: mode-specific prompts, semantically accurate starter actions, 700px reflow and global control/icon geometry now match `DESIGN.md`; request-scoped hydration/submission/polling/cancellation/retry prevents stale results, late errors and late terminal transitions across new runs and workspace switches. The final gate passes 105 API, 35 Board, 111 extension and 6 packaged-browser tests.
- Started M11 after the user accepted the homepage logic but rejected its timid visual treatment. The bounded direction keeps all product behavior and makes one blueprint task surface, stronger hierarchy and friendlier starter interactions the visual signature, informed by Milanote, FigJam and Are.na rather than generic SaaS styling.
- Added React Bits to the M11 reference set at the user's request. The mature-product audit maps a restrained, optimized `ClickSpark` to the single launch action; persistent backgrounds, magnetic controls and orchestrated entrance effects are excluded because they weaken task clarity.
- Completed M11 after re-running the motion study in product-first order. The homepage now uses a blueprint task island, one marker-yellow problem note, stronger global type/spacing/elevation rules, a 300ms React Bits-derived launch spark and a native 220ms settings reveal; all ambient, magnetic and marketing-style effects remain excluded.
- M11 acceptance passes exact Chromium viewports at 1440/1024/700/390 with no horizontal overflow, reduced-motion behavior, zero Impeccable findings and the full repository gate: 105 API tests, 37 Board tests, 111 extension tests and 6 packaged-browser E2E cases.
- Started M12 after the ultra-wide user screenshot showed that the M11 identity remained a centered colored form rather than a global visual system. The new milestone extends an architectural drafting canvas, registration marks and diagram fragments across every Board route, while widening the working composition without changing product logic.
- Completed M12 global studio canvas: widened the shell/stage to 1760/1600px, mounted one shared route-aware backdrop, added a static 128px drafting grid plus one cropped plan/section fragment, and encoded the responsive/background rules in `DESIGN.md` and `PRODUCT.md`.
- Accepted M12 at 2048/1440/1024/700/390px with exact viewport widths, no page overflow, wide-only line fragments, compact-screen decoration reduction and correct modal stacking. The design detector reports zero findings.
- Closed the tablet touch-target follow-up with a versioned design-system contract: at `≤860px`, header and card actions are at least 44px and remain visible for touch input without changing desktop density.
- Re-ran the complete delivery gate after M12: 105 API tests, 39 Board tests, 111 extension tests, 6 packaged Chrome E2E cases, lint/type/build, PowerShell contracts and all 30/108 evaluation fixtures pass.
- Completed M13 prompt proportion correction: increased the main problem input to 152px on desktop and 132px on tablet while preserving the 108px mobile height and user-controlled vertical resize.
- Accepted the updated homepage at 2048/1440/1024/700/390px with no page overflow and zero design-detector findings. Board verification passes 40 tests, lint, typecheck and production build.
- Completed M14 result comprehension: the result page now exposes the exact live or demo task, derives three distinct evidence-backed method directions before the drawing wall, and labels cards as supporting evidence with explicit problem match.
- Replaced side-by-side cards with a method comparison matrix covering problem focus, transferable method, visible observation, evidence status and application boundary. Browser acceptance passes at 1440 and 390px with local-only table scrolling; the complete repository gate passes 105 API tests, 40 Board tests, 111 extension tests, 6 packaged-browser E2E cases and all lint/type/build/evaluation checks.
- Started M15 after user review found that the research remained asset-level and too shallow. The approved direction decomposes each task into researchable subquestions, gathers project/asset evidence per subquestion, and presents multi-image project dossiers with explicit mechanism, transfer and boundary analysis.
- Resumed M15 from its persisted failing-test baseline. Split implementation into non-overlapping persistence, provider, workflow and Board dossier workstreams; the acceptance target remains one task-scoped research run with no static case library or cross-workspace index.
- Completed the M15 backend depth loop: persisted 3/4/6-question plans, subquestion-aware multilingual queries, rich project analysis fields, association-aware deduplication, mode-specific coverage/stop conditions and legacy-safe Alembic migration. The API suite, Ruff and Mypy are green.
- Raised the bounded browser inspection yield from three to six media regions per page with red-first tests; all crop privacy, size, hash and deduplication contracts remain intact.
- Closed the M15 correctness hardening as one milestone: retries resume incomplete query attempts without replaying completed work; coverage requires displayable, sufficiently evidenced and drawing-diverse assets; per-subquestion analysis survives duplicate association; and exact provider `facts` are the only fields promoted to formal evidence claims.
- Completed the deep-result Board: each task opens into a persisted subquestion map, case chapters and multi-image project dossiers with project conditions, spatial mechanism, transfer operations and application boundaries. Opening an asset preserves the active subquestion's analysis in the evidence drawer instead of falling back to one generic card caption.
- Accepted M15 at 1440px and 390px across the homepage, result chapters, dossiers and evidence drawer with no document-level horizontal overflow. Targeted gates pass 116 API tests with Ruff/Mypy, 41 Board tests with lint/type/build, and 111 extension tests plus 6 packaged-Chrome E2E cases.
- Completed the M15 repository-wide delivery gate: `scripts/verify.ps1` exits 0 with `All ArchResearch checks passed.`, including every Python/TypeScript lint, type and build check, PowerShell contract, packaged-browser E2E and all 30/108 evaluation fixtures. Implementation and verification are complete; the milestone commit remains with the main agent.
- Reopened M15 after the final boundary audit found composition gaps that isolated unit tests missed: unsupported context, non-precedent coverage, stale retry generations, duplicate-source provenance, orphan crops and in-memory-only traffic budgets.
- Closed the audited backend boundaries with red-first tests. The post-audit API baseline is 132 tests plus Ruff/Mypy; query generations and durable browser budgets survive retries/restarts, live results remain capped at four, and stronger duplicate sources now reach evidence persistence without extra image storage. The final repository-wide gate and milestone commit remain.
- Completed the final M15 retention regressions: provider/browser/provider enrichment preserves the full subquestion dossier instead of overwriting it, and default relative crop paths survive orphan cleanup. The focused tests were written red first and now pass.
- Closed M15 with the authoritative repository gate: 137 API tests, 41 Board tests, 111 Extension tests, 6 packaged-Chrome E2E cases, all lint/type/build and PowerShell contracts, plus the 30-task/108-sample evaluation fixtures pass. No live OpenAI or TinEye request is part of this gate.
- Started M16 from a real-result screenshot showing two user-visible failures: the selected project dossier had only an unavailable preview, and provider-authored context/mechanism/transfer/boundary text remained in English. The fix must address acquisition/state and Chinese output contracts rather than cosmetically hiding either failure.
- Implemented the M16 correction path red-first: Chinese is required for live planning and analysis, browser connection/pairing is visible before research, completed no-image runs can launch a fresh run after connection, and same-source/same-type crops enrich the existing project dossier. Historical no-image cards now explain the exact Trace-backed cause in Chinese and link to the original page.
- Pre-close targeted verification passed 44 Board tests plus lint/type/build and 63 browser/provider/workflow API tests plus Ruff/Mypy before the final repository and live-page gates.

## 2026-07-14

- Closed M16 with the authoritative repository gate: 141 API tests, 45 Board tests, 111 Extension tests, 6 packaged-Chrome E2E cases, all Python/TypeScript lint, type and production-build checks, PowerShell contracts and all 30/108 evaluation fixtures pass without live OpenAI or TinEye calls.
- Accepted the real result recovery UI in Chromium at 1440x1000 and 390x844 with no page-level horizontal overflow. Historical English analysis is identified instead of silently translated, all eight no-image results retain their original source links, and disconnected-extension recovery remains actionable.
- Final static checks pass: `git diff --check` reports no patch errors and a high-confidence scan of all 13 changed files finds zero credential-pattern matches.
- Restarted the local development services. Board is HTTP 200 at `http://127.0.0.1:5173`; API health is `ok` on `http://127.0.0.1:8000` with the stored 梭子蟹 API / `gpt-5.5` configuration; `/v1/browser/status` truthfully reports `connected: false` until the ArchResearch extension is paired.
- Started M17 after the next user screenshot showed the Board shell but a local-service connection error. Reproduction found stale recorded PIDs and no listeners; a retained PowerShell 7 session now keeps Board/API online while extension onboarding is completed.
- Evaluated Firecrawl from its current official repository and documentation. It is retained only as a future optional public-page parser because its cloud API is useful for fresh structured web context, while its self-host stack and AGPL server do not fit the approved no-Docker/no-Redis/no-PostgreSQL V2.1 runtime and it cannot use the user's Chrome login state.
- Chose the M17 implementation boundary: an exact loopback-only Board bridge automates one-time pairing, the existing research action requests temporary web access, and WebSocket authentication failure becomes a recoverable explicit state instead of an infinite reconnect loop.
- Established the M17 red baseline. Focused Board/Extension tests fail only because the loopback bridge modules and Manifest entry are absent, the socket reports connected before authentication, and a 1008 authentication close still becomes a retrying disconnected state.
- Completed M17 one-click onboarding: the Board securely hands a one-time code to the installed extension on loopback, persisted rotated credentials reconnect automatically, and starting or rerunning research requests the temporary page-reading permission without exposing a general browser bridge.
- Localized the extension popup and side panel, retained manual pairing as a recovery path, and stopped clearing the code after a failed attempt. Authentication rejection now stays as an explicit error instead of creating an infinite reconnect loop.
- Closed a packaged-only Manifest V3 defect found by the new end-to-end case: the loopback content script now builds as one classic script with no module import. All six persistent-Chrome cases pass, including Board-originated one-click pairing, real page crop persistence and terminal permission revocation.
- Closed M17 with the authoritative repository gate: 141 API tests, 48 Board tests, 118 Extension tests, 6 packaged-Chrome E2E cases, all Python/TypeScript lint, type and production builds, PowerShell contracts and the 30-task/108-sample fixtures pass without live provider traffic.
- Started M18 to evaluate current open-source browser-agent projects on GitHub and integrate only capabilities that strengthen public-web parsing, deterministic page inspection, recovery and evidence capture without adding a second autonomous-agent runtime or prohibited infrastructure.
- Completed the M18 primary-source audit across Firecrawl, Browser Use, Stagehand, Playwright MCP, Crawl4AI, Browsertrix Crawler, Steel Browser and Lightpanda. Selected an optional Firecrawl failure parser plus one bounded semantic page-snapshot action; rejected all duplicate browser/server runtimes.
- Established the M18 red baseline: API collection fails only because the new public-page module is absent, while Extension tests fail only because `page_snapshot` is not yet in the exact protocol/executor/content implementation. Firecrawl request/security/fallback and ambiguous-enrichment tests are in place before production code.
- Implemented the bounded M18 core. Firecrawl v2 is called with fresh `markdown/links/images` formats only after Chrome inspection fails; outputs are public-URL filtered and capped, and only one unambiguous same-source/same-type unresolved asset receives a remote preview. The extension now reads a 40-block/6000-character visible semantic snapshot before media enumeration and degrades to metadata if that observation fails.
- Focused verification is green: 27 API parser/config/workflow tests and all 121 Extension unit/protocol/content/executor tests pass. No live Firecrawl request or key was used.
- Added a separate secure Firecrawl setup path: hidden PowerShell input is passed only through stdin, zeroed after use, and stored transactionally in Windows Credential Manager with a non-secret `firecrawl.json`. Nine credential/app tests and the PowerShell security contract pass without a network probe or live key.
- Closed M18 with the authoritative repository gate: 157 API tests, 48 Board tests, 121 Extension tests, 6 packaged-Chrome E2E cases, all Python/TypeScript lint, type and production builds, PowerShell contracts and the 30-task/108-sample fixtures pass. The gate makes no live OpenAI, TinEye or Firecrawl call.
- The retained architecture is intentionally hybrid: the installed Chrome extension remains the only path for logged-in pages and precise crops, while Firecrawl is an optional public-page recovery path. Remote output cannot promote evidence, rights or verification tiers and cannot overwrite ambiguous candidates.
- Final static checks pass: `git diff --check` is clean and a quoted-literal scan across all 52 changed/untracked files finds no embedded OpenAI or Firecrawl credential. The broader first scan's ten hits were verified as runtime variable flow and Windows Credential Manager reads, not literal values.
- Restarted the completed M18 build. Board is HTTP 200 at `http://127.0.0.1:5173`; API health is `ok` at `http://127.0.0.1:8000` on the stored 梭子蟹 API / `gpt-5.5` configuration. Browser status is disconnected until Chrome reloads and reconnects the newly built extension.
- Started M19 after the user rejected fallback-only integration. Inspection found that Firecrawl currently runs only after a browser exception and the semantic page snapshot is collected but not used by visual classification; both must become normal-path research inputs.
- Corrected the first M19 inspection note after reading the complete capture function: semantic snapshots already enter the visual classifier's bounded project context. The remaining implementation gap is Firecrawl's fallback-only invocation and preview-only persistence.
- Defined the direct M19 behavior before coding: configured Firecrawl parses normal deduplicated sources within the mode page cap, its bounded Markdown augments Chrome visual classification, exact typed leads enrich one provider candidate when safe, and remaining typed URLs persist only as low-ranked unverified visual leads.
- Established the M19 red baseline with two behavior tests. Normal successful Chrome research does not call/persist Firecrawl output, and ambiguous typed Firecrawl images are discarded instead of becoming unverified visual leads; both failures match the requested product correction.
- Implemented the M19 normal path and turned the two red tests green. Each configured public source is parsed once within the selected mode's page cap before Chrome inspection; bounded parsed text augments visual classification, exact leads enrich existing assets, and otherwise typed public images persist as low-ranked unverified leads.
- Focused M19 verification passes: Ruff, canonical formatting, Mypy and 27 public-page/browser-inspection tests are green. Documentation now describes Firecrawl as a normal public-page enhancement rather than a Chrome failure fallback.
- Added and passed a fourth focused Firecrawl regression: even with no browser connection, a clearly labelled public floor-plan image becomes an unverified visual lead. Duplicate suppression remains source-scoped so a second source can still contribute provenance.
- Closed M19 with the authoritative repository gate: 159 API tests, 48 Board tests, 121 Extension tests, 6 packaged-Chrome E2E cases, all Python/TypeScript lint, type and production builds, PowerShell contracts and the 30-task/108-sample fixtures pass. No live OpenAI, TinEye or Firecrawl request is part of this gate.
- Post-gate diff review is clean on scope and security but identified one stronger-evidence interaction for focused verification: a Firecrawl preview attached before Chrome inspection must not block the same provider candidate from adopting a later local crop.
- Added a red regression for the stronger-evidence interaction. It fails because the early Firecrawl URL occupies the partial provider candidate before Chrome can attach its local crop, confirming that parsing must happen before classification but lead persistence must happen after browser inspection.
- Fixed the stronger-evidence ordering and turned all four Firecrawl tests green: parse before Chrome to supply text context, persist Chrome crops first, then add remaining public image leads. A local crop now enriches the provider dossier while the distinct Firecrawl URL remains an unverified lead.
- The post-ordering full gate reached 159 passing API tests, then Mypy rejected three optional browser/classifier/path arguments because the boolean `can_inspect` helper did not narrow their types. Added explicit assertions inside that verified guard; the full gate must be rerun.
- The authoritative post-fix gate now exits 0: 159 API tests, 48 Board tests, 121 Extension tests and 6 packaged-Chrome E2E cases pass together with every lint, type, build, PowerShell and 30/108 evaluation check. Stronger local crops retain priority over Firecrawl leads.
- Restarted the M19 build successfully. Board returns HTTP 200 at `http://127.0.0.1:5173`; API health is `ok` on the stored 梭子蟹 API / `gpt-5.5` configuration. Browser status remains disconnected until the user's installed Chrome extension reconnects.
- Opened the secure Firecrawl setup window and detected a stored configuration, but the first bounded live scrape of `example.com` returned HTTP 401. The stored value is not an authorized Firecrawl credential; setup must be repeated with a separate Firecrawl-issued Key.
- Repeated hidden setup with the user's Firecrawl-issued Key and completed a successful bounded live scrape of `example.com` (`Example Domain`, 167 Markdown characters, one link, no images). Restarted Board/API so the normal research path now loads the valid credential; no Key or page body was printed or written to project files.
- Started M20 live acceptance with the user's authorization to continue. API is healthy on 梭子蟹 `gpt-5.5`, Firecrawl was live-validated, one workspace exists, and the Chrome extension is currently disconnected; the Quick run must therefore prove public-page enrichment honestly without claiming local crops.
- Selected the existing old-industrial-renewal workspace and confirmed the Quick contract (2 rounds / 4 queries / 12 pages / 240 seconds). The live prompt will test program insertion, visitor/service circulation and sectional hierarchy together.
- Created live Quick run `ed147b57-a2ac-400c-8685-80133c4c2071`. Real planning completed with three Chinese subquestions; the first search remains active after about 90 seconds, with one provisional result and no Firecrawl event because candidate sources have not yet returned from the provider.
- M20 live run ended `failed` at the first search with `provider_error:APITimeoutError` after real Chinese planning. No Firecrawl event occurred, proving that fallback/page-only integration still depends on model source discovery. Opened the next corrective scope: direct Firecrawl Search source recall with partial preservation when model analysis times out.
- Confirmed the failed run has zero results and only three SSE Trace records. Started M21 from the official Firecrawl `/v2/search` contract: bounded web URL discovery will run before model analysis, and a model timeout must degrade to Firecrawl-sourced partial results instead of failing empty.
- Established the M21 red baseline. Focused collection fails because `PublicSearchLead` and the Firecrawl Search contract do not yet exist; request-shape, public-URL filtering and model-timeout partial-preservation tests are now in place before production code.
- Implemented the bounded Firecrawl Search client and degraded provider path. The request-shape test passes; the workflow now retains the typed plan lead and Trace, but terminal status is `blocked` rather than the required `partial` because low-ranked visual leads are outside coverage. A focused terminal-status correction remains.
- Completed the focused M21 behavior: a model search timeout now retains Firecrawl source pages and typed image leads, records separate `firecrawl_search` and degraded-model Trace events, and returns `partial` with `unverified_visual_leads` instead of an empty failure or misleading completion. Both red tests are green.
- Full API verification is green after canonical formatting: Ruff, Mypy and 161 API tests pass. The next acceptance step is a service restart followed by the same live Quick architecture question.
- Restarted the new build and retried M20 run `ed147b57-a2ac-400c-8685-80133c4c2071` as attempt 1. A real `firecrawl_search` Trace arrived before model completion; after roughly 75 seconds the model search is still active, but source discovery is no longer empty or dependent on it.
- M20 retry reached `partial` with seven Firecrawl image leads and no local crops. Result inspection found only one ArchDaily roundup source, two underlying images repeated across delivery sizes, one malformed parenthesis-truncated URL and no plan/section. M21 recovery works, but live usefulness requires remote-image normalization and stronger source selection.
- Added two M21 boundary regressions before production changes. Both fail for the intended reasons: Firecrawl parsing retains one parenthesis-truncated Markdown URL plus three delivery-size variants, and the workflow stops as `blocked` before a 30-second Firecrawl search when the 120-second model reserve no longer fits. The next change is limited to URL/image normalization and independent per-tool deadline admission.
- Turned both M21 regressions green. Public-page parsing rejects unmatched-parenthesis URLs and collapses known delivery variants while retaining the highest-quality image; workflow deadline admission now evaluates Firecrawl and model reserves independently, so a bounded public search can preserve a visual lead even when the slower model call is skipped.
- The first complete API gate reached 163 passing tests with Ruff lint and Mypy green, but Ruff format requested canonical layout in the touched workflow and focused test. This run is not accepted as final; only those two files will be formatted and the gate restarted.
- Restarted and passed the authoritative API gate after formatting only the two touched files: Ruff lint, Ruff format, Mypy and all 163 API tests are green. The remaining M21 acceptance is repository-wide verification plus a live Quick retry against the configured Firecrawl and gpt-5.5 services.
- Passed the authoritative repository gate with `All ArchResearch checks passed.`: 163 API tests, 48 Board tests, 121 Extension tests, 6 packaged-Chrome E2E cases, every lint/type/build and PowerShell contract, plus all 30/108 evaluation fixtures. The gate used mocks and made no live provider calls.
- Restarted the verified build and completed M20 attempt 2. It ended `partial/unverified_visual_leads`; Trace shows a second Firecrawl search and one model skip for insufficient reserve, proving the new fallback continues after the first slow model batch. The result endpoint still exposes seven pre-fix assets because retry preserves partial history, so a fresh run—not destructive cleanup—will be used for normalization acceptance.
- Completed clean M21 live acceptance with fresh run `eef07a27-7cd5-455b-ac21-fb5861dcc318`: all four Firecrawl searches completed, one timed-out model call degraded and three later model calls were skipped by reserve, and the run returned two unique `medium_jpg` visual leads with zero malformed URLs. M21 behavior is verified; M20 remains open because the leads still cover only one source/project and contain no plan or section.
- Started M22 with a failing workflow contract: Firecrawl still receives the model-oriented string beginning with `主问题：` instead of a concise `建筑项目图纸：` query. The regression also forbids the untrusted workspace-context suffix from entering public search input.
- Turned the M22 query contract green and passed the full API gate: Firecrawl now receives only the active subquestion plus goal/language-specific drawing terms, while the model retains the rich prompt; Ruff, format, Mypy and all 163 API tests pass with no additional provider calls.
- Passed the M22 repository-wide gate with `All ArchResearch checks passed.`: 163 API, 48 Board, 121 Extension and 6 packaged-Chrome E2E tests plus all lint/type/build/security/evaluation contracts. The remaining acceptance is a fresh live Quick comparison of source and drawing diversity.
- Completed the M22 live comparison run. Concise public-search queries raise clean output from 2 to 8 images, source pages from 1 to 2 and add the first section result, while preserving the real timeout/skip recovery. A cross-CDN delivery duplicate remains and the two source titles are roundup pages rather than verified projects, so M20 remains open.
- Added a red regression for the live `Kimo_02.jpg` duplicate. It confirms that identical `/media/images/...` identities delivered through different CDN hosts and slideshow/newsletter paths are currently treated as two assets; the fix will normalize only this shared media-path pattern, not filenames globally.
- Closed M22: shared `/media/images/...` identities now collapse across known delivery hosts/variants, documentation describes direct query recovery and evidence limits, and the authoritative gate passes 164 API, 48 Board, 121 Extension and 6 packaged-Chrome E2E tests with all lint/type/build/security/evaluation checks.
- Final M21/M22 static audit is clean: `git diff --check` reports no patch errors and all nine changed files contain zero high-confidence OpenAI/Firecrawl credential literals. Windows line-ending notices are repository conversion warnings, not content defects.
- Started M24 from the user's active-run screenshot and a live health check. Reproduction evidence is specific: the run shell says `正在搜索` while the main canvas renders the generic empty composer, and browser status is disconnected because the Board is open outside the Chrome extension environment. The fix will separate public research from optional Chrome enhancement and make both states explicit.
- Established the M24 red baseline with three focused Board failures: disconnected copy still says text-only despite Firecrawl images, the connect action still claims one-click and creates a pairing code before bridge discovery, and an active zero-result run still renders the generic `从一个具体设计问题开始` empty state instead of its submitted task/stage.
- Implemented the focused M24 behavior and turned all 48 Board tests green. A direct in-app-browser acceptance attempt hit the known host runtime error `Cannot redefine property: process` for the third time; per the recorded recovery boundary, further validation moves to system Chromium and packaged-extension E2E instead of repeating that failing control path.
- Closed M24. Active zero-result runs now preserve the submitted question, show the current research-stage explanation and list the decomposed evidence questions instead of reverting to the generic new-task empty state. Mobile layout collapses the same seven-stage sequence without changing information hierarchy.
- Browser onboarding now probes the Board bridge before creating a pairing code. Unsupported pages receive a precise Chinese explanation and do not rotate one-time credentials; copy makes clear that public Firecrawl research continues without the extension and Chrome adds logged-in page access plus precise crops.
- The authoritative repository gate exits 0: 164 API tests, 48 Board tests, 121 Extension tests and 6 packaged-Chrome E2E cases pass with all lint, type, production-build, PowerShell security and 30/108 evaluation checks. The Chrome suite covers Board-originated pairing, permission lifecycle and a real FastAPI crop.
- Restarted the verified build. Board returns HTTP 200 at `http://127.0.0.1:5173`; API health is `ok` on the stored 梭子蟹 API / `gpt-5.5` configuration. `/v1/browser/status` is correctly disconnected until the page is opened in Chrome with the ArchResearch extension loaded.
- Final M24 static checks are clean: `git diff --check` reports no patch errors, the design detector reports zero findings and the touched product/planning files contain no high-confidence OpenAI or Firecrawl credential literals.
- Started M25 from the follow-up screenshot. The user can see public Firecrawl/search images while the Board still presents “图纸提取未连接” as a prominent warning; the next correction will separate the working public-image mode from the optional Chrome enhancement and explain why both states can coexist.
- Inspected the three current live runs: they contain 8/2/7 remote image URLs and zero local browser crops. This confirms the visible images are valid public-web previews and establishes the exact provenance copy for the M25 UI.
- Established the M25 red baseline. Two API tests fail only because the fixed local Chrome launcher endpoint does not exist; five Board behaviors fail only because the old binary warning/click flow remains, automatic Chrome-page pairing is absent, remote previews are unlabeled and remote URLs still take priority over an available local crop.
- Implemented the M25 capability split and one-click handoff. The Board now treats public image research as available, labels each preview as `公开网页图片` or `Chrome 精确裁图`, prefers local crop content when present, and opens only the fixed loopback Board URL in installed Chrome when the current surface cannot host the extension. The Chrome page removes the handoff flag and pairs automatically without another code-entry step.
- Focused verification is green: 19 browser API tests and 51 Board tests pass with Ruff, Mypy, ESLint, TypeScript and the Board production build. No live provider or Firecrawl request was made.
- The authoritative M25 repository gate exits 0: 166 API tests, 51 Board tests, 121 Extension tests and 6 packaged-Chrome E2E cases pass with every lint, type, production-build, PowerShell security and 30/108 evaluation check.
- Restarted the verified build and called the fixed local Chrome launcher. Chrome opened successfully but initially remained disconnected, proving the installed/unpacked extension state—not the launcher—was stale. After the user reloaded the extension, five consecutive `/v1/browser/status` samples returned `connected: true`.
- Reopened M25 after the same real-profile connection later returned to `connected: false`; the earlier five-sample check was shorter than Chrome's MV3 idle window and is not sufficient acceptance evidence.
- Added a strict heartbeat contract: after authentication the extension sends `browser.heartbeat` every 20 seconds and the API returns `browser.heartbeat_ack`; the timer is cleared on close/disconnect and all other messages remain schema-restricted. The extension now declares Chrome 116 as its minimum supported version.
- Completed the post-heartbeat repository gate: 167 API tests, 51 Board tests, 122 Extension tests, 6 packaged-Chrome E2E cases, every lint/type/build and PowerShell contract, and all 30/108 evaluation fixtures pass without live provider traffic. Board/API were restarted; final M25 acceptance requires reloading the newly rebuilt unpacked extension and observing the real profile for longer than 30 seconds.
- Closed M25 with real-profile acceptance after the user reloaded the rebuilt extension. Ten API status samples from 19:24:33 through 19:25:18 all returned `connected: true`, crossing the 30-second MV3 idle window and confirming that the 20-second heartbeat prevents the observed automatic disconnect.
- Started M26 after the user asked why all recent runs time out and requested performance optimization. The working hypothesis is not local CPU slowness: the real Quick Trace shows a single relay-backed OpenAI web-search request consuming about 121 seconds of a 240-second run, after which later model batches are correctly skipped while Firecrawl and Chrome/public inspection continue. M26 will measure the provider client and workflow before changing timeouts or concurrency.
- Established the M26 red baseline and turned it green. The OpenAI-compatible client now limits a call to 30 seconds with no hidden SDK retry, and one timeout opens a run-scoped model-search circuit while later Firecrawl searches continue. Focused provider/startup/deadline/circuit tests pass; full API and repository gates remain.
- Completed comparable live Quick run `0ecc04c6-b8b1-4145-a01b-e6f3b2a4cb3e`. Planning finished in about 11 seconds, Firecrawl Search returned the first four leads in about 2 seconds, the model search stopped at its new 30-second boundary, and three later model batches were skipped with `previous_timeout` while public research continued. Total runtime was about 143 seconds versus about 194 seconds for the prior comparable run.
- The live Trace exposed one remaining 30-second Firecrawl PDF scrape. Added a red-first 20-second server/client timeout contract for both Firecrawl Search and Scrape; focused parser tests are green. This is a deterministic cap rather than a claim of another measured live speedup, so no second paid comparison run was issued.
- Closed M26 with the authoritative gate: 168 API tests, 51 Board tests, 122 Extension tests, 6 packaged-Chrome E2E cases, all lint/type/build and PowerShell contracts, and all 30/108 evaluation fixtures pass.
- Recorded the post-M26 delivery sequence without starting implementation: first close M20 through one Board-started Balanced run with real temporary Chrome permission; then complete the existing M23 bounded remote visual classifier; next add M27 bounded project-page expansion/evidence promotion; finally make partial causes visible and pass the M28 live usefulness gate.
- Started M29 from the user's three-branch result screenshot. Reproduction identified a structural scheduling bias rather than a display-only defect: Quick has three planned subquestions but only four static queries, and the fourth always belongs to the first branch; the global two-empty-batch stop is not fair per branch. Recorded semantic depth and fair branch coverage as the next implementation milestone before changing code.
- Established M29 red tests across schema, workflow and Board: semantic depth budgets were absent, an empty Quick run stopped after only the first two branches, and the UI exposed opaque English mode names with no research commitment. Implemented fair 6/12/24 query ceilings, complete-round no-progress stopping, per-question evidence targets, persisted pass counts and Chinese depth descriptions. Focused verification now passes 41 schema/workflow tests and all 52 Board tests, including retry/resume and zero-result branch visibility.
- Closed M29 with the authoritative repository gate: 170 API tests, 52 Board tests, 122 Extension tests and 6 packaged-Chrome E2E cases pass with lint, formatting, Mypy, TypeScript, production builds, PowerShell security contracts and 30/108 evaluation fixtures. The final red test also proves selected analysis obligations enter model research queries rather than remaining display-only. The design detector reports zero findings, credential scan and `git diff --check` are clean; the known in-app preview host error was not retried after its recorded failure boundary.
- Started M30 from the user's tool-menu screenshot. The selected direction keeps the existing five capabilities but renames the entry and actions in user language, groups them by compare/organize, export and process review, and makes disabled prerequisites visible inside each row instead of relying on gray text or hover-only explanations.
- Established the M30 red behavior first: the old menu lacked the “结果工具” entry, purpose groups, outcome descriptions, visible prerequisites and a plain-language research-process drawer. Implemented the grouped popover and turned all 53 Board tests green; ESLint, TypeScript, production build and the local design detector also pass with zero findings.
- Closed M30 with the authoritative repository gate: 170 API tests, 53 Board tests, 122 Extension tests and 6 packaged-Chrome E2E cases pass with every lint, type, format, build, PowerShell security and 30/108 evaluation check. Existing compare, style and export actions remain unchanged behind the clearer labels and prerequisites.
- Started M31 from the follow-up hierarchy feedback. Scope is deliberately visual and semantic: promote the existing result-use trigger to the filled primary action, preview “对照、规范、导出” on the closed control, and demote “发起新研究” without changing any tool workflow.
- M31 red-test setup had two bounded harness issues: the root recursive test command forwarded a Board-relative path to every package, so it found no tests; the corrected Board-local run then exposed that jsdom does not publish native `<summary>` as a button role. The implementation now makes that interactive role explicit while preserving the native details behavior.
- Closed M31 after promoting result use above task restart without changing the underlying menu actions. The focused behavior test, all 53 Board tests, ESLint, TypeScript, production build and the design detector pass; the authoritative repository gate also passes 170 API, 53 Board, 122 Extension and 6 packaged-Chrome E2E tests.
- Started M32 after visual review of the live result header. The revised scope removes the hidden-menu dependency entirely: the result canvas will contain one persistent workbench with direct compare/style/export/process actions, while the app header returns to navigation and task restart only.
- M32 behavior-first test correctly failed against the old header menu. The first green attempt exposed a narrow landmark-label mismatch—the new region inherited its headline as the accessible name—so the workbench now carries the stable task label “结果工作台” while retaining the visible outcome-led headline.
- M32 implementation is green in focused and full Board verification: the header dropdown is gone, the persistent workbench exposes all five actions directly, and 1440×1000 plus 390×844 Playwright captures confirm the intended hierarchy and responsive stacking without horizontal overflow.
- Closed M32 with the authoritative repository gate: 170 API tests, 53 Board tests, 122 Extension tests and 6 packaged-Chrome E2E cases pass with lint, formatting, Mypy, TypeScript, production builds, security contracts and 30/108 evaluation fixtures. The design detector reports zero findings.
- Started M23 with the user-approved all-gpt-5.5 constraint. Official API guidance fixes the request shape: multiple remote `input_image` items at `detail: low`, strict Responses `text.format`, Pydantic validation, a small per-run batch and no repeated classification after checkpoint/retry.
- Completed M23. Added the bounded gpt-5.5 remote image batch, low-detail multi-image request, strict Pydantic response, relevance/observation gate, unknown-provenance visual-lead persistence, deadline reserve and pre-call spent-budget checkpoint. Behavior tests cover the four-image cap, low-detail request shape, non-asset rejection, deadline skip and forced replay without a second provider call. Full `scripts/verify.ps1` passed: API 173, Board 53, Extension 122, packaged Chrome E2E 6, plus lint, format, mypy, TypeScript and production builds.
- Started M27. Scope is bounded child-page expansion inside the existing page/time budget, conservative same-page image evidence, and promotion only when a concrete project page supports the result. No generic crawl, global case library or keyword-only promotion.
- M27 red tests added. The first targeted run fails at collection because `select_project_page_links` does not yet exist, confirming the bounded link-selection behavior is absent before production changes.
- M27 targeted behavior is green: same-host project paths are selected with a two-link cap; navigation, taxonomy and external links are excluded; one child project page is parsed inside the original page budget; an exact shared image URL promotes the existing visual lead in place to partial with probable project identity, confirmed asset association, unknown primary source/rights and a URL-bound fact claim. The max-pages=1 case proves expansion is skipped.
- First static gate found no lint issue, but Ruff requested formatting for `workflow.py` and mypy found one optional-value narrowing issue in the promotion loop. Resolution is a local variable annotation/narrowing fix plus the repository formatter; no behavior change is needed.
- Completed M27. Added bounded project-link selection, one-hop parsing within the existing page/time budget, exact-image in-place promotion, conservative partial evidence state and URL/text-excerpt fact claims. Full `scripts/verify.ps1` passed: API 177, Board 53, Extension 122, packaged Chrome E2E 6, lint, format, mypy, TypeScript and production builds; no live provider calls were made.
- Started M28 with planning-with-files and impeccable product rules. Diagnosis: the Board exposes raw stop/gap codes rather than explaining why delivery is partial. The implementation target is a compact Chinese diagnosis in the recent-run row and result header, followed by one representative live acceptance run against explicit usefulness thresholds.
- M28 red tests confirm both missing behaviors: recent partial runs still say only “已交付部分结果”, and the result page has no accessible Chinese diagnosis heading while raw stop/gap codes remain behind the disclosure.
- Implemented the Chinese diagnosis mapping and flat inline result summary; Board tests now pass 54 cases. Browser visual control could not initialize because the bundled runtime raised `Cannot redefine property: process` on two clean attempts, including after a session reset. Per the browser skill boundary, no alternate automation stack was used; responsive behavior remains covered by the Board test/build gate until that runtime is available.
- M28 live acceptance passed on run `0c8fa2aa-397a-483a-9e0f-3c59d6036967`: partial/no-new-assets, 5 usable drawings, 2 concrete projects, 5 partial-or-verified results, 27/27 results with images. One of four subquestions met its depth target, so partial delivery remains honest. The gpt-5.5 search timed out once and Firecrawl continued; M27 project-page expansion promoted three real child-page assets. M20 stays open because real Chrome page commands failed and no local crop was claimed.
- Completed M28. Recent-run rows now include a short Chinese partial reason; the result page exposes a visible heading, retained-result counts, translated coverage gaps and a concrete retry explanation without raw internal codes. Impeccable detector returned zero findings. Full `scripts/verify.ps1` passed: API 177, Board 54, Extension 122, packaged Chrome E2E 6, lint, type checks and production builds.
- Started M33 as the final consolidation pass. The success condition is no longer “use the chosen depth budget and preserve whatever was found”; every planned subquestion needs a usable evidence-backed answer, while depth controls breadth, examples and analysis rigor. This pass also owns the still-open Board-started Chrome acceptance and a screenshot-based user-flow audit.
- Loaded the Product Design audit contract and saved-context policy. The system `python` alias is unavailable, so its preflight and planning catch-up will run with the bundled Codex Python runtime. No production code has changed yet.
- Completed three bounded read-only audits in parallel. They independently identified the shared completion/enrichment predicate, one-time Firecrawl page association, missing retry permission handshake, current-page bridge/socket conflation and missing bounded scroll as the highest-impact causes of incomplete research.
- Broke implementation into three non-overlapping TDD tracks: backend completion semantics, Board permission/public fallback, and lazy-load Chrome inspection. Cross-subquestion cached-page evidence reassociation remains with the primary task to avoid overlapping edits in `workflow.py`.
- The in-app Browser runtime failed again because its bundled client attempts to redefine a protected `process` global. A supported Windows capture fallback connected successfully but the policy layer could not determine the current Chrome URL and ended that turn. No screenshot was accepted and no old screenshot will be used as current audit evidence.
- Finished the completion/enrichment split with 180 API tests green: normal budget or no-new termination is now completed when every branch has a matching evidence claim, while zero-support branches remain partial.
- Added and passed a Firecrawl cached-child regression: three distinct subquestions can reuse one already parsed project page without another network fetch, and the coverage report reaches all three branches.
- Added and passed two Board truthfulness regressions: missing branches remain visible, and assets with no known subquestion association are no longer presented as evidence for the first branch.
- Probed the configured gpt-5.5 relay without exposing credentials. Structured low-detail vision succeeds with one and two remote images; added a 5xx-only four-to-two compatibility retry and passed the visual unit, Ruff and Mypy checks.
- Continued M33 after interruption and resolved the seven remaining completion-recovery regressions. Completion recovery now applies only to `precedent_research`; an explicitly enabled Chrome inspection failure adds `browser_inspection_incomplete`, preserves web results and forces an honest partial terminal state; deliberate retries replay the full three-pass Quick contract while failed-execution resume still inherits completed query keys. Seven focused API regressions and all 62 Board tests pass.
- One focused pytest command referenced two stale/nonexistent browser-inspection test names and stopped during collection; no pass was claimed. Replaced the selectors with the current stronger-source and cross-page deduplication tests.
- Closed the API regression phase. Disconnected Chrome is treated as an unavailable optional enhancement and no longer downgrades mock/public research; a connected inspection action failure remains an explicit partial result. Source inspection is stable-sorted by publication strength, and exact unique image URLs now deduplicate across provider/browser sources so a primary-source upgrade cannot be recreated later as a weaker aggregator duplicate. The complete API suite, Ruff and Mypy pass.
- The authoritative repository gate passes after the M33 completion fixes: 185 API tests, 62 Board tests, 124 Extension tests and 6 packaged-Chrome E2E cases, plus Ruff/format/Mypy, ESLint/TypeScript, production builds, PowerShell security contracts and 30/108 evaluation fixtures. No live provider traffic was used by verification.
- Ran three comparable live Quick checks. All produced 3 usable drawings from one ArchDaily project and honestly remained partial at 1/3 covered subquestions. Dedicated recovery pages execute, but neither Chinese nor forced-English Firecrawl recovery found useful project evidence after the relay model search timed out; the unsupported English experiment was reverted. A real 739x930 browser audit led to the verified `3 张可用 · 18 条待核验线索` count split; follow-up geometry checks disproved apparent screenshot clipping.
- Closed M33. Final `scripts/verify.ps1` exits 0 with 186 API, 62 Board, 124 Extension and 6 packaged-Chrome E2E tests plus all lint/type/build/security/evaluation gates. Real browser acceptance confirmed the usable-versus-lead count at 739x930; the viewport override could not alter the already claimed tab to 390px, so no new 390px screenshot claim was made. Implementation is complete; the measured live-recall and external acceptance limits remain explicit in `findings.md` and the plan.
- Started M34 after the user required complete research at every depth and asked to preserve Firecrawl quota for three portfolio demonstrations. Assumptions are explicit: all modes share the same evidence-complete floor; hard external blockers remain honest and resumable; portfolio demos use no-cost deterministic replay data. Planned order is red tests, completion/reserve implementation, three screenshot-ready modes, user-view audit, then the full repository gate.
- M34 red phase is confirmed. API collection fails because the reserve error/client contract does not exist yet; Board Quick/Balanced/Deep cases fail because only `demo=1` is recognized. Existing tests otherwise remain green in the focused run. Production implementation can now proceed against concrete behavior.
- M34 core behavior is green. All three modes now have three completion-only recovery rounds; incomplete precedent runs become blocked continuation checkpoints, while other goals retain partial delivery semantics. Firecrawl checks the official remaining-credit endpoint before paid calls and defaults to a 100-credit floor. `?demo=quick|balanced|deep` uses local replay data only, shows 3/4/6 covered subquestions and makes no fetch call. Focused API tests and all 66 Board tests pass.
- Completed M34. Continuations skip already answered branches, all modes share the 30-minute safety ceiling, and incomplete precedent research is never presented as a finished partial answer. Clean Quick/Balanced/Deep portfolio screenshots were accepted from local replay routes after rejecting a compositor-corrupted Balanced capture.
- The authoritative gate exits 0 with 189 API, 66 Board, 124 Extension and 6 packaged-Chrome E2E tests, plus all lint/type/build/security and 30/108 fixture checks. No live provider or Firecrawl traffic was used.
- Started M35 after the user corrected the portfolio requirement and requested Xiaohongshu support. Final portfolio evidence is now defined as three real provider-backed result pages; existing replay pages are retained only for zero-cost development. XHS will use visible signed-in Chrome research with temporary permission and no credential/browser-storage access.
- M35 red behavior contracts are in place. Extension tests fail because media lacks a safe source link/card caption; Board tests fail because the XHS source control and required-Chrome gate are absent; the API client omits `research_sources`. The first focused API command used the wrong relative interpreter path and is being rerun from the resolved app environment before production code changes.
- M35 source strategy was refined from user feedback and current official Pinterest policy. Case completeness remains tied to authoritative project evidence; XHS is a signed-in, bounded visual-inspiration source; Pinterest is provider/search-link-only unless an officially permitted API is supplied. This prevents sparse inspiration posts from being misrepresented as complete project cases.
- M35 focused core tests are green: 4 targeted API behaviors, all 126 Extension tests plus typecheck, and all 68 Board/API-client tests plus typecheck. The implementation now has explicit source persistence, safe same-origin media links, a bounded XHS search adapter, required-Chrome gating, and deterministic visual-platform evidence downgrades. A full-workflow XHS regression is the next gate.
- M35 authoritative repository verification exits 0: 195 API tests, 68 Board tests, 126 Extension tests and 6 packaged-Chrome E2E cases pass with Ruff formatting/lint, Mypy, ESLint, TypeScript, production builds, PowerShell security contracts and the 30/108 evaluation fixtures. Verification used mocks and local fixtures only; no OpenAI, Firecrawl, TinEye, XHS or Pinterest quota was consumed.
- Post-gate review found and fixed a provider-isolation edge case: after a model web-search timeout with no Firecrawl source, remaining XHS subquestions now continue because the signed-in browser source is treated as an independent available search path. The regression moved from one of three XHS visits to all three and passes with Ruff/Mypy.
- Made Xiaohongshu the default source for every new Board research request. Red-first Board coverage now proves the default checkbox, required-Chrome failure, explicit request payload and restoration after a user-disabled run; all 69 Board tests pass without live provider traffic.
- Closed the M35 implementation gate: `scripts/verify.ps1` exits 0 with 195 API, 69 Board, 126 Extension and 6 packaged-Chrome E2E tests plus all lint/type/build/security and 30/108 fixture checks. No paid or signed-in live source was contacted.
- Planned M36 as a quota-protected release sequence: zero-cost readiness UI, source-only signed-in XHS acceptance, frozen Quick/Balanced/Deep prompts, one-at-a-time real runs with stop-on-failure, and a portfolio evidence package. No live source was contacted while creating the plan.
- Started M36 step 1. Success is a non-technical, local-only launch check for Chrome connection, current-page extension bridge, temporary page permission and honest pending XHS login verification; it must not create a research run or contact a research provider.
- User corrected the first interpretation before delivery: quota preservation is an internal project rule, not a product balance feature. The uncommitted balance endpoint, Firecrawl status row and related tests were removed; the remaining implementation is scoped only to Chrome/XHS launch readiness. No live balance, model, search, scrape, TinEye or XHS call occurred.
- Continued M36 step 1 with red-first truthfulness coverage. Two new tests reproduced the false-positive states “backend connected but current page has no extension” and “bridge disconnected with a stale permission bit”. The Board now distinguishes local service, current-page bridge and temporary permission, exposes the Chrome handoff when this surface cannot host the extension, and restores live announcements without competing with the research-run status role.
- Narrow-browser QA at 459×986 removed the yellow disconnected-state fill and tightened the two status rows; the page remains free of document-level horizontal overflow. Board lint/typecheck pass and all 72 Board tests pass after resolving the React effect rule. No provider, Firecrawl balance/search/scrape, TinEye or signed-in XHS request was made.
- Closed M36 step 1. The authoritative `scripts/verify.ps1` gate exits 0 with 195 API tests, 72 Board tests, 126 Extension tests and 6 packaged-Chrome E2E cases, plus lint, formatting, Mypy, TypeScript, production builds, PowerShell security contracts and all 30/108 evaluation fixtures. `git diff --check`, the changed-file credential scan and the Impeccable detector are clean. The product ships no balance endpoint, meter, quota copy or provider-readiness row, and this step consumed no live quota.
- Started M36 step 3 after the user said to continue. The shipped Chrome handoff opened the local Board and the ArchResearch extension reconnected to the loopback broker. Code-path review selected a provider-free ephemeral broker harness for the one-query acceptance, avoiding the normal run path because it would load the configured model and Firecrawl clients. No XHS navigation or paid call has occurred yet.
- Ran the provider-free M36 harness twice. The first attempt exposed the need to retain the Board during broker replacement; the corrected attempt paired successfully and reached the extension permission gate. Its audit contains one sanitized `open_url`, zero provider calls and no note data. Normal API/Board services were restored and the browser broker is connected. The next action requires the user to click ArchResearch → “允许本次网页读取”; browser policy forbids Codex from changing that permission indirectly.
- Implemented the M36 onboarding correction: manual pairing is recovery-only, connected sessions present one temporary-permission action, action-specific failures replace the generic connection error, and the Board explicitly says pairing is already complete. Verification is in progress; no research provider or signed-in source was contacted by this change.
- Closed the M36 browser-onboarding correction. The Impeccable detector reports zero findings, and the authoritative repository gate exits 0 with 195 API, 72 Board, 129 Extension and 6 packaged-Chrome E2E tests plus lint, formatting, Mypy, TypeScript, builds, security contracts and 30/108 evaluation fixtures. No OpenAI, Firecrawl, TinEye or signed-in XHS request was made.
- Reopened the M36 permission checkpoint after the real Chrome popup showed that the background-routed request lost its user gesture. The approved correction is direct popup permission acquisition plus persistent one-time authorization with an explicit manual revoke; red behavior tests are next.
- M36 persistent-permission red gate is confirmed: 16 Extension and 4 Board expectations fail on the old background request, auto-revoke and temporary-permission copy. Production changes can now proceed against the real failure contract.
- M36 persistent-permission implementation is green at the focused gate: 129 Extension tests, 72 Board tests and 6 packaged-Chrome E2E cases pass. The popup now invokes `chrome.permissions.request` from the direct click, terminal states and reconnects preserve approval while closing managed tabs, and explicit revoke still removes access. The authoritative repository gate remains.
- Closed the M36 persistent-permission checkpoint. `scripts/verify.ps1` exits 0 with 195 API, 72 Board, 129 Extension and 6 packaged-Chrome E2E tests plus lint, formatting, Mypy, TypeScript, production builds, security contracts and 30/108 evaluation fixtures. No OpenAI, Firecrawl, TinEye or signed-in XHS request was made; one real Chrome click remains to accept the user-gesture path before the bounded XHS check.
- 恢复 M36 签入态小红书验收：用户确认网页读取权限已授予。接下来按红绿测试为扩展媒体枚举增加重页面边界，完成一次 provider-free XHS 复验，再运行全仓门禁；Firecrawl 演示额度保持不动。
- M36 重页面修复通过聚焦门禁：媒体枚举最多扫描 500 个节点、返回 100 个候选且复用单次布局读取；13 条内容测试、Extension lint/typecheck/build 和 6 条打包 Chrome E2E 全部通过。生产内容脚本按受管页面动态注入，真实复验无需再次申请权限。
- M36 provider-free XHS 复验已消除原 `TimeoutError`：两次媒体枚举、一次滚动与清理全部完成，零 OpenAI/Firecrawl/TinEye 调用。当前输出因没有解析到可见笔记链接而保持 blocked，下一步只检查并适配真实卡片链接结构。
- Chrome 直接 DOM 检查在 XHS 页面加载阶段超时，未获得或保存页面数据。按不重复失败原则，诊断切换到项目自身的枚举协议，并仅记录脱敏的候选数量与同站链接形态。
- 项目扩展脱敏诊断完成：两次枚举各返回 15 张图片但没有祖先链接。下一步以红测覆盖“图片与同卡链接为兄弟节点”，实现有深度、链接数和同源约束的最近卡片查找。
- 启动 M37 小红书视觉灵感编排：结果页将项目证据与视觉平台图片分区，后端保留笔记标题与平台限制；网页检查优先级改为案例研究优先项目来源、视觉参考优先小红书，而不是固定按来源层级排序。
- M37 聚焦实现已通过：案例研究优先项目站点、视觉参考优先视觉平台；XHS 裁图保留笔记标题和明确限制；Board 按子问题与图纸类型生成独立灵感板并从方案项目计数中排除。37 条 workflow、57 条 Board 测试及两端静态/构建检查均通过。
- M37 响应式实页首轮验收：1440/390 均为 `scrollWidth == clientWidth`，6/6 图片加载；发现按类型拆行导致单卡占一整行的低密度问题，正在收紧为“类型索引 + 子问题连续图网格”。
- 完成 M37：来源优先级随研究目标切换，XHS 图片保留笔记级来源并进入独立制图灵感板；最终 1440/390 实页无溢出且 6/6 图片加载。`scripts/verify.ps1` 以 198 API、73 Board、132 Extension、6 packaged E2E 全绿退出，设计检测零发现，未消耗 OpenAI/Firecrawl/TinEye 额度。M36 只剩用户重新加载一次 unpacked 扩展后的 provider-free XHS 真实验收。
- 用户重载后的 provider-free XHS 检查确认旧扫描器的连接、持久权限、滚动和标签清理正常，但 15 张可见图片仍没有笔记链接。第一版深层卡片修复在真实页把大型普通 `section` 纳入扫描，并与新搜索来源参数一起导致首次枚举超时；两项均已撤回或收紧。新增页面级 `section` 负例后，134 条扩展测试、静态检查、构建及 XHS API 测试通过。最终真实验收等待本次性能修复构建重载；正常 API 已恢复为梭子蟹 `gpt-5.5`，全程零 OpenAI/Firecrawl/TinEye 调用。
- 收紧卡片与恢复搜索参数后，真实复验仍在内容函数返回前超时，定位到 Chrome 默认等待持续加载页面的脚本注入时机。固定包内读取器现使用立即注入，不新增权限或动作；134 条扩展测试、静态检查、生产构建和 6 条 packaged E2E 全绿。正常 API 已恢复，等待最终构建重载后再做一次 source-only 验收，仍未消耗 OpenAI/Firecrawl/TinEye 额度。
- 完成 XHS 重 DOM 的确定性耗时边界：媒体节点按标签流式读取，不再先构造整页快照；链接集合先检查数量再复制；每次枚举最多 1.5 秒、500 个节点、100 个候选。新增墙钟回归后，`scripts/verify.ps1` 以 198 API、73 Board、135 Extension、6 packaged E2E 全绿退出。正常 API 保持在梭子蟹 `gpt-5.5`；最后一次 live 输出仍来自 Chrome 缓存的旧内容脚本，需重载最终构建后再验收，零付费额度消耗。
- 最终包加载后的单次 provider-free XHS 验收仍在第一条媒体命令处超时：受管页打开与关闭成功，但无枚举结果返回，确认当前后加载式内容监听器不适合该持续加载页面。按对用户的承诺停止重载循环，正常 API 已恢复为梭子蟹 `gpt-5.5`。M36 保留真实结果，M38 新增为红测驱动的临时 `document_start` 管线；本轮无 OpenAI/Firecrawl/TinEye 调用。
- 启动 M38：先以永不进入 idle 的受管页面建立红测，再实现任务前、精确来源、非持久化的 `document_start` 注册和全终态注销。实现与验证只使用本地 Mock/打包 Chrome，不调用 OpenAI、Firecrawl、TinEye 或真实小红书。
- M38 单元红门禁已建立：5 个预期失败分别证明当前实现没有 loading 期预装、页面命令仍重复注入、外部关 tab 不清监听、撤权不清受管标签。失败与计划缺口一一对应，其余 134 条扩展测试仍通过。
- M38 核心与生命周期门禁转绿：受管 tab 在导航前登记，loading 期安装并确认固定监听器，后续命令不重复注入；空白页竞态与过早注入用有界内部重试处理。工作线程重启、显式关闭、外部关 tab、五种终态、断线、撤权和重配对均有清理路径。147 条扩展测试、lint/typecheck/build 与 7 条 packaged Chrome E2E 全部通过，未调用任何真实研究服务。
- 完成 M38 最终收口：两轮只读并发复审发现并修复最新导航误判、终态晚到标签、私网状态分离、未取消重试、持久化失败、旧连接回包和一次性码重连等缺口；API 同时落地单活研究租约。最终 `scripts/verify.ps1` 以 202 API、73 Board、160 Extension、7 packaged Chrome E2E 全绿退出，未调用 OpenAI、Firecrawl、TinEye 或真实小红书。M36 现只剩一次 provider-free 小红书单源验收。
- 启动 M36 最终小红书单源验收，但当前外部 Chrome 未运行；ChatGPT Chrome 控制扩展虽已启用，其本机通信注册缺失，Codex 不能自行安装或修复。按浏览器安全边界暂停在用户授权启动 Chrome之前；尚未打开小红书、未发送研究命令，也未消耗 OpenAI、Firecrawl 或 TinEye 额度。该环境问题不回退已完成的 M38。
- 用户授权后已启动 Chrome，ChatGPT Chrome 控制通道在规定的一次重试内恢复；当前选定用户配置只打开一个空白页，尚未访问小红书。下一步通过项目自身的 provider-free 验收入口触发一次受管搜索并核对脱敏资产摘要，不能改成付费研究运行。
- 正常 API 与 Board 已在 8000/5173 启动，未创建研究运行；Board 明确报告 ArchResearch 扩展“未连接”，因此尚未具备受管小红书读取条件。下一步只执行页面提供的“在 Chrome 中启用精确提取”本地恢复动作并复核 broker 状态。
- M36 最终小红书单源验收已收口为真实 `blocked`，未转入付费作品集研究。保留 5173、只替换 8000 后，扩展完成全动作链并安全关闭标签，但两轮仅出现 13 个非图片候选、0 个笔记来源；基于旧实页的 15 张图片证据，将动态页等待改为首屏 3.5 秒、滚动后 1 秒，红绿测试、Ruff 与 Mypy 通过。唯一的修复后实页复验返回 `TimeoutError`，随即停止重试。API `/health=ok`、浏览器 broker `connected=true`、Chrome 仅余 Board 标签；OpenAI、Firecrawl、TinEye 调用均为 0，额度完整保留。验收脚本同时移除原始查询输出，并把成功门槛收紧为两轮枚举、至少一张图片和至少一个笔记来源。
- M36 阻塞记录后的全仓门禁通过：202 API、73 Board、160 Extension、7 packaged Chrome E2E 全绿，Ruff、格式、Mypy、ESLint、TypeScript、生产构建、安全契约与 30/108 评测夹具均通过。门禁仅使用本地 Mock/fixture；没有创建付费研究运行，也没有消耗 Firecrawl 保留额度。
- 启动 M39：把“修复前完整动作链可返回、仅增长浏览器内等待后变为 `TimeoutError`”固定为当前因果假设。先用红测要求小红书加载等待由本地 Python 工作线程承担，不让 MV3 WebSocket 命令为纯计时保持悬挂；全仓门禁前不再访问真实小红书，也不调用 OpenAI、Firecrawl 或 TinEye。
- M39 本地与打包浏览器门禁转绿：小红书 3.5/1 秒等待已移到 Python 工作线程；扩展为每次受管硬导航保存最新 readiness 与 `documentId`，页面消息 3 秒失败，只有“无接收端”的只读命令允许重装固定脚本一次，超时、滚动和输入不重放。XHS 聚焦 API/工作流测试、Ruff、Mypy、165 条 Extension 测试、lint/typecheck/build 与新增硬导航场景后的 8 条 packaged Chrome E2E 全部通过；尚未访问真实小红书或任何付费 Provider。
- M39 全仓门禁以 202 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿退出。门禁后的 provider-free 实页尝试精确记录 `failed_action=enumerate_media`、0 次完成枚举和 30 秒后端 `TimeoutError`；新构建应在 3 秒内失败，因此确认 Chrome 仍承载旧 service worker。正常 8000/5173 与 broker 已恢复，受管标签已由终态清理；本次仍为 0 个 OpenAI/Firecrawl/TinEye 调用。下一步先让该单 Board Chrome 配置的全部进程真正退出并重新加载 unpacked 构建，不直接重复小红书请求。
- 继续 M39 时，ChatGPT Chrome 控制通道在初始化和规定的一次延时重试后仍不可用，但 ArchResearch 项目扩展与本地 broker 保持连接。按浏览器安全边界，Codex 不用 PowerShell/Computer Use 代点 `chrome://extensions`。当前暂停在一次明确的用户手势：点击 unpacked ArchResearch 扩展的“重新加载”；完成后即可用 3 秒边界做一次可判定的 provider-free 验收，不再盲目重载。
- 完成 M39：用户重新加载 unpacked 扩展后，项目自带 Chrome handoff 自动恢复持久配对；provider-free 实页链在 5.3 秒内完成打开、两轮枚举、一次滚动和关闭，确认 30 秒挂起已消失，受管标签无泄漏。两轮均为 0 个可见媒体/笔记来源，因此记录为真实 source-empty，而非成功素材或扩展故障。正常 API/Board/broker 已恢复，OpenAI、Firecrawl、TinEye 调用为 0。后续正式研究保留 XHS 默认尝试，但空结果不阻塞权威案例来源完成覆盖。
- 锁定三种深度作品集任务：Quick/Balanced/Deep 使用同一个“旧工业厂房改造为社区文化中心”的具体问题，统一要求功能植入、公众/后勤流线、新旧结构、采光、剖面层次及平面/剖面/分析图证据。这样三张结果页展示的是 3/4/6 子问题和研究深化，而不是题目差异。Windows 凭据中的 gpt-5.5 与 Firecrawl 均可用，Firecrawl 100-credit 保留线生效；尚未发起付费运行。
- 2026-07-16：接续验收唯一真实 Quick 运行 `d13bdc67-d6b3-4006-8d89-089439a02311`。API 健康且仍使用梭子蟹 `gpt-5.5`；运行在 2026-07-15 18:09:28 结束为 `blocked`，覆盖 2/3 子问题、3 张可用图、1 个项目。判定未达到“所有深度均交付完整研究”的门槛，未启动 Balanced/Deep。下一步只做离线红测与定向修复，避免继续消耗 OpenAI/Firecrawl 额度。
- 2026-07-16：M40 四条行为红测先按预期失败，随后全部转绿：模型超时后恰好一次恢复调用；同一未覆盖分支三轮 Firecrawl 查询互不重复且恢复轮使用可信建筑域；ArchDaily 在弱来源前占用页面槽位并持久化为 `trusted_secondary`；视觉批次跳过合集页、优先具体项目页的流线/平面图并把观察合并入 `partial` 卡。API 全套测试通过；Ruff、格式、Mypy 和聚焦回归通过。首次静态门禁仅发现 workflow 需 Ruff 格式化和一个 Optional 枚举字典键类型，已定向修正，未调用 OpenAI、Firecrawl、TinEye 或真实 XHS。
- 2026-07-16：M40 全仓门禁以 205 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿退出；Ruff、格式、Mypy、ESLint、TypeScript、生产构建、安全脚本和 30/108 评测夹具全部通过。验证只使用 Mock/fixture。下一步重启本地 API 后对原 Quick run 执行一次幂等 retry，只补齐 `public_service_flows`；未达到 3/3 前仍禁止启动 Balanced/Deep。
- 2026-07-16：原 Quick run 的 attempt 1 只执行 `public_service_flows`，保留既有 5 项结果，但在 5 个 pass 后仍为 2/3 覆盖；两次 gpt-5.5 搜索超时，未启动第三次模型调用。Firecrawl 真实返回越域弱来源，XHS 出现一次命令错误，最终 `blocked/browser_inspection_incomplete`。续研后 Firecrawl 余额为 337，100-credit 保留线未触及。未启动 Balanced/Deep。
- 2026-07-16：第二轮 M40 红绿修复完成：Firecrawl 本地域名门禁、precedent research 的可选 XHS 失败降级、可信直接项目页证据提升、英文恢复查询去除未翻译中文并加入 ArchDaily site hint，以及每分支一次的可信项目页纯文本分析。相关行为、越权事实拒绝、Ruff、Mypy 和 API 全套均通过；下一步跑全仓门禁后再决定是否进行 attempt 2。
- 2026-07-16：M40 第二轮全仓门禁以 210 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿退出；全部 lint/type/build/security/evaluation checks 通过。可信直接页只按确定性出版域提升，避免模型自报 `primary` 的未知域被误升级。现可安全重启本地服务并对同一 Quick run 执行 attempt 2。
- 2026-07-16：用户提供的中转站记录显示近期模型请求总耗时约 1m17s–1m37s，开始做 `gpt-5.6sol` 的独立低成本连通性探针。探针不带网页搜索与外部工具，不改变正式研究配置，验证通过前不重跑 Quick。
- 2026-07-16：中转站模型目录确认准确 ID 为 `gpt-5.6-sol`；同一 Windows 凭据完成一次无工具、32-token 上限的 Responses 探针，4.17 秒准确返回 `OK`。正式配置仍为 `gpt-5.5`，下一步只验证结构化输出与单次 web search 兼容性。
- 2026-07-16：`gpt-5.6-sol` 的生产形态结构化 web-search 探针约 60 秒后由中转站/CDN 返回 504；Firecrawl、TinEye、XHS 均未调用。普通模型连通快但网页搜索链路仍失败，故未修改正式 `gpt-5.5` 配置，也未重跑 Quick。
- 2026-07-16：`gpt-5.6-sol` 的无搜索严格结构化输出 5.27 秒通过，确定瓶颈是中转站 web-search 工具链而非模型 JSON 能力。Firecrawl 官方文档同时确认 `includeDomains` 与 `site:` 均受支持；M40 转向“Firecrawl/建筑站点实时发现 + 模型只分析已抓取页面”。
- 2026-07-16：M40 新增红测先证明恢复轮错误地同时请求 5 个建筑域；生产代码随后改为每轮只请求一个确定性可信域，查询 `site:` 与 `includeDomains` 严格一致。聚焦 workflow 回归和全部 public-page parser 测试共 10 条通过，尚未重跑付费 Quick。
- 2026-07-16：单域 Firecrawl 实验以约 2 credits 在 3 秒内返回 4/4 个 ArchDaily 结果，证明定向域轮转有效；此前多域批次的 0 结果并非站点无资料。下一步只解析一个发现入口，验证实时“专题/标签页 → 具体项目页”展开，不创建案例库。
- 2026-07-16：额外 1-credit 入口解析确认 Firecrawl 可返回完整正文、链接和图片，但项目链接选择器误收当前页锚点、图片画廊和相关文章，耗尽恢复页容量且没有进入具体案例。停止继续付费探测，转为本地红测修复 ArchDaily 链接分类。
- 2026-07-16：按用户建议验证 `gpt-5.6-sol + medium`，无搜索严格结构化中文分析在 5.97 秒通过。决定将 medium 限定到问题拆解/已抓页面分析，搜索发现继续走确定性网页工具与低推理调用，避免把已知 504 放大。
- 2026-07-16：用户进一步明确所有 `gpt-5.5` / `gpt-5.6-sol` 调用统一 `reasoning.effort=medium`。ArchDaily 链接选择与编辑页误提升两个行为红测已建立；其中编辑页 fixture 已改为可分类平面图，使失败准确落在证据层级而非候选缺失。
- 2026-07-16：medium 推理、单域项目标题检索、ArchDaily 链接去伪和编辑页证据降级共 8 条聚焦回归全部通过。实现覆盖研究/视觉两类 Provider；下一步运行 API 全套与全仓门禁，门禁前不再发起真实 Provider/Firecrawl 请求。
- 2026-07-16：首次 API 全套只剩 2 个旧 ArchDaily fixture 失败：URL 表示具体项目但标题缺少真实的 `Project / Office` 形态，因此新规则保守降级。已只修正这两个测试数据；Ruff 同时报出 2 个触及文件的格式漂移，进入机械格式化后复验。
- 2026-07-16：两个 fixture 与格式漂移修正后，API Ruff、format、Mypy 和全套 pytest 均 exit 0。现在运行全仓 `scripts/verify.ps1`；该门禁只使用 Mock/fixture，不调用任何真实研究服务。
- 2026-07-16：全仓门禁以 213 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿退出，全部 lint/type/build/security/evaluation checks 通过。正式续研前新增最后一条性能红测：恢复轮已有可信 Firecrawl 来源时不再重复调用中转站 web-search，直接进入 medium 页面分析。
- 2026-07-16：性能红测从旧实现的 2 次模型搜索转绿为 1 次；恢复轮已有可信来源时记录 `trusted_public_sources` 并跳过中转站工具调用，弱来源仍保留一次有界模型恢复。两条聚焦测试通过，进入最终全仓复验。
- 2026-07-16：最终全仓复验以 214 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿退出。M40 代码门禁完成；现在仅重启本地服务并对原 Quick run `d13bdc67-d6b3-4006-8d89-089439a02311` 做一次真实补齐验收，失败即停止且不启动 Balanced/Deep。
- 2026-07-16：重启前确认本地 API/Board 均健康，原 Quick 仍为 attempt 3、3 张可用、1 项目、2/3 覆盖，仅缺 `public_service_flows`。将通过安全 stop/start 脚本加载新构建，再调用已有 retry 接口；不创建第二个运行。
- 2026-07-16：安全重启后原 Quick 进入 attempt 4；API 仍为梭子蟹 `gpt-5.5`，代码统一 medium。XHS 因扩展未连接被可选降级；首个缺失流线分支的 Firecrawl 搜索 1.3 秒返回 4 项，运行继续中。
- 2026-07-16：attempt 4 的两轮常规搜索只得到弱来源且未新增流线资产；medium 模型搜索仅首轮超时一次，后续由 circuit 跳过。运行仍为 2/3，正进入单域可信来源恢复轮，尚未结束。
- 2026-07-16：attempt 4 的首个 ArchDaily 恢复批次因 `intitle:" / "` 过严返回 0；为避免继续空搜索与 medium 工具调用，已主动取消并保留 3 张既有结果。下一步红测删除该条件，并在同一证据层级内把直接项目/项目列表排在编辑文章前。
- 2026-07-16：撤除 `intitle` 并加入同层级来源用途排序，项目列表优先于编辑文章；新红测与可信层级/恢复域两条相邻回归共 3 条通过。进入最终全仓门禁，随后从 cancelled checkpoint 续研同一 run。
- 2026-07-16：全仓门禁中 215 个 API 测试与 Ruff lint 已通过，仅 `workflow.py` 的新排序 tuple 需 Ruff 自动格式化；无行为失败。只格式化该文件后重跑同一门禁。
- 2026-07-16：最终全仓门禁以 215 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿退出。现在重启加载无 `intitle` 的单域恢复和列表优先排序，再对同一 run 进入 attempt 5。
- 2026-07-16：attempt 5 从 round 3 恢复仍返回 0，已立即取消。对比 4 条成功的手工单域查询后，根因缩小为英文恢复查询丢失“旧工业改造 + 社区文化中心”类型语境；新增红测要求从中文子问题提取稳定英文 typology cues，并保证 `site:` 位于 500 字符边界末尾。
- 2026-07-16：typology 与 site-suffix 红测转绿；用生产函数生成的完整查询做一次真实单域验证，3.7 秒返回 4 个 ArchDaily 结果。现在运行全仓门禁，之后 attempt 6 直接从 round 3 继续。
- 2026-07-16：typology 后门禁的 215 个 API 测试全部通过，仅新增 fixture 在 `test_browser_inspection.py` 需要 Ruff 换行。只格式化该测试文件后重复全仓门禁，生产代码不变。
- 2026-07-16：post-typology 全仓门禁以 215 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿退出。即将重启并从 round 3 checkpoint 进入 attempt 6。
- 2026-07-16：attempt 6 的 round 3 ArchDaily.com 查询仍为 0，已立即取消；根因是该批次轮换到中文查询，而同构英文查询已验证 4 条结果。测试改为真实 2 个 normal + 2 个 recovery 轮次，要求 ArchDaily.com 恢复始终生成英文 typology/issue 查询。
- 2026-07-16：destination-aware 测试转绿；以 `language=zh` 调用真实 round-3 生产函数，正确输出英文 ArchDaily.com 查询并在 2.6 秒返回 4 条。进入最终门禁，之后 attempt 7 从同一 checkpoint 继续。
- 2026-07-16：attempt 7 在同一 Quick run 上完成新逻辑验收：ArchDaily.com 恢复得到 `Oslo Opera House / Snøhetta` 并跳过中转站 web-search，Designboom 后续批次也有来源，但没有新增达到流线证据门槛的图纸；最终仍 `blocked/budget_exhausted`、3 张可用、1 个项目、2/3 覆盖。停止继续扣额度，下一步离线修复流线资产相关性/分类。
- 2026-07-16：Firecrawl 余额 234，100-credit 保留线仍安全。离线代码审计定位到 run-global 视觉批次锁：attempt 1 的 500 失败仍永久阻止后续 attempt 对新图片分类，导致 Oslo 页 40 张图为 0 候选。下一步红测改为“每 attempt 最多一批 + 同指纹跨 attempt 不重放”。
- 2026-07-16：视觉批次续研红测先从预期 2 次调用失败为 1 次，生产修复后与原同指纹去重、截止时间门禁共 3 条转绿。现在每 attempt 只允许一个新指纹批次，同一指纹跨 attempt 永不重放，无数据库迁移。
- 2026-07-16：Attempt 7 后续离线修复完成 7 条红绿行为回归：查询同时携带主问题类型语境与明确的 visitor/staff/back-of-house/loading 术语；远程识图按 `(attempt, subquestion)` 公平放行；已知失败批次仅在明确的新 attempt 重试；未分类图库跨页等距抽样；可信具体项目页上的识图结果生成同源 EvidenceClaim 并升级为 `partial`，其类型继续传入无搜索页面分析。普通合集、编辑页和视觉平台仍保持 `visual_lead`。
- 2026-07-16：来源相关性门禁已加入搜索摘要、标题与 URL 的任务词匹配。无关的 Oslo 结果不再抢占同类具体项目页，也不能仅凭可信域触发 `trusted_public_sources` 跳过；项目列表页仍优先于编辑文章以保留展开能力。API Ruff/Mypy/222 测试及全仓 222 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿；仅使用 Mock/fixture，Firecrawl 余额仍为 234。下一步只能对原 Quick run 做一次 attempt 8，3/3 前禁止 Balanced/Deep。
- 2026-07-16 暂停点：attempt 8 在首轮发现“未知弱来源先消费流线分支识图批次”，且 gpt-5.5 远程视觉再次 `InternalServerError`，已立即取消并保留 3 张结果、2/3 覆盖；Firecrawl 剩余 226，未启动 Balanced/Deep。随后新增门禁：precedent 只允许可信具体项目页调用远程识图，视觉灵感模式不受影响；API 223 条和全仓 223/73/165/8 已全绿。`gpt-5.6-sol + medium` 单图视觉探针 8.7 秒成功。用户最后要求研究与视觉全部改为 `gpt-5.6-sol`；当前仅视觉运行配置已切换，默认契约测试已先改为 5.6-sol，但生产默认、研究运行配置、脚本/文档和复验尚未完成，明天从此处继续，暂停期间不再调用任何服务。
- 2026-07-16：启动 M41，以本地、无积分、fixture 可测试的浏览器研究链路替换 Firecrawl。官方方案比较后，Browser Use CLI 定位为 CDP/动态页辅助而非核心抓取器；生产候选收敛到 Crawl4AI 只读适配器或更小的直接 Playwright 适配器。下一步审计现有 public search/page parser、依赖与测试面，再以红测完成替换；在新链路全绿前不续跑真实 Quick。
- 2026-07-16：Playwright 原型已完成公开页与 ArchDaily 实页烟测，并发现/修复缺失 meta 导致的 30 秒等待；搜索引擎挑战页和长查询排序问题已形成真实基线。用户要求不要只按依赖推断，因此暂停最终选型，改为在隔离目录中对 Playwright、Browser Use CLI、Crawl4AI 运行同题基准；不调用任何付费 Provider，也不读取用户浏览器配置。
- 2026-07-16：M41 Browser Use CLI 0.13.4 已在隔离 venv 和独立 headless Chrome profile 完成真实 ArchDaily 搜索页/项目页基准；遥测通过环境变量关闭，未接触用户 Chrome 配置、Cookie、云端浏览器或任何付费研究 Provider。搜索页 24.65 秒得到 18 个项目 URL，项目页 12.46 秒仅得到 625 字正文；等待加载两次均超时但仍可提取部分 DOM。会话恢复脚本最初因系统 `python` 别名和根目录 `.venv` 不存在失败，改用 `apps/api/.venv/Scripts/python.exe` 后成功恢复上下文。
- 2026-07-16：M41 Crawl4AI 0.9.2 同题实测完成：禁用 stealth/proxy/simulate 后，搜索页 7.23 秒、项目页 5.46 秒，项目正文约 27.3k Markdown；隔离依赖为 639.4 MB / 20,307 文件，图片输出存在大量尺寸变体重复。当前直接 Playwright 生产适配器搜索页 8.08 秒、项目页 9.69 秒，但项目正文只取到 286 字，暴露首个 `article/main` 选择错误。下一步用单浏览器、同等待策略和 body-text 做最终归一化复测，再决定生产实现；仍未调用任何付费 Provider。
- 2026-07-16：归一化直接 Playwright 复测以单隔离浏览器完成：搜索页 6.99 秒、项目页 5.23 秒，均略快于 Crawl4AI；同样得到 18 个项目 URL，并取到 6,692 字 body text、168 链接和 88 个图片引用。生产适配器的 286 字缺失已确定为正文选择器 bug，而非工具能力不足。下一步只补 Crawl4AI cleaned/fit 内容指标和 Browser Use 固定等待对照，然后做最终选型与红测修复。
- 2026-07-16：三工具归一化基准数据已齐：Crawl4AI 项目页 28,141 字 raw Markdown 实际只有 3,005 字 cleaned text；Browser Use 用元素就绪可把暖启动降至 4.51/1.31 秒，但项目正文仍只有 622 字，固定等待还会漏掉全部搜索项目。直接 Playwright 取得 6,692 字正文，速度与 Crawl4AI持平且依赖最小。下一阶段进入生产正文选择器与站点结果过滤红测，不再继续扩大候选工具安装面。
- 2026-07-16：用户补充 Browser Act 与 Browser Harness 并放宽反阻塞边界。Browser Harness 已确认是前述 Browser Use CLI 的实际内核，不重复计数。Browser Act 1.0.6 隔离安装为 372.9 MB / 9,966 文件；CLI/仓库审计确认其核心包含编译二进制、Windows UI 自动化、Cookie、代理、验证码和远程接管能力，GitHub 开源仓库不能完整审计运行时。Agent Skill 因 skill-vetter 缺三个扫描依赖未安装；仅继续做隔离、公开页、无账号的 CLI 效果测试。
- 2026-07-16：Browser Act 完成 Skill 版本握手后，`stealth-extract` 在访问目标页前以 230103 退出并强制要求 BrowserAct API Key/注册；没有创建账号、没有访问 ArchDaily、没有输出正文。四项实测选型据此收口为直接 Playwright；Browser Harness 已包含在 Browser Use 数据中，Crawl4AI/Browser Act 均因巨大依赖与额外平台约束不进入生产。下一步以红测修正文选择与已知建筑站点结果过滤，并同步新的反阻塞/人工接管边界。
- 2026-07-16：用户追加 Microsoft Playwright CLI、OpenCLI 和小红书同题测试，最终选型重新打开。两套 CLI 已隔离安装：Playwright CLI 17.2 MB / 193 文件，OpenCLI 22.5 MB / 3,582 文件。OpenCLI daemon 可启动但 Browser Bridge 未安装，因此普通公开页和 25 个小红书适配命令目前都不能访问页面；先完成 Playwright CLI 同题实测，再用现有 ArchResearch 扩展记录 XHS 登录态基线，并评估 OpenCLI bridge 是否值得一次用户授权安装。
- 2026-07-16：Microsoft Playwright CLI 的 ArchDaily 同题实测完成：暖搜索页可枚举 18 个项目，项目页取得 5,587 字正文、159 链接、59 图片和 23 figure；项目导航 7.80 秒，项目摘要命令另耗 6.37 秒。它没有比直接 Playwright 提取更多内容，CLI 进程与自动 snapshot 反而增加开销。现在进入已登录小红书的只读基线测试，公开站与 XHS 不强行共用同一工具。
- 2026-07-16：ChatGPT Chrome 对照通道初始化与规定的一次延时重试均失败；诊断确认 Chrome 正在运行、扩展已安装启用且 native host 完整，因此不通过 PowerShell/配置文件绕读用户会话。小红书实测改走项目自身的只读 JSON DSL 与 ArchResearch broker；该通道与 ChatGPT Chrome 扩展相互独立。
- 2026-07-16：中断后的 XHS provider-free 验收在 20 秒内未收到 ArchResearch 扩展连接，返回 `extension_connection_timeout`，0 次 Provider 调用，也没有向页面发送动作。该结果不能作为 0 素材或抓取失败；接下来先诊断扩展重连，并补测各候选工具对同一 XHS 公开入口的匿名可达性。
- 2026-07-16：小红书匿名同题实测完成首组：直接 Playwright 5.44 秒被重定向到 `300012 / IP 存在风险` 页面，0 笔记；Crawl4AI 6.24 秒得到登录壳的 1,039 字与 30 个链接，但同样 0 笔记。结论是匿名抓取不能替代登录态；下一步只比较候选工具的登录态桥接与只读提取能力，不能把页面壳算成成功。
- 2026-07-16：Microsoft Playwright CLI 的匿名 XHS 结果与直接 Playwright 相同：2.43 秒启动后进入 `安全限制 / 300012`，0 笔记；CLI 另暴露 Windows `.cmd` 对带 `&` URL 的引用边界。Browser Use/Browser Harness 将在隔离 CDP 配置中继续同题测试，不能接触默认 Chrome profile。
- 2026-07-16：Browser Use/Browser Harness 在隔离 CDP Chrome 中 4.84 秒完成 XHS 匿名测试，同样得到 `300012` 与 0 笔记；没有比直接 Playwright 更强。OpenCLI 首次 `.cmd` 搜索约 28 秒无输出，不能评分；下一次改为包内 Node 入口捕获明确的 Browser Bridge 状态，不重复同一调用。
- 2026-07-16：OpenCLI 直接 Node 入口的第二种调用仍约 27 秒后只输出代理 warning，没有笔记或结构化错误；结合 doctor 的 Browser Bridge 缺失，停止第三次盲重试。当前产品的真实缺陷转为 ArchResearch MV3 在本地 API 重启后未自动唤醒重连，先用 alarm 红测修复再做登录态 XHS 验收。
- 2026-07-16：MV3 自动重连红测先失败，随后通过：扩展新增 1 分钟低频 `chrome.alarms` 唤醒，启动时继续走现有幂等 pairing restore；聚焦测试、typecheck、lint 和 production build 全绿。用户只需为这次新 bundle 重新加载一次 unpacked 扩展，之后 API 重启应自动恢复；等待手势期间继续公开页解析修复。
- 2026-07-16：公开页 10 条 fixture、Ruff、Mypy 已绿，但生产适配器复读同一 ArchDaily 项目仍仅 286 字；证明页面的全部 semantic roots 都是碎片，而完整 6k+ 文本只在 body。下一条红测改为把 body 也纳入最长候选，不能把 fixture 通过误判为实页修复。
- 2026-07-16：body 与 semantic root 共同取最长后 11 条 fixture 通过，ArchDaily 实页由 286 升至 1,068 字；仍未达到归一化基准的 6,692 字，根因缩小为生产读取只等待 500ms。下一条红测固定已实测有效的 3.5s 有界 settle，再复读实页。
- 2026-07-16：3.5s settle 红测转绿；生产适配器同一 ArchDaily 实页现为 7.64 秒、5,385 字、176 链接、90 图片引用，已从 286 字缺陷恢复且高于 Crawl4AI 的 3,005 cleaned text。公开页最终选型确定为进程内直接 Playwright；继续清理模型/安全文档并跑集成门禁，登录态 XHS 等待新扩展一次重载。
- 2026-07-16：用户要求所有候选在登录后补测 XHS。M41 改为共享隔离 Chrome 登录态公平基准：只读可见 DOM，不导出 Cookie/storage；能复用 CDP 的工具同会话测试，不能复用或仍需额外 Bridge/Key 的工具明确记为能力缺失。匿名 300012/0 笔记数据只保留为未登录基线。
- 2026-07-16：登录态接入方案已确认：Direct/Crawl4AI/Browser Use 共用 CDP，Playwright CLI 关闭共享 Chrome 后复用持久 profile。OpenCLI 官方 Bridge v1.0.22 已下载并审查，默认含 `debugger/cookies/downloads/<all_urls>`；用户明确要求效果优先、允许推翻旧边界，因此改用官方原版做隔离登录态实测，权限只记成本，不预先淘汰。
- 2026-07-16：隔离登录态 Chrome 已在 9335 启动；OpenCLI `doctor` 自动启动 19825 daemon，但命令行加载官方 unpacked Bridge 后仍报告 extension missing。先检查扩展 target/加载状态，不把连接失败当抓取效果；用户可在该新窗口自行登录 XHS。
- 2026-07-16：用户登录后 Direct Playwright/CDP 真实 XHS 成功：5.37 秒，首屏 20 笔记/52 图片，滚动后 37/93，无登录墙或风控；0 图文 pair 是最近祖先规则不适配卡片 DOM。Browser Use 首个登录态诊断在执行前因 stdin surrogate 编码失败，下一次 reload 到 9335 并改用纯 ASCII 程序。
- 2026-07-16：Browser Use 第二次登录态导航在 fresh daemon/ASCII payload 下仍 IPC `Page.navigate` 超时；不再重复导航，只保留一次已有页提取机会。Crawl4AI 登录态 CDP 超过 20s 后仍挂起且无输出，精确终止两个 benchmark Python 进程；隔离 Chrome 会话仍正常。
- 2026-07-16：按“一工具一次干净 profile 重启”消除 CDP 污染后，Direct 稳定为 5.35s、20→37 笔记、52→92 图片、53→95 图文对；Browser Use/Harness 最终独占运行 4.84s、20→36、52→91、53→93。效果基本相同，后者只快约 0.5s但多 daemon/大依赖；继续给 Crawl4AI 一次干净独占复测。
- 2026-07-16：Crawl4AI 干净独占登录态为 7.29s、849 字、0 笔记、6 图片、0 图文对并有 unclosed transport warning，效果明显落后。系统 Chrome 150 同时忽略命令行 unpacked extension，OpenCLI Bridge 未加载；改用支持扩展的 Chrome for Testing 复用同一隔离 profile，不手动重复安装。
- 2026-07-16：Chrome for Testing 149 无法打开由系统 Chrome 150 写入的登录 profile，9335 未启动且不存在残留进程/Singleton 锁；不能无损复用登录态。OpenCLI 暂未评分，后续只能在已登录系统 Chrome 手动装官方扩展，或让用户在新 CfT profile 再登录一次。
- 2026-07-16：Microsoft Playwright CLI 用同一登录 profile 可启动系统 Chrome，但 `open` 与独立 `eval` 均持续不返回，无法取得页面计数；精确终止 2 个 CLI Node 与 7 个 profile Chrome 进程。公开页可用不等于登录态 XHS 可用，记为稳定性失败。
- 2026-07-16：用户安装官方 Bridge 后 OpenCLI 登录态闭环完成：0.2s 连接，4.97s 返回 20 条结构化搜索结果；`note` 21.52s 返回正文/互动但 0 图片 URL；`download` 7.92s 取得 7 张 JPG / 549,284 bytes，临时目录已安全删除。最终选型改为 Public=Direct Playwright、XHS=OpenCLI，按效果优先进入只读搜索与多图资产接入。
- 2026-07-16：恢复 M41 实施现场；上一轮 API 全套后台测试已完成到 100%，仅有既有 FastAPI TestClient 弃用警告。确认工作树中的大量改动均属于本项目既有工作，本阶段只新增 OpenCLI 小红书正式适配器、工作流接缝、运行时检测和对应测试，不清理或重写相邻功能。
- 2026-07-16：OpenCLI 首次 help 探测误用了不存在的 `dist/cli/index.js`，直接得到 `MODULE_NOT_FOUND`；包清单已确认正式 bin 是 `dist/src/main.js`，后续改用该固定入口，不重复错误调用。正式运行时将走无 shell 的 Node 参数列表，避免 Windows `.cmd` 与 URL/查询字符转义问题。
- 2026-07-16：OpenCLI 登录态生产契约探针通过：真实搜索返回 4 条结构化笔记；选择高相关分析图笔记后得到 7 张 JPG / 549,284 bytes，输出为 7 条下载状态 JSON。探针不调用模型或 Firecrawl，下载临时目录已安全删除。进入适配器红测与工作流接入。
- 2026-07-16：按用户反馈清除成品中的成本管理残留：演示页“固定回放·不消耗网页研究额度”改为“展示完整结果结构”，`budget_exhausted` 的用户文案改为“当前自动补齐已到上限”；README/失败说明不再提抓取积分、作品集预留或额度恢复。全仓产品代码与文档扫描只剩历史评测中的“时长预留”、建筑流线语义中的“空间预留”和扩展防读信用卡字段，均与供应商余额无关；Board 73 tests 通过。
- 2026-07-16：OpenCLI 红测首次整块补丁因 `test_xiaohongshu.py` 当前导入上下文与恢复摘要中的旧快照不一致而未应用，未改动测试文件。改为读取当前文件后分小块追加，避免覆盖并行/既有修改。
- 2026-07-16：读取当前工作树后确认 OpenCLI 基础适配器与 6 条红绿测试其实已在中断前落盘：固定 Node 入口、无 shell、严格 JSON、错误脱敏、`/search_result/` 支持、等距四图和站外 URL 拒绝均已实现。聚焦 `tests/test_xiaohongshu.py` 6/6 通过；不重复实现，下一步直接补 fallback、批量本地图像分类和工作流注入。
- 2026-07-16：进一步恢复确认中断前已完成 fallback、四图批量分类和主工作流接入；3 条聚焦验收通过，覆盖无扩展 OpenCLI 多图闭环、一次 batch 分类及 OpenCLI 异常后的扩展回退。现在补“OpenCLI 空结果也回退”缺口，随后跑 API 全套与真实 OpenCLI smoke。
- 2026-07-16：空结果回退生产逻辑已转到浏览器后端，但新测试被插入到一个既有长测试尚未结束的位置，后续旧断言落入新函数并触发 `NameError: run`；另外两条相邻回归通过。此为测试布局错误，不是生产回退失败；先把新测试移动到完整函数边界后复验。
- 2026-07-16：新测试移到正确函数边界后，OpenCLI 成功、异常回退、空结果回退三条相邻路径全部通过。API Ruff lint、Ruff format、Mypy 和全套 pytest 均 exit 0（仅既有 FastAPI TestClient 弃用警告）。下一步用正式依赖发现路径做一次不调用模型的登录态 search+download smoke，再跑全仓门禁。
- 2026-07-16：首次“生产发现 + search + download”组合 smoke 运行约 25 秒后工具层只返回空输出，既无退出码也无异常，不能判定成功；不原样重试。拆为纯本地 discovery 与带明确退出码的 search 两段，多图下载沿用此前两次真实 7 图证据，避免重复请求和重复下载。
- 2026-07-16：production discovery 已明确通过；OpenCLI doctor 定位 live search 等待的根因是 Chrome 未运行，daemon 1.8.6 本身正常。按浏览器控制规则已向用户请求重新启动已登录 Chrome 的明确许可，未自行弹窗。全仓 `scripts/verify.ps1` 随后以 224 API、73 Board、165 Extension、8 packaged Chrome E2E 全绿退出，lint/type/build/security/evaluation checks 全部通过。M41 标为 `implementation_complete`，仅剩登录态 smoke 与原 Quick run 续研。
- 2026-07-16：最终 diff 审计发现并修复 `THIRD_PARTY_NOTICES.md` 一处 Markdown 行尾空格；`git diff --check` 现为 exit 0，仅有 Windows LF→CRLF 提示。当前没有代码门禁缺口，等待用户授权启动 Chrome 后完成 OpenCLI 登录态 smoke，再继续同一个 Quick run。
- 2026-07-16：按用户提醒复核 Firecrawl 删除状态：`apps/`、`scripts/`、README、DESIGN、docs、`.env.example`、根 package 与 pnpm lock 均为 0 处引用。Firecrawl 只存在于 planning 文件的历史决策记录，生产架构、配置、回退和依赖均不存在该入口；后续表述不再把它说成仍需避开的运行服务。
- 2026-07-16：首个跨两文件红测补丁因 `test_browser_inspection.py` 的 import 上下文与预期不一致而整包未应用；未修改任何测试。改为先单独落 OpenCLI 适配器测试，再读取真实 import 块后追加本地图像批测，避免重复同一失败补丁。
- 2026-07-16：OpenCLI 适配器 6 条契约测试转绿。首个本地轮播批测只生成 1 个候选，原因不是生产逻辑，而是 fixture 使用 6 张纯色图，感知哈希正确将其判为同构重复；改用现有 6 组结构图案 fixture 验证四图等距抽样与单批分类。
- 2026-07-16：适配器与多图分类核心已通过 7 条聚焦测试、Ruff 和 strict Mypy：OpenCLI 使用固定无 shell Node 入口，接受 `/search_result/`，结构化筛选搜索项，错误不泄露查询/URL/stderr；轮播等距取 4 图并合并为一次视觉批次，仅保存达到门槛的规范化 PNG。下一步接入研究工作流主路径、扩展回退和 app runtime discovery。
- 2026-07-16：工作流 OpenCLI 多图集成红测转绿且 Ruff/Mypy 通过：无 ArchResearch 扩展时仍可搜索、下载、批量分类并持久化 XHS `visual_lead`；每个子问题只下载一个笔记，成功后不重复页面裁图；OpenCLI 搜索失败按顺序回退旧扩展。XHS Trace 已去掉 query/fragment，保留后端、数量、耗时阶段与错误类型。下一步把适配器注入 app runtime 并正式加入根 npm 依赖。
- 2026-07-16：正式根依赖已安装并可被 Python runtime 自动发现。首次用户态 `pnpm exec opencli doctor` 被 pnpm 的 `ERR_PNPM_IGNORED_BUILDS` 供应链门禁拦截，尚未进入 OpenCLI；生产适配器不受影响，因为它直接调用锁定包的 Node 入口。改为提供根 `pnpm opencli -- doctor` 脚本并用同一固定入口复验，不要求用户放开所有依赖构建脚本。
- 2026-07-16：第二种 `pnpm opencli -- doctor` 仍被 pnpm 的依赖状态预检拦截，说明脚本别名不能绕过未明确处置的 postinstall。第三次改为在根清单显式列出 `ignoredBuiltDependencies=[@jackwener/opencli]`：项目只使用包内已发布 CLI/适配器，不执行其联网 `postinstall/fetch-adapters`，同时让 pnpm 供应链状态变为已审计决定。
- 2026-07-16：安装脚本审计确认无网络且非全局安装直接跳过后，首次 allowlist 配置仍失败；pnpm 11 明确提示 `package.json#pnpm` 已弃用，并自动在 `pnpm-workspace.yaml` 生成 `allowBuilds: '@jackwener/opencli': set this to true or false`。最终修复位置应是 workspace 的单包 `true`，同时删除已忽略的 package 字段。
- 2026-07-16：`pnpm-workspace.yaml` 单包 allowlist 生效，`pnpm install` 与 OpenCLI postinstall 均 exit 0；审计确认本地 workspace 安装时两个脚本实际不改用户 shell/adapter。正式 `doctor` 现为 daemon 1.8.6 正常、Browser Bridge 未连接；一次 daemon restart 后状态不变。Chrome 只读检查确认当前可控 Chrome 有登录态 XHS 与 ArchResearch 扩展页，但 Chrome 内部扩展页不能由自动化接管；此项记为外部 profile/扩展激活状态，不回滚代码，也不阻塞离线门禁。
- 2026-07-16：完整 API 输出流未保留终态，旧 `.pytest_cache/lastfailed` 又包含 7 个已不存在的历史节点，单点诊断因此只得到 `test not found`。首次 JUnit 路径误指向不存在的 `apps/api/.artifacts`，报告未生成；改用已存在的仓库根 `.artifacts/api-test-results.xml` 后以 XML 总数/失败数作为最终证据，不再依赖终端点阵。
- 2026-07-16：仓库根 JUnit 报告确认 API 全套 223 tests / 0 failures / 0 errors / 0 skipped，耗时 95.207 秒；默认 XHS、OpenCLI runtime、旧扩展回退和既有研究状态机均未回归。Ruff lint 已绿，format check 指出 4 个本阶段触及文件及 M41 的 `public_pages.py` 需机械格式化；只运行 Ruff formatter 后复验，不做行为改写。
- 2026-07-16：Ruff formatter 后全 API lint/format 与 strict Mypy 通过。新增运行内 XHS 熔断回归：OpenCLI Bridge 首次失败只调用 1 次，当前题回退 ArchResearch 扩展，后续 2 个子问题直接走已成功回退后端；聚焦测试 15.3 秒通过，避免离线 Bridge 重复增加约 90 秒。进入全仓 `scripts/verify.ps1`，该门禁只使用 Mock/fixture。
- 2026-07-16：首轮全仓门禁确认 223 API tests 全绿、Ruff lint/format 全绿；唯一失败是熔断 helper 参数仍标注为只读 `Sequence[XiaohongshuSearch]`，而实现会 `pop` 已失败后端，strict Mypy 正确报 `attr-defined`。将该局部参数收紧为 `list[XiaohongshuSearch]` 后复验，不改运行行为。
- 2026-07-16：开始为新对话做无缝交接与目录迁移。已确认源/目标绝对路径均存在且不同；目标目录除 `.git` 外为空，但与源仓库发生 Git 元数据冲突，因此尚未移动或覆盖任何文件。下一步检查目标仓库是否为空壳，并生成 `HANDOFF.md` 后再迁移。
- 2026-07-16：目标 Git 已确认是 `master` 上无提交、无远程的空壳；源仓库是 `codex/archresearch-v2-1`，最新提交 `98a9a01`。一次 `Get-Item` 未加 `-Force` 导致隐藏 `.git` 报未找到，不影响 Git 检查结果；后续用 `rev-parse --git-dir` 与 `Get-ChildItem -Force` 核对，不重复同一命令。
- 2026-07-16：首次迁移命令把备份 `.git`、移动源仓库和压平子目录组合在一条长命令中，被工具安全层在执行前拒绝；源、目标均未变化。改用更简单的同盘原子重命名：先把整个空壳目标目录改名为备份，再把源仓库目录直接改名为目标路径，无覆盖、无递归复制。
- 2026-07-16：空壳目标目录因正被当前 Codex 会话作为工作目录占用，Windows 拒绝整体重命名；仍未移动任何文件。改为只备份目标空 `.git`，再将源仓库顶层项目项逐个同盘移动到已核验为空的目标根，避免覆盖且支持中断续迁。
- 2026-07-16：直接移动目标 `.git` 被 Windows 的 Hidden 属性拦截，路径和内容仍未变化。下一次先暂时移除该空壳目录的 Hidden 属性，移动后在备份位置恢复原属性；不修改内部 Git 数据。
- 2026-07-16：复核发现上一步已把空壳 Git 的全部内容移动到 `C:\Users\76384\Documents\灵感agent.empty-git-backup`，仅因当前会话占用留下空 `.git` 目录；备份含完整 `HEAD/config/objects/refs`，目标空壳无 `HEAD`。后续把源项目真实 `.git` 内容移入这个空目录，再移动其余顶层项目项，无数据覆盖。
- 2026-07-16：项目主体迁移成功：源真实 Git 的 10 个顶层项和 26 个项目顶层项已同盘移动到 `C:\Users\76384\Documents\灵感agent`；新根 `HEAD` 指向 `codex/archresearch-v2-1`，`HANDOFF.md` 已就位。旧路径仅剩空 `.git` 外壳，目标空仓库完整备份在 `C:\Users\76384\Documents\灵感agent.empty-git-backup`。进入新路径 Git、OpenCLI discovery 与聚焦测试验收。
- 2026-07-16：新路径 Git/分支/工作树与 `HANDOFF.md` 验收通过；首次 Python discovery 因 `.venv` 的 editable-install 仍指向旧绝对路径而报 `ModuleNotFoundError: archresearch_api`。这是目录迁移后的环境链接问题，业务代码未变；在新路径重装本地 editable 包并刷新 pnpm 链接后复验。
- 2026-07-16：Python editable-install 已在新路径成功重建。pnpm 检测到 `node_modules` 被同盘移动，需要重建链接，但非交互命令因 `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` 主动停止，未改依赖；下一次设置 `CI=true` 允许 pnpm 按锁文件在已核验目标内重建，仍使用 `--offline --frozen-lockfile`。
- 2026-07-16：迁移后的 pnpm 链接已用 `CI=true + --offline --frozen-lockfile` 在新根重建，复用本地缓存且 0 下载；OpenCLI 1.8.6 仍能由生产 discovery 定位。新根聚焦验收为 9 个 API 回归测试通过，根 lint/typecheck 通过，`git diff --check` 为 exit 0。项目的唯一工作根现为 `C:\Users\76384\Documents\灵感agent`；完整状态与续接顺序已固化到 `HANDOFF.md` 和 `AGENTS.md`，新对话无需携带旧聊天全文。
- 2026-07-16：新会话按 `HANDOFF.md` 恢复后，由用户手动打开此前登录的小红书 Chrome。首次 `pnpm opencli -- doctor` 显示 daemon 1.8.6 正常但 Bridge 未连接；在 Chrome 已打开这一新外部条件下按官方建议仅重启一次 daemon，复查确认 Extension v1.0.22 与 Connectivity（3.5s）均为 OK。下一步只运行生产 `OpenCliXiaohongshuSearch` search smoke，不重复下载。
- 2026-07-16：首次生产 search smoke 在进程启动前失败：当前迁移后根目录没有 `.venv\Scripts\python.exe`，系统 `python` 也只是 WindowsApps alias；这不是搜索或 OpenCLI 失败。按项目 `Resolve-PythonRuntime` 的既有顺序定位到 `apps\api\.venv\Scripts\python.exe`（Python 3.12.13），后续改用该已验证运行时，不重复错误路径。
- 2026-07-16：生产 `OpenCliXiaohongshuSearch.discover()` 后的只读同题 search smoke 已返回至少一条结构化结果；进程只在最终把含 `✍` 字符的标题输出到 GBK 控制台时触发 `UnicodeEncodeError`。错误发生在结果 JSON 的标题序列化阶段，不是 Bridge/OpenCLI/search 失败；本次无下载、无模型调用，且为避免重复真实请求不重跑。按“发现成功、至少 1 条合法笔记、无下载/模型”验收通过，下一步重启本地 API 并继续原 Quick `d13bdc67-d6b3-4006-8d89-089439a02311`。
- 2026-07-16：本地 API/Board 从新根正常启动在 8000/5173；启动后查询原 Quick `d13bdc67-d6b3-4006-8d89-089439a02311`，发现真实数据库状态已是 attempt 8、`cancelled/user_cancelled`，保留 6 个结果、3 个子问题仍缺 1 个覆盖。该状态晚于 `HANDOFF.md` 所写的“attempt 8 前”，所以不会把启动误记为自动续跑；先核对同一 run 的 retry 契约和缺口，再决定是否进入 attempt 9，不新建 Quick。
- 2026-07-16：核对 retry 契约与运行态：同一 run 可从 `cancelled` 恢复，保留 checkpoint/既有结果，仅将 attempt 8 增为 9，并受进程内单运行 gate 保护；当前 Provider 健康且为 `gpt-5.6-sol`，唯一未覆盖子问题是 `public_service_flows`。`task_plan.md` 与 `HANDOFF.md` 已按真实数据库状态修正，下一动作是同一 Quick 的一次受控 attempt 9。
- 2026-07-16：同一 Quick attempt 9 已完成并通过：`coverage_satisfied`、3/3、0 gaps、7 usable assets、3 projects、10 retained candidates。OpenCLI 搜索返回 4 条且未回退，四图批次为缺失题新增 1 个 visual lead；Direct Playwright 解析 4 个 ArchDaily 页面，attempt 9 Trace 无 Firecrawl。M40/M41 标为 complete，M36 的 XHS 阻塞解除；唯一下一步是保存真实 Quick 结果页证据，再启动同题 Balanced。
- 2026-07-16：通过本地 Board 打开真实 Quick 结果页做可视验收。覆盖状态正确为完成/3 个子问题，但多张保留候选仍展示外文/空观察占位和“重新研究”提示，因此没有保存为最终作品集截图，也没有启动 Balanced。M36 唯一下一步改为离线红测并修复 resume 历史候选的中文分析补齐，然后复验同一 Quick 页面。
- 2026-07-16：历史中文分析展示修复完成红绿循环：Board 73 tests、lint、typecheck、production build 全绿。真实 Quick 热刷新后旧项目/机制/转译/观察占位全部消失，页面显示新的来源绑定中文说明与保守边界。当前截图前检查为 13 个图片元素中 8 个已加载、5 个远程图未加载；下一步滚动触发 lazy load 并复查，未确认前不保存作品集截图或启动 Balanced。
- 2026-07-16：真实 Quick 图片复验完成：分段滚动触发 lazy load 后 13/13 图片全部加载，0 failed、0 pending。中文旧占位为 0，研究状态为完成、3 子问题、7 张可用；现在可以按 1440×900 保存干净 Quick 结果页截图。
- 2026-07-16：真实 Quick 作品集证据已保存：`.artifacts/portfolio/quick-d13bdc67-result.png`（98,937 bytes）与 `quick-d13bdc67-result-full.png`（1,154,078 bytes）。截图前确认 3/3、旧中文占位 0、13/13 图片已加载。Quick 验收完成；下一步从 Board 发起同题 Balanced，不创建第二个 Quick，Deep 继续封锁。
- 2026-07-16：从 Board 准备同题 Balanced（标准 4×3、小红书开启、Pinterest 关闭）后点击一次“开始研究”，UI 被旧 ArchResearch 扩展硬门拦截；没有创建 Balanced run、没有 Provider 请求。下一步红测修复 OpenCLI-aware readiness，保留无任一 XHS 后端时的阻塞；不绕过 Board 直接调 API。
- 2026-07-16：OpenCLI-aware Board readiness 完成红绿循环：API browser tests 23/23、Board 74/74、API Ruff/format/Mypy、Board lint/typecheck/build 全绿。新状态字段来自 Pydantic，Board TypeScript 合约已对齐；下一步重启本地 API/Board 加载新接口，然后从 Board 仅重试一次 Balanced。
- 2026-07-16：重启后的真实 Board 预检确认 OpenCLI 可独立承担小红书读取，Chrome 未连接只显示“仅跳过 ArchResearch 精确裁图”；标准 4×3、小红书开启、Pinterest 关闭均核对无误。同题 Balanced 已从 Board 唯一提交一次，run `7d8faa53-2976-4387-9d9e-67d217225b70` 从 `planning` 开始；现在只监控该 run，到 4/4 前不启动 Deep，失败也不盲目重试。
- 2026-07-16：Balanced attempt 0 在保留 33 个结果后以 `blocked/provider_error:IntegrityError` 终止；此前 gap check 为 3/4、16 usable assets、4 projects，缺 `light_section`。没有重试。SQLite 数据与红测稳定复现共享侧栏图的跨来源升级冲突；生产改为优先当前来源候选，新红测和原项目页升级回归 2/2 通过。下一步先跑全仓门禁，全绿后才允许同一 run 从 checkpoint 续研一次。
- 2026-07-16：共享图修复后门禁通过：226 API、Ruff/format/Mypy、74 Board、165 Extension、production builds、30/108 evaluation、8 packaged E2E 全绿。总脚本中的 lifecycle 子测试在 Codex 工具宿主里中断其输出会话，故其后项目门禁均按同一命令逐项执行并通过；该脚本与候选持久化无改动。服务重启后 OpenCLI doctor 全 OK，同一 Balanced 唯一 retry 到 attempt 1，从 `checkpoint_stage=inspecting` 续跑；不创建新 run，也不允许第二次自动 retry。
- 2026-07-16：Balanced attempt 1 已 `completed/completion_satisfied`：4/4、0 gaps、17 usable、4 projects、8 partial、29 visual leads、37 retained candidates。真实 Board 标准深度与四个子问题可见，38/38 图片加载；四张干净截图保存为 `.artifacts/portfolio/balanced-7d8faa53-{result,questions,inspiration,cases}.png`。full-page 截图重复固定区块，已删除且未作为证据。M20 完成；M36 下一步仅从 Board 发起同题 Deep 6×4，小红书开启、Pinterest 关闭。
- 2026-07-16：同题 Deep 已从 Board 唯一提交一次，run `b4c314a6-e4ff-449b-b20b-617ddb577b28`；提交前确认深入 6×4、小红书开启、Pinterest 关闭，页面进入 planning。现在只监控此 run 到终态并要求 6/6；失败不自动 retry，也不创建第二个 Deep。
- 2026-07-16：Deep attempt 0 以 `blocked/budget_exhausted` 诚实终止并保留 35 candidates：5/6、29 usable、6 projects、10 partial，唯一缺 `daylight_strategy`；未 retry。离线查询审计显示该题 7 passes 的 recovery 文本过长且中英混杂，round 5–7 搜索为 0。唯一下一步是红测生成短的 destination-aware 英文采光/剖面案例查询，通过完整门禁前不续跑同一 Deep。
- 2026-07-16：采光恢复查询红测已建立并按预期失败：真实 workflow 生成的国际站查询是短英文且含类型词，但只含泛化 `old new structure daylight void section`，缺 `skylight/clerestory/courtyard`。下一步仅扩充采光意图映射并保持 300 字符上限，然后先跑聚焦测试；Deep 仍未 retry。
- 2026-07-16：采光恢复查询已用最小确定性映射转绿：国际站生成 `skylight clerestory courtyard daylight section drawings`，ArchDaily.cn 使用对应中文构造词；既有类型词、目标域后缀与 300 字符边界继续受 workflow 行为测试约束。未增加轮次/预算，也未改变 XHS 或证据升级规则。现在运行完整离线门禁，门禁前 Deep 保持 blocked。
- 2026-07-16：完整 `scripts/verify.ps1` exit 0：226 API、Ruff/format/Mypy、74 Board、165 Extension、生产构建、30/108 evaluation 与 8 packaged E2E 全绿。原 Deep 复核仍为 attempt 0、`blocked`、`checkpoint_stage=composing`。下一步重启 API 载入修复、核对 OpenCLI，然后只 retry 同一 run 一次并监控；不创建第二个 Deep。
- 2026-07-16：服务重启与 OpenCLI doctor 全 OK 后，只 POST 了原 Deep `b4c314a6-e4ff-449b-b20b-617ddb577b28` 的一次 `/retry`。attempt 1 已完成为 6/6、0 gaps、34 usable、7 projects、12 partial；没有第二次 retry 或第二个 Deep。发现完成态残留旧 `budget_exhausted` 的 resume 元数据缺陷，已先以失败测试复现，再把最终 reason 收敛为 `completion_satisfied`，两项聚焦回归均绿。下一步重新跑完整离线门禁，通过后只修正该已完成 run 的旧 reason 并做 Board 作品集验收，不再执行真实研究。
- 2026-07-16：Deep 最终 Board 验收与作品集归档完成。完整离线门禁再次以 226 API、74 Board、165 Extension、8 packaged E2E 和全部静态/构建/30/108 evaluation 检查全绿退出；现有 attempt 1 已严格校正为 `completed/completion_satisfied`。结果页 44/44 图片加载、0 failed、0 pending，6 个子问题和采光分支、22 张小红书制图灵感、正式方案案例分区均可读。四张干净视口截图保存为 `.artifacts/portfolio/deep-b4c314a6-{result,questions,inspiration,cases}.png`。没有第二次 retry、第二个 Deep 或 Firecrawl 调用；M36 真实 Quick/Balanced/Deep 作品集主线完成。
- 2026-07-16：启动 M42 发布候选只读范围审计。相对 `98a9a01`，当前有 78 个已跟踪文件变化（7,823 insertions / 1,612 deletions）和 21 个未跟踪文件；4 个已跟踪删除项均属于废止的 Firecrawl 配置/测试链。主要变化集中于 API 工作流与测试、扩展协议与测试。审计不修改生产代码、不调用真实来源、不暂存或提交；下一步逐组核对变更归属、生成物边界和陈旧计划状态。
- 2026-07-16：M42 第二轮只读归属核对完成。未跟踪的 `run_gate.py`/测试、持续加载 E2E fixture、`scripts/accept-xhs.py` 和扩展 `PRODUCT.md` 均有明确 M38–M41 功能或验收归属；作品集 PNG 属于发布证据。`.playwright-cli/*`、两份 pytest XML 和 `.impeccable/live/config.json` 属于工具生成物，不应进入源码发布提交。生产源码、配置与用户文档中已无 Firecrawl 引用，删除链覆盖配置模块、凭据测试、配置脚本和验证入口；模型与来源架构统一为 `gpt-5.6-sol + medium`、Direct Playwright 与 OpenCLI 1.8.6。
- 2026-07-16：M42 安全边界只读核对完成。32 个生产源码/脚本文件的凭据模式命中为 0；两个测试命中均为固定夹具，其中一个来自基线，另一个专门验证签名 URL 脱敏。OpenCLI 固定 `shell=False` 且命令类型受限，浏览器协议仍只有枚举动作/点击目标。暂存区为空，`git diff --check` 通过。已据此把 M6/M35 标为 complete，移除已完成的正常 Chrome、登录态 XHS 和三深度截图外部门槛，并把当前默认模型记录校正为 `gpt-5.6-sol + medium`。
- 2026-07-16：M42 发布证据已冻结到 `docs/release-evidence-2026-07-16.md`：逐项记录三档 Run ID、accepted attempt、终态、覆盖与资产计数、Board 图片加载验收、10 张 PNG 的字节数和 SHA-256，以及 226/74/165/8 的离线门禁基线。Quick 的累计 Trace 明确保留 attempts 0–8 的历史 Firecrawl，但 accepted attempt 9 从 sequence 267 起没有 Firecrawl；Balanced/Deep 累计 Trace 也没有 Firecrawl。生成清单时只读本地 SQLite/文件，没有 Provider、来源或 Firecrawl 调用。
- 2026-07-16：证据查询首次调用系统 `python` 时命中未配置的 Windows Store alias；未安装或改动环境，改用 Codex 工作区内置 Python 只读打开 SQLite 后完成。该偏差不影响证据内容或项目文件。唯一下一步是发布风险复核，重点检查安装/冷启动、默认无 key 路径、迁移与运行期失败边界。
- 2026-07-16：M42 发布风险复核发现并用红绿测试修复两个冷启动缺口。`scripts/setup.ps1` 原先不会因 pip/pnpm/extension build 的 native 非零退出自动停止，现启用 PowerShell 7 native fail-fast；`scripts/start.ps1` 会在 8000 被占用时选择新 API 端口，但 Board 配对地址原先仍硬编码 8000，现由 `run-board.ps1` 注入实际 `ws://127.0.0.1:<ApiPort>/v1/browser`，默认构建仍回退 8000。脚本契约、75 Board tests、typecheck 和 lint 全绿。
- 2026-07-16：其余发布风险边界通过只读复核：应用 lifespan 自动执行 Alembic migration，版本化/未版本化恢复路径有测试；默认 `mock` 不需要 Key；OpenCLI 固定 1.8.6 且 pnpm 只批准 OpenCLI/esbuild 构建脚本；REST 已由 Vite 代理到动态端口。唯一下一步是最终完整离线门禁，不启动真实研究或 Firecrawl。
- 2026-07-16：M42 最终 `scripts/verify.ps1` 完整恢复会话并 exit 0：226 API、75 Board、165 Extension、8 packaged E2E，Ruff/format/Mypy、两端 lint/typecheck/build、30/108 evaluation 全部通过。只有既有 FastAPI TestClient/httpx2 deprecation warning，无失败；没有 Provider、公开来源、小红书或 Firecrawl 调用。证据清单已同步到 75 Board。
- 2026-07-16：提交边界复核完成：83 个 tracked changes、22 个 untracked files、staged 0。源码候选包括 7 个未跟踪的源码/文档文件；10 张 portfolio PNG 保持独立证据包；5 个 pytest/Playwright CLI/Impeccable 文件是可再生成 scratch，应排除。M42 标为 complete；下一步需要用户明确批准后才按“源码/文档”与“证据 PNG”分组暂存或提交。
- 2026-07-16：用户指出运行前检查“太重太杂”。M43 按 Impeccable distill/product 规则把“标题 + 两行能力 + 永久 footer + 两个长按钮”的三层面板压成单层状态带：只回答“研究环境是否就绪”，小红书和精确裁图合并为一句；精确裁图按需启用，刷新降为轻量动作，错误/配对消息只在发生后出现。行为红测先有 5 条预期失败；生产改造后剩 2 条旧文案断言，更新到新契约后完整 Board 75 tests、typecheck、lint、production build 全绿。
- 2026-07-16：M43 真实浏览器复核：1440px 下状态区高 50px、宽 1263px、2 个动作，无嵌套面板；截图状态为“研究环境已就绪 / 小红书可用 · 精确裁图未启用”。窄屏状态区高 114px，`scrollWidth=viewportWidth=375`，无页面横向溢出。首次视觉接管遇到旧 tab id 失效，第二次 user-tab claim 因标签已被当前会话接管而拒绝；改用 `browser.tabs.get` 取得已接管 id 后完成，不属于应用错误。该 UI 增量发生在 M42 全仓门禁之后；若进入发布提交，需先重跑完整 `scripts/verify.ps1`。
- 2026-07-16：启动 M44 证据检视器改版。用户反馈点开后只在侧边展示且显得内容少；代码审计确认根因是 `.source-inspector` 与运行记录共用 500px 宽度，已存在的图像没有进入检视器，项目条件、机制、转译、观察、推断和核验只能单列堆叠。改版限定为前端信息架构：桌面宽幅图像/分析双区，窄屏全屏单列，不新增后端字段或虚构内容。
- 2026-07-16：M44 红绿实现完成。行为测试先证明来源检视器没有图纸预览和“证据分析”区域；生产结构随后复用既有 `previewUrl`、失败回退、来源链接、核验、收藏/拒绝和备注状态，桌面改为图纸证据/证据分析双区，860px 以下切为图片优先单列。完整 Board 75 tests、typecheck、lint、production build 与 `git diff --check` 全绿。
- 2026-07-16：M44 真实浏览器复核通过：1280px 视口下检视器宽 1040px，图纸区约 494px、分析区约 546px，真实 ArchDaily 图片加载成功；390×844 下检视器和页面 `scrollWidth` 均为 390px，图片优先、分析随后，无横向溢出，测试后已清除 viewport override。浏览器接管时先误用不存在的 `browser.tabs.claim`，尺寸检查又误试 locator `boundingBox` 和 tab/playwright `setViewportSize`；均未改变页面，随后使用已支持的 `browser.user.claimTab`、页面 evaluate 和 browser viewport capability 完成复核。
- 2026-07-16：启动 M45 发布前完整离线门禁，目标是用 `scripts/verify.ps1` 覆盖 M43/M44 后的 API、Board、Extension、packaged Chrome E2E 与 evaluation 基线。门禁不使用 Provider Key、不访问真实来源、不调用 Firecrawl；工作树保持 83 个 tracked changes、staged 0，不执行暂存或提交。
- 2026-07-16：M45 完整 `scripts/verify.ps1` 输出 `All ArchResearch checks passed.`：226 API tests、75 Board tests、165 Extension tests、8 packaged Chrome E2E，Ruff lint/format、strict Mypy、两端 lint/typecheck/production build 与 30/108 deterministic evaluation 全绿。唯一警告为既有 FastAPI TestClient/httpx2 deprecation 和 packaged E2E 的 NO_COLOR/FORCE_COLOR 提示；没有 Provider、公开来源、小红书或 Firecrawl 调用。
- 2026-07-16：门禁成功输出后，额外终态轮询返回 `Unknown process id`，说明已结束的统一会话已被工具回收；随后读取应用终端又返回“没有附加终端”。两次诊断都未重新运行门禁或改变文件，最终状态以脚本自身成功哨兵、全部阶段明细和 8/8 E2E 终态为准。M45 标记 complete，staged 仍为 0；下一步等待用户明确批准后再分组暂存/提交。
- 2026-07-16：用户否决当前三档结果的研究深度，并明确暂不处理截图证据包。M46 启动，目标从“增加候选与图纸数量”改为“读取项目文章正文并形成有证据的项目研究”：正文事实、图纸观察、设计机制和可迁移步骤必须分层；缺少正文或具体证据的聚合页不能显示成项目案例。实施前不发起新的真实研究。
- 2026-07-16：用户进一步明确“深度不是多读文章，而是研究强度”。曾考虑的按档位限制正文分析篇数已撤销：所有能进入正式案例的项目都必须读正文；档位只控制因果推演、比较、冲突处理、适用/失效边界和综合论证强度。
- 2026-07-16：M46 完成红绿实现。公共页正文移到导航链接前；正文事实逐字校验后才允许写入项目条件、设计机制和转译；图片线索不再满足正式案例覆盖。新增资产绑定的运行级综合，Quick/ Balanced/ Deep 分别具有可执行的最低强度门，不达标会阻止完成。Board 新增轻量内联“研究结论”，深层比较/冲突/边界默认折叠，研究深度选项也由轮次/图数改写为核心判断、因果推演、综合论证。
- 2026-07-16：M46 验证完成。项目 Python wrapper 曾静默返回非零，改用同一 venv 的 `python -m mypy --strict` 后通过；最终 `scripts/verify.ps1` 输出 `All ArchResearch checks passed.`：228 API、76 Board、165 Extension、8 packaged E2E，Ruff/format/Mypy、两端 lint/typecheck/build 和 30/108 evaluation 全绿。真实浏览器在桌面与 390px 检查深度文案且无横向溢出；没有保存截图，也没有调用 Provider、真实来源或 Firecrawl。旧 Quick/Balanced/Deep 只保留为历史样本，不再作为新强度证明。M47 唯一下一步是新跑一条 Quick 并先审内容，再决定是否放行 Balanced/Deep。
- 2026-07-16：M47 新版真实 Quick 已从 Board 唯一创建，run `bb3a3a86-2a3c-4e54-8873-7c1ccf349a05`；只读 API 核对题目为“旧建筑更新中，如何植入新功能，并组织公共与后勤流线和剖面层次？”，`budget_mode=quick`、attempt 0、来源仅小红书。当前在 `inspecting` 且 checkpoint 持续更新；未创建或 retry 其他 run，Balanced / Deep 继续封锁，也未恢复 Firecrawl。下一步只监控该 run 到终态并审核核心答案、因果链、正文逐字引文绑定与建议强度。
- 2026-07-16：M47 新版 Quick attempt 0 已诚实终止为 `blocked/browser_inspection_incomplete`：0/3、0 formal projects、7 usable、10 retained results，gaps 为 `uncovered_subquestions`、`article_analysis_incomplete`、`research_synthesis_incomplete`、`browser_inspection_incomplete`。没有 retry，也未启动 Balanced / Deep。152 条 Trace 中 OpenCLI 小红书搜索 3 次且均未回退，Direct Playwright/local browser 读取 13 页、`public_page_analysis` 4 次、Firecrawl 0；但正文分析没有为任何资产写入项目条件、设计机制或转译，故无 `research_synthesis`。下一步定位为何已读取正文仍 `enriched=0`，先写失败行为测试，再做最小修复；同一真实 run 暂不 retry。
- 2026-07-16：M47 attempt 0 的两项确定性缺陷已完成红绿修复。公共检索原先不接收工作区 `research_context`，简短主问题会让部分分支丢失“旧工业建筑”类型；现在只把现有 brief/constraints 用于提取 typology 词。公共页扩展裁图失败原先无条件追加 `browser_inspection_incomplete`；现在 Direct Playwright 已成功解析该页时只降级，未解析时仍保持阻断。新增两项行为测试先分别以 `TypeError` 与错误 gap 失败，修复后连同旧阻断回归 3/3 通过。下一步运行完整离线门禁；通过前不 retry 真实 Quick。
- 2026-07-16：M47 修复后的完整 `scripts/verify.ps1` 已输出 `All ArchResearch checks passed.`：230 API、76 Board、165 Extension、8 packaged E2E，Ruff/format/Mypy、两端 lint/typecheck/build 与 30/108 deterministic evaluation 全绿。没有 Provider、真实来源或 Firecrawl 调用。下一步重启 API 载入修复、核对 OpenCLI，然后仅 retry 原 Quick `bb3a3a86-2a3c-4e54-8873-7c1ccf349a05` 一次；不创建第二个 Quick，也不自动进行第二次 retry。
- 2026-07-16：服务重启后 OpenCLI 1.8.6 doctor 全 OK；原 Quick 已唯一 retry 到 attempt 1，未创建第二个 run。新 Trace 已确认可选扩展裁图 `BrowserCommandError` 不再向运行中 coverage 添加 `browser_inspection_incomplete`；Direct Playwright 继续读取页面，且 Fitzgibbon Community Center 的正文分析首次出现 `relevance=2/enriched=1`。运行尚未终止，历史 coverage 仍在 checkpoint 中；下一步只监控 attempt 1 到终态，不允许第二次 retry。
- 2026-07-16：M47 Quick attempt 1 已终止为 `blocked/research_synthesis_incomplete`：0/3、0 formal projects、10 usable、8 verified/partial；`browser_inspection_incomplete` 已消失，attempt 1 的 139 条 Trace 中 Firecrawl 0，未进行第二次 retry。唯一正文 enrichment 只支持 Fitzgibbon Community Center 的面积、年份和委托背景，并明确声明正文没有公共/后勤流线机制，证据门拒绝生成机制与转译是正确行为。新根因是规划把 1/3 Quick 分支设为 `source_trace` 元问题，同时没有独立保留用户要求的剖面层次分支；下一步红测禁止 precedent research 把来源核验设为独立设计子问题，来源核验继续作为所有案例的横向证据门。
- 2026-07-16：precedent 规划元分支修复完成红绿循环。新增 provider 行为测试先证明 prompt 未禁止独立来源分支且 `source_trace` 计划不会被拒绝；生产改为明确“来源核验是每个案例的横向证据要求，不占设计子问题名额”，并对 `source/trace/verification/provenance/rights` 类型的独立 id 做结构化拒绝。两项聚焦测试 2/2 通过；模型仍违约时 workflow 将使用已有 deterministic fallback 的 program / circulation / section。原真实 run 保持 attempt 1 blocked，不做第二次 retry。下一步运行后端完整测试与静态门禁。
- 2026-07-16：M47 最终后端门禁通过：Ruff、format check、strict Mypy、231 API tests 与 `git diff --check` 全绿；只有既有 FastAPI TestClient/httpx2 deprecation warning。首次门禁在 Ruff 发现本轮 6 个 E501 行长后停止，已只换行修复并重跑成功，没有隐藏或重复逻辑失败。原 Quick 保持 attempt 1 `blocked/research_synthesis_incomplete`，没有第二次 retry、第二个 Quick、Balanced/Deep 或 Firecrawl 调用。M47 仍为 in_progress；唯一下一步是在下一次获准的真实验证中创建一条全新 Quick，确认设计分支、正文因果链和综合建议，再决定是否放行 Balanced。
- 2026-07-16：用户批准 M47 下一条全新 Quick 后，已重启 API/Board 载入规划修复；创建前 OpenCLI doctor 显示 daemon 1.8.6 正常但 Browser Bridge 未连接。按官方建议仅重启一次 daemon，复查仍未连接；只读进程检查确认当前 Chrome 进程数为 0。数据库最新 run 仍是旧 `bb3a3a86-2a3c-4e54-8873-7c1ccf349a05` attempt 1 blocked，尚未创建任何新 Quick，也没有 Provider/来源/Firecrawl 调用。下一步等待用户打开装有 OpenCLI 扩展且保持小红书登录态的 Chrome，再 doctor 一次；通过后才唯一创建新 Quick。
- 2026-07-16：用户打开 Chrome 后，OpenCLI 1.8.6 doctor 复查为 daemon / Extension v1.0.22 / Connectivity 全 OK。先确认数据库最新仍是旧 blocked run，随后通过本地产品 API 唯一创建新 Quick `83c9947b-9eb0-4c58-851c-c36900e3b8ff`：同题、`precedent_research`、`budget_mode=quick`、小红书开启、attempt 0；没有第二次提交。下一步只监控该 run 到终态，先核对三个子问题均为设计分支，再审核正文引文、因果链和建议；Balanced / Deep 继续封锁。
- 2026-07-16：新 Quick 的 live planner 已通过首个验收门，三个子问题为 `program-insertion`、`flow-separation`、`sectional-layers`，分别对应功能植入、公共/后勤流线和剖面层次；没有独立来源核验元分支。Run 当前持续处于 `inspecting` 且 checkpoint 正常更新，尚未到终态；下一步继续只读监控同一 run，不重复提交。
- 2026-07-16：新 Quick 中途 Trace 复核：85 events、Firecrawl 0；OpenCLI 小红书、Direct Playwright 和公共页正文分析均在运行。`public_page_analysis` 已有 1 条 `relevance=2/enriched=1`，但最新 gap check 仍为 0 formal projects / 0 covered，说明尚未形成同时具备正文条件、机制和转译的案例；系统继续按三个分支补搜，而非提前完成。下一步继续监控到终态。
- 2026-07-16：恢复轮次进至 query 11/15；最新 gap check 仍为 10 usable、0 formal projects、0/3，且没有新增 `public_page_analysis`，说明 round 3–4 的搜索结果尚未带来可分析的新项目正文。Run 仍在正常推进，未重试或创建其他任务；继续监控最后 4 个查询到终态。
- 2026-07-16：新 Quick `83c9947b-9eb0-4c58-851c-c36900e3b8ff` attempt 0 已终止为 `blocked/browser_inspection_incomplete`：0/3、0 formal projects、10 usable、6 verified/partial、12 retained results；151 Trace、Firecrawl 0，未 retry。规划三分支正确，但仅 SOM Landmark 候选写入项目背景，机制和转译为空。其 limitations 明确记录正文在 `In other areas, Inaba Williams peeled...` 机制段开头被截断；当前 workflow 与 provider 各自仍有 6000 字符上限。下一步先红测正文尾部机制必须进入 `public_page_analysis` 输入，再做最小截断修复；Balanced / Deep 继续封锁。
- 2026-07-16：正文双重截断已完成红绿修复。两条行为测试先分别证明 workflow 和 OpenAI page-analysis prompt 都会丢失 6000 字符后的机制段；生产新增共享 `PUBLIC_PAGE_ANALYSIS_TEXT_LIMIT=12000`，覆盖 Direct Playwright 当前有界正文长度，两个截断点统一使用该上限。聚焦测试 2/2 通过；原真实 Quick 不 retry。下一步运行完整 API 静态与行为门禁。
- 2026-07-16：正文 12k 修复后的后端完整门禁通过：Ruff、format、strict Mypy、233 API tests 全绿；首次两次门禁只因 import 排序停止，最终改用 Ruff 对单文件 import 做机械整理后全绿。下一步在 retry 前红测“单个公共页双后端失败不应覆盖其他已成功 Direct Playwright 页面”，同时保留“全部浏览器读取失败仍阻断”的既有契约。
- 2026-07-16：运行级浏览器失败聚合完成红绿修复。新增混合页面测试先证明一个不可读来源会错误覆盖另一个 Direct Playwright 成功来源；生产现分别跟踪必需视觉来源失败、公共页局部失败和公共页任一成功。单页失败继续留在 Trace，只有必需来源失败或全部公共页均未成功时才追加 `browser_inspection_incomplete`。混合成功、全部失败、同页 Direct fallback 和可选 XHS 四项聚焦回归 4/4 通过；下一步完整 API 门禁，通过前不 retry。
- 2026-07-16：正文 12k 与运行级失败聚合两项修复的完整后端门禁通过：Ruff、format、strict Mypy、234 API tests 全绿；首次门禁仅因本轮一条 E501 长行停止，换行后重跑成功。原新 Quick 仍为 attempt 0 blocked，尚未 retry。下一步重启 API/Board、复查 OpenCLI 后只 retry 同一 run `83c9947b-9eb0-4c58-851c-c36900e3b8ff` 一次；不创建新 run、不自动第二次 retry。
- 2026-07-16：服务重启后 OpenCLI doctor 全 OK；原 Quick `83c9947b-9eb0-4c58-851c-c36900e3b8ff` 已从 attempt 0 blocked 唯一 retry 到 attempt 1。当前在 `inspecting/searching` 间正常推进，运行中 API 仍暂存 attempt 0 coverage，尚不能用作终态判断。没有新建 run 或第二次 retry；下一步只监控 attempt 1 到终态。
- 2026-07-16：attempt 1 中途 Trace 至 query 7/15：新增 64 events，3 次正文分析均完成但未形成 article-ready 案例；SOM 页在 12k 输入后被判 `relevance=1/enriched=0`，说明截断修复生效后模型仍认为它与旧工业建筑功能植入不够匹配，而非再次因截断产生字段更新。当前 13 usable、0/3、无终态；继续监控，不第二次 retry。
- 2026-07-16：新 Quick attempt 1 已终止为 `blocked/research_synthesis_incomplete`：14 usable、10 verified/partial、0 formal projects、0/3；最终 gaps 已不含 `browser_inspection_incomplete`，证明失败聚合修复生效。attempt 1 有 135 Trace、Firecrawl 0、仅 1 个 Direct Playwright 页面局部失败；四次正文分析相关度均为 0/1，未生成综合。没有第二次 retry。离线复现真实短查询后确认 `program-insertion` 因问题中提到“剖面图”被 `_public_issue_focus` 优先路由成 `old new structure daylight void section`，丢失 `program insertion` 意图。下一步红测并修复分支意图优先级。
- 2026-07-16：功能植入检索意图路由完成红绿修复。行为测试先复现包含“插入/嵌套/盒体 + 平面/剖面”的子问题被错误路由为泛化剖面；生产在流线之后、采光/剖面之前识别强功能植入词，英文查询现包含 `program insertion box-in-box inserted volume independent structure exhibition workshop public activity floor plan section`。新测试与既有多域流线/采光恢复测试 2/2 通过。原 run 已用完唯一 retry，不再真实运行；下一步完整 API 门禁。
- 2026-07-16：功能植入路由修复后的完整后端门禁通过：Ruff、format、strict Mypy、235 API tests 全绿；只有既有 FastAPI TestClient/httpx2 deprecation warning。随后一次最终只读 `git diff --check` / staged-count 请求因审批服务 429 被拒绝，没有绕过或重试，也未执行任何暂存/提交。新 Quick 保持 attempt 1 `blocked/research_synthesis_incomplete`，没有第二次 retry、Balanced/Deep 或 Firecrawl。M47 仍 in_progress；唯一下一步是在下一次获准的全新 Quick 中验证修正后的功能植入查询能否找到正文机制，并重新审核 3/3 与研究综合。
- 2026-07-17：按 HANDOFF 恢复 M47；系统 `python` 是不可用的 Store 空别名，因此没有重复 session-catchup 命令，改以完整交接文件、活跃计划、记录末尾和工作树状态恢复。API/Board 已用 `scripts/stop.ps1` 与 `scripts/start.ps1` 在 8000/5173 重启并通过健康检查，最新路由生效；工作树既有修改全部保留，未暂存、未提交。
- 2026-07-17：创建前唯一一次 `pnpm opencli -- doctor` 返回 daemon 1.8.6 正常、Browser Bridge missing。只读 Chrome 诊断确认 Chrome 当前未运行，浏览器与相关 ChatGPT 扩展/native host 安装正常；没有创建新 Quick、retry 旧 Run、启动 Balanced/Deep、调用 Provider/真实来源或 Firecrawl。下一步必须先获得用户对打开 Chrome 的明确许可；打开后只做一次新的连接确认，通过才创建全新 Quick。
- 2026-07-17：用户明确许可后已打开已配置的 Chrome，OpenCLI doctor 显示 daemon、Extension v1.0.22、Connectivity 全 OK。确认同一工作区没有活跃 Run 且最新仍为旧 blocked run 后，只创建一条全新 Quick `fc3af6d4-2610-4319-9e0c-25e895d7f844`：同题、`precedent_research`、Quick、仅开启小红书可选视觉来源、attempt 0；没有 retry 或第二次提交。
- 2026-07-17：新 Quick 已在唯一 attempt 0 终止为 `blocked/research_synthesis_incomplete`：10 usable、0 formal projects、0/3；三个设计分支正确。Trace 为 13 次公共检索、13 个公共页、4 次正文分析、3 次小红书检索、Firecrawl 0；四次正文分析均未升级为 article-ready 案例，因此没有综合。Balanced/Deep 继续封锁且本 Run 不 retry；下一步定位相关正文为何没有产出条件/机制/转译，先写行为红测再做最小修复。
- 2026-07-17：M47 失败诊断完成。用生产 Direct Playwright 复读首轮命中的 `Reuse of an Industrial Space`，正文包含强机制但 drawing 列表为空；有限 DOM 检查确认图纸类型只在 `intrusswetrust-axo.jpg` 与 `intrusswetrust-seccion-1.jpg` 文件名中，通用英文类型词表没有识别。最初的语义 caption 假设未命中真实页，相关测试和生产改动已撤回，没有留下无效扩展。
- 2026-07-17：多语言 ArchDaily 图纸文件名修复完成红绿循环：3 个 `seccion/axonometria/-axo.` 类型测试先 3/3 失败，词表最小扩充后 3/3 通过；既有项目页正文分析集成测试改用真实西语 section 文件名并通过。真实页面零 Provider 复核识别出 axonometric 与 section。最终 Ruff、format、strict Mypy、全量 238 API tests 与 `git diff --check` 全绿；只有既有 TestClient/httpx2 弃用警告，staged 0。失败 Run 不 retry，Balanced/Deep 继续封锁。
- 2026-07-17：按获准的 M47 唯一下一步重启 API/Board，健康检查分别为 `ok` / HTTP 200；唯一一次 OpenCLI doctor 显示 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。只读核对确认无活跃 Run 后，仅创建全新 Quick `bf32f7f3-0a5b-4642-8481-94bbc5c2acc2`，同题、`precedent_research`、小红书可选视觉来源、attempt 0；没有 retry `fc3af6d4-...`，没有 Balanced/Deep、Firecrawl 或截图。规划首门已通过：`program_insertion`、`flow_separation`、`sectional_hierarchy` 均为设计分支；当前只读监控到终态并审核正文因果链与 Quick 综合。恢复时曾误查不存在的 `/v1/runs?limit=20` 得到一次 404，随后改用已定义的 workspace-scoped 列表端点，没有重复该错误。
- 2026-07-17：新 Quick `bf32f7f3-0a5b-4642-8481-94bbc5c2acc2` attempt 0 已终止为 `blocked/no_new_assets`，没有 retry 或第二条 Run。最终 15 usable、1 formal project、7 verified/partial、1/3，仅 `program_insertion` 覆盖；129 条 Trace 包含 12 次 Direct Playwright 公共检索、11 个公共页、4 次正文分析、3 次 OpenCLI 小红书检索和 0 Firecrawl。`L264 Office` 以逐字引文形成“工业作坊语境 → 分区顺应规则柱网 → 3 条有边界转译”，Quick 综合成功绑定该资产并诚实声明流线/剖面证据不足；目标页 `Reuse of an Industrial Space` 图纸识别成功，但正文分析一次超时。终态审计离线复现 `sectional_hierarchy` 被问题中的“屋顶加建”误路由为 program-insertion 查询；Balanced/Deep 继续封锁。唯一下一步是在另一次明确批准后先写该路由行为红测，再做最小修复与离线门禁；当前不再发起真实运行。
- 2026-07-17：按用户要求把 M47 路由修成全局规则，而非针对单题补丁。新增六组中英重叠意图行为矩阵，首轮红测在剖面层次含“屋顶加建”时稳定失败；生产改为流线/采光/功能植入/剖面层次的加权主意图判断，并为剖面生成专属垂直空间查询词。自审又以红测补住普通英文 `section + vertical relationship` 的低权重回退，避免替换旧规则时造成隐性回归。最终相邻聚焦测试 4/4、Ruff lint/format、strict Mypy 和全量 239 API tests 全绿；只有既有 TestClient/httpx2 弃用警告。没有 Provider、真实来源、小红书、Firecrawl、retry、新 Run 或截图。M47 保持 in_progress，下一步需明确批准后再以一条全新 Quick 验证 3/3，Balanced/Deep 继续封锁。
- 2026-07-17：用户批准全局路由修复后的唯一新 Quick 验证。API/Board 重启后健康检查为 `ok` / HTTP 200，staged 0；本轮唯一一次 OpenCLI doctor 显示 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。两次 PowerShell 全工作区 guard 都因嵌套数组产生无效结果，均在 POST 前拒绝；替代的只读 SQLite guard 确认 18 条历史 Run 全部终态、active 0。随后仅创建 Quick `9a72a8b2-b6d4-41c4-9a35-80bcad5bcba9`，attempt 0；规划为 `program_insertion`、`circulation_separation`、`sectional_hierarchy` 三个设计分支。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步核对三分支真实短查询并只读监控同一 Run 到终态。
- 2026-07-17：Quick `9a72a8b2-b6d4-41c4-9a35-80bcad5bcba9` attempt 0 已终止 `blocked/research_synthesis_incomplete`，没有 retry 或第二条 Run。三条真实短查询全都符合全局意图规则；`Reuse of an Industrial Space` 成为 1 个高质量正式案例，但最终 15 usable、7 verified/partial、1/3，仅功能植入覆盖。128 条 Trace 中 12 次公共检索、11 个 Direct Playwright 公共页、4 次正文分析、3 次 OpenCLI 小红书检索、Firecrawl 0；另记录小红书资产和 Designboom 正文各一次 `APIConnectionError`、第四轮剖面搜索一次 Error、最终综合一次 `APITimeoutError`。第三次 PowerShell 数组型 API 审查仍产生嵌套对象，已彻底停止该路径并改用 Python JSON，错误输出未用于结论。离线代码审查确定下一根因是缓存直接项目页只在 `parsed_now` 时做分支正文分析；下一步需明确批准后红测并实现按 `(source_url, subquestion_id)` 去重的缓存页分支重分析，不能直接复用旧结论或重跑真实 Run。Balanced/Deep、Firecrawl 和截图继续封锁。
- 2026-07-17：获准的缓存直接页分支重分析完成红绿实现。三组门控红测先全部失败于分析调用仅 1 次；生产随后复用 cached `ParsedPublicPage`/drawings，并按 `(source_url, subquestion_id)` 一次性调用正文分析。首分支拒绝、末分支拒绝和三分支全支持均按各自 branch analysis 决定 coverage；额外恢复轮测试证明搜索可重复但同分支分析不重复。相邻旧测试唯一失败是原“总分析 1 次”断言，已更新为“解析 1 次、三个新子问题各分析 1 次”。自审又红绿补齐综合输入的分支条件/机制/转译/逐字证据字段。最终 Ruff lint/format、strict Mypy、相邻 8 项、综合 7 项及全量 243 API tests 全绿；仅既有弃用 warning。没有任何真实调用、Run、retry、Balanced/Deep、Firecrawl 或截图。M47 保持 in_progress；下一步需明确批准后重启服务、只复查一次 OpenCLI，并创建一条全新 Quick 验证 3/3 与综合。
- 2026-07-17：获准的唯一 Quick `065d4a8a-aa83-42f2-b983-983037a1b5b1` 已在 attempt 0 诚实终止 `blocked/research_synthesis_incomplete`，没有 retry 或第二条 Run。终态为 16 usable、3 formal projects、2/3；缓存页对新分支重分析且不重新解析的真实验收通过，但流线仍无正文覆盖，研究综合又一次 `APITimeoutError`。只读诊断确认 `_compact_site_query()` 把流线和剖面都压缩成 `community center`，覆盖了上游正确的全局意图路由；106 条 Trace 中 Firecrawl 0。已停止真实执行，Balanced/Deep 继续封锁。唯一下一步是先为站内查询压缩器补全局中英重叠意图红测，使其保留“工业改造 + 功能/流线/剖面”的主意图，再做最小修复与离线门禁；修复前不得发起新的真实 Run。
- 2026-07-17：站内查询压缩器的第一轮全局红测已建立：9 组中英功能植入/流线/采光/剖面与社区类型重叠输入全部按预期失败。旧实现会丢失工业类型或分支主意图，并被 `vertical circulation` 与统一恢复词 `separate entrance` 抢占成 `community center`。下一步是将 workflow 现有加权意图判定提取为无循环依赖的共享纯函数，再让两层共用。
- 2026-07-17：全局压缩路由已进入绿阶段：加权意图判定收敛为 `public_pages.infer_research_issue_intent()` 共享纯函数，workflow 丰富查询与站内短查询共用同一决策；短查询另外保留工业改造/社区文化中心类型。9 组新红测、既有 6 组 workflow 重叠意图和全部 public-pages 相邻测试已绿；五轮离线重放均稳定产生“industrial reuse/工业改造 + program/flow/section 主意图”。Ruff lint 与 strict Mypy 已通过；首次 format check 仅要求新测试规范排版，机械格式化该文件后复查通过。下一步运行全量 API 离线测试。
- 2026-07-17：站内短查询全局修复的最终离线门禁完成：回归矩阵扩为 10 组并补齐恢复轮噪声与工业/社区双类型，Ruff lint/format、API 项目 strict Mypy、253 API tests 和 `git diff --check` 全绿，仅既有 TestClient/httpx2 弃用 warning。一次从仓库根目录误调 Mypy 因未读取 `apps/api/pyproject.toml` 报出既有假错，改为规定的 `apps/api` 工作目录后严格门禁通过，没有因此修改无关综合代码。本阶段没有 Provider、真实来源、小红书、Firecrawl、Run、retry 或截图，staged 为 0。M47 仍为 in_progress；唯一下一步是在另一次明确批准后重启 API/Board、唯一复查 OpenCLI 并且只创建一条全新 Quick，验证三分支真实站内短查询、3/3 正文覆盖与资产绑定综合；Balanced/Deep 继续封锁。
- 2026-07-17：用户批准的唯一真实 Quick 验证在创建前预检暂停。`scripts/stop.ps1` 提示旧端口当时无匹配进程，随后 `scripts/start.ps1` 正常启动 8000/5173，健康检查为 `ok` / 200。本轮唯一 doctor 检查为 daemon 1.8.6 正常但 Browser Bridge missing；Chrome 进程数 0，工作区 active Run 0，最新仍是 blocked `065d4a8a-...`。未重启 daemon、未重复 doctor、未 POST Quick，没有 retry、Balanced/Deep、Provider、真实来源、Firecrawl 或截图。唯一下一步是等待用户打开已安装 OpenCLI Bridge 且保持小红书登录态的 Chrome；打开后只做一次新的连接确认，通过才创建唯一新 Quick。
- 2026-07-17：用户打开 Chrome 后，唯一一次新 OpenCLI 连接确认为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON guard 确认同一工作区 14 条历史 Run 均终态、active 0，随后仅 POST 一次创建 Quick `b7c91d0d-42c9-429a-ae4f-11368c9caa0d`，attempt 0。规划首门已通过：`program_insertion`、`flow_separation`、`sectional_layers` 均为设计分支；当前只监控该 Run 到终态，不提交第二条、不 retry、不启动 Balanced/Deep。
- 2026-07-17：Quick `b7c91d0d-...` 的首个 `program_insertion` 分支已完成首轮检索与页面读取：ArchDaily 返回 Public Library in Labin、Blue School、COOP incubator 和 Walker Hall，两次正文分析均未形成 article-ready 提升；这是真实召回质量结果，没有放宽证据门。Run 正在检视 `flow_separation`，当前 Trace 中 Firecrawl 0，继续只读监控同一 Run。
- 2026-07-17：同一 Quick 的 `flow_separation` 首轮已产生两组正文提升：Australian Red Cross Processing Centre 为 `relevance=2/enriched=1`，De Nederlandsche Bank 为 `relevance=4/drawing_count=2/enriched=2`，表明新站内短查询已不再只返回普通社区中心。`sectional_layers` 已进入页面检视，当前首两页尚无正文提升；Firecrawl 仍为 0。
- 2026-07-17：同一 Quick 的中期 gap checkpoint 为 16 usable、1 formal project、12 verified/partial、1/3，仅 `flow_separation` 达到正文覆盖；`program_insertion` 与 `sectional_layers` 仍缺条件—机制—转译链。正常两轮后 workflow 已进入 completion recovery，只搜索未覆盖分支，没有放宽门槛或启动其他 Run。
- 2026-07-17：Quick `b7c91d0d-...` 已在唯一 attempt 0 诚实终止 `blocked/research_synthesis_incomplete`，没有 retry 或第二条 Run。终态为 20 usable、1 formal project、12 verified/partial、1/3，仅流线覆盖；12 条真实站内短查询全部保持正确类型与主意图，证明全局压缩路由修复通过真实验收。失败点收敛为功能/剖面的公共页召回与图纸准入，以及最终综合一次 `APIConnectionError`。唯一正式项目 De Nederlandsche Bank 的流线分支保留 6 段逐字引文；Trace 中 Firecrawl 0。一条只读终态审计命令又在 `apps/api` 目录中误用根相对 venv，命令在 Python/网络/SQLite 前停止；改用 app-local `.venv` 后审计完成。已停止真实执行，Balanced/Deep 继续封锁；下一步只能离线诊断正确短查询为何仍未让功能/剖面到达 article-ready 图纸。
- 2026-07-17：新一轮 M47 恢复已按 HANDOFF 顺序完成；planning-with-files 的示例命令再次命中不可用的 Windows Store `python` 别名，未执行脚本或项目逻辑，随后改用已验证的 `apps/api/.venv/Scripts/python.exe` 成功完成 catch-up。分支仍为 `codex/archresearch-v2-1`，staged 0，大量既有修改与未跟踪文件全部保留。当前唯一动作仍是离线红测“弱公共页不得永久耗尽较晚强项目页的远程图像分类机会”，通过完整门禁前不创建新 Run。
- 2026-07-17：只读代码诊断确认当前远程图像分类是“首个符合表面准入的具体项目页立即消费”，且当前 attempt/子问题一旦出现 `started` 就阻止任何后续页面，哪怕首批 `added=0`。下一步以两页顺序夹具建立红测：首个可信具体页只有照片，第二个可信具体页含问题相关图纸与逐字正文；只允许一个实际分类批次，且必须落到第二页并触发正文分析。
- 2026-07-17：两页行为红测按预期失败：唯一远程批次的 4 个候选全部来自首个弱页，第二个强页图像不在调用中，正文分析为 0。失败精确锁定批次分配而非证据校验。另一次只读 SQLite 输出因 Windows GBK 无法编码项目名中的 `Ž` 而停止，改为显式 UTF-8 后完成；真实 Trace 同样显示 `sectional_layers` 首批 classified 4 / added 0，后续三个公共页均再无分类机会。
- 2026-07-17：跨公共项目页的单批轮转抽样已完成红绿实现。每个 attempt/子问题仍最多一次远程调用、最多 4 图；分类结果回写各自 source/page，随后只为获得图纸的可信项目执行分支正文分析。新增红测、14 个批次/文章/缓存相邻回归、整份 browser-inspection 测试、完整 Ruff lint/format、strict Mypy、254 API tests 和 `git diff --check` 全部通过；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步重启 API/Board、只检查一次 OpenCLI、用 Python JSON 确认 active 0，然后创建一条全新 Quick。
- 2026-07-17：API/Board 已重启并加载跨页批次修复，健康检查为 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON guard 确认同一工作区 15 条历史 Run 全部终态、active 0，随后仅创建全新 Quick `fe0aea3a-1908-4409-9aa6-013f440312df`，attempt 0、同题、`precedent_research`、小红书可选视觉来源。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控该 Run 到终态并核对 3/3、跨页远程批次和资产绑定综合。
- 2026-07-17：新 Quick 的三项规划仍为 `program_insertion`、`flow_separation`、`sectional_hierarchy`。功能植入首轮已解析四个 ArchDaily 项目；Blue School 依靠原已标注图纸先完成 `relevance=3/drawing_count=1/enriched=1` 的正文分析，其余页面继续等待查询末尾的跨页远程批次。四次可选扩展精确裁图均为 `BrowserCommandError`，但 Direct Playwright 正文读取成功，因此不覆盖正式公共研究；继续监控同一 Run。
- 2026-07-17：跨页批次已在前两分支真实执行：功能植入为 4 图/4 页但上游 `InternalServerError`，仍由 Blue School 正文覆盖；流线为 4 图/3 页、classified 2 / added 1，证明多个具体项目共享唯一批次。流线另有 Librairie Avant-Garde `relevance=2/drawing_count=4/enriched=4`，但当前正式 coverage 仍只有功能植入 1/3，说明该页尚未满足完整条件—机制—转译门。Run 正进入剖面分支，没有 retry 或第二条 Run。
- 2026-07-17：全新 Quick `fe0aea3a-1908-4409-9aa6-013f440312df` 已在唯一 attempt 0 终止 `blocked/no_new_assets`，不得 retry。最终 16 usable、9 verified/partial、1 formal project、1/3，仅 `program_insertion`；跨页批次真实覆盖功能 4 页、流线 3 页、剖面 2 页，证明全局分配修复生效。功能与剖面分类各遇一次上游 `InternalServerError`，流线 classified 2 / added 1，但没有形成完整正文链。资产绑定综合成功并只使用 Blue School，明确声明流线/剖面证据不足。下一步只读审计结果与来源页面，定位下一项确定性全局缺陷后继续红绿循环。
- 2026-07-17：终态来源审计找到下一项可离线修复的全局缺陷：英文恢复域结果排序没有使用已经正确生成的英文短查询，而是继续与中文研究上下文比较，导致 Designboom/Dezeen 无关新文章与真正的 textile-warehouse 项目同分，前者先耗尽每分支恢复页容量。不得放宽正文门或针对站点标题打补丁；下一步先红测“异语言研究问题 + 英文恢复查询 + 混合站内结果”必须优先检查与查询重叠的具体项目页。只读 SQL 曾误用不存在的 `query_attempts.sequence` 列，随后通过 `pragma table_info` 改按 `created_at` 完成，数据库无写入。
- 2026-07-17：进一步代码复核确认英文查询已进入排序上下文，真正缺口是字面 token 无法关联 `industrial reuse` 与 `convert textile warehouse`；同时通用展开把单段 `/architecture/` 频道根页误认为项目链接。下一红测范围收敛为公共来源准入的两条全局规则：工业/仓库/工厂/改造同义词必须提高真实项目排序；频道、栏目或站点根页不得扩展为项目。证据门、域轮转、每分支页预算和 Provider 权限保持不变。
- 2026-07-17：公共来源准入两组红测先 2/2 失败，随后转绿：站内结果与 workflow 共用类型/意图相关性评分，功能、流线、采光、剖面四类工业改造同义项目均排在无关新闻前；非 ArchDaily 路径标记必须带具体后续 slug，频道根页不再占项目扩展槽。30 条 public-pages 测试、4 条恢复排序相邻测试及 workflow 同义词页槽测试均通过。Ruff lint 通过，format check 只要求机械排版新增参数化测试；格式化后进入完整离线门禁。
- 2026-07-17：公共来源准入修复的最终离线门禁完成：完整 Ruff lint/format、strict Mypy、259 API tests、`git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步重启 API/Board、复查一次 OpenCLI 和 active guard，再只创建一条全新 Quick；`fe0aea3a-...` 及所有旧 Run 均不得 retry，Balanced/Deep 继续封锁。
- 2026-07-17：API/Board 已重启并加载共享相关性与具体 slug 准入规则，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。单个 Python JSON 守卫确认 16 条历史 Run、active 0 后只 POST 一次，创建全新 Quick `080d60e9-714a-4f87-b4e0-49bad1394fac`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控该 Run 到终态并审核恢复域项目排序、3/3 与资产绑定综合。
- 2026-07-17：新 Quick 首轮已达 2/3：Australian Red Cross 形成 `flow_separation` 正文覆盖；Amaya Headquarters 与 Antao Headquarters 的剖面分析使 `section_layers` 覆盖，剖面跨页批次为 4 图/4 页、classified 2 / added 1。当前 12 usable、2 formal projects、12 verified/partial，仅缺 `program_insertion`；功能首轮的 Office Building Transformation 正文分析曾 `APITimeoutError`，其余功能项目未通过正文门。Run 继续恢复轮，不 retry。
- 2026-07-17：Quick `080d60e9-714a-4f87-b4e0-49bad1394fac` 已在唯一 attempt 0 终止 `blocked/research_synthesis_incomplete`，不得 retry。最终 12 usable、12 verified/partial、2 formal projects、2/3，覆盖流线与剖面；Designboom 恢复结果准确到达钢厂与铁路仓库项目，但功能唯一批次已在首轮消费，故后到强页无法分类。最终综合另有 `APIConnectionError`；108 条 Trace、Firecrawl 0。新的跨轮红测先失败于唯一调用仍只含弱页，生产改为按子问题累计到最后计划查询后转绿；10 个远程批次相邻测试和整份 browser-inspection 测试通过。
- 2026-07-17：跨轮候选择优的最终离线门禁完成：正常轮弱页与恢复轮强页红测、15 个批次/缓存/正文相邻回归、整份 browser-inspection 测试、完整 Ruff lint/format、strict Mypy、260 API tests 和 `git diff --check` 全绿，staged 0。每 attempt/子问题仍只有一次最多 4 图调用；非文章研究模式保持即时分类。下一步重启服务、复查一次 OpenCLI 与 active guard，再创建一条全新 Quick。
- 2026-07-17：API/Board 已重启并加载跨轮候选择优，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON 守卫确认 17 条历史 Run、active 0 后只创建全新 Quick `98a0b853-3531-479e-a308-dc3341ffd787`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控跨轮最终批次、3/3 与资产绑定综合。
- 2026-07-17：Quick `98a0b853-3531-479e-a308-dc3341ffd787` 已在唯一 attempt 0 终止 `blocked/budget_exhausted`，不得 retry。最终 14 usable、14 verified/partial、2 formal projects、2/3，覆盖功能与剖面，缺流线；跨轮最终批次在第 5 轮真实执行为 4 图/4 页，证明时机修复生效，但唯一远程调用遇 `InternalServerError`。资产绑定综合成功，106 条 Trace、Firecrawl 0。下一步只读重建该批次的 4 个页面/图像候选，判断能否扩大确定性图纸类型识别、减少对不稳定远程视觉的依赖。
- 2026-07-17：首次五页 Direct Playwright 重建在第三个 AKQA 页的 15 秒 `Page.goto` 边界超时，未产生输出或写入。按不重复失败规则放弃该批量路径，不 retry AKQA；改为独立复读两个恢复轮 Designboom 页面并逐页输出 URL/alt/确定性类型，继续保持零 Provider、零 Firecrawl、零 Run。
- 2026-07-17：两个 Designboom 恢复页独立复读成功，确认唯一批次被全页媒体污染：候选混入 250×200 相关文章新闻图、其他项目 masterplan 缩略图和 consentmanager 隐私 SVG；铁路仓库页唯一确定性 `site_plan` 正是无关总体规划新闻图。下一步红测并修复 Playwright 页面媒体语义根边界，保留无语义页面回退；不修改远程调用次数或正文证据门。
- 2026-07-17：媒体语义根红绿完成。首轮“最长任意语义根”虽通过基础假 DOM，但实页仍选择包含相关新闻的 `main`；只读根审计确认正文级 article 与短卡片可按 1000 字规模分离。增强红测先失败后转绿，生产现优先最长正文级 article，无该根才回退最长语义根/全页。铁路仓库零 Provider 复验只剩 12 张正文项目图，4 个批次候选均为当前项目，错误类型与隐私/新闻污染归零。下一步运行完整公共页、静态与 API 门禁。
- 2026-07-17：媒体语义根最终离线门禁完成：32 条 public-pages 测试、完整 Ruff lint/format、strict Mypy、262 API tests 和 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步重启服务、复查一次 OpenCLI 与 active guard，再创建一条全新 Quick；所有旧 Run 均不得 retry。
- 2026-07-17：API/Board 已重启并加载正文媒体语义根，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON 守卫确认 18 条历史 Run、active 0 后只创建全新 Quick `c41c0cb8-0c3c-4116-b97d-142403ca70b7`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控正文图候选、3/3 与资产绑定综合。
- 2026-07-17：Quick `c41c0cb8-0c3c-4116-b97d-142403ca70b7` 已在唯一 attempt 0 终止 `blocked/research_synthesis_incomplete`，不得 retry。最终 5 usable、0 formal projects、0/3；三个跨轮批次均成功完成（added 0/2/2），证明清洁媒体消除了本轮 `InternalServerError`，但 ArchDaily 图片数从 28–40 降至 4–9 后丢失了正文根外的项目图库图纸，所有文章门无法形成完整链。154 条 Trace、Firecrawl 0。下一步红测“正文根图片 + 标题强匹配图库图片”的安全并集，不恢复无关全页媒体。
- 2026-07-17：标题强匹配安全并集的真实页复验通过：Designboom 铁路仓库保留 16 图且无 Louvre/consent/250×200 污染，ArchDaily Australian Red Cross 保留 15 图并恢复 elevation/section，无 Parramatta 跨项目污染。本轮实页只读没有 Provider、Run、Firecrawl 或截图；下一步补项目缩写的确定性 DOM 测试，然后运行全部离线门禁。
- 2026-07-17：安全并集最终离线门禁完成：新增 `ARCBS` 缩写图库图纸恢复/同事务所跨项目排除回归，33 条 public-pages 测试、完整 Ruff lint/format、strict Mypy、263 API tests 和 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步重启 API/Board，本轮只复查一次 OpenCLI 和 active-run guard，然后只创建一条全新 Quick。
- 2026-07-17：API/Board 已重启并加载安全并集，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON 守卫确认 19 条历史 Run、active 0 后只创建全新 Quick `023d083e-449e-403e-a053-e8e060def8f5`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控该 Run 到终态，审核 3/3 和资产绑定综合。
- 2026-07-17：Quick `023d083e-449e-403e-a053-e8e060def8f5` 已在唯一 attempt 0 终止 `blocked/research_synthesis_incomplete`，不得 retry。终态 16 usable、2 projects、1/3，仅 `sectional_hierarchy` 覆盖；功能/流线最终批次均是 4 图/4 页并成功 added 3/2，但钢厂功能页正文分析 `InternalServerError`，Carnegie Library 流线页为 relevance 2/drawing 0/enriched 0，最终仍缺完整正文链。下一步只读终态审计批次图片归属与强页分析机会，然后红测全局容错；不放宽证据门。
- 2026-07-17：跨页批次项目身份红测已稳定复现：四个项目页各含一张标题强匹配当前项目图和一张尾部相关新闻图，现实批次 4/4 错选 `unrelated-school-news`。下一步在页内候选上按项目身份分数稳定排序，同分保留现有确定性类型/等距抽样顺序，然后先跑相邻批次回归。
- 2026-07-17：跨页项目身份抽样红绿完成。公共页标题强匹配与 workflow 现共用 `project_image_identity_score()`；每页候选先按项目身份分数稳定降序，同分保留原确定性图纸类型和等距抽样顺序，已删除尾图无条件置顶。新红测与跨页强图、单页画廊、安全并集/缩写四类相邻回归 5/5 全绿；调用次数、批次上限、页轮转、指纹和正文门未改。下一步复验真实 Carnegie/Designboom 首选，再跑完整离线门禁。
- 2026-07-17：项目身份抽样最终离线门禁完成：真实 Carnegie 首选从相关新闻切回当前项目，Designboom 同分候选稳定保序；完整 browser-inspection + public-pages 相邻测试通过，最终源码态 Ruff lint/format、strict Mypy、264 API tests、`git diff --check` 全绿，staged 0。仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步重启 API/Board、本轮只检查一次 OpenCLI 与 active guard，再创建一条全新 Quick。
- 2026-07-17：API/Board 已重启并加载项目身份抽样，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON 守卫确认 20 条历史 Run、active 0 后只创建全新 Quick `d7ac516c-ba3d-4102-a061-2b27add09fe4`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控该 Run 到终态。
- 2026-07-17：Quick `d7ac516c-ba3d-4102-a061-2b27add09fe4` 已在唯一 attempt 0 终止 `blocked/budget_exhausted`，不得 retry。终态 17 usable、1 formal project、1/3，仅 `flow_separation` 覆盖；功能批次 4 图/4 页、added 3，Office Building Transformation 正文 relevance 4 但 drawing 0/enriched 0；剖面批次 4 图/4 页、added 3，LYCS 强页正文分析 `APITimeoutError`，ATX Factory relevance 2/drawing 0。项目身份抽样已让流线形成正式链，但下一层需审计分类资产的页归属与分类后正文分析门控；不放宽证据标准。
- 2026-07-17：当前项目后代图库红测已建立：假 DOM 的正文 article 之外同时含当前项目媒体子路径的 thumb/空 alt 和 medium/有 alt 变体，以及其他项目链接。旧逻辑按预期未补回 medium 图；下一步仅扩展安全并集的结构准入与同链接变体去重。
- 2026-07-17：当前项目严格后代图库准入已转绿：原始 DOM 图片记录最近链接，标题强匹配或同主机当前页严格后代链接才能补入，同一媒体链接优先有 alt/非 thumb 变体。假 DOM 3/3 和真实 Office/Designboom 复验通过；Office 恢复 section，无跨项目污染。下一步红测并排除明确 loader/spinner/placeholder 占位图，然后运行完整离线门禁。
- 2026-07-17：明确占位图过滤红绿完成：最终快照仅排除 URL/alt 中独立词元 `loader|loading|spinner|placeholder`，不按扩展名泛化屏蔽 GIF。33 条 public-pages 测试全绿。真实 Office 最终 17 图、占位词全 0 且保留 section；ATX 为 16 张项目图但确定性图纸仍为 0，证明纯照片项目不会被误升级。下一步完整离线门禁。
- 2026-07-17：当前项目严格后代图库 + 占位图过滤的最终离线门禁完成：完整 browser-inspection/public-pages 相邻测试、Ruff lint/format、strict Mypy、264 API tests 和 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步重启 API/Board、本轮唯一 OpenCLI 与 active guard，再创建一条全新 Quick。
- 2026-07-17：API/Board 已重启并加载当前项目后代图库，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON 守卫确认 21 条历史 Run、active 0 后只创建全新 Quick `604cb714-a072-40a9-8498-f5f02072b61b`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控到终态。
- 2026-07-17：Quick `604cb714-a072-40a9-8498-f5f02072b61b` 已在唯一 attempt 0 终止 `blocked/budget_exhausted`，不得 retry。终态 35 usable、1 formal project、1/3，仅 `flow_separation` 覆盖；三个最终批次 added 4/4/3，后代图库让正文分析候选显著增加，但 program 与 section 仍未形成完整链，LYCS 剖面页连续第二轮 `APITimeoutError`。下一步只读审计 Firecrawl/综合结果、文本长度与耗时，然后红测全局请求边界。
- 2026-07-17：终态审计确认该 Run 168 条 Trace、Firecrawl 0，资产绑定综合成功（1 case/1 conflict）。长度对比显示失败与成功页在 8.7k–11.7k 有重叠，不做破坏证据的硬截断。下一红测把全局 public-page analysis 单次边界/worst-case 从 30 秒提至 45 秒，其他调用不变。
- 2026-07-17：公共页正文 45 秒边界红绿完成：两个 Provider 测试先精确失败于实际 request timeout/worst-case 均仍是 30，生产新增 `OPENAI_PAGE_ANALYSIS_TIMEOUT_SECONDS=45`，仅 `analyze_public_page()` 与它的 deadline 属性使用。客户端、规划、检索、综合仍为 30 秒/0 retry，调用次数未变；3 条 Provider 相邻测试全绿，并保留 6,500 字后机制证据。下一步完整离线门禁。
- 2026-07-17：正文 45 秒边界最终离线门禁完成：Ruff lint/format、strict Mypy、264 API tests 和 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步重启 API/Board、本轮唯一 OpenCLI 与 active guard，再创建一条全新 Quick。
- 2026-07-17：API/Board 已重启并加载正文 45 秒边界，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON 守卫确认 22 条历史 Run、active 0 后只创建全新 Quick `214fe57f-6df3-49a9-a297-c285c15205dd`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控到终态。
- 2026-07-17：Quick `214fe57f-6df3-49a9-a297-c285c15205dd` 在唯一 attempt 0 已达 18 usable、3 formal projects、3/3、gaps 0，但唯一资产绑定综合调用以 `APITimeoutError` 失败，终态 `blocked/research_synthesis_incomplete`，不得 retry。77 条 Trace、Firecrawl 0。正文 45 秒已消除本轮全部 page-analysis 超时；下一步红测并统一所有结构化 Provider 请求的 45 秒单次边界，仍 0 retry。
- 2026-07-17：统一 45 秒 Provider 边界红绿完成：客户端默认/worst-case 与 Quick/Balanced/Deep 资产综合新断言先失败于 30 秒，生产改为单一 `OPENAI_REQUEST_TIMEOUT_SECONDS=45`，SDK 仍 `max_retries=0`，正文专用重复常量已删除。客户端/正文/三档综合 3 条相邻测试全绿，调用次数和 Run attempt 规则不变。下一步完整离线门禁。
- 2026-07-17：统一 45 秒 Provider 边界的最终离线门禁完成：启动集成中唯一遗留 30 秒预期已对齐，最终 Ruff lint/format、strict Mypy、264 API tests 和 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步重启 API/Board、本轮唯一 OpenCLI 与 active guard，再创建一条全新 Quick。
- 2026-07-17：API/Board 已重启并加载统一 45 秒 Provider 边界，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON 守卫确认 23 条历史 Run、active 0 后只创建全新 Quick `a7d4d9e9-7510-4f66-a418-e6c3378faf1e`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控到终态。
- 2026-07-17：Quick `a7d4d9e9-7510-4f66-a418-e6c3378faf1e` 已在唯一 attempt 0 终止 `blocked/budget_exhausted`，不得 retry。终态 19 usable、2 formal projects、2/3，覆盖流线/剖面，功能缺口；资产绑定综合成功（3 cases/2 conflicts），106 条 Trace、Firecrawl 0。与上一条 3/3 Run 比较确认页面相同但 planner 措辞使 Shrewsbury/Office relevance 从 3/4 漂移到 0/1。已红绿新增 `_public_page_analysis_question()`：保留原子问题并按共享意图追加稳定功能/流线/采光/剖面焦点；输入门、图纸门和证据门不变。纯函数红测与正文/缓存分支分析 5/5 通过；下一步完整离线门禁。
- 2026-07-17：稳定正文分析焦点的最终离线门禁完成：新测试导入顺序机械校正后，Ruff lint/format、strict Mypy、265 API tests 和 `git diff --check` 全绿，staged 0。下一步重启 API/Board、本轮唯一 OpenCLI 与 active guard，再创建一条全新 Quick。
- 2026-07-17：API/Board 已重启并加载稳定正文分析焦点，健康检查 `ok` / 200；本轮唯一 OpenCLI doctor 为 daemon 1.8.6、Extension 1.0.22、Connectivity 全 OK。Python JSON 守卫确认 24 条历史 Run、active 0 后只创建全新 Quick `42668844-497e-453f-bcd4-38c9f25cbb25`，attempt 0。没有 retry、Balanced/Deep、Firecrawl 或截图；下一步只读监控到终态。
- 2026-07-17：Quick `42668844-497e-453f-bcd4-38c9f25cbb25` 已在唯一 attempt 0 终态通过：`completed/completion_satisfied`，18 usable、3 formal projects、3/3、gaps 0。资产绑定综合完成（4 cases），数据库中有 3 个具备条件/机制/转译的正式项目与 30 条逐字证据声明；78 条 Trace、Firecrawl 0，active Run 0。没有 retry、Balanced/Deep、截图、暂存或提交。最终离线基线仍为 Ruff/format、strict Mypy、265 API tests 与 `git diff --check` 全绿；本次授权范围完成，后续档位等待另行批准。
- 2026-07-17：用户已明确批准启动 M47 的下一阶段 Balanced。执行边界固定为：同题、全新 Run、attempt 0；先做服务健康、一次 OpenCLI 和 active-run=0 守卫，再只读监控。通过标准为 4/4、gaps 0、逐字正文证据绑定、资产综合完成且包含比较与适用边界、Firecrawl 0。若失败，只能诊断后做全局红测/最小修复并创建另一条全新 Balanced；不得 retry 旧 Run。Deep、截图、暂存和提交仍未授权。
- 2026-07-17：Balanced 创建前预检通过：API `ok` 且仍为 `gpt-5.6-sol`，Board 200；本阶段唯一一次 `opencli doctor` 为 daemon 1.8.6、Extension 1.0.22、Connectivity 0.1s 全 OK；显式 active 状态查询为 0，最新 Run 仍是已完成 Quick，staged 0。下一步只从该 Quick 读取同一题目并 POST 一条全新 Balanced，绝不 retry 历史 Run。
- 2026-07-17：active-run 守卫再次在同一 Python POST 边界确认 0 后，只创建一条同题 Balanced `0e27aeba-31a7-4283-8f4b-1714916e6563`：`precedent_research`、小红书可选视觉来源、attempt 0，题目与通过 Quick 完全一致。没有 retry 或第二次提交；下一步只读监控到终态并验收 4/4、比较、适用边界与 Firecrawl 0。
- 2026-07-17：Balanced 规划首门已通过，四个持久分支为 `program_insertion`、`public_service_flow`、`sectional_layers`、`old_new_interface`，均为需要原始图纸和项目正文交叉核验的设计问题。当前 attempt 0 正常在 searching/inspecting 间推进，已解析 Office Building Transformation 等项目页；暂态 coverage 尚未生成，不作失败判断。
- 2026-07-17：Balanced attempt 0 的首个有效 gap-check 已达到 19 usable、3 formal projects、13 verified/partial、4/4、gaps 0；`old_new_interface/program_insertion/public_service_flow/sectional_layers` 全部有正式覆盖。运行仍继续完成 24 条计划查询和 Balanced 综合，当前只能判定覆盖门已过，尚不能提前接受终态；扩展裁图的可选 `BrowserCommandError/skipped` 未阻断 Direct Playwright 正文链。
- 2026-07-17：Balanced `0e27aeba-31a7-4283-8f4b-1714916e6563` 已在唯一 attempt 0 终止 `blocked/research_synthesis_incomplete`，不得 retry。最终研究覆盖为 19 usable、3 formal projects、13 verified/partial、4/4、每分支 3 passes，终态前 gap-check 的 gaps 为 0；唯一资产综合调用以 `APITimeoutError` 失败后加入综合缺口。下一步先审计综合耗时/输入规模和现有 Provider 边界，写全局档位复杂度红测，再做最小修复与离线门禁；不会创建新 Run 直到门禁通过。
- 2026-07-17：三档综合超时行为红测先精确失败于实际 `[45,45,45]`，生产现只将综合调用分级为 Quick 45、Balanced 60、Deep 90 秒，其他 Provider 调用与 0 retry 不变。首次补丁误命中相邻正文分析调用，已在任何测试/运行前通过全调用点检查纠正并记录；正文 45 秒、三档综合提示与强度拒绝 3 条聚焦回归全绿。下一步执行完整 Ruff/format、strict Mypy、全 API suite 与 `git diff --check`。
- 2026-07-17：档位级综合边界最终离线门禁完成：Ruff lint、47 文件 format check、strict Mypy 18 source files、265 API tests 与 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。没有 Provider、真实来源、小红书或 Firecrawl 调用。下一步重启 API/Board 载入新边界，执行 active-run=0 守卫后创建一条全新 Balanced；本阶段不重复 OpenCLI doctor。
- 2026-07-17：服务已重启并加载档位级综合边界，API `ok`/Board 200，active Run 0，最新仍是封存的 blocked Balanced。首次把 stop/start 放在同一 PowerShell 调用时 stop 脚本结束了宿主，start 未执行；已记录并改为独立 `start.ps1`，随后健康验证通过。没有重复本阶段 OpenCLI doctor。下一步只 POST 一条全新 Balanced。
- 2026-07-17：同一 Python 边界确认 active 0 后只创建一条全新 Balanced `ec5902d1-ea70-4853-bba9-4ea250d0656c`，同题、attempt 0；旧 `0e27aeba-...` 未 retry，也没有第二次 POST。下一步只读监控 4/4 与 60 秒资产综合终态，Deep 继续未启动。
- 2026-07-17：对话中断未影响正在运行的 `ec5902d1-...`，既有 monitor session 仍连接同一 attempt 0。恢复审计时 Run 已到第 22/24 条计划查询：27 usable、21 verified/partial，四分支 pass 计数 5/6/5/5；此时正式 coverage 仍为 0，因为跨轮远程批次按设计在各分支最后查询才执行，`public-service-flow` 批次刚完成 classified 4 / added 2。继续等最后两条查询和正文分析，不提前判失败。
- 2026-07-17：第二次中断后原 monitor 进程已自然结束，恢复改用 API/SQLite 并确认 Balanced `ec5902d1-ea70-4853-bba9-4ea250d0656c` 在唯一 attempt 0 终止 `blocked/research_synthesis_incomplete`，不得 retry。最终 30 usable、24 verified/partial、四分支各 6 passes，但 0 formal projects、0/4，gaps 为 uncovered/article-analysis/synthesis incomplete；最后 `fabric-interface` 批次 classified 1 / added 0，因无 article-grounded cases 没有调用综合，所以 60 秒边界尚未实测。下一步只读比较两条 Balanced 的真实查询、页面和正文分析机会，定位全局波动来源。
- 2026-07-17：第二条 Balanced 的持久计划离线重放确认 `fabric-interface` 被错误归为 flow。已先增加意图、站内查询和正文焦点三层中英重叠红测，旧实现 4 项失败；生产新增共享 interface 加权词和对应中英检索/分析焦点后，14 项聚焦矩阵全绿。下一步运行完整 public-pages/workflow 相邻回归、Ruff/format/Mypy、全 API suite 和 `git diff --check`，门禁前不创建新 Run。
- 2026-07-17：interface 全局修复最终离线门禁完成：完整 public-pages/workflow 相邻回归、Ruff lint、47 文件 format check、strict Mypy 18 source files、267 API tests 与 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。没有 Provider、真实来源、小红书或 Firecrawl 调用。下一步独立 stop/start 服务，健康与 active-run=0 通过后只创建一条全新 Balanced；不重复本阶段 OpenCLI doctor。
- 2026-07-17：API/Board 独立 stop/start 后健康为 `ok`/200，active Run 0；没有重复 OpenCLI doctor。同一 Python POST 边界只创建全新 Balanced `33d61834-27d8-49f2-a8cf-2f0b51cec067`，同题、attempt 0；`0e27aeba-...` 与 `ec5902d1-...` 均未 retry。下一步只读监控 4/4、interface 查询、60 秒综合与 Firecrawl 0。
- 2026-07-17：第三条 Balanced 的规划首门生成 `program_insertion/public_service_flow/sectional_hierarchy/old_new_interface` 四个设计分支；生产函数重放分别判为 program/flow/section/interface。`old_new_interface` 的真实英文焦点现为 `old new structural interface retained frame slab truss ... connection detail`，正文焦点为保留结构/围护/设备遗存与新介入的脱开、开洞、退让、跨接或加固，不再落到流线。Run 当前仍在早轮 searching/inspecting，继续只读监控。
- 2026-07-17：第三条 Balanced 首轮已形成 1 个正式项目并覆盖 `sectional_hierarchy`；interface 查询真实命中 Fenix I Warehouse Renovation，但该强页正文分析发生一次 `APITimeoutError`。到第 11/24 条查询时仍为 22 usable、1 formal project、1/4，program/flow 已 3 passes、section/interface 2 passes；Run 尚有恢复轮，继续观察不同页面能否在现有 45 秒正文/0 retry 规则下补齐，不提前扩大边界。
- 2026-07-17：第三条 Balanced 到最终轮前为 26 usable、1 formal project、1/4，仅 `sectional_hierarchy` 覆盖；program/flow/interface 已各 5 passes，section 3 passes。第 21 条查询开始执行最终跨轮远程批次，program classified 4 / added 1；下一步继续监控 flow/section/interface 批次与批次后的正文分析，仍不提前判终态。
- 2026-07-17：Balanced `33d61834-27d8-49f2-a8cf-2f0b51cec067` 已在唯一 attempt 0 终止 `blocked/budget_exhausted`，不得 retry。最终 29 usable、29 verified/partial、1 formal project、1/4，仅 `sectional_hierarchy`；interface 路由真实命中 Fenix I 并使用正确焦点，但首个强页正文分析超时，后续 Arcadia 等页 relevance 2/drawing 0 未成链。新 60 秒 Balanced 综合已成功（1 case/1 comparison/3 conflicts），明确保留另三分支证据缺口。下一步只读审计所有 program/flow/interface 正文机会和 Firecrawl/Trace，再选择可行为红测的全局缺陷；不创建新 Run。
- 2026-07-17：终态审计确认该 Run 207 条 Trace、Firecrawl 0，13 次正文分析、3 个最终远程批次；综合成功。Office、Australian Red Cross 与 AKQA 的持久 limitations 共同证明 Provider 把多选项子问题解释为单页必须覆盖全部，尽管各页已提供部分具体机制证据，仍只保留 context 而清空 mechanism/transfer。下一步先为“单页可完成一个受支持的部分机制并诚实标注其余缺口”写 Provider 输入合同红测，再做最小提示修复和完整离线门禁；不降低逐字引文、图纸绑定或 relevance 要求。
- 2026-07-17：Provider 部分机制行为红测先失败，生产只新增“一个有逐字证据的条件—操作—结果可形成受限机制，其余列举项进入 limitations”的明确合同后，正文分析与 12k 尾部机制两条聚焦测试转绿。没有更改模型、timeout、schema、证据门、调用预算或 Run retry。下一步运行 providers/workflow 相邻回归、Ruff/format/Mypy、全 API suite 与 `git diff --check`，门禁前不创建新 Run。
- 2026-07-17：部分机制合同最终离线门禁完成：完整 providers/workflow 相邻回归、Ruff lint、47 文件 format check、strict Mypy 18 source files、267 API tests 与 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。没有 Provider、真实来源、小红书或 Firecrawl 调用。下一步独立重启服务、健康与 active-run=0 通过后只创建一条全新 Balanced；不重复 OpenCLI doctor。
- 2026-07-17：API/Board 独立重启并加载部分机制合同，健康 `ok`/200，active Run 0；未重复 OpenCLI doctor。同一 Python POST 边界只创建全新 Balanced `33e5cae8-b84c-4bbf-903c-bc85032bf349`，同题、attempt 0；前三条 Balanced 均未 retry。下一步只读监控 4/4、部分机制落盘、60 秒综合和 Firecrawl 0。
- 2026-07-17：第四条 Balanced 到第 14/24 条查询为 37 usable、37 verified/partial、0 formal projects/0 of 4。新合同下 Shrewsbury 功能植入页面达到 relevance 2 / drawing 1 / enriched 1，未再因只支持部分策略直接判零，但当前仍未满足完整条件—机制—转译门；Office relevance 0，Australian Red Cross 正文分析 `APITimeoutError`，section 三页均 relevance 1。Run 仍有 10 条计划查询，继续只读到终态，不提前追加修复。
- 2026-07-17：Balanced `33e5cae8-b84c-4bbf-903c-bc85032bf349` 已在唯一 attempt 0 终止 `blocked/research_synthesis_incomplete`，不得 retry。最终 44 usable、44 verified/partial、0 formal projects、0/4，故无综合输入；部分机制提示让 Shrewsbury 等页出现 relevance 2/enriched 1，但没有完整条件—机制—转译链。下一步只读审计本轮 page-analysis 字段、逐字事实与持久化门，判断模型是否仍把 evidence-grounded mechanism 误当成必须在原文中逐字出现；门禁前不创建新 Run。
- 2026-07-17：Direct Playwright/`LocalBrowserPageParser` 三页只读审计完成，无 Provider、Run、Firecrawl、截图或数据库写入。Office 与 Australian Red Cross 的已知机制句分别位于 12k 输入约 3.1k/3.4k，正文根与当前解析均保留；Shrewsbury 当前 article 仅约 900 字且来源只含历史/铁框架事实，确实没有介入机制。首次诊断打印误读不存在的 `image_urls` 字段，在首个页面解析后中止；改用实际 `images` 字段并按页隔离后完整通过。下一步检查 Provider 输出合同与逐字校验，先写“中文事实转述不要求逐字、机制可复制受支持事实、丰富度不阻断单页准入”的全局失败行为测试，再做最小修复。
- 2026-07-17：Provider 结构合同红测按预期失败，最小生产修复明确了中文事实转述、唯一逐字字段、机制对受支持事实的复用，以及研究强度属于后续丰富目标。首次绿测因新文案替换了既有“逐字摘录”关键词而 1/3 失败；保留原连续逐字摘录措辞后 3/3 通过。下一步执行完整相邻回归与 Ruff/format/Mypy/全 API/`git diff --check`，门禁前不创建 Run。
- 2026-07-17：结构合同修复的最终离线门禁完成：Ruff lint、47 文件 format check、strict Mypy 18 source files、267 API tests 与 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。没有 Provider、真实来源、小红书或 Firecrawl 调用。下一步独立重启 API/Board，健康与 active-run=0 通过后只创建一条全新 Balanced；不重复本阶段 OpenCLI doctor。
- 2026-07-17：API/Board 已独立重启并加载结构合同修复，健康为 `ok`/200，模型仍为 `gpt-5.6-sol`；本阶段未重复 OpenCLI doctor。Python JSON 守卫确认历史 29 条、active 0 后只创建全新同题 Balanced `a16cd228-4519-4fb2-a35a-eb151d824099`，attempt 0。首次健康守卫误用不存在的 `/v1/health`，只读 404 后在任何列表读取/POST 前停止；改用 `/health` 后完整通过。下一步只读监控到终态，旧 Run 不 retry，Deep 不启动。
- 2026-07-17：Balanced `a16cd228-4519-4fb2-a35a-eb151d824099` 已在唯一 attempt 0 封存为 `blocked/research_synthesis_incomplete`，不得 retry。最终 21 usable、2 formal projects、3/4，仅缺 `sectional_hierarchy`；201 条 Trace 中 Firecrawl 0，三次远程批次均完成，运行级综合保留为 incomplete。只读审计确认 Office/AKQA 的正式正文链已绑定 section 图，但未为剖面分支重分析；实际剖面候选正文均无完整机制或未选图。下一步先红测“搜索不再返回强页时，每个新分支仍至多复用一次已解析正式页”，再做最小全局修复和完整离线门禁。
- 2026-07-17：跨分支正式页复用红测先因测试夹具漏传必填 `assets` 而在行为前失败；补齐夹具后精确失败为实际 1 次、期望 3 次。最小生产实现增加分支级一次性集合和最高排名正式页选择，复用仍调用既有 `_try_public_page_branch_analysis()`。新回归与“缓存页每个新问题重分析 / 同问题不重复”相邻矩阵 5/5 通过。下一步完整离线门禁，门禁前不创建 Run。
- 2026-07-17：跨分支正式页复用最终离线门禁完成：完整 browser-inspection 相邻回归、Ruff lint、47 文件 format check、strict Mypy 18 source files、268 API tests 与 `git diff --check` 全绿，staged 0；仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示。下一步独立重启 API/Board，健康与 active-run=0 后创建一条全新 Balanced；不重复 OpenCLI doctor。
- 2026-07-17：API/Board 已重启并加载跨分支复用修复，健康 `ok`/200、模型 `gpt-5.6-sol`；未重复 OpenCLI doctor。Python JSON 守卫确认历史 30 条、active 0 后只创建全新同题 Balanced `079a1d1e-36b0-434c-bdc6-b6da273457fe`，attempt 0；`a16cd228-...` 未 retry。下一步只读监控 4/4、正式页跨分支重分析、60 秒综合与 Firecrawl 0。
- 2026-07-17：Balanced `079a1d1e-36b0-434c-bdc6-b6da273457fe` 已在唯一 attempt 0 封存为 `blocked/budget_exhausted`，不得 retry。最终 40 usable、2 formal projects、3/4，仅 `public_logistics` 未覆盖；Office 的跨分支剖面/界面分析真实成功，Balanced 综合完成并包含比较、冲突和适用边界，157 条 Trace、Firecrawl 0。Red Cross 本轮 6,386 字正文不含历史上曾出现的中央巷道句；三次重复读取及完整 HTML/textContent 均确认当前来源确实缺失，不能补写。站点原序审计发现 De Nederlandsche raw 4 被空 metadata 的 URL-slug 重排挤出页槽。下一步红测“空 title/description 保持站点原序、有 metadata 继续语义重排”，再做最小修复与完整门禁。
- 2026-07-17：空 metadata 保序初始红测失败并以简单 key 修复转绿，但真实 ArchDaily 复验仍返回 Shrewsbury/Carnegie，暴露同 URL 的后续带标题重复项会重新触发评分。测试升级为“首现空 metadata、后续重复补标题”的真实 DOM 形状后再次精确失败；生产改为根据首次出现状态决定是否保序。最终聚焦矩阵 6/6 且零 Provider 实页前四包含 De Nederlandsche。下一步完整 public-pages/browser-inspection 与静态/全 API/diff 门禁，门禁前不创建 Run。
- 2026-07-17：空 metadata 站点保序最终离线门禁完成：完整 public-pages 与 browser-inspection 相邻回归、Ruff lint、47 文件 format check、strict Mypy 18 source files、269 API tests 与 `git diff --check` 全绿，staged 0。首次并行 browser-inspection 的 session id 未保留，原进程结束后单独重跑并明确 exit 0；没有遗留测试进程。下一步独立重启、健康与 active-run=0 后创建全新 Balanced；不重复 OpenCLI doctor。
- 2026-07-17：API/Board 已重启并加载站点保序修复，健康 `ok`/200、模型 `gpt-5.6-sol`；未重复 OpenCLI doctor。Python JSON 守卫确认历史 31 条、active 0 后只创建全新同题 Balanced `72592364-c5ef-44dc-a978-f1037e70d2c8`，attempt 0；所有旧 Run 未 retry。下一步只读监控 De Nederlandsche 召回、4/4、60 秒综合与 Firecrawl 0。
- 2026-07-17：Balanced `72592364-c5ef-44dc-a978-f1037e70d2c8` 已在唯一 attempt 0 封存为 `blocked/research_synthesis_incomplete`，不得 retry。最终 26 usable、2 formal projects、3/4；站点保序让 De Nederlandsche 进入并覆盖 `public-logistics`，唯一缺 `sectional-layers`，150 条 Trace、Firecrawl 0。只读审计确认一次性正式页复用按全局排名选择了 1 张 section 的 Blue School，而未选当时已有 2 张 section 的正式 De Nederlandsche。下一步红测“按当前意图匹配图纸数选择唯一复用页”，再做最小修复和完整门禁。
- 2026-07-17：按意图图纸数选择唯一复用页的红测精确失败为最后仍选择 1-section 来源；生产收集正式来源后按共享 intent 的匹配图纸数、总图纸数、既有排名排序，仍只分析第一项。目标与“不再返回强页仍复用 / 同分支不重复”3/3 通过。下一步完整 browser-inspection、静态、全 API 和 diff 门禁，门禁前不创建 Run。
- 2026-07-17：意图图纸选择最终离线门禁完成：完整 browser-inspection、Ruff lint、47 文件 format check、strict Mypy 18 source files、270 API tests 与 `git diff --check` 全绿，staged 0。首次静态门禁只发现 `workflow.py` 机械格式漂移；仅格式化该文件并从最终源码重跑全部检查。下一步独立重启、健康与 active-run=0 后创建全新 Balanced；不重复 OpenCLI doctor。
- 2026-07-17：API/Board 已重启并加载意图图纸选择修复，健康 `ok`/200、模型 `gpt-5.6-sol`；未重复 OpenCLI doctor。Python JSON 守卫确认历史 32 条、active 0 后只创建全新同题 Balanced `aeefe013-0af9-4f96-869d-79126caa30a8`，attempt 0。下一步只读监控剖面复用选择、4/4、60 秒综合与 Firecrawl 0。
- 2026-07-17：Balanced `aeefe013-0af9-4f96-869d-79126caa30a8` 已在唯一 attempt 0 封存为 `blocked/research_synthesis_incomplete`，不得 retry。三轮研究达到 4/4、gaps 0、4 formal projects、29 usable；意图图纸复用真实选择 COOP 并以 relevance 4 / drawing 4 覆盖剖面，interface 也独立通过。唯一综合调用对 11 个正式资产在 60 秒 `APITimeoutError`，127 条 Trace、Firecrawl 0。一次只读 gap 查询误把 `con.close()` 放在循环内，打印首条 4/4 后第二次迭代报 `ProgrammingError`，无写入。下一步红测并精确去重同页同类型同分析/证据的综合 cases，再做最小修复和完整门禁。
- 2026-07-17：综合案例精确去重完成红绿与离线门禁。旧实现端到端红测收到 3 cases 而非 2；生产现只折叠同来源、同类型、同分析、同证据的案例，并保留排名最前真实 asset ID。键级回归证明不同分支分析或证据必须保留；聚焦 9 项、完整 browser-inspection、Ruff lint/format、strict Mypy 18 source files、272 API tests 与 `git diff --check` 全绿。Windows `mypy.exe` launcher 两次无诊断退出 1，改用同 venv 的 `python -m mypy --strict --no-incremental` 后明确通过。封存 `aeefe013-...` 只读重放为 5 cases，符合预期 11→5；没有 Provider、真实来源、Run、retry、Firecrawl、Deep 或截图。下一步独立重启服务，健康与 active-run=0 后只创建一条全新同题 Balanced attempt 0。
- 2026-07-17：API/Board 已独立重启并加载综合精确去重，健康 `ok`/200、模型 `gpt-5.6-sol`；未调用 OpenCLI doctor。Python/数据库显式守卫确认 active 0，同一原子边界再次确认后只 POST 一次创建全新同题 Balanced `26562aec-a24c-40d4-b270-467027004f3b`，attempt 0、小红书为可选视觉来源；`aeefe013-...` 未 retry。下一步只读监控 4/4、5-case 量级综合、比较/适用边界和 Firecrawl 0。
- 2026-07-17：Balanced `26562aec-a24c-40d4-b270-467027004f3b` 已在唯一 attempt 0 封存为 `blocked/budget_exhausted`，不得 retry。最终 27 usable、1 formal project、2/4，仅 `program_insertion/old_new_interface` 覆盖；18 个实际查询、24 个 Direct Playwright 页面、10 次正文分析、两个成功远程批次、1 次成功综合，179 条 Trace、Firecrawl 0。综合去重真实消除了超时并输出比较限制/冲突/适用边界。只读审计将剩余缺陷定位为证据完整且有相关图纸元数据时模型仍漏填 drawing_ids；下一步先红测“只对完整正文链＋当前意图图纸提供确定性审计入口，照片/不完整证据不准入”，再做最小全局修复与离线门禁。
- 2026-07-17：证据完整分析的确定性图纸审计入口完成红绿与离线门禁。端到端红测先证明模型漏填 IDs 时相关 plan 无正式机制；Provider 合同红测同时证明提示缺少审计入口语义。生产现只在逐字支持的 context/mechanism/transfer 完整且存在当前 intent 相关项目图纸时绑定一个入口，photo/不完整证据继续拒绝；共享类型表也供正式页复用排序。聚焦缓存矩阵 6/6、完整 browser-inspection、Ruff lint/format、strict Mypy 18 source files、273 API tests 与 `git diff --check` 全绿。没有 Provider、真实来源、Run、retry、Firecrawl、Deep 或截图。下一步独立重启，健康与 active 0 后只创建一条全新同题 Balanced attempt 0。
- 2026-07-17：API/Board 已独立重启并加载确定性图纸审计入口；同一 Python 创建边界确认 API `ok`、模型 `gpt-5.6-sol`、Board 200、active 0 后，只 POST 一次创建全新同题 Balanced `944a67c9-37bb-4e16-99fc-8bb4e83abebd`，attempt 0、小红书为可选视觉来源。`26562aec-...` 未 retry；下一步只读监控 4/4、正式正文案例、精确去重综合、比较/适用边界和 Firecrawl 0。
- 2026-07-17：Balanced `944a67c9-37bb-4e16-99fc-8bb4e83abebd` 已在唯一 attempt 0 封存为 `blocked/research_synthesis_incomplete`，不得 retry。最终 29 usable、2 formal projects、3/4，`program/circulation/section` 覆盖，structure 6 passes 后仍缺；11 次正文分析中 De Nederlandsche structure 超时，SMOORE relevance 2 但正文无机制。精确去重后综合仅 3 cases 仍 `APITimeoutError`，Trace Firecrawl 0。只读量化确认 cases wire JSON 为 11,608 字符，顶层分析与分支分析重复。下一步红测并仅压缩综合序列化载荷，不调 timeout、不加 retry、不丢资产级 provenance。
- 2026-07-17：综合 wire payload 去重完成红绿与最终门禁。三档双 case 测试先失败于分支 case 仍含五个重复顶层字段；生产只改序列化，有分支时移除重复顶层分析，无分支 case 保持完整。封存 `944a67c9-...` 只读量化为 11,608→6,362 chars（-45.2%）。Ruff 首次并行检查仅因 `.ruff_cache` 竞争发出 access warning，改为串行后 lint/format 无警告通过；strict Mypy 18 source files、273 API tests 与 `git diff --check` 全绿。下一步独立重启、健康与 active 0 后只创建一条全新同题 Balanced attempt 0。
- 2026-07-17：API/Board 已独立重启并加载综合 wire 压缩；同一创建边界确认 API `ok`、模型 `gpt-5.6-sol`、Board 200、active 0 后，只 POST 一次创建全新同题 Balanced `0b2db151-f171-49fb-9ccd-449ac9627200`，attempt 0、小红书为可选视觉来源。`944a67c9-...` 未 retry；下一步只读监控 4/4、正式正文案例、压缩综合、比较/适用边界和 Firecrawl 0。
- 2026-07-18：Balanced `0b2db151-f171-49fb-9ccd-449ac9627200` 已在唯一 attempt 0 封存为 `blocked/research_synthesis_incomplete`，不得 retry。最终 27 usable、3 formal projects、3/4，仅缺 sectional_layers；5-case 压缩综合仍 `APITimeoutError`，Trace Firecrawl 0。Balanced 输出合同已红绿收敛为固定 2/2/1/2/2 结构、每条 <=100 汉字、1,600 tokens，timeout 60 秒、0 retry 不变。Ruff/format、strict Mypy 18 source files、273 API tests 与 `git diff --check` 全绿。下一步独立重启、active 0 后只创建一条全新同题 Balanced；若仍失败，不再继续堆提示词，转向执行层确定性 fallback。
- 2026-07-18：Balanced `926adfb5-835d-42ec-810a-27927ec15f38` 在唯一 attempt 0 封存为 `blocked/research_synthesis_incomplete`，不得 retry：27 usable、4 formal projects、4/4、唯一 gap 为综合、131 Trace、Firecrawl 0。随后只读复用其 5 cases 做两次 scratch 综合：第一次 39.4s 返回后结构校验失败，第二次 37.6s 以 2/2/1/2/2 完整通过；scratch 未写数据库或修改 Run。
- 2026-07-18：Balanced `5bf96ad9-ae5f-47b6-bd2a-ccae055d02e7` 在唯一 attempt 0 封存为 `blocked/budget_exhausted`，不得 retry：32 usable、2 formal projects、2/4，综合完成，179 Trace、Firecrawl 0。LYCS 剖面与 Canal Hub 新旧界面两个强页正文分析分别单次超时。当前唯一下一步是 scratch 这两个页面的分析输入；通过前不创建新 Run，不继续堆提示词，不启动 Deep 或截图。
- 2026-07-18：新会话按 `HANDOFF.md` 恢复 M47，完整读取活跃计划与两份记录末尾，并确认分支 `codex/archresearch-v2-1` 的全部既有修改均未暂存且保持原状。planning-with-files 的系统 `python` 示例再次命中无效 Windows Store 别名，未重复同一命令；改用 Codex bundled Python 后 catch-up 无未同步输出。当前仍只执行 LYCS/Canal Hub 的无数据库 scratch 分析，未创建或 retry Run，未调用 Firecrawl、Deep 或截图。
- 2026-07-18：LYCS `section_layers` 的首个且唯一 scratch 用封存 Run 原问题、同一 Direct Playwright 页面、Balanced requirements、45 秒和 0 retry 重建生产输入；页面读取完成，但结构化正文分析再次以 `APITimeoutError` 终止。调用未写 SQLite、未改变 Run，且不再重复 LYCS；下一步对 Canal Hub 做一次独立 scratch，然后以两页实测决定 fallback 行为测试。
- 2026-07-18：Canal Hub `old_new_interface` 的首个且唯一 scratch 读取 8,435 字正文、40 张页面图片，按当时候选规则仅重建出 1 张 photograph；结构化分析在 35.19s 成功，但只返回 4 条逐字背景/保留事实，机制与转译均为空。严格门会诚实拒绝该页，未写 SQLite。下一步只做零 Provider 正文审计，确认两页的可恢复证据边界；在确认前不实现会补写 Canal 机制的 fallback。
- 2026-07-18：零 Provider 正文审计完成：LYCS 10,747 字与 Canal Hub 12,000 字快照均包含目标强机制，Canal 的机制位于先前 8,435 字 scratch 截断点之后。没有重复 LYCS 模型调用、没有数据库写入或新 Run。下一步先审计既有页面 settle 和 fallback 约束，写能同时复现“过早快照”和“page-analysis 异常直接丢页”的行为红测，再实现最小执行层修复。
- 2026-07-18：动态正文复读与 page-analysis 聚焦 fallback 已完成红绿最小实现。红测精确失败于缺少 late snapshot、45 秒预算和超时直抛；生产现于同一导航额外等待 1 秒并选择更长快照，OpenAI page analysis 仅在 timeout/connection 暂态错误后用最多 6,000 字的意图聚焦正文再调用一次，worst-case 显式为 90 秒，SDK `max_retries=0` 保持。4 条聚焦测试全绿。首次红测命令猜错一个既有 node id，收集阶段未执行测试；随后用 `rg` 取得真实函数名后重跑，不再重复错误节点。
- 2026-07-18：实现后的真实零 Provider 聚焦复核通过：LYCS 10,747→1,547 字、Canal Hub 12,000→2,382 字，两条关键机制逐字原文均保留。补充 section intent 与非暂态错误不 fallback 回归后，完整 `test_public_pages.py + test_providers.py` 63 tests、四文件 Ruff lint/format 全绿；首次格式检查只发现本轮嵌套提示的 3 条 E501，拆分字面量后从最终源码重跑通过。
- 2026-07-18：fallback 阶段完整离线门禁完成：browser-inspection 清空陈旧 pytest cache 后明确 exit 0；Ruff lint、47 文件 format check、strict Mypy 18 source files、277 API tests 和 `git diff --check` 全绿，仅既有 TestClient/httpx2 与 LF→CRLF 提示。一次外层会话丢失 browser test 句柄后误读了旧 `lastfailed` 节点，4 个节点均已不存在且只在收集阶段失败；随后 `--cache-clear` 完整重跑明确通过。staged 0，未创建/retry Run、未调用 Firecrawl、Deep 或截图。
- 2026-07-18：API/Board 重启并加载 fallback，健康 `ok`、模型 `gpt-5.6-sol`、Board 200。组合 stop/start 命令因 `stop.ps1` 不设置 `$LASTEXITCODE` 只完成停止；健康确认端口关闭后单独执行一次 `start.ps1` 成功，没有重复 stop。单一 Python 创建边界确认作品集工作区 active 0 后，只 POST 一次创建最终同题 Balanced `08e0ad17-cac5-4918-9d57-bf4c6f32f237`，attempt 0、小红书可选视觉来源；所有封存 Run 未 retry。下一步只读监控到终态。
- 2026-07-18：最终 Balanced `08e0ad17-cac5-4918-9d57-bf4c6f32f237` 在唯一 attempt 0 终止 `blocked/budget_exhausted`，不得 retry：48 usable、6 projects、3/4，仅缺功能植入；综合成功且满足 2/2/1/2/2，161 Trace、20 次正文分析全部完成、Firecrawl 0。一次轮询误把 SSE `/events` 当 JSON，解析失败后改用 SQLite `mode=ro`，没有影响 Run。终态四页功能批次 added 4，但全部为 photograph；零 Provider 元数据审计未发现任何可确定的图纸类型信号。没有启动 Deep、截图或第二条 Run。
- 2026-07-18：启动 M48 全局文字优先规则。范围是所有档位的 `precedent_research`：正文逐字证据链决定案例和覆盖，图片仅作同来源预览/链接锚点；`visual_reference_search` 或明确找图任务继续要求图片相关性。先红测并修改现有准入、coverage、Provider 提示与 Board/文档文案，不创建或 retry Live Run。
- 2026-07-18：M48 两条后端行为红测按预期失败：photo 预览候选的完整正文机制未持久化；无图页面的 page-analysis calls 为 0。Board 新文案断言也精确失败于旧“未提取到图纸/正文与图纸分析”文案；pnpm 参数转发实际运行 76 项而非过滤后的 2 项，但仅新增两项失败。进入最小生产修复。
- 2026-07-18：会话恢复时 `planning-with-files` 的 `session-catchup.py` 因系统 `python.exe` 仅为 Microsoft Store 占位符而未启动；已改为直接核对 `HANDOFF.md`、M47/M48、`findings.md`/`progress.md` 末尾和 git 状态，不重复该失败命令。工作树 staged 仍为 0，继续保留全部既有修改。
- 2026-07-18：M48 后端定向绿测通过 10 项：完整正文证据可在无图、同源照片预览和意图匹配预览三种情况下持久化并计入 coverage，Provider 提示同步。Board 将 precedent 的阶段、案例、空状态和检视器改为正文优先，移除“仅为补图而重跑”的入口；明确视觉工具保持图片语义，Board 76/76 全绿。
- 2026-07-18：M48 继续清除跨分支复用中的最后一个无图门，并为“无图强正文复用于后续子问题”补红绿测试。完整 browser-inspection + Provider 套件 97/97 通过；旧 precedent 视觉分类测试改为验证正文分析不等待图片分类、同源图片仅作 photograph 预览且图片归属保持 unknown。
- 2026-07-18：离线门禁中 Ruff 报新增 fixture 一处 102 字符行，已手动换行；Mypy 报综合分支 helper 返回宽泛 dict，已改为显式 `ResearchSynthesisBranchAnalysis`，strict Mypy 18 source files 通过。完整 API 首轮 278/279，唯一旧测试仍要求每个分支有可显示图片；按 M48 coverage 契约更新后完整 API 279/279 通过。Board 76/76、lint、typecheck、build 与 `git diff --check` 均已通过。
- 2026-07-18：一次同时追加 findings/progress 的补丁因 findings 末尾锚点不一致而未应用；读取真实末尾后改用现有最后一行追加，没有重复同一失败补丁，也未影响代码。
- 2026-07-18：M48 完整 `scripts/verify.ps1` 门禁 exit 0：279 API、76 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全绿。没有 Provider/Firecrawl/Live Run/Deep/截图调用；进入交接收尾。
- 2026-07-18：用户明确批准 M49 唯一一条全新 Quick attempt 0，用于验收 M48 正文优先规则。`planning-with-files` catch-up 找到 7 条尚未同步的上一轮消息；核对 git diff、M48 plan 和两份记录末尾后确认代码/门禁已同步，仅需新增 M49 运行记录。下一步先重启服务加载当前代码并验证 active Run 0，然后只创建一次；不得 retry、Balanced、Deep 或截图。
- 2026-07-18：M49 启动检查发现 `.archresearch/dev-processes.json` 仍记录昨晚 8000/5173 进程，但当前 API 8000 已拒绝连接，属于陈旧服务状态而非研究 Run 失败。下一步改用 SQLite `mode=ro` 做 active Run 0 守卫，再分别执行一次 stop 清理状态和一次 start，避免重复组合命令。
- 2026-07-18：SQLite `mode=ro` 确认全库 active Run 0；`stop.ps1` 只清理陈旧进程记录并提示无匹配监听，随后单独一次 `start.ps1` 成功加载当前代码，API 8000/Board 5173 就绪。API 健康 `ok`，目标作品集工作区 `fd3b06cd-...` 仍有 4 条历史 Run、active 0，最新仍是封存 Balanced `08e0ad17-...` blocked。进入唯一 Quick 创建前 payload/模型字段核对。
- 2026-07-18：创建前最终守卫再次确认目标工作区 active 0、Provider“梭子蟹 API”、模型 `gpt-5.6-sol`。只发送一次 POST 创建 M49 Quick `c8a67738-a827-462f-8914-760e519d1201`，attempt 0，问题为“旧建筑更新中，怎样植入新功能，并组织清晰的人车流线和空间层次？”，`research_sources=[]` 以隔离正文验收。不得 retry；下一步仅只读监控到终态。
- 2026-07-18：M49 已规划 3 个子问题并进入项目页检查，首条完整正文案例已出现；运行未终止、未 retry。一次 SQLite 只读统计 `python -c` 因 PowerShell/Python 双层引号截断而在解释前触发 `SyntaxError`，未连接数据库；后续改用 here-string 传参，不重复原命令。
- 2026-07-18：M49 Quick `c8a67738-a827-462f-8914-760e519d1201` attempt 0 已终止并封存为 `blocked/research_synthesis_incomplete`，不得 retry。正文验收通过 3/3、30 usable、30 verified/partial、5 projects、8 article-ready，8/8 context/mechanism 绑定同源逐字 excerpt，14 page analyses completed、0 failed、Firecrawl 0；唯一 synthesis 调用 `APITimeoutError`，因此整体验收未通过。未创建 Balanced/Deep、未启用视觉来源或截图。
- 2026-07-18：用户批准 M50 仅离线修复 Quick 综合超时。范围严格限定为：只读重建 M49 精确去重后的综合 payload，先红测 Quick 每个已覆盖子问题保留最强逐字案例的确定性上限，再最小实现；Balanced/Deep、爬取、图片和输出强度不变。期间不调用 Provider、不创建或 retry Run。
- 2026-07-18：M50 只读重建确认 M49 精确去重为 7 cases，当前 Quick 粗截前 6 个、payload 10,310 字，三个子问题各 2 个案例且每 case 只有一个分支。决定红测 Quick 按现有 rank 每题取首个案例并去重，总量 3；Balanced/Deep 不变。重建未调用 Provider或写数据库。
- 2026-07-18：M50 行为红测构造三个计划子问题、每题两个案例，旧 Quick 精确失败为仍发送全部 6 个且沿原输入起始；最小实现新增 Quick-only 确定性选择，目标与既有三档 Provider 契约转绿，完整 Provider 27 项及完整 API 回归均 exit 0。封存 M49 的只读 scratch 从 7 cases 选择 3 cases、三题全覆盖、payload 5,330 字；未调用 Provider、未写数据库、未创建或 retry Run。
- 2026-07-18：M50 首次完整门禁在 280 API 全绿后仅因新增 Provider 测试需 Ruff 格式化而退出 1；只机械格式化该测试文件，27 项 Provider 回归与 format check 立即转绿。随后从最终源码重跑 `scripts/verify.ps1`，280 API、76 Board、165 Extension、8 packaged E2E、Ruff、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation 全部 exit 0；`git diff --check` 也通过。staged 0，未创建/retry Run、未调用 Provider、Firecrawl、Deep 或截图。
- 2026-07-18：用户明确批准 M51 唯一一条全新 Quick attempt 0，用 M49 同一工作区、问题和 `research_sources=[]` 真实验收 M50；先确认 active Run 0 并只重启一次加载当前代码，再只 POST 一次。不得 retry M49 或其他封存 Run，不启动 Balanced/Deep、Firecrawl 或截图。
- 2026-07-18：M51 创建边界确认 M49 仍为封存 attempt 0、全库 active 0、历史 Run 46，随后只执行一次 stop/start 加载 M50；API `ok`、模型 `gpt-5.6-sol`、Board 200。最终守卫后只 POST 一次创建全新 Quick `a2cf2e20-af1f-4ffb-a2c2-9498c48ffe73`，attempt 0、`research_sources=[]`；下一步仅只读监控到终态，不重发、不 retry。
- 2026-07-18：M51 在唯一 attempt 0 完成 `completed/coverage_satisfied`：21 usable、7 projects、3/3、gaps 0，11/11 page analyses completed、48 Trace、Firecrawl 0。零 Provider 审计确认 9 个精确去重 cases 经 Quick 规则映射三题后按 asset ID 收敛为 2 cases / 5,662 chars，每题各 2 条逐字 evidence；唯一 synthesis 从 gap-check 到完成约 32.3 秒。未 retry 任何 Run，未启动 Balanced/Deep、视觉来源或截图。
- 2026-07-18：用户批准 M52 只读发布/提交边界审计。范围仅为相对 `98a9a01` 盘点 tracked/untracked 改动、区分产品/测试/文档/证据与本地工具输出、检查凭据和生成物风险并提出最小提交顺序；除规划记录外不改产品代码，不 stage、commit、清理、移动或删除用户文件。
- 2026-07-18：M52 只读清单得到 83 个 tracked 路径、22 个 untracked 文件、staged 0。未跟踪项分为 7 个候选源/测试/文档、10 张作品集 PNG 和 5 个本地工具输出；`.gitignore` 当前未排除 `.artifacts`、`.playwright-cli`、`apps/api/.artifacts` 或 `.impeccable/live`，后续提交必须显式列路径，禁止 `git add -A`。
- 2026-07-18：M52 依赖核对确认 7 个未跟踪候选均有产品、测试、验收或恢复归属，其中 `run_gate.py` 与持续加载 fixture 是当前 tracked API/E2E 的必要依赖。按目录量化显示后端、扩展、Board 与文档跨阶段耦合，建议保留一个可独立通过完整门禁的产品源码/测试/文档提交，再把 10 张 PNG 作为独立证据提交；5 个本地工具输出始终排除。
- 2026-07-18：M52 只读安全与证据检查通过：10 张 PNG 全部匹配 release manifest 的 bytes/SHA-256，新增 tracked 内容与 7 个候选文件无高置信凭据命中，生产范围 Firecrawl 引用 0。两份 pytest XML 哈希不同但均为可再生成报告；继续排除 XML、Playwright CLI 与 Impeccable live 输出。
- 2026-07-18：M52 完成 105 路径集合核对：84 product、5 records、11 evidence、5 local outputs，missing/unexpected/overlap 均为 0。审计没有修改产品代码、stage、commit、清理、移动或删除任何用户文件。唯一后续建议是先经用户批准补精确 `.gitignore` 规则，再按 product → records → optional evidence 的显式路径顺序处理提交。
- 2026-07-18：用户纠正发布标准并批准 M53 当前规则三档真实验收。Quick 沿用 M51；只先创建一条同工作区/同问题/`research_sources=[]` 的 Balanced attempt 0，要求 4/4、2/2/1/2/2 综合、completed、Firecrawl 0。只有 Balanced 通过才创建一条 Deep attempt 0，要求 6/6 和 Deep 综合合同；任一失败封存且不 retry，不截图、不改代码。
- 2026-07-18：M53 Balanced 创建边界确认 M51 Quick 已 completed attempt 0、全库 active 0、历史 Run 47，API `ok`、模型 `gpt-5.6-sol`；只 POST 一次创建 Balanced `d995bed5-8c40-4df9-80e4-4fe6256d3628`，attempt 0、`research_sources=[]`。下一步仅只读监控，Balanced 未通过前不创建 Deep。
- 2026-07-18：M53 Balanced `d995bed5-8c40-4df9-80e4-4fe6256d3628` 唯一 attempt 0 完成 `completed/coverage_satisfied`：34 usable、6 projects、4/4、gaps 0，综合 2/2/1/2/2，唯一 synthesis 约 34.6 秒；9 cases / 19,726 chars、12 page analyses completed / 4 failed、60 Trace、Firecrawl 0。Balanced 已满足创建唯一 Deep 的前置条件；未 retry、未截图。
- 2026-07-18：M53 Deep 创建边界再次确认 Balanced prerequisite 已 accepted、全库 active 0、历史 Run 48，API `ok`、模型 `gpt-5.6-sol`；只 POST 一次创建 Deep `6e255a32-2859-49d4-a5d9-a26d4d511ca2`，attempt 0、`research_sources=[]`。下一步仅只读监控到终态，不 retry、不截图。
- 2026-07-18：M53 Deep 的首个只读监控器在 30 分钟本地观察上限到达后退出 1，但 Run 本身仍在唯一 attempt 0 正常执行：最后为第三轮页面检查、55 assets、164 Trace，无终态。该错误只属于监控脚本，不重启、不 retry、不干预 Run；改用新的只读观察器继续。
- 2026-07-18：M53 Deep `6e255a32-2859-49d4-a5d9-a26d4d511ca2` 最终封存 `blocked/research_synthesis_incomplete`，不得 retry：55 usable、8 projects、6/6、24 page analyses completed、169 Trace、Firecrawl 0，但 synthesis events 0。只读时间线确认 Run 1,833.3 秒，达到 6/6 后仍为可选资产密度继续研究，越过 1,800 秒 deadline，收尾因剩余时间不足直接跳过 Deep 综合。下一步只做离线红测和最小模式综合保留时间修复，不创建新 Run。
- 2026-07-18：M53 Deep reserve 行为红测精确失败为已 6/6、只剩 95 秒时仍执行 6 次公共搜索；最小实现向 OpenAI Provider 暴露 45/60/90 档位综合时限，并在 workflow 研究阶段预扣该 reserve。目标、既有 deadline 回归和 synthesis/deadline 聚焦 9 项全绿；未增加 1,800 秒总预算、未 retry、未创建新 Run。
- 2026-07-18：Deep reserve 修复静态 lint 通过，首次 format check 仅要求机械格式化 `workflow.py`；只格式化该文件后聚焦 9/9 保持通过。完整 `scripts/verify.ps1` 从最终源码通过 281 API、76 Board、165 Extension、8 packaged E2E、Ruff、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation。失败 Deep 仍封存，未 restart、未 retry、未创建新 Run；下一条 Deep 必须重新获批。
- 2026-07-18：用户明确批准修复后的唯一替代 Deep attempt 0。恢复时 `planning-with-files` 的系统 `python` 命令再次命中 Microsoft Store 占位符，未执行项目逻辑；没有原样重试，改用 Codex bundled Python 完成 catch-up。随后完整读取 HANDOFF、活跃计划和两份记录末尾，确认分支 `codex/archresearch-v2-1`、staged 0，并保留全部既有修改。
- 2026-07-18：严格执行一次 stop、一次 start 加载 Deep reserve 修复；API `ok`、Provider 为梭子蟹 API、模型 `gpt-5.6-sol`、Board 200。SQLite `mode=ro` 创建守卫确认历史 Run 49、active 0，M51 Quick 仍 completed/attempt 0，旧 Deep `6e255a32-...` 仍 blocked/attempt 0。只 POST 一次创建全新 Deep `fa03effa-5939-407c-a35c-c051fbbd9dca`，attempt 0、`research_sources=[]`；下一步只读监控同一 Run 到终态，不重发、不 retry、不截图。
- 2026-07-18：替代 Deep 已从 searching 进入 inspecting，attempt 仍为 0；首轮 coverage 尚未形成，服务和只读监控正常。没有第二次 POST、retry、Firecrawl、视觉来源或截图操作。
- 2026-07-18：尝试用 Ctrl-C 结束一个过于频繁输出时间戳的只读监控进程时，统一执行后端明确返回“不支持中断”；未重复该操作，也未影响研究 Run。保留该监控到 Run 终态，后续降低读取频率。当前仍为 inspecting、attempt 0，无终态或失败信号。
- 2026-07-18：替代 Deep 首轮已规划 6 个子问题并进入 analyzing；只读 SQLite 中期统计为 8 source pages、12 asset candidates、5 次 public_page_analysis、22 Trace、Firecrawl 0。attempt 仍为 0，尚未形成首个 coverage checkpoint，不据此提前判定通过。
- 2026-07-18：用户中断的是一次只读监控等待，研究 Run 未受影响；恢复核对确认全库仍只有 Deep `fa03effa-...` 一条 active Run、attempt 0。中期数据已增长到 22 asset candidates、7 次 public_page_analysis，Firecrawl 0，coverage 尚未形成。一次 45 秒单次等待超过 Windows 执行工具 30 秒返回窗口而未带回输出，未重复该等待方式，改为短轮询读取。
- 2026-07-18：短轮询显示同一 Deep 在 inspecting 与 analyzing 间正常切换，仍处于首轮、attempt 0，coverage 为空且无 stop reason；没有出现卡死、终态或新错误信号。
- 2026-07-18：首轮继续推进到 12 source pages、33 asset candidates、11 次 public_page_analysis、41 Trace，Firecrawl 0；状态仍为 inspecting、attempt 0，尚未进入第一次 gap-check。
- 2026-07-18：第一次 gap-check 已形成：34 usable、5 projects、3/6，已覆盖 `program_insertion`、`flow_separation`、`structural_dialogue`；缺 `daylight_strategy`、`sectional_hierarchy`、`public_sequence`，gaps 为 uncovered_subquestions/article_analysis_incomplete。Run 已进入未覆盖分支恢复，当前 query 4/42 指向 sectional_hierarchy；Firecrawl 仍为 0，未放宽正文门。
- 2026-07-18：第一轮恢复继续复用已解析强页，正文分析累计 15 次、Trace 55；最新仍围绕 sectional_hierarchy，最近的 Gate M West Bund 页面复用并重新关联 2 个候选。最新 coverage 仍为 3/6，Firecrawl 0。
- 2026-07-18：恢复分支继续读取 Shitang Village Internet Conference Center（11,231 字正文、27 image leads），正文分析累计 16 次、Trace 57；coverage 仍以 sequence 48 的 3/6 为最新 checkpoint，Firecrawl 0。
- 2026-07-18：恢复轮又读取 Jimo Scientific Creation Center 的 12,000 字正文，正文分析累计 17 次、Trace 59；最新 gap-check 仍是 3/6，Firecrawl 0，Run 无 stop reason。
- 2026-07-18：sequence 64 的第二次 gap-check 提升到 49 usable、5 projects、4/6，新覆盖 `sectional_hierarchy`；仍缺 `daylight_strategy` 与 `public_sequence`。随后 De Nederlandsche Bank 的 daylight 正文分析以 relevance 4、enriched 1 完成，正文分析累计 19 次、Trace 69，Firecrawl 0，等待下一 checkpoint 确认是否升为 5/6。
- 2026-07-18：采光恢复继续读取 Holy Redeemer Church and Community Centre 的 12,000 字正文；正文分析累计 21 次、Trace 73。latest gap-check 仍为 4/6，Firecrawl 0，下一 checkpoint 尚未写入。
- 2026-07-18：sequence 80 的第三次 gap-check 提升到 57 usable、7 projects、5/6，`daylight_strategy` 已正式覆盖；唯一未覆盖分支为 `public_sequence`，正文分析累计 23 次、Trace 84，Firecrawl 0。Run 已只针对最后缺口继续恢复。
- 2026-07-18：最后缺口的 De Nederlandsche Bank `public_sequence` 正文分析已 completed，relevance 4、enriched 3、drawing_count 3；正文分析累计 24 次、Trace 85、Firecrawl 0。等待下一 gap-check 确认 6/6，并观察 reserve 是否直接转入综合。
- 2026-07-18：sequence 91 的第四次 gap-check 已正式达到 57 usable、7 projects、6/6、gaps 0；六个分支各至少一次正式通过，唯一 remaining enrichment gap 是 `insufficient_subquestion_assets`。当时 synthesis events 仍为 0，Run 在研究 reserve 尚未触底时继续有界 enrichment；接下来必须确认它在保留的 90 秒边界前停止研究并发起恰好一次综合。
- 2026-07-18：替代 Deep 在 sequence 152 再次确认 57 usable、7 projects、6/6 后，于预留窗口内发起唯一一次 `research_synthesis`；sequence 153 约 61 秒后记录 `failed/InternalServerError`，随后 sequence 154 composing。Run 总耗时 1,767.7 秒，终态封存 `blocked/research_synthesis_incomplete`、attempt 0，绝不 retry。
- 2026-07-18：终态只读审计为 12 article-ready assets、7 article-ready projects；六个分支均有逐字支持的完整条件—机制—转译链，program/flow/structure/section/daylight/public 分别有 1/5/3/4/4/4 条。25 次 public_page_analysis completed、2 failed，154 Trace、Firecrawl 0、`research_sources=[]`。reserve 修复通过 Live 调度验收，但 Deep 综合合同因上游 InternalServerError 无输出，M53 三档整体验收仍未完成；未创建第二条 Run、未 retry、未截图。
- 2026-07-18：收尾守卫确认 active Run 0、分支 `codex/archresearch-v2-1`、94 个既有 changed paths 全部保留、staged 0、`git diff --check` exit 0。HANDOFF、task plan、findings 与 progress 已同步最终封存状态；当前没有获批的进一步 Live 动作。
- 2026-07-18：用户以“继续”明确批准 M54 离线综合失败策略，范围不包含新 Run。当前先用封存 Deep `fa03effa-...` 的只读 cases 证明 deterministic synthesis 能否满足现有 Deep schema 与资产引用约束；确认后按行为红测、最小执行 fallback、完整离线门禁推进。不得调用 Provider、retry、写封存 Run、恢复 Firecrawl、启用视觉来源或截图。
- 2026-07-18：首个只读 scratch 被用户中断，未调用 Provider、未写数据库或源码；改为短输出原型后通过。封存 Deep 重建为 11 cases、12 eligible branches、5 comparison pairs，确定性结果精确满足 Deep 2/2/1/2/2，所有引用 ID 合法。进入行为红测：可恢复综合错误应完成本地 fallback，普通编程异常不得被掩盖。
- 2026-07-18：M54 三条行为测试先红：pure builder 不存在，`InternalServerError` 后仍返回 `None`；普通 `AssertionError` 的旧失败语义保持通过。最小实现只在 timeout/connection/InternalServer/rate-limit/结构 ValueError 等可恢复综合错误后构造 evidence-only fallback，输出标记 `generation_mode=deterministic_fallback`；普通编程异常继续 blocked。聚焦 3/3 已转绿，未调用 Provider或写封存 Run。
- 2026-07-18：自审红测又捕获 Quick 错误输出 comparison，以及泛化 `ValueError` 会吞编程错误。生产现按三档输出 Quick 1/0/0/0/1、Balanced/Deep 2/2/1/2/2；仅已知 Provider 结构错误消息或 Pydantic ValidationError 可恢复，普通 ValueError 仍失败。首次 Ruff 报两处长行、format 要求机械重排，首次 Mypy 报 Optional 列表未收窄；均只做排版/显式类型修正后，synthesis 9/9、Ruff、format、strict Mypy 18 files 全绿。
- 2026-07-18：扩大相邻回归通过：完整 `test_browser_inspection.py` 与 27 项 Provider 套件全部绿。封存 Deep 用最终生产 builder 再次零写入重放为 11 cases、2/2/1/2/2、全部资产 ID 合法、最长 statement 255 字；未调用 Provider、未修改任何 Run。
- 2026-07-18：新增完整 workflow fixture 首次仍 blocked；只读测试数据库确认测试 Provider 的 `plan(**kwargs)` 不符合真实四位置参数协议，触发默认计划后 branch IDs 与 cases 不匹配，生产 fallback 未执行。仅修正测试签名后，模拟唯一 synthesis `InternalServerError` 的完整 Deep workflow 转为 `completed/completion_satisfied`，coverage 和单条 completed synthesis Trace 均标记 deterministic fallback；生产代码无需为该测试错误调整。
- 2026-07-18：从最终工作树重跑完整 `scripts/verify.ps1`，288 API、76 Board、165 Extension、8 packaged Chrome E2E、Ruff lint/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全部通过。全程没有 Live Provider、真实来源、Firecrawl、Run、retry 或截图；M54 implementation complete。
- 2026-07-18：M54 收尾守卫确认 active Run 0、分支 `codex/archresearch-v2-1`、staged 0、`git diff --check` exit 0；全部既有工作树修改保持原状。当前唯一下一步是等待用户明确批准一条全新 Deep attempt 0 做 Live 验收，未经批准不重启服务、不 POST。
- 2026-07-18：用户以“开始”明确批准 M54 后唯一一条全新 Deep attempt 0。按 HANDOFF 恢复并保留全部工作树修改后，严格执行一次 stop、一次 start；API `ok`、Provider 梭子蟹 API、模型 `gpt-5.6-sol`、Board 200、历史 Run 50、active 0。创建守卫确认 M51 source completed/attempt 0、两条旧 Deep 均 sealed blocked/attempt 0，只 POST 一次创建 Deep `96e03065-a758-4c76-be33-49ccae9fbd8d`，attempt 0、`research_sources=[]`。下一步仅只读监控，不重发、不 retry、不截图。
- 2026-07-18：用户明确授权“修不好就一直修”，取消当前三档验收主线逐轮审批。若 Deep 失败，将自动封存、离线红绿修复、完整门禁并创建全新 attempt 0，直到通过；仍禁止 retry、Firecrawl、视觉来源、截图和提交。当前 `96e03065-...` 继续只读监控，不中断。
- 2026-07-18：Deep `96e03065-...` 首轮正在 inspecting：4 source pages、6 asset candidates、1 次 public_page_analysis、8 Trace、Firecrawl 0，attempt 0；尚未形成 coverage checkpoint，不提前判定质量。
- 2026-07-18：同一 Deep 首轮完成第一个子问题页组后进入 query 2/42 `public_logistics`；当前 4 source pages、12 asset candidates、4 次 public_page_analysis、18 Trace、Firecrawl 0，attempt 0，无错误或终态。
- 2026-07-18：Deep 首轮继续到 8 source pages、20 asset candidates、6 次 public_page_analysis、24 Trace；最新 Gate M West Bund 页面读取 11,802 字并 enriched 2，Firecrawl 0，尚未 gap-check。
- 2026-07-18：同一 Deep 已进入首轮 query 3/42 `sectional_hierarchy`；当前 8 source pages、23 asset candidates、9 次 public_page_analysis、34 Trace、Firecrawl 0，attempt 0。
- 2026-07-18：剖面首轮中 De Nederlandsche Bank 分析 completed/relevance 3/enriched 2；随后 Shitang Village 读取 10,866 字并 enriched 20。当前 11 source pages、34 asset candidates、12 次 public_page_analysis、41 Trace、Firecrawl 0。
- 2026-07-18：新旧结构首轮中 De Nederlandsche Bank completed/relevance 4/enriched 2；随后 O Star Power Culture and Arts Centre 读取 12,000 字并 enriched 8。当前 15 source pages、47 asset candidates、17 次 public_page_analysis、57 Trace、Firecrawl 0。
- 2026-07-18：Deep `96e03065-...` latest gap-check（sequence 100）已达 47 usable、7 projects、6/6、gaps 0；六题全部正文覆盖。24 次 public_page_analysis、101 Trace、Firecrawl 0。仅剩 `insufficient_subquestion_assets` 与 `insufficient_multi_asset_projects` 可选 enrichment，Run 在 reserve 前继续有界研究。
- 2026-07-18：Deep `96e03065-a758-4c76-be33-49ccae9fbd8d` 已在唯一 attempt 0 封存 `blocked/research_synthesis_incomplete`，不得 retry：47 usable、7 projects、6/6、正文 gaps 0，24 次 public_page_analysis completed、140 Trace、Firecrawl 0、`research_sources=[]`，但 synthesis events 0。Run 总耗时 1,743.7 秒；M54 fallback 未失败，而是综合根本没有被调用。
- 2026-07-18：只读时间线确认最后一轮可选搜索在 research deadline 前约 35 秒启动，搜索结束后直接来源页循环仍连续解析 4 页并越过预留边界，收尾只剩约 56 秒，低于 Deep 综合所需 90 秒。下一步进入 M55：先红测公开搜索仍可执行、但每个直接来源页都需逐页满足 parser worst-case reserve；再最小修复、完整门禁并自动创建一条全新 Deep attempt 0。用户既有持续授权有效，无需再次批准。
- 2026-07-18：M55 两条行为红测按预期 2/2 失败：公开搜索各执行一次且模型搜索保持跳过，但直接来源页仍各解析一次；Deep fixture 也因此在搜索后消费了本应留给综合的 parser 窗口。失败精确锁定 `workflow.py` 的直接来源页条件，未改生产代码、未调用 Provider 或创建 Run。
- 2026-07-18：直接来源页条件已加入每页重新计算的 parser worst-case reserve。首次绿测因旧测试时钟新增一次合法读取后抛 `StopIteration` 而产生 fixture-only failed；改为稳定尾值后目标 2/2 通过。Deep 覆盖现模拟同批两页：公开搜索一次、第一页时间足够而解析、第二页时间不足而跳过、综合恰好调用一次。下一步扩大 browser/Provider 与静态回归。
- 2026-07-18：M55 扩大回归通过：deadline 相邻 6/6、完整 `test_browser_inspection.py`、27 项 Provider 契约、Ruff lint、41 文件 format check、strict Mypy 18 source files 均 exit 0。展开页 deadline fixture 只因新增直接页时钟读取而一度提前跳过父页；补一格稳定时钟后仍保持“父页解析、子页 reserve 不足跳过”的原合同。下一步运行最终全仓 `scripts/verify.ps1`。
- 2026-07-18：从最终工作树运行 `scripts/verify.ps1` 全绿：288 API、76 Board、165 Extension、8 packaged E2E、Ruff/48 文件 format、strict Mypy、两端 lint/typecheck/build 和 30/108 evaluation 全部 exit 0；此前 `git diff --check` 也通过。M55 implementation complete，未创建或 retry Run、未调用 Provider/Firecrawl、未截图。下一步按持续授权一次 stop/start、active 0 守卫后只 POST 一条全新 Deep attempt 0。
- 2026-07-18：严格执行一次 stop、一次 start 加载 M55；API `ok`、Provider 梭子蟹 API、模型 `gpt-5.6-sol`、Board 200。SQLite `mode=ro` 守卫确认历史 Run 51、active 0，M51 来源仍 completed/attempt 0；随后只 POST 一次创建全新 Deep `e1a00ca9-ed48-4033-b64d-228a7996e300`，attempt 0、同工作区/同问题、`research_sources=[]`。下一步仅只读监控，不重发、不 retry、不截图。
- 2026-07-18：新 Deep `e1a00ca9-...` 首轮 inspecting 正常推进：4 source pages、11 asset candidates、3 次 public_page_analysis、19 Trace、Firecrawl 0，attempt 0；尚未形成 coverage checkpoint 或 synthesis event，不提前判断通过。
- 2026-07-18：同一 Deep 已进入第三个查询页组：10 source pages、27 asset candidates、13 次 public_page_analysis、63 Trace、Firecrawl 0，attempt 0。前两组均正常完成并进入下一查询，仍未写入首次 gap-check 或 synthesis event。
- 2026-07-18：首轮后半段继续到 18 source pages、29 asset candidates、18 次 public_page_analysis、138 Trace，Firecrawl 0；多个查询无新增页面后又命中新的 4 页组，均为正常检索差异。attempt 0，尚未首次 gap-check。
- 2026-07-18：Deep `e1a00ca9-...` 已在唯一 attempt 0 封存 `blocked/time_budget_exhausted`，不得 retry：49 usable、12 projects、5/6，仅缺 `adaptive_interface`，25 次正文分析 completed / 2 failed、195 Trace、Firecrawl 0。综合实际调用一次，Provider `InternalServerError` 后 M54 deterministic fallback completed 并输出 Deep 2/2/1/2/2；M55 reserve 修复通过 Live 验收。
- 2026-07-18：只读时序审计确认第三轮末已为 5/6，但第四轮固定顺序先启动已覆盖 `program_insertion`；唯一未覆盖 `adaptive_interface` 没有获得下一正常轮机会，随后 research deadline 到达。进入 M56：先红测 coverage 未完成时下一轮必须跳过已覆盖题并优先执行未覆盖题，再做最小 workflow 调度修复、完整门禁和全新 Deep attempt 0；持续授权有效，无需请示。
- 2026-07-18：M56 行为红测按预期失败：模拟 A/B 已覆盖、C 第一轮未覆盖时，旧正常轮实际搜索顺序为 `A,B,C,A,B,C`，没有让已覆盖分支让位；目标为 `A,B,C,C`。失败精确锁定 loop 顶部仅对 retry/recovery 跳过已覆盖题的条件，未改生产代码或创建 Run。
- 2026-07-18：M56 最小实现只扩展现有已覆盖分支跳过条件。参数化测试确认 coverage 未齐时目标顺序 `A,B,C,C`，coverage 已齐但 enrichment 未满时仍为 `A,B,C,A`；连同 M55 parser reserve、Deep synthesis reserve 和 workflow deadline 相邻合同共 6/6 通过。下一步完整 browser/Provider/静态与全仓门禁。
- 2026-07-18：M56 完整 browser-inspection 与 27 项 Provider 契约通过。首次静态门禁仅报告本轮 `workflow.py`、`test_browser_inspection.py` 需要 Ruff 机械格式化；lint 已通过且尚未进入 Mypy。只格式化这两个文件后从最终源码重跑目标与静态门禁。
- 2026-07-18：只机械格式化 M56 两个触及文件后，coverage-first/M55/deadline 目标 6/6、Ruff lint、41 文件 format check、strict Mypy 18 source files 全绿。下一步 `git diff --check` 与最终 `scripts/verify.ps1`。
- 2026-07-18：M56 首次完整 `scripts/verify.ps1` 在 289/290 API 通过后仅失败于一条旧恢复计数：fixture 仍要求已覆盖 `program` 查询 2 次；新结果只查 1 次，但总 Provider 查询仍为 8、最终仍 completed 3/3。将旧计数更新为精确 coverage-first 序列 `program,circulation,section,circulation,section,circulation,section,circulation`，生产代码不因旧断言回退。
- 2026-07-18：旧恢复断言与参数化 M56 合同从最终源码通过，随后完整 `scripts/verify.ps1` 全绿：290 API、76 Board、165 Extension、8 packaged E2E、Ruff/48 文件 format、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation 全部 exit 0。M56 implementation complete；下一步一次 stop/start、active 0 守卫并只创建一条全新 Deep attempt 0。
- 2026-07-18：用户指出不能把持续授权理解为每个修复后自动创建新 Deep。M56 后已执行一次 stop/start，但尚未 POST，立即停止创建路径。新全局流程为“封存→合并诊断→封存 Run 形状零 Provider 端到端重放→完整门禁→仅一条必要 Live attempt 0”；无需逐轮请示，但不再用 Live Run 逐个发现可离线暴露的问题。
- 2026-07-18：M57 联合预检第一次只失败于错误观察面：生产公开搜索会把原子子问题压成站点短查询，因此 parser query 不保留 `[branch-f]` 文本；Run 已实际 completed 6/6。改用持久化 `QueryAttempt.subquestion_id` 验证唯一执行的是缺失 branch-f，生产逻辑不变。
- 2026-07-18：M57 联合预检转绿：初始 5/6，仅执行 branch-f；公开搜索 1、模型搜索 0；直接来源页只解析第一页并按 reserve 跳过第二页；最终 completed 6/6；唯一综合 `InternalServerError` 由 deterministic fallback 完成 Deep 2/2/1/2/2。未调用 Live Provider、未写封存 Run、未 POST。
- 2026-07-18：M57 完整 browser-inspection 与 27 项 Provider 套件通过；首次静态门禁仅要求机械格式化联合预检所在 `test_browser_inspection.py`。生产代码不变，格式化后重跑目标、静态与完整门禁。
- 2026-07-18：格式化后 M57 联合预检/coverage-first/恢复合同 4/4、Ruff/format/Mypy 全绿；随后 `git diff --check` 与完整 `scripts/verify.ps1` 通过 290 API、76 Board、165 Extension、8 packaged E2E、两端构建及 30/108 evaluation。全程 active 0、无 POST、无 Live Provider/Firecrawl/截图。现在才允许创建一条必要的新 Deep。
- 2026-07-18：M57 综合预检后创建守卫为 API `ok`、模型 `gpt-5.6-sol`、Board 200、历史 Run 52、active 0，M51 来源 completed/attempt 0；随后只 POST 一次创建必要的新 Deep `3ed657fd-ecd8-4bcc-899a-fe8207afbea2`，attempt 0、同工作区/同问题、`research_sources=[]`。下一步只读监控；失败时不立即创建下一条。
- 2026-07-18：新 Deep `3ed657fd-...` 首轮前两组正常完成并进入下一查询：8 source pages、22 asset candidates、8 次 public_page_analysis、36 Trace、Firecrawl 0，attempt 0；尚未首次 gap-check 或 synthesis event。
- 2026-07-18：同一 Deep 首轮继续到 12 source pages、24 asset candidates、17 次 public_page_analysis、65 Trace，Firecrawl 0，attempt 0；页面分析持续完成，仍未首次 gap-check。
- 2026-07-18：新 Deep sequence 91 首次 gap-check 为 47 usable、8 projects、5/6；已覆盖 daylight/old-new/people-vehicle/public-service/sectional，唯一缺 `program_insertion`，gaps 为 uncovered_subquestions/article_analysis_incomplete，Firecrawl 0。下一轮应由 M56 只优先该缺失分支。
- 2026-07-18：Deep `3ed657fd-...` 随后直接终止 `blocked/time_budget_exhausted` 并永久封存：47 usable、8 projects、5/6、25 次正文分析、93 Trace、Firecrawl 0；唯一综合由 M54 fallback completed 2/2/1/2/2。没有第二轮，因此不创建下一条 Run，转入合并离线审计。
- 2026-07-18：终态时序确认首轮六题分别执行 4/4/4/5/5/3 次正文分析并耗时约 4:06/4:27/4:14/4:19/6:35/4:02。进入 M58：离线红测 coverage 未齐时的单题单轮公平分析边界，保证首轮轮转后仍有缺失题恢复窗口；coverage 齐全后 enrichment 不受该边界限制。
- 2026-07-18：逐次分析审计确认 M58 边界可定为每题单轮 3 次：封存 Run 五个已覆盖分支均在前三次内形成有效链，第 4/5 次只增 enrichment；预计最多释放 7 个 45 秒调用窗口。下一步参数化红测 coverage 未齐时只解析/分析 3 页，6/6 后同一查询仍可分析第 4 页。
- 2026-07-18：M58 首个参数化 fixture 因使用 `studio.example` 而未进入可信正文分析，两组均为 0 次；这是测试来源准入错误，不是公平边界行为。改为本地假解析的 ArchDaily 数字项目 URL 后重跑，仍不访问网络。
- 2026-07-18：可信 URL 后 fixture 两组各执行 12 次，原因是默认 completion recovery 把三题都排入测试，每题均分析 4 页；这确认旧生产没有单题上限，但不是目标单元边界。将测试显式设为零 recovery、单查询后再判断 3/4 合同。
- 2026-07-18：六题缩放最坏耗时联合重放已实质 completed 6/6，查询顺序为六题首轮后只恢复 branch-a，并产生唯一 fallback synthesis；首次仅因断言要求每查询必须正好 3 次而失败，实际总分析 19。改为正确合同：每个 `(branch, query generation)` 最多 3 次，总数不超过 21；缓存复用和 deadline 可进一步减少调用。
- 2026-07-18：联合重放第二次仅因错误要求 parser URL 数不高于模型分析数而失败；本地解析后可能因 45 秒分析 reserve 不足而不调用模型，这是既有安全行为。移除两种不同成本计数的绑定，只保留每 `(branch, query generation) <= 3`、恢复查询、时间窗口和综合合同。
- 2026-07-18：M58 六题缩放最坏耗时联合重放最终转绿：查询顺序六题首轮后只恢复 branch-a，七个 query generation 各最多 3 次正文分析，总计 19/上限 21，research deadline 内 completed 6/6；唯一综合 fallback 满足 2/2/1/2/2。单题参数化 3/4 合同也已通过，未调用 Live Provider 或 POST。
- 2026-07-18：M58 目标 9/9、完整 browser-inspection 与 27 项 Provider 契约通过；首次静态门禁仅有测试 fixture 两条 103 字符英文句超出 E501。手工折行这两个字符串后从最终源码重跑，生产逻辑不变。
- 2026-07-18：M58 两条 E501 fixture 折行后的最终源码已重跑 M54–M58 聚焦回归，9/9 全绿；仅既有 TestClient/httpx2 warning。未调用 Provider、未 POST 或 retry Run。下一步串行执行 Ruff lint、format check 与 strict Mypy。
- 2026-07-18：M58 首次最终静态门禁中 Ruff lint 已通过，format check 唯一要求机械重排 `test_browser_inspection.py`，因此 Mypy 尚未执行。只格式化该测试文件，再从最终文件重跑 9 项聚焦回归与完整静态门禁；生产逻辑不变。
- 2026-07-18：机械格式化唯一测试文件后，M54–M58 聚焦回归 9/9、Ruff lint、48 文件 format check、strict Mypy 18 source files 全绿；仅既有 TestClient/httpx2 warning。下一步执行 `git diff --check` 与完整 `scripts/verify.ps1`，继续禁止 Live Run。
- 2026-07-18：M58 最终 `git diff --check` 通过，仅既有 LF→CRLF 提示；完整 `scripts/verify.ps1` 全绿：293 API、76 Board、165 Extension、8 packaged Chrome E2E、Ruff/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全部 exit 0。M58 implementation complete；实现和门禁阶段没有 Live Provider、Run、retry、Firecrawl、视觉来源或截图。
- 2026-07-18：同步 M58 基线时，首次 `HANDOFF.md` 补丁因一条旧句上下文不完全匹配而被原子拒绝，没有部分写入；按当前精确文本重打后已同步。当前唯一下一步收敛为一次代码 reload、active 0 守卫和一条必要 Deep attempt 0；失败后必须回离线，不得连续 POST。
- 2026-07-18：严格 stop 一次、start 一次加载 M58；API `ok`、Provider 梭子蟹 API、模型 `gpt-5.6-sol`、Board 200。创建边界确认 M51 source completed/attempt 0、同工作区历史 Run 12、active 0、最新为封存 `3ed...` 后，只 POST 一次创建必要 Deep `7b69bf9c-86fe-4cb4-976e-1de92dbe69a7`，attempt 0、`research_sources=[]`。下一步仅只读监控，不重发、不 retry、不截图；若失败先回离线诊断。
- 2026-07-18：Deep `7b69bf9c-...` 首个 `program_insertion` 查询按 M58 恰好执行 3 次正文分析后轮转；sequence 15 为 7 usable、1 project、1/6，sequence 16 已进入 `public_service_flows`。Run 仍为 attempt 0，Firecrawl 0；继续只读监控六题首轮与恢复窗口。
- 2026-07-18：Deep `7b69bf9c-...` 已在唯一 attempt 0 终止并永久封存：`blocked/research_synthesis_incomplete`，33 usable、4 projects、4/6、19 次正文分析、117 Trace、Firecrawl 0。M58 把首轮降到 16 次并保留恢复轮，但第 3 轮 `old_new_structure` 连续 3 次分析后，采光只解析未分析、空间序列未获得该轮；唯一综合 `InternalServerError` 且 4/6 形状未产生 fallback。没有 retry 或第二次 POST，下一步只做合并离线诊断、红绿修复和完整门禁。
- 2026-07-18：终态只读审计首次因当前沙箱拒绝启动 WindowsApps `pwsh` 别名而未执行、无任何项目或 Run 副作用；获准改用沙箱外 PowerShell 7 后一次完成本机 API/Trace 读取。该环境问题与 ArchResearch 运行失败无关。
- 2026-07-18：封存 `7b69...` 的生产 `_research_synthesis_cases()` 零写入重建为 4 cases，仅 old-new 形成 1 个同题比较分支，低于 Deep fallback 所需 2 个；`fallback_available=false` 为正确合同。进入 M59：先用多缺口六题 fixture 红测“首轮最多 3、后续未覆盖题每轮 1 次、6/6 后不限”，门禁前禁止 POST。
- 2026-07-18：M59 多缺口六题红测按预期失败于终态 `blocked` 而非 `completed`：旧实现让后续轮的早期缺失题继续消费最多 3 次分析，190 秒 research window 内无法让 A/E/F 全部恢复。下一步只改 coverage 未齐时的 round>1 单题限额为 1，首轮 3 与 6/6 后不限保持。
- 2026-07-18：M59 首次最小实现（round 1=3、round>1=1）后目标测试仍为 `blocked`；现有首个断言未暴露查询/时钟细节，暂不扩大生产修复。先增强失败消息读取 coverage、查询顺序、分析代际和模拟时钟，区分真实路径与 fixture 预算误差。
- 2026-07-18：增强诊断确认 round>1=1 已生效，但 fixture 在 A 恢复后模拟时钟 147 秒、research deadline 190 秒，仅余 43 秒，低于模型搜索既有 45 秒 reserve，故 E/F 未启动。将测试总时限从 280 校准到 310（research deadline 220）：新逻辑可让 A/E/F 各 1 次，旧逻辑仍会让前两题各 3 次并阻断 F；生产边界不再扩大。
- 2026-07-18：校准后的 M59 多缺口六题 fixture 转绿：首轮每题最多 3 次，后续 A/E/F 各恰好 1 次，在 research deadline 内完成 6/6；唯一 synthesis `InternalServerError` 继续由 M54 fallback 完成 Deep 2/2/1/2/2。下一步扩大 M54–M59 聚焦、完整 browser/Provider 与静态门禁。
- 2026-07-18：M54–M59 聚焦 9/9、完整 browser-inspection 84 项、Provider 27 项全绿；仅既有 TestClient/httpx2 warning。M59 生产增量仍只有 follow-up limit 常量与 round>1 选择，下一步 Ruff/format/Mypy、`git diff --check` 和完整全仓门禁。
- 2026-07-18：M59 首次静态门禁中 Ruff lint 已通过，format check 唯一要求机械重排 `test_browser_inspection.py`，Mypy 尚未执行。只格式化该测试文件，再从最终源码重跑 M54–M59 9 项与完整静态门禁。
- 2026-07-18：机械格式化唯一测试文件后，M54–M59 聚焦 9/9、Ruff lint、48 文件 format check、strict Mypy 18 source files 全绿。下一步 `git diff --check` 与完整 `scripts/verify.ps1`；仍无 Live Run。
- 2026-07-18：M59 最终 `git diff --check` 通过，仅既有 LF→CRLF 提示；完整 `scripts/verify.ps1` 全绿：293 API、76 Board、165 Extension、8 packaged Chrome E2E、Ruff/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全部 exit 0。M59 implementation complete；实现阶段没有 POST、Provider、Firecrawl、视觉来源、截图、暂存或提交。
- 2026-07-18：M59 Live 前首次只读守卫因 PowerShell 将 13 条 JSON Run 嵌套为单元素数组而错误输出 `runs=1/active=1`；在 stop/start 或 POST 前即停止，无副作用。改为逐项枚举后精确确认同工作区 13 条、active 0、最新 `7b69...` blocked/attempt 0；staged 0、健康与模型守卫通过。
- 2026-07-18：严格 stop 一次、start 一次加载 M59；API `ok`、模型 `gpt-5.6-sol`、Board 200。创建边界再次逐项确认同工作区 13 条、active 0、M51 source completed/attempt 0、最新 `7b69...` blocked 后，只 POST 一次创建必要 Deep `aa1923fd-45cc-44f4-9a7c-edd6565b1994`，attempt 0、`research_sources=[]`。下一步仅只读监控，不重发、不 retry、不截图。
- 2026-07-18：Deep `aa1923fd-...` 首轮前两题均按 3 次分析上限轮转：功能植入有 1 pass 但尚未 article-ready，公众/后勤流线已正式覆盖；sequence 28 为 17 usable、2 projects、1/6，累计 6 次正文分析、Firecrawl 0。已进入第 3 题，继续只读监控。
- 2026-07-18：Deep `aa1923fd-...` sequence 70 首轮终点为 30 usable、2 projects、4/6、六题各 1 pass、16 次正文分析；缺 program/daylight。round 2 只从 program 开始，首个搜索返回 0 后将轮到 daylight；当前约用时 16 分钟、Firecrawl 0，继续只读监控。
- 2026-07-18：Deep `aa1923fd-...` 已在唯一 attempt 0 终止并永久封存：`blocked/budget_exhausted`，35 usable、2 projects、4/6、program/daylight 各 7 passes、22 次正文分析、170 Trace、Firecrawl 0。M59 round 2–7 公平切片已真实通过；唯一综合 deterministic fallback 完成 Deep 2/2/1/2/2。没有 retry 或第二次 POST，下一步离线审计两题查询/返回页/正文资格。
- 2026-07-18：两题强页字段审计确认 evidence gate 正确；首次 Python 输出仅因 GBK 无法编码 `•` 在打印前失败，设置本次 stdout UTF-8 后同一零写入查询完成。Direct Playwright scratch 又证明两个 Provider 失败页当前快照无对应设计机制正文，故不做页分析重试。下一步仅 scratch 验证零结果域向 ArchDaily EN 的单次文本回退；禁止 POST。
- 2026-07-18：ArchDaily fallback 与精简查询 scratch 均未改善结果，搜索层方案撤销；随后两张 Designboom 0/1 页面正文 scratch 发现明确 program/daylight 条件—操作—结果。代码审计定位 `_persist_public_page_analysis()` 在 evidence 校验前丢弃低 relevance。进入 M60：先红测 evidence-complete relevance 1 必须以有效 2 持久化，同时 evidence-incomplete relevance 1 仍为 0；门禁前禁止 POST。
- 2026-07-18：M60 双侧红测结果为 `F.`：证据完整 relevance 1 旧实现仍为 0/3，证据不完整 relevance 1 正确保留 0/3。最小修复仅在逐字 context/mechanism/transfer 完整时计算 `effective_relevance=max(raw,2)`，否则低分拒绝不变。
- 2026-07-18：M60 执行层双侧测试 2/2 转绿；相邻 Provider 输入红测随后按预期失败于缺少“完整链时 relevance 至少为 2”的一致性文案。只补该提示约束，执行层逐字校验继续作为最终安全门。
- 2026-07-18：用户要求停止盲修后完成只读耗时审计：七条 Deep 合计 202.8 分钟，正文分析槽占 167.9 分钟；172 次正文分析中 77 次未增加证据并消耗约 74.6 分钟。7 月 17 日以来 35 条 Live Run 合计约 12.6 小时，仅 3 条 completed；当前 active 0。没有修改运行数据、创建 Run、执行门禁或截图。
- 2026-07-18：用户批准开始项目级多来源文字证据。M60 独立 relevance-floor 方案标记为 superseded，但已有局部改动全部保留并纳入 M61。M61 先建立同项目跨来源合并、错误项目隔离、一次补查/两页上限、部分结果不整体 blocked、文字 coverage 前无图片批次的行为红测，再做最小实现；整组通过前不跑完整门禁，当前禁止 POST。
- 2026-07-18：M61 第一组行为红测 7/7 精确为红：部分正文分析不会创建候选，因而同项目两来源无法合并且异项目也无可隔离记录；没有项目定向补查；1/3 有用 precedent 仍为 blocked 且图片批次仍可执行；OpenAI 单页超时仍请求两次并报告 90 秒 worst-case。失败均对应待替换的旧合同，没有测试装配或 Live 副作用。下一步只改执行层项目证据聚合、补查边界、终态/图片顺序和单次请求合同。
- 2026-07-18：M61 最小实现已完成相邻验证。部分 context/mechanism 事实现在以 claim-level URL/excerpt 持久化；严格标准化项目标题只把同项目证据合并到一个候选，异项目保持隔离；article-ready、coverage 与 synthesis 接受同候选的多来源逐字证据。每个项目只执行一次多域定向文字补查，调用方即使无视 `limit=2`，执行层也硬截断为两页；补充页跨子问题复用，模型按分支分析项目文字包。正文分析超时不再同页二次请求，worst-case 从 90 秒降为 45 秒。precedent 有正式分支但未全覆盖时终态为 partial，浏览器必需检视失败仍保持 blocked；文字未全覆盖时远程图片批次为零。
- 2026-07-18：M61 目标 8/8、完整 browser-inspection、27 项 Provider、workflow/results/schema 相邻套件、Ruff lint/41 文件 format check、strict Mypy 18 source files与 `git diff --check` 均通过。新增 Deep 4/6 零 Provider 形状验证确认有引用综合时终态为 partial 而非 blocked。仅既有 TestClient/httpx2 warning 与 LF→CRLF 提示；未创建 Run、未调用真实来源/Provider/Firecrawl、未截图。下一步只运行一次完整仓库门禁。
- 2026-07-18：会话 catch-up 首次命中禁用的 Windows Store `python` alias，第二次确认本机也没有 `py` launcher；没有项目副作用。改用 Codex 工作区 bundled Python 后成功恢复 29 条未同步上下文，并确认此前完整门禁的 PowerShell 5 原生命令退出传播异常。
- 2026-07-18：没有重复整个仓库门禁，只补跑一次根 `pnpm run check`，并显式检查 `$LASTEXITCODE`。结果 Board 6 files / 76 tests、Extension 16 files / 165 tests 全绿，两端 lint、typecheck 与 production build 均完成，命令 exit 0。结合此前 301 API、Ruff、strict Mypy、8 packaged E2E 和 `git diff --check`，M61 标记为 implementation complete；全程无 POST、Live Provider、Firecrawl、Deep、截图、暂存或提交。
- 2026-07-19：用户在看到唯一 Live Deep 计划后明确回复“开始”，授权范围仅为一条全新 attempt 0。创建前只读守卫确认 API `ok`、Provider 梭子蟹 API、模型 `gpt-5.6-sol`、Board 200；M51 来源 `a2cf2e20-...` 为 completed/attempt 0、同工作区历史 Run 14、active 0、最新 `aa1923fd-...` blocked/attempt 0；分支未变且 staged 0。现有服务仍是 2026-07-18 M59 启动实例，因此下一步只 stop/start 一次加载 M61，健康与 active 0 复核后只 POST 一次。
- 2026-07-19：严格 stop 一次、start 一次加载 M61；复核 API `ok`、模型 `gpt-5.6-sol`、Board 200、来源 Quick completed/attempt 0、历史 Run 14、active 0、staged 0。随后唯一一次 POST 创建 Deep `0715ab7e-6580-42ae-9d84-77a0c772c760`，attempt 0、同工作区/同问题、`research_sources=[]`；响应明确为 created。下一步只读监控，不重发、不 retry、不创建第二条、不启用 Firecrawl/视觉来源/截图。
- 2026-07-19：新 Deep 首轮监控至 sequence 32：4 次 `public_page_analysis` completed、2 次 failed；两次失败均为单条 `APITimeoutError` 且 `retry_count=0`，之后继续检索/检视，证明 M61 不再同页立即二次请求。Run 仍为 attempt 0，Firecrawl 0，尚无 coverage checkpoint；继续只读监控。
- 2026-07-19：新 Deep 已进入首轮第 6/42 个查询 `daylight_strategy`，累计 7 次正文分析 completed、8 次 failed。中途 De Nederlandsche Bank 原始 relevance 1 / enriched 2 后经终态审计确认只是两个同项目资产写入部分 context，未形成 mechanism/transfer；Emerson relevance 1 仍 enriched 0。该指标只证明低分部分 claim 可保留，不能证明正式 coverage。attempt 0、Firecrawl 0，继续只读监控首轮 gap-check。
- 2026-07-19：唯一 Deep sequence 182 首个正式 gap-check 为 38 usable、5 projects、4/6；覆盖 `old_new_structure/program_insertion/public_logistics/sectional_layers`，缺 `program_sequence/daylight_strategy`，两题各 7 passes。正文分析 13 completed / 11 failed，Firecrawl 0；文字未齐时图片批次严格为 0。Run 尚未终止，继续只读等待唯一 synthesis 与 M61 partial 终态；不创建第二条。
- 2026-07-19：唯一 Deep `0715ab7e-...` 已在 attempt 0 终止并永久封存为 `partial/research_synthesis_incomplete`：38 usable、5 projects、4/6、13 次正文分析 completed / 11 failed、184 Trace、Firecrawl 0、图片批次 0。M61 将同一 4/6 有用引用形状从 blocked 改为 partial 的 Live 合同通过；但唯一综合未完成，M53 Deep 验收仍失败。下一步只做 coverage 与 synthesis 的合并离线审计，不 retry、不创建第二条。
- 2026-07-19：合并审计定位 M62：项目补查只读取同项目 rank 第一张 candidate；封存 Run 的多个项目第一张为空分析、后续资产才有机制/部分 context，故没有执行多来源路径。新增行为测试构造第一张空资产、第二张同项目已有完整机制、当前分支缺失的形状；旧实现按预期红于 `parser.search_calls == 0` 而目标为 1。综合另经零写入重建确认为 7 cases、仅 1 个比较分支、fallback unavailable，保持原证据门。下一步只聚合同项目全部 candidate 判断补查资格。
- 2026-07-19：M62 最小实现聚合同项目全部候选判断补查资格；第一张空资产不再遮蔽后续候选的已验证机制。首次绿测已执行 search/add=1，只因合法 rerank 改变列表首项而失败；断言改为按 `program_sequence` 定位补齐资产后通过。主 URL context claim 与补充 URL mechanism claim 均精确绑定。
- 2026-07-19：机械格式化新增测试后，M61/M62 聚焦 6/6、完整 browser-inspection、Provider 27/27、Ruff lint、41 文件 format check、strict Mypy 18 source files 全绿；仅既有 TestClient/httpx2 warning。下一步 `git diff --check` 后只运行一次最终全仓门禁，继续禁止 POST。
- 2026-07-19：M62 `git diff --check` 通过。唯一全仓门禁中 API 302/302、Ruff、48 文件 format、strict Mypy、两端 lint/typecheck、Extension 165/165 均通过；并发 Board 无失败详情 exit 1，PowerShell 7 正确中止并返回非零。没有重复全仓门禁；独立 Board 6 files / 76 tests 通过，随后只补跑被中断的两端 build 与 packaged E2E，8/8 通过。结合 30/108 evaluation，M62 implementation complete；没有 POST、第二条 Run、Firecrawl、视觉来源、截图、暂存或提交。
- 2026-07-19：新会话按 HANDOFF 顺序恢复完成：M62 为 implementation_complete、M53 仍 in_progress；Deep `0715ab7e-6580-42ae-9d84-77a0c772c760` 保持封存 partial 且绝不 retry，工作树全部既有修改/未跟踪文件保留。用户已明确批准 HANDOFF 的唯一下一步；先只读核验 active 0 与运行实例仍为 M61，再严格重载一次 M62，并只创建一条全新 Deep attempt 0。若失败立即回到离线合并诊断，不连续 POST；继续禁止 Firecrawl、视觉来源、截图、暂存、提交和推送。
- 2026-07-19：创建前精确守卫确认目标工作区 15 条历史 Run、active 0，M51 source completed/attempt 0，封存 `0715ab7e-...` 仍 partial/attempt 0，staged 0；服务启动时间确认为 M61 实例。严格 stop 一次、start 一次加载 M62 后，API `ok`、Provider 梭子蟹 API、模型 `gpt-5.6-sol`、Board 200，历史状态再次全量通过。随后唯一一次 POST 创建 Deep `eea9cf24-58d1-451d-828a-ec8aa7706a59`，attempt 0、`research_sources=[]`；下一步只读监控该 Run，不重发、不 retry、不截图。
- 2026-07-19：M62 Live Deep `eea9cf24-...` 已终止并永久封存 `partial/no_new_assets`：24 usable、6 projects、4/6，缺 sectional/public 两题且各 7 passes；23 次正文分析 12 completed / 11 failed，Firecrawl 0、图片批次 0。唯一 synthesis 的 `APIConnectionError` 已由 deterministic fallback 完成 2/2/1/2/2。终态守卫为历史 Run 16、active 0、旧封存 `0715ab7e-...` 未变、staged 0；未 retry 或创建第二条。
- 2026-07-19：失败后的合并离线诊断确认 M62 eligibility 实际触发 5 次补查但全部 result_count 0；生产查询把项目名、通用 case-study 词和过多分支词绑定到单次多域搜索，且 public_interface 退化为泛化 adaptive-reuse 词。进入 M63：先以纯函数红测锁定简短“项目名 + 少量分支焦点”及公共界面焦点，再只改查询生成并跑离线门禁；当前禁止重载、POST、真实来源、Firecrawl、视觉来源和截图。
- 2026-07-19：M63 纯函数红测按预期失败：sectional 补查仍输出项目名加 `architecture case study section mezzanine atrium vertical space`，未达到精简的项目名加两词焦点；旧实现因此在第一个断言处停止。下一步仅修改 `_project_text_supplement_query()`，补齐 public-interface 识别并缩短各分支焦点，然后重跑同一节点。
- 2026-07-19：M63 最小 query-only 实现完成首轮绿测：sectional 精简为项目名加 `section mezzanine`；共享大厅/室内外连续等公共界面题明确使用 `public interface shared lobby`；其余既有分支也只保留 2–3 个焦点词。没有增加搜索次数、页面、域、timeout 或 retry。纯函数节点与 M61/M62 同项目合并、异项目隔离、一次两页补查、多候选资格、partial/无图片合同共 6/6 通过；下一步扩大 public-pages、完整 browser/Provider 与静态门禁。
- 2026-07-19：M63 相邻完整 public-pages、browser-inspection 与 Provider 套件退出 0；命令开头仅计划复读因工作目录在 `apps/api` 而找不到根 `task_plan.md`，未影响测试且不重复整组。后续从绝对根读取计划，再执行 Ruff lint/format、strict Mypy 与 `git diff --check`。
- 2026-07-19：M63 最终静态门禁通过：Ruff lint、48 文件 format check、strict Mypy 18 source files 与 `git diff --check` 全绿，仅既有 LF→CRLF 提示。生产增量仍只有 query builder，未调用真实来源、Provider、Firecrawl 或 POST。下一步只运行一次完整 `scripts/verify.ps1`；若门禁中断，按失败点补齐，不盲目重复全仓。
- 2026-07-19：M63 唯一完整 `scripts/verify.ps1` 全绿：303 API、76 Board、165 Extension、8 packaged Chrome E2E、Ruff/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全部 exit 0。M63 标记 implementation complete；当前服务仍加载 M62，本轮不重载、不创建下一条 Live Run。未来只有获得新的明确 Live 授权后，才先重载 M63、复核 active 0，并只 POST 一条全新 Deep attempt 0。
- 2026-07-19：最终守卫确认分支 `codex/archresearch-v2-1`、94 个既有 changed/untracked 路径全部保留、staged 0、`git diff --check` 通过；API `ok`、模型 `gpt-5.6-sol`、Board 200、历史 Run 16、active 0。旧 `0715ab7e-...` 保持 partial/research_synthesis_incomplete/attempt 0，新 `eea9cf24-...` 保持 partial/no_new_assets/attempt 0；没有 reset、checkout、clean、stage、commit 或 push。
- 2026-07-19：用户以“开始”明确批准 M63 后唯一一条全新 Deep attempt 0。恢复确认 M63 为 implementation_complete、M53 为 in_progress，当前服务仍加载 M62；下一步先只读复核健康、模型、Board、staged 0、历史两条封存 Run 与 active 0，全部通过后严格 stop/start 一次加载 M63，并只 POST 一次。失败后立即回离线合并诊断，不创建第二条。
- 2026-07-19：M63 Live 创建前守卫确认历史 Run 16、active 0，M51 source completed/attempt 0，两条失败 Deep 均保持 partial/attempt 0，staged 0。严格 stop 一次、start 一次加载 M63；重载后 API `ok`、Provider 梭子蟹 API、模型 `gpt-5.6-sol`、Board 200、历史状态与 active 0 再次通过。下一步只 POST 一次全新 Deep attempt 0。
- 2026-07-19：唯一一次 POST 已创建 M63 Live Deep `c553f2be-52fe-436e-8891-0d4fadcad5e0`，attempt 0、`research_sources=[]`，响应明确为 created。下一步只读监控该 Run 到终态，不重发、不 retry、不创建第二条、不启用 Firecrawl/视觉来源或截图。
- 2026-07-19：M63 Live 中断前最新只读 checkpoint 为 33 usable、5 projects、4/6，缺 program-insertion 与 old-new-structure，两题各 5 passes；24 次正文分析 13 completed / 11 failed，4 次项目补查仍为 0，Firecrawl/图片批次 0。用户中断的只是监控等待，未发送写请求。
- 2026-07-19：继续时 API/Board 已无 listener，进程状态文件仍是陈旧 PID；未立即重启。SQLite `mode=ro` 确认 Deep `c553f2be-...` 已在服务退出前永久封存为 `partial/research_synthesis_incomplete`：46 usable、5 projects、4/6，program-insertion 与 old-new-structure 各 7 passes，198 Trace、active 0。下一步只做补查/正文/综合的合并离线诊断，不 retry 或创建第二条。
- 2026-07-19：合并诊断确认 26 次正文分析为 14 completed / 12 failed，4 次 project_text_supplement 全部 0，唯一 synthesis `APIConnectionError`；6-case 生产重建无法生成 fallback，合同正确。进一步对四次补查重建发现搜索项目名带 ArchDaily 栏目/地点前缀，而候选项目名干净。进入 M64：先红测 page/source 标题冲突与 cached-case reuse，再只改补查搜索名来源；当前服务保持停止，禁止 POST、Provider、真实来源、Firecrawl 和截图。
- 2026-07-19：M64 两层红测均精确复现旧缺陷：直接 helper 因 `RENOVATION` page title 与 clean source/candidate identity 不同而没有补齐 `program_sequence`；完整 workflow 同样未记录一次 bounded supplement search。调用链确认 direct source 可保留干净搜索标题，但既有候选可能按 parsed-page 标题建立。下一步候选只在 clean source identity 或 parsed-page identity 任一严格匹配时聚合，搜索名仍只用 clean source/case title。
- 2026-07-19：M64 最小实现已使两层目标 2/2 转绿：候选只接受 clean source identity 或同页 parsed-title identity；首次补查使用 clean source title，并把两种 identity 登记为同一 attempted/pages 缓存别名，后续 cached branch 复用同一两页文字包而不发第二次搜索。搜索次数、两页上限、项目隔离和证据门不变。下一步扩大 M61–M64 聚焦、完整 browser/Provider 与静态门禁。
- 2026-07-19：M64 完整 public-pages、browser-inspection 与 Provider 相邻套件退出 0；带前缀 parsed title 的一次补查和后续 branch bundle 复用均保持。下一步运行 Ruff lint/format、strict Mypy 与 `git diff --check`，继续禁止服务重启和 POST。
- 2026-07-19：M64 静态门禁全绿：Ruff lint、48 文件 format check、strict Mypy 18 source files 与 `git diff --check` 通过，仅既有 LF→CRLF 提示。生产改动限于 supplement identity/source title 与缓存别名；下一步只运行一次完整 `scripts/verify.ps1`，不重启服务或创建 Run。
- 2026-07-19：M64 唯一完整 `scripts/verify.ps1` 全绿：303 API、76 Board、165 Extension、8 packaged Chrome E2E、Ruff/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全部 exit 0。M64 标记 implementation complete；服务仍停止。根据持续授权与本次“继续”，下一步先清理陈旧进程状态并加载 M64，复核 active 0 后只 POST 一条全新 Deep attempt 0。
- 2026-07-19：M64 Live 创建前 SQLite 守卫确认历史 Run 17、active 0，M51 与三条封存 Deep 均保持原终态/attempt 0，staged 0；8000/5173 无 listener，仅有陈旧状态文件。`stop.ps1` 一次清理后 `start.ps1` 一次加载 M64，重载后 API `ok`、Provider 梭子蟹 API、模型 `gpt-5.6-sol`、Board 200、active 0 再次通过。下一步只 POST 一条全新 Deep attempt 0。
- 2026-07-19：唯一一次 POST 已创建 M64 Live Deep `8d12e14b-c79f-4379-b8e2-6b61dea424e1`，attempt 0、`research_sources=[]`，响应为 created。下一步只读监控该 Run 到终态，不重发、不 retry、不创建第二条、不启用 Firecrawl/视觉来源或截图。
- 2026-07-19：M64 Live Deep `8d12e14b-c79f-4379-b8e2-6b61dea424e1` 已永久封存为 `partial/research_synthesis_incomplete`：38 usable、4 projects、5/6，唯一缺 flow-separation、7 passes；22 次正文分析为 14 completed / 8 failed，2 次补查均 0，Firecrawl/图片批次 0。唯一 synthesis `InternalServerError`；6-case 零写入重建中 flow branch 0、fallback unavailable，合同正确。未 retry 或创建第二条。
- 2026-07-19：flow 合并审计显示 6 次正文分析 5 completed / 1 timeout；Shitang 与 Huzhou 仅保留项目/功能 context，均明确缺少公众—后勤入口、核心筒、装卸、消防和无障碍机制，Gate M 唯一分析 timeout，Designboom/Dezeen 两页 relevance 0。下一步按真实 query_attempts schema 审计七轮查询与召回，不调用真实来源。
- 2026-07-19：只读 Trace 与调用链确认持久化 `query_attempts` 是 provider planner prompt，不是本地浏览器 URL；M64 的真实 precedent 搜索每轮单站轮换，走 `_compact_site_query()`。初始把 provider prompt 重建到多域压缩器的假设已撤销并记入 task plan，未据此修改生产代码。
- 2026-07-19：M65 目标站点语言红测先因测试误把 Designboom/Dezeen 的 `s=` 参数当成 `q=` 而报 `KeyError`；修正测试读取后取得真实红线：两个英文站均错误得到中文查询。生产最小修复后目标 4/4 通过，其中 3 个站点语言案例与 1 个 M64 多域精确项目保护案例均为 green。下一步运行 public-pages/browser/Provider 相邻套件、静态门禁和唯一完整仓库门禁；禁止 Live Run。
- 2026-07-19：M65 相邻 public-pages/browser-inspection/Provider 套件通过；首次静态门禁仅要求机械格式化本轮测试，格式化后 Ruff、48 文件 format、strict Mypy 18 source files 与 `git diff --check` 全绿。唯一完整 `scripts/verify.ps1` 随后通过 307 API、76 Board、165 Extension、8 packaged E2E、两端 lint/typecheck/build 与 30/108 evaluation；M65 标记 implementation complete。
- 2026-07-19：最终只读守卫确认分支仍为 `codex/archresearch-v2-1`，全部既有 changed/untracked 路径保留，staged 0；数据库 active Run 0，M64 Live `8d12e14b-...` 仍 sealed partial/attempt 0；API ok、Board 200。当前进程在 M65 修改前启动，仍加载 M64。本次授权的唯一 Live 已执行并封存，不继续创建 Run；后续若获批准，必须先重载 M65、再复核 active 0。
- 2026-07-19：用户以“下一步”明确批准 M65 的唯一 Live 验收。前置守卫确认四条既有 Deep 均保持 partial/attempt 0、active 0、staged 0，全部 changed/untracked 路径保留；API/Board 已离线且仅剩陈旧进程记录。严格执行 `stop.ps1` 一次清理、`start.ps1` 一次加载 M65；重载后 API ok、Provider 梭子蟹 API、模型 `gpt-5.6-sol`、Board 200、active 0。下一步只 POST 一条全新 Deep attempt 0，明确 `research_sources=[]`。
- 2026-07-19：唯一一次 POST 已创建 M65 Live Deep `76f52c79-5fd9-4738-837d-5576eb3a72cd`，响应为 created、attempt 0、budget deep、`research_sources=[]`。下一步只读监控该 Run 到终态，不重发、不 retry、不创建第二条、不启用 Firecrawl/视觉来源或截图。
- 2026-07-19：M65 Live 在第二轮末的只读 Trace gap-check 已达到 31 usable、6 projects、6/6、gaps 0、3 multi-asset projects；六题 passes 为 daylight 2、public-logistics 2、program-zoning 2、old-new 2、sectional 1、shell-insertion 2。Run 因 `insufficient_subquestion_assets` 继续 enrichment，尚未进入 synthesis；下一步继续只读监控它能否在预留窗口内完成深度与综合。
- 2026-07-19：M65 Live Deep `76f52c79-5fd9-4738-837d-5576eb3a72cd` 已完成 `completed/completion_satisfied`：attempt 0、51 usable、9 formal projects、6/6、gaps 0；21/21 queries completed，正文分析 29 completed / 8 failed，Trace 228。唯一 synthesis `APIConnectionError` 由 M54 deterministic fallback 成功生成 Deep 2/2/1/2/2，case_count 14；Firecrawl/视觉/图片批次 0，active Run 回到 0。未 retry 或创建第二条。
- 2026-07-19：M53 当前规则三档 Live 验收完成：Quick M51、Balanced `d995bed5-...`、Deep M65 均 accepted。M53 与 M65 标记 complete；所有旧失败 Deep 保持 sealed attempt 0。staged 0、`git diff --check` 通过，工作树全部既有修改与未跟踪文件保留。当前主线不再需要 Live Run。
- 2026-07-19：用户要求确认三种深度对不同问题是否都能跑通，并指出 Board 找不到最新 Run。M66 只读审计确认既有三档 Live 均属同一问题家族，不能作为跨问题证明；当前 Board 虽选中 `M6 真实研究验证`，却恢复到旧的 32-reference/4-subquestion 未完成 Run。下一步检查 API 列表与前端排序/恢复逻辑，并建立多问题 × 三深度的离线确定性验证；禁止新增 Live Run和截图。
- 2026-07-19：记录可见性根因已定位：最新 M65 Deep 存在于 `作品集实测 · 旧厂房社区文化中心`，不是当前选中的 `M6 真实研究验证`。API 对目标工作区按 created_at 降序正确返回 `76f52c79-...` 为第 1；Board 只展示当前工作区前三条，因而用户看到的 7/17 旧记录不是排序错误或数据丢失。下一步在现有 Board 中切换到目标工作区并验证记录卡片。
- 2026-07-19：已把用户当前 Board 切换到 `作品集实测 · 旧厂房社区文化中心` 并打开首条 M65 Deep；DOM 确认“研究已完成 / 51 张可用参考 / 深入研究结论 / 6 个子问题”可见。另发现详情页显示 0 个正文项目，与 coverage 的 9 formal projects 冲突；先继续多问题三档矩阵和落库/展示口径诊断，不创建 Live Run。
- 2026-07-19：M66 新增 3 问题族 × 3 深度的 9-cell workflow 回归。前五轮 fixture 红线依次验证 synthesis 不能绕过正文 excerpt、空 parser 不产生案例、不可信域被拒、precedent 以 local-browser 入口为准、ArchDaily 还需数字路径加 `Project / Studio` 标题；最终完整 text-first fixture 9/9 green。临时数据库确认每题 Quick 3/3、Balanced 4/4、Deep 6/6，且综合结构分别为 1/0/0/0/1 与 2/2/1/2/2。
- 2026-07-19：Board 新红测证明仅把 text-grounded candidate 的 observations 清空，就会从 1 个正文案例降为 0；生产最小修复删除 `analysisReady` 中的 `observations.length`，保留 context/mechanism/transfer/text-excerpt 四重门，目标测试已 green。下一步跑完整 workflow/Board 相邻回归、静态与全仓门禁，再重载本地 Board 验证真实 M65 Deep 案例可见。
- 2026-07-19：M66 恢复后的只读 Board 节点确认正确工作区 `作品集实测 · 旧厂房社区文化中心` 仍被选中，最近任务恰有 3 条；第一条为 M65 Deep 的“深入 · 51 张参考 · 7/19 · 研究已完成”。下一步只打开该首条记录，核对正文案例数量与 9 个 formal projects 的统计口径；未创建或 retry Run，也未截图。
- 2026-07-19：打开首条真实 M65 Deep 后，Board 已显示正文案例及逐字证据，0-case 修复通过页面实证；摘要当前显示“14 个已完成正文分析的项目案例”，但 Run coverage 为 9 formal projects。下一步先补 Board 红测，要求跨子问题重复案例按项目身份去重为 9，再做最小统计修复；未创建或 retry Run，也未截图。
- 2026-07-19：新增 Board 行为红测以两个 text-grounded 结果资产复现同一项目重复计数；旧实现按预期找不到“1 个项目案例”，同时页面已有且仅有 1 个合并项目 dossier。失败只落在摘要使用 `caseResults.length`，下一步改为唯一项目计数，不改分支归组、证据门或 API。
- 2026-07-19：用户补充 M66 验收口径：三种深度全部以项目案例包为基准，不把单页等同于案例或在单页上死磕。先完成 Board 唯一项目计数，再审计并按需参数化现有 project-level multi-source 行为测试到 Quick/Balanced/Deep；继续禁止新增 Live Run、retry 和截图。
- 2026-07-19：将同项目双正文来源的完整 workflow 回归从 Quick 扩成 Quick/Balanced/Deep 参数矩阵，三档 3/3 通过，coverage 分别为 3/3、4/4、6/6；同一案例合并两页证据且每条 claim 保留自己的 URL。Board 唯一项目计数红测也已在最小 `Set(project)` 修复后转绿；下一步扩大相邻测试与静态检查。
- 2026-07-19：M66 最终源码的完整 `scripts/verify.ps1` 一次通过：310 API、78 Board、165 Extension、8 packaged E2E、Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全绿。下一步仅在现有本地 Board 复核真实 M65 Deep 显示 9 个唯一项目；不创建或 retry Run、不截图。
- 2026-07-19：真实 M65 Deep 页面最终复核通过：正文案例摘要为 9 个唯一项目，与 coverage 一致；页面仍呈现 13 个按子问题归组的项目 dossier，37 张网页图片明确仅作待核验预览。没有丢失跨题案例关系，也没有把资产数误称为项目数；下一步只做浏览器交付标记、规划收口与 Git 只读守卫。
- 2026-07-19：现有 Board 结果页已标记为 deliverable 并保留给用户。M66 标记 complete，HANDOFF 同步三类问题 × 三档 9/9、三档项目级多来源不变量、最新 Deep 的真实工作区及 9 个唯一项目口径；当前不再有获批 Live 或产品动作。最后只运行 `git diff --check`、staged=0、active Run=0 只读守卫。
- 2026-07-19：最终 active 守卫首次使用 PowerShell 枚举工作区时错误展平 API 数组并得到 `Workspace not found`；该命令只有 GET，输出未作为结论，也未重复。`git diff --check` 已 exit 0（仅既有 LF→CRLF 提示），staged 0；下一步改用 Python 标准 JSON 一次性核验 active Run 与 M65 Deep 终态。
- 2026-07-19：Python 标准 JSON 最终只读守卫有效枚举 3 个工作区、60 条历史 Run，确认 active Run 0；M65 Deep `76f52c79-...` 保持 `completed`、attempt 0、工作区 `fd3b06cd-...`。M66 工作内容全部完成，最后从最终文档状态重跑 Git whitespace/staged/status 守卫后交付。
- 2026-07-19：用户否决成品顶栏暴露 `M6 真实研究验证` 与模糊 `新建`。启动 M67 production workspace naming polish；Impeccable product/polish 规范确认这是命名与心智模型的功能性漂移。下一步只读追踪工作区名称来源和创建动作，实现前先补行为测试；不截图、不创建 Run、不 stage/commit/push。
- 2026-07-19：M67 根因确认：内部名称来自持久化 Workspace，API 已支持 PATCH，Board 尚无 rename client/UI；`新建` 是前端模糊文案。决定先红测“新建工作区”和现有工作区重命名，再最小接入 PATCH；随后只迁移本机三条内部验收名称，不改 workspace ID 或历史 Run。首次只读 Python 列表因控制台编码显示乱码，未据此猜测 brief，下一次改用 UTF-8 输出。
- 2026-07-19：UTF-8 只读清单取得三条工作区的精确名称、brief 与 Run 数。方案收敛为：不新增重命名 UI；红测明确创建动作与上次工作区选择持久化，前端最小修复后通过已有 PATCH 一次性迁移三条内部名称。保持 60 条历史 Run、workspace ID、brief 和证据不变。
- 2026-07-19：M67 两条 Board 红测按预期失败：旧顶栏找不到“新建工作区”，且预存 `workspace-two` 后仍被初始化为首个 `workspace-live`。失败分别锁定模糊文案和强制最老工作区；下一步只新增一个本地选择键并改两处按钮文案，不改布局或研究加载流程。
- 2026-07-19：M67 前端最小修复完成：`新建工作区 / 取消新建 / 创建工作区` 文案明确；`archresearch.activeWorkspaceId` 在初始化、切换和创建时恢复/更新。两条目标测试 2/2 green。随后 active 0 与旧名称精确守卫通过，已有 PATCH 将三条内部名称迁为项目语义名称；60 条历史 Run 与所有 ID 保持不变。下一步跑完整 Board 与静态门禁，再做无截图真实页面 QA。
- 2026-07-19：Board 80/80、lint、typecheck、production build 已通过。无截图真实页面 QA 第一轮确认三个新名称及“新建工作区”均可见，旧内部词为 0；当前浏览器尚无 remembered selection，故首次 fallback 到历史研究。下一步选择目标工作区、刷新并验证选择保持，再测 390px DOM 几何。
- 2026-07-19：真实选择器已切到 `旧厂房社区文化中心`，最近任务立即恢复完成 Deep 51-reference 卡片，旧工作区数据未串入。下一步刷新同一 tab；若仍选中第三项则本地记忆链验收通过，然后临时切到 390px 仅做 DOM overflow/触控尺寸检查。
- 2026-07-19：刷新同一真实页面后，`旧厂房社区文化中心` 在异步内容完成前即保持 selected，选择记忆链验收通过。下一步按 Browser viewport 能力临时设 390px，读取顶栏/控件 DOM 几何与 44px 触控高度；禁止截图，检查后恢复 viewport。
- 2026-07-19：本地浏览器确认支持临时 viewport override，并要求完成后 reset。下一步仅设置 390×844、读取 `.app-header` / `.workspace-switcher` / select / create button 的边界与页面 scrollWidth，不采集图像。
- 2026-07-19：390×844 override 已生效；首次只读几何 evaluate 因受限页面环境没有 `HTMLSelectElement` 构造器而失败，未改页面或数据。该写法封存，下一步取新 DOM snapshot 后改用普通 tag/value/selectedIndex 读取，不重复构造器判断。
- 2026-07-19：新 DOM snapshot 后改用 tag/value/selectedIndex 的只读几何检查成功。390px 下页面 scrollWidth=clientWidth=375，select 与 create button 均 44px 高、同排且未越界，当前工作区名称正确；无截图。下一步 reset 临时 viewport，运行一次完整仓库门禁与最终 Git/active 守卫。
- 2026-07-19：临时 390px viewport 已 reset；默认产品视图仍选中 `旧厂房社区文化中心`，顶栏为三个项目语义名称 + “新建工作区”，最近任务第一条仍是完成 Deep 51-reference。浏览器 QA 已结束，下一步保留该首页为 deliverable 后不再调用浏览器。
- 2026-07-19：修正后的产品首页已作为 deliverable 保留。M67 最终完整 `scripts/verify.ps1` 一次通过：310 API、80 Board、165 Extension、8 packaged E2E、Ruff/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全绿。M67 标记 complete，HANDOFF 已同步；最后只读核验 active 0、60 Runs、三个名称、staged 0 与 `git diff --check`。
- 2026-07-19：最终 API 只读守卫确认三个工作区名称精确为 `旧厂房更新 · 历史研究`、`旧工业建筑 · 结构与功能植入`、`旧厂房社区文化中心`，历史 Run 总数仍为 60、active 0；M65 Deep 保持 completed/attempt 0。最后从该记录状态执行 Git whitespace/staged/status 守卫，不再改文件。
- 2026-07-19：M68 恢复按 HANDOFF 顺序完成：M62 仍为 implementation_complete，M53/M65/M66/M67 均 complete，封存 Deep `0715ab7e-...` 保持 partial 且禁止 retry，M67 最终 active Run 0；分支为 `codex/archresearch-v2-1`，全部既有 modified/untracked 文件保留。planning catch-up 首次因 GBK 无法输出 `▾` 中止，改用显式 UTF-8 后成功恢复 21 条未同步上下文，并完成 `git diff --stat`；没有服务、Run 或数据写入。
- 2026-07-19：用户将 M68 信息架构收敛为父子层级：工作区选择/新建不进入顶栏，也不作为与最近研究相似的独立模块；它只在首页嵌入“最近研究”标题行，控制下方任务列表，结果页隐藏。下一步读取现有首页 JSX/CSS 和测试，在改生产代码前添加行为红测。
- 2026-07-19：M68 新增两条 Board 行为测试后取得预期红线：旧实现找不到“研究项目”，因为唯一 combobox 仍名为“工作区”且位于 `.app-header`；首页归属与结果页隐藏两项均失败在同一旧 DOM 边界，其余 80 项通过。下一步只移动现有 workspace DOM 到 `.recent-panel` header、统一项目文案并做最小响应式样式，不改状态/API。
- 2026-07-19：M68 最小 DOM/CSS 已将同一选择/创建逻辑移入最近研究 header，并从结果页卸载；首条结构测试转绿。第二条测试首次 green run 仅因同步读取尚未完成的 recent-run hydration 而失败，改为等待既有“查看上次结果”动作，生产逻辑不因 fixture 时序调整。
- 2026-07-19：等待后“查看上次结果”仍未出现，进一步读取 fixture 确认默认 `createLiveFetch()` 明确返回空历史列表；不是产品时序。结果页隐藏测试改用既有 `existingRunStatus: completed` 形状，停止重复等待空 fixture。
- 2026-07-19：修正 fixture 后 M68 两条目标行为测试 2/2 green。旧对外工作区文案扫描为 0；内部 Workspace schema/storage/API 未改。下一步跑包含创建、记忆和跨工作区竞态的完整 App/Board 回归，再做 lint/typecheck/build。
- 2026-07-19：完整 App 65 项首次回归为 62 pass / 3 fail；三项都依赖 active result 中的旧全局 selector，与 M68 结果页无项目管理控件的验收直接冲突。保留仍可达的首页请求/失败/terminal hydration 切项目竞态；下一步把一项改为 active-result 隐藏断言，移除两个不可达旧交互及其单用 fixture，再从最终测试状态重跑。
- 2026-07-19：M68 测试边界收敛后完整 App 63/63、Board 80/80 通过；首页请求成功/失败、terminal hydration、selection persistence 与 active poll/cancel 保护仍覆盖。Board ESLint、TypeScript typecheck 和 Vite production build 全部 exit 0。下一步只做无截图真实页面桌面/390px DOM QA，再运行完整仓库门禁与最终守卫。
- 2026-07-19：现有 localhost tab 已重载 M68；无截图 DOM 证明确认顶栏无项目管理、最近研究内有项目父级控件、3 条历史任务正常加载、目标项目仍 selected，1265px 无页面级溢出。下一步从新 DOM snapshot 进入一条现有结果验证控件隐藏，再返回首页做 390px viewport QA。
- 2026-07-19：通过唯一“查看上次结果”打开现有完成 Deep，结果页 DOM 仅保留“发起新研究”当前任务动作，项目 select/新建入口为 0；研究结果与图纸类型筛选完整。下一步用该明确动作返回首页，读取 viewport capability 后做 390×844 DOM QA；全程无截图、POST 或 Run 变化。
- 2026-07-19：结果页唯一“发起新研究”已返回首页，第三个项目仍 selected、3 条最近任务仍在，创建态没有意外恢复；浏览器确认支持临时 viewport override。下一步读取该 capability 的完整合同，设置 390×844 后只做 DOM 几何读取并立即 reset。
- 2026-07-19：390×844 临时 viewport 的无截图 DOM QA 通过：页面无横向溢出，最近研究内项目 select/新建项目同排且均 44px 高，第三个项目保持 selected，顶栏仍无管理控件。下一步只打开/关闭新建项目表单核对浮层边界，不提交；随后 reset viewport 并结束浏览器验证。
- 2026-07-19：390px 项目创建浮层无截图 QA 通过：浮层/input/submit 均未横向越界，输入与提交目标高 44px，submit 在空名称时禁用；没有填入或提交。下一步点击唯一“取消新建”，reset viewport，并将当前首页作为 deliverable 保留。
- 2026-07-19：已取消创建、reset 临时 viewport；默认首页保持 `旧厂房社区文化中心` 与 3 条最近任务，浏览器 error log 0。无截图桌面/结果/390px QA 全部完成；下一步 finalize 当前首页为 deliverable 后只运行完整仓库门禁与 Git/active Run 守卫。
- 2026-07-19：M68 完整 `scripts/verify.ps1` 一次通过：310 API、80 Board、165 Extension、8 packaged E2E、全部静态/构建与 30/108 evaluation 全绿。最终只读 API 守卫为 3 个项目、60 Runs、active 0；M65 completed/attempt 0，封存 M61 `0715ab7e-...` partial/attempt 0。M68 标记 complete，下一步仅同步 HANDOFF 并运行 Git whitespace/staged/status 守卫。
- 2026-07-19：HANDOFF 已同步 M68 新基线与“无获批后续动作”；首轮最终 `git diff --check` exit 0（仅既有 LF→CRLF 提示），staged 0，分支/全部既有 modified、deleted、untracked 路径保持。追加本条后只重跑同一组只读 Git 守卫并交付。
- 2026-07-19：用户启动 M69 dossier density and question synthesis。planning catch-up 以 UTF-8 成功恢复 7 条未同步上下文，`git diff --stat` 与三份规划文件末尾已复核，M68 仍 complete。下一步先按 Impeccable product/layout 规范无截图读取 CollectUI 的可借鉴模式，再审计现有 dossier JSX/CSS 与数据契约；生产修改前先补行为红测。
- 2026-07-19：CollectUI 首次只读浏览把新建 tab、直达与完整 DOM 合在一次 15 秒调用中并超时，控制内核自动重置；没有截图、下载、交互或项目写入。下一步先读取 browser-troubleshooting，再重新初始化并拆分导航/读取，不原样重试。
- 2026-07-19：browser-troubleshooting 已读取；随后对用户给定根 URL 的聚焦 HEAD 诊断返回 HTTP 500，确认站点当前不可用。停止重复根 URL，下一步查询可用搜索工具做一次 CollectUI 索引回退，再进入本地 dossier 审计。
- 2026-07-19：环境无独立 web-search connector；通过 in-app Browser 执行一次聚焦 Google 索引查询，普通结果为 0。下一步仅打开该结果页已明确提供的“图片”索引读取缩略图标题/来源，不截图；随后结束外部研究并记录可验证结论。
- 2026-07-19：M69 continuation 已重新载入 planning-with-files、Impeccable product/layout 与 Browser 约束，并确认现有 PRODUCT/DESIGN 明确禁止项目档案嵌套卡片和同尺寸分析格。planning catch-up 辅助脚本因当前 shell 无可用 `python` 命令而在任何项目读写前退出；该命令封存。首次规划追加又因跨文件锚点不一致被 `apply_patch` 整体拒绝，现已按各文件精确末行完成记录。下一步完成 CollectUI 图片索引的唯一回退读取后结束外部研究，再进入 JSX/CSS/测试审计。
- 2026-07-19：CollectUI 最后索引回退也在图片入口导航时超时并重置浏览连接，未产生可验证参考内容；外部研究已按失败规则终止，不再调用该站、搜索或浏览器。下一步审计 Board dossier JSX/CSS/测试与 `WorkResult` 数据形状，先写问题小结和自然高度布局的行为红测。
- 2026-07-19：Board dossier 审计完成：现有四项分析由 4-column/2×2/1-column CSS grid 控制，项目预览和来源 footer 在其后；`caseGroups` 已聚合每题 dossier 并提供题目 scoped analysis，足够前端确定性生成问题小结，无需 API/schema 或 Provider 修改。下一步读取现有完成 Run fixture 的精确文本，添加结构与小结红测。
- 2026-07-19：新增 M69 两条 focused Board 行为测试并取得预期 2/2 red：旧四格 DOM 仍存在，且双项目章节没有问题小结；现有无正文案例测试也加入“不生成小结”守卫。下一步最小修改 `App.tsx` 与 dossier/question-summary CSS，然后重跑同一 focused tests。
- 2026-07-19：`App.tsx` 已改为两条自然高度分析列，并从每题 dossier 的机制/转译/边界确定性生成一个小结；CSS 使用平面 ledger 而非嵌套卡片。focused 3/3 green。完整 Board 82 项首轮仅 1 项因新增 summary 造成旧 chapter-wide 文本查询歧义；下一步收窄该测试到 dossier 后重跑。
- 2026-07-19：M69 Board 局部门禁完成：完整 82/82 tests、lint、typecheck、production build 全绿。下一步重新连接因外部索引超时而自动重置的 in-app Browser，只访问 localhost 做无截图桌面与 390px DOM 几何/内容顺序 QA；不创建 Run、不提交数据。
- 2026-07-19：已认领最近使用的 localhost tab 并显式 reload M69；首页 DOM、项目选择和研究入口正常，未触发任何提交。下一步等待已有最近任务加载，只打开已完成 M65 Deep，读取新 dossier/问题小结的桌面几何与内容来源；全程无截图。
- 2026-07-19：首页 hydration 后通过唯一“查看上次结果”打开现有 M65 Deep；真实结果 DOM 已显示 M69 两列 dossier 和题目小结，Run/项目/子问题基线未变。下一步对第一份 dossier、全部 summary 和页面宽度做一次聚合 desktop geometry 读取，再进入 390px viewport；不再采集整页 DOM。
- 2026-07-19：desktop 无截图 DOM QA 通过：1265px 无页面溢出，首 dossier 独立列高度不同，6/6 案例章节都有且只有一个紧随 dossier list 的问题小结。已读取 viewport capability 合同；下一步临时设置 390×844，聚合读取列折叠、summary 顺序和页面宽度后立即 reset。
- 2026-07-19：390px 首轮 QA 通过列折叠和无溢出，但真实长文暴露 summary 仍复制同一案例四个步骤、首题高约 907px。下一步 reset viewport，先在双项目 summary 测试加入“每个项目只取首个转译动作”的红线，再改聚合规则并重跑 Board/几何。
- 2026-07-19：summary 简洁性 focused test 已取得预期 red，准确命中同一 dossier 的第二个 transfer step；viewport override 已 reset。下一步仅收紧 questionSummary transfer projection，再重跑 focused/full Board 和本地 390px 高度。
- 2026-07-19：每项目一个首要 transfer 的最小修复完成；focused green、Board 82/82、lint/typecheck/build 全绿。真实 390px summary 高度由约 907px 降至约 530px，第二案例步骤未重复、6 个小结/列折叠/无溢出均保持。下一步 reset viewport，运行 Impeccable 本地 detector 与最终全仓门禁、active/staged/Git 守卫。
- 2026-07-19：临时 viewport 已 reset，Impeccable detector 0 findings；本轮源码 diff 边界复核完成。下一步把“每题案例后收束正文机制/首要转译/边界”的新规则同步进 PRODUCT/DESIGN，再运行最终完整仓库门禁。
- 2026-07-19：PRODUCT/DESIGN 已同步 M69 小结规则。首次完整门禁的监控会话只回传前两项通过后即丢失最终块，无法判定整体结果且进程已结束；已封存该监控方式。下一步使用更长 outer yield 和 bounded poll 重新运行一次完整门禁并捕获 exit code。
- 2026-07-19：M69 完整仓库门禁已明确 exit 0：310 API / 82 Board / 165 Extension / 8 packaged E2E，全部静态、构建、安全契约和 30/108 evaluation 通过。下一步更新 HANDOFF/M69 状态，再执行 active 0、60 Runs、封存/完成 Deep、Git whitespace/staged/status 最终只读守卫。
- 2026-07-19：首轮最终守卫已确认 Git whitespace exit 0、staged 0、dirty tree 保持；API runs 枚举返回 404，active 结论作废且该命令封存。下一步检查 workspace 原始响应后用 Node JSON 完成 active/60 Runs/M65/封存 Deep 守卫。
- 2026-07-19：Node 标准 JSON 最终守卫确认 3 项目 / 60 Runs / active 0，M65 completed/attempt 0，封存 `0715ab7e-...` partial/attempt 0；Git whitespace exit 0、staged 0、全部既有 dirty/untracked 路径保持。M69 已标记 complete，PRODUCT/DESIGN/HANDOFF 同步；当前无获批后续产品动作。
- 2026-07-19：最终 in-app Browser tab finalize 因控制绑定在交付前消失而两次未执行；停止为清理标签页而重连。现有 localhost 结果页仍由用户浏览器保留，产品/Run/文件状态未受影响。最后仅重跑 Git whitespace/staged/branch 守卫后交付。
- 2026-07-19：用户启动 M70 dossier full-width density refinement。目标收敛为全宽四行分析台账，以及单项预览的横向证据布局；保留 M69 小结与全部来源/权利/图片语义。下一步读取用户截图原图并审计 evidence-sheet CSS，生产修改前先补结构红测。
- 2026-07-19：用户截图与 App/CSS 审计完成，空面精确对应两列 analysis shell 和三列单项 gallery。下一步修改现有 M69 结构测试为“4 个全宽直接行”，并新增单项 gallery layout 红测；红线成立后才改 JSX/CSS。
- 2026-07-19：M70 focused behavior tests 取得预期 3/3 red，分别锁定 analysis flatten、single preview 和 multi-asset grid。下一步最小修改 App JSX 与对应 responsive CSS，再重跑同一 focused set。
- 2026-07-19：M70 JSX/CSS 最小实现完成，focused tests 3/3 green。下一步跑完整 Board 83 项、lint/typecheck/build 与 Impeccable detector，再重载真实 M65 页面做桌面/390px DOM 几何。
- 2026-07-19：首版 full-width ledger 的完整 Board 83/83 通过，但用户真实页复核发现中文正文仍被 72ch 锁在左半幅。下一步给 transfer 行补稳定 class 的红测，再取消窄栏上限、宽屏两栏流式排布，之后重新跑 Board 与真实几何。
- 2026-07-19：transfer wide-flow focused test 已取得预期 red。下一步添加单一 class 与 CSS column-count 2/1 响应规则，重跑 focused/full Board。
- 2026-07-19：M70 Board 局部门禁完成：83/83 tests、lint、typecheck、build 和 detector 0 findings。下一步重新连接用户当前唯一 localhost tab，读取最终 HMR 后的 desktop 几何：analysis 内容宽度、transfer 两栏实际落位、single preview 横向占宽；随后做 390px 一栏/无溢出检查。
- 2026-07-19：已重新连接 in-app Browser，当前只有一个 `http://127.0.0.1:5173/` ArchResearch tab，无需选择或清理重复页。下一步认领该 tab，直接读取 HMR 后的结果页聚合几何；不截图、不点击。
- 2026-07-19：desktop 无截图 DOM QA 通过：transfer 右栏实际落位，analysis 仅保留 24px 右 padding；单项预览 full-width 左图右文。下一步读取 viewport capability，设置 390×844 后验证 transfer column-count=1、single preview 图上文下、document 无溢出，然后 reset。
- 2026-07-19：390px DOM QA 通过：无页面溢出，transfer 单栏，single preview 图上文下且铺满 gallery，6 个小结保持。下一步 reset 临时 viewport、读取 error log，随后更新 PRODUCT/DESIGN 并运行完整仓库门禁与最终 Run/Git 守卫。
- 2026-07-19：临时 viewport 已先 reset；随后 error-log 读取因 claimed tab 被浏览器释放而未执行，停止为日志重连。桌面/390px 几何验收有效。下一步同步 PRODUCT/DESIGN 的 full-width ledger/single preview 规则，再跑完整仓库门禁。
- 2026-07-19：PRODUCT/DESIGN 已同步 M70 密度规则，完整仓库门禁 exit 0（310 API / 83 Board / 165 Extension / 8 packaged E2E）。下一步只读核验 60 Runs/active 0、M65/封存 Deep、Git whitespace/staged/status，再标记 M70 complete 并同步 HANDOFF。
- 2026-07-19：最终 Node/Git 守卫通过：60 Runs、active 0、M65 completed/attempt 0、封存 Deep partial/attempt 0、whitespace exit 0、staged 0。M70 标记 complete，HANDOFF 已同步；最后从最终文档状态重跑 Git whitespace/staged/branch 后交付。
- 2026-07-19：M71 全界面认知负担审计开始；已恢复 HANDOFF/M70/规划末尾与 dirty Git 状态，并登记只读范围和禁截图、禁 Run、禁产品修改边界。下一步解析 critique slug/ignore，随后并行启动隔离 Assessment A（用户视角设计评审）与 Assessment B（detector + 独立浏览器 DOM 证据）。
- 2026-07-19：Assessment A 先进入父上下文，随后才读取 Assessment B，符合 critique 防锚定顺序。A 基于源码、CSS 和只读真实 Deep 数据完成用户视角评审；B 的 App/source detector 0 findings。Browser unavailable，故没有实时 DOM/overlay 声称。综合报告确定 Nielsen 23/40、4 个 P1、1 个 P2。
- 2026-07-19：完整 M71 critique 已写入 `.impeccable/critique/2026-07-19T13-47-08Z__apps-board-src-app-tsx.md`；slug 首次记录，trend 仅 `23`。临时 body 已删除。M71 标记 complete；审计没有修改产品代码，下一步等待用户选择先处理“结果概览与层级”或“术语与深度说明”等范围。
- 2026-07-19：M72 human-readable result overview 启动。planning catch-up 通过显式 API venv Python 恢复 6 条未同步消息并复核完整 dirty tree；Impeccable clarify/product 规则已读取。下一步审计 synthesis contract 和现有 fixture，先新增一个真实 fallback 形状的行为红测，锁定“机器串不能做标题、最多三条立即动作、完整审计信息仍在”。
- 2026-07-19：M72 contract/source 审计完成：fallback generation mode 已持久化，Board 只缺可读投影。已决定不触碰 API/Run：正常短 answer 原样保留，machine-shaped answer 从既有 causal mechanism 生成标题，recommendations 去壳/去重/截为三项，完整原文进入现有 audit details。下一步先把该真实 fallback 形状写成 Board 行为红测。
- 2026-07-19：M72 red-green 第一轮完成。新 fallback 概览测试旧版准确失败；`client.ts` 仅声明已有 `generation_mode`，`App.tsx` 加入可读投影，`styles.css` 只让完整原文跨 audit 全宽。精确 focused test 1/1 通过。下一步同时复核正常简短 synthesis 与 fallback，再跑完整 Board 84 项和静态/构建门禁。
- 2026-07-19：M72 Board 行为门禁完成：正常 synthesis focused 1/1、fallback focused 1/1、完整 Board 84/84 通过，targeted `git diff --check` 无错误。下一步运行 Board lint/typecheck/build 与 Impeccable detector，再尝试只读无截图 localhost DOM QA；Browser 若仍不可用则明确以测试/源码收口，不声称实测。
- 2026-07-19：M72 静态/构建与真实页 QA 完成：lint/typecheck/build、detector 0；真实 M65 标题 54 字、2 条清理后的动作、完整原文折叠保留，desktop 与 390px 均无 overflow，viewport 已 reset。下一步读取 browser error log 后将当前真实结果页作为 deliverable 保留，再同步 PRODUCT/DESIGN、运行最终 Git/active Run 守卫并标记 M72 complete。
- 2026-07-19：Browser error log 0，当前真实 M65 结果页已作为 deliverable 保留。PRODUCT/DESIGN 已同步“短 answer 不动、machine-shaped answer 只做证据绑定展示投影、最多三条动作、完整原文可审计”规则。完整仓库门禁 310/84/165/8 exit 0，Run/Git 守卫通过，M72 标记 complete。按用户“一项一项来”，下一项是 M71 P1 #2：把完整证据库存改为渐进披露，不与本项混做。
- 2026-07-19：M73 progressive case disclosure 启动。planning catch-up 用显式 API venv Python 恢复 6 条未同步消息，dirty tree 保持；Impeccable distill/product 规则已读取。下一步审计 `caseGroups`/dossier 排序、现有 question-summary JSX/CSS 与相关测试，先锁定默认摘要、closed inventory、展开完整性的行为红测。
- 2026-07-19：M73 source/test/CSS 审计完成。代表案例可直接取既有 dossier 前两项；问题小结默认只保留首条 evidence-bound mechanism；完整 dossier JSX 原样置入 native details。下一步新增三项目行为红测，验证 summary 在前、两条 flat representative ledger、inventory 默认关闭及展开后 3/3 完整顺序和来源都在。
- 2026-07-19：M73 三项目行为测试与最小 JSX/CSS 已在续接前完成：默认小结在前、首条机制、两个代表案例和 closed inventory 的结构断言均通过；当前唯一失败是用 `queryByRole(...).not.toBeInTheDocument()` 错判 closed details 内容。续接时默认 `python` 命中 Windows 商店占位符而退出，未改项目；随后改用 `apps/api/.venv/Scripts/python.exe` 成功 catch-up，并复核 HANDOFF、M73 活跃阶段、规划末尾、完整 dirty tree 与 `git diff --stat`。下一步把该断言改为“默认不可见、展开后可见”，重跑 focused test。
- 2026-07-19：M73 focused test 现已 1/1 green：完整 Warehouse dossier 在 closed native details 中保持 DOM 与审计数据，但默认 `not.toBeVisible()`；点击“查看完整案例与证据”后同一节点可见，3/3 项目顺序和 3 个原始来源保持。下一步一次运行完整 Board 回归，合并识别所有依赖 full dossier 默认展开的旧断言后统一修正。
- 2026-07-19：完整 Board 首轮为 63 pass / 22 fail；失败已合并诊断为同名文本歧义、未展开即操作 full dossier、以及 M69 summary 旧合同三组，没有 API/data 丢失。下一步新增一个仅供测试的 inventory 展开 helper；逐项按真实用户意图改为等待可见代表标题或先展开，再更新原 M69 小结断言为 heading 后首条机制，最后一次重跑完整 Board。
- 2026-07-19：测试迁移第一批完成：新增真实点击 native summary 的 `openCaseInventories` helper；22 个失败涉及的同名等待已改为可见代表案例，详细档案/预览/来源/收藏/对照交互先显式展开；原 M69 测试改为小结紧跟章节标题且只含首条机制，其余机制、首要动作和边界在代表案例验证。源码搜索已无 `findByText('Live Mill Conversion')` 歧义；剩余需补一处既有 demo 对照测试的显式展开，然后跑 focused/full 回归。
- 2026-07-19：补齐 demo 对照测试的 inventory 展开后，完整 `App.test.tsx` 68/68 green。旧测试现在分别验证默认代表层或通过真实 summary 点击进入完整证据层，没有再以 hidden dossier 冒充用户可见内容。下一步运行全部 Board 测试，再执行 lint/typecheck/build 与 Impeccable detector。
- 2026-07-19：M73 Board 局部门禁完成：85/85 tests、ESLint、TypeScript typecheck、Vite production build 全部 exit 0；Impeccable detector 对 `App.tsx`/`styles.css` 返回 0 findings。下一步使用既有 localhost 结果页做无截图 desktop/390px DOM QA：6/6 小结在前、每题代表案例 ≤2、6 个 inventory 默认关闭、展开后项目/来源保持且无横向溢出，最后 reset viewport 并读取 error log。
- 2026-07-19：in-app Browser 既有绑定可用，当前用户只保留一个 `http://127.0.0.1:5173/` 的 ArchResearch Board tab；尚未认领、点击、截图或改 viewport。下一步认领该唯一 tab并显式 reload M73，再读取当前首页/结果状态；只进入既有 M65 completed 结果，不发起研究。
- 2026-07-19：首次 tab 接管误用了当前 Browser API 不存在的 `browser.tabs.claim`，立即返回 `not a function`，没有页面操作或状态变化；该方法封存不重试。下一步读取 bootstrap troubleshooting/current binding docs 确认正确接管接口，继续复用 open tab id 2，不另开页面。
- 2026-07-19：Browser 完整文档及被截断的 fallback 小节已补读，正确接口确认是把 `openTabs()` 返回的完整对象传给 `browser.user.claimTab(...)`。下一步复用已保存的唯一 open-tab 对象接管页面并 reload；后续所有点击先 snapshot、确认唯一 locator，再操作。
- 2026-07-19：正确 claim 调用前的续接代码误给未定义的 `m73Tab` 赋值，JavaScript 在执行 Browser API 前即中止，页面仍未接管或 reload；该变量名封存。下一步用新的 `var m73ClaimedTab = await browser.user.claimTab(m73OpenTabs[0])` 一次完成接管，再 reload/snapshot。
- 2026-07-19：唯一 localhost tab 已成功接管并显式 reload M73；fresh DOM 为首页，第三个项目 `旧厂房社区文化中心` 仍 selected，最近任务正在异步读取，没有提交动作。尝试 `waitForLoadState(networkidle)` 时当前后端明确不支持该状态，未改页面；该等待方式封存。下一步读取 browser-troubleshooting，并按 snapshot 中唯一“正在读取最近任务…”状态做 hidden/detached 定向等待后再 snapshot。
- 2026-07-19：按 loading paragraph 定向等待后首页 hydration 完成；唯一“查看上次结果”打开既有 M65 51-reference completed Run。真实默认层已聚合验证 6/6 小结在前、每题代表案例不超过 2、6/6 inventory closed、完整 13 个 chapter-dossier 与 13 个来源 link 均仍在 DOM，自然 796px 无 overflow。下一步展开一个 3-project inventory 验证可见完整顺序/来源，再读取 viewport capability 做 1440 与 390px QA。
- 2026-07-19：第三题唯一 inventory 已按 snapshot-grounded locator 展开，3 个 dossier 顺序与 3 条原站来源均完整；未触发来源跳转或任何持久写入。首次读取误用不存在的 `.dossier-footer` 导致来源计数 0，随后在同一 bounded inventory 内按 `a[href]` 校正为 3，旧 selector 封存。下一步先收起该 inventory，读取 viewport capability 文档并做 1440/390 几何与默认关闭复核。
- 2026-07-19：第三题 inventory 已收回 `open=false`，真实结果恢复默认折叠；viewport capability 合同已读取，允许临时 `set({width,height})` 且结束前必须 `reset()`。下一步先设 1440×1000 聚合读取 desktop 的 summary/representative/inventory/overflow 几何，再设 390×844 验证单列、summary 顺序与无溢出，最后 reset。
- 2026-07-19：1440×1000 与 390×844 无截图 DOM QA 均通过：两档都无页面横向溢出、6/6 小结在前、每题代表≤2、6/6 inventory closed；宽屏代表条为身份+三列方法，390px 全部单列且内容宽 351.2px。下一步立即 reset 临时 viewport，读取 error-level browser log；随后把 M73 渐进披露规则同步 PRODUCT/DESIGN 并运行最终完整仓库门禁。
- 2026-07-19：临时 viewport override 已 reset，Browser error log 为 0；当前仍是既有 M65 completed 结果页，第三题 inventory 已收起，没有新建/retry Run、来源跳转、截图或持久交互。下一步把该唯一 localhost tab 作为 deliverable 释放；此后不再调用 Browser，进入文档同步与最终门禁。
- 2026-07-19：唯一 localhost M65 结果页已作为 deliverable 释放，此后不再调用 Browser。PRODUCT/DESIGN 已同步 M73 成品合同：题目判断在前、最多两个代表案例、完整 dossier/预览/逐字证据/来源/操作统一在默认关闭的原生 details 中保留。下一步运行 targeted whitespace 检查，再执行完整 `scripts/verify.ps1`。
- 2026-07-19：M73 完整 `scripts/verify.ps1` 明确 exit 0：310 API、85 Board、165 Extension、8 packaged E2E，Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、dev-common/Provider security/process lifecycle 与 30/108 evaluation 全绿。targeted `git diff --check` 也为 exit 0。下一步只读核验 3 projects / 60 Runs / active 0、M65 completed attempt 0、封存 Deep partial attempt 0，以及全树 whitespace/staged/status；通过后标记 M73 complete 并同步 HANDOFF。
- 2026-07-19：首轮最终守卫中，全树 `git diff --check` exit 0、staged 0 和完整 dirty/untracked status 均有效；Node Run 枚举脚本因 PowerShell 把 JSON-quoted多行参数的 `\n` 原样传给 `node -e`，在任何 HTTP 请求前 `SyntaxError` 退出，Run 结论无效。该多行传参方式封存；下一步把同一只读脚本压成无换行单行再执行。
- 2026-07-19：单行 Node JSON 守卫 exit 0，确认 3 projects / 60 Runs / active 0；M65 `76f52c79-...` 仍 completed/attempt 0，封存 Deep `0715ab7e-...` 仍 partial/attempt 0。结合完整门禁、真实页 QA、全树 whitespace exit 0 和 staged 0，M73 标记 complete。下一步同步 HANDOFF 为新基线，并把 M71 P1 #3“后处理工具位于答案之前”登记为等待用户继续后才启动的下一项。
- 2026-07-19：HANDOFF 已同步 M73 完成基线与“当前无获批产品修改”；首次 post-sync 守卫确认 task plan/HANDOFF 目标行准确、`git diff --check` exit 0、staged 0、分支和全部既有 dirty/untracked 路径保持。追加本条后只重跑最终 Git whitespace/staged/status 守卫并交付，不再改文件。
- 2026-07-19：用户批准继续，M74 answer-first result tools placement 标记 in_progress。恢复已完整读取 HANDOFF、规划末尾、findings/progress 末尾和 dirty Git 状态；planning catch-up 只报告 5 条已同步上下文。Impeccable layout/product 规则已读取。下一步审计 critique P1 #3 与 `result-workbench` JSX/CSS/测试，生产修改前先添加“研究内容 DOM 顺序先于五项工具且工具仍完整”的行为红测。
- 2026-07-19：P1 #3 与现有 workbench 审计确认：五项工具当前作为 `resultViewOpen && results.length > 0` 的独立 section，直接出现在研究正文之前；移动整段 JSX 即可保留所有状态、handlers、dialogs 与禁用逻辑。M74 不采纳本轮范围外的“选择后才出现新上下文条”重构。下一步读取 result content 的完整 wrapper/结束边界与关联测试，确定最小插入点后先写 DOM-order 红测。
- 2026-07-19：`result-workbench` 与 `results-section` 的 sibling 边界已确认，生产变更可收敛为同条件 JSX 块搬移加单一 margin 方向调整。下一步定位 results-section 的结束行和现有工具测试，在该测试先新增 DOM-order/完整五工具断言并取得旧结构 red，再移动生产块。
- 2026-07-19：最终插入点确认在 `.empty-filter` 之后、`.comparison-dock` 之前；现有工具测试已覆盖五项文案、disabled prerequisites 和 Trace dialog，可在同一测试补 `研究结果` 先于 `结果工作台` 的 DOM-order 断言及恰好五个按钮。下一步只改测试并跑 focused red。
- 2026-07-19：M74 focused 行为测试取得预期 1/1 red：`研究结果.compareDocumentPosition(结果工作台)` 的 FOLLOWING bit 为 0，准确证明旧 DOM 是工具在答案前；失败发生在顺序断言，生产逻辑未改。下一步原样搬移 workbench JSX 到 empty-filter 后，并把外层 margin 从 bottom separation 改为 top separation，再跑同一 focused test。
- 2026-07-19：M74 最小实现完成：同一 workbench JSX 原样移到 empty-filter 后、comparison dock 前；base/mobile margin 分别改为上方 `space-7/space-6`，没有更改任何按钮、handler、disabled 条件或 overlay。focused 顺序+五工具+Trace 行为测试 1/1 green。下一步跑完整 App/Board 回归，重点确认选择、对照、导出、表达规范和 active/provisional 结果仍通过。
- 2026-07-19：M74 完整 App 68/68、Board 85/85 回归全绿；选图、收藏/拒绝、对照、个人/分享导出、表达规范、Trace、completed/partial/active/provisional 状态均保持。下一步并行运行 Board lint/typecheck/build、Impeccable detector 和 targeted whitespace，再做真实 M65 desktop/390px 无截图顺序与几何 QA。
- 2026-07-19：M74 Board lint、TypeScript typecheck、Vite production build、targeted whitespace 全部 exit 0，Impeccable detector 0 findings。下一步复用现有 in-app Browser 绑定，只认领唯一 localhost tab、reload M74，并打开既有 M65；桌面/390px 验证研究结果在工具前、五个工具完整、无横向溢出和 error log 0，不截图、不创建 Run。
- 2026-07-19：Browser 复用确认只有一个 localhost tab（id 3）。首次 claim 代码引用了上一格 `else` 块未持久化的 `m74OpenTabs`，在 Browser API 调用前即 `not defined`，页面未接管/reload；该变量封存。下一步在同一格重新 `openTabs()`、校验唯一 URL 后直接 claim/reload/snapshot，不另开 tab。
- 2026-07-19：同格 openTabs/claim/reload 成功；第三个项目仍 selected，最近任务 hydration 后唯一“查看上次结果”可用。点击既有 M65 后的即时 snapshot 处于结果 fetch 的短暂 `尚无研究结果` 占位，但 Run 状态已显示 completed/51 references；没有提交或新 Run。下一步按该 snapshot 的唯一占位 region 做 detached 定向等待，再读取完成 DOM 顺序。
- 2026-07-19：占位定向等待后，真实 M65 snapshot 明确按“研究结果完整内容 → 结果工作台”输出，五个工具及三项初始 disabled 条件完整。1440 聚合读取中 Browser 注入的 element wrapper 不支持 `compareDocumentPosition`，在几何输出前 TypeError；viewport set 已生效、页面未改。该 API 在浏览器 QA 中封存，下一步用同一 selector 的文档顺序数组和 absolute top 坐标验证 desktop，再做 390px。
- 2026-07-19：真实 M65 desktop/390px 无截图 DOM QA 通过：两档文档顺序均为研究结果后工具，五项工具、三项 disabled prerequisites 完整；desktop 两列、390px 单列且无页面横向溢出。下一步 reset viewport、读取 error log 并将唯一 localhost 结果页作为 deliverable；随后同步 PRODUCT/DESIGN、运行完整仓库门禁与最终 Run/Git 守卫。
- 2026-07-19：临时 viewport override 已 reset，Browser error log 0；当前保留既有 M65 completed 结果页，未展开来源、未选择图纸、未触发工具写操作、未创建/retry Run。下一步把该 tab 作为 deliverable 释放，此后不再调用 Browser。
- 2026-07-19：唯一 localhost M65 结果页已作为 deliverable 释放，此后不再调用 Browser。PRODUCT/DESIGN 已同步“任务 → 结论 → 问题拆解 → 案例 → 继续使用工具”的结果顺序及禁用说明保留规则。下一步运行完整 `scripts/verify.ps1` 并捕获明确 exit code，随后只读核验 Run/Git 基线。
- 2026-07-19：M74 完整 `scripts/verify.ps1` 明确 exit 0：310 API、85 Board、165 Extension、8 packaged E2E，Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、dev-common/Provider security/process lifecycle 与 30/108 evaluation 全绿。下一步只读核验 3 projects / 60 Runs / active 0、M65 completed attempt 0、封存 Deep partial attempt 0，以及全树 whitespace/staged/status；通过后标记 M74 complete 并同步 HANDOFF。
- 2026-07-19：M74 最终只读守卫通过：3 projects / 60 Runs / active 0，M65 `76f52c79-...` completed/attempt 0，封存 `0715ab7e-...` partial/attempt 0；`git diff --check` exit 0、staged 0。M74 标记 complete。下一步同步 HANDOFF，并把 M71 P1 #4 深度/证据术语改写登记为等待用户继续后才启动的独立项。
- 2026-07-19：HANDOFF 已同步 M74 完成基线与“当前无获批产品修改”；post-sync 守卫确认 task plan/HANDOFF 目标行准确、`git diff --check` exit 0、staged 0、分支和全部既有 dirty/untracked 路径保持。追加本条后只重跑最终 Git whitespace/staged/status 守卫并交付，不再改文件。
- 2026-07-19：用户批准继续 M71 P1 #4，M75 plain-language depth and evidence terminology 标记 `in_progress`。新会话已完整读取 HANDOFF、task plan 活跃状态、findings/progress 末尾与完整 dirty Git 状态；planning catch-up 的 17 条未同步消息只覆盖恢复与本次授权，`git diff --stat` 未出现新的不明产品增量。M62 仍为 implementation_complete，M53 已随 M65 验收转为 complete；新范围只改对外深度产出说明与证据术语首次解释，内部枚举、Run/证据数据和来源检视器 P2 不变。下一步审计 `App.tsx`/测试及所有对外出现点，然后先写行为红测。
- 2026-07-19：M75 源码/测试/CSS 审计完成。所有待改文案均集中在 Board 展示层：`researchDepthOptions`、`tierLabels`、relevance 页脚、案例章节/计数和 browser-readiness 文案；`api/client.ts` 仍直接把选中的 `quick/balanced/deep` 写入 `budget_mode`。下一步先新增 Board 行为测试，同时锁定普通话文案、三档 POST 值、evidence tier 映射和 relevance 数值语义；旧实现应因旧文案精确失败。
- 2026-07-19：M75 行为红测精确为 4/4 red：三档都缺“以多个案例为依据”的成品差异，结果页仍以“正文案例”为标题。最小展示实现后 focused 4/4 green，其中三条测试实际解析 POST body 并确认 `budget_mode` 仍分别为 `quick/balanced/deep`；partial 实例仍保留 `data-tier=partial`，relevance 3 只映射成“与本题高度相关”。完整 App 首轮为 68 pass / 4 fail，已合并确认均是旧文案断言：3 处仍期待“已完成正文分析”，1 处仍期待“已核验正文”；没有数据、分组或 fallback 逻辑回归。下一步只迁移这四条断言后重跑 App。
- 2026-07-19：旧断言迁移后完整 App 72/72、Board 89/89、ESLint、TypeScript typecheck、Vite production build、targeted `git diff --check` 全绿；Impeccable detector 对 `App.tsx/styles.css` 为 0 findings。真实页无截图 desktop/390px QA 通过三档完整说明、案例证据标题、普通话 tier/relevance 与无溢出；Browser error log 0。浏览器恢复中先后误用不存在的 `tab.playwright.reload` 和 `tab.capabilities.viewport`，均在调用边界立即失败且未改页面；随后按当前文档改用 `tab.reload()` 合同和 `browser.capabilities.get("viewport")`。用户续接导致 Node 会话变量失效后，新绑定中旧 tab 列表为空，因此按 troubleshooting 只新建一个 localhost QA tab，最终作为 deliverable 保留。未截图、未输入问题、未创建/retry Run，下一步同步 PRODUCT/DESIGN 与非历史用户文档，再跑最终完整门禁。
- 2026-07-19：M75 中断续接已按 HANDOFF 完整恢复：M75 仍为 `in_progress`，M62 仍为 `implementation_complete`，M53 已为 `complete`，工作树全部既有 dirty/untracked 资产保留。`planning-with-files` 首次 catch-up 因系统 `python` 命中 Microsoft Store 占位符而未执行，未产生副作用；改用 `apps/api/.venv/Scripts/python.exe` 后成功恢复 17 条未同步上下文。PRODUCT/DESIGN 及三份非历史用户文档修改已在工作树，仍需最终术语残留审计、全量 Board/静态/构建门禁、一次完整 `scripts/verify.ps1` 与只读 Run/Git 守卫后才能收口。
- 2026-07-19：M75 最终 diff/旧术语/文档审计完成：生产 UI 无旧首见术语残留，仅设计规范禁用例和测试负断言保留旧词；三档请求值、证据 tier、relevance 排序/门限均未改。PRODUCT/DESIGN 与三份非历史用户文档已对齐，targeted `git diff --check` exit 0。下一步重跑最终 Board 89 项、lint/typecheck/build 和 detector。
- 2026-07-19：M75 最终 Board 局部门禁通过：89/89 tests、ESLint、TypeScript typecheck、Vite production build 全部 exit 0，Impeccable detector 对 `App.tsx/styles.css` 返回 0 findings，全树 `git diff --check` exit 0（仅既有 LF→CRLF 提示）。本轮未截图、未调用 Provider/Firecrawl、未创建或 retry Run。下一步仅运行一次完整 `scripts/verify.ps1`。
- 2026-07-19：M75 唯一一次最终 `scripts/verify.ps1` 明确 exit 0：310 API、89 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、dev-common/Provider security/process lifecycle 与 30/108 evaluation 全绿。未启动第二次 verify，未调用 Provider/Firecrawl、未创建或 retry Run、未截图。下一步只读核验 3 projects / 60 Runs / active 0、M65 和封存 Deep 状态、staged 0，再标记 M75 complete 并同步 HANDOFF。
- 2026-07-19：M75 最终只读守卫通过：3 projects / 60 Runs / active 0，M65 `76f52c79-...` 保持 completed/attempt 0/completion_satisfied，封存 Deep `0715ab7e-...` 保持 partial/attempt 0/research_synthesis_incomplete；`git diff --check` exit 0、staged 0，全部既有 dirty/untracked 资产保留。M75 已标记 complete 并同步 HANDOFF；来源检视器 P2 仍延后，当前无自动下一项产品修改。
- 2026-07-20：用户启动 M76，要求补齐小红书视觉路径的当前版本验证。已完整恢复 HANDOFF、task plan 活跃状态、findings/progress 末尾和 dirty Git 工作树；M75 complete，M62 仍 implementation_complete，M53 complete，既有修改与未跟踪资产全部保留。下一步只读审计 workflow/OpenCLI/测试与历史 Run，先判定缺少当前 Live 证据还是实现合同缺口；未通过离线预检前不创建 Run。
- 2026-07-20：M76 历史盘点确认 60/60 Run 都是 precedent，visual-reference Run 为 0；precedent + XHS 有旧 Live 记录，包括已完成 Quick `42668844-...`，但 M65 最终 Deep 为 `research_sources=[]`。下一步聚焦审计 `visual_reference_search` 的搜索、下载、四图批分类、coverage/终态与 Board 分区行为测试，仍不创建 Run。
- 2026-07-20：M76 离线审计发现 `visual_reference_search` 的确定性 coverage 缺陷：XHS 视觉资产正确保持 `visual_lead`，但子问题覆盖只计 `verified/partial`，使独立视觉目标无法 completed。下一步先读取完整检视循环和非 precedent 目标值，然后新增一条每题一篇 XHS/最多四图/单批分类/正确 completed 的 workflow 行为红测；生产修复前不创建 Run。
- 2026-07-20：M76 检视循环审计又确认一个双后端边界缺口：OpenCLI 只下载首篇 XHS 笔记，但后续笔记可在扩展连接时落入通用 browser inspection，绕过每题一笔记限制。下一步写一条完整 visual-goal workflow 红测和一条双后端一笔记红测，同时保护 XHS `aggregator/visual_lead`、四图单批和扩展回退。
- 2026-07-20：M76 红测边界已确定。完整 Quick `visual_reference_search` fixture 使用三个默认视觉子问题，每题一篇 XHS 笔记、四图、一次 batch，并要求 3/3 coverage 与 completed；双后端 fixture 返回两篇 XHS 笔记且同时连接扩展，要求只有排序首篇进入下载/裁图。下一步只添加这两条测试并取得旧实现 red。
- 2026-07-20：M76 两条行为红测均精确失败：三题 XHS 图纸灵感 Run 仍为 `partial`，双后端场景中 browser 实际执行 `open_url` 检视第二篇笔记。失败分别命中 coverage 计算和每题一笔记边界，没有 fixture-only 错误。下一步最小修改 `workflow.py`：仅视觉目标从 usable visual assets 建立子问题关联，并按 subquestion 固定首个 XHS source URL。
- 2026-07-20：M76 首次 green 回归中，双后端一笔记测试已通过；完整视觉 Run 也已达到 completed/3-of-3/gaps 0，仅测试误要求 3 个资产而失败。原因是三篇 fixture 都把相同 pattern-0 作为首张分类图，真实感知哈希正确去重为一个资产并关联三题。下一步仅调整 fixture 使三篇笔记首图不同，不改生产去重。
- 2026-07-20：M76 两条目标行为测试最终 2/2 green。完整 Quick visual workflow 为 completed、coverage 3/3、gaps 0，三个子问题各搜索/下载一篇笔记，各以 4 图做一次 batch，全部资产仍为 `aggregator/visual_lead/rights unknown`；双后端测试确认次选笔记不触发 `open_url`。下一步扩大到完整 browser-inspection/workflow/XHS/run-schema 套件。
- 2026-07-20：M76 扩大离线回归全绿：完整 `test_browser_inspection.py`、`test_workflow.py`、`test_xiaohongshu.py`、`test_runs_results.py` 和 `test_schemas.py` 全部通过，仅既有 TestClient/httpx2 warning。precedent 正文证据、OpenCLI→扩展搜索回退、视觉去重和 Run/API 合同未回归。下一步运行 Ruff lint/format、strict Mypy 与 whitespace。
- 2026-07-20：M76 首次静态门禁中 Ruff lint 和 targeted whitespace 通过；format check 仅要求机械重排 `workflow.py`。从仓库根直接运行 `mypy apps/api/src` 以 exit 1 且无诊断结束，不构成有效类型失败，该调用封存不重复。下一步只格式化 `workflow.py`，然后从 `apps/api` 工作目录运行项目 strict Mypy 并重跑目标测试。
- 2026-07-20：机械格式化 `workflow.py` 后，M76 最终目标 2/2、Ruff lint、两文件 format check、`python -m mypy apps/api/src` strict 18-source 和 targeted whitespace 全绿。生产改动仅为视觉目标 coverage relationship assets 与每 subquestion 唯一 XHS selected source，没有修改 schema、Board、Provider、tier、rights 或 precedent 正文门。下一步仅运行一次完整 `scripts/verify.ps1`。
- 2026-07-20：M76 唯一一次完整 `scripts/verify.ps1` 明确 exit 0：312 API、89 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、dev-common/Provider security/process lifecycle 与 30/108 evaluation 全绿。没有调用 Live Provider/XHS、Firecrawl、Run 或截图。下一步只读检查 active Run、API/Board/Provider 与 OpenCLI Browser Bridge；通过后重载一次 M76 服务。
- 2026-07-20：M76 Live 前置守卫中，active 仍 0、历史仍 60，API/Board/Provider 均正常；唯一一次 OpenCLI doctor 显示 daemon 正常但 Browser Bridge extension 未连接。未重载服务、未 POST、未创建 Run。下一步只读检查 Chrome 进程/安装位置、OpenCLI 扩展持久配置和 ArchResearch browser status，再决定最小恢复动作。
- 2026-07-20：只读 Bridge 诊断确认 Chrome 进程为 0，但 Default profile 内 OpenCLI 1.0.22 和当前 ArchResearch 扩展均已安装/启用。未修改 Chrome 偏好、扩展或 daemon。下一步使用已解析的 Chrome executable 隐藏启动 Default profile 空白页，等待扩展连接后重做 doctor；仍不 POST。
- 2026-07-20：隐藏启动 Default Chrome 成功，随后 OpenCLI doctor 确认 daemon/extension/connectivity 全部 OK，连接 profile 为 `2btxrgkn`。未打开研究页或创建 Run。下一步执行唯一 limit=1 XHS 结构化搜索预检；成功后才检查 start/stop 脚本并重载 M76。
- 2026-07-20：唯一 limit=1 XHS 预检已返回一篇真实结构化笔记，登录态/搜索可用。仓库已定位 `scripts/stop.ps1` 与 `scripts/start.ps1`；下一步完整读取两脚本的 PID/工作目录边界，然后只执行一次 stop 和一次 start 加载 M76，仍未 POST。
- 2026-07-20：M76 服务重载已严格执行一次 `stop.ps1` 和一次 `start.ps1`；脚本只停止工作区记录的 8000/5173 listener，新 API/Board 已在原端口 ready。Chrome 与 OpenCLI daemon 未被停止。当前仍未 POST；下一步在单一 Node 脚本中复核 health、精确 workspace、60 Runs 与 active 0，然后只创建一条 Quick `visual_reference_search` attempt 0。
- 2026-07-20：创建前原子守卫确认 health `ok` / `gpt-5.6-sol`、精确唯一 `旧厂房社区文化中心` workspace、60 Runs 与 active 0；随后只 POST 一次创建 Quick `visual_reference_search` `e525ca77-22b6-4751-8062-3730f933ba13`，attempt 0、`research_sources=[xiaohongshu]`。问题覆盖蓝灰配色、线稿轴测、分层剖面和公共/后勤流线分析表达。下一步只读监控该 Run，不重发、不 retry。
- 2026-07-20：M76 中断续接已按 HANDOFF 完整恢复并复核活跃计划、findings/progress 末尾和完整 dirty Git 状态；M62 保持 `implementation_complete`，M53 已随 M65 转为 `complete`，M75 complete，M76 in_progress。系统 `python` 再次命中 Microsoft Store 占位符，未产生副作用；改用 `apps/api/.venv/Scripts/python.exe` 完成 planning catch-up，16 条未同步上下文均指向同一既有 Live Run。下一步只读 GET `e525ca77-22b6-4751-8062-3730f933ba13`，events 不再按 JSON 解析；不创建或 retry Run。
- 2026-07-20：正确按 SSE `data:` 帧读取 M76 Live Trace；Run 当前 inspecting，三个视觉子问题已 3/3、11 usable，`xiaohongshu_search`/`xiaohongshu_assets` 均恰好 3 次。Run 因视觉资产不计 `verified/partial` 仍启动第二轮通用 enrichment；该现象先登记为候选调度缺口，继续只读监控唯一 attempt 0，不重发、不 retry。
- 2026-07-20：唯一 M76 Live 已 `completed`，但质量验收失败并封存不 retry：3 篇小红书笔记产生 11 张合格本地 `aggregator/visual_lead/unknown-rights` 资产，随后错误的 verified/partial enrichment 又加入 2 张非小红书 unknown-publication 视觉线索。下一步只做离线合并诊断、行为红测和最小修复；完整门禁前不创建下一条 Run，当前无需 GitHub 对比研究。
- 2026-07-20：M76 两轮视觉 fixture 行为红测精确失败于 `stop_reason=completion_satisfied`，证明旧实现没有在三题视觉 coverage/enrichment 达成后退出。生产最小修复仅让 `visual_reference_search` 以 usable visual assets 对照 enrichment 数量目标；报告中的 `verified_or_partial`、precedent 和其他目标语义不变。下一步重跑同一 focused test。
- 2026-07-20：首次 green 仍为 `completion_satisfied`，合并诊断确认 fixture 的通用分类器每个四图批只接受 1 张，总 usable 仅 3，未复现 Live 的 11 usable 形状；生产比较修复并非失败。测试现改用同批四图均相关的局部分类器，仍保留真实感知去重并要求至少 6 usable、3 个项目后首轮停止；不放宽产品 enrichment 目标。
- 2026-07-20：M76 视觉路径已扩为 Quick/Balanced/Deep 三档离线矩阵并 3/3 green：默认 3/4/6 个视觉子问题分别执行 3/4/6 次小红书搜索、同数笔记下载与同数四图批分类，均在首轮末题后 `coverage_satisfied`，Provider 不进入第二轮。下一步扩大到 browser/workflow/XHS/Run schema/API 回归。
- 2026-07-20：M76 扩大离线回归全绿：完整 `test_browser_inspection.py`、`test_workflow.py`、`test_xiaohongshu.py`、`test_runs_results.py` 与 `test_schemas.py` 通过，仅既有 TestClient/httpx2 warning。下一步运行 Ruff lint/format、strict Mypy 与 whitespace；仍不创建或 retry Live Run。
- 2026-07-20：M76 修复后 Ruff lint、两文件 format check、strict Mypy 18 source files 与 targeted whitespace 全绿；生产差异仅为视觉目标 enrichment 质量计数。下一步只运行一次完整 `scripts/verify.ps1`，随后只读核对 61 Runs、active 0、M76 记录可见性和 staged 0。
- 2026-07-20：M76 修复后唯一一次完整 `scripts/verify.ps1` 明确 exit 0：314 API、89 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、dev-common/Provider security/process lifecycle 与 30/108 evaluation 全绿。下一步只读核对 61 Runs、active 0、旧 M76 Live 记录可见性和 Git 守卫；通过后才允许重载服务并创建一条必要的全新 attempt 0，绝不 retry 旧 Run。
- 2026-07-20：最终只读守卫确认 3 projects / 61 Runs / active 0；旧 M76 Live `e525ca77-...` 是 `旧厂房社区文化中心` 最新记录，completed/attempt 0/completion_satisfied。首次 PowerShell workspace 枚举因数组包装请求了错误 URL，结论作废且未改状态；改用已验证的单行 Node 守卫成功。Git whitespace exit 0、staged 0，XHS Trace 三次批次为 4/3/4 且 token marker 0。下一步只读 Bridge 预检，通过后重载修复服务并创建一条必要的新 Quick attempt 0。
- 2026-07-20：OpenCLI doctor 全绿后已严格执行一次 stop/start 加载修复代码；重载后原子守卫确认 health ok / `gpt-5.6-sol`、精确 workspace、61 Runs 与 active 0。随后只 POST 一次创建全新 Quick visual Run `36889c78-880e-4b1f-8757-3050d1100cf2`，attempt 0、`research_sources=[xiaohongshu]`；新问题覆盖总平面分区、爆炸轴测、疏散/无障碍流线及材料新旧对比，避免只复验原问题。下一步只读监控，不重发、不 retry。
- 2026-07-20：第二条 M76 Live 当前仍 active；首题已出现一组 XHS 资产，但首轮仍并行执行通用网页搜索。用户明确要求先做 GitHub 类似项目对比且不要错一次就新开 Run；已停止任何新增 Live 计划，只让当前 Run 自行终止。GitHub 初筛锁定 OpenCLI、xiaohongshu-mcp 和两个单笔记下载器，下一步读取具体搜索→详情→图片控制流并形成离线修复合同。
- 2026-07-20：第二条 M76 Live `36889c78-...` 已永久封存 `blocked/no_usable_assets`，不得 retry：0 usable、0/3，XHS search 3、XHS assets 1、通用 local search 6、Trace token marker 0。GitHub 对比确认成熟实现均分离搜索列表与显式单笔记详情；下一步只做离线红绿，三档 fixture 必须证明 XHS 有结果时不调用同题通用 Provider、不混入网页资产。用户明确要求不要失败即新开，因此当前不再规划任何 Live Run。
- 2026-07-20：用户将 M76 目标改为 performance-first，不再要求遵守此前“一题一篇”等产品规则。上一生产补丁因 apply_patch 锚点不匹配被整体拒绝，代码未改；暂停窄修复，当前没有 active/new Live 计划。下一步完成 GitHub 方案比较并设计“首篇快路径、无 usable 时有界尝试同次搜索的下一篇、覆盖即停”的三档离线合同。
- 2026-07-20：GitHub 对比后选择继续使用 OpenCLI，吸收 search/detail 分离与 ranked fallback，不新增 XHS MCP 进程。三档 fixture 已改为每题 top-2：第一篇四图全不相关、第二篇相关；要求 XHS search 一次、download/classify 两次、覆盖后停止且 Provider 0 调用。下一步运行旧实现 red，再实现来源独占与 top-2 early-stop；当前不创建 Live Run。
- 2026-07-20：performance-first 红绿完成两条核心合同。Quick/Balanced/Deep top-2 matrix 先 3/3 red 后 3/3 green：XHS 成功时 Provider 0 调用，首篇 0 usable 时自动处理第二篇并完成；扩展详情快路径也先因多开次选页而 partial，补齐 attempt/satisfied 记账后 green。Quick 视觉上限调整为 24 calls / 48 MiB；Balanced/Deep 原上限足够。下一步合并重跑三组 focused 与完整相关回归。
- 2026-07-20：用户要求把图纸灵感从建筑设计中独立，并取消灵感三档。后端已改为 architectural Quick 恢复 12-call 原合同、visual goal 固定 24 calls / 48 MiB；相关 5 项回归通过。Board 新双入口与灵感无深度行为先 2/2 red 后 green，完整 App 73/73 通过；灵感提交固定 `visual_reference_search/quick`，建筑设计三档和来源反查保留。下一步完整 Board/API 回归与静态门禁，不创建 Live Run。
- 2026-07-20：续接恢复完成：系统 `python` 与 `py` 均不可用，改用项目 `apps/api/.venv/Scripts/python.exe` 成功运行 planning catch-up；完整 HANDOFF、M76 活跃计划、findings/progress 末尾与 dirty Git 状态均已复核。用户截图所示旧“视觉参考”按钮经源码和两个当前 localhost DOM 交叉确认已被独立“图纸灵感”入口取代，当前旧按钮计数 0；未改产品代码、未创建或 retry Run、未截图网页。下一步仍是 M76 完整 Board/API 回归与静态门禁。
- 2026-07-20：M76 收口相关回归全绿：完整 browser-inspection/workflow/XHS/Run/schema API 套件 `175 passed`，完整 Board `90 passed`；唯一 warning 为既有 Starlette TestClient/httpx2 deprecation。未创建或 retry Run。下一步并行运行 Ruff lint/format、strict Mypy 与 Board lint/typecheck/build。
- 2026-07-20：M76 首轮静态门禁中 Ruff lint、strict Mypy 18 source、Board lint/typecheck/build 全绿；Ruff format 仅要求机械重排 `workflow.py` 与 `test_browser_inspection.py`。该结果已登记为一次格式门禁错误；下一步只格式化这两文件并重跑相关 API、Ruff 与 Mypy，不改行为合同。
- 2026-07-20：两文件机械格式化后，M76 API 相关套件再次 `175 passed`，Ruff lint/41-file format、strict Mypy 18 source 全绿；Board 90 tests、lint/typecheck/build 亦已通过。PRODUCT 与 DESIGN 已对齐双主入口、建筑设计独有三档、灵感固定 24 calls / 48 MiB、ranked top-2 与首篇成功即停合同。下一步审计文档和 focused whitespace 后运行唯一一次最终 `scripts/verify.ps1`。
- 2026-07-20：M76 唯一一次最终 `scripts/verify.ps1` 明确 exit 0：313 API、90 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48-file format、strict Mypy 18 source、两端 lint/typecheck/build、process/security contracts 与 30/108 evaluation 全绿。未调用 Provider/Firecrawl、未创建或 retry Run、未截图。下一步仅执行 Run/Trace/Git 只读守卫并同步 M76/HANDOFF。
- 2026-07-20：M76 最终只读守卫通过：3 projects / 62 Runs / active 0；`e525ca77-...` 保持 completed/attempt 0/completion_satisfied，`36889c78-...` 保持 blocked/attempt 0/no_usable_assets，两条 Trace token marker 均为 0；全树 `git diff --check` exit 0、staged 0，全部既有 dirty/untracked 资产保留。M76 标记 `implementation_complete`，最终性能版本未经 post-fix Live 验收且不得自行创建 Run。
- 2026-07-20：用户明确“开始”后批准 M76 唯一 post-fix Live 验收。创建前只读预检通过：API health ok、精确 `旧厂房社区文化中心` workspace 存在、3 projects / 62 Runs / active 0；OpenCLI 1.8.6 daemon、1.0.22 extension、profile `2btxrgkn` 与 connectivity 全绿。下一步严格执行一次 stop/start 加载最终 M76，仍未 POST、未创建或 retry Run。
- 2026-07-20：最终 M76 已严格执行一次 stop/start 并在 8000/5173 ready。重载后原子守卫再次确认 health ok、精确 workspace、62 Runs / active 0，随后只 POST 一次创建全新图纸灵感 Run `f5be3f17-2698-4aa6-8f03-155a04e4c5a2`：attempt 0、goal `visual_reference_search`、budget compatibility `quick`、research_sources `[xiaohongshu]`。下一步只读监控该 Run，不重发、不 retry。
- 2026-07-20：唯一 post-fix Live `f5be3f17-...` 已 completed/attempt 0/coverage_satisfied：5 usable、3 projects、3/3、gaps/enrichment gaps 0。5 个结果全部来自 3 篇唯一小红书笔记，均为 aggregator/visual_lead/unknown rights；每题一次 `xiaohongshu_search` 和一次 `xiaohongshu_assets`。三次同题通用 `openai` search 均 skipped/selected_xiaohongshu_note，local-browser tools 0、Trace token marker 0。该 Run 证明首篇成功快路径；top-2 fallback 保持离线证明，不再为触发 fallback 创建 Run。下一步最终 63 Runs/active 0、三条 M76 状态和 Git 守卫。
- 2026-07-20：M76 Live 最终只读守卫通过：3 projects / 63 Runs / active 0，新 Run 已在项目历史可见；三条 M76 Run 分别保持 completed/completion_satisfied、blocked/no_usable_assets、completed/coverage_satisfied，全部 attempt 0，三条 Trace token marker 均为 0。全树 whitespace exit 0、staged 0，工作树既有 dirty/untracked 资产保持。M76 可标记 complete，不再创建 Live Run。
- 2026-07-20：用户截图启动 M77 visual-style direction planning。范围是把 visual goal 从 precedent 式“问题拆解/项目原文”改为面向宽泛真实提问的“灵感方向/不同风格类型”，同时保持建筑设计研究原结构与 M76 固定预算、XHS top-2、来源独占、provenance/rights 不变。已完成 planning catch-up 与 Impeccable product/clarify 上下文；下一步审计 planner/fallback/Board 条件文案并先写行为红测，不创建 Live Run、不截图。
- 2026-07-20：M77 planner/Board 审计完成。最小跨层合同为：visual planner 输出短的图纸类型 × 表达风格方向，不拆功能/流线/项目事实；Board 在 active/completed visual 状态显示“灵感方向”及灵感图计数，隐藏项目原文和 0 项目措辞；precedent 保持原问题拆解。样式结构无需改变。下一步只新增 Provider/Mock 与 Board 行为红测，旧实现应精确 red。
- 2026-07-20：M77 红测有效：Mock visual plan 仍返回 visible_features/composition/drawing_language，OpenAI prompt 缺少 style-direction 和禁止功能问题拆解合同，API 2/2 red；Board precedent 对照通过，visual 完成态找不到“灵感方向”，运行态仍显示“已经拆成 3 个证据问题”，2/2 red。失败均命中生产旧语义，无 fixture-only 错误。下一步最小实现 planner 与 goal-conditional Board 文案/阶段/计数。
- 2026-07-20：M77 核心实现后 API Provider/Mock 2/2 green；Board active visual 与 precedent 对照 green，完成态唯一失败为任务顶部与方向区重复同一句说明。按 Impeccable clarify 去重后 focused Board 3/3 green。残留审计定位 progress details 与 partial diagnosis 的 precedent 术语，下一步补两条红测并完成同一条件语义，不改变布局。
- 2026-07-20：M77 active progress 与 partial diagnosis 两条残留测试先 2/2 red 后 green：visual 进度详情改为确定方向/搜索灵感/读取图纸/分析画面等，partial 改为灵感图与方向覆盖，无结果提示给出图纸类型/风格词示例。Provider 通用尾句冲突负断言先 red 后 green，visual prompt 已不再要求 design/source-verification issue。核心 focused 为 API 2/2、Board 5/5 green；下一步扩大相关回归。
- 2026-07-20：M77 扩大回归为 API 204 passed、Board 92/92；宽泛输入 placeholder 已红绿改为“找几种剖面图风格/效果图怎么出”，PRODUCT/DESIGN/README/architecture 已对齐灵感方向。首轮静态门禁中 format、strict Mypy、Board lint/typecheck/build 与 Impeccable detector 0 findings 通过，Ruff lint 仅报 Provider prompt 101>100；已登记并只拆分相邻字符串，下一步重跑 Ruff 与 focused Provider。
- 2026-07-20：M77 续接已完整恢复 HANDOFF、活跃计划、findings/progress 末尾与 dirty Git 状态；系统 `python` 命中 Microsoft Store 占位符，改用项目 venv 后 planning catch-up 成功。旧视觉问句的展示层兼容测试 1/1 green：按实际图纸类型重标旧分支，新短风格标签不改，历史 Run 数据不迁移。下一步运行完整 Board 与 M77 相关 API 回归；不创建/retry Run、不重载服务、不截图。
- 2026-07-20：M77 扩大回归与静态门禁全绿：Board 93/93；相关 API 到 100% 且 exit 0，仅既有 TestClient/httpx2 deprecation；Board lint/typecheck/build、Ruff lint/41-file format、strict Mypy 18 source 与 Impeccable detector 0 findings 全部通过。旧视觉问句兼容没有改变 precedent 语义、Run 数据或样式结构。下一步只运行一次完整 `scripts/verify.ps1`，失败时先离线合并诊断，不创建 Live Run。
- 2026-07-20：M77 第一轮完整 `scripts/verify.ps1` 明确 exit 0：Board 93、Extension 165、packaged E2E 8 以及 API、Ruff、strict Mypy、两端 lint/typecheck/build、process/security 与 evaluation 全绿。用户随后指出旧兼容仍错误地按图纸类型分组，因此该门禁只作为变更前基线，不作为最终验收。下一步新增“指定一种图纸类型时只变化风格”和“旧 Run 明示旧版分组”的行为红测，再做最小修复；仍不重载服务、不创建/retry Run、不截图。
- 2026-07-20：M77 第二轮红绿完成。Mock 的自然轴测请求保持同类型测试先通过，OpenAI 故意返回剖面/平面/效果图时的纠正测试与 Board 旧记录诚实分组测试均先 red 后 green。后端现在提取用户明确图纸类型，prompt 要求固定类型，并在模型漂移时用同序确定性风格方向替换错误标题与 rationale；Board 旧问句显示“旧版灵感分组 N”，不再伪造图纸类型方向。Provider visual 4/4、Board visual 3/3 green；入口 placeholder 与产品文档已统一自然轴测提问。下一步完整 Board 与相关 API 回归。
- 2026-07-20：M77 第二轮扩大回归中相关 API 全部到 100% / exit 0；完整 Board 首轮 92 pass / 1 fail，唯一失败是旧断言仍期待被新规则替换的方向说明，生产 DOM 正确。迁移该断言后完整 Board 93/93 green。指定类型纠正、旧版分组、建筑设计语义、XHS/workflow/Run schema 均无行为回归。下一步重跑静态与构建门禁。
- 2026-07-20：M77 第二轮静态与构建门禁全绿：Board lint/typecheck/build、Ruff lint/41-file format、strict Mypy 18 source 与 Impeccable detector 0 findings。当前实现固定用户指定图纸类型、只变化风格；旧 Run 显式标识旧版分组；未改 M76 budget/top-2/source/provenance/rights。下一步只运行变更后的最终一次 `scripts/verify.ps1`。
- 2026-07-20：M77 变更后最终 `scripts/verify.ps1` 明确 exit 0：317 API、93 Board、165 Extension、8 packaged E2E，Ruff lint/48-file format、strict Mypy 18 source、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。最终只读守卫确认 3 projects / 63 Runs / active 0；三条 M76 Run 保持 completed/completion_satisfied、blocked/no_usable_assets、completed/coverage_satisfied，全部 attempt 0；staged 0、`git diff --check` exit 0。M77 标记 `implementation_complete`；当前 API 仍加载 M76，未重载、未创建/retry Run、未截图。
- 2026-07-20：用户以“下一步”明确批准 M77 的唯一 Live 验收。M78 标记 in_progress；目标是一次重载后只创建一条自然轴测风格 Run，验证 plan 全部保持轴测图且风格互异，同时保留 M76 top-2、XHS 来源独占、provenance/rights 与失败后先离线诊断边界。下一步只读复核 63 Runs / active 0、API/Board/Provider 与 OpenCLI Bridge；未通过前不 stop/start、不 POST。
- 2026-07-20：M78 Live 前置第一层通过：planning catch-up 只发现本轮工具调用；OpenCLI 1.8.6 daemon、1.0.22 extension、profile `2btxrgkn` 与 connectivity 全绿；持久状态仍为 3 projects / 63 Runs / active 0，精确目标工作区 `旧厂房社区文化中心` 唯一存在。下一步只读核对 health/Board，再严格执行一次 stop/start 加载 M77；仍未 POST。
- 2026-07-20：M78 最终重载预检通过：API health 为 `ok / openai / 梭子蟹 API / gpt-5.6-sol`，Board HTTP 200。stop/start 源码确认只停止工作区状态文件记录的 8000/5173 listener 并在可用端口重启，不操作 Chrome/OpenCLI。下一步严格执行一次 stop 和一次 start，ready 后重新做 63 Runs / active 0 原子守卫；仍未 POST。
- 2026-07-20：M77 已严格执行一次 stop/start 并在 8000/5173 ready；重载后原子守卫保持 health `ok / gpt-5.6-sol`、精确目标工作区、63 Runs / active 0。ResearchSpec/Board client 合同确认自然问题只需 `visual_reference_search / quick / [xiaohongshu]`，无需手工注入 subquestions。下一步在同一 Node 脚本内再次守卫后发送唯一一次 POST；不重发、不 retry。
- 2026-07-20：M78 创建前同脚本守卫再次确认 63 Runs / active 0，随后唯一一次 POST 创建 Live Run `10d31b4c-94dd-4442-b24a-fc1b241e658e`：created / attempt 0 / `visual_reference_search` / quick compatibility / `[xiaohongshu]`，问题为“我想出一张轴测图，帮我找风格”。下一步只读 GET 该 Run 与 Trace，验证 plan 全部固定轴测图；不重发、不 retry。
- 2026-07-20：M78 Live 首个只读快照为 inspecting / attempt 0；持久化 plan 精确为“精细线稿轴测图 / 拼贴叙事轴测图 / 材质渲染轴测图”，三个 rationale 只描述可见风格，未出现其他图纸类型或 precedent 问题。规划合同已 Live 通过。下一步只读等待该 Run 自然终止，再合并审计 results/Trace/coverage；不重发、不 retry。
- 2026-07-20：唯一 M78 Live `10d31b4c-...` 自然完成 `completed/attempt 0/coverage_satisfied`：10 usable、3/3、每方向 1 pass、3 篇 XHS-only、10/10 aggregator/visual_lead/unknown rights、三次通用 OpenAI search 均 skipped、Trace marker 0。但逐图合并审计发现 8 张 axonometric 外仍混入 1 analysis_diagram + 1 site_plan，均来自拼贴方向同一笔记；该 Run 质量验收因此不算完整通过，永久封存且绝不 retry。下一步只做离线合并诊断和明确图纸类型的逐图准入红绿，不创建第二条 Live Run。
- 2026-07-20：M78 两条离线行为测试先 2/2 red 后 green。workflow deterministic fallback 现在复用 M77 风格方向生成器，规划调用失败时自然轴测问题仍输出线稿/拼贴/材质轴测图；XHS 单笔记四图混合 axonometric/analysis/site-plan 时只持久化两张 axonometric，删除被拒本地文件，并在 Trace 记录 accepted/type-mismatch 计数。三方向 fixture 最终 6 张轴测、3/3、每题首篇即停。下一步扩大 browser/workflow/XHS/Run/API 回归；不创建第二条 Live Run。
- 2026-07-20：M78 首轮扩大回归仅 1 项失败：旧 top-2 fixture 沿用 precedent 默认“剖面”问题却只产 analysis_diagram，新类型门按设计拒绝，导致 stop_reason 改变；其余相关测试通过。fixture 改为未指定图纸类型的通用视觉请求后，top-2、fallback 与指定轴测三条 focused 3/3 green。下一步重跑完整相关套件；生产门不放宽，不创建 Live Run。
- 2026-07-20：M78 修复后完整相关 API 套件到 100% / exit 0；Ruff lint、41-file format check 与 strict Mypy 18 source 全绿。明确类型门只影响 visual 分支的逐图持久化；未指定类型仍保留多图纸探索，M76 top-2/source exclusivity 与 precedent 不变。下一步只运行一次 M78 最终 `scripts/verify.ps1`；不创建第二条 Live Run。
- 2026-07-20：M78 最终 `scripts/verify.ps1` 明确 exit 0：319 API、93 Board、165 Extension、8 packaged E2E，加 Ruff/48-file format、strict Mypy 18 source、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。随后在 64 Runs / active 0 守卫下只重载服务、不创建 Run，最终 API/Board 已加载逐图类型门；post-reload health `ok / gpt-5.6-sol`，唯一 Live `10d31b4c-...` 保持 completed/attempt 0/coverage_satisfied，staged 0。M78 标记 implementation_complete；旧 10-asset 结果保持历史原样且绝不 retry。
- 2026-07-20：M79 来源检视器层级精简已启动并标记 in_progress。验收锁定为先写 Board 行为红测，再最小调整抽屉结构，使逐字原文先于重复分析；不改 API/Run 数据，不创建或 retry Run，不调用 Provider/Firecrawl，不截图，不执行 Git 写操作。
- 2026-07-20：已完成 Impeccable product/distill 上下文与现有来源检视器源码/测试审计。重复内容精确落在右栏的项目条件、空间机制、怎么转译和适用边界，逐字引文则在其后的 open details 内；下一步核对单图证据条是否已展示图面观察/方法推断，再写 M79 行为红测。
- 2026-07-20：M79 信息架构已收敛：抽屉不再承担 dossier 的案例分析副本，只承担来源核验；逐字 claim 先显示，四项来源/归属/权利元数据随后常显，保存动作与备注不变。下一步在现有精准证据定位测试上增加 DOM 顺序、逐字引文和重复标题缺失断言，并先运行旧实现 red。
- 2026-07-20：M79 行为红测有效：focused Board 旧实现找不到“逐字原文证据” region，可访问树同时确认项目条件/机制/转译/观察/推断仍先于 evidence claim。失败命中生产旧 DOM，无 fixture 错误；下一步最小替换检视器右栏结构并删除仅由该旧结构使用的 CSS。
- 2026-07-20：首次合并生产补丁因 `selectedAnalysis` 后续锚点与当前源码不一致被 `apply_patch` 整体拒绝，未产生部分修改。已改为按精确边界拆分 state、JSX 和 CSS 三组小补丁，不重复该失败方式。
- 2026-07-20：拆分后的 M79 最小实现完成，focused 行为测试从缺少“逐字原文证据” region 的 red 转为 1/1 green；测试同时证明原文区域先于核验矩阵，逐字 excerpt/PDF/URL 均保留，六类重复 dossier 标题不再出现在检视器。下一步运行完整 Board 测试，迁移确实被新合同替代的旧断言，不回退生产层级。
- 2026-07-20：完整 Board 首轮为 92 pass / 1 fail。唯一失败是跨子问题测试在已验证 dossier 分支机制后，又要求检视器重复同一机制和边界；生产新 DOM 正确。只把该旧 inspector 断言迁移为逐字来源证据存在且分支机制不重复，然后重跑完整 Board。
- 2026-07-20：旧断言迁移后完整 Board 93/93 green；Board lint/typecheck/build 全绿，Vite 产物成功生成；Impeccable detector 0 findings。下一步补齐 PRODUCT/DESIGN 的检视器职责，并审计无 `evidenceClaims` 旧记录 fallback 是否已有行为覆盖，再进入最终全仓门禁。
- 2026-07-20：文档与数据准入审计完成。M79 将同步 PRODUCT/DESIGN 的来源检视器职责，并删除未获需求支持的零-claim fallback；正式检视器入口继续依赖现有 article-ready claim 合同。之后重跑 focused/完整 Board 与静态门禁。
- 2026-07-20：M79 文档同步与零-claim speculative fallback 清理完成；旧 inspector selectors/derived state 搜索为 0。完整 Board 93/93、lint/typecheck/build 再次全绿。下一步运行 Impeccable detector 与 focused diff/whitespace 审计，通过后执行一次最终 `scripts/verify.ps1`。
- 2026-07-20：M79 最终门禁明确 exit 0：319 API、93 Board、165 Extension、8 packaged E2E，以及全部静态、构建、process/security 和 evaluation 检查。M79 标记 complete；本轮未创建/retry Run、未调用 Provider/Firecrawl、未截图、未执行 Git 写操作。下一步只读复核 whitespace、staged 0 与完整 dirty 状态，然后等待用户指定新的产品工作。
- 2026-07-20：M80 已获用户批准并标记 in_progress。planning catch-up 仅恢复 M79 完成与本轮批准上下文，完整 dirty tree 仍为用户资产；当前源码确认检视器为 M79 新 DOM。下一步连接 in-app Browser，打开既有 M65 结果并执行桌面/390px 无截图 DOM 与键盘检查。
- 2026-07-20：in-app Browser 成功连接但首次打开 `127.0.0.1:5173` 返回 connection refused，说明 Board 当前离线；没有页面或 Run 写入。下一步读取现有启动脚本并只启动 M80 验收所需的本地服务，ready 后再导航一次。
- 2026-07-20：已确认标准 `scripts/start.ps1` 仅隐藏启动工作区 API/Board 并等待 ready，属于 M80 所需本地环境恢复；不会 POST Run 或调用外部 Provider。下一步执行一次 start，读取其实际端口，再让既有 in-app Browser tab 导航到 ready Board。
- 2026-07-20：API/Board 已在 8000/5173 ready。复用首次失败 tab 时，浏览器因该 tab 已转为内部 `data:` 错误页而在 localhost 导航前拒绝，未读取或修改应用。按浏览器恢复合同不再重试该 tab；下一步创建一个 fresh tab 后只导航一次到 ready Board。
- 2026-07-20：fresh tab 已成功加载 Board；当前项目和 3 条最新历史均可见，active Run 没有变化。M65 Deep 因首页只显示最近三条而不可直接点击，不能改用视觉记录。下一步审计现有 URL/历史加载合同，优先使用产品已有的 run 深链；若没有，再选择同产品范围内可见的正文案例记录并明确证据边界。
- 2026-07-20：产品无 run 深链；第一个替代项目的最新 Quick 为旧规则 partial，真实结果页显示 0 个 article-ready dossier，无法打开来源检视器。该 Run 未被 retry 或修改。下一步使用“发起新研究”返回首页，切到剩余的正文研究项目并只打开其最新既有结果。
- 2026-07-20：已返回首页并选择最后一个候选正文项目 `旧厂房更新 · 历史研究`；选择动作只改变当前 UI 项目上下文，没有 POST Run。下一步等待既有历史加载，若最新结果含 article-ready dossier，则用其完成 M80；否则停止 UI 候选轮转并改用明确的验收边界报告。
- 2026-07-20：最后一个候选项目的最新既有 Balanced 检查点成功加载，真实页面显示 2 个已核对原文项目和可展开完整案例库存，满足 M79 来源检视器入口条件。下一步只展开第一题库存，选择其中一个既有 evidence sheet 打开检视器并执行桌面 DOM/键盘检查。
- 2026-07-20：第一题真实 dossier 已展开并打开 De Nederlandsche Bank 来源检视器；1280px 桌面结构、无横向溢出、原文优先、核验/权利常显、来源/动作/备注完整及打开焦点全部通过，重复分析标题为 0。下一步验证 Escape 关闭与焦点返回，再按 viewport capability 合同切到 390px 重开同一检视器。
- 2026-07-20：Escape 关闭和焦点返回已真实通过；焦点回到原始 evidence-sheet trigger。已读取 viewport override/reset 合同，下一步设置 390×844，使用同一已验证 trigger 重新打开检视器并做单列/overflow/touch-target DOM 审计，之后恢复默认 viewport。
- 2026-07-20：390px 首轮验收除 touch target 外全部通过；真实测量显示四个 inspector 交互目标仅 36–37.6px，低于 44px。下一步先关闭检视器并 reset viewport，新增失败的窄屏样式合同，再只调整 inspector controls 到 44px；不立即重复浏览器检查。
- 2026-07-20：检视器已关闭、viewport 已恢复默认，焦点返回仍正确。未找到现有 CSS 文本测试调用，下一步读取 `design-system.test.ts` 的既有模式，并用同一测试风格先写 44px red；当前不再操作浏览器。
- 2026-07-20：已定位既有 raw-CSS 响应式合同测试和 860px touch-size 规则。下一步只新增 inspector 三类交互目标的 44px tablet/mobile 断言，运行旧 CSS red，再在同一 media query 增加最小规则。
- 2026-07-20：M80 会话追赶完成。系统 `python` 再次命中 Microsoft Store 占位符，按既有离线诊断改用 `apps/api/.venv/Scripts/python.exe` 后 catch-up 成功；`git diff --stat` 仅确认完整 dirty tree 仍在，未改写、清理或暂存任何用户资产。下一步保持不变：先补 inspector 三类移动端 44px raw-CSS 红测。
- 2026-07-20：M80 compact-screen 触控合同红测有效：`design-system.test.ts` 3 项中仅新 inspector 断言失败，旧 CSS 的关闭、原始来源、保存/驳回控件没有 44px 规则；另两条既有 responsive 合同通过。下一步仅在 860px media query 为这三类 selector 增加 `min-height: 44px`。
- 2026-07-20：M80 最小 CSS 实现完成，860px media query 只新增 inspector heading button、原始来源 link、inspector action buttons 的 `min-height: 44px`；focused raw-CSS 测试 3/3 green。下一步扩大到完整 Board 测试、lint/typecheck/build 与设计检测器。
- 2026-07-20：M80 Board 局部门禁全绿：94/94 tests、ESLint、TypeScript typecheck、Vite production build 全部 exit 0；Impeccable detector 对 `App.tsx/styles.css` 返回 0 findings。修复符合项目既有 compact-screen ≥44px 控件规范。下一步只运行一次完整 `scripts/verify.ps1`，失败时先离线合并诊断，不创建 Run。
- 2026-07-20：M80 最终 `scripts/verify.ps1` 明确 exit 0：319 API、94 Board、165 Extension、8 packaged E2E，以及 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿；仅既有 TestClient/httpx2 deprecation warning。下一步复用既有真实页面做 390×844 无截图控件实测，要求关闭/原始来源/保存/驳回均 ≥44px 且布局不回归。
- 2026-07-20：M80 已恢复 in-app Browser 的既有 `127.0.0.1:5173` ArchResearch Board 页签；未创建新页签、未截图、未导航外站。下一步确认当前 viewport/项目与检视器关闭状态，再设置 390×844 重开同一既有证据。
- 2026-07-20：恢复页签当前为 1280×720、检视器关闭、真实 partial/Balanced 结果仍在，9 个既有 evidence trigger 可用；没有页面写入或 Run 变化。下一步临时设置 390×844，打开同一首个 evidence trigger 并测量四类控件与布局内容。
- 2026-07-20：390px 首次重开误把 `.evidence-sheet-actions button` 当作来源入口，实际命中“加入方法对照”；同一按钮立即恢复原状态，未打开检视器、未发后端请求。离线 DOM 审计已确定正确入口为 `.evidence-sheet-main[aria-label^="查看 "]`，后续不再猜测 selector。
- 2026-07-20：M80 390×844 post-fix 真实 DOM 验收通过：关闭 44px、原始来源 44px、收藏/拒绝各 44px；检视器单列且 preview 在 evidence 前，8 条逐字 claim 与 8 个定位链接、四项核验字段、备注完整，重复 dossier 标题 0，document/body 横向溢出均 0，打开焦点为“关闭”。下一步 Escape 关闭并核对焦点返回，reset viewport，回首页恢复 `旧厂房社区文化中心`。
- 2026-07-20：恢复后的浏览器封装不提供 `tab.playwright.keyboard`，首次 Escape 调用在执行前失败，检视器和 390px override 均未改变。下一步改用已聚焦关闭按钮 locator 的 `press('Escape')`，不重复失败方法。
- 2026-07-20：改用关闭按钮 `locator.press('Escape')` 后键盘合同通过：检视器关闭，焦点精确返回原 `查看 De Nederlandsche Bank ... 剖面图证据` 按钮；viewport 已 reset 为 1280×720。随后已返回首页，当前临时项目仍为 `旧厂房更新 · 历史研究`；下一步恢复原始 `旧厂房社区文化中心` 并检查页面错误日志。
- 2026-07-20：浏览器首页上下文已恢复原始项目 `旧厂房社区文化中心`，viewport 1280×720、检视器关闭。恢复绑定未直接暴露 Playwright 标准 `consoleMessages/pageErrors` 方法，下一步按 Browser 公共诊断合同读取页签错误日志，不尝试 DOM 注入或截图。
- 2026-07-20：当前 Browser 页签与 Playwright 公共方法集确认没有直接 console/page-error 读取入口；没有调用未定义方法或改变页面。下一步只检查 Browser capability 列表是否提供 diagnostics/error-log，若没有则以真实 DOM 无错误状态和现有 test/E2E 门禁诚实收口。
- 2026-07-20：Browser capabilities 仅提供 visibility 与 viewport，没有 diagnostics/error-log 读取能力；因此不声称读取不可用的 console 历史。下一步只做可验证的页面错误态 DOM 审计，关闭 M80 早期遗留的 connection-refused interstitial，保留已恢复原项目的 Board 页签。
- 2026-07-20：Board 页面的可见错误态 DOM 审计已先完成；随后 Browser URL 策略阻止绑定早期 `data:` connection-refused interstitial，因此不再尝试关闭或导航该旧页签。有效 localhost Board 页未受影响，下一步只读取已保存审计结果并标记该页为最终 deliverable。
- 2026-07-20：恢复的 localhost tab 实例未实现其原型列出的 deliverable marker，调用在页面状态改变前失败；不再重试非产品页签元数据。下一步只读取已保存的 DOM 错误态审计与 URL/title，保持有效 Board 页原状。
- 2026-07-20：M80 浏览器最终状态确认：有效页签为 `http://127.0.0.1:5173/` / `ArchResearch Board`，原项目已恢复，viewport reset，检视器关闭；可见 alert 0、错误文本 0、Vite overlay false。Browser 不提供 console 历史能力，因此只报告可验证的 DOM 错误态。下一步做 3 projects / 64 Runs / active 0 只读守卫，随后停止本地服务并运行 Git 守卫。
- 2026-07-20：最终只读状态守卫首次误用无前缀 `/workspaces`，API 返回 404，所得 0/0/0 已丢弃且没有写入。下一步从 Board client 读取实际 base URL 后只重跑一次正确 GET，不重复错误路径。
- 2026-07-20：`/v1/workspaces` 正确命中，但响应为包装对象；第二个只读脚本误把包装对象当工作区，null-id GET 返回 `Workspace not found`，1/0/0 同样丢弃。下一步只读输出响应结构，再按真实集合字段聚合，不再猜测。
- 2026-07-20：响应结构诊断确认 `/v1/workspaces` 实为 3 项 JSON array，PowerShell `@(...)` 对 `Invoke-RestMethod` 的数组保留语义导致前次误计。显式逐项聚合后的最终只读守卫通过：3 projects / 64 Runs / active 0；封存 Run `10d31b4c-...` 仍为 completed / attempt 0 / coverage_satisfied。下一步停止 M80 启动的本地 API/Board。
- 2026-07-20：M80 本地 API/Board 已由标准 `scripts/stop.ps1` 成功停止。最终 Git 守卫通过：全树 `git diff --check` exit 0（仅既有 LF→CRLF 提示）、staged 0；`git status --short --branch` 保留完整 dirty/untracked 用户资产，未 reset/checkout/clean/stage/commit/push。下一步将 M80 标记 complete，并把 HANDOFF 唯一下一步改为“无已批准产品工作，等待用户指定”。
- 2026-07-20：M80 已标记 complete 并同步 HANDOFF。最终验收为 1280×720 与 390×844 真实 DOM/键盘通过，四类 inspector 控件实测 44px；319 API / 94 Board / 165 Extension / 8 E2E 全仓门禁、detector 0、whitespace/staged 守卫全绿。当前 3 projects / 64 Runs / active 0，封存 `10d31b4c-...` 未 retry，服务已停止；当前没有已批准的自动下一步，等待用户指定新的产品工作。
- 2026-07-20：M80 最终交接守卫再次通过：task_plan 中 M77/M78 保持 `implementation_complete`、M79/M80 为 complete，HANDOFF 明确无已批准自动下一步；端口 8000/5173 listener 0、staged 0、全树 whitespace exit 0，完整 dirty/untracked 工作树保持。
- 2026-07-20：用户报告 `127.0.0.1:5173` connection refused；根因是 M80 收尾按计划停止了本地服务，不是页面回归。已按用户请求重新执行标准 `scripts/start.ps1`，API health `ok`、Board HTTP 200，持久状态保持 3 projects / 64 Runs / active 0；没有创建或 retry Run。
- 2026-07-20：用户以截图提出并随后明确批准 M81：图纸灵感应在固定预算内累计更多高相关帖子，同一帖子多图按帖子分组，帖子级文案/来源/权利只显示一次，图片仍可独立选择且保留逐图类型门。首次只读阶段无法写文件；权限恢复后 planning catch-up 已取回 brief，M81 标记 in_progress。下一步审计 workflow 早停、结果字段和 Board 渲染，先写 API/Board 行为红测。
- 2026-07-20：M81 首轮源码索引完成。后端已有 ranked XHS note 与逐图过滤/trace 基础，Board flat-card 重复由逐 result 渲染产生；当前不需要新增持久化 schema，预计只需 workflow 累计策略、Board `sourceUrl` 分组派生与相应测试/CSS。下一步读取精确函数/fixture/DOM 边界，确定红测插入点。
- 2026-07-20：M81 根因收敛为两个精确状态/映射点：workflow 的 `xiaohongshu_satisfied_subquestions` 在首个 `added > 0` 后阻断同方向后续 note；Board 的 `typeGroups.flatMap(...).map(result)` 生成逐图重复卡。下一步读取现有 top-2、type-gate 与 inspiration-board fixture，先扩展为“多个成功 note 均保留”和“同 sourceUrl 只渲染一份 note 文案”的红测。
- 2026-07-20：M81 红测合同确定：后端同一次 ranked search 最多尝试 4 帖，累计 3 个 usable notes 后停止；每帖仍最多 4 图、M78 type gate 与 budget 不变。Board 在每个方向内按 sourceUrl 派生 note group，帖子标题/摘要/原帖/权利一次，图片继续以 asset ID 独立打开与加入对照。下一步先修改测试 fixture 与断言并运行旧实现 red。
- 2026-07-20：M81 API/Board 红测有效。API 期望前三篇 usable ranked notes，旧实现只持久化 note 1；Board 期望 3 个有名称的 note articles、3 个原帖链接和 5 个独立对照按钮，旧实现渲染 5 个匿名逐图 articles/5 个链接。失败均命中生产旧行为，无 fixture-only 错误。下一步最小实现 usable-note target/attempt limit 与 sourceUrl note grouping。
- 2026-07-20：M81 实现边界复核完成：OpenCLI 与 extension 两条 XHS inspection 都需累计 usable-note count；Board 可复用现有分析/摘要 helper 和逐图 inspector/comparison state，无 schema/API 迁移。下一步修改 workflow constants/state/trace 与 inspirationGroups/render/CSS，然后重跑同两条 focused tests。
- 2026-07-20：M81 核心实现 focused green。workflow 同一次 ranked search 最多尝试 4 帖、累计 3 个 usable notes 后停，OpenCLI/extension 均记录 usable-note count；Board 每方向按 sourceUrl 生成 note article，帖子元数据/摘要/原帖/权利一次，帖子内两列图片仍按 asset ID 打开与独立对照。API 1/1、Board 1/1 通过。下一步运行完整 browser-inspection 与 Board 套件，迁移 superseded first-note 断言。
- 2026-07-20：扩大回归为 Board 94/94 green；browser-inspection 仅旧 extension first-note fixture 失败。该 fixture 每方向只返回 rank 1–2 并断言首篇停，与 M81 三篇 usable target 冲突，生产逻辑正确不回退。下一步把 fixture 扩为 rank 1–4，断言每方向仅打开 1–3 后重跑完整套件。
- 2026-07-20：extension fixture 扩为 rank 1–4 后 focused 仍以 partial 失败，当前断言顺序未显示 opened-note/coverage 形状。下一步仅改善失败诊断：先断言打开顺序，再用 stop reason/coverage 解释终态；不再猜测或重复相同运行参数。
- 2026-07-20：失败诊断显示 extension 只在 branch 1 打开 rank 1–4，根因是 fixture 每页复用同一组 media URLs，后续帖子全部被正确去重为 added 0。已让 fixture 的 media URL 绑定当前 note slug，保持生产去重不变；下一步重跑该 focused test，要求每方向仅打开 rank 1–3。
- 2026-07-20：唯一媒体 URL 后 extension 依次打开 branch-1 ranks 1–3 与 branch-2 rank 1，随后按固定 24-call 预算停止；这条 fallback 无法在不突破预算的前提下实现三帖/方向。下一步把该旧 completed fixture 迁移为“在固定预算内累计四帖后 partial/budget exhausted”，而 OpenCLI focused test继续证明默认主路径三帖成功目标。
- 2026-07-20：extension fallback 合同迁移后 focused green：它在 24-call 上限内累计四篇帖子后诚实 partial；默认 OpenCLI focused 仍证明每方向前三篇 usable notes 均保留。下一步重跑完整 browser-inspection，并行执行 API/Board 静态、构建与 Impeccable detector。
- 2026-07-20：M81 扩大门禁中完整 browser-inspection、Ruff lint、strict Mypy 18 source、Board lint/typecheck/build 和 Impeccable detector 0 findings 全绿；唯一失败是 Ruff format 要求新增测试机械换行。下一步只格式化 `test_browser_inspection.py`，复跑 format 与 OpenCLI/extension 两条 focused。
- 2026-07-20：Ruff format 后 41 files clean，OpenCLI 三篇 usable 与 extension 24-call partial 两条 focused 2/2、Board note-group focused 1/1 全绿。PRODUCT/DESIGN 与 Board 说明已从 top-2/首篇停止/逐图卡改为一次搜索最多四帖、三篇 usable 目标和帖子内图片组。下一步重跑完整 API browser/Board 与静态门禁，随后无截图真实 DOM QA。
- 2026-07-20：M81 无截图浏览器 QA 已连接当前 ready Board；in-app Browser 没有可认领的现有页签，按浏览器合同新建单一本地 `http://127.0.0.1:5173/` 页签，首页成功加载且未 POST/创建/retry Run。下一步等待既有任务加载，只打开已有图纸灵感结果并核对帖子级 DOM、逐图对比与响应式几何。
- 2026-07-20：M81 桌面无截图结构首验通过：打开既有封存 `我想出一张轴测图，帮我找风格` 只读取历史结果；Board 显示 3 篇帖子 / 10 张图，帖子元数据和来源/权利各一次，逐图检视与独立对照控件完整。下一步用一次 bounded DOM 几何读数核对 article/图片/文案/链接/对照数量与桌面 overflow，再进入 390×844。
- 2026-07-20：M81 desktop DOM/geometry contract 全部满足：3 article、10 image、10 compare；每 article 的 title/observation/source/rights 都恰为 1，帖子外层两列、帖子内图片两列，document/body overflow 0。已读取 viewport capability，下一步临时设为 390×844 验证帖子单列、内图双列、对照 ≥44px 和 overflow 0，随后 reset。
- 2026-07-20：M81 mobile contract 全绿：390×844 下帖子单列、帖子内图双列，10 个逐图对照目标均 44×44px，原笔记链接 44px，document/body overflow 0。下一步 reset viewport，读取 error-level tab logs；若无运行时错误，则执行唯一一次最终 `scripts/verify.ps1`。
- 2026-07-20：M81 无截图浏览器 QA 完整结束：desktop/mobile 帖子层级、逐图独立选择、44px 触控、零 overflow 与 error log 0 均通过；viewport 已恢复 1265px，单一既有结果页保留。下一步运行最终全仓门禁；任何失败先离线合并诊断，不创建 Live Run。
- 2026-07-20：M81 最终全仓离线门禁 exit 0：319 API / 94 Board / 165 Extension / 8 packaged E2E，加 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 evaluation 全绿。下一步只读复核 3 projects / 64 Runs / active 0、封存 Run、whitespace/staged 0/status；通过后标记 M81 complete 并同步 HANDOFF。
- 2026-07-20：M81 最终 Git 守卫已确认 whitespace exit 0、staged 0，完整 dirty/untracked 用户资产保持。首次 API 数量脚本因已知 PowerShell 数组语义得到无效 1/0/0，已记录并丢弃；下一步仅用 Node 标准 JSON 重跑正确的只读聚合。
- 2026-07-20：M81 已 complete。默认 OpenCLI 每方向最多尝试 4 帖并累计 3 个 usable notes，每帖最多 4 图；extension fallback 在固定预算内诚实 partial。Board 按帖子显示一次标题/观察/来源/权利，逐图仍可检视和独立对照。desktop/390px 无截图 QA、319 API / 94 Board / 165 Extension / 8 E2E、全部静态/构建、whitespace/staged 守卫全绿；最终状态 3 projects / 64 Runs / active 0，封存 `10d31b4c-...` 未 retry，服务保持运行。当前没有已批准的自动下一步，等待用户指定新的产品工作。
- 2026-07-20：M81 交接守卫完成：task_plan 中 M77/M78 保持 `implementation_complete`、M81 为 complete，HANDOFF 明确当前无已批准自动下一步；API health `ok`、Board HTTP 200，服务保持运行；最终 whitespace exit 0、staged 0，完整 dirty/untracked 工作树未清理或写入 Git。
- 2026-07-20：M82 single post-fix Live accumulation acceptance 已获用户明确批准并标记 in_progress。唯一 Run 将使用 `旧厂房社区文化中心`、`我想出一张轴测图，帮我找风格`、visual_reference_search / quick / XHS-only；验收多帖子累计、逐图轴测类型门和帖子级 Board。无论成功或失败都不 retry、不连续创建第二条，先完成离线合并审计。
- 2026-07-20：M82 Run `cbf8053b-f0c6-4408-b0d7-4b0ffff13b16` 已以唯一一次 POST 创建，初态 created / attempt 0。下一步只读监控至 terminal；不会 retry 或创建第二条。
- 2026-07-20：M82 只读监控确认 Run 从 searching 进入 inspecting，仍为 attempt 0、无 stop reason，尚未到 terminal。继续只监控同一 ID，不追加 Run。
- 2026-07-20：M82 继续在 searching/inspecting 间按方向推进，说明 workflow 已进入后续搜索/检查循环；coverage 尚未持久化、无 stop reason。仍不干预执行，不创建或 retry Run。
- 2026-07-20：M82 唯一 Live Run 已 terminal partial，Live 验收失败：11 results 均为通用网页资产而非小红书帖子，且类型不符合轴测图。已停止 Live 创建路径并封存该 Run；下一步只读提取 run/trace/result 形状并与离线合同合并诊断，不 retry、不补开第二条。
- 2026-07-20：M82 第一轮离线合并诊断完成：OpenCLI 命令先失败；extension fallback 返回通用网页而非小红书 note，随后 generic OpenAI 被抑制但这些 extension 结果仍进入通用页面检查/持久化。下一步只读定位 workflow 的 fallback 来源准入与既有测试缺口，形成一条全局修复方案；不改数据、不创建 Run。
- 2026-07-20：M82 根因已收敛为来源降级边界，不是帖子累计或 Board 分组：本次 extension XHS backend 在 run 启动时未连接，OpenCLI 失败后空 XHS sources 没有抑制 Direct public search，导致通用网页结果进入 visual workflow。下一步只读查现有服务日志和相邻测试，确认 OpenCLI 失败表象与最小缺失行为合同后结束诊断；当前不实施修复。
- 2026-07-20：M82 测试缺口已确认：既有 M81 只证明 XHS 成功累计和 XHS URL extension 检查，不证明所有 XHS backend 失败时的 XHS-only fail-closed。诊断还需确认当前 API 服务是否自动 reload 了 M81 workflow，排除“旧进程代码”干扰，然后以只读状态守卫收口。
- 2026-07-20：M82 合并诊断形成双根因：① Live API 未加载 M81 workflow，故本次不是新累计逻辑的有效测试；② OpenCLI 失败且 extension XHS backend 未连接时，当前来源边界仍会降级 Direct public search。下一步只做当前状态/工作树守卫并把 M82 标记为 failed acceptance / diagnosis complete；不自动重启后再开 Run，不实施未获批准的生产修复。
- 2026-07-20：M82 已标记 complete（Live acceptance failed, diagnosis complete）。唯一 Run `cbf8053b-...` 永久封存；没有 retry 或第二条 Run。当前没有已批准自动下一步，等待用户决定是否先修复 XHS-only fail-closed 并重载服务；在此之前绝不再做 Live 测试。
- 2026-07-20：M82 最终守卫：3 projects / 65 Runs / active 0；旧封存 `10d31b4c-...` completed/attempt 0，新封存 `cbf8053b-...` partial/attempt 0/browser_inspection_incomplete。whitespace exit 0、staged 0，工作树完整保留。当前服务仍是早于 M81 的 API 进程，未擅自重启；等待用户批准 fail-closed 修复与受控重载。
- 2026-07-20：M83 已获批准并标记 in_progress。计划：① 新增 XHS-only 全后端失败的行为红测；② 最小 fail-closed 修复；③ focused + full offline gate；④ active 0 下标准 stop/start 一次加载 M81+M83。全程不创建/retry Run。
- 2026-07-20：M83 红测边界已确定，下一步只编辑 `test_browser_inspection.py` 增加 XHS-only failure × local/model 两种搜索面的参数化合同，先运行旧生产得到 red，再修改 workflow。
- 2026-07-20：M83 红测 2/2 按预期失败：local 搜索 3 次、model 搜索 3 次。下一步只在 workflow 搜索能力计算中为精确 XHS-only visual Run 关闭两类通用搜索，不改 XHS searcher fallback、累计、预算或其他研究目标。
- 2026-07-20：M83 核心红绿完成，focused 5/5。下一步运行完整 `test_browser_inspection.py`、Ruff lint/format 和 strict Mypy；若通过再执行唯一一次全仓门禁。
- 2026-07-20：M83 完整 browser-inspection 与 API 静态门禁全绿。下一步只同步 README/architecture/PRODUCT/DESIGN 的“四帖三 usable + 全 XHS backend 失败不降级通用网页”合同，然后执行唯一一次 `scripts/verify.ps1`。
- 2026-07-20：M83 首次 full gate 已记录为失败，不会原样重跑。两条 board-export fixture 依赖被新合同废止的“XHS-only → generic mock asset”行为；下一步读取其设置，最小迁移测试数据入口，先跑 export + fail-closed focused。
- 2026-07-20：M83 export fixture 已只补显式空来源，生产代码未再改。下一步运行完整 board-export 文件 + fail-closed/M81 focused，确认新来源合同和导出行为同时通过。
- 2026-07-20：M83 focused 10/10 全绿。下一步重跑修复后的完整 `scripts/verify.ps1`；若通过，再在 active 0 下标准 stop/start 服务并核对新 API 进程晚于 workflow 修改时间。
- 2026-07-20：M83 最终全仓离线门禁 exit 0：321 API / 94 Board / 165 Extension / 8 packaged E2E，加全部静态、构建、process/security 与 evaluation。下一步只读确认 active 0 后标准 stop/start 一次，验证新 API started_at 晚于 workflow last-write；不创建 Run。
- 2026-07-20：M83 服务已标准重载，API/Board ready，3/65/0 保持。时间先后守卫的首次 bool 因 UTC 解析错误作废；下一步仅用 DateTimeOffset 重算 API started_at 是否晚于 workflow last-write，然后进入最终 Git/计划收口。
- 2026-07-20：时间守卫第二种 PowerShell 解析仍被 `ConvertFrom-Json` 日期强制转换污染，已记录并停止该方法。下一步改用 Node 原始 JSON/mtime 一次性重算，不再尝试第三种 PowerShell 时间转换。
- 2026-07-20：M83 已达 `implementation_complete`：XHS-only local/model 两条通用降级均 fail-closed，M78/M81 路径保持；最终 321 API / 94 Board / 165 Extension / 8 E2E 全绿。标准服务重载成功且新 API 确认晚于 workflow 修改，当前 3 projects / 65 Runs / active 0；未创建/retry Run。当前没有已批准自动下一步，等待用户指定是否需要新的 post-fix Live 验收。
- 2026-07-20：M83 最终交接守卫通过：M77/M78/M83 均为 `implementation_complete`；API health ok、Board HTTP 200、3 projects / 65 Runs / active 0，两条封存 Run 状态未变。全树 whitespace exit 0、staged 0，完整 dirty/untracked 工作树保留；当前无已批准自动下一步。
- 2026-07-20：M84 loaded post-fix Live accumulation acceptance 已标记 in_progress。下一步只读预检 API loaded time、3/65/active 0、`/v1/browser/status`；预检不通过则停在环境修复，不创建 Run。
- 2026-07-20：M84 没有创建 Run。首次环境 doctor 发现 OpenCLI daemon 存活但 Bridge extension 未连接；按 doctor 建议下一步只重启 OpenCLI daemon 一次并重新 doctor。若仍失败则停止并请求用户在 Chrome 恢复 Bridge，不消耗 Run。
- 2026-07-20：M84 OpenCLI 修复尝试已到第二步仍失败，已停止重复 daemon restart/doctor。下一步只检查既有用户 Chrome 是否运行并可唤醒；不安装扩展、不授权权限。若 Bridge 不恢复，M84 停在 preflight blocked。
- 2026-07-20：M84 preflight 仍未创建 Run。下一步只启动用户现有 Chrome 默认会话一次并再次 doctor；若既有 Bridge 自动连接则继续，否则停下请求用户处理扩展。
- 2026-07-20：M84 preflight 通过，OpenCLI Browser Bridge 已连接；当前仍未创建 Run。下一步再次确认 3/65/active 0，然后只 POST 一条全新 attempt 0，并只读监控该 ID。
- 2026-07-20：M84 Run `965fde3c-3a75-41b2-a119-381ee1f21c44` 已唯一一次 POST 创建，初态 created / attempt 0。下一步只读监控该 ID 到 terminal，不 retry、不补开第二条。
- 2026-07-20：M84 只读监控确认 Run 已连续处于 inspecting，仍为 attempt 0、无 stop reason；与 M82 sequence 4 即 OpenCliCommandError 不同，本次已越过搜索入口。继续只监控同一 ID。
- 2026-07-20：M84 首方向实时审计通过关键路径：3 个 ranked note 已处理，2 usable / 1 全量类型拒绝，8 persisted images 全为 axonometric 且来源仅 XHS。Run 仍 inspecting；继续等待第 4 帖与后续方向，不干预。
- 2026-07-20：M84 已停止 Live 执行并禁止第二条 Run。当前合并诊断焦点是固定 24-call 与“三帖/方向”目标不相容：首方向为排除错类型消耗 16 calls，第二方向再耗 8 即清零。下一步只读提取最终 run/coverage/全 Trace 预算形状，不改代码、不 retry。
- 2026-07-20：M84 离线合并预算诊断完成。Live 验证了 OpenCLI、XHS-only、逐图 type gate 与首方向三帖累计，但整体验收因 24 per-image slots 在第二方向耗尽而失败；无第二条 Run。下一步需要产品决策：提高/重定义视觉预算，或改为跨方向公平调度并降低每方向目标；当前不擅自选择成本策略。
- 2026-07-20：M84 最终守卫：3 projects / 66 Runs / active 0；`965fde3c-...` partial/attempt 0/time_budget_exhausted，另外两条封存 Run 状态未变。API health ok、Board HTTP 200、whitespace exit 0、staged 0；工作树全部保留，当前无已批准自动下一步。
- 2026-07-20：M85 已获批准并标记 in_progress。唯一下一步是先新增“48 逐图槽位可覆盖三方向最坏四帖尝试”和“inspection exhaustion 返回 `visual_budget_exhausted`”两类行为红测，再做 24→48 与停止原因的最小生产修改。保留 48 MiB、M78/M81/M83/extension 合同；本阶段不创建或 retry Live Run。
- 2026-07-20：M85 首次红测中容量 fixture 在旧实现按预期只到 partial，Board 也按预期缺少新停止原因映射；耗尽原因 fixture 先被 `ResearchPlan` 至少三方向的 schema 门拦截，尚未触达生产。下一步只补齐该测试的三方向合法形状，并把容量断言前置为明确的 24→48 差异后重跑红测；不修改生产。
- 2026-07-20：修正 fixture 后 M85 红测有效：容量断言得到 24 而非 48；耗尽恢复得到 `no_usable_assets` 而非 `visual_budget_exhausted`；Board 得到通用 partial 文案。现已做最小生产修改：视觉逐图上限 48、字节上限仍 48 MiB，视觉任务在搜索前优先识别 inspection exhaustion，Board 增加自然语言映射。下一步重跑三条 focused 红绿并迁移受新 48 槽合同影响的 extension 断言。
- 2026-07-20：M85 首轮绿化后，48 槽最坏三方向 fixture 与 Board 映射已通过；仅“已耗尽且零资产”形状仍在最终终态归并被 `no_usable_assets` 覆盖。下一步只让该终态分支保留已确定的 `visual_budget_exhausted`，其他零资产 fail-closed 继续使用 `no_usable_assets`，然后重跑 focused。
- 2026-07-20：终态原因保留后 M85 focused 仅剩旧 extension 24-slot 顺序断言失败；新实际为前两方向各 3 帖、第三方向 2 帖，共 8 帖后恰好 48 slots。下一步迁移该 superseded 断言，并同时要求 extension 在第三方向未达到三帖目标时继续 `partial/visual_budget_exhausted`；OpenCLI 最坏四帖 fixture继续证明三方向均不饿死。
- 2026-07-20：迁移 extension 顺序后，它在 48 slots 下误报 `completed/coverage_satisfied`，因为现有完成门只看逐图资产 coverage，不看 M81 usable-note target。现已仅为 XHS-only visual 的 coverage/completion/终态三处加入每方向三帖门，并在最后查询恰好耗尽时设置 `visual_budget_exhausted`。下一步重跑 focused，确认 OpenCLI 三方向 3 帖完成、extension 第三方向 2 帖诚实 partial。
- 2026-07-20：首次完成门实现引用不存在的 `subquestions` 局部名，触发 `NameError`，该轮 16-slot/failed 结果全部作废。修正时同时收窄语义：只在 XHS-only visual 已耗尽预算且存在不足三帖方向时禁止 completed；未耗尽的一帖/方向类型过滤 fixture 仍可按既有 coverage 完成。下一步重跑同一 focused 矩阵。
- 2026-07-20：M85 focused 最终 7/7 green；完整 browser-inspection 在 Ruff 机械格式化后的最终源码上明确 exit 0，Board 完整套件 95/95 green，Ruff lint green。新合同证明 OpenCLI 三方向最坏四帖恰用 48 slots 并完成，extension 8 帖后第三方向仅 2 usable 时 `partial/visual_budget_exhausted`；M78/M81/M83 保持。下一步运行 strict Mypy、最终 Ruff format 与 Board lint/typecheck/build，再进入全仓门禁。
- 2026-07-20：扩大静态门中 Ruff lint/41-file format 与 Board lint/typecheck/build 全绿；直接 `mypy.exe src` 单独返回 exit 1 但 stdout/stderr 为空，不能作为有效类型诊断。下一步改用同一 venv 的 `python -m mypy src` 单独取权威输出，不重复无诊断的启动方式。
- 2026-07-20：同一 venv 的 `python -m mypy src --show-error-codes` 权威通过 18 source files，确认前一条空输出 exit 1 是启动方式异常，不是类型错误。M85 扩大门禁现已全部绿色；下一步执行唯一一次全仓 `scripts/verify.ps1`。
- 2026-07-20：M85 最终全仓门禁明确 exit 0：323 API / 95 Board / 165 Extension / 8 packaged E2E，加 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。SQLite 只读确认 3 projects / 66 Runs / active 0 及三条封存 Run 未变。首次服务探针误用了过期的 8765，connection refused 不能证明服务离线；下一步读取标准 dev-process state 与实际 8000 端口后决定 stop/start 或只 start。
- 2026-07-20：M85 已达 `implementation_complete`。active 0 下标准 stop/start 一次成功，新 API started_at 晚于 workflow last-write，API health ok、Board 200，3 projects / 66 Runs / active 0 保持；三条封存 Run 未 retry 或迁移。最终 whitespace exit 0、staged 0，完整 dirty/untracked 工作树保留。当前没有已批准自动下一步，等待用户指定产品工作。
- 2026-07-20：M86 已获批准并标记 in_progress。唯一下一步是只读预检当前服务确实加载 M85、3 projects / 66 Runs / active 0 与 OpenCLI/extension XHS surface；预检通过后只 POST 一条全新 attempt 0。无论终态如何都不 retry、不补开第二条；失败立即转入持续离线诊断和红绿修复。
- 2026-07-20：M86 preflight 通过：loaded M85、3/66/active 0、API/Board ready，ArchResearch browser connected/XHS available，OpenCLI doctor daemon/Bridge/connectivity 全绿。下一步在同一 POST 脚本内再次要求 runs=66 且 active=0，然后只创建一条 natural-axis visual_reference_search / quick / XHS-only attempt 0；该命令不重复。
- 2026-07-20：M86 唯一 Live Run `23b6f84c-8658-43c7-9317-32c0696ca583` 已以唯一一次 POST 创建，初态 created / attempt 0，workspace `旧厂房社区文化中心`，natural-axis / visual_reference_search / quick / XHS-only。下一步只读监控该 ID 到 terminal；不得 retry、不得补开第二条。
- 2026-07-20：M86 首次只读监控误把 `/events` SSE 当 JSON，Node 在解析 `event:` 帧时失败；没有 API 写入或 Run 影响。停止该方式，后续用普通 GET 读取 run/results，并从 SQLite 只读提取 Trace，不重复 SSE JSON 解析。
- 2026-07-20：M86 Run 正在 inspecting；planner 生成精细线稿/拼贴叙事/材质渲染三个轴测方向。第一方向首帖 4/4 axonometric 接受，当前 1 post / 4 images / visual_calls_used 4，XHS-only、generic skipped。下一步继续只读监控同一 ID，不干预、不创建新 Run。
- 2026-07-20：M86 第一方向 note 2 消耗 4 slots、2 个候选均类型 mismatch；note 3 接受 3 张 axonometric，usable_note_count=2。当前持久化 2 posts / 7 images，全部 XHS-only axonometric，run visual_calls_used 已到 16，表明 note 4 正在处理。继续只读监控同一 Run。
- 2026-07-20：M86 第一方向 note 4 接受 4 张并达到 3 usable posts；第二方向 note 1 接受 1/3 axonometric。当前 4 posts / 12 images，visual_calls_used=24 后仍在 inspecting，已真实越过 M84 旧上限；第二方向 note 2 正在处理。继续只读监控，不创建新 Run。
- 2026-07-20：M86 第二方向 notes 2–3 分别接受 3/3 与 4/4，达到 usable_note_count 3，coverage 2/3。第三方向 OpenCLI search completed/result_count 4、generic skipped，当前 5 unique posts / 16 axonometric images / visual_calls_used 32，正在检查第三方向首帖。继续只读监控同一 ID。
- 2026-07-20：M86 第三方向 notes 1–2 各接受 3/3 axonometric，usable_note_count=2。当前 7 unique posts / 22 images / visual_calls_used 40，第三方向 note 3 正在处理；第三方向已获得真实分类容量，generic 仍为 0。继续只读等待终态。
- 2026-07-20：M86 唯一 Run `23b6f84c-...` 已 terminal completed/attempt 0/coverage_satisfied：25 images、8 unique XHS posts、3/3、visual_calls_used 40 / 48，全部持久类型 axonometric；第三方向 note 3 接受 3、拒绝 1 mismatch 并达到 usable_note_count 3。下一步只读审计全部 tiers/rights/Trace generic 0 与每方向帖子数，并无截图复核 Board 帖子级分组；不创建第二条 Run。
- 2026-07-20：M86 数据层最终审计全绿：每方向 3 posts，25 images 全为 XHS-only axonometric / aggregator / visual_lead / unknown rights，generic Trace 0；当前 3/67/active 0。in-app Browser 存在两个 localhost Board 页签，下一步认领最近页签、仅 reload 获取最新 Run，再从可见 DOM 打开 M86 并核对 3 方向 / 9 方向级帖子组 / 25 图片，禁止截图和任何提交动作。
- 2026-07-20：M86 真实 Board 已打开且快照显示 3 方向分别 3篇/11张、3篇/8张、3篇/9张，顶部唯一汇总 8篇/25张。首个 bounded evaluate 错把 accessible region name 当 `aria-label`、错把按钮 accessible name 当 textContent，方向 -1/compare 0 均作废；有效结果为 9 articles、9 headings/source links、每帖 rights 一次、all image alt axonometric、overflow 0、error logs 0。下一步改用 snapshot 明示的 role/name 计数，不重复错误 selector。
- 2026-07-20：M86 role/name DOM 复核全绿：精细线稿/拼贴叙事/材质渲染各 3 articles，图片与独立对照均为 11/8/9，原笔记链接各 3；页面 9 帖子组、28 方向级图片实例，数据层仍为 8 unique posts / 25 unique assets。无截图、overflow 0、error log 0。下一步保留当前 M86 结果页，然后执行最终 3/67/active 0、Git whitespace/staged 守卫并收口计划。
- 2026-07-20：M86 已 complete。唯一 Run `23b6f84c-...` completed/attempt 0/coverage_satisfied，结果页已作为 deliverable 保留；最终 API health ok、Board 200、3 projects / 67 Runs / active 0，三条历史封存 Run 状态未变。whitespace exit 0、staged 0，工作树全部保留；没有 retry 或第二条 Run。当前没有已批准自动下一步，等待用户指定产品工作。
- 2026-07-20：M87 已获批准并标记 in_progress。假设“用户也不想看研究过程”指两种结果页都移除 Trace 前台入口/抽屉，但后端 Trace 数据与 API 继续保留诊断用途。下一步读取现有 result-workbench CSS、trace state/fetch/drawer 与相邻 Board 测试，先写建筑设计/图纸灵感功能分流及 trace 不可见的行为红测，再实施最小 JSX/CSS。
- 2026-07-20：M87 源码、CSS 与相邻测试读取完成。下一步先改 Board 行为测试为 design/visual 两套互斥工具，并要求两种模式均无 Trace 入口/抽屉；运行 focused 取得有效 red 后，才修改最小 JSX/CSS。不会创建或 retry Run。
- 2026-07-20：M87 行为红绿完成，focused 6/6。建筑设计工作台现为 3 项，图纸灵感工作台为 4 项且独有表达规范；两者均无研究过程入口/抽屉，后端/前端诊断事件数据保留。下一步同步 PRODUCT/DESIGN 的模式职责，再跑完整 Board tests、lint、typecheck、build 与前端 detector。
- 2026-07-20：M87 PRODUCT/DESIGN 已同步；Board 96/96、lint/typecheck/build、detector 0、whitespace 均通过。又清理了本次改动造成的 demo Trace 孤儿数据，下一步重跑最终 Board 静态门，再用既有设计与图纸灵感结果做无截图真实 DOM/响应式验收；不创建 Run。
- 2026-07-20：M87 无截图真实 design/visual DOM 与 390×844 响应式验收全绿，交叉工具 0、Trace UI 0、overflow 0、error logs 0；临时项目选择与 viewport 已恢复。下一步执行唯一一次最终 `scripts/verify.ps1`，然后核对 3 projects / 67 Runs / active 0、whitespace/staged 与工作树守卫并标记 implementation_complete。
- 2026-07-20：M87 已达 `implementation_complete`。最终全仓门禁为 323 API / 96 Board / 165 Extension / 8 packaged E2E 全绿，detector 0；当前 3 projects / 67 Runs / active 0，`10d31b4c-...` 保持 attempt 0 封存。staged 0、工作树全部保留，没有 Run、Provider、Firecrawl、截图或 Git 写操作。当前没有已批准自动下一步，等待用户指定产品工作。
- 2026-07-20：M88 已获批准并标记 in_progress。planning-with-files catchup 首次调用系统 `python` 失败（Windows Store alias，无可用解释器）；不会重复该命令，下一步用项目 venv Python 执行同一只读 catchup。之后读取 Impeccable product register 与当前 comparison/export JSX、测试和 CSS，先写删除对照且导出选择仍可用的行为红测。
- 2026-07-20：M88 catchup 已用项目 venv Python 成功读取；报告的一条 unsynced tool context 正是本轮 M88 计划写入，`git diff --stat` 与 task_plan/findings/progress 已复核，无遗漏的代码动作。下一步读取 Impeccable product register 和当前 UI/测试/CSS，随后立即记录源码边界。
- 2026-07-20：M88 已完成 product register 与 comparison/export 全局引用审计。下一步读取需删除的 panel/dock JSX、两类逐图选择按钮与相邻行为测试/CSS，先将测试改为导出板选择语义并要求对照 UI 全局为 0，再运行旧生产取得 red。
- 2026-07-20：用户在实现前修正范围：建筑设计保留对照；M88 只移除 visual compare 入口并阻止 visual 访问 dock/dialog。计划与验收已更新，尚未编辑生产代码。下一步写两种模式红测：design 仍有 3 项且可进入对照，visual 仅 style/private/share 三项且逐图选择只表达导出用途。
- 2026-07-20：M88 JSX/测试/CSS 精确边界读取完成。下一步只编辑 Board 测试：保留 design 3 工具与 demo comparison 合同；visual 期望 3 工具、无任何对照入口，逐图导出选择启用两个导出按钮且不出现 comparison dock/dialog。先运行 focused 获得 red，再改生产。
- 2026-07-20：M88 focused 红测已得到预期 2 failures。下一步做最小生产修改：visual compare tile 不渲染、visual 逐图按钮改导出选择文案、visual 不渲染 comparison dock/dialog、visual desktop grid 改三列；design comparison 路径保持不变。
- 2026-07-20：M88 生产修改已完成，focused 4/4 green。下一步同步 PRODUCT/DESIGN：对照仅属于建筑设计，图纸灵感逐图选择只服务导出；然后运行完整 Board tests、lint、typecheck、build 与 Impeccable detector。
- 2026-07-20：M88 PRODUCT/DESIGN 已同步，完整 Board 96/96、lint/typecheck/build 与 detector 0 全绿。下一步使用既有 M86 visual 与历史 design 结果做无截图 DOM/390px QA，核对两边均三列、visual 对照 UI 0、design 对照仍可达；不提交表单或创建 Run。
- 2026-07-20：M88 Browser 首次认领调用引用了未跨调用保留的 `m88OpenTabs`，得到 ReferenceError；未操作页面或数据。已改为在同一调用重新读取 tabs 并认领，成功 reload 当前 localhost 首页。下一步等待已有项目数据加载后打开 M86 visual 结果并做只读 DOM 验收。
- 2026-07-20：M88 当前被认领页签原选中项目为 `旧工业建筑 · 结构与功能植入`；已临时切到 `旧厂房社区文化中心` 并确认最新 M86 visual 25 张结果可见。验收完成后必须恢复原项目，不创建任务。下一步点击唯一“查看上次结果”并只读核对 visual 三列、导出选择与无对照 UI。
- 2026-07-20：M88 M86 visual desktop QA 通过：三列、三工具、28 个导出选择、对照 UI 0、overflow 0。下一步做 390×844 只读布局检查并 reset viewport；随后切回原 `旧工业建筑 · 结构与功能植入`，打开既有 design 结果验证三列和对照仍可达。
- 2026-07-20：M88 visual mobile QA 通过并已 reset viewport，当前已 reload 回首页等待项目数据。下一步恢复原 `旧工业建筑 · 结构与功能植入` 选择并打开最新既有 design result，做 desktop/390px 三列与对照保留验收。
- 2026-07-20：M88 已恢复验收前的 `旧工业建筑 · 结构与功能植入`，并打开其最新既有 design 结果。快照确认工作台仍为“对照案例策略 / 案例研究板 / 分享证据板”三项；该历史结果当前无正式可选资产，因此对照按钮按合同 disabled，不通过 live board 选择强行启用。下一步只读核对 computed 三列与 390px，无持久化交互。
- 2026-07-20：M88 design desktop/390px QA 通过，项目选择保持验收前的 `旧工业建筑 · 结构与功能植入`，viewport 已 reset。下一步读取 error logs、finalize 被认领页签，再执行最终全仓门禁与 3/67/0、whitespace/staged 守卫。
- 2026-07-20：M88 已达 `implementation_complete`。最终全仓 `scripts/verify.ps1` exit 0：323 API / 96 Board / 165 Extension / 8 packaged E2E 全绿；真实 visual/design DOM 与 390px QA、detector 0、error logs 0。当前 3 projects / 67 Runs / active 0，原项目选择已恢复，未创建/retry Run。当前没有已批准自动下一步，等待用户指定产品工作。
- 2026-07-20：M89 已获批准并标记 in_progress。planning catchup 已用项目 venv 完成；6 条 unsynced context 均为上一轮 M88 已落盘的对话/工具记录，`git diff --stat` 与 M88 planning 尾部复核无遗漏。下一步只读读取 GitHub skill 完整路由，确定 connector/CLI 可用面，再检索并记录 2–4 个相关开源模式。
### 2026-07-20 — M89 GitHub reference scan

- 按计划完成只读 GitHub 检索与 README 阅读：SiYuan、OCA Knowledge、React-Konva Moodboard、Dibyasom Moodboard、Inline Studio；Foam 404、一个设计灵感仓库为空，均未纳入依据。
- 研究结论已落盘到 `findings.md`：结果区应内容优先、工具轻量；建筑设计的对照是判断工具，图纸灵感是视觉素材收集/表达规范/来源分享，不共享同一套功能语义。
- 尚未修改生产代码，未创建或 retry Run，未截图、未 stage/commit/push。
- 下一步：读取当前 Board 结果工具 JSX/CSS/测试，写 M89 行为红测后做最小实现。

### 2026-07-20 — M89 scope clarification

- 用户澄清“不要做太重”针对的是点开两个功能后产出的结果，不是入口工具带本身。
- 已停止入口轻量化实现；新的实现重点是：建筑设计产出可读的策略/证据对照，图纸灵感产出按帖子组织的视觉灵感板与可执行表达规范。入口保持现状，避免扩大范围。
- 已读取 Impeccable product register 与现有 API/Board 产物路径。当前导出 HTML 对两种模式共用一套通用卡片模板，表达规范只保存单色/线宽/字体/布局文字，对照弹层仍以通用表格为主；这些是 M89 的候选改造点。
- 下一步：先写产物级红测（建筑设计导出包含策略/证据维度；图纸灵感导出包含帖子级来源与视觉表达字段且不出现案例策略字段），再做后端渲染与 Board 触发的最小实现。

### 2026-07-20 — M89 artifact implementation

- 先写后端红测，旧实现按预期失败：manifest 没有 artifact kind，两种导出 HTML 都是同一套通用卡片。
- 已完成最小后端实现：`create_export` 按 run goal 写入 `case_research_board` / `visual_inspiration_board`；案例板渲染条件/机制/转译/边界/逐字证据；视觉板按帖子 source URL 分组并在页首渲染表达规范；manifest 同步保存结构化字段。
- 已完成 Board 表达规范增强：新增纹理、版式备注输入，读取与保存保持兼容既有 style profile，测试覆盖保存 payload。
- 通过：API board export 全套 8/8，相关 public-page 41/41；Board 96/96，lint、typecheck、build 全绿；Impeccable detector 0；Ruff format/check 与 git diff --check 通过。
- 尚未运行全仓 `scripts/verify.ps1`，尚未做服务重载或浏览器 QA；没有创建/retry Run、没有截图、没有 Git 写操作。

### 2026-07-21 — M89 verification resume

- 首次全仓 `scripts/verify.ps1` 已完成 326 API、Ruff、48-file format、strict Mypy，并进入根 TypeScript lint/typecheck；会话被环境中断后原进程 ID 不再存在，无法取得最终退出码，因此不能把该次运行记为完整通过。
- 不重复已经有明确输出的 Python 门禁；下一步读取 verify 脚本剩余顺序并逐项运行未确认的 TypeScript/build/E2E/evaluation 门禁，最后用显式退出码收口。

### 2026-07-21 — M89 verification and reload complete

- 环境中断后的剩余门禁已逐项补齐：根 `pnpm run check` exit 0（Board 96/96、Extension 165/165、两端 lint/typecheck/test/build），`apps/extension/test:e2e` 8/8 通过。
- 标准 `scripts/stop.ps1` → `scripts/start.ps1` 一次成功；API/Board health 200，服务已加载 M89 导出渲染与 style profile 字段。
- 只读守卫：3 projects / 67 Runs / active 0；`10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍 completed / attempt 0 / coverage_satisfied；staged 0，`git diff --check` 通过（仅 Windows 换行提示）。
- M89 已可标记 `implementation_complete`；没有创建、retry 或迁移任何 Run，没有截图、Firecrawl 或 Git 写操作。

### 2026-07-21 — M90 started

- 用户真实导出页确认三张图全部 404；浏览器 DOM 与直接 HTTP 请求共同定位为导出路径 `/v1/results` 与实际路由 `/v1/assets` 不一致。
- 已先红绿修复该路径并通过 board-export 8/8；服务尚未重载，旧导出页尚未兼容恢复。
- GitHub 已完成第一轮仓库与代码搜索，下一步读取 Open WebUI、LibreChat、Mem0 的具体历史/记忆实现和官方市场产品的保留规则，再确定 ArchResearch 默认条数/天数；不删除现有 Run。

- GitHub 第二轮实现读取完成：Open WebUI 50/60 条分页且无默认 TTL；LibreChat 对临时聊天提供 1–8760 小时保留；Mem0 使用最近 10 条会话消息做记忆提取，长期记忆默认不过期、可选 expiration date。下一步补齐市面产品官方保留规则，并据此形成 ArchResearch 的默认值与红测。
- 市面产品官方规则已补齐：普通历史/显式记忆默认保留到用户删除，30 天用于明确的临时会话；Slack 也把付费默认永久保留与免费 90 天/1 年限期区分。ArchResearch 因而采用“首屏 10 条、正式历史不自动过期”，不把 UI 窗口误作数据库删除上限。
- 旧导出兼容行为先红后绿：`/v1/results/{id}/content` 先稳定复现 404，随后作为隐藏兼容 route 复用 canonical handler；有效本地图像两条路径均 200，缺失/越界/远程 storage path 仍 404。board-export 8/8 与 content focused 测试全绿。
- 历史整理行为先红后绿：12 条 fixture 在旧页面只显示 3 条；现在 Board 保留 API 返回的完整项目 Run 列表，默认显示 10 条并明确“完整历史不会自动过期”，单一轻量按钮可展开 12 条或收回最近 10 条。没有修改数据库、删除记录或创建 Run。
- M90 服务已标准重载。无截图真实 QA：当前 26-Run 项目默认渲染 10 条，展开精确为 26、收起恢复 10；439px 下 document scrollWidth 等于 viewport。用户原旧导出三图均从 legacy route 200 返回，页面最终自然尺寸为 943×1160、1080×1336、943×1157，Board/export error logs 均为 0。
- M90 最终全仓 `scripts/verify.ps1` exit 0：326 API、97 Board、165 Extension、8 packaged E2E，加 Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿；Impeccable detector 0。
- M90 最终只读守卫：3 projects / 67 Runs / active 0；封存 `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍 completed / attempt 0 / coverage_satisfied；`git diff --check` exit 0、staged 0，全部既有 dirty/untracked 工作树保持。M90 可标记 `implementation_complete`，当前没有已批准下一步。

### 2026-07-21 — M91 started

- 用户指出 M90 首页默认 10 条与页面不匹配，截图中右栏把整行高度拉长并在左栏制造大面积空白；同时要求说明当前记录/记忆架构。
- 已确认当前只有 durable Run history 与 per-Run user state，没有独立 memory store。唯一下一步是先把测试改为首页 3 条 + bounded 全部历史 overlay，再做最小 Board 实现和无截图实测；不修改 67 条持久记录。
- M91 红绿完成：旧生产在 12-Run fixture 上默认渲染 10 条；新生产固定首页 3 条，全部 12 条进入 modal overlay，Escape 关闭并精确返回入口焦点。Board 97/97、lint/typecheck/build 与 Impeccable detector 0 全绿。
- 服务在 3 projects / 67 Runs / active 0 下标准重载。无截图真实 QA：当前 26-Run 项目首页只有 3 行；1053px 下 starter/recent/home-sections 三者高度均为 398.8px、无横向溢出。全部历史 overlay 显示 26 行，高 931.2px 且小于 986px viewport，关闭后仍是 3 行、焦点返回入口；error logs 0。
- M91 最终全仓 `scripts/verify.ps1` exit 0：326 API、97 Board、165 Extension、8 packaged E2E，加 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。当前仍为 3 projects / 67 Runs / active 0，封存 `10d31b4c-...` attempt 0 未 retry。
- 现有 retention 实现复核：ResearchRun/Workspace/ReferenceBoard/SavedReference 无默认 TTL；带 `expires_at` 的临时候选资产通常 7 天，SourcePage/EvidenceClaim 通常 30 天，QueryAttempt/TraceEvent 在启动清理中按 30 天移除。不存在 memory table、memory extractor 或 memory retrieval。

### 2026-07-21 — M92 started

- 用户要求案例研究板要么删除、要么形成与研究界面不同的价值；同时明确不建记忆层，只做历史策略：删除未完成记录、所有 Run 默认 14 天、单条可永久保留。
- 只读状态盘点为 12 completed / 38 blocked / 15 partial / 2 cancelled，active 0；55 条未完成记录待在 retention 合同、文件清理与回归覆盖完成后一次受控删除。唯一下一步是先写 migration/API/export/Board 红测，再做最小实现。
- M92 行为红测有效覆盖四个缺口：无 retention schema/API、旧库会被误 stamp 到 head、cleanup 不删过期 Run/文件、case export 仍为单图长文；Board 无永久保留控制且仍用旧“案例研究板”命名。
- M92 核心实现已转绿：Run 默认 14 天，永久/取消永久 PATCH 可用；migration 给既有记录 14 天 adoption grace，并正确处理无版本 b8 schema；cleanup 删除过期 Run、候选/导出目录和关联收藏但保留 permanent；案例私有导出变成无图片的跨案例问题×项目策略矩阵与去重动作清单；Board 全部历史显示剩余天数与设为/取消永久。
- 相关 29 API tests、Board 97/97、Ruff/format、strict Mypy、Board lint/typecheck/build 全绿。下一步先跑完整全仓门禁，再在服务停止状态通过同一安全 `delete_runs` helper 删除当前 55 条 incomplete，随后迁移/重载与无截图 QA。
- 2026-07-21：M92 首次聚焦复验误调用未进入 PATH 的 `uv`，命令在 Python 测试前停止；随后改用项目既有 `apps/api/.venv/Scripts/python.exe`，API 红测有效得到单项目导出 201 而非预期 422。Board 红测同时证明一项选择仍错误启用矩阵。
- 2026-07-21：双项目 Board fixture 首轮沿用了相同来源且缺少正确绑定的 evidence claim，第二项目未进入可见案例；修正为独立来源与 claim 后转绿。随后导出调用断言错误读取第一条 board PATCH 而非最后一条，改为核对最后一次持久化选择；这两次均为测试形状问题，未改生产合同。
- 2026-07-21：API 与 Board 已统一要求私有策略矩阵至少两个不同项目；聚焦 API 9/9、完整 Board 97/97 通过。服务在 active 0 下标准重载；真实同项目导出返回 422，随后仅创建一个两项目导出 `709e1297-8fe3-4599-8b4b-23d79a8b97bd`，原 board 选择在 finally 中恢复。
- 2026-07-21：无截图真实 DOM 验收确认新导出为 2 项目列 / 6 问题行 / 8 动作 / 0 图片 / 0 旧长文块、overflow 0、Board 与导出 error logs 均 0。一次此前浏览器恢复脚本使用 TypeScript `as` 断言导致解析失败，动作未执行；后续只用普通 JavaScript 成功恢复用户原项目选择。
- 2026-07-21：M92 最终全仓 `scripts/verify.ps1` 明确通过：330 API / 97 Board / 165 Extension / 8 packaged E2E，Ruff/49-file format、strict Mypy、两端 lint/typecheck/test/build、process/security/evaluation 全绿。当前进入最终 3 projects / 12 Runs / active 0、封存 Run、Git whitespace/staged 守卫；不创建或 retry Run。
- 2026-07-21：M92 最终守卫通过：3 projects / 12 Runs / active 0，全部 12 条均 completed 且处于 14 天 adoption grace；`10d31b4c-...` 保持 completed / attempt 0、未 retry。`git diff --check` exit 0（仅既有 LF→CRLF 提示）、staged 0，完整 dirty/untracked 用户工作树保留。M92 标记 `implementation_complete`，当前没有已批准自动下一步。
- 2026-07-21：用户随后报告 Board `ERR_CONNECTION_REFUSED`。只读检查确认 `.archresearch/dev-processes.json` 是陈旧状态且 8000/5173 均无 listener；使用项目 stop/start 脚本清理并恢复服务。最终 API health ok、Board HTTP 200、3 projects / 12 Runs / active 0；未创建或 retry Run。尝试代用户重载现有 in-app tab 被浏览器 URL policy 在动作前拒绝，未绕过；服务端已恢复，用户只需重新加载页面。
- 2026-07-21：M93 已获批准并标记 `in_progress`。planning catchup 的 6 条 unsynced context 均为已落盘的服务恢复与本轮需求开场，`git diff --stat` 未发现额外未解释代码动作。唯一下一步是审计 `SavedReference` 模型/API/cleanup 与当前 Board 选择状态，写收藏聚合、持久保留和底部操作条行为红测；不创建或 retry Run。
- 2026-07-21：M93 数据边界已确认：复用无 asset FK 的 SavedReference snapshot，不新增 memory/entity table；保存时补 run question/goal 与独立本地收藏副本，Run 清理保留收藏，另设 workspace 聚合 GET 和 collection-id DELETE。下一步读取现有 lifecycle/API/Board 测试 fixture 与 dock/home overlay CSS，先写失败行为测试。
- 2026-07-21：M93 首次 focused 红测命令给了过短的 1 秒超时，并误写 lifecycle 测试函数名，pytest 在收集阶段停止，Board 尚未得到有效结果；没有生产或持久数据改动。已查明正确函数为 `test_cleanup_removes_expired_runs_and_files_but_keeps_permanent_runs`，下一步用正常 60 秒边界重跑，不重复错误命令。
- 2026-07-21：M93 有效红测已取得：API save snapshot 缺 `question`，workspace collections/content/delete endpoint 尚不存在；lifecycle 删除过期 Run 时仍删 SavedReference。Board 98 tests 中仅新增两条失败，分别命中主页缺个人收藏入口和 visual 仍显示“导出个人灵感板”且无收藏 dock。下一步先实现独立收藏副本/聚合/删除，再接 Board。
- 2026-07-21：M93 核心红绿完成：SavedReference snapshot 现含 question/goal/展示字段，本地图复制到独立 collections 目录；workspace 聚合、独立内容读取和 collection-id 删除可用，过期 Run 清理不再删除收藏。Board 主页入口按建筑方案/图纸灵感和研究题目两级分组；visual 私有导出入口改为查看收藏，逐图选择改收藏语义，通用底部 dock 提供清空、design 对照与明确添加动作。相关 API 18/18、Board 98/98 green。下一步补 design 收藏行为断言、同步 PRODUCT/DESIGN/architecture 后跑静态与全仓门禁。
- 2026-07-21：M93 首轮静态门中 Board lint/typecheck 与 API strict Mypy 通过；Ruff 仅发现新增 lifecycle 断言 101 字符超过 100 的 E501。下一步只换行该测试断言，再重跑 Ruff/format 和行为套件，不改生产逻辑。
- 2026-07-21：扩大门禁命令再次错误使用 1 秒工具超时，进程在 pytest 输出刷新时被外部终止并产生 `OSError: Invalid argument`；该结果无效，Board 后续命令未完成。不会重复短超时；下一步用 300 秒边界运行同一确定性门禁。
- 2026-07-21：M93 扩大门禁在正常时限下全绿：Ruff/41-file format、331 API、99 Board、Board lint/typecheck/build 均通过，Impeccable detector 0。建筑与图纸两种结果均已有底部收藏行为覆盖。下一步只读确认 3/12/active 0 后标准重载服务，再用真实既有结果做无截图 desktop/390px DOM QA；不新建 Run。
- 2026-07-21：用户在 M93 QA 前修正信息架构：个人收藏必须是独立页面而非弹窗，收藏页和研究结果页都要有“返回主页”。原弹窗实现不再满足验收，未进入最终 QA。唯一下一步改为先写页面导航行为红测：主页入口进入非 dialog 页面、两类页面均可返回主页且主页输入区恢复；再做最小 Board 状态与 CSS 调整。
- 2026-07-21：会话恢复脚本首次被 Windows Store `python` 别名拦截，随后命令发现 `python/python3` 都是无效别名；已通过 Codex workspace dependency runtime 的 Python 成功完成 catchup。catchup 的 14 条未同步内容均为本轮收藏页面修正与恢复动作，`git diff --stat` 未发现额外未解释实现。
- 2026-07-21：用户补充收藏停靠条状态与同题分组语义：提交后只显示“加入成功”，新勾选未收藏项后恢复原操作。最初将同题语义理解为“多项并存”后，用户已纠正为“新批次覆盖旧批次”；一次提交中的多项仍共同保留。下一步 Board 红测改为验证新批全部保存后删除该题目的旧批。
- 2026-07-21：M93 页面/成功态红测有效得到 3 failures：旧收藏只有 `role=dialog`、结果页只有“发起新研究”而无“返回主页”、保存后仍显示已选数量及 disabled 操作按钮。测试 helper 已支持第二 asset 的独立收藏响应；生产代码尚未修改。首次合并测试补丁因 mock 返回块多一个 `created_at` 而被整体拒绝，拆分后成功写入。
- 2026-07-21：首轮页面实现复验中独立收藏页与结果页返回主页已转绿；成功态测试的唯一失败是旧收藏 snapshot 使用默认 `liveQuestion`，而实际提交 Run 是“旧厂房如何植入展厅？”，实现按精确题目正确未删除。已修正 fixture，生产逻辑不因该失败改动。
- 2026-07-21：第二次替换复验仍未出现 DELETE，进一步读取发现旧收藏 fixture 被相邻同形的 `secondCandidate` 锚点插入策略矩阵测试，成功态测试仍只有 candidates。已将 fixture 移到正确用例并恢复相邻测试；这两次均为测试数据/补丁定位错误，不修改生产替换条件。
- 2026-07-21：M93 页面导航、成功态与同题替换聚焦测试 3/3 green；API 收藏内容聚焦测试 1/1 green。收藏已退出 overlay/focus trap，主页/收藏/结果三视图互斥；同题新批先全部保存，再删除旧批，成功后停靠条仅显示“加入成功”，新选未收藏项后恢复操作。
- 2026-07-21：完整 Board 首轮为 95/100；5 个失败均来自旧文案断言，其中“返回结果后新研究恢复默认小红书来源”还要求保留原重置行为。现已区分返回来源：从收藏页返回保留主页草稿，从结果页返回清空上一任务表单并恢复默认来源；四处旧“发起新研究”和一处旧成功文案测试已同步。
- 2026-07-21：完整 Board 100/100、lint/typecheck/build、Impeccable detector 0 全绿；PRODUCT/DESIGN/architecture 已同步独立页面、双返回、成功态和同题整批替换。首次 PowerShell 状态汇总把三个 Run 数组误判为三个 active 对象，已弃用；Node 结构化复核为 3 projects / 12 completed Runs / active 0，可进入标准服务重载。
- 2026-07-21：服务标准 stop/start 成功，API/Board 200、3/12/active 0。真实收藏页 DOM 已证明 `region=个人收藏`、H1、建筑方案/图纸灵感分区和题目分组均加载；发现顶栏与页内重复两个“返回主页”，已删除页内重复动作并把收藏/结果测试收紧为各一个。
- 2026-07-21：最终无截图 DOM QA 通过。桌面收藏页 0 dialog/0 aria-modal、body 未锁滚动、1234px viewport 无溢出；390×844 override 下实际 375px client 与 scrollWidth 相等，收藏页/最宽条目 351px，返回按钮 44px。既有 visual 结果页同样无溢出，唯一返回按钮 44px，底部 dock 左右各 12px，并在 5 项已收藏 + 1 项新选时只显示“已选 1 张图纸”。收藏页和结果页返回后主页输入区均恢复，页面 error logs 0；浏览器控制自身一次 Statsig 注册超时未进入页面日志或影响动作。
- 2026-07-21：M93 最终全仓 `scripts/verify.ps1` exit 0：331 API / 100 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。门禁后再次标准 stop/start，API/Board 200；最终 3 projects / 12 completed Runs / active 0，`10d31b4c-...` completed/attempt 0/coverage_satisfied，`git diff --check` 通过、staged 0。M93 可标记 `implementation_complete`；未创建或 retry Run、未截图、未执行 Git 写操作。

### 2026-07-22 — Session recovery

- 按 `HANDOFF.md` 完整恢复并核对所有 `in_progress` / `implementation_complete` 阶段；当前无 `in_progress`，M77、M78、M93 均为 `implementation_complete`。最新基线仍为 3 projects / 12 completed Runs / active 0；`10d31b4c-94dd-4442-b24a-fc1b241e658e` 永久封存且绝不 retry。
- `planning-with-files` catch-up 首先命中无效 Windows Store `python` 别名，`py` launcher 也不存在；随后使用 Codex bundled Python 成功完成，未报告未同步上下文。`git status --short --branch` 确认仍在 `codex/archresearch-v2-1`，既有 dirty/untracked 工作树完整保留且无 staged 项。
- 当前没有已批准的自动下一步，等待用户指定产品工作。本次未恢复 Firecrawl、未创建或 retry Run、未截图，也未执行 reset、checkout、clean、stage、commit 或 push。
- 用户报告 Board `ERR_CONNECTION_REFUSED`。只读检查确认 5173/8000 均无 listener，`.archresearch/dev-processes.json` 只是 2026-07-20 的陈旧 PID 记录；通过项目 `stop.ps1` / `start.ps1` 清理并标准恢复服务。最终 API `/health` 与 Board 均为 200，结构化复核仍为 3 projects / 12 completed Runs / active 0；未创建或 retry Run，也未改动产品数据。
- 复核 Pinterest 官方开发者规则：未经 Pinterest 明确许可不得用自动化 scraping/data extraction 访问其信息；官方 API 需要开发者应用、审批和 access token，且存储/复用仍受严格限制。现阶段继续只把 Pinterest 作为普通搜索结果中的原站链接线索，不建设直抓或本地素材采集链。
- 用户批准 M94：从当前产品中删除 Pinterest，只保留 XHS。已完成生产/测试/文档引用初扫并标记 `in_progress`；确认没有 Pinterest 直抓客户端，删除面集中在来源契约、Board 复选框/标签、workflow 提示与相关测试/文档。下一步先写行为红测，再改生产代码；历史记录和持久 Run 不迁移。
- M94 代码边界复核确认需要同时拒绝通用 Provider 意外返回的 Pinterest/`pin.it` URL，否则删除旧视觉平台识别会把它们误当普通来源。行为合同调整为：API schema 只接受 XHS、Board 只展示 XHS、新 Provider 结果 fail-closed 丢弃 Pinterest，历史数据库不清理。
- M94 红测有效：旧生产分别暴露双值枚举、接受 Pinterest POST、持久化 Pinterest Provider 结果并渲染 Board 复选框。最小生产删除后 schema/API/Board/client 已转绿，Provider 的 `asset/page is None` 也通过；首次复验仅因测试下方残留已删除资产的旧字段断言报 `AttributeError`，已清理该测试形状，不改生产逻辑。
- M94 聚焦回归现为 API 3/3、Board 1/1 与 client 9/9 全绿。当前产品复扫只剩 denylist 域名和负行为测试，UI/schema/TS/prompt/当前 docs 均无 Pinterest 能力。下一步运行 Ruff/Mypy、Board lint/typecheck/build 与较宽测试，再进入完整仓库门禁。
- M94 较宽门禁通过：API 相关三文件 72/72，Ruff lint/format 与 strict Mypy 18 source files 全绿；Board 100/100、ESLint、TypeScript 和 production build 全绿。改动保持在 schema/workflow、Board/client、四个行为测试和两份当前文档；下一步运行唯一一次完整 `scripts/verify.ps1`。
- M94 最终完整 `scripts/verify.ps1` exit 0：331 API / 100 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。active 0 后标准 stop/start；loaded API/Board 均 200，OpenAPI 来源枚举仅 `xiaohongshu`，Board 模块无 Pinterest 文案，最终仍为 3 projects / 12 completed Runs / active 0。M94 标记 `implementation_complete`；未创建或 retry Run、未截图、未执行 Git 写操作。
- 用户批准 M95 并指出启动设置过重、案例来源看起来只有 ArchDaily。已按 Impeccable `distill` 恢复目标：建筑设计只保留研究深度为主决策，补充资料按需，readiness 收成一行；XHS 从建筑设置移除并仅在图纸灵感中自动启用。下一步只读审计最近 Run 与可信站点路由，再先写行为红测。
- M95 首轮来源审计：最近 Quick/Balanced 的保留结果确为 ArchDaily-only，但最新 Deep 同时含 Designboom；源码公共搜索按轮循环 ArchDaily/ArchDaily China/Designboom/Dezeen/Divisare。下一步查询持久 SourcePage/QueryAttempt 主机分布，确认问题发生在调度、抓取还是证据准入，再决定是否改后端。
- M95 SQLite 审计确认 ArchDaily-only 是调度缺陷：Quick/Balanced 在 round 1 内完成，所有 3/4 次查询都被同一 round-based selector 固定到 ArchDaily；Deep 只有进入后续 round 才触及 Designboom/Dezeen。下一步先写同轮查询轮换的后端红测，以及精简启动器/模式派生 XHS 的 Board 红测。
- M95 红测有效：同轮三次公共搜索实际为 ArchDaily/ArchDaily/ArchDaily；Board 首页没有外置“研究方式”；建筑设计 POST 仍携带 `xiaohongshu`。下一步只修改域名 selector 与启动器 JSX/CSS/source derivation，使这三项转绿。
- M95 核心红绿转绿：同轮前三次站点现为 ArchDaily/Designboom/Dezeen，首页外置紧凑“研究方式”，建筑 POST `research_sources=[]`；补充区只剩 URL/文件且 XHS 从建筑设置移除。下一步迁移受影响的旧 Board 断言、同步产品文档，再跑完整相关门禁。
- M95 首次完整 Board 为 80/82；仅两个 Chrome open/auto-pair 用例仍从建筑首页寻找图纸灵感环境入口。已把测试前置状态迁到图纸灵感，连接能力不变；PRODUCT/DESIGN/README/architecture 已同步紧凑启动器、模式内置 XHS 与同轮多站轮换。
- M95 扩大门禁：Board 82/82、lint/typecheck/build 与 API static 全绿；API 行为 144/146。两处失败是旧 round-based 域名顺序和补查列表顺序断言。已把普通查询轮换拆成独立常量，补查可信域名顺序恢复不变，并只更新恢复测试的新 query-index 序列。
- M95 首次全局 query-index 轮换的三项聚焦复验为 2/3；唯一失败证明某个恢复分支不再获得 ArchDaily China 中文查询。已改为 round offset + 本轮公共查询序号的二维轮换，并同步恢复测试：首轮跨站、同分支跨轮、站点语言三项同时保留。
- M95 二维轮换扩大复验全绿：API 行为 150/150，Board 100/100，Ruff/format、strict Mypy、Board lint/typecheck/build 与 Impeccable detector 0 均通过。无截图真实 DOM 同时确认建筑模式只有研究方式与 URL/文件补充资料，图纸灵感独占小红书研究环境；390px 和桌面宽度均无横向溢出。
- M95 最终完整 `scripts/verify.ps1` exit 0：331 API / 100 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。active 0 后完成一次标准 stop/start，API/Board 均 200；最终仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-94dd-4442-b24a-fc1b241e658e` completed/attempt 0 且未 retry。M95 标记 `implementation_complete`；未创建 Live Run、未截图、未恢复 Firecrawl、未执行 Git 写操作。
- 用户批准 M96：个人收藏先显示建筑方案 / 图纸灵感两个主页式入口，不再同页混排；建筑模式改为项目与研究文字视图，图纸灵感改为图片视图。planning catch-up 的 8 条未同步上下文均是 M95 完成、M96 用户需求与本轮开场；`git diff --stat` 未发现额外实现。下一步读取收藏渲染、fixture 与样式，先写模式入口和内容分流行为红测。
- M96 已加载 Board PRODUCT/DESIGN、Impeccable product register 与 distill 规则。界面方向固定为渐进披露而非同页双分区：分类页只选模式，建筑页保留研究文字，图纸页只保留图片并把管理动作降级；不新增模态、导出或数据模型。
- M96 源码定位完成：收藏由 `collectionOpen`、`collectionSections` 和一套统一 item CSS 组成，API/收藏快照已足够支持分流；现有单测仍断言两类同页展开。下一步读取准确 JSX/fixture/CSS 范围并先把该测试改成入口→模式页、建筑无图、灵感无文案的失败合同。
- M96 准确渲染范围已确认：收藏 JSX 位于单一 `collection-page`，页面顶栏已有唯一主页返回；旧样式固定 88×64 缩略图 + 文案 + 删除。新行为测试将覆盖分类层不泄漏内容、两种模式的返回路径，以及建筑/图纸各自的内容删减。
- M96 数据与组件边界已确认：现有 snapshot 足够，主页 `research-entry-switch` 和 Lucide 图标可沿用产品语汇。实现将只增加收藏子视图状态、模式化 JSX/CSS 与行为测试，不触碰 API、持久化或收藏替换合同。
- M96 行为红测有效：旧页面找不到 `group "收藏类型"`，并在失败 DOM 中确认仍同时渲染两种题目、两个缩略图和两套文案。测试现固定分类页内容不泄漏、建筑页无图片、图纸页无题目/观察文字、两级返回与来源/删除动作；下一步做最小状态、JSX 与 CSS 实现。
- M96 核心红绿完成：新增 `index / precedent / visual` 收藏子视图；分类页复用主页双入口语汇，建筑页移除图片与类型标签并保留问题/项目/研究文字，图纸页移除全部题目/标题/观察文案并以原比例图片网格呈现，来源/删除为图面角落图标。聚焦行为测试 1/1 green，API 与持久化未改。
- M96 Board 扩大门禁全绿：100/100 tests、ESLint、TypeScript、production build 均通过，Impeccable detector 为 0。下一步用当前真实收藏做无截图 desktop/390px DOM QA，重点核对分类层无内容泄漏、建筑 0 图片、图纸仅图片与低干扰管理动作。
- M96 loaded DOM 第一门通过：现有真实 2 个建筑收藏 / 1 个图纸收藏在分类页仅呈现为两个主页式入口，内容未预展开，唯一主页返回动作保持。下一步分别进入两种模式核对内容删减和响应式几何。
- M96 loaded desktop 建筑门通过：1440px 分类页 scrollWidth 等于 viewport，建筑页展示真实题目、2 个项目与研究机制、0 图片，并保留来源/删除和唯一“返回收藏分类”。下一步检查图纸图片-only DOM 与两种模式的 390px 触控/溢出。
- M96 loaded desktop 图纸门通过：真实页为 1 image / 1 source / 1 delete，0 二级题目、0 copy block、0 研究问题或观察文字，且无溢出。下一步仅剩 390px 触控/几何、页面错误日志与文档/最终门禁。
- M96 390px DOM QA 全绿：双入口 176×69px；图纸图片 365px 且来源/删除 44×44px；建筑 0 图片、所有来源/删除高 44px；三种收藏视图均无横向溢出或元素越界。下一步查页面错误日志、恢复分类默认视图与 viewport，再同步产品文档和最终门禁。
- M96 浏览器收口通过：页面 error logs 为 0，viewport 已 reset，真实页恢复到无内容预展开的收藏分类并作为 deliverable 保留。整个 QA 未截图、未打开外部来源、未删除收藏或改动产品数据。
- M96 产品文档已同步：PRODUCT/DESIGN 固定分类页双入口、建筑文字-only、图纸图片-only 与两级返回；architecture 明确两种界面仍读取同一 SavedReference 聚合端点，不新增模型或迁移。下一步执行本阶段唯一一次完整 `scripts/verify.ps1`，通过后只读确认 active 0 并标准重载服务。
- 用户在完整门禁运行中修正 M96 交互：收藏应默认建筑并常驻两边切换，不要空白分类层。正在运行但尚未结束的 `scripts/verify.ps1` 已主动终止，其结果无效且不计作最终门禁。下一步先改行为测试为默认建筑 + 原位切换红测，再移除 index 状态/返回层并同步文档与真实 QA。
- M96 修正后红测有效：旧实现的建筑入口没有默认 `aria-pressed=true`，证明仍是一次性分类按钮而非常驻切换器。新测试同时固定默认建筑内容立即可见、切到图纸后控件仍在、可原位切回建筑且不存在“返回收藏分类”。
- M96 修正后核心红绿转绿：收藏子视图收敛为 `precedent / visual`，每次打开固定 `precedent`；双入口常驻并用 `aria-pressed` 表示当前模式，下方内容原位切换，index/返回分类层及其 CSS 已删除。聚焦测试 1/1 green。
- M96 修正后 Board 扩大门禁再次全绿：100/100 tests、ESLint、TypeScript、production build 与 Impeccable detector 0。PRODUCT/DESIGN/architecture 已同步为默认建筑 + 常驻双向切换。下一步复验 loaded desktop/390px 默认态与原位切换，再运行唯一有效的完整仓库门禁。
- M96 修正后 loaded 默认态通过：收藏打开即显示真实建筑题目、2 个项目和研究机制；建筑 switch pressed、图纸 switch 常驻未选；0 图片、0 返回分类层、唯一主页返回保持。下一步核对原位切换和 390px 几何。
- M96 修正后真实 desktop/390px QA 全绿：常驻切换器原位往返，图纸态为 1 image / 0 copy / 0 question，建筑态恢复 0 图片与 pressed；桌面无溢出，390px switch 为 176×54px、图纸操作 44×44px、无元素越界。下一步查错误日志、reset viewport、保留默认建筑页，然后运行唯一有效完整门禁。
- M96 修正后浏览器收口完成：页面 error logs 0，viewport reset，默认建筑 pressed / 0 图片 / switch 常驻的收藏页已作为 deliverable 保留。未截图、未点击来源/删除、未改动收藏数据。
- M96 唯一有效完整 `scripts/verify.ps1` exit 0：331 API / 100 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。下一步只读确认 3 projects / 12 completed Runs / active 0 后标准 stop/start，再做 loaded/Git/计划收口。
- M96 已达 `implementation_complete`。active 0 下标准 stop/start 成功，Board 进程晚于 App 修改且 API/Board 200；最终 3 projects / 12 completed Runs / active 0，`10d31b4c-94dd-4442-b24a-fc1b241e658e` completed/attempt 0、未 retry。`git diff --check` exit 0、staged 0；未创建 Live Run、未截图、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。当前没有已批准的自动下一步，等待用户指定产品工作。
- 用户批准 M97：图纸收藏的图片本身应可直接打开高清图，本地收藏副本优先，右上来源仍指向原帖。planning catch-up 的 6 条未同步上下文均为 M96 完成、M97 需求与本轮开场；`git diff --stat` 未发现额外实现。下一步读取当前视觉 item 测试/JSX/CSS，先写高清链接红测。
- M97 源码边界确认：现有 `previewUrl` 已提供正确高清目标，图片仍是非交互元素，来源/删除是独立 sibling。下一步只在 M96 行为用例中新增高清链接 href/target 断言，先取得旧实现缺链接的红测。
- M97 行为红测有效：旧 DOM 有图片、原帖来源和删除，但找不到 `link "打开高清图片：轴测图参考"`。测试已固定本地 collection content href、`target=_blank` 与 `rel=noreferrer`；下一步只包裹图片并补焦点/指针样式。
- M97 核心红绿完成：图面现为独立 `collection-visual-open` 链接，使用现有 `previewUrl` 打开本地高清副本并保留原图回退；原帖来源/删除 sibling 未改。链接支持 `zoom-in` 指针与可见键盘焦点，聚焦测试 1/1 green。
- M97 Board 扩大门禁全绿：100/100 tests、ESLint、TypeScript、production build 与 Impeccable detector 0。下一步在 loaded 真实收藏中核对高清链接 href/target、与原帖来源 URL 分离、390px 无溢出，并直接请求本地 content 验证图片响应。
- M97 loaded DOM 首门通过：真实图纸收藏的图面已成为 `打开高清图片` 链接并指向本地 collection content，原帖来源仍单独指向 XHS，删除按钮独立。下一步读取链接 target/rel、键盘焦点/390px 几何，并请求本地 URL 验证 200 图片响应。
- M97 loaded 验收通过：高清链接 target/rel/title 正确、zoom-in 指针生效，desktop/390px 链接完整覆盖图面且无溢出，页面 error logs 0；真实本地 href 返回 200 image/png / 1,547,533 bytes。下一步保留当前图纸收藏作为 deliverable，同步产品文档后运行完整门禁。
- M97 首次 browser finalize 使用了已被释放的旧 tab binding，cleanup 在动作前失败，页面和数据未改变。按 Browser 恢复规则不重复该 binding；下一步重新认领当前 localhost 页签后只 finalize 一次。
- M97 按恢复规则重新认领当前 localhost 页签并成功 finalize，图纸收藏高清入口页已作为 deliverable 保留；此后不再执行浏览器动作。
- M97 完整 `scripts/verify.ps1` 已 exit 0：331 API / 100 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。完整门禁之后尚未执行本阶段的 active-0 标准重载与最终 loaded/Git 守卫。
- M97 续接使用 `planning-with-files` 恢复出 23 条未同步上下文，均与上述门禁、交接摘要及本轮开场有关；系统 `python` Store 别名失败后改用 Codex bundled Python 成功完成 catch-up。既有 dirty/untracked 工作树保持不变，下一步只做只读 Run 基线复核、active 0 下 stop/start、loaded/Git 守卫和计划收口。
- M97 重载前只读状态守卫通过：API/Board 200，3 projects / 12 completed Runs / active 0；`10d31b4c-94dd-4442-b24a-fc1b241e658e` 保持 completed / attempt 0 / coverage_satisfied。可以进入本阶段唯一一次标准 stop/start，未创建或 retry Run。
- M97 已达 `implementation_complete`。active 0 下唯一一次标准 stop/start 成功；重载后 API/Board 200，Board 进程晚于 `App.tsx` 修改时间，状态仍为 3 projects / 12 completed Runs / active 0，封存 Run 保持 completed / attempt 0 / coverage_satisfied。`git diff --check` exit 0、staged 0；未截图、未创建或 retry Run、未恢复 Firecrawl、未执行任何禁止的 Git 写操作。
- 用户批准 M98：建筑收藏应按案例所回答的研究子问题组织，并展示相关研究内容，当前“项目名 + 单段机制”过于简陋。下一步只读审计 SavedReference snapshot、收藏 API 与结果页 dossier 字段，再先写行为红测；图纸灵感收藏不改。
- M98 数据边界审计完成：真实收藏 snapshot 有丰富的项目级机制/转译/边界，但缺 `subquestion_ids / subquestion_analysis / project_context / evidence_claims`；原始 AssetCandidate 与 Run 已具备全部逐题数据，两个现有建筑收藏也仍可关联回来。下一步确定最小耐久 snapshot 子结构，并分别写 API 保存/旧收藏投影与 Board 逐题 dossier 红测。
- M98 结构已定：snapshot 新增自包含 `case_subquestions`，每项保存子问题题目、逐题条件/机制/转译/边界和一条可追溯原文；旧收藏 GET 在原资产仍存在时只读兼容投影。Board 以研究题目 → 案例子问题 → 项目展开，默认四行摘要并按需展开剩余研究内容。下一步写 API 与 Board 红测，不先改生产代码。
- M98 红测有效：API 聚焦用例在 `snapshot["case_subquestions"]` 处明确 `KeyError`；Board 聚焦用例找不到案例子问题标题，失败 DOM 仍是旧的“研究题目 → 项目名 + 一段机制”。两端均证明旧行为，现可开始最小后端 snapshot/legacy projection 与前端逐题台账实现。
- M98 核心红绿转绿：API 现在用同一纯投影写入新收藏并为仍有原资产的旧 snapshot 补出 `case_subquestions`，聚焦测试 1/1；Board 按研究题目 → 案例子问题 → 项目渲染条件、机制、两步转译、首要边界与可展开的剩余内容/逐字证据，聚焦测试 1/1，图纸高清链接合同同用例保持。下一步显式化 Pydantic snapshot schema，并跑两端静态与扩大测试。
- M98 扩大门禁全绿：Pydantic/OpenAPI snapshot 与 TypeScript contract 已显式对齐；Ruff lint/format、strict Mypy 18 source files、API 收藏/schema/生命周期 32/32、Board 100/100、ESLint、TypeScript 与 production build 全部通过。下一步运行 Impeccable detector、审查本阶段 diff，然后 active 0 下重载并做真实无截图 DOM 验收。
- M98 Impeccable detector 为 0。实现审查进一步把逐字证据改为逐题条件/机制 statement 精确匹配、无匹配即省略，并把窄屏证据来源触控目标补到 44px；收紧后 API 聚焦、Ruff、format 与 strict Mypy 再次全绿。下一步复核 active 0 并标准重载真实服务。
- M98 active 0 下标准重载成功；loaded API/Board 200、OpenAPI 含逐题 snapshot schema、现有 2 个建筑收藏投影为 3 个子问题 / 4 个案例关联，3 projects / 12 completed Runs / active 0 与封存 Run 未变。无截图桌面 DOM、展开证据及 390px 几何均通过，唯一发现是移动端“查看完整相关内容”summary 实测 40px；下一步先写 44px CSS 红测再修复。
- M98 移动 summary 44px 行为测试红绿完成，真实 390px summary/来源/删除均 44px、无溢出；图纸图片-only 与 M97 高清/原帖双链接回归通过。最终真实建筑页为 3 个子问题 / 4 个案例、details 全关、0 图片、error logs 0，并作为 deliverable 保留；viewport reset 已调用，立即同调用读取仍回报旧 390px，因桌面/移动几何已分别通过而不重新认领页面。下一步同步产品文档并运行最终完整门禁。
- M98 legacy durability 红绿完成：旧 snapshot 首次读取时把可验证逐题包只增不删地写回 SavedReference，新收藏保存时直接自包含；不建表、不改 Run。PRODUCT/DESIGN/architecture 已同步逐题层级、平面台账、精确证据、44px 与懒升级边界。下一步运行本阶段唯一一次完整 `scripts/verify.ps1`。
- M98 第一次完整门禁被 shell 60 秒外层 timeout 终止，未输出具体测试失败，结果无效。下一步使用更长外层时限重跑一次并以明确 exit code 为准；不重复短时限调用。
- M98 权威完整 `scripts/verify.ps1` 重跑 exit 0：331 API / 101 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。下一步只读确认 active 0，标准重载最终代码并触发旧收藏逐题包持久升级，再做 loaded/Git/计划收口。
- 2026-07-23：M98 已达 `implementation_complete`。active 0 下最终标准重载成功，API/Board 200 且进程均晚于对应源码；当前 3 projects / 12 completed Runs / active 0，`10d31b4c-94dd-4442-b24a-fc1b241e658e` 保持 completed / attempt 0 / coverage_satisfied。两个旧建筑收藏的逐题包已通过 workspace GET 一次性升级，并由 SQLite 只读核对为 1+3 条真实持久数据。最终 Impeccable detector 0、`git diff --check` exit 0、staged 0；未创建或 retry Run、未截图、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。当前没有已批准的自动下一步，等待用户指定产品工作。

## 2026-07-23 local service availability recovery

- 已按用户截图确认新故障边界：localhost 返回 `ERR_CONNECTION_REFUSED`，M98 产品实现与完整门禁不重做。
- planning-with-files catch-up 首次命中无效 Windows Store `python` alias，随后改用项目 venv Python 成功恢复 11 条未同步上下文；既有 dirty/untracked 工作树已通过 `git status --short --branch` 与 `git diff --stat` 只读确认并完整保留。
- 当前步骤：读取 5173/8000 监听器与进程记录，并在任何重启前从 SQLite 只读证明 active 0 与封存 Run不变。
- 重启前守卫已通过：5173/8000 无监听，进程记录中的 API/Board pid 均已退出；SQLite 只读确认 3 projects / 12 completed Runs / active 0，封存 Run 保持 completed / attempt 0 / coverage_satisfied。
- 下一步：执行本次恢复唯一一次标准 `scripts/stop.ps1` 后 `scripts/start.ps1`，再用 HTTP 与只读状态复核验收；不做浏览器截图或产品修改。
- 标准恢复已执行一次：`scripts/stop.ps1` 正常识别无匹配监听并清理过期状态，`scripts/start.ps1` exit 0。未创建或 retry Run，未触碰 Firecrawl、收藏数据或产品源码。
- 当前步骤：复核 API/Board HTTP、端口与新进程时间，并再次只读确认 3 / 12 / active 0 和封存 Run 状态。
- 重载验收通过：API 200、Board 200；5173/8000 均由新进程监听，API pid 45428 与 Board pid 45640 的启动时间晚于对应最新源码。持久状态仍为 3 projects / 12 completed Runs / active 0，封存 Run 仍为 completed / attempt 0 / coverage_satisfied。
- 当前只剩 `git diff --check`、staged 0 与最终工作树只读守卫，然后把本次运维恢复标为 complete；不重跑 M98 产品门禁。
- 本次本地服务恢复完成：最终 API/Board 仍为 200，`git diff --check` exit 0、staged 0，105 个既有 dirty/untracked 条目全部保留。task plan 已标记 complete，HANDOFF 已写入恢复后的 verified baseline；当前仍没有已批准的自动产品下一步。

## M99 architecture-collection hierarchy

- 用户批准新的唯一产品工作：强化建筑收藏“原题目 → 案例子问题 → 项目 → 分析字段”的分层感，现有研究数据、图纸收藏和高清入口均不改。
- planning-with-files catch-up 已恢复 10 条未同步上下文；`git diff --stat` 仅反映既有大工作树。Impeccable 已加载 Board PRODUCT/DESIGN，确认应使用平面结构线、留白和固定字阶，禁止嵌套卡片或彩色侧条。
- 下一步：完整读取 Impeccable layout/product register，随后审计当前收藏 JSX、样式和行为测试，先写语义层级红测。
- Impeccable product/layout register 与当前 App/CSS/测试已完整审计。现有 h2/h3/h4/h5 语义顺序正确，但视觉分组没有对应的命名 region 和可见层级标签；M99 将补齐这些结构，并用一张外层工作纸、整行子问题头、项目纸头和明确字段标签轨形成四级节奏。
- 下一步：只修改现有收藏行为用例，要求“原研究题目”与“项目案例”标签以及 question/subquestion region，先确认旧实现红测；生产 JSX/CSS 暂不改。
- M99 聚焦行为测试已取得预期红色：1 failed / 81 skipped，唯一失败是无法找到命名的原研究题目 region；现有所有内容仍在失败 DOM 中，证明不是数据问题。
- 下一步：最小修改收藏 JSX 增加 question/subquestion region 与两级可见标签，并在现有 CSS token 上重排工作纸、章节头、项目纸头和 112px 字段轨；不改任何数据与交互。
- M99 核心实现完成并通过同一聚焦用例：1/1 green。`App.tsx` 只新增层级 region、两级标签与字段内容 wrapper；`styles.css` 只重排建筑收藏工作纸/章节头/项目纸头/台账，图纸灵感、高清链接、来源、删除和 disclosure 行为保持。
- 下一步：检查本阶段 diff 与现有 token，运行完整 Board、ESLint、TypeScript、production build 和 Impeccable detector；若静态门发现任意偏差，只修本阶段行。
- M99 扩大门禁全绿：101 Board tests、ESLint、TypeScript、production build、Impeccable detector 0。字体全部收敛到现有 24/20/16/14/12 token 阶梯，没有引入临时字阶。
- 真实无截图 QA 已通过桌面与 390px：1 原题 / 3 子题 / 4 项目层次可访问且几何分离，桌面与窄屏均无横向溢出；移动来源/删除/summary 为 44px。图纸 image-only、本地高清入口与原帖入口回归无变化，最终页面已切回建筑方案。
- 下一步：同步 PRODUCT/DESIGN/architecture 的层级表达，运行本阶段完整仓库门禁；随后只读确认 active 0，标准重载并做最终 HTTP/DOM/Git 守卫。
- PRODUCT、DESIGN 与 architecture 已同步 M99 的唯一外层工作纸、24/20/16px 题目层级、浅色子问题章节头、平面项目纸头、112px 字段轨与移动单列规则；数据模型、API 和图纸收藏合同没有变化。
- 下一步：运行本阶段权威完整 `scripts/verify.ps1`，以明确 exit 0 为准；不在门禁期间创建或 retry Run。
- M99 首次完整门禁被 120 秒 shell 外层 timeout 终止，未输出具体测试失败，结果无效。下一步使用长外层时限重跑同一权威脚本一次，并按短间隔轮询直至取得明确 exit code；不重复短时限调用。
- M99 权威完整 `scripts/verify.ps1` 长时限重跑 exit 0：331 API / 101 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。
- 下一步：只读确认 3 projects / 12 completed Runs / active 0 与封存 Run 状态，再执行本阶段唯一一次标准 stop/start；随后复核新进程、HTTP、最终 DOM、Git 守卫并收口。
- M99 active 0 下唯一一次标准 stop/start 已完成；重载后 API/Board 200，新 API/Board 进程均晚于对应源码，状态仍为 3 projects / 12 completed Runs / active 0，封存 Run保持 completed / attempt 0 / coverage_satisfied。`git diff --check` exit 0、staged 0。
- 最终浏览器 reload 前发现续接后的旧控制变量已释放，动作前失败且未改页面。下一步重新连接同一个 localhost 页签，只执行一次最终刷新、DOM/日志/viewport reset 与 finalize。
- M99 最终 loaded 复核通过：1280×720 为 1 原题 / 3 子题 / 4 项目，24/20/16/12/14px 层级、0 图片、0 默认展开、无横向溢出、error logs 0；页面保持默认建筑方案。结合此前 390px 与图纸态回归，本阶段达到 `implementation_complete`。
- 最终步骤：写入 HANDOFF，运行规划文件更新后的 `git diff --check` 与 staged 0 守卫，再将当前收藏页作为 deliverable finalize；当前没有已批准的自动下一步。
- M99 最终 Git/服务守卫通过：分支仍为 `codex/archresearch-v2-1`，`git diff --check` exit 0、staged 0，105 个既有 dirty/untracked 条目完整保留，API/Board 仍为 200。HANDOFF 已更新；当前收藏页可作为 deliverable finalize。

## M100 continuous case collection with project images

- 用户批准新的唯一产品工作：去掉 M99 的块状工作纸/字段格与“查看完整相关内容”，把全部研究内容连续展示，并为建筑案例恢复少量项目图片。
- planning-with-files catch-up 已恢复 8 条未同步上下文；`git diff --stat` 仅反映既有大工作树。Impeccable layout/product register 已读取，方向固定为连续文档流、留白与细分隔线，禁止嵌套卡片和新装饰容器。
- 下一步：只读审计两个真实建筑收藏的 snapshot、本地 collection 文件和同项目正式资产，确定最多三图的最小耐久数据边界；随后先写 API/Board 红测。
- 图片数据审计完成：De Nederlandsche Bank 有 7 张同 Run/同项目正式图片，Instituto 有 1 张；现有建筑 snapshot 各只含单一 `image_url` 且无本地 `collection_file/storage_path`。M100 需要新增 typed `case_images` snapshot 列表，并为旧收藏独立懒升级，最多三张且优先实际收藏资产与类型多样性。
- 下一步：在现有 API 收藏持久性用例中造出同项目三图并要求 save/legacy read 都返回 `case_images`；在 Board 收藏用例中要求三张项目图、无 details、全部转译/边界/原文默认可见。先取得两端红测，不先改生产代码。
- M100 API/Board 聚焦红测均按预期失败：API 缺 `case_images`；Board 的第三条转译仍被关闭的 details 隐藏。测试已同时固定三图 URL/类型/本地首图回退、无 summary/details、全部转译/边界/逐字原文默认可见。
- 下一步：新增 typed `case_images` 投影、保存和旧收藏独立懒升级；Board 读取最多三图并把所有逐题内容合并为连续 story，随后先重跑同一两端红测。
- M100 两端核心红绿完成：API 聚焦收藏持久性 1/1 green，Board 聚焦收藏行为 1/1 green。新增 typed `case_images`，旧收藏独立懒升级；建筑案例现在直接显示最多三图与全部逐题内容，无“查看完整相关内容”。
- 下一步：运行 Board 全套、API 收藏/schema 邻接、Ruff/format/Mypy、ESLint/TypeScript/build 与 Impeccable detector；若门禁发现问题，只修 M100 触及行。
- M100 扩大门禁全绿：101/101 Board、27/27 API collection/schema、Ruff、format、strict Mypy、ESLint、TypeScript、production build、detector 0。唯一测试编辑失误是响应式合同局部变量漏改，已在运行前修正并记录，生产代码未受影响。
- 下一步：只读确认 active 0 后标准重载 API/Board，GET 当前 workspace 收藏触发 `case_images` 一次性升级并直接核对 SQLite；随后使用真实页面无截图检查连续布局、多图与 390px。
- M100 loaded 数据升级与无截图 QA 完成：真实快照已持久保存 De Nederlandsche Bank 3 图、Instituto 1 图；桌面 6 个图片实例全部加载，0 disclosure、0 块状外框/底色、0 横向溢出、0 error logs，所有转译/边界/逐字证据默认可见。
- 390px 下图片轨与研究正文折为单列，6 图加载、来源/删除/证据来源均 44px且无溢出。图纸灵感仍精确为 1 张本地高清图、0 建筑内容，切回建筑后 viewport 已 reset。
- PRODUCT、DESIGN 与 architecture 已同步连续案例档案、最多三张 typed 同项目正式图片、完整研究内容默认展开及图片不承担机制证据的边界。下一步运行 M100 权威完整 `scripts/verify.ps1`；通过后只做 active-0 最终重载、loaded/Git/HANDOFF 收口。
- M100 权威完整 `scripts/verify.ps1` exit 0：331 API / 101 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。
- 重载前一次 PowerShell `Invoke-RestMethod` 数组包装再次误计为 1/1/active 1，已立即判为无效且未据此执行写操作；Node 结构化聚合正确确认 3 projects / 12 completed Runs / active 0，封存 Run completed / attempt 0 / coverage_satisfied。随后只执行一次标准 stop/start。
- M100 已达 `implementation_complete`。重载后 API/Board 200且进程晚于对应源码，建筑收藏快照保持 3+1 张 typed 案例图；最终页面为 1 原题 / 3 子题 / 4 案例 / 6 图片实例、0 disclosure、0 overflow、0 error logs，默认建筑方案。`git diff --check` exit 0、staged 0；未截图、未创建或 retry Run、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。当前没有已批准的自动下一步，等待用户指定产品工作。

## M101 solution-first architecture collections

- 用户批准新的唯一产品工作：M100 实际页面仍不好看，建筑收藏只需清楚展示案例如何解决当前问题。planning-with-files catch-up 恢复出 8 条 M100 收口与 M101 开场上下文；`git diff --stat` 只反映完整既有工作树。
- Impeccable distill/product register 已读取。方向固定为项目图片上下文 + 一句核心解法 + 可执行步骤，移除收藏表面的项目条件、完整边界与逐字证据；底层 snapshot、来源/删除、题目层级与图纸灵感不改。
- 下一步：审计当前收藏行为用例和 M100 JSX/CSS，先写行为红测要求解决方案聚焦且审计字段不渲染，再做最小生产修改。
- M101 结构审计完成：当前 340px 左侧图片区先于全文 story，是截图失衡的直接原因。生产目标收敛为 answer-first 的“核心解法 + 最多三步怎么做”，图片改为其后的紧凑图带；不改快照/API。下一步只修改现有收藏行为用例取得红测。
- M101 行为红测有效：1 failed / 81 skipped，旧 DOM 明确缺少 `Live Mill Conversion 的解法` region，并继续显示项目条件、适用边界、逐字证据且图片先于正文。下一步最小替换收藏案例 JSX 与对应 CSS，再重跑同一用例。
- M101 核心红绿完成：收藏案例 DOM 现为命名的“核心解法 + 怎么做”region，步骤去重并最多三条；项目条件、边界、逐字证据不再渲染，紧凑图带位于解法之后。行为与响应式合同 2/2 green；图纸收藏断言同用例保持。下一步跑完整 Board 静态/测试/构建与 detector，再加载真实页面无截图验收。
- M101 扩大 Board 门禁全绿：101/101 tests、ESLint、TypeScript、production build、Impeccable detector 0。Loaded desktop 达到 answer-first、3×220px 紧凑图带与 0 旧审计字段；390px 发现图片 auto-fit 仍为单列 570px 高，下一步先加两列移动图带 CSS 红测再最小修复。
- M101 移动图带红测有效：现有 ≤620px CSS 不含两列规则，聚焦设计合同 1/1 按预期失败。已仅为移动端补 `repeat(2, minmax(0, 1fr))`；下一步重跑同一测试并复验 390px 几何。
- M101 移动图带聚焦测试转绿，真实 390px 复验为两列两行、305px 高、图片全部加载且无溢出；图纸灵感 1 图 / 本地高清入口回归通过。下一步切回默认建筑、reset viewport，同步产品文档后跑完整权威门禁。
- 默认建筑已恢复、viewport reset。PRODUCT、DESIGN 与 architecture 已同步 solution-first 投影、最多三步、答案后紧凑图带及完整快照数据仍保留的边界。下一步重跑最终 Board 全套与权威 `scripts/verify.ps1`。
- M101 权威完整 `scripts/verify.ps1` exit 0：331 API / 101 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。下一步只读确认 3 projects / 12 completed Runs / active 0 与封存 Run，再做一次标准重载和最终 DOM/Git/HANDOFF 收口。
- M101 已达 `implementation_complete`。3 projects / 12 completed Runs / active 0 守卫下标准 stop/start 成功，API/Board 200且新进程晚于源码；封存 Run `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied。最终 loaded 建筑收藏为 1 原题 / 3 子题 / 4 案例 / 3 解法 / 每项最多 3 步 / 6 图，0 旧审计字段、0 overflow、0 error logs，默认建筑方案。未截图、未创建或 retry Run、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push；当前没有已批准的自动下一步。

## M102 enlarge collection imagery

- 用户批准新的唯一产品工作：建筑收藏和图纸灵感图片都再大一点。planning-with-files catch-up 恢复 7 条 M101 收口与 M102 开场上下文；`git diff --stat` 仅反映完整既有工作树。
- Impeccable layout/product register 已读取。范围固定为 architecture/visual collection CSS：桌面建筑约 300px 三列，手机首图全宽 + 两张半宽；图纸灵感减少桌面列数并保持手机全宽。下一步审计当前两类网格 CSS和设计合同，先写红测。
- M102 CSS 合同红测有效：旧样式仍是建筑 160–220px、图纸灵感 220px 起步且移动端没有首图跨列；聚焦 design-system test 1/1 按预期失败。下一步只修改这三处尺寸规则并重跑同一测试。
- M102 核心 CSS 红绿完成：建筑改为 220–300px auto-fit，图纸灵感改为 340px 起步，≤620px 建筑首图跨两列。102/102 Board tests、ESLint、TypeScript、production build 与 detector 0 全绿。下一步 loaded desktop/390px 无截图核对两种收藏实际尺寸和高清链接。
- M102 loaded desktop 建筑图验收通过：三轨均 300px，三张真实首案例图各约 298×224px，6 图加载、0 overflow、0 error logs，M101 解法/三步不变。下一步切换图纸灵感核对宽度与本地高清 href，再做 390px 两种模式。
- M102 图纸灵感桌面网格实测为两条 503px 轨，本地高清 href/target/rel 不变；切换后即时检查早于图片解码完成，尚不能记录实际高度。下一步用唯一图片的可见/自然尺寸条件等待加载，再继续 390px。
- M102 图纸灵感加载后桌面实测为约 502×520px，1080×1440 源图已加载，高清入口和 0 overflow 保持。下一步设置 390px，核对图纸全宽以及建筑首图跨列/两张半宽。
- M102 首次 390px visual evaluate 假设 viewport 后仍停留图纸态，实际页面已恢复默认建筑，`getComputedStyle(null)` 只读失败并已记录。Fresh snapshot 后正确验收建筑为首图约 350×262px、次图各 170×128px，loaded/overflow 全绿。下一步从该 snapshot 精确切换图纸灵感并等待唯一图片可见。
- M102 390px 图纸灵感验收通过：图片约 365×486px 全宽，loaded、本地高清 href、0 overflow 均保持；随后切回默认建筑、6 图/3 解法不变并 reset viewport。下一步同步 PRODUCT/DESIGN 图片尺寸合同后跑完整权威门禁。
- PRODUCT/DESIGN 已同步建筑 220–300px/移动首图跨列及图纸灵感 340px 起步/移动全宽合同。下一步运行 M102 权威完整 `scripts/verify.ps1`；通过后只做 active-0 重载、最终 DOM/Git/HANDOFF 收口。
- M102 权威完整 `scripts/verify.ps1` exit 0：331 API / 102 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。下一步只读确认 3 projects / 12 completed Runs / active 0 与封存 Run，再标准重载并做最终 DOM/Git/HANDOFF 收口。
- M102 已达 `implementation_complete`。3 projects / 12 completed Runs / active 0 下标准 stop/start 成功，API/Board 200且 Board 进程晚于 CSS；封存 Run `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied。最终 loaded 桌面建筑三轨仍为 300px、6 图 loaded、3 解法、0 overflow/error logs；390px 建筑首图约 350×262px、次图 170×128px，图纸灵感约 365×486px且本地高清 href 不变。未截图、未创建或 retry Run、未恢复 Firecrawl、未执行禁止的 Git 写操作；当前没有已批准的自动下一步。

## M103 architecture collection question directory

- 用户批准新的唯一产品工作：建筑方案默认显示问题目录，点进目录项后只看该案例子问题详情。planning-with-files catch-up 恢复 8 条 M102 收口与 M103 开场上下文；`git diff --stat` 仅反映完整既有工作树。
- Impeccable craft/product 已读取，用户原请求与既有 PRODUCT 已给出明确方向，无需新增视觉资产。范围固定为收藏页内部本地导航状态、目录/详情 JSX/CSS 与 Board tests；不改 API、快照或图纸灵感。下一步审计 collection state/render boundaries，先写行为红测。
- M103 行为红测有效：旧实现找不到“建筑问题目录”，并仍一次展开 fixture 的两个案例子问题。核心红绿已完成：默认目录、单题详情、明确返回，以及切换类型/离开/重开重置均由同一 Board 用例固定；102/102 Board tests、ESLint 与 TypeScript 已通过。
- 真实无截图 QA 通过：桌面和 390px 都显示 3 个目录入口、初始 0 详情/0 图片且无横向溢出；进入首题后仅 1 个子问题、2 个案例，移动返回为 44px，M102 的 350px 主图/170px 次图保留。图纸灵感本地高清入口未变，切回建筑恢复目录。viewport 已 reset。
- PRODUCT、DESIGN 与 architecture 已同步“问题目录 → 单个子问题详情”的本地状态、平铺分隔行、重置规则与无新 API/持久化边界。下一步运行 production build、Impeccable detector 和权威完整 `scripts/verify.ps1`；通过后只读确认 active 0，再执行一次标准重载与最终 DOM/Git/HANDOFF 收口。
- M103 最终源码的权威 `scripts/verify.ps1` exit 0：331 API / 102 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿；Impeccable detector 0。active-0 标准 stop/start 已完成，API/Board 200，最终 loaded 桌面保持 3 个目录入口、0 详情/案例/图片与 0 overflow。下一步只做最终 Run/Git 守卫、HANDOFF 和浏览器交付，不再改生产行为。
- M103 已达 `implementation_complete`。最终守卫为 API/Board 200、3 projects / 12 completed Runs / active 0，封存 Run `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied；`git diff --check` exit 0、staged 0，105 个既有 dirty/untracked 条目完整保留。未截图、未创建或 retry Run、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。当前没有已批准的自动下一步，等待用户指定产品工作。

## M104 clarify supplementary inputs

- 用户批准新的唯一产品工作：讲清“参考网页”和“上传文件”的使用场景。planning-with-files catch-up 恢复 12 条 M103 收口与 M104 开场上下文；Git 仍是完整既有工作树。Impeccable clarify/product 已读取，方向固定为可选、场景化、输入前说明，不增加交互层或改变请求语义。
- 下一步：只读核对 `reference_url` 与 files 的真实请求流、现有表单 JSX/CSS/测试，确定不夸大的精确文案；随后先写 Board 红测，再做最小生产修改。
- 真实数据流审计完成：URL 是 workspace 级研究线索、PDF 文本进入上下文、普通图片仅在来源反查中参与最新图反搜；初始“优先核对/图片帮助理解条件”表述会夸大能力，已从计划中纠正。下一步固定场景化文案与可访问描述的红测，不改 API 或 workflow。
- M104 行为红测有效：旧界面找不到“添加资料（可选）”，证明 optional 和使用场景没有进入可访问文案。最小生产修改已完成：保留原折叠与字段，只新增一条总说明、两个场景化标签/输入前帮助文本及 `aria-describedby`；未改请求、artifact、workflow 或文件类型。
- 下一步：重跑同一 focused 测试转绿，再运行完整 Board、lint/typecheck/build 与 detector；若通过，加载真实桌面/390px 页面无截图核对层级、换行、44px 与 overflow。
- M104 扩大前端门禁全绿：102/102 Board、ESLint、TypeScript、production build 与 Impeccable detector 0。已找到用户现有的唯一 localhost 页签；下一步认领同一页签并做无截图 desktop/390px DOM 与几何验收，不创建新页签或 Run。
- 同一用户页签已重新加载最终源码；首页默认可访问树已显示“添加资料（可选）”，字段仍保持折叠，研究方式与主动作未受影响。下一步等待现有 workspace hydration 后精确展开该区域，核对说明、标签、accessible description 和桌面几何。
- Loaded desktop 资料区验收通过：展开入口唯一且标记 optional，region/说明/标签/description 均可访问；intro 跨两列、两字段各约 463px、输入 37–39px，1084px 页面无横向溢出。下一步读取既有 viewport capability，临时设 390×844 验证单列、帮助文本换行、44px 触控与无溢出，随后 reset。
- 既有 viewport capability 可用，已临时设置 390×844；下一步先取 fresh DOM snapshot 确认资料区状态，再做一次聚合几何读取，结束后立即 reset。
- 390px 语义、单列换行与 overflow 通过；发现两个输入分别只有 39/36px，入口按钮已为 44px。下一步在 design-system test 固定 compact `.research-options input` ≥44px，确认红后最小补 CSS，再复验并 reset viewport。
- Compact input 合同已红绿完成：旧 ≤860px 规则缺少资料输入触控高度，focused design-system test 先失败；仅新增 `.research-options input { min-height: 44px; }` 后转绿。下一步让 HMR 更新同一 390px 页面，聚合复测两个输入与 overflow，然后 reset viewport。
- 真实 390px 复验通过：URL 与 file input 均为 44px，单列宽 306px、无横向溢出；viewport 已 reset。下一步同步 PRODUCT/DESIGN 对可选资料的诚实语义，重跑最终前端门禁并执行完整仓库 `scripts/verify.ps1`。
- PRODUCT、DESIGN 与 architecture 已同步 optional、workspace 级 artifact 及 URL/PDF/image 的真实用途。M104 权威完整 `scripts/verify.ps1` exit 0：331 API / 103 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿；Impeccable detector 0。下一步只读确认 3 projects / 12 completed Runs / active 0 与封存 Run，再做一次标准服务重载和最终 DOM/Git/HANDOFF 收口。
- Active 0 下标准 stop/start 已完成，API/Board 200，Board 进程晚于 App/CSS。最终 loaded 桌面再次确认唯一可选资料 region、两项输入、正确 labels/describedBy、941px 两列与 0 overflow；下一步只做最终 Run/Git 守卫、HANDOFF 和浏览器交付。
- M104 已达 `implementation_complete`。最终守卫为 API/Board 200、3 projects / 12 completed Runs / active 0，封存 Run `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied；`git diff --check` exit 0、staged 0，105 个既有 dirty/untracked 条目完整保留。未截图、未创建或 retry Run、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。当前没有已批准的自动下一步，等待用户指定产品工作。

## M105 scenario-mapped supplementary inputs

- 用户批准新的唯一产品工作：让“建筑设计研究 / 来源反查 / 图纸灵感”的资料输入和主动作真正对应。planning-with-files catch-up 恢复 M104 收口与本次纠偏上下文；完整 HANDOFF、所有 active phase 行、findings/progress 尾部和 Git 状态均已复核，既有 105 个 dirty/untracked 路径继续视为用户资产。
- Impeccable clarify/product 已读取。范围固定为 Board 启动器的场景专属 JSX、切换清理行为、现有 CSS 复用与 Board tests；不改 API、artifact、workflow，不创建或 retry Run。下一步先写建筑/来源反查/图纸灵感映射与隐藏资料清理的行为红测。
- M105 行为红测有效：完整 Board 运行中仅两个新场景合同失败（101/103 仍通过）。旧 DOM 找不到建筑专属“添加项目资料（可选）”，来源反查也没有常显图片区；图纸灵感仍保留通用资料按钮。下一步最小修改 `selectResearchGoal` 与按 goal 条件渲染，保持 API/workflow 不变。
- M105 核心红绿完成：建筑资料改为可选 PDF + 指定 URL；来源反查常显必需 JPG/PNG + 可选疑似 URL，CTA 为“开始反查”；图纸灵感不渲染任何资料输入。场景实际变化会清空 URL/files 并收起建筑资料区，视觉提交测试确认没有 `/inputs` 请求。103/103 Board、ESLint、TypeScript、production build 均通过，源码 detector 命令 exit 0。下一步同步 PRODUCT/DESIGN/architecture，再做 loaded desktop/390px 无截图 QA。
- PRODUCT、DESIGN 与 architecture 已同步三个场景的输入、CTA、格式限制和未提交状态清理边界。Loaded desktop/390px 无截图 QA 全绿：建筑与来源反查两列/单列语义、accept/required/describedBy、CTA 和切换清理正确；图纸灵感 0 资料输入；桌面/390px 均 0 overflow，紧凑输入 44px，viewport 已恢复。下一步运行权威完整 `scripts/verify.ps1`，通过后只做 active-0 重载、最终 DOM/Git/HANDOFF 收口。
- M105 权威完整 `scripts/verify.ps1` exit 0：331 API / 103 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。下一步只读确认 3 projects / 12 completed Runs / active 0 与封存 Run，再执行一次标准服务重载和最终 DOM/Git/HANDOFF 收口。
- M105 已达 `implementation_complete`。active 0 下标准 stop/start 成功，API/Board 200，Board 进程晚于最终 App 源码；最终用户页签停在常显“来源反查材料”状态，必需 JPG/PNG、可选疑似 URL、“开始反查”、0 建筑资料与 0 overflow 均正确。最终持久状态仍为 3 projects / 12 completed Runs / active 0，封存 Run `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied；`git diff --check` exit 0、staged 0，105 个既有 dirty/untracked 条目完整保留。未截图、未创建或 retry Run、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。当前没有已批准的自动下一步，等待用户指定产品工作。

## M106 project brief as research boundary

- 用户批准新的唯一产品纠偏：项目任务书用于收束思考范围，系统应以阅读任务书后的项目条件来组织问题拆解与检索。planning-with-files catch-up 恢复 M105 收口和本次语义校正；Impeccable clarify/product 已读取。范围固定为建筑资料入口、展开说明、任务书帮助文本、对应 Board test 与产品合同，不改请求、artifact 或 workflow。
- 下一步先把“入口具体化、任务书收束范围、案例页仅作线索”的精确文案写成行为红测，再做最小生产修改；来源反查与图纸灵感合同必须原样通过。
- M106 聚焦红测有效：旧界面仍只有“添加项目资料（可选）”，找不到具体的任务书/案例页入口，更没有“任务书用于收束范围”的说明。下一步只替换三处建筑资料文案并重跑同一测试。
- M106 核心红绿完成：入口改为“添加任务书或案例页（可选）”，总说明区分任务书收束范围与案例页补充线索，任务书 accessible description 明确读取场地/功能/限制后作为拆题与案例检索边界。聚焦 1/1、完整 103/103 Board、ESLint、TypeScript 和 production build 全绿；来源反查与图纸灵感既有合同未改。PRODUCT、DESIGN、architecture 已同步且明确不承诺独立预分析或自动改写问题。下一步 loaded 页面无截图核对最终可访问文案，再执行完整门禁与收口。
- Loaded 页面无截图核对通过：建筑默认折叠入口为“添加任务书或案例页（可选）”；展开后的总说明、任务书标签、收束边界帮助文本与案例 URL 线索说明均在可访问树中正确分离。下一步只读切换来源反查/图纸灵感确认 M105 文案隔离，再运行权威完整门禁。
- 来源反查与图纸灵感真实切换回归通过：两者均不显示任务书收束文案；来源反查保持专属图片区/“开始反查”，图纸灵感保持 0 资料 region/“查找灵感”与 0 overflow。下一步运行 M106 权威完整 `scripts/verify.ps1`。
- M106 权威完整 `scripts/verify.ps1` exit 0：331 API / 103 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。下一步只读确认 3 projects / 12 completed Runs / active 0 与封存 Run，再执行一次标准服务重载和最终 DOM/Git/HANDOFF 收口。
- M106 已达 `implementation_complete`。active 0 下标准 stop/start 成功，API/Board 200且 Board 进程晚于 App 源码；最终用户页签停在展开的建筑资料区，任务书“读取项目约束 → 收束拆题与案例检索”、案例页独立线索与 0 overflow 均正确。最终持久状态仍为 3 projects / 12 completed Runs / active 0，封存 Run `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied；`git diff --check` exit 0、staged 0，105 个既有 dirty/untracked 条目完整保留。未截图、未创建或 retry Run、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push；当前没有已批准的自动下一步，等待用户指定产品工作。

## Completion roadmap planning

- 只读完成度审计结论为：本地单用户 V2.1 约 82%，作品展示约 90%，小范围内测约 80%，公开发布约 60%。核心缺口集中在任务书问题确认、真实来源反查、独立用户验证、维护性和发布复现，不在继续堆叠结果页功能。
- 已将 P0–P2 的 M107–M111 提案写入 `task_plan.md`，每项均包含交付物和可验证验收；全部保持 pending user approval，没有新增 `in_progress` 阶段。
- planning-with-files 首次 catch-up 命中 Windows Store `python` 别名并在读写前退出；改用 `apps/api/.venv/Scripts/python.exe` 后成功恢复 26 条未同步审计/规划上下文。没有改动产品源码、服务或 Run 数据。
- 当前唯一下一步仍是等待用户选择一个提案阶段。未截图、未创建或 retry Run、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。

## M107 real task-brief analysis

- 用户提供真实三页任务书 `2024 研一概念设计-窦平平.pdf` 与问题“耕织图是二维画作，建筑是三维的，如何转译提取元素”。本轮仅批准读取、问题收束与 M107 需求校准，未批准产品实现或 Live Run。
- 使用 bundled `pdfplumber` 完整提取三页文本；没有截图。任务书确认项目对象、研究/设计任务、八周教学顺序和最终成果要求，并直接给出长廊分段、工作流程嵌入空间、长卷如展览等可转译线索。
- 已把该样本记录为 M107 首个真实 fixture：产品应先显示项目边界，再把问题拆成过程序列、空间语法、人物—器具—场所关系、多感官交互和原型验证；不得把问题收窄为装饰纹样立体化。
- 当前仍为分析完成、实现待批准；没有修改产品源码、服务或 Run 数据，没有执行禁止的 Git 操作。

## M107 task-brief question confirmation implementation

- 用户纠正上一轮意图：任务书和问题是用于产品验证的真实素材，不是要求单独分析；现已明确批准开始完善项目。M107 从 proposal 转为 `in_progress`。
- 验收固定为“PDF + 主问题 → 先读取边界并生成可编辑问题目录 → 用户确认 → 才创建现有研究 Run”；无 PDF 保留直接开始。真实三页智慧博物馆任务书与《耕织图》二维到三维转译问题必须进入 deterministic fixture，实施期间不创建 Live Run。
- Impeccable product register 与现有 PRODUCT/DESIGN/CSS 已读取；新增流程必须是启动器内的渐进式工作面，不使用 modal、嵌套卡片或装饰性动效，紧凑屏触控仍不低于 44px。
- 源码审计完成：现有客户端在单个 `startResearch()` 中上传 inputs 后立即创建 Run；后端已有 PDF 页数/文本提取和 planning provider，workflow 已优先使用持久的 existing subquestions。实现将复用这些能力，新增非持久化 preview endpoint 与可选确认问题契约，不增加数据库表或迁移。
- M107 红测有效。API 三项分别因 `ResearchSpec` 无 confirmed subquestions、`ResearchPlan` 无摘要/边界、`brief-review` route 404 失败；Board/client 三项分别因缺少 review client、Run body 未携带问题目录、旧 UI 直接创建 Run 失败。其余同次 Board 102 项仍通过，证明新增合同精准覆盖缺口。
- M107 核心红绿完成：非持久化 `/brief-review`、真实《耕织图》fixture、摘要/边界/问题目录、确认问题写入 Run，以及 Board 内联编辑/删除/补充/确认交互均已实现。聚焦 API 3/3、Board/client 3/3 green；预览阶段数据库中 Run 和 InputArtifact 都为 0。
- 扩大门禁现为 334/334 API、105/105 Board，Ruff/41-file format、strict Mypy、Board ESLint/TypeScript/build 与 Impeccable detector 0 全绿。首次权威脚本仅因 120 秒外层时限在无失败输出时被终止；下一次保持脚本不变，只把调用时限提高到 5 分钟。
- M107 权威完整 `scripts/verify.ps1` 在五分钟时限下 exit 0：334 API / 105 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。
- active 0 下标准 stop/start 成功；API/Board 200，两端进程均晚于最终源码。Loaded 用户页签已装入真实任务书与主问题但未点击预览 CTA：桌面/390px 标签、文件名、44px CTA/移动输入、0 overflow 和 0 error logs 通过，未调用 Provider、未上传文件、未创建 Run；viewport 已 reset，页面作为 deliverable 保留。
- M107 已达 `implementation_complete`。最终持久状态仍为 3 projects / 12 completed Runs / active 0，封存 Run `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied；`git diff --check` exit 0、staged 0，106 个既有及本次 dirty/untracked 条目完整保留。未截图、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push；当前没有已批准的自动下一步。

## M107 real feature-flow acceptance

- 用户现已明确批准测试这一功能流程，并在首轮预览后纠正为“标准研究”。唯一动作固定为使用真实任务书与《耕织图》问题，先验证非持久化任务书整理，再编辑、删除、补充四个问题，最后只确认一次并创建一条 Standard Run；不得 retry、补开第二条 Run 或以截图代替行为证据。
- 首次概览预览已真实返回项目摘要、5 条任务书边界和 3 个子问题；确认前数据库仍为 3 projects / 12 Runs / active 0 / 0 InputArtifact，证明预览零持久化。用户同时指出文件控件已显示文件名时，下方又渲染同名标签会形成重复；先以行为红测删除第二份文件名展示，再重载并按 Standard 重新预览。
- 验收基线为 3 projects / 12 completed Runs / active 0；标准确认前 Run 总数与 InputArtifact 总数必须不变，确认后只能增加 1 条 Run 和 1 份 PDF artifact，confirmed subquestions 必须与页面最终四个问题逐字一致。该 Run 跟踪到诚实终态后 active 必须回到 0，封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 必须继续保持 completed / attempt 0 且绝不 retry。
- 用户进一步纠正产品语义：任务书整理属于研究内部过程，不应形成新的边界预览/问题编辑界面；交互必须保持一次“开始研究”，最后仍只展示正常研究结果。现已用新红测固定“brief-review POST → input POST → Standard Run POST”的后台顺序、4 条 typed subquestions、0 中间 region，并删除整套问题确认 JSX/CSS 与重复文件名标签。完整 105 Board、lint、typecheck、build 均通过；下一步重载后只创建一次真实 Standard Run。
- active 0 下标准重载后，真实页面保持普通启动器、Standard 默认选中与唯一“开始研究”；任务书文件名不再渲染第二个标签。一次热更新遗留 React 异常使首次交互未触发任何请求，数据库仍为 12 Runs / 0 artifact；对现有页签做一次 clean reload 后，同一输入的唯一有效提交进入“正在准备研究…”，没有中间任务书界面。
- 唯一 Live Run `ff16988d-ebbd-4da4-b5e2-3cd080dcf0be` 为 `balanced` / attempt 0，逐字保留真实主问题和后台生成的 4 条任务书约束问题；只新增 1 份 `2024 研一概念设计-窦平平.pdf` artifact（3 页）。Trace 的第二条 planning event 为 `planner: checkpoint`，证明 workflow 未重新拆题。
- 该 Run 最终 `completed / completion_satisfied`：28 usable assets、8 projects、4/4 covered，四问各 3 次 project pass，107 条 EvidenceClaim；结果页直接展示普通标准研究结论、问题拆解和案例证据，0 中间任务书 region，本次正式 reload 后页面 error logs 0。最终为 3 projects / 13 completed Runs / active 0，封存 Run 保持 completed / attempt 0 / coverage_satisfied。
- 修正后权威完整 `scripts/verify.ps1` exit 0：334 API / 105 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。未截图、未 retry 或创建第二条 Run、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。

## M112 global project-preview copy deduplication

- 用户批准新的唯一全局纠偏：建筑结果的同项目多图只写一次共同说明；每张图只保留图纸类型、独有观察、相关度和操作。验收覆盖所有建筑 Run 与单图/多图形状，不改后端数据、研究内容、Inspector、来源、对照或收藏行为。
- planning-with-files 与 Impeccable distill/product 已恢复；两次中断期间因 Windows 受限沙箱无法启动 PowerShell，没有改动任何文件。权限恢复后已完整读取 HANDOFF、活动计划、近期 findings/progress 与当前 dirty worktree；当前仍为 3 projects / 13 completed Runs / active 0，封存 Run 不动。
- M112 红绿完成：旧实现对同项目三图重复渲染共同推断 3 次；新项目级投影把重复标题/观察与 legacy 缺省说明归并一次，逐图仅保留唯一文案，单图显式回归原标题/观察。106/106 Board、ESLint、TypeScript、production build 与 Impeccable detector 0 全绿。
- active 0 下清理陈旧进程记录并标准重启 API/Board，二者均为 200。真实 M107 结果的 5 个多图与 6 个单图无截图 DOM QA 全绿；390px 单列、44px 操作、0 overflow、0 error logs，viewport 已恢复。下一步运行权威完整 `scripts/verify.ps1`，通过后只做最终 Run/Git/HANDOFF 收口。
- M112 已达 `implementation_complete`。权威 `scripts/verify.ps1` exit 0：334 API / 106 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。最终仍为 3 projects / 13 completed Runs / active 0，封存 Run `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied；API/Board 200、detector 0、`git diff --check` exit 0、staged 0。未创建或 retry Run、未截图、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push；当前没有已批准的自动下一步，等待用户指定产品工作。

## Post-M112 progress and roadmap audit

- planning-with-files catch-up、完整 HANDOFF、所有 active plan 行、近期 findings/progress 与 Git 状态已复核；M107/M112 均为 `implementation_complete`，没有 `in_progress` 阶段。服务仍为 API/Board 200，数据为 3 projects / 13 completed Runs / active 0 / 3 collections / 1 InputArtifact，封存 Run 不动。
- 只读能力审计确认来源反查是唯一未做真实闭环验收的核心功能：当前 13 条 Run 中 9 条 precedent、4 条 visual、0 条 source lookup。维护性热点为 3,662 行 Board、4,045 行 CSS、4,846 行 workflow；本地 gate 有 `pytest-cov` 依赖但没有覆盖率输出/阈值；仓库没有 `.github` CI。
- `task_plan.md` 已更新为 M108 单次真实来源反查 → M109 小型用户 pilot → M110 characterization/覆盖率/分段抽取 → M109 六人完整验证 → M111 clean-machine/CI/发布收口。全部仍为 proposal pending separate approval；当前没有已批准的自动下一步，也没有修改产品源码、服务或 Run 数据。

## M113 source removal, case selection, and history controls

- 用户批准新的唯一主线：删除来源反查；建筑历史结果以“当前子问题中的项目案例”为选择/收藏单位，不再逐图勾选；修复截图中的项目选择控件并回归完整历史弹层。M113 已标为 `in_progress`，M108 提案改为 superseded。
- planning-with-files catch-up 与 Impeccable product register 已恢复；截图和现有 PRODUCT/DESIGN 显示信息单位与控件层级不一致。下一步读取选择、收藏、goal enum、项目控件和 history dialog 的精确实现，先写失败行为测试，再做最小产品/契约删除和交互修复。实施期间不创建或 retry Run，不截图，不执行禁止的 Git 操作。
- M113 红绿与收口完成：来源反查/TinEye 已从前后端契约、执行路径、配置、现行文档和评测夹具删除；建筑选择提升到项目档案，图纸灵感逐图选择保持；项目菜单和历史弹层完成键盘/移动端修复。Board 107/107、API 327/327、Extension 165/165、packaged E2E 8/8、25/108 evaluation、Ruff/format、strict Mypy、两端 lint/typecheck/build、Impeccable detector 0 全绿。
- active 0 下唯一一次标准 stop/start 已完成。API `ok`、Board 200、OpenAPI 只接受两类 goal；无截图 desktop/390px DOM QA 和 error logs 0 通过。最终仍为 3 projects / 13 completed Runs / active 0；封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍 completed / attempt 0 / coverage_satisfied。未创建或 retry Run、未截图、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push；当前没有已批准的自动下一步。

## M114 project taxonomy audit

- 用户质疑项目菜单分类缺少依据。只读 API 审计确认三项是 Workspace 人工名称而非内容分类：历史研究容器 2 Runs、空 Workspace 0 Runs、主 Workspace 11 Runs；主 Workspace 同时混入旧厂房与苏州科技馆《耕织图》两个不同任务书语境。
- 本轮未改名称、未移动 Run/InputArtifact/收藏、未删除空 Workspace。M114 审计完成；下一步若获批准，应先设计可逆的数据迁移与归档边界，再改持久数据和菜单呈现。

## M115 implementation and M116 start

- 用户已批准项目重组。M115 代码和真实数据事务已完成：新增 Workspace `archived_at` schema/API/migration，项目菜单分成当前项目与历史归档，归档视图只读；M107 Run `ff16988d-ebbd-4da4-b5e2-3cd080dcf0be`、任务书和 2 条关联收藏迁入新苏州科技馆项目，原验证项目归档，空 Workspace 删除。事务后只读 API 为旧厂房 10 Runs / 3 collections、苏州科技馆 1 Run / 2 collections、历史归档 2 Runs / 0 collections，总计 13 completed / active 0；封存 Run 仍 completed / attempt 0。M115 仍需 loaded no-screenshot QA、完整门禁和最终收口。
- 用户新增并批准 M116：优化收藏详情排版；同案例在不同子问题下独立选择；选择数量按子问题关联计数；精确保存明确选中的子问题；成功后取消所有勾选并清空 Board 选择。根因和前后端合同已定位，下一步先写 API/Board 失败行为测试。
- 会话恢复首次调用系统 `python`、第二次调用 `py -3` 均因 Windows Store 别名/命令不存在失败；没有产品写入。改用 `apps/api/.venv/Scripts/python.exe` 后 catch-up 成功，恢复 56 条未同步上下文。HANDOFF、活动计划、近期 findings/progress 和 dirty/untracked worktree 均已复核。
- M116 首轮前端红测已按预期失败：同 asset 的第一题被选择后第二题按钮也变为 pressed；保存 body 只有 `note`、没有 `subquestion_ids`；CSS 仍是纵向 flex，未满足宽屏双列。API 初次红测因默认 mock candidate 只有一个 subquestion 而在测试准备阶段 `StopIteration`，现已改为在 fixture 内给同一 asset 绑定两个已有 Run 子问题，下一次只验证目标合同。
- M116 第一轮实现后 API 精确子问题测试、client payload 测试和全部 6 项 design-system 测试已转绿。Board 行为已执行到保存后清空，仅测试筛选 Board PATCH 时对同 URL 的初始 GET `init` 为 `undefined` 未做可选判断而抛错；已修正测试过滤器，生产流程没有报错。
- M116 扩大测试为 15/15 API `test_runs_results.py` 与 109/109 Board 全绿，ESLint、Ruff 通过。首次 TypeScript typecheck 只因视觉选择分支的数组字面量被推断为不含可选 `subquestionId` 的窄类型而失败；已给 `pendingSelections` 加现有 `CollectionSelection[]` 标注，运行逻辑未变。
- M115/M116 无截图实页 QA 已通过：项目菜单 2 当前 + 1 归档，归档只读且 2 Runs / 0 新建入口；真实 M107 同案例跨两个子问题选择为 `false/false → true/false`，底部计数 1。收藏详情 1440px 为 815.5/489.3px 双列、488px 单图、20px 问题标题；390px 单列、350px 图、44px 操作；两个视口 overflow 0。未保存收藏、未创建或 retry Run、未截图。
- Impeccable detector 0、`git diff --check` exit 0。首次完整 `scripts/verify.ps1` 已通过 process/security/evaluation 和 329 API tests，但 Ruff format check 指出 M115 的 `apps/api/tests/test_migrations.py` 需机械格式化后退出 1；生产测试没有失败。另一次只读 `rg scripts/*.ps1` 因 PowerShell 不展开该 glob 返回 Windows 路径错误，已改用明确 `Get-ChildItem` 获取脚本列表，不重复该命令。
- 格式化 M115 迁移测试后，第二次权威 `scripts/verify.ps1` exit 0：329 API / 109 Board / 165 Extension / 8 packaged E2E，加 Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 25/108 evaluation 全绿。
- 第一次 PowerShell 运行态聚合再次把 JSON 数组包装成单对象并误报 1 project / 1 Run / active 1，已在 stop/start 前判无效；改用 Node 原始 JSON 解析后确认 3 projects（2 active + 1 archived）/ 13 completed Runs / active 0，封存 Run completed / attempt 0 / coverage_satisfied。随后严格执行一次标准 stop 和一次 start；API/Board 恢复 200，结构化状态不变。
- 最终用户页签已 reset viewport 并在新服务上重载，停在苏州项目首个收藏子问题详情；默认 834px 为单列、1 张案例图、overflow 0、console error 0。M115/M116 均可标记 `implementation_complete`；未保存新收藏、未创建或 retry Run、未截图、未恢复 Firecrawl、未执行 reset/checkout/clean/stage/commit/push。
- 最终 API `/health` 为 `ok / gpt-5.6-sol`，Board 200；Node 结构化守卫仍为 3 projects（2 active + 1 archived）/ 13 completed Runs / active 0，封存 Run 未变。`git diff --check` exit 0、cached 文件 0，完整既有 dirty/untracked 工作树保留。

## M117 question-derived history record names

- 用户指出“2 当前 + 1 归档”不能只是针对现有记录的一次性结果。M117 已标记 `in_progress`，范围固定为项目级归档/恢复，不把 Run 自动过期混入其中。
- 恢复与源码审计确认缺口：后端通用 PATCH 可直接写 `archived_at`，没有 active Run / last-active guard；Board 只分组查看，归档页标题写死且没有恢复操作。下一步先写 API、client、Board 失败行为测试，再做最小实现；不迁移现有数据、不创建或 retry Run。
- M117 红测有效：API archive/restore 路由为 404，client 方法不存在，Board 找不到归档/恢复按钮且归档页仍是固定名称。失败与目标合同一致，现开始实现服务端统一守卫和通用 UI 操作。
- 用户澄清目标不是项目归档生命周期，而是历史 Run 的记录名：只按研究问题内容生成简短概括。已停止原方向并手工撤回本轮新增的 Workspace archive/restore 路由、客户端方法、菜单/归档页动作及对应测试，保留 M115 的既有归档数据与只读展示；未改真实数据。M117 已重写为 question-derived history record names，下一步先写 API/Board 红测。
- M117 正确方向的 API/Board 红测均按预期失败：Run 响应缺少 `title`，最近研究仍显示完整 `question`。实现后聚焦测试转绿，Board App + client 为 97/97；多句式 API 测试首次仅发现长标题机械截断成“的…”，已调整为截断时去掉悬空连接词，未改变问题原文。
- M117 的 26 项 schema/workspace API、97 项 Board App/client、Ruff、strict Mypy、Board lint/typecheck/build 与 `git diff --check` 已通过。完整 `scripts/verify.ps1` 首次仅因本地命令超时配置为 120 秒被宿主终止，没有测试失败输出；需用足够的命令超时重新执行一次权威门禁。
- 权威 `scripts/verify.ps1` 在足够超时下 exit 0：335 API / 109 Board / 165 Extension / 8 packaged E2E，加全部 Ruff/format/Mypy、两端 lint/typecheck/build、process/security 与 25/108 evaluation 全绿。标准 stop/start 后 API/Board 200；Node 原始 JSON 确认 3 projects（2 active + 1 archived）/ 13 completed Runs / active 0 / 13 titles，封存 Run completed / attempt 0 / coverage_satisfied。
- 无截图实页 QA 在旧厂房项目的 10 条完整历史中发现问题自带冒号时标题出现 `：：`。已先新增 `旧厂房改造：如何植入公共功能？` 红测复现，再把冒号纳入疑问分隔符归一化；未点击保留、未创建或 retry Run。
- 390×844 历史弹窗 DOM/几何检查确认 10/10 标题、overflow 0、保留操作 44px，但关闭按钮仍为 36px，低于既定触控标准。已先用 design-system 红测复现，再只为移动端 `.history-panel .panel-heading button` 补 `min-height: 44px`。
- M117 最终扩大回归为 335/335 API、110/110 Board；Ruff/format、strict Mypy、Board lint/typecheck/build、detector 0 和 `git diff --check` 通过。先前权威全仓脚本已验证 165 Extension / 8 packaged E2E 与全部 process/security/evaluation，之后的最终变更仅为已全量覆盖的标题标点归一化和历史关闭按钮 44px。
- 最终服务重载后 API/Board 200，真实状态仍为 3 projects（2 active + 1 archived）/ 13 completed Runs / active 0 / 13 derived titles；封存 Run 保持 completed / attempt 0 / coverage_satisfied。无截图 desktop/390px QA 通过苏州标题“耕织图：转译提取元素”、旧厂房 10/10 历史标题、双冒号 0、overflow 0、关闭/保留 44px、error log 0；项目选择和默认视口已恢复，用户页签保留在可用的 `http://127.0.0.1:5173/`。
- 未创建、取消或 retry Run，未改真实数据，未恢复 Firecrawl，未截图，未执行 reset/checkout/clean/stage/commit/push。M117 可标记 `implementation_complete`；当前没有已批准的自动下一步，等待用户指定产品工作。

## M118 idempotent local-service recovery

- 用户批准从完善计划的 P0 运行可靠性开始。只读诊断确认页面 `ERR_CONNECTION_REFUSED`，状态文件仍记录昨日 5173/8000 进程，但两个端口当前均无监听；API/Board 历史日志只有正常启动记录。M118 已标记 `in_progress`，下一步先扩展 PowerShell 生命周期红测，再让 `start.ps1` 复用健康状态并自动收敛陈旧/半活状态；不创建或 retry Run、不恢复 Firecrawl、不改真实数据。
## 2026-07-25 M118 resumed implementation

- 按 `planning-with-files` 执行 session catch-up；系统 `python` 命中 Windows Store alias 并失败，随后改用 Codex bundled Python 成功恢复 15 条未同步上下文。未改产品数据或运行态。
- 复核 HANDOFF、M118 活动计划、近期 findings/progress、`git status --short --branch` 和 diff stat；既有 dirty/untracked 工作树完整保留，staging/commit/push 等禁止操作均未执行。
- `scripts/dev-common.ps1` 新增一次性 HTTP readiness 与完整 API/Board pair 健康判断；`scripts/start.ps1` 新增健康复用、陈旧/半活状态安全清理、启动失败日志尾部和 launcher 空值守卫。
- 生产补丁后 `git diff --check -- scripts/dev-common.ps1 scripts/start.ps1` exit 0；M118 仍为 `in_progress`，下一步运行 PowerShell 生命周期聚焦测试。
- 聚焦测试通过：`scripts/tests/process-lifecycle.tests.ps1` exit 0，`scripts/tests/dev-common.tests.ps1` exit 0；覆盖健康双服务、半活状态、无监听状态、workspace listener 与无关 listener 的停止边界。
- 对真实昨日陈旧状态执行一次 `scripts/start.ps1`，新状态为 API 8000 / 200、Board 5173 / 200。第二次执行明确输出 `ArchResearch development services are already running.`，四项 listener/launcher PID 前后完全一致。
- 首次数据守卫误用无前缀路径得到 404；改成 `/v1` 后又复现 PowerShell 顶层 JSON 数组包装问题。未发生写入，随后按既有可靠做法改用 bundled Node 原始 JSON 解析，不再重复失败命令。
- Node 只读守卫确认 3 projects / 13 Runs / 13 completed / active 0；封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 保持 completed / attempt 0 / coverage_satisfied。下一步运行完整 `scripts/verify.ps1`。
- 第一轮完整 `scripts/verify.ps1` exit 0：336 API / 110 Board / 165 Extension / 8 packaged E2E 及全部静态、构建、安全与评测门禁通过。随后仅把 API launcher 移入既有 `try/catch`，确保最早启动失败也进入日志/清理路径；聚焦生命周期、dev-common 和 diff check 再次通过。
- 最终权威 `scripts/verify.ps1` 在上述控制流修正后再次 exit 0：336/110/165/8 全绿，Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security、25/108 evaluation 全部通过。
- 最终 API/Board 200，真实状态仍为 3 projects / 13 completed Runs / active 0，封存 Run保持 completed / attempt 0 / coverage_satisfied；`git diff --check` exit 0、cached 0，所有既有 dirty/untracked 文件保留。未创建、取消或 retry Run，未恢复 Firecrawl，未截图，未执行 reset/checkout/clean/stage/commit/push。M118 标记 `implementation_complete`，当前没有已批准的自动下一步。

## M119 verified workspace backup and safe restore

- 用户明确批准下一步实施完整工作区数据备份、预检与失败不覆盖的安全恢复。M119 标记 `in_progress`；验收范围固定为 SQLite、收藏本地图、任务书/上传附件和导出，明确排除源码、凭据、日志、依赖、构建产物和运行进程状态。
- `planning-with-files` catch-up 使用 bundled Python 成功恢复 9 条未同步上下文；M118 已完整收口。下一步只读梳理数据库、附件、收藏图片和导出路径及现有 API/Board 模式，然后先写失败行为测试。
- 只读存储审计确认 M119 白名单为 SQLite + `runs/collections/workspaces/exports`，并确认需排除约 2.5GB `tool-bench`、日志、截图、runtime、provider/browser token 和进程状态。跨机器恢复还需在 staging DB 中安全重写两个 typed `storage_path` 字段到目标 data root。
- 已读取 Impeccable context、ArchResearch PRODUCT/DESIGN、product register、CSS tokens 和 App shell；数据管理确定为独立平面页面，恢复采用预检后确认，不使用弹窗或主页功能卡。
- 第一次并行读取 main/database/API/App shell 的 JS 返回表达式引用未定义变量，四个 nested tool 未执行；已修正后成功读取，未改文件。确认恢复需绕开普通 Session，active 0 下 staging 校验、engine dispose、可回滚 entry swap 和 Database rebind。
- 真实 SQLite 只读审计确认 schema head `d0f1a2b3c4d5`，52 条 `InputArtifact/AssetCandidate.storage_path` 全部存在且位于允许的 `workspaces/` 或 `runs/`，当前数据可无损打包。
- 新增 `test_workspace_backup.py`，固定完整五类 durable entry round trip、portable DB path、损坏 hash/路径穿越拒绝和中途 swap 失败回滚。首次红测在 collection 阶段因 `archresearch_api.workspace_backup` 尚不存在而失败，目标有效。
- 新增 backup service、Database reconnect、Pydantic API schemas 与三个数据备份路由。两次模块加载错误分别为 `get_settings` 定义顺序和 Starlette/FastAPI `UploadFile` 类型不匹配，已各自针对性修正；Windows 临时 DB 锁定由所有 sqlite3 连接显式 closing 解决。
- 后端 4/4 聚焦测试全绿。Board client/App 红测先分别因缺少方法与缺少“数据管理”入口失败，随后实现 Blob 下载、FormData 预检/恢复、完整数据页面与响应式样式，2/2 聚焦测试转绿。下一步补 portable DB 引用的预检守卫后运行扩大门禁。
- 2026-07-25 中断恢复：按要求完整复核 HANDOFF、所有 `implementation_complete` / `in_progress` 计划行、findings/progress 末尾与 `git status --short --branch`；M119 仍为唯一 `in_progress`，既有 103 个 dirty/untracked 路径完整保留。系统 `python` 首次命中 Microsoft Store alias 失败，随后改用 Codex bundled Python 完成 planning catch-up，恢复 13 条未同步上下文；未改产品数据、服务或 Run。
- 当前恢复点：M119 行为测试 4/4、Board 112/112、Board lint/typecheck 已绿；下一步只修 `api.py`、`workspace_backup.py`、`test_workspace_backup.py` 的 Ruff/format 和 manifest category Mypy 类型问题，再扩大到 API/Board/全仓门禁。真实数据只允许先备份下载与只读预检，独立校验和回滚安全确认前不得真实恢复。
- M119 静态收口完成：Ruff 只机械整理三个 M119 文件并删除测试未使用导入；manifest 文件类别改为由 `_durable_files` 携带白名单 `BackupCategory`，没有使用不受检的强制转换。Ruff check/format、strict Mypy 19 source files 与聚焦备份测试 4/4 全绿；下一步运行完整 API 与 Board 门禁。
- M119 第一轮扩大门禁：Board 112/112、ESLint、TypeScript 与 production build 全绿；API 为 336 passed / 4 failed。四个失败全部是既有 PDF/PNG multipart 路径意外返回 422（任务书 review、workspace upload、unsupported-file 415 合同与 delete fixture），而 M119 4/4 仍绿。下一步读取 422 detail 并修复 M119 引入的全局 FastAPI 上传参数解析回归，不改既有上传业务语义。
- 上传回归根因是 M119 声明式备份端点将模块级 `UploadFile` 从 Starlette 类型替换为 FastAPI 子类，导致既有 `request.form()` 的实例检查全部失败。现已明确分离：备份端点使用 `FastAPIUploadFile` 注解，手动 multipart 与存储 helper 使用 Starlette `UploadFile`；相关 10/10 用例转绿。
- M119 第二轮扩大门禁全绿：完整 API 340/340，Ruff check、50-file format 与 strict Mypy 19 source files 通过；Board 112/112、ESLint、TypeScript 与 production build 通过。下一步运行 Impeccable detector 和全仓权威门禁，再在 active 0 下加载最终源码并只做真实备份下载与预检。
- Impeccable context/product register 与现有 PRODUCT/DESIGN/CSS 复核完成，`App.tsx` + `styles.css` detector 为 0 findings；数据管理保持平面文档页、标准文件输入、预检后危险确认和移动 44px，不需新增界面修改。下一步复核备份交换实现并运行权威全仓门禁。
- 备份服务前两段安全复核发现一个门禁前缺口：ZIP 的 `manifest.json` 在 `archive.read()` 前没有独立未压缩大小限制，entry 数量也应在遍历前拒绝。下一步完成交换/回滚段审查后，先加恶意 manifest 红测，再补最小早期上限；真实数据仍未触碰。
- 安全复核红绿完成：新增 oversized manifest 行为测试先因缺少上限失败；实现 1 MiB manifest `file_size + bounded read` 双重限制，并把 ZIP entry 数量上限前移。聚焦 M119 测试现为 5/5，Ruff/format/strict Mypy 全绿；下一步运行最终权威 `scripts/verify.ps1`。
- M119 最终源码权威 `scripts/verify.ps1` exit 0：341 API / 112 Board / 165 Extension / 8 packaged E2E，加 Ruff/51-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 25/108 evaluation 全绿。Impeccable detector 0。
- 重载前 Node 原始 JSON 只读守卫确认 API health ok、3 projects / 13 Runs / 13 completed / active 0；封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 保持 completed / coverage_satisfied。下一步仅在该 active-0 守卫下标准重载服务，再执行真实备份下载与只读预检。
- active 0 下标准 stop/start 完成；最终 API/Board 均 200，OpenAPI 三个 data-backup 路由已加载，状态仍为 3 projects / 13 completed Runs / active 0。
- 真实备份下载与只读预检通过：`.artifacts/archresearch-backup-20260725T200720.zip` 为 70,993,145 bytes，SHA-256 `2fad30316f8c01979b4b4248ebf04b209b25db450bd59ec6f96ad384ff61b4ee`；预检 ready，66 files、73,830,230 uncompressed bytes、3 projects / 13 Runs / 5 collections / 1 InputArtifact。
- 独立 ZIP/hash/SQLite 检查器复算 66/66 文件、52 条 portable paths 与全部计数，禁入项 0。未调用真实 restore endpoint；恢复成功与失败不覆盖由临时工作区 round-trip 和 mid-swap rollback 测试证明。下一步只做最终数据/服务/Git 守卫并收口 M119。
- 最终 SQLite 守卫首次误查 `research_runs.run_attempt`，实际字段为 `attempt`；只读脚本报 `no such column`，未发生写入。同条命令后续仍确认 API/Board 200、rollback archive 0、`git diff --check` exit 0、cached 0；下一步只用正确字段重跑数据库聚合与封存 Run 守卫。
- 正确字段的最终 SQLite 只读守卫通过：3 projects / 13 Runs / 13 completed / active 0；封存 Run 为 completed / attempt 0 / coverage_satisfied。API/Board 200、真实 rollback archive 0、`git diff --check` exit 0、cached 0，完整既有 dirty/untracked 工作树与新真实备份均保留。
- M119 已达 `implementation_complete`。最终权威门禁为 341 API / 112 Board / 165 Extension / 8 packaged E2E，全部静态/构建/process/security/evaluation 与 detector 0；真实备份下载、API 预检和独立 66-file/hash/SQLite 检查通过，真实 restore 未执行。未创建、取消或 retry Run，未恢复 Firecrawl，未截图，未执行 reset/checkout/clean/stage/commit/push。当前没有已批准的自动下一步，等待用户指定产品工作。

## M120 conservative workspace cleanup

- 用户批准清理工作区。M120 标记 `in_progress`，只删除仓库内可再生成的缓存、构建输出、临时日志和测试 XML；保留源码/测试/迁移/规划/发布证据、真实 `.archresearch` 数据、作品集 PNG、Impeccable 记录、M119 真实备份和所有用途不明的未跟踪项。禁止 `git clean/reset/checkout`、stage/commit/push。
- 初次候选体积统计脚本因 `foreach` 后直接管道触发 PowerShell `empty pipe element` 解析错误，未执行任何删除。下一步用数组收集结果后再格式化，不重复失败语句。
- 修正后的只读统计确认无争议缓存/构建/测试临时输出约 46 MB；`.archresearch/tool-bench` 另有 50,930 files / 2,495,562,275 bytes 的旧工具 benchmark 环境。冻结发布证据明确把两份 XML、`.playwright-cli` 和 extension live config 定义为可再生成本地输出，并把 10 张 portfolio PNG 单独列为必须保留的证据包。
- 清理目标加入旧 `.archresearch/screenshots` 12 图（不属于发布证据）与残留 `firecrawl.json`；保留 current logs/runtime/provider/browser token/dev-processes、durable DB/runs/collections/workspaces/exports、node_modules、work、Impeccable critique、全部源码与 M119 ZIP。下一步检查运行进程未引用 tool-bench，验证目标绝对路径后逐项删除。
- 19 个目标绝对路径均已验证位于当前 workspace，tool-bench 运行进程引用为 0。首次删除命令因把路径计算与多目标递归删除组合在一条 PowerShell 中被执行策略拦截，0 文件删除；下一步改用已验证的固定绝对路径逐项调用原生 `Remove-Item`。
- 固定路径 `Remove-Item -Recurse` 仍被执行策略拦截，0 删除；改用同一 PowerShell 进程的 .NET Directory/File API 处理已独立验证的固定绝对路径后成功。19 个目标全部消失，释放 2,543,468,519 bytes（2.369 GiB）。
- M120 已达 `implementation_complete`：API/Board 200，SQLite 仍为 3 projects / 13 Runs / 13 completed / active 0，封存 Run completed / attempt 0 / coverage_satisfied；10 张 portfolio PNG、M119 ZIP、critique、work、依赖和全部产品/规划资产保留。`git diff --check` exit 0、cached 0；未运行会重新生成缓存/dist 的测试，沿用清理前最终 341/112/165/8 全仓门禁。未使用 git clean/reset/checkout，未 stage/commit/push，未创建或 retry Run、未截图、未恢复 Firecrawl。
- M120 终检恢复期间，系统 `python` 首次命中 Microsoft Store alias 而失败，改用 Codex bundled Python 成功执行 planning catch-up；一次 HANDOFF 定位命令因 `M78` 多条匹配返回数组而失败，改为直接读取匹配整行；一次额外数据守卫误用不存在的 `/v1/runs` 集合端点得到 404，随后从 OpenAPI 发现并改用 `/v1/workspaces/{workspace_id}/runs`。三次均为只读错误，未改文件、运行态或数据。
- 最终轻量终检通过：`git diff --check` exit 0（仅 LF→CRLF 提示）、cached 0、`in_progress` 计划行 0、API/Board 200。只读 API 确认 3 projects / 13 Runs / 13 completed / active 0，封存 Run 为 completed / attempt 0 / stop_reason `coverage_satisfied`。M120 保持 `implementation_complete`，当前没有已批准的自动下一步，等待用户指定产品工作。

## M121 next-step planning

- 用户批准制定下一步计划，未批准直接实施。`planning-with-files` catch-up 成功；复核现有 roadmap 确认它仍停在 M113 时点，需按 M118–M120 已完成的运行恢复、备份恢复和清理成果重排未完成项。下一步只读审计目标用户验证、维护性和可重复发布三类实际缺口，不创建 Live Run、不改产品代码。
- 缺口审计确认三个单体热点为 App 3,930 行、CSS 4,325 行、workflow 4,716 行；CI 文件 0；现有 setup 只有本机脚本与文档，尚无 clean-machine/update 证明；测试依赖含 `pytest-cov` 但无统一覆盖率门限。M119 数据备份不覆盖当前巨大未提交源码状态，因此任何 Git 基线冻结都必须先获得用户独立授权。
- 已重写 completion roadmap：M121 目标用户小规模验证为 P0，M122 行为字符化与有界拆分为 P1，M123 clean-machine/CI/发布收口为 P2。三阶段均仅为 `proposed`，未标记 `in_progress`。计划明确 M119 备份不保护源码，所以继续大规模实施前需要用户单独授权源码基线冻结；当前仍不得 stage/commit/push。未创建 Live Run、未改产品代码或真实数据。

## M121 Xiaohongshu-derived proxy test

- 用户批准开始测试：先从小红书公开内容收集 8–10 条去个人信息的建筑学生问法，再以当前产品模拟真实问题。M121 标记 `in_progress`；本轮只验证问题兼容性、模式分流、拆解与结果质量，不冒充 2–3 名真实参与者的可用性观察。Live/provider 样本固定为最小代表集、逐条执行、active 不超过 1；封存 Run 不 retry，不截图。
- 一次命令帮助探测误用 `pnpm opencli -- --help`，OpenCLI 将 `--help` 解析为未知命令并 exit 2；未访问站点或写入状态。改用 `pnpm exec opencli xiaohongshu --help -f yaml` 成功读取只读命令合同。
- OpenCLI doctor 确认 daemon 19825 / v1.8.6 正常，但 Browser Bridge 未连接，因此本轮尚未执行小红书搜索、未创建产品 Run。下一步按技能后备路径连接用户 Chrome 并检查已有登录态；不截图。
- in-app Browser 打开小红书聚焦搜索页在 30 秒内超时并自动重置控制会话，未做任何页面写操作。Chrome browser binding 随后报 unavailable；按 troubleshooting 先做的两次轻量列表都只返回 in-app Browser。下一步执行技能规定的 Chrome 安装/运行/扩展/native host 只读检查；不自行启动 Chrome，不安装或修复扩展。
- 四项只读诊断通过安装链：Chrome 150 已安装，ChatGPT Chrome Extension 在 Default profile 已安装/enabled，native host manifest 和 registry 正确；唯一阻塞为 Chrome 当前未运行。根据 chrome skill，下一步必须请用户明确允许打开 Chrome 窗口，获批后只执行一次标准启动并重连。M121 保持 `in_progress`，尚未采集样本或创建 Live Run。
- 获批后执行一次 Chrome 启动；启动脚本未及时返回导致控制内核重置，但随后只读检查确认 Chrome 已运行且 browser binding 正常，因此未重复启动。
- 已在用户 Chrome 的小红书登录态中完成一次聚焦搜索，得到 9 个候选笔记 ID/标题方向。尚未将标题当成学生问题；下一步逐篇读取正文，排除代做、付费服务和纯情绪内容，保留可转化为产品输入的 8–10 条去个人信息问法。尚未创建 Live Run或截图。
- 已逐篇读取前两篇正文：XHS-P01 得到 2 条可用建筑研究问题（任务书→核心理念/案例分析，方案好坏的独立判断标准）；XHS-N01 确认为天正给排水/45° 轴测图生成命令，作为产品边界负样本保留。尚未创建 Live Run。
- 又读取 XHS-B01 与 XHS-N02：前者形成“100+ 页课设如何压缩成约 5 页作品集并保留设计逻辑”的具体但当前产品边界外问题；后者只有“设不出来/临近截止”情绪，应要求补充具体建筑与设计冲突。当前已有 2 条正样本问题 + 3 条边界/负样本，尚未创建 Live Run。
- 2026-07-26 恢复时完整复核 HANDOFF、`task_plan.md` 的 active/implementation-complete 阶段、findings/progress 末尾和 `git status --short --branch`；M121 仍为唯一 `in_progress`，分支 `codex/archresearch-v2-1` 的全部既有 dirty/untracked 资产保留。系统 `python` 再次命中 Microsoft Store alias，planning catch-up 未执行且没有原样重试；未发生产品或数据写入。
- 用户已明确允许继续已批准的 M121 小红书代理测试。读取 XHS-N03 后确认其为临近提交时的焦虑/回避支持边界，不适合任何产品 Live Run；随后候选 `6a2e5c42` 详情页读取在 60 秒内超时并重置 Chrome 控制内核，正文未取得、未计入样本。下一步重新绑定已运行的 Chrome，优先从现有搜索结果读取其他具体空间冲突；仍不重复启动 Chrome、不截图、不创建 Run。
- Chrome 重绑定后确认原窗口仍只有搜索页与 `6a2e5c42` 详情页，未重复启动窗口。该详情的整页文本与结构化内容导出又各自超时并重置控制内核；按不重复失败规则正式跳过该候选。下一步不再调用该详情页桥接读取，改用公开只读页面数据或搜索页中的其他候选。
- 公开搜索索引未返回可用 XHS 链接，且无登录 search HTML 只含客户端空壳；未据此生成问题。随后对 Chrome History 只做小红书 URL 前缀读取，找到此前公开访问过的完整 note token URL，不读取其他站点或账号数据。
- 通过公开 note SSR 数据取得 XHS-B02 完整标题/正文，形成合用前室消防合规边界样本；另一候选 `69d0f72e` 只有标题、无可验证正文，已跳过。当前可计语料为 7 条（2 正向、5 边界/负向），尚未创建 Live Run；下一步继续寻找至少 1 条适合案例研究的具体空间问题。
- 又从两篇公开 XHS note SSR 数据取得可验证正文，形成“既有建筑高差连廊”和“封闭公共建筑中庭采光/回游动线”两个正向案例研究问题。代理语料现为 9 条（4 正向、5 边界/负向），已达到 8–10 条采集标准；M121 仍为 `in_progress`，下一步筛选全部语料与当前启动器的模式/补充信息需求，再只选 1 条最强正向问题做 Quick Live。
- 已按当前启动器合同完成 9 条语料分流：3 条可直接建筑研究、1 条需先收束范围、5 条应补充信息或明确分流；没有图纸灵感正向样本。唯一 Live 选择 XHS-P02 高差连廊问题，XHS-P03 不另开 Run。
- Live 创建前只读预检通过：API health ok，3 projects / 13 completed Runs / active 0；“旧厂房社区文化中心”10 Runs、“苏州科技馆 · 蚕桑丝织智慧博物馆”1 Run、“历史研究归档”2 Runs，全部 active 0。下一步只向第一个活动项目 POST 一条全新 Quick attempt 0；任何失败不 retry、不补开第二条。
- 唯一 M121 Quick Live 已创建：`5e4184cf-c3e7-47ce-bc5d-8761bcc632a1`，question 为既有建筑高差连廊问题，goal `precedent_research`、attempt 0，初始状态 `created`。未附任务书、参考 URL 或 XHS source；从此只轮询该 Run，禁止第二次 POST 或 retry。
- 唯一 Run 依次经过 searching/inspecting/analyzing/verifying/gap_check 与缺口恢复，Trace 从 9 增长到至少 56 条，没有并发或 retry；最终 `completed / completion_satisfied / attempt 0`，26 usable、3/3、gaps 0。
- 终态只读审计：结果列表 4 个项目、26 个 partial assets、来源为 ArchDaily/Designboom；正式 coverage 只有 1 个项目，enrichment gaps 为项目多样性与多图项目不足。综合诚实声明高差、流线分离、全天候性能和新旧节点均无直接证据，因此流程跑通但实际回答质量不足，完成状态偏乐观。一次 PowerShell 结果汇总命令因缺右括号在解析阶段失败，修正表达式后成功，未发生写入。
- 当前数据库应为 3 projects / 14 completed Runs / active 0；下一步只做 loaded Board 无截图可见性和最终 Run/封存/Git 守卫，不创建第二条 Live、不修改产品代码。
- Loaded Board 无截图检查通过：首页正确项目出现最新 Run，打开后完整显示 3 个子问题、1 个正式项目、23 张仅预览线索和研究工具，页面无空白或流程中断。测试同时确认顶层“研究已完成/每题已有方案证据”和下层“1 个部分匹配项目/仍需核对”存在强度冲突。
- 结果页保留在 in-app Browser 供用户查看。尝试调用未暴露的 `markDeliverable` 失败；随后仅关闭 XHS 域标签的命令在 Chrome 桥接层超时并重置控制内核，关闭结果不确定，未原样重试、未影响 Run 或本地应用。
- M121 代理轮已完成但阶段保持 `in_progress`：代理问法不能替代 2–3 名真实建筑学生的任务完成、耗时、导航与理解观察。当前没有已批准的第二条 Live、自动产品修复或真实参与者下一步，等待用户指定。
- 最终只读守卫通过：3 projects / 14 Runs / 14 completed / active 0；M121 Run `5e4184cf-...` 为 completed / attempt 0 / completion_satisfied / 3/3 / project_count 1，封存 Run `10d31b4c-...` 仍 completed / attempt 0 / coverage_satisfied。`git diff --check` exit 0（仅既有 LF→CRLF 提示），cached 0，分支仍为 `codex/archresearch-v2-1`；所有既有修改与未跟踪文件保留，未执行 reset/checkout/clean/stage/commit/push。
## M121 proxy-result repair evaluation

- Completed a read-only implementation audit after the proxy test. The backend already separates minimum completion from enrichment completion; the evidence-strength conflict is caused by Board copy and counts, not by hidden synthesis or a broken Run lifecycle.
- Ranked the minimal Board projection fix above a backend terminal-state change. The proposed implementation keeps the completed result accessible, shows `研究已形成初步依据` when enrichment gaps remain, counts only text-evidence-qualified projects as formal cases and labels other assets separately as previews/leads.
- Added deterministic acceptance criteria to `task_plan.md`, including the M121-shaped regression, enrichment-complete control and visual/active/partial/blocked non-regression checks. No product code or tests were changed, no service/data mutation occurred, and no Live Run, retry, Firecrawl call or screenshot was made.
- M121 remains `in_progress` because the proxy round does not replace observation of 2–3 target users. The evaluated repair is not authorized for implementation; current next action remains waiting for the user to choose whether to implement it or proceed with the real-participant pilot.

## M124 full-result delivery and inline history

- User approved implementation and strengthened the contract beyond the M121 presentation-only proposal: completion now requires all coverage plus enrichment targets and active result content is withheld until terminal delivery. After a clarification screenshot, the UI target is the workspace-selection popover, not the complete Run-history dialog.
- Loaded `planning-with-files` and `impeccable`, completed session catch-up, and read the Board PRODUCT/DESIGN context plus product-register rules. M124 is now `in_progress`; implementation will begin with failing behavior tests and will not create/retry a Live Run, take screenshots or mutate durable data.
- Read-only implementation tracing identified the exact change surface: workflow normal/recovery completion predicates; Board active hydration; and Board history state/render/responsive CSS. Existing result persistence, history API and retention mutation can remain unchanged.
- First backend red test failed exactly at the intended contract: the incomplete-enrichment Quick fixture returned `completed` instead of required `partial`. The parallel harness stopped before preserving Board output, so Board red tests will be run separately; the backend command will not be repeated unchanged.
- First green attempt reached the required `partial` state but exposed a more precise terminal reason than the test assumed: the added recovery round found no new assets, so the existing workflow correctly used `no_new_assets` rather than generic `budget_exhausted`. Updated only that assertion; no production change was needed for this mismatch.
- Board behavior tests for inline 12-record history and active-result withholding passed after implementation. The suite then found one stale design-system assertion requiring the removed history dialog's mobile close button; it is replaced with the relevant 44px inline retention-action contract.
- Strict Quick enrichment used all five configured bounded rounds (15 subquestion queries), not the initially assumed three rounds/9 queries, before ending `partial/no_new_assets`. Updated the characterization to the actual configured ceiling; this is the intended proof that minimum coverage no longer returns early.
- The user clarified again that neither the workspace popover nor an inline “当前项目 / 历史归档” replacement should remain; the surface must lead directly with question records. M124 now targets a flat cross-workspace question history while preserving workspace ownership in the background. The strict-result changes remain.
- The first combined menu-removal patch matched an outdated import adjacency (`ChevronDown` was followed by `ChevronRight`), so `apply_patch` rejected the entire patch and changed no product file. Re-read the exact import/state/effect blocks and split the edit into smaller verified patches rather than repeating it.
- A Board run unintentionally executed the full suite because the package test wrapper did not apply the requested Vitest name filter. Fifty-two failures mostly shared one obsolete synchronization assumption: tests waited for the now-removed visible workspace name before interacting. Added that name only as a visually hidden loading/context marker, kept project taxonomy absent from the visual UI, and changed the new archived-record assertion to await the asynchronous cross-workspace merge.
- A follow-up combined patch used an invalid cross-file hunk delimiter and was rejected before changing files. Reapplied the App marker and test/progress updates as two valid patches instead of repeating the malformed patch.
- Correct Vitest filtering (`pnpm ... exec vitest`) showed the archived flat-record and active-withholding tests green. The remaining inline-history test read its first Run synchronously after only the workspace marker had loaded; changed that first assertion to await the deliberate asynchronous all-workspace Run merge.
- Full workflow characterization produced 9 failures under strict completion. Several fixtures intentionally stop below depth and should change to `partial`; the cross-question matrix should already satisfy its targets, so its assertion now includes the complete coverage report before deciding whether production scheduling regressed.
- 2026-07-26 interruption recovery re-read HANDOFF, the active plan context and recent findings/progress, then preserved the full dirty/untracked tree shown by `git status --short --branch`. The default `python` command again resolved to the Microsoft Store placeholder, so planning catch-up was run once with the bundled Codex Python and reported the unsynced M124 context; no product data or Run state changed.
- The latest user clarification is final for home history: no workspace selector and no visible “当前项目 / 历史归档” categories. Cross-workspace Runs appear directly by question-derived title, while opening one restores its workspace only in background state. Current source and three focused Board tests already implement this; remaining M124 work is stale-test/document alignment and full verification.
- Continuation recovery completed with bundled-Python catch-up, full HANDOFF review, active/implementation-complete plan review, recent findings/progress review and `git status --short --branch`; all dirty and untracked user assets remain untouched. Loaded no-screenshot DOM inspection confirms 14 flat question records and no visible project/archive taxonomy. Updated PRODUCT/DESIGN wording to the implemented cross-workspace question-history contract; M124 remains `in_progress` pending workflow-test and full-gate closeout.
- Workflow fixture alignment completed under the strict enrichment contract. The first authoritative gate exceeded its 120-second outer timeout; the next reached API 341/341 and stopped only on a 102-character test name. After shortening that name, Ruff passed and the final complete `scripts/verify.ps1` run exited 0: 341 API / 113 Board / 165 Extension / 8 packaged E2E plus all lint, format, type, build, process, security and evaluation checks.
- M124 loaded no-screenshot acceptance confirms one flat list of 14 cross-workspace question records, no visible workspace selector, “当前项目 / 历史归档” taxonomy, archive region or history dialog, exactly one “新建项目”, zero horizontal overflow and zero page errors. Opening a record only restores its workspace in background state.
- Final durable-state verification is 3 projects / 14 completed Runs / active 0. The first PowerShell summary misread the workspace arrays and was discarded; bundled Python confirmed the aggregate. Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` remains completed / attempt 0 / coverage_satisfied, permanently sealed and never retried.
- M124 is now `implementation_complete`. M121 remains `in_progress` only for observation of 2–3 real target users; its presentation repair was absorbed by M124. No Live Run, retry, Firecrawl call, screenshot, durable-data mutation, reset, checkout, clean, stage, commit or push occurred. There is no approved automatic next product step.

## M125 bounded recent-history viewport

- User feedback on the loaded flat list identifies two density defects: the complete 14-record history stretches the page, and the stacked header leaves excessive blank space above the first record. M125 is `in_progress` with a bounded, keyboard-scrollable record viewport and compact title/action header as the approved target.
- Scope remains Board-only and presentation-only. All records, retention actions, question-derived titles, opening behavior, background workspace synchronization and “新建项目” remain; no project/archive taxonomy, history dialog, Live Run, screenshot or durable-data mutation is permitted.
- Added behavior-first App/CSS contracts for a named focusable history region, compact two-column header and bounded native scrolling. Both focused tests first failed on the missing contracts, then passed after the minimal `App.tsx`/`styles.css` implementation. The first green App rerun exposed only a synchronous query during the legitimate loading state; changing it to await the region resolved the test without changing production.
- Complete Board verification passes 114/114 tests, ESLint, TypeScript and production build; Impeccable detector reports `[]`. Loaded no-screenshot DOM/geometry at default and 445×736 confirms all 14 records, 52.8px header, 400px bounded viewport, native overflow, keyboard focus contract, zero horizontal overflow and zero page errors.
- M125 is `implementation_complete`. The local page remains available at `http://127.0.0.1:5173/`; no Run, provider, Firecrawl, screenshot, durable-data or prohibited Git action occurred.

## M126 shorter recent-history viewport

- User requested one more density reduction after M125 loaded. M126 is `in_progress` and changes only the viewport cap to 320px / 45dvh, targeting roughly three visible records while preserving the full scrollable list and all behavior.
- Red test confirmed the old `400px / 55dvh` production cap. Updated the viewport CSS plus `apps/board/PRODUCT.md` and root `DESIGN.md` to `320px / 45dvh` and roughly three visible records.
- Impeccable setup first used an obsolete `taste-skill` path, then succeeded from the installed user-scoped path. Planning catch-up could not use the disabled Windows Store `python` alias; existing planning files were already synchronized and no project state was changed.
- Focused CSS behavior is green. Complete Board verification passes 114/114 tests, ESLint, TypeScript and production build; Impeccable detector reports `[]`.
- Loaded no-screenshot geometry at the 445×736 override confirms a 320px bounded viewport, 1,871px scroll content, all 14 records, page overflow 0 and page errors 0. The temporary viewport override was reset and the user's local Board tab remains open.
- M126 is `implementation_complete`. No Run, provider, Firecrawl, screenshot, durable-data mutation or prohibited Git action occurred; there is no approved automatic next product step.
- Final read-only state guard used raw JSON with explicit array preservation after discarding one invalid PowerShell-collapsed count: 3 projects / 14 Runs / 14 completed / active 0. The sealed Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` remains completed / attempt 0 / coverage_satisfied and was not retried.

## M127 restart-safe local availability

- User reported that the Board is unavailable after every restart and authorized a fix. Recovery used the app-owned Python runtime and confirmed planning files already contain the completed M126 state.
- Existing startup code already handles stale state, verifies workspace-owned listener PIDs and reuses a healthy API/Board pair. The missing behavior is automatic invocation after Windows sign-in; M127 is `in_progress` with behavior-first registration and real cold-start/reuse acceptance.
- Added the autostart behavior test first; it failed because `scripts/configure-autostart.ps1` did not exist, reproducing the missing activation path rather than another HTTP or listener defect.
- The first green attempt proved create/re-register/remove and correctly rejected an unmanaged shortcut, but the test harness did not catch the child PowerShell nonzero exit. Enabled native-command error propagation in the test only; production safety behavior was unchanged.
- Added `scripts/configure-autostart.ps1`, registered it in `scripts/verify.ps1`, and documented enable/disable plus restart recovery. Focused tests pass for create, idempotent re-registration, managed removal, unmanaged-shortcut refusal, process lifecycle and runtime resolution.
- Installed the real current-user startup shortcut. The first real `scripts/start.ps1` run replaced the stale prior-login state and restored API/Board 200; the second reported `already running` and retained listener PIDs 33660/26036. Durable state remains 3 projects / 14 Runs / active 0.
- Complete `scripts/verify.ps1` passed: 341 API, 114 Board, 165 Extension and 8 packaged E2E tests, all lint/format/type/build gates and the new autostart contract. M127 is `implementation_complete`; no Run, retry, Firecrawl, screenshot or durable-data mutation occurred.

## 2026-07-26 recovery checkpoint

- Reported the expected current state, absence of an approved automatic next step and recovery acceptance criteria before project actions.
- Read all 154 lines of `HANDOFF.md`; reviewed `task_plan.md` phase rows whose status is `in_progress` or `implementation_complete`; read the recent ends of `findings.md` and `progress.md`; then ran `git status --short --branch` as required.
- One read-only PowerShell chunk command failed during parsing because the parent shell expanded `$lines`. The corrected single-quoted inner command completed the full HANDOFF read; no project or runtime state changed.
- Bundled-Python `planning-with-files` catch-up completed with no unsynced output. Recovery confirms M77/M78/M93/M126/M127 `implementation_complete`, M121 as the sole `in_progress` phase, 3 projects / 14 Runs / active 0, and sealed Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` never to be retried.
- `git status --short --branch` remains on `codex/archresearch-v2-1` with the complete pre-existing modified/deleted/untracked tree preserved and no staged entries. No reset, checkout, clean, stage, commit, push, screenshot, Live Run, retry or Firecrawl action occurred. There is no approved automatic next product step; waiting for the user to specify product work.

## 2026-07-26 next-step planning

- User requested the next-step plan, not implementation. Loaded `planning-with-files`; bundled-Python catch-up reported only the just-completed recovery summary and this plan request as unsynced context.
- Read-only `git diff --stat` confirms the current source-risk shape remains 104 tracked paths with 27,097 insertions / 4,677 deletions plus separate untracked product assets. No Git write occurred.
- Refreshed the completion roadmap from the stale M120/341-112 baseline to the M127/341-114 baseline and recorded M121 as the sole active product-confidence gap.
- Defined the recommended next action as a 2–3-person, 8–10-task real target-user pilot with de-identified observation fields, severity/frequency thresholds and explicit no-screenshot/no-auto-Run/no-retry boundaries. M122 and M123 remain proposals only.
- Marked the older M121 Board-only repair proposal as historical and superseded by M124 so it cannot be mistaken for a current implementation option.
- No product code, test, service, durable data or Run was changed. No Firecrawl, screenshot, reset, checkout, clean, stage, commit or push occurred.

## 2026-07-26 M121 plan terminology correction

- User correctly flagged “来源核验” as a removed feature. Replaced every current-pilot occurrence of source/evidence inspection or source trust with result-state comprehension, result structure, history and collection continuity.
- Historical M113 records remain intact because they document the completed removal of source lookup/TinEye. No product code, browser state, service, durable data or Run changed.

## M128 answer-first research and collection reading surfaces

- User approved a UI redesign before the M121 pilot and supplied current screenshots of the architectural result and collection detail surfaces. M128 is `in_progress`; scope excludes drawing inspiration, APIs, schemas, exports, provider calls and durable data.
- Loaded `planning-with-files`, Impeccable `distill` and product-register guidance. Acceptance is behavior-first: useful results are readable without disclosure; research/source/provenance/audit copy is absent from both surfaces; desktop/390px no-screenshot QA, Board gates, detector 0 and the complete repository gate pass.
- Inspected both supplied screenshots at original resolution. The result page has five repeated reading stops per question and hides complete case content; the collection page disconnects title, solution, moves, images and actions across excessive horizontal space. External layout research will target continuous answer units rather than another card treatment.
- CollectUI root navigation and one focused search both timed out before returning content in the in-app browser. Neither call changed the local app or external state; browser retries are stopped and the research path moves to bounded public HTTP reads.
- Bounded HTTP confirmed CollectUI itself returns 200, while its server-rendered category page exposes no usable article/list/detail samples. Layout research is complete with a flat editorial case-answer direction: chapter conclusion once, then every case as project identity, mechanism, up to three actions, a short non-audit boundary and at most one recognition image. No browser retry or screenshot was used.
- Behavior-first tests failed against the old source/audit/disclosure UI as expected. Implemented the answer-first JSX/CSS and migrated superseded Board contracts: App 89/89, responsive design system 8/8 and TypeScript typecheck now pass. PRODUCT/DESIGN agree that architecture results and architecture collection expose no source, evidence, provenance, verification or research-process interaction; durable data, visual inspiration and export payloads remain intact.
- Re-ran the complete Board suite after removing the orphaned `tierLabels`; 114/114 passed and the Impeccable detector returned `[]`. Read-only `git diff --stat`, `git diff --check` and `git status --short --branch` preserved the existing dirty/untracked tree; diff check reported only existing LF→CRLF warnings.
- Loaded no-screenshot QA found legacy source-audit phrases still leaking through the completed Run projection. Added red tests for the actual templates, then filtered audit boundaries, removed recommendation provenance wrappers/tails, hid the completed architectural status strip and removed recognized publication suffixes from architectural display names. Three focused App tests pass.
- Responsive QA found the 390px result filter at 36px. Added a failing design-system contract, raised only `#asset-filter` to 44px on mobile and passed the focused test. Loaded 1440/390 result and collection geometry now has zero page overflow, clipped text, hidden cases, unloaded images or source/audit copy; collection delete is 44×44, console errors are 0, the viewport override is reset and the localhost Board tab remains open.
- The first complete Board rerun found four stale assertions that still required a completed status strip or audit-only limitation. Migrated only those expectations; the focused four-test rerun passed, followed by complete Board 115/115, ESLint, TypeScript, production build and Impeccable detector `[]`.
- Complete `scripts/verify.ps1` exited 0 after 267.9s: 341 API, 115 Board, 165 Extension and 8 packaged E2E tests passed with all format, lint, type, build, process, security and evaluation gates. `git diff --check` remains clean apart from existing LF→CRLF warnings and staged output remains empty.
- Discarded one invalid PowerShell state aggregate that treated API arrays as one wrapper object. A distinct raw-JSON pass with explicit list flattening confirms 3 projects / 14 Runs / 14 completed / active 0; sealed Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` is the sole match and remains completed / attempt 0 / coverage_satisfied. M128 is `implementation_complete`; no Live Run, retry, Firecrawl call, screenshot, durable-data mutation, reset, checkout, clean, stage, commit or push occurred. There is no approved automatic next product step.

## 2026-07-26 完成度检查与保留期修复

- 用户要求检查项目完成度。只读复验（`mode=ro` SQLite、health、OpenAPI、`pytest --collect-only`）确认 3 workspaces / 14 completed Runs / active 0、341 API 测试与记录一致，并刷新了过时的 M122 文件规模与 diff 规模。
- 审计发现 P0：14 条 Run 全部 `keep_forever=0`，12 条在 2026-08-03 同时到期，而 `cleanup_expired_data` 在每次 API 启动执行硬删除，M127 开机自启会在每次登录触发它。8 天后的首次无人值守登录就会删掉封存 Run 与全部验收证据；留存的 M119 备份因保存已过期时间戳而无法保护。
- 用户批准后，用既有 `PATCH /v1/runs/{id}/retention` 把 14 条 Run 标为永久保留，14/14 返回 200。独立只读复读为 `keep_forever=1` / 到期时间为空，按 `lifecycle.py` 原谓词实测可删除 Run 归零。未改代码、schema 或迁移，可逐条撤销。
- 更正一处自述：Board 最近研究每条记录本就显示"还剩 N 天 / 设为永久"（`App.tsx:585-594`），先前"无警告"的说法过重；缺口在于验收证据与普通 Run 无区分、无集中到期提示、删除后无撤销。
- 未创建、重试或取消任何 Run，未调用 Provider/Firecrawl，未截图，未执行 reset/checkout/clean/stage/commit/push；staged 仍为 0。

## M129 source baseline protection

- 用户批准按 A→B→C 顺序推进，A 为源码基线保护，并要求用 planning-with-files 记录进度。`session-catchup.py` 报告无未同步上下文，说明规划文件与上一会话一致。
- 只读重算边界：104 个已跟踪改动路径（95 modified / 5 deleted）+27,421 −4,788，加 26 个未跟踪文件，共 130 个路径全部分类为 112 产品 / 5 工程记录 / 11 发布证据 / 2 排除的本地输出。
- `.gitignore` 精确补入 `.impeccable/` 与 `.artifacts/*.zip`，未跟踪条目从 26 降为 24，`git check-ignore` 确认 70 MB 数据备份 ZIP 与 Impeccable critique 已排除，`.artifacts/portfolio/` 仍可见。
- 凭据扫描覆盖全部已跟踪 diff 新增行与 14 个未跟踪候选，高置信命中 0；生产源码 Firecrawl/TinEye 引用 0，绝对用户路径泄漏 0。
- 首次完整门禁失败于 `Resolve-WorkspaceRuntime` 的 "pnpm was not found"。当时记为“调用方式问题”，该判断错误：本机确实没有 pnpm，是并发的 Codex 会话在此期间装上 `pnpm@11.7.0`，后续前台执行才解析成功。教训是失败原因必须落到可验证的事实，不能用“换种调用方式就好了”替代根因。
- 前台门禁随后停在 `scripts/tests/process-lifecycle.tests.ps1`：`Get-CimInstance Win32_Process` 抛 `Microsoft.Management.Infrastructure.Native` 类型初始化异常，禁用沙箱后同样失败。当时只能判定为本地环境限制，改为逐步执行其余门禁并显式记录每步退出码，不修改产品代码去迁就环境；根因随后由 Codex 定位为 MSIX 版 pwsh 7.6.4 加载 MMI 原生 DLL 被拒，且它会让 `start.ps1` 杀掉自己刚启动的服务。
- 其余门禁全绿且逐步 exit=0：dev-common 测试、Provider 安全契约、autostart 测试、评测夹具、341 API、Ruff check、Ruff format 51 files、strict Mypy 19 files、根 `pnpm run check`（含 115 Board / 165 Extension）与 8 packaged Chrome E2E。
- 用户在明确的授权确认中选择“两次提交”并选择暂不纳入发布证据。按精确 pathspec 文件 stage 112 个产品路径（12 A / 5 D / 95 M），核验无 `.artifacts`、`.impeccable`、工程记录或发布证据泄漏后创建产品基线提交 `d772902`；全程未使用 `git add -A`，未 push。
- 提交后立即发现共享工作区并发写入：`git status` 重新出现 `scripts/dev-common.ps1` 与 `scripts/tests/process-lifecycle.tests.ps1` 修改，mtime 为 20:03:44 / 20:03:14，`dev-common.ps1` 由提交内 269 行增长到 431 行，方向是移除监听检测的 WMI 依赖。据此暂停第二次提交：工程记录正是双方共同维护的文件，此刻提交会捕获撕裂状态。
- 用户批准等待并发轮次结束后再收口。20:20 起文件 mtime 连续 12 分钟不变，判定该轮已结束；Codex 的记录显示它已用完整 `scripts/verify.ps1`（退出码 0，341/115/165/8，四个 PowerShell 套件全绿）验证过当前工作树。
- 独立复验通过：`scripts/dev-common.ps1`、`start.ps1`、`stop.ps1` 中 `Get-CimInstance`/`Get-WmiObject`/`Get-NetTCPConnection` 命中 0；先前在本 shell 无法执行的 `scripts/tests/process-lifecycle.tests.ps1` 现在 exit=0 通过。M129 门禁的唯一缺口因此闭合。
- 按已批准的分层边界收口为两次提交而不是混合提交：`scripts/dev-common.ps1` 与 `scripts/tests/process-lifecycle.tests.ps1` 属产品层，单独提交；`AGENTS.md`、`HANDOFF.md`、`task_plan.md`、`findings.md`、`progress.md` 属工程记录层，单独提交。产品改动与记录改动保持可分别回退。
- 基线为 `98a9a01` → `d772902` 产品基线 → `06f3424` WMI-free listener 修复 → `775c4b7` 工程记录，均为本地提交，未 push。提交后工作树只剩两个有意排除的未跟踪条目：`.artifacts/`（10 张 portfolio PNG，数据备份 ZIP 已 ignore）与 `docs/release-evidence-2026-07-16.md`。
- 收口后在本会话执行了一次端到端权威门禁 `scripts/verify.ps1`，退出码 0：dev-common、Provider 安全契约、process-lifecycle、autostart 四个 PowerShell 套件全绿，341 API、115 Board、165 Extension、8 packaged Chrome E2E 全通过，Ruff、strict Mypy 19 files、两端 lint/typecheck/build 与评测夹具均通过。这是 WMI 修复后本 shell 首次完整跑通 `verify.ps1`。
- 只读复验 durable 数据未变：3 workspaces / 14 Runs 全部 completed / active 0，`keep_forever=1` 14/14，封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍为 completed / attempt 0 / coverage_satisfied。`git diff --check` 除既有 LF→CRLF 提示外无问题。
- 本轮未创建、重试或取消任何 Run，未调用 Provider/Firecrawl，未恢复 Firecrawl，未截图，未改动 durable 数据，未执行 reset/checkout/clean/push。

## M129 无 WMI 依赖的本地服务启停

- 用户报告 `127.0.0.1` 打不开。日志显示服务正常运行到 14:43 后中断且无报错，`.archresearch/dev-processes.json` 已不存在，端口空闲，判定为进程被停掉而非产品代码故障。
- 重启时暴露两个真实故障。其一，`Resolve-WorkspaceRuntime` 抛 "pnpm was not found"：此前的 pnpm 由 Codex 运行时提供，本机从未安装。按根 `package.json` 的 `packageManager` 安装 `pnpm@11.7.0`（`%APPDATA%\npm`），属环境修复，未改仓库文件。
- 其二，装好 pnpm 后 `start.ps1` 仍失败：API 与 vite 都已就绪，脚本在 `start.ps1:94` 的 `Get-CimInstance` 归属校验处抛 `DllNotFoundException … E_ACCESSDENIED`，catch 分支随即把自己刚启动的两个服务 `Stop-Process -Force`。已确认 Winmgmt 服务正常、Windows PowerShell 5.1 的 WMI 正常、独立新起的 pwsh 同样失败，故障域限定在 MSIX 版 pwsh 7.6.4 这一个安装。
- 按仓库规则先红后绿：现有 `process-lifecycle.tests.ps1` 在本机整体失败即为初始红灯；随后把测试自身的 `Get-NetTCPConnection` 助手换成 `Get-TcpListeningProcessIds`，并新增两条断言——工作区子进程的命令行必须在不经 WMI 的前提下可读且指向工作区，`dev-common.ps1` 不得出现 `Get-CimInstance`/`Get-WmiObject`/`Get-NetTCPConnection`。
- 实现只替换两个探测原语：监听进程改用 `netstat -ano`（按环回端点与未连接对端地址判定，不依赖本地化状态词），命令行改用 `NtQueryInformationProcess` 读 PEB，`Add-Type` 惰性编译一次。函数签名、`Test-CommandLineReferencesWorkspace` 的归属语义和 `start.ps1`/`stop.ps1` 的调用点均未改动。`process-lifecycle.tests.ps1` 转绿。
- 真实生命周期复验通过：`Get-WorkspaceListeningProcessIds` 正确认出 vite(node, PID 29284) 与 uvicorn(python, PID 37856)；`Stop-WorkspaceTcpListeners` 只停这两个；`start.ps1` 正常输出 Board/API 地址并写入 state（pid 与 launcher_pid 区分正确），重复执行走 "already running" 复用分支，`stop.ps1` 停净并删除 state，再次 `start.ps1` 恢复服务，Board 与 API 均返回 200。
- 未改动产品代码、schema、迁移、durable data；未创建/重试/取消任何 Run，未调用 Provider 或 Firecrawl，未截图，未执行 reset/checkout/clean/stage/commit/push。
- 完整门禁 `scripts/verify.ps1` 退出码 0：341 API、115 Board、165 Extension、8 packaged Chrome E2E 全通过，dev-common / process-lifecycle / autostart / provider 安全契约四个 PowerShell 套件全绿，Ruff、Ruff format、strict Mypy（19 source files）、两端 lint/typecheck/build 与 evaluation fixtures 均通过。与 M128 基线数字一致，说明改动没有影响任何产品行为。
- 门禁跑完后本地服务仍在运行（Board 200 / API 200，state 记录 pid 与 launcher_pid 正常），`git diff --check` 除既有 LF→CRLF 提示外无问题，staged 仍为 0。
