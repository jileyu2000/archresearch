# Findings

## Environment

- Workspace started empty except for `work/` and `outputs/`.
- No existing Git repository or code needs preservation.
- Bundled runtimes are available for Python, Node.js, pnpm, documents, and browser testing.
- Runtime versions: Node.js 24.14.0, pnpm 11.7.0, Python 3.12.13.
- The bundled Python interpreter does not preinstall FastAPI; the API app must declare/install its own locked development dependencies in a workspace virtual environment.

## Approved Product Decisions

- Runtime: local Windows/Chrome single-user application.
- Surfaces: Chrome MV3 extension + local board + local FastAPI executor.
- Storage: SQLite and local files only.
- Search: OpenAI web search for text/image discovery; Chrome for page inspection; TinEye for reverse image lookup.
- Result delivery: verified, partial, and visual-lead tiers; private full export and rights-filtered share export.
- No platform case library, global index, or cross-workspace retrieval.

## Frontend Direction

- Subject: an architectural evidence desk, not a chat application.
- Layout: compact top bar, one research composer and a flat drawing grid. Workspaces use a selector; evidence and Trace use contextual drawers; compare actions appear only after selection.
- Palette: vellum `#F3F5F2`, graphite `#171B19`, blueprint `#315CF4`, signal red `#E4583E`, evidence green `#2D846B`, fog `#DDE3DF`.
- Type: system Chinese sans for readability, condensed/mono utility labels for drawing metadata.
- Default disclosure: cards show the drawing, project, type, evidence tier and one useful sentence; provenance, claims, boundaries, notes, StyleProfile, exports and Trace stay one explicit action away.
- Avoid permanent side rails, repeated filter buttons, generic rounded SaaS cards, decorative gradients, and chat-first layout.

## M7 Usability Simplification

- The current 1330 px screenshot exposes four permanent regions, nine asset filters, full evidence metadata, four evidence prose blocks, comparison/export controls and the stage rail at once. The feature set is valid, but the default disclosure level is too high for first-time users.
- The primary task is singular: describe a design problem and review useful drawings. Workspace administration, evidence audit, comparison, StyleProfile, exports and Trace are secondary and should not compete with that path.
- Mature references support a simpler pattern: Perplexica centers one research prompt and defers modes; Zotero keeps the item list primary and details contextual; Karakeep shows an immediate asset collection with filters and metadata available on demand.
- The redesign should use a compact top bar plus a single results canvas. Workspaces become a menu, asset filters become one horizontal segmented row with an overflow menu, and source evidence becomes a contextual drawer opened from the selected result.
- Result cards should show only image, project, asset type, evidence tier and one useful sentence by default. Detailed source status, facts, observations, inferences, boundaries, notes and evidence locators remain available in the drawer.
- Advanced actions remain complete but move behind clear entry points: compare selection in a bottom action bar, StyleProfile/Trace/export in a tools menu, and research progress in a compact status strip.
- Accepted implementation: the default result screen contains only the workspace selector, new-research action, tools menu, one asset-type select and a flat reference grid. Source evidence opens from a card; advanced research inputs stay collapsed; cancel/retry remain directly reachable from the run status.
- Responsive acceptance passed at 1440, 1024 and 390 px: four/three/one-column grids, no document-level horizontal overflow, full-width mobile evidence drawer, no permanent inspector or stage navigation, and no browser console warnings or errors.
- The legacy `diagram` label remains readable but is merged into the single `analysis_diagram` filter option, preventing duplicate “分析图” choices.

## M8 Mature-product UI study

- Karakeep's strongest transferable pattern is an image-first masonry collection: mixed-height assets fill the canvas, metadata is quiet, actions live at card edges, and navigation/search remain stable. ArchResearch should borrow the browsing rhythm, not its generic bookmark taxonomy.
- Linkwarden confirms that a permanent full dashboard, metric tiles and a dark shell would be wrong for this product. Architecture drawings need a light inspection surface and should not compete with summary statistics.
- Vane/Perplexica confirms that sources and research progress work best as compact, collapsible context near the active question. ArchResearch should keep evidence one action away and avoid turning the result board into a prose answer page.
- Chosen direction: a restrained light asset browser with a slim utility rail, a command-like research bar, a masonry drawing wall and a contextual evidence drawer. The distinctive domain cue is a “digital pin-up wall”: drawing sheets retain their native aspect ratios, hover lifts only the active sheet, and evidence appears as small source pins rather than decorative borders.
- Palette: neutral studio canvas `#EEF0ED`, paper `#FFFFFF`, graphite `#171A18`, secondary ink `#5E6661`, blueprint action `#2F5BFF`, verified green `#1F7A5A`, and warning vermilion `#D6533C`. Color stays restrained and functional.
- UI typography remains a single system sans family for trust and speed. Drawings, project titles and one method sentence form the hierarchy; uppercase utility labels and mono metadata are reduced rather than used as a visual theme.
- First 1440 px implementation check confirms the intended asset-browser hierarchy: four masonry columns, drawing sheets with varied heights, compact source pins, icon actions, no permanent inspector and no horizontal overflow. The previous engineering-table feel is gone.
- The first visual pass also exposed two token-alignment refinements: the sticky header should lose its redundant visible “工作区” field label to approach the documented compact shell height, and card method text should use the documented 13px compact-body tier instead of 12px.
- The refined mobile pass is a clean single-column drawing feed with the workspace on its own second row, no horizontal overflow and intact evidence pins. The remaining concrete mobile issue is the 30px compare icon target; the design system requires a 44px touch target below 620px.
- The consistency audit found that the repository had both a legacy `apps/board/DESIGN.md` and the new root `DESIGN.md`. M8 removes the legacy file so the root system is the single inherited authority for Board and later extension surfaces.
- The 1024 px pass showed why generic CSS masonry is wrong for ranked evidence: column-first flow visually placed result 2 below results 3/5. The final system uses an ordered 1/2/3/4-column pin-up grid with intrinsic image ratios, so browsing stays lively without sacrificing relevance order.
- Final ordered-grid checks pass at 1024 and 390 px: result order remains 1→2→3→4, the grid resolves to three/one columns, mobile compare targets are 44px, and neither viewport has document-level horizontal overflow. The first eight images load eagerly so the initial desktop and tablet wall does not reveal blank lazy placeholders; later results remain lazy.
- The source drawer passes the mobile interaction check at full viewport width with no overflow, initial focus on “关闭”, Escape support and return focus covered by the Board test. The final 1440 px wall resolves to four ordered columns, a 60px shell and zero console warnings/errors.
- The final Impeccable design-system detector returns an empty finding set: CSS no longer uses an undocumented font, radius or color, and the root `DESIGN.md` is the only design authority.

## External Capability Facts

- OpenAI Responses web search supports text and image results, raw result metadata, source lists, and domain filters.
- OpenAI strict Pydantic parsing and cropped `input_image` requests support the research and nine-class visual contracts; this project now defaults both paths to `gpt-5.5`.
- The 梭子蟹 relay configuration uses `https://suoxie.codes/v1` with API model `gpt-5.5`; the `suoxie/gpt-5.5` prefix belongs only to OpenCode routing. ArchResearch stores its Key under Windows Credential Manager service `ArchResearch/suoxie`, account `api-key`.
- Windows PowerShell 5.1 can misdecode UTF-8 scripts without a BOM. Security-sensitive executable PowerShell scripts are kept ASCII-safe and parser-tested under both Windows PowerShell 5.1 and PowerShell 7.
- The user-supplied 梭子蟹 credential passed the live Responses + `web_search` capability probe. The committed local configuration selects `gpt-5.5` for both research and visual classification; the credential is present only in Windows Credential Manager.
- TinEye uses `https://api.tineye.com/rest/`, an `x-api-key` header, and returns matches with backlink page URLs; crawl dates are not publication dates and are not persisted as evidence.
- Chrome MV3 supports optional host permissions; extension code must be bundled and cannot execute remotely hosted code.
- Playwright tests MV3 extensions through a persistent Chromium context.

## Implemented Boundaries

- Browser pairing rotates a one-time code to a persistent token; only its SHA-256 digest is stored by the API.
- Browser commands are enumerated and payload-strict. Generic `safe_click` is a no-op on untrusted pages.
- Candidate inspection sends at most three cropped regions per page to the visual classifier; full pages and image data are excluded from Trace.
- API DNS resolution rejects failed, private, reserved, and IPv4-mapped private A/AAAA results before navigation; the extension rechecks final URLs.
- Saved-reference snapshots survive temporary candidate cleanup. Startup cleanup removes expired candidates, source metadata, queries, and Trace, then resumes active runs.
- Board and API currently run as user-session background processes, not Windows services. A reboot or host process cleanup can leave the saved launcher state behind while ports 5173/8000 are no longer listening; `scripts/start.ps1` safely recreates both listeners and rewrites the state file.

## M6 Delivery Status

- The evidence-desk layout passed screenshot acceptance at 1440, 1024 and 390 px with seven inspectable drawing assets, automatic first-result selection, no document-level overflow and no console errors.
- The real Board restores SQLite runs, evidence, saved/rejected state, notes, comparison selection, StyleProfile and Trace. Historical asset labels normalize through the nine-class read contract, and `has_local_content` prevents nonexistent crop requests.
- The first live `gpt-5.5` Quick run produced eight unique source leads and reached `coverage_satisfied`; its versioned report explicitly records that the browser was unavailable and no drawing crops were produced.
- A separate packaged-Chrome replay now proves the missing half of that loop: real FastAPI WebSocket pairing, fixed-page inspection, `captureVisibleTab`, PNG persistence, content delivery and terminal permission revocation.
- Chrome requires task-scoped optional `<all_urls>` for continuous `captureVisibleTab`; no permanent host permission or manifest content script exists, and API/protocol/final-tab tests still reject `file:`, `ftp:`, `chrome:`, loopback, private and reserved destinations.
- Uvicorn declares `websockets>=14,<16`, so the production launcher supports the same real network handshake exercised by E2E.
- `scripts/verify.ps1` is the complete offline delivery gate: 105 API tests, 26 Board tests, 111 extension tests, 6 packaged Chrome E2E cases, lint/type/build checks, PowerShell safety/process tests, 30 research tasks and 108 deterministic classification samples.
- Remaining acceptance is external rather than unfinished code: a user-provided TinEye live test, opt-in paid execution and human labeling of 30 live tasks, 100+ independent rights-cleared real images, six-student usability research and a normal-profile logged-in Chrome run.

## Open-source reference study (M6)

- LangChain `open_deep_research` validates a bounded research loop with separate summarization/research/compression roles, explicit model/tool configuration, source-aware outputs, and benchmarkable run records. ArchResearch should borrow its observable stages and evaluation record shape, but keep the approved deterministic single-run state machine instead of adopting LangGraph or a multi-agent runtime.
- Karakeep demonstrates mature mixed-asset handling: links, images, PDFs, automatic metadata fetching, OCR, lists, highlights, browser capture, two-phase fetching, skeleton states, bulk actions, archival and SSRF hardening. ArchResearch should borrow its immediate-item-first loading, persistent user organization and resilient asset preview patterns, not its global bookmark library or server-first architecture.
- AFFiNE demonstrates a local-first edgeless canvas that mixes images, notes, embeds and database views. ArchResearch should borrow direct manipulation and multiple views of the same selected references, while retaining a constrained evidence-board layout rather than a general-purpose infinite canvas.
- Browsertrix demonstrates crawl jobs as first-class resumable objects with start/schedule/manage/share states and high-fidelity browser capture. ArchResearch should borrow visible job status, recoverable failures and explicit scope, not its Kubernetes/cloud crawl infrastructure.
- Zotero/Better Notes patterns reinforce keeping metadata, annotations and source navigation bound together. ArchResearch evidence cards should make every supported fact jump to its URL/PDF/page/region and keep notes user-editable without rewriting provenance.
- Perplexica provides a useful real-time search UX reference—query expansion/focus modes, current-web results, image search and citations—but ArchResearch should avoid collapsing the output back into a chat answer; the board and evidence hierarchy remain primary.
- GPT Researcher's official event model supports query/source/image/tool events and downloadable logs. ArchResearch will expose only the eight user stages while keeping URLs, retries, timing and costs inside the expandable Trace.
- STORM's useful transferable idea is coverage-driven follow-up from distinct viewpoints. Here those viewpoints become design strategy, plan organization, section space, circulation, adaptive reuse and drawing expression; no simulated personas or multi-agent runtime are needed.
- Linkwarden's orthogonal search/filter/sort/bulk-action views map well to Run-as-temporary-collection plus asset type, evidence tier, project and rights filters. ArchResearch will not add permanent page archives or a global library.
- Excalidraw and tldraw validate separating durable document assets from transient selection/session state. The first board remains a constrained evidence grid with persisted comparison selection; a general canvas dependency is unnecessary, and tldraw's current production-license restriction rules it out.
- Chrome's official permissions lifecycle requires `permissions.request()` from an extension user gesture and `permissions.remove()` when the task ends. The extension side panel therefore remains the explicit “authorize and start” surface; the localhost board cannot silently grant host access.

## M9 Problem-first navigation contract

- The default `/` surface did contain a question field, but `hydrateRun()` automatically collapsed it as soon as the latest completed run restored. This made a persisted result page silently replace the homepage and caused the user's confusion.
- A completed historical run is now data restored in the background, not the current view. The homepage remains a single problem composer and exposes “查看上次结果” only when history exists; the result view exposes the inverse “发起新研究” transition.
- An unfinished run is the intentional exception: it resumes directly into progress/results so cancellation and recovery remain visible. Explicit `?demo=1` continues to open a result demonstration.
- “研究结果” names the result section; “图纸类型” remains only a filter. The accessible collection name is “研究结果列表”, and the shared page container is “研究工作区”.

## M10 Homepage audit

- Audit scope: the logged-in `/` workbench at 1280×720. The user goal is to state a concrete architecture problem, understand what kinds of research are possible, and reopen prior work without landing in a full result wall.
- Strengths: the screen has one unmistakable primary action, a properly labelled question field, a visible workspace switcher and an explicit route to the previous result.
- Structural risk: after the composer, roughly two thirds of the viewport is inert canvas. The page tells users where to type but does not teach the three supported research goals, provide varied architecture-specific starting points or make previous work legible in context.
- The single example sentence forces all onboarding into placeholder text, which disappears as soon as the user types. “查看上次结果” in the far header is useful navigation but too weak to function as a recent-work surface.
- The accepted direction is a workbench home, not a marketing landing: one dominant composer, three compact goal paths, four problem starters and a small recent-research list. Full result cards, feature sales copy, dashboards and generic “how it works” panels remain excluded.
- Accessibility evidence from the current DOM confirms a labelled textarea and named buttons. Screenshot evidence alone cannot establish keyboard order, focus visibility, contrast or zoom resilience; those remain implementation checks.

## M10 Mature-product homepage patterns

- Perplexity starts a session from the homepage query box and keeps search mode, source choice and file attachment adjacent to that primary input; durable sessions move to History rather than remaining as full answers on the homepage. Source: https://www.perplexity.ai/help-center/en/articles/10354769-what-is-a-thread
- NotebookLM pairs a “create/find sources” prompt with named recent-work groupings such as Recent, Shared and Title. Its source-search results explain why each source relates before import. This supports a compact recent-research section below ArchResearch's composer, not a restored result wall. Source: https://support.google.com/notebooklm/answer/16296687
- Figma's file browser makes Recents a first-class return path and keeps templates/community discovery nearby. The transferable idea is “continue work + start from a known pattern”; ArchResearch should use architecture problem starters instead of generic templates or a marketplace. Source: https://help.figma.com/hc/en-us/articles/14381406380183-Guide-to-the-file-browser
- Notion places its AI search module at the top of Home while search surfaces recently viewed pages for immediate return. The transferable idea is to combine one dominant action with contextual recent work, without visually equating both. Source: https://www.notion.com/help/search
- Linear team home uses pinned resources and shortcuts to common destinations. For a single-user architecture tool, three goal-specific research paths are enough; adding a permanent sidebar, team metrics or configurable dashboard would be unnecessary. Source: https://linear.app/docs/default-team-pages
- Combined pattern: one primary composer, lightweight mode affordances next to it, 3–4 domain-specific starter questions, and recent work below. This is materially more useful than a lone hero form but remains a workbench rather than a marketing homepage.

## M10 Accepted workbench home

- The homepage now combines one dominant problem composer with three visible research paths, four architecture-specific problem starters and up to three recent task summaries. Full drawing cards and evidence remain exclusive to the result view.
- Completed historical runs no longer trigger result/board/evidence hydration during bootstrap. Opening “查看上次结果” or a recent-task row loads the selected run on demand, while unfinished runs still resume automatically.
- The starter rows fill and focus the question without submitting; research-path buttons expose pressed state and remain in the request contract. Attachment fields and research depth stay progressively disclosed.
- The home follows the global 4/8px rhythm, 960px work surface, flat structure-line hierarchy and one blue primary action. Static composer shadow was removed; the lower index uses open rows rather than generic feature cards.
- Every research path now has a specific prompt, starter actions say that they fill the question, and global geometry is aligned to the documented 36px controls, 16/20px icons and 8px control radius.
- Historical hydration, terminal-result hydration, new-run submission, active polling, cancellation and retry are request-scoped. Switching workspaces cannot revive an old poll, inject a late response, close the new home or flash the previous result wall; a completed in-flight poll cannot overwrite a successful cancellation.
- Browser acceptance passed at 1280×720, 1024×768, 700×800 and 390×844 with no horizontal overflow. The 700px footer reflows before controls collide; mobile research-path, attachment and submit targets are at least 44px high. The before/after comparison confirms that the former inert canvas is now used for starting and continuing work without becoming a result wall.
- Repository verification remains green: 105 API tests, 35 Board tests, 111 extension tests, 6 packaged Chrome E2E cases, lint/type/build checks, PowerShell contracts and all 30/108 evaluation fixtures. The design detector reports no findings and the muted prompt text measures 5.25:1 contrast.

## M11 Livelier visual direction

- The current homepage logic is accepted, but its all-neutral flat surfaces, medium-scale heading and evenly weighted rows make it read as a tidy form rather than a creative architectural workbench.
- The requested change is visual, not structural: preserve the single composer, three research modes, four problem starters, recent research and explicit result transition.
- Milanote makes creative work approachable by letting mixed visual materials and flexible boards carry the product identity; the transferable lesson is a visible creative working surface, not more dashboard chrome. Source: https://milanote.com/
- FigJam uses a friendly whiteboard metaphor, direct “start a whiteboard” action and colorful working artifacts to reduce the seriousness of an otherwise complex tool. ArchResearch should borrow the immediate, welcoming task surface while keeping its deterministic research flow. Source: https://www.figma.com/figjam/online-whiteboard/
- Are.na organizes research as understandable blocks and channels and frames the product around learning and connection. The transferable lesson is to make starting points feel like material on a desk rather than feature cards. Sources: https://www.are.na/about and https://www.are.na/editorial/information-systems
- Recommended signature: one committed blueprint-blue composer zone with a white working form inset, supported by stronger type scale and livelier starter-row states. The rest of the page stays quiet so the focal surface reads as intentional rather than decorative.
- This is an explicit design-system evolution: the homepage may use one committed blueprint surface for the active task, while result/evidence screens retain the existing restrained color rule so drawings and provenance remain primary.
- Miro's current dashboard documentation confirms the durable hierarchy: create, search and recent boards are first-class dashboard actions, while templates remain a secondary exploration path. ArchResearch should likewise keep one strong research start and a quieter recent-work area. Source: https://help.miro.com/hc/en-us/articles/360017571294-What-is-on-your-dashboard
- FigJam's official surface uses direct starts, recognizable template categories and playful collaboration artifacts to make a complex tool feel approachable. The transferable part is friendly, task-bound feedback around concrete starts, not decorative animation across the whole page. Source: https://www.figma.com/figjam/online-whiteboard/
- React Bits is a copy-ready animated React component collection with TypeScript/CSS variants. Its repository is licensed under MIT plus Commons Clause, so an adapted in-product component is suitable, but the license notice and source attribution must be retained. Sources: https://reactbits.dev/get-started/index and https://github.com/DavidHDev/react-bits
- Motion was selected in the product-first order requested by the user. FigJam and Miro use short, event-bound reaction bursts; Milanote and Eagle use motion to preserve continuity during direct manipulation or reveal; Cosmos uses ambient image motion mainly to establish a marketing atmosphere. Sources: https://help.figma.com/hc/en-us/articles/1500004290981-Stamps-emotes-and-high-fives, https://help.miro.com/hc/en-us/articles/360021249320-Reactions, https://help.milanote.com/en/articles/491831-moving-content-between-boards, https://www.eagle.cool/ and https://www.cosmos.so/
- The resulting rule is “action response, not ambient display”: no orbiting imagery, cursor trails, magnetic controls, glare or page-load choreography on the research workbench.
- React Bits `ClickSpark` is retained only on the unique “开始研究” action. It uses six lines, a 12px radius and 300ms lifetime, fires only after a complete pointer click on an interactive target, starts no idle animation loop and disappears entirely under `prefers-reduced-motion`.
- React Bits `AnimatedContent` was inspected as the closest mapping for Milanote-style state continuity, but its GSAP + ScrollTrigger implementation is unnecessary for one local expansion. ArchResearch uses the same low-displacement idea only when “添加资料和研究设置” appears: native CSS, 8px to 0, 220ms, no blur and no scroll trigger.
- A second decorative component is intentionally not added. `Magnet` changes target stability, `GlareHover` reads as retail polish, `StickerPeel` implies unsupported dragging, and persistent/background effects would compete with the design question and later drawing evidence.
- Live-browser and persistent-Chrome checks at 1440/1024/700/390 confirm the blue task island, yellow problem note, CTA feedback and responsive reflow have no document-level horizontal overflow. The apparent crop in the first raw headless screenshot came from Chrome's minimum outer-window behavior; Playwright viewport metrics remained exactly equal at every acceptance width.

## M12 Global studio-canvas correction

- The user's 2048px screenshot exposes a composition problem hidden by the earlier 1440px checks: the homepage stops at a 1040px centered island, leaving roughly half of an ultra-wide viewport as undifferentiated gray. The page has color but no full-screen visual field.
- The requested correction is global, not another homepage-only accent. Home, result wall, empty/loading states and drawers need to inherit the same architectural canvas vocabulary while keeping evidence semantics and drawing legibility intact.
- Approved system expansion: a tiled drafting grid, registration/crop marks and exactly one route-specific off-canvas plan or section fragment using only the existing graphite, blueprint and marker palette. These are static, `aria-hidden`, pointer-inert and reduced on compact screens; fake dimensions, axes and arbitrary decorative shapes remain prohibited.
- Layout correction: widen the homepage working frame on large screens and use the side margins as intentional annotation zones; do not simply stretch form controls edge to edge or fill the space with extra feature cards.
- Success criteria: no inert ultra-wide field at 2048px, coherent visual continuity in home and result routes, no decoration over interactive content, no document overflow at 390/700/1024/1440/2048, and unchanged task behavior.
- Exact 2048px baseline: the composer is 1040px (50.8% of the viewport), the shared workspace stops at 1440px and the result wall stops at 1320px with only four columns. Both routes leave 304–504px of blank space on each side, so the issue affects the whole product rather than only the homepage.
- Ultra-wide structure needs a dedicated wider frame. The final direction keeps four result columns and enlarges each drawing sheet instead of adding a fifth dense column; image legibility is more valuable than maximizing item count.
- Mature canvas products treat the viewport itself as the work surface: tldraw uses an inset-0 canvas and camera framing; Miro/FigJam use subtle grids plus frames/sections to make spatial emptiness navigable; Excalidraw derives identity from a consistent line language rather than ambient effects. Sources: https://github.com/tldraw/tldraw, https://help.figma.com/hc/en-us/articles/15300412458647-Explore-FigJam-files, https://help.miro.com/hc/en-us/articles/360018261813-Frames and https://github.com/excalidraw/excalidraw
- The transferable rule is a low-contrast spatial reference layer rather than a marketing background: global grid and frame/crop marks at rest, page-specific diagram fragments in wide margins, and no continuous animation.
- Code boundary: mount one `StudioBackdrop` before the sticky header inside `.research-desk`, tag it with the current home/results view, and keep its own fixed overflow-clipped container pointer-inert. Do not add transform, filter, containment or z-index to `.board-workspace`, because that would interfere with fixed drawers and modal stacking.
- First 2048px implementation check: the home composer and result wall both expand to 1600px inside a 1760px shell with exact `scrollWidth === clientWidth`. The four problem starters become one horizontal launch strip; result drawings remain four larger columns.
- The global 128px registration grid, one cropped plan fragment on home and one cropped section fragment on results now make the unused margins read as an architectural working field. Each route shows only its own diagram and the result route reduces background strength, so decoration does not compete with drawing evidence.
- Final responsive acceptance covers 2048, 1440, 1024, 700 and 390px with no document-level horizontal overflow. Diagram fragments appear only above 1380px, the grid softens at 860px and disappears at 620px; modal/drawer content remains above the backdrop with an opaque surface and locked page scroll.
- M12 ships without a new animation dependency. The existing task-bound `ClickSpark` remains the only expressive motion, while the global canvas is static and supports `prefers-reduced-motion` by construction.
- Tablet QA found the only non-blocking usability gap: 700px header actions were 38px high and comparison controls were 30px. The `≤860px` global contract now promotes both to 44px, keeps card actions visible for touch input and preserves the two-column result wall without overflow.
- Repository verification after M12 passes 105 API tests, 39 Board tests, 111 extension tests and 6 packaged-Chrome E2E cases, plus Python/TypeScript lint, type checks, production builds, PowerShell contracts and all 30/108 evaluation fixtures.

## M13 Research prompt vertical proportion

- The 1600px-wide composer made the former 108px textarea read as a shallow strip. The user's requested correction is vertical working space, not another width increase or a chat-style layout change.
- The prompt now uses one semantic responsive token: 152px on desktop, 132px at `≤860px` and the existing 108px at `≤620px`. Vertical resize remains available for longer briefs.
- Exact Chromium acceptance at 2048/1440/1024/700/390px confirms the intended heights, unchanged mobile density and no document-level horizontal overflow. The design detector reports zero findings; Board verification passes 40 tests plus lint, typecheck and production build.

## M14 Result comprehension and method comparison

- The demo route had shown a drawing wall without restating its preset task, so users could not tell whether results answered their own input or an example. Result provenance is now explicit: `演示任务` for `?demo=1`, otherwise the persisted `ResearchRun.question` as `本次研究任务`.
- A result is now presented as `task → evidence-backed method directions → supporting drawings`. The three directions reuse existing ranked `inference` values and prefer distinct drawing types; they do not introduce a new unsupported synthesis claim.
- Drawing cards state `可借鉴方法` and `问题匹配`; detailed observations, source claims and limitations remain in the evidence inspector. This preserves the fact/observation/inference/boundary separation.
- “对比” was ambiguous because the former modal only repeated card text in columns. The accepted “方法对照” matrix compares `解决什么 / 可借鉴方法 / 图中看到 / 证据状态 / 使用边界`, explains whether references compete or combine, and uses the first ranked selected result only as a starting recommendation rather than inventing a new score.
- Exact browser acceptance at 1440×1000 and 390×844 confirms no document-level overflow. On mobile the matrix scrolls inside its own container, keeps the dimension column sticky and exposes an explicit horizontal-swipe hint.
- The complete offline verification remains green after M14: 105 API tests, 40 Board tests, 111 extension tests, 6 packaged-Chrome E2E cases, PowerShell safety/process contracts and all 30/108 evaluation fixtures.

## M15 Research-depth correction

- M14 made the result understandable but did not deepen the research itself: one ranked `inference` per asset still produces a gallery with short captions rather than a decomposed architectural argument.
- The required unit of presentation is no longer a single image card. It is `subquestion → project dossier → multiple supporting assets`, with each dossier explaining project conditions, spatial mechanism, visible evidence, transferable operation, mismatch/boundary and provenance.
- This remains task-scoped real-time research. Subquestions, project groupings and temporary assets belong to one `ResearchRun`; they do not form a platform case library or cross-workspace index.
- Depth modes must change evidence obligations rather than only time limits: Quick should cover at least 3 subquestions / 2 projects / 6 assets, Balanced 4 / 4 / 12, and Deep 5–6 / 6 / 18 when the live web supplies usable evidence. Partial delivery remains valid when those targets cannot be met.
- A previously seen asset can legitimately advance a new subquestion. Duplicate persistence now unions task-scoped subquestion associations; only a batch that adds neither a new asset nor a new association counts as empty.
- Browser inspection considers up to six candidate media regions per page, while run-level capture, preview-byte and page-attempt budgets are reserved and committed to SQLite before more work proceeds. Crop dimensions, public-URL checks and temporary storage rules remain bounded across restarts.
- `project_context` is useful dossier context but does not become a formal fact merely because the model emitted it. Persistence creates evidence claims only from exact entries in `facts`; context is source-supported only when the provider repeats the exact claim there. `design_mechanism` and transfer steps remain explicitly interpretive analysis.
- The durable plan records 3 / 4 / 6 subquestions for Quick / Balanced / Deep and uses their stable IDs in query attempts. A result may support more than one subquestion, so `subquestion_analysis` stores context, mechanism, transfer, observation and limitation per association instead of forcing one asset-level explanation onto every chapter.
- Retry safety is based on execution generations and each query key's latest state. A retry inherits only its contiguous incomplete chain across service restarts; an independent later retry is not poisoned by stale `started` rows from an older generation.
- Coverage is evidence-aware: an item must be relevant and have displayable local or remote content; precedent subquestions count only `partial` or `verified` evidence, require at least two qualifying assets, and a multi-asset project contributes depth only when it also spans at least two drawing types.
- Board comprehension follows the research structure directly: a subquestion chapter groups project dossiers, each dossier explains conditions, mechanism, transfer and boundary beside a complementary image set, and the evidence drawer resolves the analysis for the exact subquestion association that opened it.
- M15 visual acceptance at 1440px and 390px covers home, result chapters, dossiers and evidence drawers without page-level horizontal overflow. The authoritative `scripts/verify.ps1` gate exits 0 with 137 API tests, 41 Board tests, 111 Extension tests, 6 packaged-Chrome E2E cases and every lint, type, build, PowerShell and 30/108 evaluation check passing; the default gate makes no live provider calls.
- Perceptual deduplication no longer erases provenance. A later page can contribute a lightweight source relation without another classification/file; an existing provider asset adopts the first real crop/hash, stronger publication sources upgrade only the source fields, and unused duplicate files are removed without upgrading rights or project identity.
- Subquestion analysis is monotonic across provider search, browser observation and later provider passes: supported project context and mechanism survive blank follow-up fields, while transfer steps, observations and limitations append in stable deduplicated order.
- Lifecycle cleanup now interprets stored crop paths with the same working-directory-relative semantics as creation and duplicate removal. The default `.archresearch/runs/...` path is preserved, while an unreferenced sibling crop is deleted.

## M16 Chinese output and preview recovery

- The reported live run `58f4b9f9-327c-4e07-99d3-2864aa8c49b1` contains eight persisted results, but every result has `image_url = null` and `has_local_content = false`. The Board is accurately rendering the absence of an asset; this is not an `<img>` loading failure.
- Its inspection Trace contains repeated `BrowserUnavailableError` events for candidate project pages. The live web-search provider supplied source pages and analysis, but the Chrome extension was not connected to enumerate/crop page drawings, so the run could not produce a visual reference board.
- Seven of the eight results contain English user-facing analysis. `OpenAIResearchProvider.search()` currently constrains structure and evidence but not output language, and the Board correctly displays the persisted text unchanged.
- Product correction requires both sides: enforce Simplified Chinese at planning/search generation boundaries, and expose browser readiness before research plus an actionable source/retry state for historical no-image results. A frontend-only translation or fabricated fallback image would hide the actual acquisition failure.
- Live planning and search prompts now require Simplified Chinese for every user-facing subquestion and analysis field. Official project names may retain their source language, but project conditions, mechanisms, transfer steps, facts, observations, inferences and boundaries are generated in Chinese on new runs.
- Browser readiness is a local first-class state: `/v1/browser/status` reports whether an authenticated ArchResearch extension WebSocket is attached, while the existing one-time pairing endpoint is exposed from the Board. Connection alone does not imply site permission, so the interface also tells the user to grant the research-time page-reading permission in the extension.
- A completed no-image run is immutable; its retry endpoint is intentionally a no-op. The Board therefore starts a new run with the same workspace, question, goal and depth after the extension connects instead of pretending to repair the historical result.
- Browser crops now conservatively enrich a provider candidate when there is exactly one unresolved candidate with the same source URL and drawing type. This preserves the project dossier and analysis while preventing an ambiguous crop from being attached to the wrong project.
- Missing previews are presented as an acquisition state, not a broken image: Trace-backed extension failures receive a precise Chinese explanation, every no-image drawing keeps a direct source link, and no placeholder is presented as architectural evidence.
- Historical English analysis is not silently machine-translated. The Board keeps official project names and source links, replaces legacy English analysis with explicit Chinese regeneration messages, and localizes publication, association and rights enums. New runs remain the only source of substantive Chinese summaries.
- Live Chromium acceptance at 1440×1000 and 390×844 confirms the disconnected-extension notice, pairing actions, immutable-run recovery, Chinese legacy handling and source links without document-level horizontal overflow.

## M17 Runtime and extension onboarding reliability

- The user's screenshot says “无法连接本地 ArchResearch 服务”, which is distinct from the extension-disconnected state. At reproduction time, `.archresearch/dev-processes.json` still named prior PIDs while neither 5173 nor 8000 had a listener; Vite's log showed `ECONNREFUSED 127.0.0.1:8000`.
- Restarting inside a retained PowerShell 7 session restores Board 200, API health 200 with 梭子蟹 API / `gpt-5.5`, and `/v1/browser/status = { connected: false }`. This isolates the remaining preview blocker to extension installation/pairing rather than the image renderer or provider.
- The packaged MV3 extension is present at `apps/extension/dist` with a valid manifest, popup, side panel and compiled background worker. It still must run in the user's normal Chrome profile and authenticate to the local browser WebSocket before a new research run can crop page drawings.
- Firecrawl is useful for public-web discovery and normalization: its official API covers Search, Scrape, Parse, Crawl, Map and structured extraction, including JavaScript-heavy pages and clean Markdown/JSON output. It cannot replace the local extension's user-profile login state or trustworthy on-screen drawing capture.
- Firecrawl must not enter the V2.1 default runtime. The official self-host stack requires Docker plus PostgreSQL and Redis, explicitly conflicting with the approved local-first architecture; self-hosting also lacks Fire-engine's advanced IP-block/robot handling. The core repository is AGPL-3.0, so copying or embedding its server code would add licensing obligations.
- The bounded future fit is an optional `public_page_parser` provider behind the existing source-page boundary: explicit public HTTP(S) URLs only, deterministic mock by default, no logged-in pages, no case-library persistence, and no Firecrawl dependency or key in the first release.
- One-click pairing should preserve the existing one-time-code and persistent-token protocol instead of weakening loopback authentication. The smallest safe bridge is one packaged content script restricted to `http://127.0.0.1/*` and `http://localhost/*`; it accepts an exact versioned `window.postMessage` contract and forwards only pairing/status/permission commands to the fixed background controller.
- The Board can then issue a code and hand it to the installed extension in one action. Research-time `<all_urls>` access remains a separate browser permission requested from the user's explicit “开始研究” action and revoked at terminal state; the local bridge never receives arbitrary selectors, JavaScript or page content.
- The extension currently marks the socket connected before authentication completes and retries every 1 second after a 1008 authentication close, making an invalid/expired code appear as permanent `Connecting`. Authentication rejection must become a stable error with no retry until a fresh one-click pairing replaces it.
- The completed bridge is loopback-only and command-enumerated. The Board can request one-time pairing, status and temporary research permission; it cannot send selectors, scripts, page contents or general browser actions through this surface.
- Pairing survives browser restarts through the existing rotated token. Users normally click “一键连接浏览器” once; a fresh one-time code is required only after disconnecting, clearing extension storage, reinstalling the extension or receiving an expired/invalid authentication rejection.
- Packaged-Chrome acceptance caught a build-only failure that unit tests could not: Manifest V3 content scripts cannot execute an ES-module import. The bridge validation is therefore kept inside the single compiled content-script entry, and the real packaged test now pairs from a loopback Board page before exercising asset capture and permission revocation.
- The extension popup and side panel are now Chinese. Manual endpoint/code entry remains available only as recovery, preserves the code on failure, and points the user back to the Board's one-click path.
- The authoritative repository gate exits 0 with 141 API tests, 48 Board tests, 118 Extension tests, 6 packaged-Chrome E2E cases, all lint/type/build and PowerShell checks, and all 30/108 evaluation fixtures. No live OpenAI, TinEye or Firecrawl calls are part of this gate.

## M18 Open-source browser research integration

- Scope: evaluate current primary GitHub repositories for browser-agent discovery, extraction and recovery patterns. Adopt only bounded provider/protocol capabilities compatible with the local-first Windows/Chrome architecture; reject a second autonomous browser loop, server platform or global crawl store.
- The first GitHub web-search/open pass returned no source payload after the search timed out. Research will continue through the official GitHub repository API/raw README endpoints with bounded requests rather than repeating the same failing search or cloning repositories.
- Official GitHub metadata (2026-07-14) confirms the first comparison set is active rather than abandoned: Firecrawl (AGPL-3.0, TypeScript), Browser Use (MIT, Python), Stagehand (MIT, TypeScript), Playwright MCP (Apache-2.0, TypeScript), Crawl4AI (Apache-2.0, Python), Browsertrix Crawler (AGPL-3.0, Docker-oriented), Steel Browser (Apache-2.0, TypeScript) and Lightpanda (AGPL-3.0, Zig).
- Firecrawl's official README exposes the most relevant missing capability: public-web Search/Scrape/Crawl plus Markdown, screenshot and structured JSON output, including JS-heavy pages and document/media parsing. It is suitable as an optional hosted `public_page_parser`, not as the login-state browser or a bundled server.
- Browser Use is a full Python agent loop with custom tools, structured output, fine-grained control and real Chrome-profile reuse. Its session/profile concept validates the existing extension direction, but importing its autonomous runtime would duplicate ArchResearch planning, weaken the enumerated action boundary and add another browser owner.
- Stagehand's transferable pattern is `observe/act/extract` with previewable AI actions, caching and self-healing. ArchResearch should borrow “observe before act” and action-result trace semantics, but not add its second TypeScript browser stack because the extension already owns logged-in Chrome and the backend already owns planning.
- Playwright MCP demonstrates that structured accessibility snapshots and deterministic tools are cheaper and less ambiguous than screenshot-only control. ArchResearch can add a bounded semantic page snapshot to its existing extension protocol and keep screenshot crops for architectural visual evidence; running a separate MCP server is unnecessary.
- Crawl4AI offers strong local Markdown extraction, heuristic/BM25 content filtering, sessions, caching and crash recovery, but its normal install brings a managed Playwright browser and its broad cookie/script/hook surfaces exceed the fixed ArchResearch browser DSL. Its “fit Markdown” and checkpoint ideas are transferable; the runtime dependency is not.
- Browsertrix Crawler is deliberately a high-fidelity Docker/Puppeteer/CDP archiving crawler. WARC/WACZ capture and parallel crawl infrastructure solve preservation rather than task-scoped architectural evidence, so it remains an evaluation/replay reference only.
- Steel Browser provides managed CDP sessions, state, debugging and Markdown/screenshots behind cloud or Docker APIs. That duplicates the user's installed-Chrome bridge and conflicts with the no-Docker first release; its explicit session cleanup reinforces the existing run-owned tabs/permission lifecycle.
- Lightpanda is a fast agent-oriented CDP browser with Markdown dumping and session isolation, but its supported local paths are Linux/WSL/macOS or Docker and the repository is AGPL-3.0. It is not a Windows Chrome-login solution and is excluded from V2.1.
- Preliminary adoption boundary: implement Firecrawl as an optional public-page parser behind a small concrete client with deterministic mock/fallback, and add one bounded semantic page-snapshot action inspired by Playwright MCP/Stagehand's observe-before-act model. Do not import Browser Use, Stagehand, Crawl4AI, Steel, Browsertrix or Lightpanda runtimes.
- Firecrawl's current official v2 example uses `POST https://api.firecrawl.dev/v2/scrape` with a Bearer API key and a single public URL. The optional client can use `httpx` already present in the API, avoiding the Firecrawl SDK and its dependency surface.
- The current workflow persists provider sources first, then opens each source through the extension, reads page metadata, enumerates up to six media regions, captures bounded previews and visually classifies them. A public-page parser should enrich source context/fallback discovery before this inspection, never replace visual classification or upgrade provenance/rights.
- `SourcePage` currently stores URL/title/publisher/tier/access/hash but no temporary parsed body. To avoid a migration and durable third-party text store, parsed Markdown/semantic outline should remain run-local and feed only bounded inspection/provider context plus Trace summaries.
- Startup already supports dependency injection for research, reverse-image, browser and visual providers. M18 should add `PublicPageParser | None` the same way, instantiate Firecrawl only when a key is present, and pass it through resume/new-run execution without changing default Mock behavior.
- The extension's approved protocol already separates metadata, media enumeration and fixed actions. A new `page_snapshot` action can remain exact and read-only: return only visible headings, paragraphs and figure captions with per-item/count/total-character limits; reject sensitive pages using the existing guard.
- `inspect_source_page` is the correct observe-before-act boundary: open → wait → metadata → bounded semantic snapshot → enumerate media → crop/classify. Snapshot failure should degrade to metadata-only context so an older/restricted page cannot erase otherwise usable visual results.
- Firecrawl integration should not create durable page-body columns. It can supply a bounded run-local `ParsedPublicPage` containing canonical metadata, Markdown and public image leads; those leads remain `visual_lead/unknown-rights` until the existing visual and source-association pipeline verifies them.
- Firecrawl's official scrape documentation provides a direct bounded format set: `markdown`, cleaned `html`, `rawHtml`, `screenshot`, `links` and `images`. M18 can request only `markdown`, `links` and `images`, avoiding full HTML and expiring screenshots; one scrape costs one base credit, so calls must be page-budgeted and deduplicated per run.
- The response includes metadata plus extracted image URLs. The parser will cap Markdown, link count and image count, reject private/reserved URLs on both input and output, and expose counts/hashes to Trace rather than logging third-party page bodies or the API key.
- Existing test seams support red-first M18 without refactoring: `RecordingBrowser` can assert the new read-only action order and graceful snapshot failure; extension protocol/content tests can enforce exact no-selector payloads and sensitive-page blocking; the TinEye app/config pattern can cover optional Firecrawl construction/injection.
- The Firecrawl fallback should enrich only an existing same-source unresolved asset when one parsed Markdown/image lead maps unambiguously to that asset's drawing type. It must not create a new project identity, fact, rights claim or result-tier upgrade. Ambiguous/multiple matches remain untouched and are recorded only as lead counts.
- Existing `_persist_inspected_assets` already implements the exact conservative pattern needed for real crops. M18 will add a smaller Firecrawl enrichment function that updates only `image_url` on one unresolved same-source/same-type candidate, reranks, and leaves perceptual hash, local storage, observations and evidence claims unchanged.
- Startup health tests assert exact JSON, so optional parser readiness should be observable through `app.state.public_page_parser` and Trace without expanding the existing health response contract or exposing whether a secret exists.
- The existing secure provider setup gives M18 a reusable contract: hidden `SecureString` input, stdin-only child handoff, BSTR zeroing, Windows Credential Manager storage and a non-secret local config. Firecrawl should get a parallel small script/helper and PowerShell security test rather than putting its key in `.env` documentation as the primary path.
- Unlike the model provider, Firecrawl setup does not need a billable capability probe. The runtime client is already contract-tested with `MockTransport`; setup should validate the public HTTPS base URL and store the key transactionally, then tell the user that the first fallback scrape may consume a credit.
- Documentation needs three surgical updates: README key setup and fallback behavior, architecture's provider graph/protocol action list/open-source tradeoff table, and failure matrix behavior when Firecrawl is unconfigured, rate-limited or ambiguous. Existing security and no-case-library boundaries remain unchanged.

## M19 Direct browser-research enrichment

- User rejected M18's fallback-only product role. The accepted interpretation is that a useful open-source capability must improve the normal research path: public-page parsing should run for ordinary candidate pages when configured, while the installed Chrome extension still supplies login state, dynamic interaction and exact visual crops.
- Current code confirms the Firecrawl gap: it is called only inside the Chrome-inspection exception branch and can only backfill one unresolved preview. A closer read corrects the initial snapshot assessment: `page_snapshot` is already bounded into `project_text` and passed to `VisualClassifier.classify`, so that open-source-inspired capability already improves normal drawing classification rather than merely tracing page content.
- The minimum direct improvement is therefore a task-scoped dual observation: parse each deduplicated public source once, feed bounded Firecrawl Markdown into the same visual-classification context, and persist clearly typed Firecrawl image leads even when Chrome inspection succeeds. No second autonomous agent loop, durable page-body store or evidence-tier promotion is needed.
- Firecrawl image leads can be useful without pretending they are verified: persist each uniquely typed, public image URL as a low-ranked `visual_lead` when it cannot unambiguously enrich an existing provider asset. Set relevance below the coverage threshold, keep project/asset association and rights unknown, add no facts/observations, and explain that local visual classification/source binding is still pending.
- Parsed Markdown belongs only in the current `inspect_source_page` call. It can be combined with the already bounded extension snapshot inside the existing 1,200-character `project_text` prompt, which already marks all supplied text as untrusted. This improves classification context without adding a database column or retaining third-party page bodies.
- Public parsing and Chrome inspection need separate per-run URL sets and bounded page counters. Sharing the same counter would let parsing consume all Chrome crop capacity; leaving parsing unbounded would make paid Firecrawl usage unpredictable. Each path should remain capped by the selected mode's existing page limit.
- Final diff review found an interaction to verify before commit: a one-to-one Firecrawl lead is attached before Chrome inspection, so the later real crop must still be allowed to replace that remote preview on the same source/type candidate. Otherwise the direct enhancement would accidentally prevent the stronger local evidence from enriching the provider dossier.

## M20 Live architecture research acceptance

- Live acceptance starts from the existing workspace `M6 真实研究验证`, whose brief and constraints already target old-industrial-building renewal, section drawings and source verification. Reusing it avoids creating artificial workspace state.
- The active API reports 梭子蟹 `gpt-5.5`; Firecrawl was validated immediately before this run. `/v1/browser/status` is disconnected, so the acceptance task should expect structured public image leads but no local Chrome crop and should judge the UI's honesty accordingly.
- Quick mode is bounded to 2 rounds, 4 queries, 12 pages and 240 seconds. The representative question will combine adaptive-reuse program insertion, separated visitor/service circulation and sectional spatial hierarchy so the required 3-question decomposition and multiple drawing types can be evaluated in one run.
- Live planning succeeded in Simplified Chinese and produced three appropriately distinct, evidence-oriented subquestions: structure/function insertion, visitor/service flow separation and sectional public-space hierarchy. This validates the M15/M16 decomposition and language contracts against the real relay.
- After roughly 90 seconds the run remains in the first `searching` stage with only one result and no Firecrawl Trace. Firecrawl correctly cannot start until the provider returns candidate source pages; this exposes provider search latency rather than a parser failure and must be observed through the 240-second Quick deadline.
- The run terminated after about 131 seconds with `provider_error:APITimeoutError` during the first search and no Firecrawl Trace. Because source discovery is still exclusively upstream of Firecrawl parsing, a slow model web-search call can prevent the newly integrated public-page capability from running at all.
- This live evidence changes the next technical priority: Firecrawl Search must participate directly in candidate-source discovery, not only parse URLs returned by the model. Its source leads can be parsed and preserved as low-confidence evidence while model-authored architectural analysis remains a separate enrichment step.
- The official Firecrawl v2 Search contract is `POST /v2/search` with `query`, `limit`, default web sources, optional domain filters and a response under `data.web` containing URL/title/description. Search can optionally scrape results, but the minimum integration should use URL metadata and retain the existing bounded per-page parser so image handling and public-URL filtering remain in one place.
- Firecrawl Search is currently priced at 2 credits per 10 results. M21 will cap each query at four web results and keep the existing 4-query Quick ceiling; it will not call Firecrawl Agent, Interact or autonomous browsing.
- The failed run's event endpoint is SSE text rather than a JSON list. Raw inspection confirms exactly three events—planning start, planning complete and first search start—and the results endpoint is an empty array. Earlier PowerShell array counts were response-shape artifacts, not a provisional result.
- The first M21 implementation preserves a Firecrawl-derived plan image but ends `blocked` because terminal status checks only `coverage.usable_assets`; deliberately low-ranked `visual_lead` items have relevance 1 and are excluded. The product contract should treat any preserved result as a partial delivery while keeping coverage unsatisfied and the lead visibly unverified.
- On the real retry, `firecrawl_search` appears in Trace within the first 25 seconds while the model remains in `searching`. This confirms direct source discovery now occurs before the slow provider call and is checkpointed independently.
- The real retry now terminates `partial` with seven remote visual leads instead of failing empty. All seven come from one ArchDaily Chinese roundup page and are low-confidence `visual_lead`/unknown-rights/unknown-association items, so the status and boundary copy are honest but project/drawing diversity is still inadequate.
- Five apparent results are delivery-size variants of only two underlying ArchDaily images (`thumb_jpg`, `newsletter`, `medium_jpg`), and one Markdown-extracted URL is truncated at an unescaped parenthesis. Public image normalization must collapse known delivery variants and reject unbalanced-parenthesis URLs before a live Board can be considered useful.
- The current leads are three elevation variants and four render variants; there is no plan or section despite the prompt. This live task therefore passes failure recovery but not research usefulness. Source diversification and image deduplication are required before closing M20.
- M21 attempt 2 proved independent deadline admission in a real run: after one slow gpt-5.5 batch, a later model call was skipped for `insufficient_time_reserve` while another Firecrawl search still ran, and the run ended `partial` instead of empty. However, retry intentionally preserves earlier candidates, so its result endpoint still contains the seven pre-fix delivery variants and malformed URL; a fresh run is required to validate new normalization rather than mutating preserved history.
- Fresh run `eef07a27-7cd5-455b-ac21-fb5861dcc318` validates the new normalization without historical assets: exactly two results remain, both highest-quality `medium_jpg` variants, with two distinct underlying image identities and zero malformed URLs. This closes the duplication/URL-integrity defect.
- The same fresh Quick run executed all four bounded Firecrawl searches. The first gpt-5.5 search timed out and degraded to four retained sources; the remaining three model calls were skipped for insufficient reserve while each corresponding Firecrawl search still completed with four leads. The run ended honest `partial/unverified_visual_leads`, proving task progress and partial preservation through a real relay timeout.
- Live usefulness is still below M20 acceptance: the two surviving images are one elevation and one render from a single ArchDaily roundup, not plans/sections or multiple projects. Direct discovery and normalization are correct, but source ranking/query quality or page extraction must improve before calling the architecture research result useful.
- Database/Trace inspection shows the four searches collapsed to seven unique public pages and all seven were successfully parsed; this is not a page-fetch failure. Firecrawl found 74 remote images across Scribd, ResearchGate, two ArchDaily pages and one studio page, but only the first ArchDaily roundup exposed URL/alt keywords that the deterministic nine-class recognizer could type. The bottleneck is remote visual classification, not search execution or page availability.
- The second ArchDaily roundup yielded 40 images and the studio page yielded 10, yet `enriched: 0` because their image URLs/alts do not contain `plan`, `section`, `elevation`, `diagram` or Chinese equivalents. Persisting them as an arbitrary drawing type would be misleading; the next quality step needs bounded vision classification of remote image candidates or an equally evidence-preserving image-search contract.
- The existing `OpenAIVisualClassifier` already submits an `input_image.image_url` string to the Responses-compatible client and is available in the live workflow even when the Chrome browser is disconnected. A public candidate URL can therefore use the same strict nine-class output without adding a downloader, browser runtime or new provider abstraction; the implementation must remain globally bounded by the existing durable visual-call budget and keep the result unverified.
- Current browser classification reserves calls and preview bytes through `InspectionBudget`; public URL classification has no local preview bytes but still needs one durable call reservation per candidate. Reusing this budget preserves retry/restart cost limits and avoids an independent untracked vision loop.
- Directly reusing `_persist_inspected_assets` for remote classification is unsafe without a contract change: it intentionally refuses new candidates without a local `storage_path`, while cross-source duplicates may carry no file. A separate public-image classification path would need explicit remote-only semantics and should not be smuggled through the local-crop model.
- Quick mode permits 12 visual calls, but the configured relay already spends roughly 120 seconds on a timed-out search. Sequential remote-image classification could overrun the four-minute run and add opaque cost. Before adding that path, improve the cheaper discovery input: Firecrawl currently receives the full provider prompt (including long task/context text) truncated to 500 characters rather than a concise search-engine query.
- `_queries_for` deliberately builds rich model prompts up to 8,000 characters and appends untrusted workspace context; that is appropriate for model analysis but not for a search-engine query. The public search should receive a separate concise string based on the active subquestion, language/round and goal-specific drawing terms, while `QueryAttempt` retains the full model prompt for traceability.
- Existing `RecordingPublicSearchParser` already records exact search strings, so a red-first workflow test can prove that Firecrawl receives the active subquestion and architectural drawing intent but never the `Untrusted user design context` suffix. This change stays inside query orchestration and does not introduce domain hard-coding or a static case list.
- M22 live comparison shows an immediate discovery change before the slow model returns: the first concise query yields four distinct domains/pages (ArchDaily, Zhihu, 建筑档案 and a design site) instead of the previous batch's academic PDF/Scribd/ResearchGate-heavy results. Final image usefulness still depends on page parsing and cannot be inferred from this early source list alone.
- Fresh M22 run `13b6b6d9-dacb-4f3d-a4ee-def188d4df6a` materially improves retrieval: eight results from two parsed source pages versus two from one source, and the asset mix now includes a section alongside elevations/renders. All four Firecrawl searches completed; one model call timed out and three were skipped by reserve as expected.
- The apparent two-project count is still only two ArchDaily roundup page titles, not two verified individual projects. It must not be presented as project-level evidence diversity. One image (`Kimo_02.jpg`) also appears twice through `snoopy.archdaily.com/.../slideshow/` and `images.adsttc.com/.../newsletter/`; URL normalization needs a conservative shared media-path identity across delivery CDN hosts.
- The live runtime uses one OpenAI-compatible client with a 120-second request timeout for research and vision. Every live web-search call has exhausted that full timeout, so adding a sequential remote-image loop behind it would not run within Quick's 240 seconds. Any remote classification milestone must batch images and explicitly fit its worst-case call inside the remaining run deadline, or reprioritize the consistently failing model web-search call.
- Existing visual tests prove strict structured classification for one image but there is no batch contract. A safe batch addition needs indexed outputs, a small per-request image cap, low-detail remote images, exact index validation and the existing durable `visual_calls_used` budget; otherwise retries could silently multiply cost.
- The user's M24 screenshot shows an active `正在搜索 / 0 项参考` run, but the main canvas falls back to the generic composer empty state (“从一个具体设计问题开始”) inside a large dashed box. Active research should retain the submitted question and expose meaningful stage progress; it must not look like the user's task disappeared.
- The API is healthy on gpt-5.5 while `/v1/browser/status` is `connected: false`. The screenshot is shown in Codex's in-app browser, where the separately installed ArchResearch Chrome extension cannot inject its Board bridge. One-click pairing cannot honestly succeed from that surface; public Firecrawl research should continue without it, and the UI needs a precise Chrome-only explanation instead of a generic failure.
- Code reproduction confirms the active-run blank state: `resultViewOpen && results.length === 0` unconditionally renders the generic `board-empty` composer copy, even when `isRunActive` is true. The status strip remains above it, producing the contradictory screenshot. The correct branch is an active-run canvas containing the submitted question, current stage and decomposed subquestions; only a terminal/no-run state should show the generic empty prompt.
- The one-click handler currently rotates/creates a pairing code before checking whether the page can reach the installed extension bridge. In the in-app browser the bridge times out, so every click needlessly creates another one-time code and still cannot reach Chrome. The safe sequence is bridge status probe → pairing-code creation → pair → API connection poll; an unavailable bridge should explain that public research continues and Chrome is only needed for logged-in pages/precise crops.
- M24 confirms the product boundary in executable tests: Firecrawl-backed public research is the default path and must not be described as text-only, while the installed Chrome extension is an enhancement for logged-in pages and precise local crops. A disconnected extension therefore cannot disable or visually erase an active public-web run.
- The correct connection sequence now probes the page bridge before requesting a one-time code. Codex's in-app browser receives an immediate, accurate explanation without rotating credentials; a packaged Chrome page with the installed extension still completes pairing, temporary host permission and real crop persistence end to end.
- The active-run canvas follows the existing global design system: fixed spacing tokens, a flat action-tinted surface and the established type scale. The initial gradient, fluid padding and card shadow were removed so this state remains part of the architectural workbench rather than a separate generic dashboard panel.
- The user's M25 screenshot confirms that the new factual copy is still framed as a large blue “图纸提取未连接” warning. This is semantically wrong when public-web images are already visible: remote images come from Firecrawl/public search, while the Chrome extension supplies a different capability—authenticated/dynamic page reading and precise local crops.
- The UI needs capability-based state, not one binary “image extraction” state. The default should read as an active `公开网页模式`; disconnected Chrome belongs in a secondary enhancement row and should not use failure language or dominate the task. A genuine public-search/parser failure remains the only reason to use an error treatment.
- Live API inspection proves the distinction on the user's current workspace. Run `13b6b6d9-dacb-4f3d-a4ee-def188d4df6a` contains eight remote `image_url` previews and zero `has_local_content` assets; its sample is an ArchDaily CDN image. The two earlier runs likewise contain only remote previews. The visible drawings therefore come from public-page discovery, not from a falsely reported Chrome connection.
- Real Windows acceptance isolated the remaining connection failure to the user's Chrome extension state, not the API or pairing protocol. The new endpoint opened the fixed Chrome Board URL correctly, but the broker stayed disconnected until the user reloaded the unpacked extension. Immediately afterward, five consecutive API samples reported `connected: true`, confirming persistent-token authentication and the handoff path in the actual Chrome profile.
- The authenticated connection later dropped without user action. Chrome's official MV3 lifecycle documentation explains the timing: an extension service worker can stop after 30 seconds of inactivity, while Chrome 116+ resets that idle timer whenever a WebSocket message is sent or received. The official example uses a 20-second keepalive, which is the smallest direct fix for this bridge.
- A persistent pairing token and a live socket solve different problems. The stored token means the user should not pair for every run; the authenticated heartbeat keeps that already paired socket available between browser actions. Temporary page permissions remain scoped to the active research session and are still revoked at terminal states.
- Real Chrome acceptance confirms the distinction: after rebuilding and reloading once, all ten connection samples over 45 seconds remained authenticated. Normal research runs can now reuse the stored token and live bridge without re-pairing; only replacing/reloading extension storage or deleting the local pairing token should require a new code.

## M26 Quick-path performance and timeout isolation

- The repeated `APITimeoutError` is emitted by the OpenAI-compatible relay call, not by Firecrawl or the local browser. In the latest live Quick run, planning took about 8.5 seconds, Firecrawl Search returned four sources in about 1.8 seconds, and the first model search then occupied roughly 121 seconds before timing out. The remaining model calls were skipped to protect the 240-second deadline.
- Increasing Quick's total duration would hide rather than fix the bottleneck. The performance target is to cap optional model search enrichment to a smaller share of each mode, preserve independent Firecrawl/Chrome progress, and avoid repeated slow calls once the relay demonstrates timeout behavior during the same run.
- Current provider settings already match the low-latency OpenAI search controls: Responses `web_search`, `search_context_size: low`, and `reasoning.effort: low`. The avoidable delay is local retry policy: a 60-second SDK timeout with one automatic retry produces the observed 120-second critical path.
- The narrow M26 correction is a 30-second no-retry provider client plus a run-scoped timeout circuit. After one model-search timeout, later query batches continue through Firecrawl and Chrome but do not repeat the same slow model call. Planning still has deterministic fallback, and a new research run resets the circuit.
- Live comparison confirms the circuit behavior: the first relay search ended at 30 seconds, then all later model checkpoints recorded `previous_timeout` while Firecrawl continued across the remaining subquestions. The total run improved by about 51 seconds (roughly 26%) despite inspecting more public pages than the old run.
- The comparison was started through the local API to keep the question identical, so it did not invoke the Board's user-gesture permission request. Its `BrowserCommandError` events do not show a broken extension; they show that direct API starts intentionally lack temporary site access. A normal Board-started run is still required to exercise precise Chrome crops.
- Firecrawl's official v2 API accepts an explicit millisecond timeout. The final 20-second bound is sent to both `/search` and `/scrape` and also enforced by the local HTTP client, preventing one slow PDF/page from consuming 30 seconds while preserving later-source progress.
- M29 diagnosis: the latest Quick policy decomposes the task into three subquestions but schedules four queries by breadth-first truncation (`3 + 1`), so only the first subquestion receives a second pass. The global `consecutive_empty_batches >= 2` rule can also stop a run before later branches receive equivalent gap research. The result UI counts returned assets, not search attempts, which makes researched-but-empty branches look untouched.
- M29 product decision: depth will be presented and enforced across three axes—decomposition depth, per-subquestion research coverage, and analysis/evidence depth. Every branch receives a first pass; later passes prioritize evidence gaps. Time remains a hard failure/cost ceiling because remote pages and providers can hang, but elapsed minutes no longer define a successful depth level.
- M29 implemented contract: 概览 = 3 questions × 2 passes, 标准 = 4 × 3, 深入 = 6 × 4. Query ceilings are now the exact breadth-first product of those values (6/12/24); per-question evidence targets are 2/3/3 drawings, with progressively stronger analysis obligations. A no-progress stop is evaluated only after an entire follow-up round, never after two individual branches.
- New runs persist distinct completed pass counts per subquestion in `coverage_report.subquestion_passes`. The result decomposition can therefore distinguish “已调研 2 轮但未找到可用图纸” from “没有研究”，while legacy runs remain compatible when this field is absent.
- Analysis depth is executable rather than decorative configuration: each model research query now carries only the selected mode's named obligations. Quick requests visible observation + mechanism; Balanced adds transfer steps + applicability boundary; Deep adds multi-source verification + cross-case comparison. Public Firecrawl queries remain concise and unaffected.
- M30 screenshot diagnosis: the result header menu is a flat five-action list headed only “工具”. Labels describe internal feature names (`Trace`, `表达规范`) but not outcomes, and native disabled styling removes emphasis without revealing prerequisites. First-time architecture users therefore cannot tell which actions organize selected drawings, which export them, or why three entries are unavailable.
- M30 solution keeps the standard details popover but changes its information architecture: “结果工具” opens three flat groups—整理与对照、导出、研究过程. Every row has a verb-led outcome and one concise explanation; disabled compare/export rows retain readable contrast and state the missing selection count. User-facing `Trace` terminology is removed from both the menu, drawer and API error copy while internal TraceEvent contracts remain unchanged.
- M31 hierarchy diagnosis: clearer menu contents did not fix discovery because the closed trigger remained a small white utility beside the filled-blue “发起新研究” button. On a result page this reverses the intended priority: using the current research should be dominant, while abandoning it for a new task is secondary. The trigger also concealed all outcomes behind the generic noun “结果工具”.
- M31 resolution uses the existing global blueprint action token rather than inventing another accent: “整理与导出” is a 44px filled-blue control with a visible “对照 · 规范 · 导出” preview and directional chevron; “发起新研究” returns to the neutral surface style. At the 620px system breakpoint the preview and chevron hide while the explicit action label remains, preserving a compact two-action header.
- M32 screenshot correction: even the promoted blue control still reads as navigation because it occupies only the far-right edge of the 60px app header. The large result canvas beneath it contains no persistent result-use surface, so the workflow remains visually “research → read” instead of “research → select → transform/export”. More button weight cannot repair this spatial hierarchy; the actions must move into the result canvas and stay exposed.
- M32 desktop acceptance at 1440×1000 confirms the composition now has one unmistakable focal tool: a full-width workbench precedes the research question, with a committed blueprint instruction plane at left and a dense 2×2 action matrix at right. The task title remains the strongest content headline below it, while the app header is visually quieter and no longer competes with result use.
- M32 mobile acceptance at 390×844 shows no horizontal overflow: the blueprint instruction plane leads, four actions become one readable vertical list with full-width separators, and the research task begins immediately after the workbench. Disabled prerequisites remain legible rather than fading into unavailable gray controls.

## M23 bounded remote visual classification

- OpenAI's current vision guide confirms that Responses accepts multiple `input_image` parts in one content array, supports fully qualified remote URLs, and bills every image as input tokens. For this classification-only step, `detail: low` is the correct bounded choice because it uses a 512×512 representation; gpt-5.5's omitted/auto behavior is equivalent to the much more expensive original-detail path. Source: https://developers.openai.com/api/docs/guides/images-vision
- OpenAI's structured-output guide recommends Responses `text.format` with a strict JSON Schema when the model should return application data rather than call a tool. M23 will therefore use a small schema-bound classification response and still validate it with Pydantic before persistence. Source: https://developers.openai.com/api/docs/guides/structured-outputs
- Implemented one conservative remote batch per ResearchRun, capped at four previously untyped Firecrawl images. The batch is admitted only when its declared 30-second worst-case reserve fits the current run deadline.
- A `remote_visual_batch` started checkpoint is committed before the provider call. Resume and retry treat that marker as spent visual budget, so an interrupted call cannot be issued again; the tradeoff is intentionally cost-safe and may omit a result after a process crash.
- Remote results are persisted only when the model returns one of the nine asset types, relevance >= 2 and at least one direct visible observation. Project identity, asset association, primary-source status and rights all remain `unknown`; facts and inferences remain empty.

## M27 bounded project-page expansion and evidence promotion

- Started after M23 with a deliberately narrower scope than a general crawler: follow only a small number of public HTTP(S) links already discovered on a parsed source page, stay inside the existing ResearchRun page/deadline budget, and never introduce a global index.
- Promotion must be evidence-driven. A child project page may strengthen publication and project/asset association only when deterministic page/image relationships support the claim; visual similarity or shared keywords alone must remain a visual lead.
- Existing `ParsedPublicPage.links` is currently collected and traced but never consumed. The smallest integration point is therefore the existing per-source Firecrawl parse path, not a second crawler or new service.
- Initial promotion should target `partial`, not `verified`: an image explicitly returned from a concrete child project page supports probable image–page and project association, but it does not by itself prove that the page is the primary publisher. Verified status still requires stronger primary-source evidence.
- Link expansion will be one hop only and prefer same-host URLs with explicit project/work/portfolio path markers or numeric article/project identifiers, while excluding search, tag, category, author, about and contact paths. This avoids turning navigation/footer links into an open crawler.
- An exact image URL enumerated on the child project page is deterministic association evidence. The existing candidate can be promoted in place to `partial`, with `project_identity=probable`, `asset_association=confirmed`, `primary_source=unknown`, an explicit URL-bound fact claim and unchanged unknown rights. New typed images on that page may enter under the same conservative evidence state.
- Implemented expansion inside the normal Firecrawl parse path. It is strictly one hop, two links per parent, same normalized host, and shares both `max_pages` and the parser's declared deadline reserve with ordinary page parsing.
- A concrete child page must have a non-empty title and a deterministically typed image. Exact image URLs promote existing leads in place; newly observed typed images enter as partial under the same association boundary. No item is upgraded to verified without separate primary-source evidence.

## M28 result diagnosis and release-quality acceptance

- The API already exposes `stopReason` and structured coverage gaps, but the Board currently renders raw internal codes such as `budget_exhausted` and `insufficient_verified_or_partial`. This explains the user's confusion: the status is technically present but not translated into a decision-oriented diagnosis.
- The result header should stay compact and inline with the existing flat workbench. It should state the delivery reason in Chinese, summarize what was retained, list only the concrete missing coverage dimensions, and explain whether retrying with deeper research can help. The recent-run row should carry a short reason, not duplicate the full diagnosis.
- Impeccable product guidance reinforces the existing design system: use familiar inline disclosure, no modal or new dashboard card; preserve fixed typography, restrained semantic color, visible keyboard focus and readable disabled/error states.
- Live Balanced acceptance run `0c8fa2aa-397a-483a-9e0f-3c59d6036967` finished `partial/no_new_assets` with 5 usable images across 2 concrete projects and 5 partial-or-verified items, exceeding the M28 release threshold of 4/2/2. All 27 retained results had image previews.
- The partial state remains correct rather than a failure: only 1 of 4 decomposed subquestions reached its per-question drawing target. The live run also confirmed M27 in production: child project-page expansion promoted three ArchDaily images with URL-bound evidence. GPT web search timed out once, the circuit opened, and Firecrawl preserved progress.
- Browser connection status was true, but page commands returned `BrowserCommandError`/`BrowserNavigationError`; therefore M20's stricter Chrome-crop acceptance remains open. M28 can close because its public-web usefulness threshold passed without misrepresenting those remote images as Chrome crops.

## M33 completion-first product contract

- The user's expectation exposes a product-contract problem rather than a wording problem: selecting a lighter depth still means “answer my whole question”, not “accept an incomplete answer sooner”. Depth may change the number of decomposed branches, desired examples per branch and analysis obligations, but normal budget/no-progress exhaustion must not by itself make partial delivery the default outcome.
- M33 will separate a mandatory completion floor from depth-specific enrichment targets. A run can be complete only when every planned subquestion has at least one usable, evidence-backed answer; additional drawings, projects and cross-case comparison describe enrichment progress rather than completeness. `partial` remains reserved for a branch with no usable support or a genuine external blocker after bounded recovery.
- The final acceptance must start through the Board so the temporary Chrome permission handshake is exercised. Prior direct-API runs are useful provider evidence but cannot close the product path because they bypass the user-gesture permission request.
- Product Design preflight initially failed only because `python` is not on PATH; the Codex bundled Python runtime is available and will be used instead. The previous in-app browser bootstrap error remains recorded and will be diagnosed with a different supported capture path rather than repeated unchanged.
- Backend audit confirmed one overloaded predicate: `_coverage_satisfied` currently controls both enrichment early-stop and terminal `completed`, while the same `gaps` array contains per-question density, total assets, project diversity and multi-image project goals. M33 will retain those targets for continued enrichment but introduce a separate completion floor for terminal status.
- A completion-floor asset must be displayable, relevant, partial/verified and have a persisted EvidenceClaim; a visual lead or a nominal partial row without evidence cannot complete a branch. The same asset may support more than one branch only when the current branch received an explicit association from its own query/analysis path.
- The live 1/4 branch outcome has a concrete Firecrawl cause: `parsed_pages` and project-child URLs are cached for the whole run, but `_persist_expanded_project_page` is called only during the first parse. If a later subquestion independently returns the same source, no existing child-page evidence is associated with that branch. Reusing an already parsed child page after a query hit is bounded and costs no extra fetch, but it must remain an explicit branch association rather than a global case-library link.
- Chrome audit found two deterministic product defects beyond the direct-API acceptance mismatch: terminal runs revoke site access, yet ordinary retry does not request it again; and page inspection enumerates only the initial viewport even though the extension already exposes a safe fixed scroll action. The Board must also distinguish “some extension socket is connected” from “this page can call the extension bridge”, allowing public-web research to continue when only the latter is absent.
- Code-level user review found additional defects for the final audit rather than silent scope expansion: empty branch sections are filtered, unassigned assets are forced into the first branch, private/share “board export” is currently a JSON manifest rather than a visual board, remote image failures have no recovery state, in-progress results are not streamed, selection state is overloaded, and StyleProfile is a manual form despite extraction-oriented copy.
- The completion contract is now implemented as two persisted layers. `gaps` contains only missing answer branches; `enrichment_gaps` retains asset density, project diversity, verified/partial volume and multi-asset project ambitions. The search loop still stops early only on enrichment satisfaction, while terminal `completed` depends on evidence-backed branch coverage.
- A live relay probe using the configured gpt-5.5 runtime confirmed one-image and two-image low-detail structured visual requests both succeed. The earlier four-image live batch produced a 500, so the classifier now retries only the first two candidates when a four-image request is rejected by a server-side 5xx. The worst-case reserve is raised to 60 seconds; validation, timeout and client errors are not retried.
- Cached Firecrawl project pages are now reused only when a later subquestion independently returns the same parent source. Already parsed child pages are not fetched again; their exact image/page evidence is explicitly associated with the current branch and only new associations count as progress. A regression test proves one child page is fetched once but can support all three independently searched Quick branches.
- The result UI no longer assigns orphan assets to the first subquestion. Every promised branch stays visible, with a truthful empty-evidence state when necessary, while unassociated assets appear in a separate “待归组的图纸线索” chapter and do not count as an answer.
- M33 completion recovery must be goal-specific. Applying the extra precedent completion pass to `source_lookup` changed its bounded six-query contract without improving reverse-image verification.
- A provider result can carry source-bound facts while an explicitly requested Chrome inspection still fails. Treating repeated provider associations as a fully inspected result hides that failure, so the run now remains partial with `browser_inspection_incomplete` while retaining every public-web asset.
- Query-generation retry semantics are intentionally asymmetric: service recovery inherits completed query keys from the immediately failed execution, while a user-requested retry starts a new generation and replays the full depth plus completion-recovery contract.
- Chrome availability and Chrome execution failure are different product states. A disconnected extension must not prevent public-web research from completing; once an inspection command is actually attempted, a failure is a real provenance gap and must remain visible.
- Cross-source source promotion requires one identity rule in both provider and browser persistence. A unique exact remote image URL is strong enough to merge the candidate across pages; publication strength determines whether the canonical source is upgraded, and weaker later results must not recreate a duplicate.
- M33 live runs `16dac046-5034-4b4f-91b7-4307b9388609` and `34c96f98-8c84-48ef-bdb3-c441a393d86f` used the new per-branch recovery page capacity, but both remained partial at 3 usable drawings, 1 project and 1/3 covered branches. Chinese recovery returned a Zhihu collection, Tekla help and an unrelated paper; forcing an English architecture prefix still returned a paper library, Zhihu, Matterport help and an archive guide. The experiment did not improve evidence coverage and was reverted.
- Current-browser acceptance succeeded at a 739x930 viewport. The result header says `21 项参考` while the diagnosis says only 3 usable drawings; 18 entries are unverified visual leads, so the count overstates usable output. A follow-up geometry check found no actual horizontal overflow: status actions and every workbench button have matching client/scroll widths and remain inside the 692px work surface. The apparent right-edge clipping came from the screenshot presentation and does not justify a breakpoint change.
- Final user-facing residuals after M33: the configured relay's gpt-5.5 web search repeatedly hits the 30-second circuit boundary; Firecrawl alone cannot guarantee complete evidence for every architecture question; a real user-profile Chrome permission/crop pass still requires the extension popup grant; TinEye has not been live-accepted with a user key; StyleProfile remains manual editing; workspace deletion still lacks a visible Board entry. None of these are hidden behind a `completed` state.

## M34 complete-answer delivery and portfolio demos

- User intent is stricter than M33's honest partial terminal delivery: every selected depth should pursue a complete answer across all planned branches. The truthful boundary remains that unavailable evidence or an external provider outage cannot be relabeled as complete; the product should retain progress and offer automatic/manual continuation instead of presenting partial as a normal finished outcome.
- Firecrawl credits are now a release asset. No more live research or screenshot preparation should consume them during M34. Three portfolio screenshots must come from deterministic replay/demo data, and the live client needs an explicit reserve gate before starting another paid call.
- M34 success is behavioral: red-first tests for complete-floor continuation and reserve enforcement, deterministic Quick/Balanced/Deep demo routes, screenshot acceptance, and a user-view audit of other completion blockers.
- Current code already has a strict `_completion_satisfied` predicate and a separate enrichment predicate, but precedent research still has exactly one completion-recovery round and can terminate `partial` on `no_new_assets` or budget exhaustion. M34 should change the user-visible lifecycle and bounded continuation policy, not weaken the evidence predicate.
- Firecrawl is instantiated as one `FirecrawlPageParser` and every call currently reaches `/search` or `/scrape` without an account-level reserve concept. The smallest concrete gate belongs inside that client so all workflow paths share it; it must fail closed before an HTTP call and expose a deterministic reason to Trace.
- Board already supports a fully local `?demo=1` mode backed by `apps/board/src/data/mock.ts` and local `/public/demo` images, so portfolio variants should extend that mechanism (`demo=quick|balanced|deep`) instead of creating API workspaces or consuming provider quota.
- Firecrawl's current official v2 API exposes `GET /team/credit-usage` with `remainingCredits`, and its search response reports credits used. This supports a deterministic preflight reserve gate instead of guessing local usage. M34 will check remaining credits before every paid search/scrape when a reserve is configured; inability to verify the balance fails closed. Sources: https://docs.firecrawl.dev/api-reference/endpoint/credit-usage and https://docs.firecrawl.dev/api-reference/endpoint/search.
- The current Board calls incomplete terminal work “已交付部分结果” and labels the action “重试研究”. That language treats incompleteness as a finished deliverable. Precedent research with uncovered branches should instead be terminal `blocked` with retained interim evidence, and the action should say “继续补齐研究”; `partial` remains valid for other goal types and recoverable browser/source verification gaps.
- First live Quick demo acceptance at the browser's actual 1280×720 viewport has no horizontal overflow (`scrollWidth=1265`) and correctly shows three subquestions plus the local demo label. The screenshot also exposes a portfolio weakness: the mode distinction is present only in small text above the task, while the first fold is otherwise almost identical across all three modes. Add a concise depth contract beside the task so three portfolio captures are visibly different without scrolling.
- The first browser navigation failure was environmental: both localhost services were stopped, and the claimed tab reached Chrome's offline interstitial. `scripts/start.ps1` restored Board/API without any provider call; a fresh tab then loaded the Quick fixture successfully. Do not treat the offline interstitial as a Board regression.
- M34 closes the normal product-path issue: Quick/Balanced/Deep now have the same complete-answer floor, a shared 30-minute safety ceiling and three recovery rounds; a user continuation dynamically skips evidence-complete branches. “Depth” therefore means decomposition and analysis richness, not willingness to leave holes.
- A hard external blocker remains a product boundary, not a status trick. When provider search, source access, rights/evidence binding or the Firecrawl reserve prevents completion, the run retains evidence as `blocked` and exposes “继续补齐研究”; automatically spending without a ceiling would conflict with the user's quota requirement.
- Firecrawl reserve enforcement is implemented and mock-tested against the official remaining-credit contract with a default floor of 100. The Board does not yet show the live remaining balance or let a non-technical user change the reserve; today that setting is environment/configuration level.
- Three accepted local replay pages visibly encode 3/4/6 subquestions at 1280×720 with no horizontal overflow. The original Balanced capture was rejected because the browser screenshot compositor wrote black blocks even though DOM inspection found none; a fresh-tab recapture is clean. Files are correctly stored as JPEG under `docs/assets/portfolio-demos/`.
- Final user-view residuals after M34: relay web-search reliability still limits real recall; TinEye has no real-key acceptance; signed-in Chrome still needs the extension permission gesture; StyleProfile remains manually edited; workspace deletion lacks a visible Board entry; live Firecrawl balance is not yet visible in the UI. These do not weaken or bypass the completion predicate.

## M35 XHS visible-page research and real portfolio acceptance

- The user clarified that replay screenshots are not the final portfolio proof. Quick/Balanced/Deep must each run against real providers and show a genuine result page; replay routes remain useful only for development, tests and emergency presentation fallback.
- Firecrawl and model credits should not be spent while building XHS support. The final three real runs are a separate acceptance phase after the code gate passes, with the remaining balance checked before starting each run.
- Account authorization means the user signs in to Xiaohongshu in their own Chrome and explicitly grants the extension page access once. Credentials and browser storage remain outside ArchResearch. Only visible search results, media, captions and public source links may enter the temporary workspace; permission remains user-revocable.
- The integration must stay inside the existing enumerated browser DSL: open/navigate, fixed scroll, enumerate visible media, read bounded metadata, capture regions and type only into an explicitly identified search field. No arbitrary JavaScript/selectors from the backend, anti-bot workarounds, CAPTCHA handling, social actions or general form submission.
- Official Xiaohongshu material confirms a first-party search-result concept with a required keyword and a note-search target, but the published entry is an app deeplink rather than a documented public web-search API: `https://pages.xiaohongshu.com/activity/deeplink` (`xhsdiscover://search/result`). The mini-app open platform exposes mini-app/business capabilities, not a general public-note search API. Therefore M35 should integrate the visible signed-in web UI through Chrome rather than inventing or reverse-engineering a private HTTP endpoint.
- The official user-agreement host at `https://agree.xiaohongshu.com/` is JavaScript-rendered and yielded no searchable text through the read-only web fetch. This prevents claiming a blanket automation permission from the terms. The conservative product boundary remains user-initiated, low-volume visible-page research with source links and temporary local retention.
- Public GitHub projects found in the survey focus on cookie-based MCP automation and automatic publishing. Those approaches conflict with ArchResearch's no-cookie, no-social-action and no-remote-code boundaries, so they will not be integrated. The useful pattern is only local visible-page clipping; implementation stays inside the already shipped extension protocol.
- The extension already has the exact safe primitives M35 needs: public-URL navigation, fixed wait/scroll, bounded page metadata/snapshot, visible-media enumeration, bounded region capture and search-field typing. The backend cannot send selectors or JavaScript, and `safe_click` is intentionally a no-op. XHS support does not require a second automation runtime or cookie-bearing MCP service.
- The missing link is result association. Current media enumeration returns image URL, alt, figure caption and viewport region, but not the enclosing public note URL; Xiaohongshu search thumbnails therefore cannot become source-addressable cards. The minimal protocol-compatible enhancement is a bounded same-page `link_url` derived locally from the nearest safe HTTP(S) anchor plus bounded nearby card text.
- Current workflow only inspects URLs already returned by OpenAI/Firecrawl. A low-volume signed-in source search must run before that inspection loop and merge its note URLs as ordinary unknown/aggregator `ProviderSource` records. XHS search pages and note pages must share the existing page/visual budgets; no unbounded feed crawler is needed.
- The Board currently has no domain/source preference in its request contract. Reusing `allowed_domains` for XHS would incorrectly constrain OpenAI and Firecrawl to Xiaohongshu and reduce architectural-source diversity. M35 needs an explicit persisted `research_sources` choice instead of overloading the existing domain filter.
- When XHS is selected, Chrome is no longer an optional enhancement: the run depends on the user's signed-in visible page. The composer should expose a clear “小红书灵感” source toggle, require a connected/temporarily authorized extension before starting, and tell the user to sign in themselves. When it is not selected, the current public-web fallback remains unchanged.
- To limit account and platform load, M35 should issue at most one Xiaohongshu search-page visit per planned subquestion in each execution generation, collect at most four note links per visit, and then process those note pages through the existing shared page/capture budgets. Depth does not authorize feed crawling.
- User clarified that source value depends on intent. ArchDaily, project sites and architectural publications have the project identity, description and drawing set needed for `precedent_research`. Xiaohongshu and Pinterest are more useful for sparse visual inspiration such as drawing language, massing studies and analysis-diagram techniques. Routing and ranking must preserve that distinction rather than treating every source as an interchangeable case record.
- Pinterest's current official developer rules explicitly prohibit automated scraping/data extraction except where Pinterest permits it, while its help material positions Pinterest as a visual idea-discovery/search product. M35 must not add direct Pinterest Chrome enumeration or Firecrawl parsing without official permission. A Pinterest preference may bias provider/web-search queries and retain original Pin links as `visual_lead`, but those links cannot satisfy case evidence completeness.
- For XHS, the same sparse-post limitation is encoded in evidence semantics: the search-page and note link can support a visible-image observation, but it remains an aggregator visual lead with unknown rights and cannot by itself confirm project identity, asset association or a design-strategy fact.
- Implemented source behavior is now deterministic rather than prompt-only. Any provider asset whose source host is Xiaohongshu, Pinterest or `pin.it` is forced to `aggregator / unknown identity / unknown association / unknown primary / unknown rights / visual_lead`; unsupported facts, project context, mechanism and transfer steps are removed while direct visual observations remain. Pinterest hosts are excluded from both Chrome inspection and public-page parsing. XHS search results skip public parsing but may enter the existing Chrome crop classifier after explicit authorization.
- XHS execution is bounded to one search-page visit per planned subquestion for the current execution, one fixed scroll and four note links. The search page consumes the existing browser page budget; note-page crops consume the same remaining page and visual-classification budgets. The full-workflow regression proves three Quick subquestions create exactly three search visits and three persisted aggregator source pages.
- Source availability is isolated: a relay/model web-search timeout opens only the model circuit. It no longer terminates remaining XHS branch searches when the authorized Chrome source is still available, and XHS failures likewise retain public/model results with an explicit incomplete-browser trace.
- Xiaohongshu is now a Board product default rather than an optional discovery checkbox. It is restored for every new research request; if the user's signed-in Chrome bridge is unavailable, the Board blocks before launch with an explicit connection instruction instead of silently omitting the default source. Pinterest remains opt-in.

## M36 zero-cost release preflight

- The smallest useful product check is one compact, persistent section inside the existing research form, replacing the disappearing Chrome-only notice. It uses flat status rows rather than a settings page, modal, metric tiles or a dashboard.
- User clarified that Firecrawl balance management is not part of the finished product. No balance endpoint, meter, reserve copy or settings surface should be added; Codex preserves quota internally during development and final portfolio execution.
- API-level Chrome connection, current-page extension bridge availability and persistent host permission are distinct states. The Board combines the API connection with the local extension bridge; the backend must not claim permission it cannot see.
- Chrome connection cannot prove Xiaohongshu login. Step 1 therefore says “登录态待可见页面验证”; the later source-only acceptance will use an explicit bounded visible-page check without reading cookies, storage, account identity or private content.
- Model configuration and capability are also excluded from this launch strip. A true capability probe consumes a real request and belongs to the final live acceptance, not the Chrome/XHS status UI.
- Narrow live-browser QA found the first status strip was too visually heavy because disconnected rows reused a yellow warning surface; that conflicts with the global rule reserving marker yellow for the single homepage prompt note. The corrected strip keeps neutral flat rows and uses icons plus explicit copy for state.
- A backend WebSocket connection can belong to another Chrome tab while the current page has no extension bridge. The readiness model now requires both signals before claiming current-page availability, treats `disconnected/error` as not authorized even when the permission bit is stale, and leaves Xiaohongshu login explicitly unverified.
- The in-app browser viewport measured 459×986 with `scrollWidth=444`; the preflight had no page-level horizontal overflow. Desktop behavior remains covered by the production build and packaged browser gates until supported Chrome visual control is available again.
- Final narrow-browser inspection kept both status labels on one line, retained 44px touch targets and confirmed `scrollWidth=444` within the 459px viewport. The local detector reports zero design-rule findings.
- The ChatGPT Chrome-control binding was unavailable after its single documented retry, so no desktop Chrome or signed-in XHS claim was fabricated. Repository builds and the packaged 6-case extension E2E remain green; the later user-authorized XHS source check stays an explicit pending gate.

## M36 bounded Xiaohongshu acceptance

- The running API is healthy on `127.0.0.1:8000`; its browser broker was initially disconnected. Calling the shipped local Chrome handoff opened `?connect=chrome`, and the packaged ArchResearch extension authenticated after three half-second polls. This used no research provider.
- There is intentionally no public endpoint for arbitrary browser commands. A normal run would also invoke its configured research provider and optional Firecrawl parser, so it is not acceptable for the source-only gate. The safe acceptance route is an ephemeral local broker using the shipped `BrowserBroker`, `PairingAuthority` and `XiaohongshuBrowserSearch` only; it never loads provider configuration or Windows provider credentials.
- `XiaohongshuBrowserSearch` already enforces the exact live boundary: one search URL, one 1200px downward scroll, two visible-media enumerations, at most four same-domain note links and a guaranteed tab close. The extension rejects every command until the user grants `<all_urls>` from its own UI; terminal notification closes managed tabs while explicit revoke or uninstall removes permission.
- The first harness attempt stopped both API and Board, so the extension had no local page from which to re-pair and correctly remained disconnected. The corrected attempt kept Vite alive, opened the shipped Chrome handoff and authenticated the extension to the provider-free broker.
- The final M38-era source-only check separated connection success from extraction success. Keeping Board on 5173 while replacing only API 8000 let the extension authenticate and execute `open_url → wait → enumerate_media → scroll → wait → enumerate_media → close_tab`; the second viewport returned 13 schema-valid media candidates, but none were images or source-bound notes. Earlier fully loaded runs had exposed 15 images, so a red test raised the bounded initial/scroll waits to 3.5/1.0 seconds. The focused XHS test, Ruff and Mypy passed, but the one post-fix live retest ended in `TimeoutError`. This is a real extraction blocker, not permission, pairing, provider or quota failure; no further live retry is permitted in this checkpoint.
- The provider-free claim is structural rather than telemetry-derived: `scripts/accept-xhs.py` constructs only `BrowserBroker`, `PairingAuthority` and `XiaohongshuBrowserSearch`. Its `provider_calls: 0` field is an audit marker, not a provider meter. The acceptance output now omits the raw query and requires two enumerations, at least one image and at least one returned note source before reporting `completed`.
- The corrected source-only attempt recorded exactly one sanitized `open_url` action and zero provider calls. The extension rejected it with `BrowserCommandError` before any Xiaohongshu navigation because the current task permission was not granted; no scroll, media enumeration or note content occurred.
- Chrome control can read the local Board, but browser security policy blocks `chrome://extensions/`; Codex cannot operate the toolbar popup or change permission through an alternate route. One explicit user click is the remaining live acceptance gate, after which later research does not ask again.
- Automatic pairing and persistent page access are separate product states. The Board already exchanges the one-time code and the extension persists the server-rotated local token, so later sessions must not ask for another code. Chrome may report a declined permission as a valid status with `research_permission: false`; the UI validates that post-condition instead of relying only on rejected messages.
- The real popup screenshot proved the permission request still did not reach Chrome: the click was forwarded to the service worker before `chrome.permissions.request`, so the browser no longer treated the API call as the direct user gesture. The permission API must run synchronously from the popup/side-panel click handler, then the fixed background command may only synchronize the granted state into the socket client.
- User rejected per-run permission prompts. M36 therefore changes the retention boundary from task-scoped host permission to explicit one-time approval: `<all_urls>` remains granted across research completion and reconnects until the user clicks the dedicated revoke control or uninstalls the extension. Managed research tabs still close at every terminal state, and the browser DSL, no-cookie boundary and manual revoke remain unchanged.
- M36 恢复确认：用户已通过 Chrome 的直接点击授予持久网页读取权限。配对、权限、打开小红书与任务结束关闭受管标签页均已在真实链路证明；当前唯一的签入态验收阻塞收敛为小红书重 DOM 在首次 `enumerate_media` 时超时。下一步必须在扩展端限制扫描节点与返回候选数量，并继续保持零 OpenAI/Firecrawl/TinEye 调用。
- 小红书超时修复采用扩展端确定边界：`collectMedia` 按 DOM/首屏顺序最多检查 500 个 `img/canvas/svg`，得到 100 个合格候选即停止，并把可见性判断与尺寸判断共用同一次 `getBoundingClientRect`。受管页面每次命令前通过固定包内 `assets/content.js` 注入，新打开的验收页可读取当前构建，不需要重新授予持久权限。
- 重页面修复后的 provider-free 实测不再超时：4 秒内完整执行 `open_url → wait → enumerate_media → scroll → wait → enumerate_media → close_tab`，协议边界为 true，`provider_calls` 为空。结果仍为 `no_visible_note_links`，说明剩余缺口已从性能/权限问题收敛为小红书当前卡片 DOM 的媒体—笔记链接关联。
- 脱敏枚举诊断确认首屏存在 15 个合格媒体候选，但 `with_link_count` 为 0。问题不是没有图片，而是图片没有直接嵌在可识别的链接中。回归范围已从简单兄弟节点扩展到深层图片容器：解析器仅把 `section.note-item/article/li/figure` 视作语义卡片，同时提取同卡标题；普通 `div` 卡片保留最多四层、最多八个链接的同源兜底，页面级普通 `section` 明确排除。
- 现有后端已经具备图像级基础：每个 XHS 笔记页最多枚举 6 张图，Quick/Balanced/Deep 的全局视觉分类上限分别为 12/36/72，逐图记录类型、相关性、观察、原图 URL、裁图与感知哈希。真正缺口在结果编排：Board 当前把所有 visual lead 按 `project_name` 塞进“项目案例”，并把“待核验项目”计入项目数量，造成证据语义错误。
- 用户明确了两类来源不是固定高低关系：ArchDaily/事务所官网主要提供落地案例与方案设计机制；小红书主要提供制图表达、图纸风格、形体推演和分析图语言。当前工作流始终按 publication tier 优先检查网页，会让视觉参考任务先耗费视觉预算在建筑网站上，因此必须按 ResearchGoal 切换检查顺序。
- M37 首轮 1440/390 Playwright 截图确认 6 张图片全部加载且页面无横向溢出，但“子问题 → 每种类型一整行 → 单卡”在类型分散时产生大面积空白和过长页面。类型应改为紧凑索引，资产在同一子问题内连续排布；卡片类型标签继续承担逐图识别。
- M37 最终视觉验收采用“子问题标题 + 类型数量索引 + 连续图网格”：1440×900 与 390×844 均加载 6/6 图片，页面无横向溢出；视觉参考任务不再渲染空的方案案例区。Impeccable 检查为零发现。
- M36 链接修复采用红测约束：普通兄弟标题链接与六层 `section.note-item` 嵌套图片均必须返回同源笔记 URL 和中文标题。第一次实现把任意 `section` 当卡片，真实页首次枚举超时；新回归要求页面级普通 `section` 不得关联图片，并把范围收紧到明确卡片语义。公开资料建议的 `web_explore_feed` 在当前登录态也发生加载超时，已撤回到此前实测可完成的 `web_search_result_notes`。134 条扩展测试、Extension lint/typecheck/build 与 XHS API 聚焦测试通过；复验继续保持零 OpenAI/Firecrawl/TinEye 调用。
- 收紧卡片范围后，真实运行仍在第一次 `enumerate_media` 的 30 秒边界前没有任何枚举结果，说明超时发生在 `chrome.scripting.executeScript` 等待动态文档进入默认 `document_idle`，而不是 `collectMedia` 内部。Chrome 的固定脚本注入现显式使用 `injectImmediately: true`，仍只加载随扩展打包的 `assets/content.js`。134 条扩展测试、lint/typecheck/build 与 6 条打包 Chrome E2E 全部通过。
- 立即注入后的真实尝试仍加载了 Chrome 缓存的旧 `content.js`，因此不能据此判定新解析器耗时。最终实现不再用 `querySelectorAll` 预先物化整页媒体，也不在判定链接上限前复制整个链接集合；单次读取同时受 1.5 秒、500 个节点和 100 个结果三重边界约束，超限时返回已取得的部分结果。全仓门禁以 198 API、73 Board、135 Extension、6 packaged E2E 全绿退出。
- 用户加载最终包后的约定单次验收仍在首个 `enumerate_media` 处触发 30 秒 `TimeoutError`，且没有任何枚举摘要；`open_url` 与 `close_tab` 正常，`provider_calls` 为空。这证明阻塞在动态页面的内容监听器生命周期/消息调度，而不是媒体循环本身。继续调扫描器或要求重复重载没有证据价值；下一技术节点是任务前临时、精确域名、非持久化的 `document_start` 注册，并在所有终态注销。
- M38 启动检查确认当前 `executeInManagedTab` 在每条页面命令前才调用 `executeScript`，而 `open_url` 只创建并登记标签；这正是持续加载页面的生命周期竞态。Manifest 已有 `scripting` 权限且最低 Chrome 116，动态 `registerContentScripts`、`unregisterContentScripts`、`persistAcrossSessions: false` 与 `runAt: document_start` 均在当前类型定义和运行版本范围内，无需新增高风险权限。
- `registerContentScripts` 的当前 Chrome 类型契约只提供 URL `matches`，没有 `tabId` 目标；按小红书 origin 注册会同时进入用户其他同域标签。为保持“只触碰受管标签”的边界，M38 改用现有 `tabs` 权限：为受管 tab 安装短生命周期 `tabs.onUpdated` loading 监听并立即注入固定包内脚本，关闭/终态时移除监听。动态 origin 全局注册不进入实现。
- M38 packaged-Chrome 验收用一个永不结束 HTTP 响应保持 `document.readyState === "loading"`，并在任何 coordinator 页面命令前从 service worker 直接发送固定内容协议；监听器能够返回 SVG 图块，证明它已在 `open_url` 返回前就绪。扩展没有任何动态注册脚本，受管 tab id 在打开期间存在于 `chrome.storage.session`，关闭后清空。静态页、懒加载、恶意页、终态重连和 FastAPI 裁图共 7 条 E2E 全部通过。
- Chrome 在目标导航前可能补发显式 `about:blank` loading 事件，也可能在目标 `loading` 事件到达时尚未建立可注入文档。最终顺序是：忽略目标前的临时空白事件；观察到安全公共 URL 后仅对固定脚本做 50ms 间隔、5 秒上限的就绪重试；任何页面读取、滚动或输入动作都不重试。
- MV3 工作线程恢复通过 `chrome.storage.session` 找回受管 tab id，并在读取本地配对前关闭遗留标签、清空会话记录。显式撤权和重配对会等待 `closeAllManagedTabs` 完成，再移除权限或建立新连接，避免旧异步清理误伤新任务。
## M38 final concurrency audit (2026-07-15)

- A green 198 / 73 / 149 / 7 repository gate did not cover lifecycle cancellation while `open_url` was still waiting for the early content listener. `BrowserCommandExecutor` needs a lifecycle generation so a tab that resolves after terminal cleanup is immediately removed and cannot re-enter the managed set.
- The loading listener retained the last safe URL after a later unsafe redirect. A private-network navigation could therefore trigger another bundled-script injection from a status-only update. Any unsafe URL must invalidate and dispose the registration immediately, then close the managed tab.
- Listener cleanup removed only `tabs.onUpdated`; it did not cancel the readiness timeout, retry timer, or callbacks from an in-flight injection. The registration must be a disposable object, and `createTab` must consume the readiness promise when navigation/persistence fails before it can be awaited.
- The initial `storage.session` write sat outside `createTab` cleanup. A persistence failure could leave the provisional `about:blank` tab behind. Persistence is part of the same transactional open operation and must fail closed.
- These are release blockers despite the passing gate. M38 remains `in_progress` until red-first regressions, minimal fixes, packaged Chrome E2E, the full repository gate and `git diff --check` all pass again. No live research provider is involved.
- The red suite reproduced all six extension races plus one stale-socket reply: overlapping navigation, terminal-during-open, unsafe redirect, retry disposal, early navigation failure, session-storage failure and a late result crossing into a replacement WebSocket. The minimal implementation now uses document and lifecycle generations, disposable loading registrations and connection-bound replies; 156 Extension tests, lint and typecheck pass.
- Terminal messages and managed tabs are intentionally global to the single local Chrome bridge. Allowing two API runs at once would let either terminal close the other run's tabs. V2.1 therefore adopts an explicit single-active-run lease: create/retry return a Chinese 409 while another run is active, restart recovery resumes incomplete runs sequentially, and the lease is released only after terminal delivery has completed or timed out safely.
- The last audit found three additional event sequences and each now has an exact regression: status-only loading with a private `pendingUrl`, terminal cleanup during a provisional `open_url`, and reconnect after a one-time code has rotated. The browser port now closes provisional tabs synchronously, unsafe observed URLs cannot reuse earlier safe state, and the live client authenticates with the persisted token.
- Final M38 verification is authoritative: 202 API, 73 Board, 160 Extension and 7 packaged-Chrome E2E tests pass with all lint/type/build/security/evaluation gates, and `git diff --check` exits 0. Live research quota and the signed-in XHS page were not touched.

## M39 XHS command-boundary hardening (2026-07-16)

- The live sequence separated two timeout causes. XHS page-settling delays were pending `wait` commands inside the MV3 service worker, even though no browser access occurs during the delay. They now run in the local Python research worker; the browser still owns only open, read, scroll and close actions. A red-green API test proves there are no browser-side wait commands and preserves exact 3.5/1.0-second bounds.
- M38 waited only for the first managed document. A later safe hard navigation started a new bundled-script injection but `sendContentCommand` could immediately target the old document. Each loading generation now exposes its current readiness; page commands wait for the latest generation and target the `documentId` returned by packaged `executeScript`.
- `chrome.tabs.sendMessage` previously had no extension-side deadline, leaving the API to discover a missing reply after 30 seconds. Content messages now have a 3-second bound. A read-only command may reinstall the fixed bundled listener once only when Chrome immediately reports no receiver; a timed-out read, scroll, input or click is never replayed. The underlying promise is handled even if Chrome settles it late.
- The packaged browser gate now hard-navigates one managed tab from a finished document to a second response that remains in `loading`, then immediately enumerates the second document. This raises the deterministic extension baseline to 165 unit tests and 8 packaged E2E cases; lint, typecheck and production build pass without live provider or XHS traffic.
- The authoritative post-M39 repository gate passes at 202 API / 73 Board / 165 Extension / 8 packaged Chrome E2E. The first provider-free live attempt after that gate recorded `failed_action=enumerate_media`, no completed enumeration and a backend `TimeoutError` after 30 seconds. Because the new port rejects any page-message timeout after 3 seconds, this timing proves that the user Chrome was still executing the pre-M39 service worker; it is not evidence against the new command boundary.
- Closing only Chrome's visible main window did not unload every process in the profile, so reopening the window preserved the old unpacked extension worker. Before any further source request, all Chrome processes for this single-Board profile must exit and a fresh browser process must start after the `dist/background.js` build timestamp. This is a build-activation check, not another XHS retry.
- The ChatGPT Chrome-control channel remained unavailable after its documented two-second retry, while the separate ArchResearch broker stayed connected. Browser policy therefore forbids Codex from substituting shell or computer control to operate `chrome://extensions`. The only remaining activation step is one explicit user click on the ArchResearch unpacked extension's reload control; after that, the next acceptance can distinguish the new build immediately from its 3-second message boundary and sanitized `failed_action`.
- After the explicit unpacked-extension reload, the shipped handoff restored the persistent pairing without another code. The provider-free acceptance then completed `open_url → enumerate_media → scroll → enumerate_media → close_tab` in 5.3 seconds. Both enumerations returned zero candidates, so M39 fixes the lifecycle/timeout claim but does not fabricate XHS assets. The workflow already treats a successful empty XHS search as source-empty and continues public/model research; only an exception marks browser inspection incomplete.
- The one remaining signed-in XHS acceptance is currently waiting on the external browser environment: Google Chrome is installed but not running, and Codex's separate ChatGPT Chrome-control native-host registration is absent. This does not affect the packaged ArchResearch extension tests or reopen M38; no XHS navigation or provider call occurred during diagnosis.
- 2026-07-16 作品集 Quick 真实运行 `d13bdc67-d6b3-4006-8d89-089439a02311` 终止为 `blocked/budget_exhausted`，不能作为完整结果：3 个子问题仅覆盖 2 个，3 张 `partial` 全部属于同一个南头 if 创意工厂项目；`public_service_flows` 在 5 次定向补齐后仍无可显示证据。首次 OpenAI 搜索在 30 秒后 `APITimeoutError`，之后整次运行均由 timeout circuit 跳过；Firecrawl 能持续返回 4 条结果，但后续主要落到知乎、行业页和弱相关 PDF，未形成新资产。远程视觉分类还出现一次 `InternalServerError`，现有卡片缺少 observations、mechanism、transfer strategy 和分支分析。按真实作品集门禁冻结 Balanced/Deep，先用 Trace/fixture 修复权威项目页召回和流线证据提取，不继续付费运行。
- M40 离线复现确认四个共同根因：(1) 同一分支的 Firecrawl 恢复轮只重复中英文两条固定查询；(2) 一次模型超时会永久熔断剩余 28 分钟；(3) Firecrawl 的 ArchDaily 结果仍为 `unknown`，有限解析槽位会先被弱来源占用；(4) 远程视觉批次在合集页先启动，具体项目页的图纸没有中文可见观察。修复后恢复轮换检索角度并收窄到 5 个可信建筑出版域，ArchDaily 等来源得到 `trusted_secondary` 排序，一次超时只允许一次恢复调用，合集页把唯一视觉批次让给展开后的项目图纸；直接观察会合并回已有 `partial` 卡及对应子问题分析。
- M40 第一次续研仍为真实 `blocked`：只研究了缺失的 `public_service_flows`，但两次有界 gpt-5.5 网页搜索均在 30 秒超时；Firecrawl 虽接收 `includeDomains`，仍返回知乎等越域页面，5 轮无新增资产。XHS 的一次 `BrowserCommandError` 又把案例研究标成 `browser_inspection_incomplete`。由此新增三条确定性修复：Firecrawl 返回值本地二次域名过滤；XHS 在 precedent research 中只记补充源失败、不阻塞正式 3/3 覆盖；可信直接项目页上的已命名图纸也能绑定 URL/图注升级为 `partial`，不再只支持“合集→子页”路径。
- 为避免 gpt-5.5 网页搜索超时后卡片只剩一句归属，OpenAI provider 新增有界的 `public_page_analysis`：对已收集的可信具体项目页做纯文本结构化分析，不再调用 web search；每个子问题最多一次，输出设计机制、转译步骤和边界。正式事实必须携带 `page_text` 中可逐字匹配的摘录，本地校验失败时只保留方法推断，不写事实或项目条件。直接可信域与展开后的可信项目页均可使用；弱来源和合集页不能触发。
## 2026-07-16 M40 relay latency and gpt-5.6sol probe

- The relay dashboard screenshot shows recent requests taking about 77–97 seconds end to end. This exceeds the application's current 30-second model-search timeout, so relay/upstream latency must be measured separately from local workflow timeouts.
- Before changing production defaults, use a minimal no-tool, short-output request to discover and verify the exact `gpt-5.6sol` model identifier. The probe must not call Firecrawl, TinEye, XHS, or web search and must never print the stored API key.
- The persisted non-secret runtime configuration remains on `gpt-5.5` for research and vision. The project API virtual environment is available at `apps/api/.venv`, so the new model can be probed through the same OpenAI-compatible client and Windows credential backend without copying the Key into a command or file.
- The relay's authenticated `/models` response advertises the exact identifiers `gpt-5.6-luna`, `gpt-5.6-sol`, and `gpt-5.6-terra`. A no-tool `gpt-5.6-sol` Responses probe returned exactly `OK` in 4.17 seconds and reported `gpt-5.6-sol` as the serving model. This proves basic Key/model connectivity, not yet web-search suitability.
- A production-shaped `gpt-5.6-sol` probe using strict `ProviderSearchResult`, required `web_search`, low search context, and an ArchDaily domain filter failed at the relay/CDN with HTTP 504 after about 60 seconds. The application did not impose its 30-second provider timeout in this isolated probe. This isolates the slow/failing path to relay-backed web search rather than basic model connectivity; production must remain on `gpt-5.5` until a no-search structured probe and a successful search comparison exist.
- A no-search strict Pydantic probe on `gpt-5.6-sol` succeeded in 5.27 seconds. The model is therefore compatible with the project's structured page-analysis shape; the failed capability is the relay's web-search path, not Responses structured output.
- A second no-search probe using `gpt-5.6-sol`, `reasoning.effort=medium`, strict structured output, Chinese mechanism analysis, and two transfer steps succeeded in 5.97 seconds. Medium reasoning is viable for planning/page analysis, but must remain separate from the relay web-search call that already failed with a 504.
- Firecrawl's official v2 Search documentation confirms that the current request shape is correct: `includeDomains` accepts hostname-only values and `site:` is a supported query operator. Therefore the observed off-domain live responses are not caused by our parameter spelling. Keep deterministic local domain filtering and add the redundant `site:archdaily.com` operator for trusted recovery queries. Source: https://docs.firecrawl.dev/api-reference/endpoint/search
- ArchDaily exposes a public project index at `https://www.archdaily.com/search/projects`, and its project pages have stable numeric-slug URLs visible from the official project listings. A real-time fallback can scrape this public index/search surface and expand only same-host numeric project links; it must not persist a platform case library or treat the index page itself as project evidence. Source: https://www.archdaily.com/search/projects
- ArchDaily also exposes stable topic/category routes such as `/category/adaptive-reuse` and `/search/projects/.../categories/adaptive-reuse`, which list concrete numeric-slug project pages. The current M40 query already uses `site:archdaily.com`, but sends five trusted domains together in `includeDomains`. Since live Search returned only off-domain items that the local gate correctly removed, the next behavior change should make each completion-recovery request target one trusted architecture domain at a time (ArchDaily first) rather than asking one weak search batch to satisfy five domains.
- Code inspection confirms `_public_search_domains()` currently returns the complete five-domain trusted tuple for every recovery round, while `_public_search_query()` hard-codes only ArchDaily/ArchDaily CN site operators. The query and domain filter can therefore diverge and later recovery rounds do not change source channel. The smallest corrective contract is one deterministic trusted domain per recovery round, with the same domain used in both `site:` and `includeDomains`.
- The red behavior test reproduced the mismatch: recovery received all five trusted domains instead of one. The implementation now rotates one deterministic domain per recovery round and derives the matching `site:` operator from that same value. Focused workflow and all public-page parser tests pass; user-provided `allowed_domains` remain unchanged.
- A quota-bounded live Firecrawl Search using the new one-domain shape (`site:archdaily.com` plus `includeDomains: ["archdaily.com"]`) returned four valid ArchDaily leads in about 3 seconds, versus zero locally accepted leads from the previous five-domain recovery batch. The leads include adaptive-reuse cultural-space roundups and a Cultural Center tag page; these are discovery surfaces only and must expand to concrete project pages before formal evidence promotion.
- One bounded scrape of the first ArchDaily discovery lead returned 12,000 markdown characters, 40 links, and 22 images, proving the page is parseable. However, `select_project_page_links()` selected the same article with a `#` fragment, two image-gallery child URLs, and a related article as “project pages”; no typed drawing image was found on the roundup itself. The live completion failure is therefore also a deterministic link-classification bug that wastes the two-page recovery budget before reaching actual case pages.
- No further paid source call is needed to reproduce that bug. Add fixtures for canonical-self fragments, ArchDaily gallery/photo children, related editorial articles, and concrete numeric-slug project pages; only the last class may consume expansion capacity.
- Opening the same ArchDaily roundup through the public page reader shows a subscriber/editorial shell: it exposes gallery links, tags, author/navigation links and the canonical URL, but not stable clickable links for the named projects. Therefore a roundup with zero valid project links must not be reclassified as a direct project. For ArchDaily, a concrete project page should have a canonical two-segment numeric-slug URL and a project-style title (`Project / Office`); listing/markdown links with the same project-style anchor can be expanded, while self fragments and `-photo` gallery children are rejected.
- The user's “medium” instruction refers specifically to the OpenAI Responses `reasoning.effort` parameter. The successful probe used `model="gpt-5.6-sol"` and `reasoning={"effort":"medium"}`; production wording/configuration must keep those two concepts separate.
- The user subsequently made `reasoning.effort="medium"` a global model-call rule for both `gpt-5.5` and `gpt-5.6-sol`. All production Responses calls (planning, model search if retained, page analysis, and visual classification) must use medium; retrieval latency is controlled by decoupling/bounds rather than silently lowering reasoning.
- Attempt 7 resumed the same Quick run on the verified build and exercised the new path without opening a second run: an ArchDaily.com recovery lead (`Oslo Opera House / Snøhetta`) was parsed, relay web search was skipped after trusted Firecrawl discovery, and a later Designboom batch was also accepted. Neither source produced a flow asset that raised `public_service_flows` to the evidence floor; the run ended honestly `blocked/budget_exhausted` at 3 usable assets, 1 project, 2/3 branches. The next fix must improve flow-asset relevance/classification, not add another unchanged retry.
- Firecrawl reports 234 credits remaining after attempt 7, so the 100-credit portfolio reserve remains intact. No further live search should run until the visual-classification gate is fixed.
- `_remote_visual_batch_started()` currently treats any historical `remote_visual_batch: started` event as a permanent run-wide lock. The run's first visual batch failed with an upstream `InternalServerError`, yet attempts 2–7 and all newly discovered project pages were therefore barred from visual classification. This explains why the parsed Oslo page exposed 40 images but added zero candidates. The correct durable rule is: at most one distinct remote batch per execution attempt, and never repeat an already-started image fingerprint across attempts.
- Trace events already persist both `retry_count=run.attempt` and `summary.batch_fingerprint`, so the corrected gate needs no schema or migration: block any fingerprint seen before, block a second batch in the current attempt, but allow the first unseen fingerprint after an explicit retry. Existing same-image retry coverage should remain at one call; a new different-image retry regression must move from one to two calls.
- The different-fingerprint retry regression failed on the old run-global lock and now passes. The implementation computes the batch fingerprint before admission, rejects any fingerprint seen in historical `started` events, and rejects a second `started` event with the current attempt number. Three focused tests confirm same-fingerprint deduplication, new-fingerprint continuation, and deadline-reserve behavior.
- Parallel read-only audits exposed three remaining completion blockers. First, a remote classification accepted on a concrete trusted project page still persists as `visual_lead` without an EvidenceClaim, so precedent coverage can never count it. Second, the batch allowance should be fair per `(attempt, subquestion_id)` and a known failed fingerprint may be retried only after an explicit later attempt; completed or crash-unknown batches remain non-replayable. Third, all-untyped pages currently submit the first four images, which biases toward hero photographs instead of drawings later in the gallery.
- Query relevance also remains incomplete: the real flow subquestion omits the main problem's industrial/community-cultural typology, while `_public_typology_focus()` only reads the subquestion; `public service circulation` is ambiguous; trusted-domain status can suppress model discovery even for an irrelevant Oslo result; and `PublicSearchLead.description` is discarded before page-slot ranking. The smallest query correction is to derive typology from main question plus subquestion, use visitor/staff/back-of-house/loading terminology, and rank deterministic relevance before page shape.
- `_persist_expanded_project_page()` already defines the conservative exact-page promotion contract needed for remote classifications: probable project identity, confirmed asset association, trusted publication tier, `partial`, and a same-source EvidenceClaim. The remote path should mirror that contract only when the source is a concrete trusted/primary project page; editorials/listings remain visual leads. Stored remote asset types must also feed `_public_page_drawings()` so the subsequent no-search medium page analysis can produce mechanism and transfer guidance.
- Both ArchDaily classification regressions failed on the old behavior as intended. The selector consumed self/gallery URLs before the concrete project. The editorial-promotion fixture initially used an untyped interior photo and therefore created no candidate; it was corrected to a floor-plan asset so the red assertion specifically distinguishes `visual_lead` from the old false `partial` promotion.
- M40 production behavior now passes the eight focused regressions: every OpenAI Responses call carries medium reasoning; ArchDaily recovery uses one matching domain plus `site:` and `intitle:" / "`; project-link selection canonicalizes away self fragments, rejects gallery paths and only expands project-style markdown anchors; ArchDaily editorial pages cannot promote typed images to `partial` or trigger page analysis merely because they are trusted publications.
- The complete API gate is green after updating two legacy ArchDaily fixtures to realistic `Project / Office` titles. Ruff, format, Mypy, and all API tests pass. No production rule was relaxed to satisfy the old fixtures.
- The authoritative repository gate passes with 213 API, 73 Board, 165 Extension, and 8 packaged Chrome E2E tests plus all lint, type, build, security, and 30/108 evaluation checks. The gate used mocks/fixtures only.
- Before another live continuation, one remaining redundant path should be removed: a completion-recovery batch that already has a trusted Firecrawl source still attempts relay `web_search`. Since that tool path is proven to 504 while no-search medium analysis succeeds, trusted recovery leads should skip model discovery and proceed directly to parse/page analysis.
- The new performance regression failed at two model calls on the old workflow and now passes at exactly one: normal discovery may attempt the model once, but a completion-recovery batch with a primary/trusted-secondary public lead records `trusted_public_sources`, skips redundant relay web search, and continues into page parsing. A companion regression confirms weak public leads still retain the single bounded model-recovery opportunity.
- The final post-performance repository gate passes with 214 API, 73 Board, 165 Extension, and 8 packaged Chrome E2E tests. All production Responses request shapes now use medium reasoning. No live provider or source service was contacted by this gate.
- Pre-restart live state is healthy but still serves the prior process image: run `d13bdc67-d6b3-4006-8d89-089439a02311` is attempt 3, blocked at composing with 3 usable partials, one project, and 2/3 branches; only `public_service_flows` is uncovered after five passes. The persisted provider model remains `gpt-5.5`; after restart its requests will inherit the new global medium reasoning rule.
- The same run is now attempt 4 on the verified build. Browser/XHS is unavailable and truthfully skipped without blocking precedent completion. The first `public_service_flows` Firecrawl batch returned four sources in about 1.3 seconds; no second run exists.
- Attempt 4 normal rounds behaved as bounded fallback: the first medium model web-search call timed out once, later normal-round model discovery was skipped via the timeout circuit, and Firecrawl retained four weak public sources per round. These rounds added no usable flow asset; the run is now entering the domain-targeted completion recovery where the M40 change is expected to matter.
- Attempt 4 was deliberately cancelled at the first completion-recovery result to preserve quota: `site:archdaily.com intitle:" / "` returned zero even though the previously measured same-domain query without `intitle` returned four. The operator is officially valid but too restrictive for this search backend/query shape. Remove only `intitle`; keep the one-domain filter and conservative project-page classifier.
- Without the title operator, the measured result order places editorials before the useful Cultural Center tag/listing page. Since recovery has two page slots per branch, trusted sources need a deterministic within-tier priority: direct project-style titles first, project/tag/category listing pages second, generic editorials last. This lets one listing scrape expose a project link and leaves the second slot for that concrete project.
- The listing-priority regression failed on the editorial-first old order and now passes. The live-proven single-domain query no longer adds `intitle`; within the same publication tier, project-style titles rank 2, project/search/tag/category listing paths rank 1, and generic editorials rank 0. Publication tier remains the stronger key, so this does not promote weak sites over trusted sources.
- The final post-ranking repository gate passes with 215 API, 73 Board, 165 Extension, and 8 packaged Chrome E2E tests. All checks are green after formatting only the touched workflow file; no live service was called by verification.
- Attempt 5 resumed directly at the ArchDaily recovery checkpoint but still returned zero without `intitle`. The remaining difference from the successful manual query is semantic: the generated English recovery query contains the flow issue but omits the Chinese subquestion's typology/context (`adaptive reuse industrial building`, `community cultural center`). Recovery query construction must translate stable typology cues separately from the issue cue and reserve suffix space so `site:...` is never truncated.
- Typology/context extraction now composes independent English cues for adaptive reuse, industrial buildings, and community cultural centers while retaining the flow issue. The recovery query reserves its final characters for the trusted `site:` suffix. Focused regressions pass, and a live call using the exact code-generated query returned four ArchDaily leads in 3.7 seconds with both context and suffix assertions true.
- The post-typology authoritative gate passes with 215 API, 73 Board, 165 Extension, and 8 packaged Chrome E2E tests after formatting only the new fixture. The verified build is ready for attempt 6.
- Attempt 6 resumed at recovery round 3 and still returned zero because that query batch's language was Chinese, whereas the exact English production-query probe returned four. Trusted-domain search language must follow the destination: ArchDaily.com and other international architecture domains use English discovery terms; ArchDaily.cn may use Chinese. Result analysis remains Simplified Chinese through the medium model contract.
- Destination-aware recovery is now covered by a real two-normal/two-recovery fixture. A live probe deliberately passed `language="zh"` for round 3 with target ArchDaily.com; the production function emitted an English architecture query, preserved the site suffix, and Firecrawl returned four leads in 2.6 seconds. This exactly matches the previously failing checkpoint shape.
- Attempt 7's zero-image Oslo parse was not a Firecrawl image shortage: a historical `remote_visual_batch: started` event from attempt 1 globally barred later pages. The durable gate now records `subquestion_id`, allows one batch per subquestion in each explicit attempt, permanently deduplicates completed or crash-unknown fingerprints, and permits a known failed fingerprint only after the run attempt increases. This preserves the charge boundary while preventing one failed branch from starving the other branches.
- Remote classification now becomes formal precedent evidence only when the image came directly from a concrete `primary` or `trusted_secondary` project page. In that case the page supports image association, not the visual interpretation: the candidate is `partial`, association is confirmed, project identity remains probable, rights and primary-source status remain unknown, and a same-source EvidenceClaim binds the page-level association. Editorals, listings and sparse visual platforms cannot cross this gate.
- Untyped galleries are sampled across their full order rather than taking four leading hero images (10-image contract: 1/4/7/10). Accepted remote asset types are read back into `_public_page_drawings()`, so the bounded no-search medium analysis can add mechanism and transfer guidance for images whose filenames and captions reveal no drawing type.
- Trusted-domain membership is no longer enough to suppress model recovery or win a scarce page slot. The workflow retains Firecrawl's bounded description as private transient metadata, calculates task relevance from the English semantic query plus title/URL/description, and applies it within expandable project/listing page classes. This rejects the observed Oslo false positive while keeping a listing ahead of a relevant-sounding editorial that cannot directly supply a project drawing.
- The authoritative post-fix repository gate passes with 222 API, 73 Board, 165 Extension and 8 packaged Chrome E2E tests plus lint, type, build, security and evaluation checks. It used no live OpenAI, Firecrawl, TinEye or XHS calls; Firecrawl remains at 234 credits with the 100-credit portfolio reserve intact.
- Attempt 8 proved that per-subquestion fairness still needs source admission: the first unknown page consumed `public_service_flows` remote vision and both the four-image and two-image gpt-5.5 calls failed with `InternalServerError`. The run was cancelled before later Firecrawl rounds. Precedent research now spends remote vision only on concrete primary/trusted-secondary project pages; visual-reference research may still classify unknown visual pages. The post-fix repository gate passes at 223/73/165/8.
- A real one-image `gpt-5.6-sol` remote visual classification with `reasoning.effort=medium` succeeded in 8.7 seconds, correctly returned `section`, relevance 4 and four Chinese observations. This supports using 5.6-sol for the no-search visual path. The user then requested 5.6-sol for every model path; implementation is intentionally paused mid-transition, with default-contract tests changed first and production defaults/config/docs still pending.

## M41 local browser research selection

- Browser Use CLI is useful for attaching to an already-open Chrome over CDP and for bounded dynamic-page diagnostics. It is not the production scraper: autonomous use still needs a model, and its profile workflow can synchronize login data to Browser Use Cloud. ArchResearch must not use that profile synchronization or import Xiaohongshu cookies/storage.
- Crawl4AI provides local asynchronous Playwright crawling, Markdown/link/media extraction and deterministic configuration, making it the closest no-credit Firecrawl replacement. If selected, ArchResearch will expose only public-page, read-only operations; proxy, stealth and anti-detection features remain prohibited.
- Microsoft Playwright MCP, Stagehand, Steel, Scrapling and agent-browser were also compared from their official repositories. Their broad tool surfaces, additional model/cloud dependence, Docker/remote-browser bias, anti-detection capabilities or browser-state access make them less aligned with the current narrow JSON action boundary.
- The candidate set is now intentionally small: a local Crawl4AI adapter versus an even smaller direct-Playwright adapter. Public search/page parsing will use the selected deterministic adapter; authenticated Xiaohongshu remains on the existing MV3 extension; all page content stays untrusted and no case library/global index is introduced.
- Direct Playwright has now passed real system-Chrome smoke checks: a public page produced bounded text/links, ArchDaily site search returned four concrete project URLs, and one project page produced 2,613 text characters, 40 links and 35 image URLs without downloading image bodies. The first smoke also exposed and fixed a 30-second wait for a missing optional meta-description.
- Search-engine HTML is not a viable generic default in the current network: Bing HTML returned no result nodes, DuckDuckGo HTML displayed a bot challenge, and Brave displayed a captcha. Bing RSS returned structured items but ignored longer architecture/site queries. Verified architecture-site search pages work for ArchDaily, Dezeen, Designboom and Divisare; short two-word intent phrases rank much better than Firecrawl-style long queries.
- The earlier dependency-only recommendation is not yet the final tool decision. At the user's request, M41 now requires an equal live benchmark of direct Playwright, Browser Use CLI and Crawl4AI on the same search and page-parse tasks before the production adapter is finalized.
- Browser Use CLI 0.13.4 actually installs about 260 MB across 14,230 files and brings OpenAI, Anthropic, Google API, MCP and PDF dependencies that are unrelated to deterministic page extraction. Anonymous telemetry is enabled by default, so the benchmark set `BH_TELEMETRY=0`. The current CLI is backed by `browser-harness`, normally attaches to an already remote-debuggable Chrome and exposes arbitrary `js(...)` and raw CDP. The test instead attached it to a new isolated headless system-Chrome profile and did not enable profile sync, cloud browsers or cookie access.
- On the same ArchDaily task, Browser Use found 18 concrete project URLs, but `wait_for_load` still timed out at 20 seconds and the search took 24.65 seconds. Its project-page run timed out at the 8-second load boundary and finished in 12.46 seconds with 625 body-text characters, 131 links and 44 images. It works only after supplying custom DOM JavaScript; project titles were empty and text completeness was lower than the direct-Playwright baseline. Selection remains open until Crawl4AI is measured under the same conditions.
- Crawl4AI 0.9.2 was installed and executed, not inferred from documentation. Its isolated environment is about 639.4 MB / 20,307 files and includes both Playwright and Patchright, Playwright Stealth, LiteLLM/OpenAI, tokenizers, NumPy/SciPy/Shapely and other broad dependencies. With every stealth/proxy/simulation option disabled and installed Chrome selected explicitly, the ArchDaily search completed in 7.23 seconds with the same 18 project URLs; the project page completed in 5.46 seconds with about 27.3k Markdown characters, 80 links and 51 image records. The extraction is materially richer, but the search page emitted 468 image records and project image variants were duplicated, so substantial deterministic cleanup is still required.
- The first production-backend Playwright rerun completed the same search in 8.08 seconds with 18 projects and the project page in 9.69 seconds with 74 image URLs. Its current `article, main, [role=main]` first-match selector returned only 286 text characters on the live project page, revealing a real parser-selection bug and making the prior 2.6k-character smoke non-reproducible. A normalized one-browser/body-text benchmark is still required before deciding whether Crawl4AI's richer result justifies its 639 MB dependency surface or whether the direct adapter should keep Playwright and fix deterministic extraction.
- The normalized direct-Playwright run reused one isolated browser and the same 3.5-second settle policy. It completed search in 6.99 seconds and the project page in 5.23 seconds, slightly faster than Crawl4AI's 7.23/5.46 seconds, while returning the same 18 project URLs, 6,692 body-text characters, 168 links and 88 image references. This proves the production text deficit is the first-match selector, not a Playwright capability limit. Crawl4AI's apparent 27.3k text figure is raw Markdown including links/images; its model exposes raw/fit variants, so a cleaned-text comparison is needed before treating that figure as four times more content.
- Crawl4AI's normalized content counters change the interpretation: on the project page it produced 28,141 raw-Markdown characters but only 3,005 cleaned-text characters; `fit_markdown` was empty. Direct Playwright exposed 6,692 body-text characters on the same page. Crawl4AI therefore does not recover four times more readable evidence; most of the larger number is Markdown/link/image markup.
- Browser Use was also rerun with an element-readiness condition instead of its failing complete-load wait. With a warm isolated Chrome it reached the search results in 4.51 seconds and the first project image in 1.31 seconds, but the project body still contained only 622 text characters. A fixed 3.5-second wait was faster (4.39/3.75 seconds) but missed all search projects. Its speed comes from attaching to a long-lived Chrome and early selector readiness, capabilities direct Playwright can implement without the 260 MB daemon/LLM dependency and remote-debugging permission surface.
- User replaced the earlier blanket “no anti-detection” boundary. M41 now allows an isolated browser, stable UA/fingerprint compatibility and explicit human takeover for public research, plus user-authorized logged-in reads. Automated CAPTCHA solving, paywall/access-control bypass, default proxy rotation, cookie/password/private-message reads and all write actions remain outside the product. This makes reachability less conservative without turning the research tool into an evasion crawler.
- Browser Harness is not a fourth independent implementation: `browser-use 0.13.4` installs and runs `browser-harness 0.1.5`, so the measured Browser Use results are already Browser Harness results.
- Browser Act 1.0.6 was installed only into an isolated benchmark venv. It occupies about 372.9 MB / 9,966 files and bundles OpenCV, NumPy, AV/FFmpeg, Windows UI automation, cookie APIs, proxy purchase/rotation, CAPTCHA solving and remote assist. Its CLI describes itself as “stealth and captcha bypass”. The public GitHub repository contains the Agent skills, but important runtime modules including paths, extractor, proxy session and stealth Chrome runner are compiled `.pyd` binaries; the runtime is therefore not fully source-auditable from that repository. The skill-vetter prerequisite check also reported `aguara`, `skill-scanner` and `jq` missing, so the Agent Skill itself was not installed into Codex or the product.
- Browser Act's actual read test reached the CLI but not the target page. After the required `get-skills main` and `get-skills core --skill-version 2.0.2` handshake, `stealth-extract` exited in 1.91 seconds with error 230103 and required a BrowserAct API key/registration link. No ArchDaily request or output file was produced. This directly disqualifies it as the no-key, no-credit, fixture-testable public-page engine; no account was created and no user credential was requested.
- Provisional M41 leader remains direct Playwright, but the user expanded the benchmark before finalization to Microsoft Playwright CLI and jackwener/OpenCLI plus an explicit Xiaohongshu track. No final production selection is valid until those measurements are recorded.
- Microsoft Playwright CLI installs as only about 17.2 MB / 193 files in the isolated Node prefix. It exposes persistent in-memory sessions, Chrome selection, DOM eval, network inspection and element readiness over the same Playwright engine; its potential value is lower per-command latency and ready-made session control, not a different renderer.
- OpenCLI 1.8.6 installs as about 22.5 MB / 3,582 files and is fully open source, but generic browser commands require its own Chrome Browser Bridge extension and daemon. `doctor` started the local daemon on port 19825 and correctly reported the extension missing. The package exposes a purpose-built `web read` command and 25 Xiaohongshu commands, including read-only search/feed/note/download paths, which is potentially more valuable for signed-in XHS than for ordinary public pages. It cannot be performance-tested against the logged-in page until its bridge is present; no OpenCLI skill or extension has been installed into the user's Chrome.
- Microsoft Playwright CLI completed the same live ArchDaily task. Its warm search-page eval took 0.87 seconds and reported 2,649 visible text characters, 122 links, 62 images and all 18 concrete project URLs. Navigating to the project took 7.80 seconds; the project summary returned 5,587 body-text characters, 159 links, 59 images and 23 figures. The summary eval itself unexpectedly took 6.37 seconds. It is a thin session/command wrapper over the same Playwright renderer, not a richer extractor; process round-trips and automatic snapshots add overhead that an in-process long-lived browser can avoid.
- The ChatGPT Chrome comparison channel could not attach even after the required delayed retry. Diagnostics show Chrome 150 is running, the ChatGPT Chrome Extension is installed/enabled in the selected Default profile, and the native-host manifest/registry/origin are all correct. This is a communication failure in the comparison-control channel, not evidence that the product's separate ArchResearch extension is disconnected. Per the Chrome control boundary, no PowerShell or filesystem fallback will read the user profile; XHS benchmarking continues through the product's own allowlisted broker, while opening another Chrome window would require a separate user confirmation.
- The first resumed XHS acceptance attempt is a connection failure, not a source-empty result: the standalone loopback broker waited 20 seconds and returned `extension_connection_timeout` with zero provider calls. No page action was sent and no XHS content was read. This means the current unpacked ArchResearch extension is not attached to the broker after the service interruption; its extraction quality cannot be scored from this run. The next comparison must separately measure anonymous public reachability and authenticated-bridge readiness.
- Anonymous XHS is not a viable substitute for the user's authorized Chrome session. Direct Playwright reached a Xiaohongshu error redirect in 5.44 seconds with `error_code=300012` and an explicit `IP 存在风险` message encoded in the URL; it yielded 0 note links and only the error-page image. Crawl4AI reached a login/search shell in 6.24 seconds (reported HTTP 301), extracted 1,039 text characters and 30 navigation/login links, but still 0 note links and one image record. Crawl4AI's `success=true` is therefore transport success, not research success. Both tools need an authenticated user-session bridge for XHS.
- Microsoft Playwright CLI reproduced direct Playwright's anonymous XHS result: browser launch/open took 2.43 seconds, eval took 0.80 seconds, and the final page was `安全限制` with `error_code=300012`, 41 text characters, 0 links and 0 note links. A Windows `.cmd` invocation also split the optional `&source=...` query parameter despite passing a PowerShell variable; this did not cause the risk redirect but confirms another wrapper-specific quoting edge that an in-process adapter avoids.
- Browser Use/Browser Harness attached to its existing isolated CDP Chrome profile and completed the anonymous XHS check in 4.84 seconds. It reached the same `安全限制 / error_code=300012` page with 41 text characters, 0 links and 0 note links. Its warm-session speed does not improve reachability or extraction. Browser Act remains untestable on XHS for the same earlier reason: it requires its API key before opening any target URL.
- The first OpenCLI XHS search invocation waited about 28 seconds and returned no process output through its Windows `.cmd` wrapper; this is logged as an inconclusive CLI-wrapper attempt, not a result. Because `opencli doctor` already reports its Browser Bridge missing, the next attempt will invoke the package's Node entrypoint directly to capture the actual bridge error instead of repeating the wrapper call.
- The direct Node-entrypoint OpenCLI retry also waited about 27 seconds and emitted only an Undici proxy-agent warning, with no XHS rows or structured failure. Together with `doctor: Browser Bridge not connected`, this is sufficient to classify the installed CLI as non-operational without its extension; a third identical retry is prohibited by the three-strike discipline. Actual logged-in OpenCLI quality remains unmeasured until a Browser Bridge is installed, while its public architecture-page value is already below an in-process Playwright adapter.
- The ArchResearch MV3 disconnect was reproducible as a service-worker wake problem after the loopback API stopped. A red entrypoint test required a one-minute `chrome.alarms` wake event; the packaged extension now declares the non-host `alarms` permission, creates `archresearch.reconnect`, and re-enters the idempotent restore path when woken. The focused test, TypeScript, ESLint and production build pass. Because the bundle changed, one final unpacked-extension reload is required; after that, ordinary API restarts should no longer require manual reloads.
- The first content-root fix made all 10 fixtures pass and correctly chooses the longest `article/main/[role=main]`, but a live production-adapter read of the ArchDaily project still returned only 286 text characters in 4.52 seconds. The live body contains thousands of characters while all selected semantic roots are fragments. The deterministic rule must therefore compare body text with semantic-root candidates instead of using body only when no roots exist; this live failure is the next red case.
- Comparing body text with all semantic roots fixed the structural selection bug: 11 fixtures pass and the same live page rose from 286 to 1,068 characters. It is still below the 6,692-character normalized benchmark because production waits only 500 ms before reading while the equal benchmark waited 3.5 seconds for the client-rendered article. Content readiness, not selection, is now the remaining deficit; page reads should use the already-proven bounded 3.5-second settle rather than returning an incomplete article faster.
- The 3.5-second bounded settle red test is green. The production adapter now rereads the same ArchDaily project in 7.64 seconds with 5,385 body-text characters, 176 links and 90 image references, versus the broken 286-character result. This is within the 20-second page contract and materially richer than Crawl4AI's 3,005 cleaned-text characters. Direct in-process Playwright is therefore the measured public-page selection; the remaining M41 work is integration/full gates, boundary documentation and authenticated XHS acceptance.
- The user requires the XHS candidates to be retested after login. The authenticated benchmark will use one isolated, explicitly user-signed-in Chrome profile and inspect only visible DOM; it will not export cookies or storage. Direct Playwright, Browser Use/Browser Harness and Crawl4AI may attach to the same CDP browser. ArchResearch uses its own allowlisted bridge. OpenCLI and Playwright CLI are scored only if they can reuse the same session without credential export; otherwise the missing bridge/session-reuse capability is itself the measured limitation. Browser Act remains key-blocked before navigation.
- Authenticated-session mechanics are confirmed: Crawl4AI 0.9.2 has `cdp_url` with cleanup disabled; Browser Use/Harness also attaches to the shared CDP endpoint; Playwright CLI lacks external CDP but supports a named persistent profile, so it can run sequentially after the shared browser closes. OpenCLI requires one extension instance per Chrome profile.
- The official OpenCLI v1.8.6 release provides Browser Bridge v1.0.22 (44,990 bytes; SHA-256 `9D2E3D053948BEAB5D97124AA79B1532D2122E33E461ECA56CAC113AFD33207A`). Its manifest requests `debugger`, `cookies`, `downloads`, `<all_urls>`, tabs, activeTab, alarms, storage and tabGroups. At the user's direction, this is recorded as an integration/permission cost rather than an automatic disqualification. The logged-in benchmark will use the official unmodified extension in the dedicated test profile and only invoke read commands; measured XHS output quality decides whether OpenCLI, ArchResearch's bridge or a hybrid wins.
- The shared authenticated benchmark Chrome profile is now open on CDP port 9335 with Chrome 150. The OpenCLI CLI has no `daemon start` command; `doctor` is the supported auto-start path and started daemon v1.8.6 on port 19825. However, `doctor` still reports Browser Bridge not connected after command-line loading the official unpacked extension. This is a setup result, not an extraction score; extension-target discovery or one manual load may still be required before the logged-in run.
- Authenticated direct Playwright succeeded on the shared profile: 5.37 seconds, no login wall or risk page, 20 note URLs / 52 image references at first settle and 37 / 93 after one 1,200px scroll. The initial direct-ancestor rule produced zero image-note pairs even though both sets are present, proving the XHS result cards use a sibling/common-card structure; association extraction must walk a bounded common ancestor before tool quality can be compared.
- The first authenticated Browser Use/Harness attempt did not reach navigation because its stdin runner raised `UnicodeEncodeError` on a surrogate while compiling the diagnostic program. This is a harness input-encoding failure, not an XHS result. The next attempt will reload the daemon for CDP 9335 and use an ASCII-only extraction program rather than repeating the same payload.
- Browser Use/Harness's second authenticated attempt used an ASCII-only payload and a freshly reloaded daemon bound to CDP 9335, but timed out inside its IPC layer during `Page.navigate` before extraction. Its public-page benchmark worked, so the failure is specific to authenticated XHS navigation/session stability. A final extraction-only attempt may reuse a page opened by another tool; the failing navigation path will not be repeated.
- Crawl4AI's authenticated CDP run exceeded its configured 20-second page timeout and remained alive without emitting a result for well over a minute. The two exact benchmark Python processes were terminated after command-line verification; the shared Chrome 150 session remained healthy. This is a real timeout/cleanup failure on the XHS logged-in path, not an empty-result score.
- A clean exclusive-profile rerun fixed the cross-tool CDP contamination. Direct Playwright reproduced in 5.35 seconds with 20→37 notes, 52→92 unique image references and 53→95 unique image-note pairs after one scroll. Browser Use/Harness then received its final clean exclusive run and succeeded in 4.84 seconds with 20→36 notes, 52→91 images and 53→93 pairs. The two tools expose effectively identical DOM; Browser Use's 0.51-second warm advantage comes from its daemon, while direct Playwright keeps the smaller dependency and simpler lifecycle.
- Crawl4AI's clean exclusive authenticated rerun completed in 7.29 seconds but returned only 849 text characters, 0 note URLs, 6 image references and 0 image-note pairs; it also emitted unclosed Windows transport warnings. Its `success=true` describes transport completion, not useful XHS extraction. It does not preserve/render the logged-in result set as effectively as direct Playwright or Browser Use.
- Chrome 150 ignored the command-line unpacked extension flags in the isolated system-Chrome profile: CDP target discovery showed only the Xiaohongshu service worker, and OpenCLI daemon restart/doctor still reported Bridge missing. This is a Chrome extension-loading constraint, not an OpenCLI extraction result. The next non-manual path is Chrome for Testing/Playwright Chromium, which supports unpacked extensions and can reopen the same isolated profile after system Chrome closes.
- Installed Chrome for Testing 149 supports unpacked extensions but immediately exited when asked to reuse the profile last written by system Chrome 150; CDP port 9335 never opened and OpenCLI remained disconnected. There were no remaining profile processes or singleton locks, so the blocker is version/profile compatibility rather than an unclean shutdown. OpenCLI now needs either one manual official-extension install in the already-authenticated system-Chrome test profile or a second login to a fresh Chrome-for-Testing profile; it has not yet been scored.
- Microsoft Playwright CLI's authenticated persistent-profile attempt launched system Chrome with the same isolated profile, but both the initial `open` process and a separate session `eval` remained blocked without returning any page summary. Two exact session Node processes and seven profile Chrome processes were terminated after verification. The CLI works on public pages but is not a stable logged-in XHS extractor in this environment; merely launching the browser is not counted as success.
- After the user installed the official Bridge into the authenticated isolated system-Chrome profile, OpenCLI connected in 0.2 seconds. `xiaohongshu search` returned 20 structured rows in 4.97 seconds with title, author, likes, publication date and tokenized note URL. A 3-row rerun took 4.48 seconds. `xiaohongshu note` returned body/interaction fields in 21.52 seconds but no image URLs. `xiaohongshu download` then completed a selected analysis-diagram note in 7.92 seconds and produced seven JPEGs totaling 549,284 bytes; the verified temporary directory was deleted immediately. This is the strongest site-specific XHS result measured.
- M41's measured architecture is now hybrid: direct in-process Playwright for public architecture sites, and OpenCLI's official Browser Bridge/site adapter for authenticated XHS discovery plus carousel assets. Browser Use matches raw DOM output but adds a daemon and broad dependencies; Crawl4AI loses the logged-in result set; Playwright CLI hangs; Browser Act remains key-gated. OpenCLI's broad extension permissions remain an explicit product tradeoff, not a reason to ignore its superior XHS output.
- M41 正式接入点已确认：现有 `XiaohongshuBrowserSearch` 只把扩展页面媒体转成 `ProviderSource`，后续仍依赖通用 browser inspection 才生成资产；OpenCLI 的优势恰好是独立于 ArchResearch 扩展返回结构化笔记并下载整组图片。因此正式适配器不能只替换搜索结果，还需直接产出“笔记元数据 + 本地多图”，再由现有资产分类/持久化边界接管。OpenCLI 主路径不可用时才回退现有扩展搜索，且 XHS 始终保持 `aggregator/visual_lead`，不升级为正式项目事实证据。
- OpenCLI 1.8.6 的可固定只读命令契约已由本地包清单和 help 核对：`xiaohongshu search <query> --limit N -f json` 输出 `rank/title/author/likes/published_at/url`；`note <url> -f json` 输出字段表；`download <url> --output <dir> -f json` 下载整组媒体。正式适配器只暴露这三种命令，但首版主闭环只需 `search + download`，避免每条笔记额外约 20 秒的正文读取。包 bin 为 `dist/src/main.js`，Node >=20；调用应使用 `subprocess.run([node, entry, ...], shell=False)`，不可经 `.cmd`/shell。
- 登录态实探再次验证正式契约：4 条结构化搜索约 5 秒，结果 URL 为 `/search_result/<id>?xsec_token=...`；选中“分析图分享｜老旧建筑改造的方案生成”后下载到 7 张 JPG、549,284 bytes，下载命令本身约 0.27 秒传输并输出 7 行 JSON 状态。CLI 会把进度条写到 stderr，因此适配器只解析 stdout，Trace 只保留数量/耗时/错误类型，不能保存完整命令、token URL 或 stderr。临时探针目录已在统计后删除。
- 多图分类的最低开销路径已确定：复用现有 `RemoteVisualClassifier.classify_remote_batch`，把最多 4 张本地图片转为受限低清 data URL，一次调用 `gpt-5.6-sol + medium`，随后只把达到相关性门槛的图片规范化为候选 PNG。Quick 的 12 张视觉额度正好可为 3 个子问题各评估 4 张；不应对 7 张轮播逐张发 7 次模型请求。XHS 文件仅在 run 临时目录停留，持久结果仍走现有 `AssetCandidate(storage_path, perceptual_hash)` 与 7 天 TTL。
- 中断前实现恢复后确认：正式 `@jackwener/opencli@1.8.6` 依赖、App 运行时发现、API/workflow 注入、OpenCLI→扩展异常回退、四图单批分类与签名 URL Trace 脱敏均已落盘并通过聚焦测试。剩余明确缺口是 `_try_xiaohongshu_search` 把“后端成功但返回 0 条”当最终成功，不会尝试下一后端；这会浪费已连接扩展的回退能力，需以空结果红测修复。
- 空结果回退缺口已修复。正式 runtime discovery 返回 `opencli-xiaohongshu`；live doctor 显示 daemon 1.8.6 正常但 Browser Bridge 未连接，系统检查同时确认 Chrome 当前未运行。因此 live search 的阻塞条件是浏览器进程关闭，不是依赖、适配器或配对失效。ChatGPT Chrome Extension 的 native-host 注册缺失是 Codex 浏览器控制通道的独立问题，不属于 ArchResearch/OpenCLI，不纳入本项目修复。
- OpenCLI 已成为根项目锁定依赖而非临时 benchmark：`@jackwener/opencli@1.8.6`，Node >=20，运行时从根 `node_modules/.../dist/src/main.js` 自动发现。官方一次性环境要求是安装 Chrome Web Store 的 OpenCLI Browser Bridge 并保持小红书登录；CLI daemon 按需启动。README/architecture 中“XHS 只走 ArchResearch 扩展”和“OpenCLI 尚未进入生产”的旧描述必须同步删除，架构图应明确 Public=Direct Playwright、XHS=OpenCLI、ArchResearch MV3=通用登录页/故障回退。
- 正式依赖自动发现已从根包验证为 `OpenCliXiaohongshuSearch`，但 API `ResearchSpec.research_sources` 的默认仍是空列表；Board 当前显式提交小红书，所以界面路径正常，但任何 API 调用者省略该字段都会违背用户已确认的“小红书默认一起搜”。默认值应在 API Schema 改为 `[xiaohongshu]`，调用者仍可显式传空列表关闭它。
- OpenCLI 1.8.6 的安装脚本已完整审计：`postinstall.js` 在非全局 pnpm 安装下直接跳过 shell completion；`fetch-adapters.js` 没有网络请求，非全局且未显式触发时也直接跳过。它只在特定模式下维护 `~/.opencli` 的本地适配器 override/manifest。可以在 pnpm `onlyBuiltDependencies` 中只批准 `@jackwener/opencli`，无需放开其他依赖脚本；先前 `ignoredBuiltDependencies` 会被本环境的供应链门禁视为未处置，不能用于可复现 setup。
- OpenCLI Bridge 离线时的性能风险：历史实测一次缺 Bridge 的命令可挂约 27 秒；如果三个子问题都重复尝试，会平白增加约 81–90 秒。运行内 failover 必须带熔断：首个 OpenCLI 搜索失败后立刻从该 run 的搜索器队列移除，当前题回退 ArchResearch 扩展，后续题直接用已成功后端；新 ResearchRun 才重新探测 OpenCLI。
- 会话迁移审计：当前真实仓库位于 `C:\Users\76384\Documents\Codex\2026-07-11\agent-pdf-gpt`，目标项目目录为 `C:\Users\76384\Documents\灵感agent`。目标除 `.git` 外为空，但源与目标各自都有 `.git`，不能直接平铺移动或覆盖；需先确认目标仓库是否为空壳，再选择保留源 Git 的安全迁移方式。
- 2026-07-16：M40/M41 真实集成验收完成。同一 Quick `d13bdc67-d6b3-4006-8d89-089439a02311` 的 attempt 9 使用 `opencli-xiaohongshu` 返回 4 条结构化笔记且 `fallback=false`，一个 `public_service_flows` 四图批次分类 2 张并新增 1 个 visual lead；Direct Playwright 的 `local_browser` 解析 4 个 ArchDaily 项目页。attempt 9 Trace 工具集合没有 Firecrawl，最终 `coverage_satisfied` 为 3/3、0 gaps、7 usable assets、3 projects，结果端点保留 10 个候选。XHS 仍只补视觉线索，正式案例证据来自建筑站点。
- 2026-07-16：真实 Quick 的研究覆盖与作品集页面验收必须分开。Board 显示 3/3、7 张可用，但 resume 保留的多个候选仍渲染“此历史结果的项目条件/空间机制为外文”“尚未生成中文视觉观察”“连接扩展并重新研究”。这违反 M36 的中文分析强制标准；根因方向是 retry 只补缺失子问题并保留旧候选，却没有为已保留历史候选补齐中文分析字段。Balanced/Deep 继续封锁，先用 fixture 红测复现并离线修复。
- 2026-07-16：Board 历史候选中文降级红测转绿：展示层现在用项目名、图纸类型和来源边界生成中文项目说明、保守机制边界与两步核对方法，不写回 API、不升级证据等级、不调用模型。真实 Quick 页面四类旧占位均为 0，3/3 与 7 张可用保持；首屏 DOM 的 13 个图片元素有 8 个已加载，余下 5 个均为远程图片（其中一个重复），需先滚动触发 lazy load 后再判断是否为真实失效链接。
- 2026-07-16：真实 Quick 的 5 个初始未加载远程图并非失效链接，而是一次大幅滚动越过了 lazy-load 触发区。按图像位置逐段滚动后，页面 13/13 `<img>` 均 `complete && naturalWidth > 0`，0 failed、0 pending。作品集验收应在截图前分段预热长页面，不能把首屏未加载数误判为远程资产失效。
- 2026-07-16：M41 还有一处 Board 契约遗漏：生产 API 默认发现 OpenCLI，ArchResearch 扩展只是 XHS 搜索回退，但研究表单仍在小红书选中且扩展未连接时硬拦截提交，提示“请先在 Chrome 登录小红书并连接 ArchResearch”。真实 Balanced 首次点击因此 0 run、0 Provider 调用。修复必须让 UI 从 API readiness 得知 OpenCLI 是否可用；不能简单删除全部检查，否则真正无 OpenCLI/扩展时会把失败推迟到运行中。
- 2026-07-16：Board XHS readiness 契约已改为后端能力 OR 扩展能力：`/v1/browser/status` 由 Pydantic `BrowserStatusRead` 暴露 `xiaohongshu_search_available`；Board 在独立后端可用时允许 XHS 研究并标注“OpenCLI 已配置 · 运行时验证登录态”，否则继续要求 ArchResearch 扩展。没有简单删除安全门，真正两种后端都不可用的阻塞测试仍保留。
- 2026-07-16：Balanced attempt 0 的 `provider_error:IntegrityError` 已确定为本地候选升级缺陷，而不是 Provider 故障。Designboom 的同一侧栏图可同时出现在多个文章来源；`_persist_expanded_project_page` 原先只按 `image_url` 偏好任一 visual lead，再把它的 `source_url` 改成当前项目页，若当前页已有同图候选便撞上 `(run_id, source_url, image_url)` 唯一约束。正确行为是先选当前来源的候选，只有当前来源不存在时才跨来源提升 visual lead；这样既保留跨页证据升级，也不会合并两个真实来源记录。
- 2026-07-16：Balanced attempt 1 证明修复穿过真实共享图路径并完成 4/4。最终 17 usable、4 projects、8 partial、29 visual leads、37 retained candidates，所有 38 个结果页图片完成加载。页面明确把 9 张小红书灵感与方案案例分区，XHS 只回答“图怎么出”，来源与权利状态分开显示；可作为 M20/M36 的真实标准深度证据。浏览器 full-page 截图会重复拼接固定区块，作品集应使用四张干净视口捕获而不是接受损坏长图。
- 2026-07-16：Deep 的唯一缺口不是视觉素材不足，而是 `daylight_strategy` 没有拿到可关闭正式案例证据的 partial。该题累计 7 passes，最终仍有 29 usable、6 projects、10 partial、5/6；round 5–7 的 completion recovery 搜索把完整中文主问题、中文子问题、分析要求和 workspace context 拼成长查询，所谓英文轮也保留大段中文，Direct Playwright 搜索连续返回 0。下一修复应沿用 destination-aware 思路生成短英文 typology + daylight mechanism + drawing-type 查询，不能增加轮次或把 XHS visual lead 升级为案例证据。
- 2026-07-16：公共检索调用链复核排除了“把 8,000 字模型查询直接交给浏览器”的假设：`_public_search_query` 已接收原始子问题并按目标域选择语言。真实缺口在 `_public_issue_focus`，它把所有采光/剖面问题统一压成 `old new structure daylight void section`，没有天窗、高侧窗、庭院等可检索构造词。行为红测已稳定失败于首条 ArchDaily 查询缺少 `skylight`，同时锁定国际站查询只含 ASCII、包含 `clerestory/courtyard/section` 且不超过 300 字符。
- 2026-07-16：最小采光映射修复通过完整离线门禁（226 API、74 Board、165 Extension、8 E2E、30/108 evaluation 与全部静态/构建检查）。它没有扩大检索次数或来源权限，因而下一次真实动作可以安全限定为原 Deep checkpoint 的一次 retry。
- 2026-07-16：原 Deep 的唯一 retry 已实质成功：attempt 1 终态覆盖 6/6、0 gaps、34 usable、7 projects、12 partial。新采光查询命中并读取 4 个 ArchDaily 项目页，其中 2 个页面产生正式 enriched 结果；XHS 仍只增加 visual leads。终态同时暴露一个独立的 resume 元数据缺陷：若补齐发生在早于 `normal_rounds` 的继承轮次，最终状态会是 `completed`，但 `stop_reason` 沿用默认 `budget_exhausted`。新的 resume 红测稳定复现，正确契约是 gaps 为空且 enrichment 未满时统一写 `completion_satisfied`。
- 2026-07-16：Deep 结果页最终验收证明采光修复同时穿过数据和展示链路：第 4 个子问题明确呈现侧窗、天窗、屋顶开口、内院/新采光体量的核验要求，并显示 7 轮、1 个方案项目、2 张证据图纸和 6 张灵感图。长页必须分段滚动预热；完成后 44/44 图片加载且无失败或挂起。作品集仍应使用结果总览、问题拆解、制图灵感、正式案例四张视口截图，避免浏览器 full-page 拼接固定区块的损坏模式。
- 2026-07-16：M42 初步范围审计发现两类发布记录漂移：M6 与 M35 的总表状态仍为 `implementation_complete`，但后续完整门禁和真实 XHS/Quick/Balanced/Deep 证据已经满足其剩余验收；`External acceptance gates` 仍把正常 Chrome/登录态 XHS 和三深度真实截图列为未完成。真实未完成外部门槛只剩 TinEye 用户 Key、30 个动态站点任务的人类标注、100+ 权利明确真实图和六人可用性研究。修正记录前仍需逐组确认所有代码与生成物归属。
- 2026-07-16：发布边界需要把“可追溯证据”和“临时工具输出”分开。四组源文件应纳入候选：单活运行租约及回归、持续加载页面 fixture、脱敏 XHS source-only 验收脚本、扩展产品边界文档。十张 Quick/Balanced/Deep PNG 应作为独立证据包保留。两份 pytest XML、Playwright CLI 控制日志/页面快照和 Impeccable live 配置是可再生成的本地工具输出，不属于产品或证据包；当前 `.gitignore` 尚未覆盖这些路径，后续发布整理应只提出边界，不在只读审计阶段删除。
- 2026-07-16：M42 凭据扫描的两个非零结果均不构成泄露：Browser WebSocket 测试中的固定 secret 在 `98a9a01` 基线已存在；XHS 测试新增的是用于证明 `/search_result/` 签名 URL 可解析且 Trace 脱敏的合成 token。生产文件命中为 0。历史 `docs/superpowers` 计划和通用 provider 单测仍出现 `gpt-5.5`，它们分别是历史设计记录和显式构造任意模型的兼容性测试；当前运行默认、配置脚本、README、故障文档与启动测试已统一为 `gpt-5.6-sol`。
- 2026-07-16：真实运行证据必须区分 Run 级累计 Trace 与 accepted attempt。Quick 共经历 9 次 attempt，数据库累计工具集合包含旧 Firecrawl；最终接受的 attempt 9 可由 2026-07-16 08:41:40、sequence 267 的新 planning 边界隔离，直到 sequence 284 完成都只有 OpenCLI XHS、Direct Playwright、本地分析/视觉和 workflow 工具。发布材料只可陈述“accepted attempt 9 无 Firecrawl”，不能改写历史。
- 2026-07-16：当前 SQLite Trace 不具备可用的真实成本凭证：`cost_usd` 和相关 duration 成本字段为 0。发布证据可以固化调用次数、终态、覆盖和可见输出，但不能据此宣称零成本或填写推测金额；实际账单只能由外部 Provider 账单另行核对。
- 2026-07-16：M42 冷启动审计证明 `$ErrorActionPreference = "Stop"` 本身不保证 PowerShell 7 对 native 命令非零退出 fail-fast。安装脚本必须与验证脚本一样启用 `$PSNativeCommandUseErrorActionPreference = $true`，否则 pip/pnpm/build 失败后仍可能打印成功。该约束已加入脚本测试。
- 2026-07-16：动态端口必须同时覆盖 HTTP 与扩展 WebSocket。Vite 的 `/v1` 代理已读取实际 API origin，但 Board 过去把 pairing endpoint 固定为 8000；因此端口冲突时普通页面可用、扩展却连错端口。启动器现在通过 Vite 环境注入实际 WebSocket endpoint，默认开发/测试仍使用 8000。
- 2026-07-16：运行前检查的核心用户问题不是分别理解 Chrome、OpenCLI、权限和登录态，而是“现在能否开始研究”。原界面把同一事实重复成标题摘要、Chrome 行、小红书行和 footer，且两个完整边框按钮与状态同权，造成约 238px 的独立仪表盘感。合并为“环境结论 + 一句能力边界 + 按需动作”后，桌面高度降到 50px，功能和失败信息仍可达；这是信息架构删重，不是单纯缩字号。
- 2026-07-16：证据检视器的“内容少”感不是数据缺失，而是媒介与布局错配：500px 文字抽屉遮住了被分析图纸，用户无法把“图中直接可见”和“设计方法推断”与图像对照；同时六类信息在同一阅读轴上排成长文。最小修复是让现有 `previewUrl` 成为检视器的主证据区，并在桌面把分析与核验放到并列阅读区，保留移动端单列。
- 2026-07-16：M43/M44 的两次 Board 视觉增量没有改变 API、扩展协议或打包浏览器行为。发布前完整门禁仍保持 226/75/165/8 与 30/108 evaluation 的既有基线，说明运行前检查减重和证据工作区扩展均局限在预期的 Board 表现层。
- 2026-07-16：现有“深度”主要扩大子问题、检索轮次和候选数量，未强制每个正式项目形成文章级分析。虽然 Direct Playwright 会读取 `article/main/body` 文本，但 `LocalBrowserPageParser.parse()` 把页面链接 Markdown 放在正文前，`_public_page_analysis_text()` 又只取前 6000 字，链接密集的 ArchDaily 页面可能在正文进入模型前就被截断。
- 2026-07-16：用户校正了“深度”的定义：读取项目正文是所有正式案例的共同准入条件，不是 Deep 多读文章；Quick、Balanced、Deep 的差异必须是研究强度。Quick 提炼最强因果链与直接建议；Balanced 强制条件—操作—空间结果、案例比较和适用边界；Deep 进一步强制多项跨案例比较、证据冲突与不确定性、多个失效边界及综合建议。
- 2026-07-16：此前 `DEPTH_TARGETS.analysis_requirements` 只影响检索词，没有进入项目页分析。现在同一强度契约同时进入正文分析和运行级综合；结构化结果若低于所选档位会失败并留下 `research_synthesis_incomplete`，不能靠更长但同质的摘要冒充深入研究。
- 2026-07-16：项目页直接列出图片只证明图片—页面归属。覆盖门现在要求正文逐字引文支持项目条件与设计机制，并存在由该机制导出的转译步骤；不满足时加入 `article_analysis_incomplete`。Board 也只把完成该分析的资产编入项目档案，其余公开网页图片明确保留为待核验线索。
- 2026-07-16：运行级 `ResearchSynthesis` 的每条回答、因果链、比较、冲突、边界和建议都绑定输入资产 ID；综合输入只含项目条件/机制对应的正文引文，不会把图片 alt 或“项目页列出此图”的归属声明当成机制证据。界面先显示核心答案与落地建议，比较/冲突/边界按需展开，并使用平面结构线而非新增重卡片。
- 2026-07-16：当前 `PublicPageAnalysis` 只要求 facts 的 `text_excerpt` 逐字存在，并仅让 `project_context` 等于一条受支持 fact；`design_mechanism` 与 `transfer_strategy` 可在无逐字正文证据时直接持久化。Board 随后对空字段生成“现有记录只确认图纸类型与来源关系”等泛化兜底，因此界面看似有五段分析，实质只有图片归属。修复必须从正文排序、证据模型和项目资格三处入手，而非继续润色兜底文案。
- 2026-07-16：M47 的新版真实 Quick 没有再把图片归属冒充项目研究：虽然保留 10 条结果、7 条可用线索，所有候选的 `project_context`、`design_mechanism`、`transfer_strategy` 都为空，唯一逐字引文只是 “Image n of n” 或页面标题，因此正式项目计数为 0，综合也没有生成。问题已从“假装有深度”收敛为“正文读取成功但分析升级失败”：13 个 `local_browser` 页面读取成功、4 次 `public_page_analysis` 均 completed，其中 3 个相关页面的 `enriched` 仍为 0；15 次扩展 `browser` 跳过并使最终 stop reason 覆盖为 `browser_inspection_incomplete`，但不是正文内容缺失的唯一原因。
- 2026-07-16：M47 attempt 0 的 15 条实际 QueryAttempt 仍包含长的模型检索提示，但真实公共网页检索使用 `_public_search_query` 的短查询；该短查询此前只看主问题和当前子问题，不看工作区 brief/constraints。因主问题只写“旧建筑”，流线分支会丢失“工业建筑”类型，找到普通社区中心；工作区语境接入 typology 提取后，每个分支都能保留 `adaptive reuse industrial building`，而完整 brief 不会进入搜索词。另一个独立缺陷是扩展裁图的 `BrowserCommandError` 会覆盖更准确的 `article_analysis_incomplete/research_synthesis_incomplete`；Direct Playwright 成功时不应把可选精确裁图当成正文研究硬门。
- 2026-07-16：attempt 1 证明正文门本身工作正确：Fitzgibbon 页的逐字引文只支持面积、年份和委托背景，模型明确指出正文未说明公众/后勤流线、核心筒、坡道、楼梯或后勤入口，因此 `design_mechanism` 与 `transfer_strategy` 保持为空。无法完成的结构性原因是 live planner prompt 允许 precedent 子问题隔离 `source-verification` issue，实际生成了 `source_trace`；但 coverage 又要求每个子问题都具备正文支持的设计机制和转译，元问题天然无法满足。来源可追溯性应是所有正式案例的准入/核验维度，不应占用 Quick 的设计研究分支。
- 2026-07-16：规划修复已在新真实 Quick 中生效。模型没有仅换掉 `source_trace` 名称，而是实际保留了用户问题中的三项设计决策：功能如何植入既有结构、公众与后勤如何通过入口/核心筒/装卸口组织、原层高/夹层/挑空/屋顶/地下如何形成剖面层次；三个 rationale 仍把来源核验作为证据要求，而非独立研究成果。
- 2026-07-16：正确规划没有自动等于有效研究。新 Quick 前 8 个查询后虽积累 10 条 usable 线索，并有一个正文分析发生字段更新，但 article-ready 项目仍为 0；强度门因此继续保留 `article_analysis_incomplete`。当前行为符合“宁可阻断也不把图片归属或一般背景冒充机制”的产品边界，最终是否可用仍取决于恢复轮次能否找到真正陈述设计机制的项目正文。
- 2026-07-16：新 Quick 的失败暴露出正文链仍是假完整：Direct Playwright 对 SOM Landmark 返回了正文，但 `_public_page_analysis_text()` 先截到 6000 字符，`OpenAIResearchProvider.analyze_public_page()` 又对 `page_text` 再截 6000；模型恰好在 `In other areas, Inaba Williams peeled...` 处看到截断并因此拒绝机制。正文准入不能只证明“调用过 parser”，还必须让与问题相关的后段机制进入模型输入。另有一个公共页 Direct Playwright 与扩展均失败、两次小红书资产处理出现 `APIConnectionError`，导致最终 stop reason 被 `browser_inspection_incomplete` 覆盖；但它不是 0 formal projects 的首要原因。
- 2026-07-16：正文输入不应无界扩张，但 6000 字符小于当前 parser 的 12000 字符合同，导致“页面读取成功”与“正文被完整分析”不一致。统一 12000 上限后，正文尾段进入模型输入，同时持久化仍要求 `text_excerpt` 在同一有界正文中逐字出现，因此不会放宽事实证据门。
- 2026-07-16：检索路由当前按“流线 → 采光 → 剖面 → 功能”检查任意关键词。`program-insertion` 子问题为要求图纸证据而自然包含“平面图与剖面图”，因此先命中泛化剖面分支，实际 ArchDaily 查询变成 `adaptive reuse industrial building old new structure daylight void section...`，完全没有“插入/嵌套/盒中盒/新功能”等主意图。分支主意图必须优先于证据媒介词，否则 planner 虽正确，检索仍会找偏。
- 2026-07-17：M47 恢复后 API/Board 已通过仓库脚本在 8000/5173 重启，最新功能植入查询路由已载入。唯一一次 OpenCLI doctor 显示 daemon 1.8.6 正常但 Browser Bridge 未连接；Chrome 诊断确认浏览器当前未运行，而 Chrome 本体、ChatGPT Chrome Extension 和 native host 均已安装且启用。该诊断不证明 OpenCLI 扩展状态，只证明本次不能在 Chrome 关闭时创建依赖小红书的真实 Quick。
- 2026-07-17：用户许可打开 Chrome 后，OpenCLI doctor 恢复为 daemon 1.8.6、Extension v1.0.22、Connectivity 全 OK。全新 Quick `fc3af6d4-2610-4319-9e0c-25e895d7f844` 的真实 Trace 证明 `research_sources=[xiaohongshu]` 不是“只搜小红书”：它表示可选视觉来源开关，正式公共来源仍内置执行；本次有 13 次 `local_browser_search`、13 个 Direct Playwright 公共页、4 次 `public_page_analysis`、3 次小红书检索和 0 Firecrawl。
- 2026-07-17：新 Quick attempt 0 终止为 `blocked/research_synthesis_incomplete`：10 usable、0 formal projects、0/3，gaps 为 `uncovered_subquestions/article_analysis_incomplete/research_synthesis_incomplete`。三个分支语义正确且没有来源元分支；首轮功能植入已找到 `Reuse of an Industrial Space` 等相关 ArchDaily 页，但全部 4 次正文分析都未写入可绑定的项目条件、机制和转译。失败已从“功能植入短查询误路由”收敛到“相关公共正文进入分析后仍没有形成 article-ready 案例”，不得 retry，先以真实文章机制构造行为红测。
- 2026-07-17：Direct Playwright 复读 `Reuse of an Industrial Space / RuizEsquíroz` 证明正文完整包含可用机制：既有桁架模数组织四个区域、恢复旧办公室夹层与屋脊天窗、在最后一跨打开庭院并插入屋顶维护楼梯、第三榀桁架下设置服务带。真正阻断点是 ArchDaily 将图纸 `alt` 写成通用 `Image 18/21`，类型只存在于 CDN 文件名 `intrusswetrust-axo.jpg` 与 `intrusswetrust-seccion-1.jpg`；旧英文词表令 drawings 为空，`_persist_public_page_analysis` 因无 selected URL 必然返回 0。
- 2026-07-17：最小修复只扩充确定性图纸类型词表：`seccion/sección` 为 section，`axonometria/axonometría/-axo./_axo.` 为 axonometric；不猜测含糊的 `p0/p1`。真实页面零 Provider 复核现识别出 1 张轴测与 1 张剖面；工作流集成测试使用同一西语文件名并证明逐字正文事实、机制、转译和 Quick 综合可以绑定。Ruff、format、strict Mypy 与全量 238 API tests 通过，证据门未放宽。
- 2026-07-17：获准的新 Quick `bf32f7f3-0a5b-4642-8481-94bbc5c2acc2` 证明多语言图纸修复穿过真实链路：`Reuse of an Industrial Space` 识别并批量分类了 `intrusswetrust-axo.jpg` / `intrusswetrust-seccion-1.jpg`，但该页唯一正文分析遇到 `APITimeoutError`，所以仍只保留图纸归属。另一页 `L264 Office` 成为首个 article-ready 正式案例：两段逐字引文支持工业作坊语境和“功能分区顺应规则柱网”的机制，生成 3 条有边界的转译建议；运行级 Quick 综合也成功绑定该资产。这证明正文门和综合链有效且没有靠图片数量放宽，但终态仍只有 1 个正式项目、覆盖 `program_insertion` 1/3。
- 2026-07-17：同一 Run 的确定性新缺陷在生产查询函数中复现：`sectional_hierarchy` 问题含“屋顶加建”，而 `_public_issue_focus()` 在剖面层次判断前检查 `加建`，因此 Designboom/Dezeen/Divisare 的剖面补齐查询错误使用 `program insertion / box-in-box / inserted volume`，没有使用层高、挑空、夹层、下沉、屋顶与垂直交通词。最终 15 usable、7 verified/partial 仍只有 1/3，诚实终止 `blocked/no_new_assets`；综合明确声明流线和剖面证据不足。Balanced/Deep 必须继续封锁，且该 Run 不 retry；下一步应先用行为红测锁定显式剖面层次意图优先于问题中的“加建”媒介词，再做最小路由修复。
- 2026-07-17：检索主意图路由已改为全局加权规则，不再依赖“首个命中词”。流线、采光、功能植入、剖面层次各自使用明确短语与支持线索计分：`功能植入/program insertion`、`采光策略/daylight strategy`、`剖面层次/sectional hierarchy` 等主意图权重大于单个 `插入/加建/section`，多个空间线索可共同决定主意图；同分才沿用稳定的保守顺序。剖面查询本身也改为层高、挑空、夹层、下沉、屋顶加建和垂直交通，而不再混用泛化采光词。
- 2026-07-17：全局稳定性测试覆盖六类中英重叠表达：功能植入同时要求剖面图、剖面层次包含夹层/下沉/屋顶加建、采光策略包含“插入天窗”、流线包含加建与核心筒、英文 roof extension/mezzanine/sunken floor，以及只有普通 `section + vertical relationship` 的低权重回退。首轮红测准确复现剖面被 program insertion 抢占；实现后新矩阵、既有功能植入优先、工作区类型语境和四轮流线/采光回归均绿。最终 Ruff、format、strict Mypy 与 239 API tests 全绿，未改变来源、轮次、证据门或 Provider 权限。
- 2026-07-17：全局路由修复后的新 Quick `9a72a8b2-b6d4-41c4-9a35-80bcad5bcba9` 证明三条真实 ArchDaily 短查询均正确：功能植入使用 `program insertion/box-in-box`，流线使用 `visitor/staff/back-of-house/loading`，剖面使用 `sectional hierarchy/double-height/mezzanine/sunken/roof/vertical circulation`。`Reuse of an Industrial Space` 本次正文分析 `relevance=3` 并形成唯一 article-ready 案例，5 段逐字正文引文支持既有桁架模数、恢复夹层/屋脊天窗、“最少介入”和第三榀桁架下服务带，生成 5 条有边界转译；说明图纸识别、正文门和全局查询均已生效。
- 2026-07-17：新 Quick 仍终止为 `blocked/research_synthesis_incomplete`：15 usable、1 formal project、7 verified/partial、1/3，仅覆盖 `program_insertion`。确定性缺陷是直接项目页缓存只在 `parsed_now` 分支执行 `_persist_expanded_project_page`、远程图像分类和 `_try_public_page_analysis`；当后续剖面查询返回同一批已解析的 ArchDaily 直接项目页时，`parsed_now=false` 且页面无合集子链接，现有 cached reassociation 分支什么也不做。因此正式候选只含 `subquestion_ids/subquestion_analysis=[program_insertion]`，即使同一正文含夹层、天窗和服务带，也不会在剖面问题下重新接受证据门分析。正确全局修复应按 `(source_url, subquestion_id)` 对缓存直接项目页执行一次分支级正文分析，复用解析/图纸但不复用结论；只有新分析再次产出逐字支持的条件、机制和转译才绑定该分支，不能直接追加 ID。
- 2026-07-17：流线分支还有独立的真实召回不足：首轮返回 Fitzgibbon、Pierrefonds、Las Americas 等普通社区中心，正文没有旧工业改造的访客/工作人员/货运/维护分流机制；后续 Designboom 流线页分析出现一次 `APIConnectionError`。另有一次小红书资产 `APIConnectionError`、一次第四轮剖面 `local_browser_search` Error 和最终综合 `APITimeoutError`；它们均保留在 Trace。全 Run 128 events、12 次公共检索、11 个公共页、4 次正文分析、3 次 OpenCLI 小红书检索、Firecrawl 0。该 Run 不 retry，Balanced/Deep 继续封锁。
- 2026-07-17：缓存直接项目页的分支分析已完成全局红绿修复。执行内维护 `(source_url, subquestion_id)` 集合：正文与图纸只解析一次，同一页面命中新子问题时重新调用正文分析；同一分支在后续恢复轮重复出现时不再次调用。只有该分支重新产出逐字支持的 `project_context + design_mechanism + transfer_strategy` 才追加 coverage 关联；无机制响应只保存诚实 limitation。coverage 现在直接检查 `subquestion_analysis` 的三项正式字段，不再让视觉相关 `subquestion_ids` 单独覆盖一个后来变成 article-ready 的资产。
- 2026-07-17：研究综合输入也已对齐分支证据。`ResearchSynthesisCase` 新增结构化 `subquestion_analysis`，每个已通过分支携带自己的条件、机制、转译、limitations 和只匹配该分支条件/机制陈述的逐字证据；资产仍保持单一 asset ID，不拆成伪造的多个案例。综合强度与 allowed asset ID 校验未放宽，旧通用字段保留兼容。
- 2026-07-17：稳定性测试包含三种分支门场景（全支持、首分支拒绝、末分支拒绝）和同源恢复轮去重；旧实现三种场景均只分析一次，新实现分别分析三个不同问题，拒绝分支不进入 covered IDs，重复恢复搜索不增加分析调用。相邻直接页提升、无逐字证据拒绝、合集子页缓存复用、Provider 综合强度测试全部通过；最终 Ruff lint/format、strict Mypy 和 243 API tests 全绿，仅既有 TestClient/httpx2 warning。没有 Provider、真实来源、小红书、Firecrawl、Run 或截图。
- 2026-07-17：获准的唯一新 Quick `065d4a8a-aa83-42f2-b983-983037a1b5b1` 在 attempt 0 终止为 `blocked/research_synthesis_incomplete`：16 usable、3 formal projects、9 verified/partial、2/3，覆盖 `program-insertion` 与 `sectional-hierarchy`，缺失 `flow-separation`。`Sephardic Community Center` 与 `Fitzgibbon Community Center` 都在不重新解析页面的情况下针对新剖面分支再次执行正文分析，Fitzgibbon 以分支自有逐字引文形成 `relevance=4/enriched=1`，证明缓存分支修复穿过真实链路。
- 2026-07-17：新的确定性阻断在下游 `public_pages._compact_site_query()`：它未复用 workflow 已经全局加权的主意图，仍以“首个命中词”压缩站内搜索。用该 Run 的固化问题、子问题和工作区语境离线重放五轮生产函数后，旧实现将所有流线与剖面查询压为 `community center`；剖面中的 `vertical circulation` 被误当为流线主意图，第 4 轮统一追加的 `separate entrance / circulation core` 还会将功能植入抢占成 `community center`。这是全局查询稳定性缺口，不能对单题打补丁。Run 的 106 条 Trace 中 Firecrawl 为 0；最终综合另外出现一次 `APITimeoutError`，故未生成成功的资产绑定综合。
- 2026-07-17：站内查询压缩的全局修复已完成离线红绿。`infer_research_issue_intent()` 成为 workflow 与 public-pages 共用的加权纯函数，两层不再分别维护主意图规则；站内层另外独立保留工业改造、社区文化中心或适应性改造类型。十组中英重叠意图覆盖功能植入、流线、采光、剖面、统一恢复词噪声和“工业旧壳 → 社区文化中心”双类型；五轮生产函数重放稳定输出 `industrial reuse/工业改造 + program insertion/公众后勤流线/sectional hierarchy`。Ruff lint/format、API 项目 strict Mypy、全量 253 API tests 与 `git diff --check` 全绿；仅既有 TestClient/httpx2 warning，没有真实调用。
- 2026-07-17：全局站内查询修复后的真实 Quick 预检尚未进入研究。API/Board 重启后分别为 `ok` / HTTP 200，但本轮唯一一次 OpenCLI doctor 显示 daemon 1.8.6 正常、Browser Bridge 未连接；只读进程核对为 Chrome 0，同一工作区仍为 14 条历史 Run、active 0。因此没有创建 Quick，也没有 Provider、来源、小红书或 Firecrawl 调用；必须先由用户打开已配置且保持小红书登录态的 Chrome。
- 2026-07-17：全局站内查询修复后的唯一 Quick `b7c91d0d-42c9-429a-ae4f-11368c9caa0d` 在 attempt 0 终止 `blocked/research_synthesis_incomplete`：20 usable、1 formal project、12 verified/partial、1/3，仅 `flow_separation` 覆盖。按持久子问题与生产函数重建的 12 条实际站内短查询全部正确：国际站保持 `industrial reuse + program insertion / visitor staff back-of-house circulation / sectional hierarchy`，ArchDaily 中文站保持“工业改造 + 功能植入 / 公众后勤流线 / 剖面层次”；第 4 轮噪声也未改变主意图。因此该全局修复的真实验收通过，3/3 失败不再是路由缺陷。
- 2026-07-17：本次失败收敛到公共来源召回与图纸准入。功能植入首轮命中 Public Library in Labin、Blue School、COOP incubator 与 Walker Hall，但没有任何正文—图纸绑定通过；剖面分支的公共页远程分类提交 4 张但新增 0，最终只保留 4 张小红书 `visual_lead`。唯一正式项目 De Nederlandsche Bank 在流线分支以入口迁移、接待/登记点、三级安全分区、员工区与中庭楼梯形成 6 段逐字引文。最终 `research_synthesis` 另外以 `APIConnectionError` 失败；135 条 Trace 的工具集合中没有 Firecrawl。
- 2026-07-17：公共页远程分类的全局消费缺陷已在代码中确认。`_remote_visual_batch_started()` 只要看到当前 attempt、当前子问题的任意 `started` 事件就拒绝后续所有不同指纹；主循环又会在排序后的首个可信具体项目页上立即启动该事件。因此一个仅含照片、分类 `added=0` 的弱页会永久耗尽子问题唯一批次，较晚正文和图纸更强的项目页不能获得分类，也就无法进入正文—图纸准入。现有合集页保护只避免专题页消费，未覆盖多个具体项目页之间的质量竞争；修复必须改变全局候选提交/消费规则，同时保留每 attempt/子问题总预算、指纹去重、证据门和视觉平台限制。
- 2026-07-17：远程公共图像分类现改为查询内跨项目页的一次有界批次，而不是首个页面独占。所有符合原准入条件的具体页先完成解析；候选按正文与问题相关性、已识别图纸、来源层级和正文完整度稳定排序，再按页面轮转抽样，总数仍不超过 4。多页未标注画廊优先抽末端候选，因为建筑站点图纸通常位于摄影之后；单页仍保留原来的全页均匀抽样。分类结果按原 source/page 分组持久化，只有实际获得图纸的可信页才进入逐字正文分析。
- 2026-07-17：新行为测试以“首个可信页四张照片、后一个可信页一张未标注图纸与两段逐字正文”复现旧缺陷；旧实现唯一调用只含弱页四图，新实现仍只调用一次且包含强页图纸，随后三个分支分析均绑定该图纸。已有合集让位、未知页不消费、单页 1/4/7/10 抽样、每子问题公平、截止时间、指纹和跨 attempt 测试保持通过。最终完整 Ruff lint/format、strict Mypy、254 API tests 与 `git diff --check` 全绿。
- 2026-07-17：新 Quick `fe0aea3a-...` 终态证明跨页分配已真实生效，但下一确定性缺口转到恢复域结果准入。Designboom 的四条结果中有一条标题明确为“convert textile warehouse”，却同时混入 Frank Lloyd Wright 住宅保护、地域工艺访谈和阿根廷屋顶住宅；实际有限恢复页容量先解析了无关住宅及其错误扩出的 `/architecture/` 根页，没有到达仓库项目。下一轮 Dezeen 四条甚至全部是手机、微波炉、家具和太空镜新闻。现有排序把来源相关性与中文主问题/子问题比较，英文恢复域标题因此大多同分，站点返回顺序会让无关新文章占用页面预算；正确英文短查询尚未进入页面准入相关性上下文。
- 2026-07-17：代码复核校正了上述语言判断：workflow 的 `public_relevance_context` 已强制生成英文，因此缺陷不是简单漏传英文查询，而是字面 token 相似度无法把 `convert textile warehouse` 与 `adaptive reuse industrial building` 视作同一类型语义。另一方面，非 ArchDaily 展开规则把路径段 `architecture` 当作项目标记，所以 Designboom 文章里的 `/architecture/` 频道根页会被当成子项目，占用第二个恢复页槽位并产生虚假的“项目页直接列图” partial。下一稳定修复应同时用行为测试约束“工业改造同义类型结果优先”和“站点频道根页绝不作为具体项目展开”，二者共同保护有限公共页预算。
- 2026-07-17：公共来源准入已完成全局红绿修复。共享 `public_search_relevance_score()` 同时用于 `LocalBrowserPageParser.search()` 和 workflow 来源排序；它在保留稳定字面重叠的基础上识别工业/厂房/仓库/纺织厂/电厂与 reuse/convert/renovation 等类型同义词，并按功能、流线、采光、剖面四类意图给相关标题/摘要加权。结果只重排、不删除零分来源，避免损失无显式关键词但可能有价值的项目。非 ArchDaily 项目路径标记现在必须后接具体 slug，单段 `/architecture/`、`/projects/`、`/work/` 等频道根页不能再扩展。
- 2026-07-17：稳定性测试覆盖四类英文恢复意图，均要求工业改造同义项目压过无关住宅新闻；workflow 级页槽测试把相关描述收窄为 `convert a textile warehouse`，证明共享评分穿过两层。频道根页负例与既有同站项目、数字项目、ArchDaily 严格项目链接测试共同约束展开边界。证据门、域轮转、页面数量和 Provider 调用预算均未改变。
- 2026-07-17：新 Quick `080d60e9-...` 证明恢复排序修复真实生效：Designboom 首先解析 former steel factory museum 与 former railway warehouse，且不再展开 `/architecture/` 根页。Run 最终仍为 2/3，直接证明“查询内跨页”还不足够：program 的唯一远程批次在首轮四个 ArchDaily 页上完成，第三轮两个更强 Designboom 项目虽被正确解析，却因当前 attempt/子问题已有 `started` 而不能进入分类与文章门。这个缺口必须跨正常轮和全部计划恢复轮统一选优，不能靠允许第二次调用解决。
- 2026-07-17：远程候选现按子问题跨轮累计，并仅在该子问题的最后一个计划查询处理一次；非文章研究目标仍维持原即时行为。最终批次使用共享类型/意图相关性、正文重叠、已识别图纸和来源层级对所有轮次页面排序，再按页轮转取最多 4 图。这样保持每 attempt/子问题一次调用和原指纹预算，同时让后到的钢厂、仓库项目能与首轮候选公平竞争。
- 2026-07-17：新 Quick `98a0b853-...` 真实验证跨轮时机：流线直到第 5 轮才写入唯一 `remote_visual_batch started`，候选为 4 图/4 页；功能与剖面已在首轮正文覆盖，因此它们没有浪费后续批次。Run 仍因该唯一流线调用的 `InternalServerError` 停在 2/3，但资产绑定综合成功。这说明下一步不能再调整调用时机或增加调用次数，而应检查被选 4 图的 URL/alt 是否存在尚未纳入确定性类型词表的图纸信号，使正文分析即使视觉上游失败也能继续。
- 2026-07-17：两个恢复 Designboom 页的零 Provider 复读揭示更底层的全局图像污染。Sara Hildén 页 40 图中确定性类型为 0，四个页内候选包含项目社交封面、项目图、2026 年无关住宅新闻缩略图和另一博物馆新闻缩略图；铁路仓库页 39 图中唯一“site_plan”其实是 2026 年卡塔尔总体规划的相关文章缩略图，候选还包含社交封面、项目图和 consentmanager 的隐私 SVG。当前 Playwright `_read_page()` 对正文虽选择语义根/正文，但图片无条件从整个 document 收集，导致相关文章、导航和同意控件进入图纸识别与唯一远程批次，甚至制造错误类型 partial。
- 2026-07-17：下一全局修复应把页面图片与正文使用同一语义根边界：优先从正文最长的 `article/main/[role=main]` 根收集媒体；无语义根或根内无图时才回退全页，同时明确排除 consent 控件和固定尺寸相关文章缩略图。必须以 DOM 级行为测试锁住“正文项目图保留、相关文章/隐私图排除、无语义页回退”三种情况，不能按 Designboom 单域硬编码项目名。
- 2026-07-17：实页 DOM 结构给出稳定区分：铁路仓库页的外层 `main` 为 9159 字/39 图；当前项目正文 `article` 为 6294 字/12 图；相关文章卡片的 `article` 仅 61–420 字/1–2 图。媒体准入因此优先“文本至少 1000 字的最长 article”，没有正文级 article 时才退回最长 main/role=main；页面完全没有语义媒体时仍回退 document。该规则依赖内容规模与语义元素，不依赖 Designboom 域名。
- 2026-07-17：增强假 DOM 红测构造了“main 比正文 article 更长且混入相关文章/隐私图”的场景，旧最长根策略按预期失败，新 article 优先策略只保留项目剖面；body-only 媒体回退继续通过。铁路仓库实页复验从 39 张降到 12 张正文项目图，错误 masterplan、相关文章缩略图和 consent SVG 全部消失，确定性假 `site_plan` 从 1 降为 0；四个候选均来自同一项目正文。
- 2026-07-17：新 Quick `c41c0cb8-...` 给出必要反例：严格 article 根使 ArchDaily 项目图片从原 28–40 张降到 4–9 张，首轮正式项目为 0；最终三个清洁批次全部成功返回，说明上游可靠性改善，但只 added 0/2/2 且所有正文门仍失败。ArchDaily 的项目正文与图库并非总在同一 article 元素，不能把“正文语义根”直接等同于“完整项目媒体根”。
- 2026-07-17：稳定的媒体集合应为安全并集：始终保留正文 article 根图片；再从 document 全图中补回与当前页面标题有强身份重叠的图片（多个非通用词，或一个足够长的项目特征词），并按 URL 去重。ArchDaily 的 `Project Name - Image n of n`、项目名文件路径和事务所特征词可以恢复；Designboom 相关文章标题、当前新闻缩略图和 consent 控件因与页面标题无项目身份重叠继续排除。该规则是跨站点的项目身份门，不是域名特判。
- 2026-07-17：安全并集的两个真实页窄输出复验通过：Designboom 深圳铁路仓库页保留 16 张项目媒体，Louvre 相关新闻、consent 控件和 250×200 缩略图均为 0；ArchDaily Australian Red Cross 页保留 15 张，确定性恢复 elevation/section，且同事务所 Parramatta 项目为 0。强匹配边界应保持“至少两个项目身份词，或标题连续 4–6 词生成的项目缩写”；单一事务所名、通用改造动词不足以准入。
- 2026-07-17：Quick `023d083e-...` 终态审计确认 133 条 Trace、Firecrawl 0。安全并集已让 Australian Red Cross 恢复 section/elevation，并让两个最终跨轮批次都以 4 图/4 页完成；但功能批次 added 3 主要是照片，流线 added 2 后唯一新候选是 Carnegie Library 页内与当前项目无关的 Penn State Behrend 新闻照片。这表明“页集合准入”已改善，但“每页一张的批次抽样排序”仍会优先照片/相关卡片，没有利用项目身份与确定性图纸信号。下一修复应在既有单批、页轮转和指纹预算内重排页内候选，而不增加调用次数或放宽正文门。
- 2026-07-17：代码核对定位了直接原因：`_remote_visual_candidate_images()` 已在单页内优先确定性图纸并等距抽样未类型图，但多页轮转随后会无条件把每页的“最后一张未类型样本”移到最前。该旧启发式在富图库页上精确优先了尾部相关新闻。稳定修复应用当前项目标题对 URL/alt 的身份重叠为页内排序一级信号，然后保留原图纸类型优先与页间公平轮转；不应继续用首/尾位置猜测图纸。
- 2026-07-17：项目身份抽样修复的真实页复验符合全局边界：Carnegie Library 候选首选从 Penn State Behrend 新闻图（身份分 3）切回当前项目 `stringio.jpg`（分 8）；Designboom 钢厂四个样本都为当前项目分 7，因此稳定保留原顺序，首选 `...-13.jpg` 而不再强制尾部 `...-1800.jpg` 社交封面。这同时避免了“身份同分时再造新偏置”。
- 2026-07-17：Quick `d7ac516c-...` 的批次资产归属与零 Provider 实页分布表明，再调整首/尾位置仍不够稳定。Office Building Transformation 安全并集仅余 5 图，包含 1 张项目照、1 张天窗广告与 2 张 loader，项目图库因通用文件名/空 alt 未能通过标题门；Unit B4 的 14 图都是项目照片。LYCS/ATX 均有 3 个高身份项目样本和 1 个低身份相关新闻，新排序已正确排除新闻，但高身份首样仍可能是照片。下一个全局信号应来自页面声明的结构化当前文章图库（如 JSON-LD/schema.org image），而不是再猜画廊位置。
- 2026-07-17：JSON-LD 检查排除了结构化数据方案：Office Transformation 的 Article 只重复声明首图，Designboom 也只有单个 ImageObject，都无完整图库。但 DOM 给出了跨站点可验证的当前项目边界：ArchDaily 图库图片的链接均是“当前项目 URL 的严格后代路径”，如 `current-project/<media-slug>`；相关项目卡片链到其他项目路径。同一媒体链接同时包含 thumb/空 alt 和 medium/有 alt 变体，因此可按链接去重并优先信息更完整的变体。这可安全补回通用文件名的 first-floor-plan/site-plan/section，无需站点类名特判。
- 2026-07-17：当前页严格后代图库准入的真实复验成功：Office Building Transformation 从 5 图恢复至 18 张当前项目图，且文件名 `section.jpg` 被确定性识别为 section；Designboom 铁路仓库仍为 16 图，Louvre/consent 污染保持 0。当前唯一可见噪声是正文 article 内的 `loader-*.gif`，它不是图库补回导致，应在最终图片快照处只按明确 loader/spinner/placeholder 文件名排除，不泛化屏蔽 GIF。
- 2026-07-17：Quick `604cb714-...` 证明后代图库准入真实生效：三个最终批次均为 4 图/4 页，added 4/4/3，终态 usable 从上一轮 17 增至 35，并连续触发 3+2+1 次正文分析。但最终仍仅流线 1/3；功能强页或只有照片，或正文超时，剖面 LYCS 连续第二条全新 Run 在 `public_page_analysis` 发生 `APITimeoutError`。该重复模式已足以将下一诊断从“偶发上游”收敛到“正文分析输入规模/请求边界”，应比较连续失败页与成功页的文本长度和耗时，不盲目增加 retry。
- 2026-07-17：正文分析长度审计排除了简单截断解法：本轮失败页为 8,784–12,000 字，但 11,655 字的钢厂页成功，两者重叠；硬降到 8k 会丢失已有“6,500 字后机制”回归覆盖。代码边界则明确：SDK 默认 30 秒、`max_retries=0`，所有页分析也硬传 30 秒。下一最小修复仅把所有公共页正文分析的单次超时与 deadline worst-case 提到 45 秒；不增加调用次数、不改其他 Provider 请求的 30 秒边界，也不 retry Run。
- 2026-07-17：新 Quick `214fe57f-...` 给出了 45 秒正文边界的正向证据：本轮所有 public-page analysis 都无超时，并形成 3 个正式项目、3/3、gaps 0。唯一失败转到仍使用 30 秒的资产绑定综合，精确为 `research_synthesis APITimeoutError`。因此稳定边界应统一为所有结构化 OpenAI Provider 请求 45 秒，而不再为综合增加特例 retry；SDK 仍 `max_retries=0`。
- 2026-07-17：公共页的全局稳定性修复最终通过真实 Quick 验收。修复组合不是针对单一题目的特判：共享主意图路由与稳定正文焦点抵抗 planner 措辞漂移；正文媒体与强项目身份/严格后代图库形成安全并集；跨页、跨轮候选在一次有界远程批次中公平选优；所有结构化 Provider 请求统一 45 秒且仍为 0 retry。新 Run `42668844-497e-453f-bcd4-38c9f25cbb25` 在 attempt 0 达到 3/3 和 gaps 0，并成功完成资产绑定综合，证明查询、图纸准入、逐字正文证据与综合链同时闭合。
- 2026-07-17：首条获准 Balanced `0e27aeba-31a7-4283-8f4b-1714916e6563` 的规划门正确生成四个建筑设计分支：功能植入、公众/员工/后勤/消防流线、剖面层次、新旧结构与设备交接界面。第四分支不是来源或写作元问题，并明确要求可逆、脱开、穿越或加固策略及可追溯剖面/节点证据；因此可以继续按 4/4 正文证据门验收，无需干预 planner。
- 2026-07-17：该 Balanced 的研究链在 attempt 0 完整达到 19 usable、3 formal projects、4/4、每分支 3 passes、gap-check gaps 0，唯一终态失败是 `research_synthesis` 在统一 45 秒边界触发 `APITimeoutError`。Quick 的综合结构只强制因果链和建议，而 Balanced 还强制比较与适用边界；让所有结构化调用共享同一单次边界没有反映档位级输出复杂度。下一稳定修复应把综合请求预算显式绑定到 Quick/Balanced/Deep 的结构合同，同时保持其他 Provider 调用、0 SDK retry、证据绑定和 Run attempt 规则不变；不能为该 Run retry 或为该题写特例。
- 2026-07-17：综合边界红测已锁定为档位合同而非题目特例：Quick/Balanced/Deep 分别要求 45/60/90 秒单次综合预算，对应 1,200/2,000/3,200 最大输出和递增的因果链、比较、边界、冲突结构；公共页、规划和检索继续使用 45 秒，SDK `max_retries=0`。旧生产按预期失败为 `[45,45,45]`，最小实现只替换 `synthesize_research()` 的 timeout 映射，三条正文/综合相邻回归转绿。
- 2026-07-17：两条独立 Balanced 的差异不在检索是否执行：第二条完成 24 次 public search、29 个 Direct Playwright 页面、4 个最终远程批次和 14 次正文分析；第一条只有 12 次 public search、22 个页面和 9 次正文分析，却已在早轮形成 4/4。第二条的 program 两个首选正文分析分别 `APITimeoutError/APIConnectionError`，fabric 两个首选也均 `APIConnectionError`；随后恢复页虽有 program relevance 4、flow/section 局部 enriched，但最终没有正式案例。下一诊断必须核对已 enriched 资产为何没有满足 coverage 的条件/机制/转译/逐字证据集合，以及 Provider 失败后强页是否还有确定性分析机会；不能把失败归因于查询次数不足或放宽证据门。
- 2026-07-17：coverage 计算没有丢失第二条 Balanced 的正式结果；它诚实拒绝了半完整正文分析。Australian Red Cross 的 section 有 relevance 4、3 条逐字引文和项目条件，但 `design_mechanism` 为空、transfer 0；Yangpu 的多个 section 同样有条件和逐字引文，却没有机制/转译；Office 恢复资产只有一条引文且通用字段为空。因此 0 formal projects 是 Provider 没有形成完整条件—机制—转译链，不是 ID 连字符、资产去重或 coverage 关联 bug。下一步应比较两条 Balanced 的实际分支查询与 `_public_page_analysis_question()` 规范化结果，判断“来源/图纸可检索”措辞是否仍在压过稳定机制焦点，以及新增的新旧界面分支是否缺少独立全局意图。
- 2026-07-17：确定性重放确认 `fabric-interface` 被共享意图器错误判成 `flow`。问题的主语和主要操作是保留柱网/楼板/桁架/烟囱/设备遗存与新植入功能之间的开洞、退让、架空、跨接和材料界面，但末尾“支持公共空间和后勤设施同时运行”中的次要“后勤”词触发了现有流线类别；正文稳定焦点因此错误追加公众/员工/后勤流线，站内短查询也会沿用流线词。第一条 `old_new_interface` 仅因“新植入”被归为 program，并没有独立界面语义。下一全局修复应新增结构/构造/材料 interface 意图，让强界面短语压过次要流线词，同时保持功能、流线、采光、剖面的既有加权矩阵和证据门。
- 2026-07-17：interface 意图全局红绿完成。中英红测覆盖显式“新旧构造界面/old-new structural interface”和未显式命名但同时出现保留柱网/楼板/桁架、开洞/退让/跨接及次要公共/后勤用途的表达；旧实现四项精确失败为 flow，新增加权类别后站内压缩输出“新旧构造界面/old new structural interface”，workflow 查询使用 retained frame/slab/truss/connection detail，正文焦点强调脱开、开洞、退让、跨接或加固。公共搜索相关性也使用同一 interface 类别；原功能、流线、采光、剖面矩阵共 14 项全部通过，证据门、调用次数与 Provider retry 均未改变。
- 2026-07-17：第三条 Balanced 证明 interface 路由与 60 秒综合都已真实生效，但暴露了正文分析的全局“全列举项覆盖”歧义。Office 页面返回的 limitation 明确说它只证明商业盒子改为庭院办公室、未同时说明插入体/加建体/夹层/空间再分配全部操作，因此机制与转译置空；Australian Red Cross 明确支持恢复中央巷道集中全部水平/竖向主干交通，却因未分别证明公众、员工、货运、维护和疏散的物理分离而置空；AKQA 同样因未覆盖全套流线类别置空。单页证据门的正确合同应是：若正文逐字支持子问题列举项中的一个具体条件—设计操作—空间结果，即可形成受限机制和转译，未覆盖的其他策略/人群必须进入 limitations；不能要求一页回答整个分支，也不能无证据补齐。
- 2026-07-17：部分机制准入合同已红绿实现。Provider 输入测试先精确失败于缺少“单页不必覆盖全部列举项”的指令；最小生产变更明确允许 page_text 逐字支持的一个条件—设计操作—空间结果形成受限机制与转译，并强制把未覆盖策略/使用者写入 limitations，禁止因只回答分支一部分而清空已有证据。相邻 12k 正文尾部机制测试保持通过；这不改变 Pydantic 输出、逐字引文校验、drawing_ids、coverage、调用次数或 0 retry。
- 2026-07-17：第四条 Balanced 说明仅声明“部分机制可接受”仍不足。终态 232 条 Trace、Firecrawl 0；Shrewsbury function 页面有 3 条逐字事实与 context，但模型仍称正文只有年代/铁框架/适应性再利用、没有具体介入操作；Office 与 Yangpu 只保留单条事实。持久化没有丢字段，返回本身就没有 mechanism/transfer。Balanced 的同一 page-analysis 调用还附带 transfer_strategy + applicability_boundary 丰富要求，模型可能仍把丰富目标当成单页准入的全有或全无条件。下一步应先只读复核当前 Direct Playwright 正文中相关机制句的位置与 12k 输入边界，再决定是正文抽取问题还是提示层级问题；不能继续只堆提示句。
- 2026-07-17：零 Provider 的生产解析路径审计已区分来源缺失与模型合同不稳定。Office Building Transformation 当前 `markdown=8811`、分析输入 9019 字，挖空一层盒体并插入玻璃会议室、双庭院和天窗的操作—结果句位于约 3.1k；Australian Red Cross 当前 `markdown=10941`、分析输入 11265 字，恢复中央巷道承载全部水平/竖向主干交通并以庭院引光的句子位于约 3.4k。两者都远在 12k 内，且原始 `article` 根直接包含这些句子，说明不是导航选根或截断。Shrewsbury 当前页面则只有约 900 字 article，正文只给历史与首创铁框架事实，没有任何改造介入句；其机制为空是诚实来源边界，不能补写。下一步应红测并澄清全局结构合同：`facts.statement` 是由逐字 `text_excerpt` 支撑的中文事实转述，不要求逐字出现在英文正文；`design_mechanism` 可复用受支持的事实陈述形成受限机制；Balanced 丰富度是后续综合目标，不是单页全有或全无准入条件。
- 2026-07-17：上述三层合同已完成全局红绿。Provider 输入测试先失败于旧提示没有明确“只有 excerpt 逐字、statement 为证据化中文转述、机制复制受支持 statement、档位丰富度不作为单页准入清单”；生产仅重写 `analyze_public_page()` 的通用说明后转绿。Pydantic schema、正文逐字校验、项目/图纸绑定、coverage、timeout、调用次数和 0 retry 均未改变，Shrewsbury 这类正文无机制的页面仍不会被补写。
- 2026-07-17：新 Balanced `a16cd228-...` 证明结构合同真实改善但尚未完成：Office 首轮形成正式功能链，最终 21 usable、2 formal projects、3/4，功能、公众流线、后勤流线覆盖，唯一缺 `sectional_hierarchy`。剖面分支的 Amaya/Yangpu/Antao 正文确实没有改造剖面机制，LYCS 最终 relevance 3 但未选图；与此同时 Office 与 AKQA 已有正式正文链和 section 图，却只绑定到最初命中的功能/流线分支。当前全局缺陷是已解析的正式强页只有在后续搜索再次返回同一 URL 时才会为新分支重分析。稳定修复应让每个新分支至多复用一次当前最高排名的正式正文页，仍由该分支自己的 Provider 分析和逐字证据门决定是否准入，不直接复制跨分支结论。
- 2026-07-17：正式强页跨分支复用已完成红绿。新行为测试构造首个查询返回强项目、后两个查询只返回不同弱页的情形；旧实现只分析强页一次，新实现从现有资产绑定综合案例的排名中选第一个仍有图纸的已解析正式页，每个新分支至多重分析一次。结果仍完整经过分支问题、drawing_ids、facts/excerpt 和持久化门，不复制旧分支答案；同一 source/subquestion 不重复的既有回归继续通过。搜索次数、远程视觉批次、deadline 和 retry 均未增加。
- 2026-07-17：Balanced `079a1d1e-...` 真实验证跨分支复用：Office 未被剖面/界面搜索再次命中，却分别对 `sectional_hierarchy` 和 `old_new_junction` 完成 relevance 3/4、drawing 1、enriched 1 的独立正文分析；最终两正式项目、3/4，仅缺 `public_logistics`，Balanced 综合成功并含比较/冲突/适用边界。剩余流线召回缺陷来自站点结果二次排序：ArchDaily 当前项目卡的 title/description 均为空，原始站点结果把既有强证据 De Nederlandsche Bank 放第 4，本地评分却只从 URL slug 猜类型，把 raw 6 Shrewsbury 与 raw 9 Carnegie 推入前四，强页从未解析。稳定规则应在卡片有可见 metadata 时保留现有语义重排；title/description 都空时信任站点引擎原序，不再用 URL 猜语义。
- 2026-07-17：空 metadata 站点排序修复经历了一次必要收紧。第一版仅在合并后的 title/description 仍为空时保序，假 DOM 转绿但实页仍失败，因为 ArchDaily 同一 URL 先返回空图片链接、后返回带标题的重复链接；后者补全 metadata 后又触发 slug 重排。最终实现记录每个 URL 首次出现时的 metadata 状态：首个结果窗口全部为空时保留站点 URL 原序，仍允许后续重复项补全展示字段；否则继续共享语义评分。增强红测与 Designboom 矩阵 6/6 通过，实页前四恢复为 Red Cross、AKQA、Librairie、De Nederlandsche。
- 2026-07-17：新 Balanced `72592364-...` 证明站点保序修复真实解决 `public-logistics`，De Nederlandsche 首次进入 source_pages 并形成正式流线证据；Run 最终 26 usable、2 formal projects、3/4，仅缺 `sectional-layers`。剖面分支的一次正式页复用选择了全局排名第一的 Blue School，其只有 1 张 section 且正文不含剖面操作；当时同样正式的 De Nederlandsche 有 2 张 section 与 1 张 site plan，却未被选择。稳定复用仍应保持每分支最多一次，但候选排序先比较当前意图匹配的图纸数量，再沿用正式案例现有排名；不能简单增加第二次调用。
- 2026-07-17：正式页复用选择已按意图图纸数红绿。新测试让前两个查询分别形成 1 张 section 与 2 张 section 的正式来源，第三个剖面查询不再返回两者；旧实现复用第一个来源，新实现使用共享 intent 映射统计 section/flow/program/interface/daylight 的优选图纸类型，匹配数和总图纸数相同才沿用案例排名。目标与两项调用上限回归 3/3 通过；没有增加复用页数、搜索、视觉批次或 retry。
- 2026-07-17：Balanced `aeefe013-...` 已在三轮达到 4/4、gaps 0、4 formal projects、29 usable，但 60 秒综合对 11 个正式资产输入发生 `APITimeoutError`。结构签名审计证明同页同类型案例高度重复：COOP 4 个 section 完全相同，Yangpu 3 个 section 完全相同，De Nederlandsche 的两个 section 相同且另有一个 site plan，Blue School 一个 section。综合没有读取图像像素，稳定压缩应仅去除同来源、同 asset type、同分析和同证据的精确重复，保留不同图纸类型及任何不同分支/证据；这可把本轮 11 cases 收敛到约 5 而不解绑来源。
- 2026-07-17：综合输入精确去重已完成全局红绿。身份键包含来源 URL、资产类型、主分析、分支分析、转译步骤、限制和证据；子问题 ID 与证据按无序集合规范化，转译步骤保持顺序，只有资产 ID 和展示项目名不参与内容等价判断。排名最靠前的真实资产作为保留项，因此综合引用仍绑定可审计资产。封存 Balanced `aeefe013-...` 的只读重放从 11 cases 精确降为 5：COOP/Yangpu/Blue School 各一张 section，De Nederlandsche 同时保留 section 与 site plan；不同图纸类型未被折叠。
- 2026-07-17：新 Balanced `26562aec-...` 证明综合去重已真实解决超时：资产综合完成并包含比较限制、冲突与适用边界，Trace 0 Firecrawl；终态仍为 27 usable、1 formal project、2/4。两个未覆盖分支各完成 6 passes 和一个 `classified 4 / added 1` 远程批次。De Nederlandsche 已有 section/site plan，Yangpu 已有多张 section，正文分析分别达到 relevance 2/3，但返回 drawing_ids 为空，完整正文链因此在持久化前被丢弃。稳定边界应只在逐字支持的 context/mechanism/transfer 已完整且存在当前意图匹配的项目图纸类型时，确定性选择一个图纸作为后续审计入口；这不代表读取图像像素，也不得对照片、无关类型或不完整正文证据生效。
- 2026-07-17：确定性图纸审计入口已完成全局红绿。Provider 提示明确 drawing_ids 是基于当前项目归属、asset_type 与子问题相关性的后续人工核验入口，不要求正文逐张描述图纸，也禁止把未读取像素写成事实。持久化仅在逐字校验后的 project_context、design_mechanism 和 transfer_strategy 全部成立且模型未选任何 ID 时，从共享 intent 类型表选择第一张相关图纸；照片、无关类型、证据不完整分析和模型已明确选择的 IDs 均不受影响。该类型表同时服务正式页复用排序，避免两套规则漂移。
- 2026-07-17：Balanced `944a67c9-...` 真实验证确定性审计入口：De Nederlandsche 的 circulation 绑定 3 图/enriched 3，section 绑定 2 图/enriched 2，最终 2 formal projects、3/4；唯一缺 structure，因强 De Nederlandsche 分析 `APITimeoutError`，SMOORE 正文则诚实缺少结构介入机制。综合仅有 3 个精确去重 cases 仍超时，排除“仅案例数量”解释。序列化审计发现 payload 11,608 字符，所有 cases 同时发送顶层分析和相同的 subquestion_analysis，形成结构化重复。下一压缩只应改变综合 wire payload：有分支分析时省略重复顶层分析，无分支兼容 case 保留顶层字段；资产 ID、类型、来源、分支与证据必须完整保留。
- 2026-07-17：综合 wire payload 压缩完成红绿。双 case 三档合同测试证明：有 subquestion_analysis 时只序列化 asset_id/project_name/asset_type/source_url/subquestion_ids/subquestion_analysis，无分支兼容 case 仍保留顶层 context/mechanism/transfer/limitations/evidence。封存 `944a67c9-...` 的 3-case payload 从 11,608 降至 6,362 字符，减少 45.2%；不同资产类型和资产级 provenance 均保留。该变化不修改数据库模型、精确去重键、综合强度验证、max output、timeout 或 retry。
- 2026-07-18：Balanced `0b2db151-...` 达到 3 formal projects、3/4，功能与两条流线正式覆盖；唯一缺 sectional_layers，COOP 强分析超时、Yangpu relevance 3 但正文缺完整机制。5 个压缩 cases 仍有 15,368 字符且综合再次 60 秒超时。稳定修复转向输出工作量而非继续压输入或加 timeout：Balanced 明确只生成 2 条因果、2 条比较、1 条冲突、2 条边界、2 条建议，每条 statement 不超过 100 个汉字，max output 从 2,000 收敛到 1,600；输入证据、验证器和 0 retry 不变。
- 2026-07-18：完整 Run `926adfb5-...` 已达到 27 usable、4 formal projects、4/4、Firecrawl 0，唯一失败为综合。复用其持久 5 cases 的 scratch 综合第一次在 39.4s 返回但因结构 `ValueError` 被拒，第二次在 37.6s 完整通过 2 causal / 2 comparisons / 1 conflict / 2 boundaries / 2 recommendations，证明 1,600-token 输出限界已消除超时，剩余是单次结构波动。
- 2026-07-18：后续完整 Run `5bf96ad9-...` 的综合成功，但研究终态为 32 usable、2 formal projects、2/4、Firecrawl 0。缺失分支不是搜索空结果：LYCS Architecture Headquarters 的 sectional analysis 仅因 `APITimeoutError` 失败，Canal Hub 1958 Shenzhen 的 old-new-interface analysis 同样仅因超时失败；其他候选正文诚实缺少完整机制。效率策略改为先对这两个已保存强页做 scratch 分析，不写数据库、不 retry Run；目标阶段稳定通过后才实现执行层 fallback 和最后一次完整验收。
- 2026-07-18：执行层审计确认 `_try_public_page_analysis()` 当前对 `analyze_public_page()` 的任意异常只记录失败 Trace 并立即返回 0；`_persist_public_page_analysis()` 后续严格逐字事实、条件—机制—转译和相关图纸门从未获得降级输入。因而 fallback 应位于 Provider 调用失败与既有持久化门之间，从已保存 page text/drawing metadata 生成可审计的确定性候选；不能通过 retry Provider、放宽证据校验或直接写 Run 覆盖来实现。
- 2026-07-18：封存 Run `5bf96ad9-...` 的只读 SQLite 重建确认两条目标输入分别是 Designboom LYCS Headquarters + `section_layers` 和 ArchDaily Canal Hub 1958 + `old_new_interface`，失败 Trace sequence 164/174 均为 `APITimeoutError`。`source_pages` 只保存 URL/title/publisher，不保存正文；候选仍保留页面归属和图像元数据。因此 scratch 将用同一 Direct Playwright parser 重新获取两个固定页面，再使用封存 Run 的原子子问题与 Balanced requirements 调用 page analysis，全程不写 SQLite。
- 2026-07-18：LYCS 的唯一 scratch 再次在 45 秒、0 retry 的 `analyze_public_page()` 超时，说明该强页在当前执行合同下并不稳定，不能把此前 Run 超时当作偶发后直接发起完整 Balanced。下一实现必须覆盖同一调用失败后的确定性证据恢复，而不能依赖再次调用模型。
- 2026-07-18：Canal Hub 的唯一 scratch 在 35.19s 返回，但没有形成正式界面链：当前 8,435 字正文只逐字支持旧钢厂背景、保留两栋厂房及既有红砖/柱列/屋架/遮阳构件；没有新介入如何连接、脱开、穿插、开洞、退让、跨接或加固的正文操作，且重建 drawing metadata 只有 photograph。该页不能因先前超时就被 deterministic fallback 提升；fallback 必须继续允许“有事实但无机制”诚实失败。
- 2026-07-18：零 Provider 复读纠正了上述“Canal 来源缺机制”判断：同一页面的后续 Direct Playwright 快照达到 12,000 字，约 8.3k 后逐字写明新增内部钢结构与既有混凝土墩协同承重、全钢悬挂体系向上支承屋顶画廊且与旧结构完全脱开。先前 8,435 字 scratch 恰好在机制段前截断，证明动态正文读取本身不稳定。LYCS 的 10,747 字快照也在约 3.4k–4.2k 明确写明三个中庭嵌入大跨结构跨形成无柱采光庭院，并以坡道、连桥和楼梯连接各层。两页都是强来源；失败组合是“动态页面过早快照或结构化请求超时”，不是证据缺失。
- 2026-07-18：最小全局修复由两个有界层次组成：Direct Playwright 在原 3.5s settle 与滚动后读一次，再等 1s 复读并按正文长度择优，总边界仍不超过 20s；OpenAI page analysis 的正常完整 12k 输入不变，仅当 timeout/connection 暂态异常时，从开头上下文及当前 intent 的高相关句和相邻句确定性组成最多 6k fallback 输入。fallback 仍由结构化模型生成中文转述，并继续经过逐字 excerpt、机制/转译与图纸持久化门，避免本地 NLP 编造正式结论。
- 2026-07-18：fallback 最终离线门禁通过：完整 browser-inspection、Ruff lint、47 文件 format check、strict Mypy 18 source files、277 API tests 与 `git diff --check` 全绿。工作树 staged 0；只存在既有 LF→CRLF 提示。下一真实动作可严格限定为服务重启、active Run 0 守卫和一条最终 Balanced attempt 0，用来验收 4/4 正式正文链、2/2/1/2/2 综合和 Firecrawl 0。
- 2026-07-18：最终唯一 Balanced `08e0ad17-...` attempt 0 已封存为 `blocked/budget_exhausted`：48 usable、6 projects、3/4，缺失仅 `program_insertion`；20 次 `public_page_analysis` 全部 completed、0 failed，research synthesis 成功并严格输出 2 causal / 2 comparisons / 1 conflict / 2 boundaries / 2 recommendations，Trace Firecrawl 0。动态复读真实让 Canal 稳定达到 12k；本轮没有 page-analysis 暂态失败，因此 fallback 未被 Live 链路触发。
- 2026-07-18：功能缺口不是正文或综合失败，而是严格图纸准入边界。最终跨页批次对 4 页各分类 1 图，全部为 photograph；三页正文仍得到 relevance 3/4/4，但 drawing_ids 0，确定性审计入口按已批准规则拒绝照片。零 Provider 元数据复核显示四页分别有 21/12/26/16 张图片，URL/alt 没有 plan/section/axonometric 信号，唯一 “drawing” 是无关侧栏标题。若继续，最小候选是保持一次最多 4 图不变，先红测多页无类型图库不总取每页第一个采样位，而是在页面间确定性错开等距位置；该策略需要新批准和新的 Live Run，不能在本次最终 Run 后静默扩大。
- 2026-07-18：用户纠正了全局产品边界：建筑设计方法研究以项目正文为主，图片只用于预览和返回源网站深入查看，不必精准对应问题或文字；严格图纸匹配只属于明确找图/出图任务。M48 因此撤销“formal precedent case 必须有相关 drawing_id”的全局假设。Quick/Balanced/Deep 的案例覆盖应只由逐字正文事实、项目条件、设计机制和转译步骤决定；代表图不能证明机制，也不能因不精确而阻止完成。
- 2026-07-18：M48 红测确认错误耦合共有三层：同来源 photo 已存在且正文链完整时，`_persist_public_page_analysis()` 仍因无 intent-matching drawing 清空机制；页面完全无图时 `_try_public_page_branch_analysis()` 甚至不调用 Provider；Board 把已有正文案例的空预览写成“未提取到图纸”。修复必须同时允许 representative photo、text-only placeholder 和文字优先 UI，但继续拒绝无逐字事实/机制/转译的 visual lead。
- 2026-07-18：M48 的正式案例准入已与图片完全解耦：完整的逐字正文证据链可以绑定意图匹配预览、任意同源照片预览，或创建 `image_url=None` 的纯文本案例；图片归属保持独立，预览不证明项目事实或设计机制。
- 2026-07-18：跨子问题复用原先仍含一个隐蔽的 `if not drawings: continue`。无图强正文页因此会在后续分支重新依赖搜索结果；删除该门并增加无图复用行为测试后，缓存正文可稳定复用于所有子问题。
- 2026-07-18：Precedent 正文分析不再等待远程视觉分类。无类型同源图片直接按 photograph 作为预览；只有 `visual_reference_search` 或明确图片/图纸任务继续以视觉类型和相关性决定结果。Board 同步移除“仅为补图而重新研究”的交互。
- 2026-07-18：`CoverageData.usable_assets` 仍表示可显示图片/本地内容数量，便于兼容现有指标；`verified_or_partial` 与 precedent 子问题 coverage 不再由 `image_url` 过滤。真实正文门仍通过 `article_ready` 的条件、机制、转译和逐字 EvidenceClaim 决定。
- 2026-07-18：M49 单一真实 Quick `c8a67738-...` attempt 0 证明 M48 正文主链成立：3/3、5 projects、8 article-ready cases，功能/流线/剖面分支分别有 2/4/2 个正文案例；8/8 案例的顶层 context 与 mechanism 均有同来源非空 text_excerpt，14 次 page analysis 全部 completed、0 failed，Firecrawl 0。所有正式案例本轮恰好都有同源预览，因此“完全无图”仍由离线行为测试证明，而不是虚构为 Live 命中。
- 2026-07-18：M49 整体没有通过，唯一终态 gap 是 `research_synthesis_incomplete`。唯一 synthesis 事件以 `APITimeoutError` 失败；正文 coverage 在综合前已达到 30 usable / 30 verified-or-partial / 5 projects / 3 of 3 / enrichment gaps 0。该 Run 必须封存且不得 retry；若继续，下一问题应只围绕 Quick 的运行级综合超时，不得回到图片门或扩大研究范围。
- 2026-07-18：M50 用生产 `_research_synthesis_cases()` 只读重建 M49：8 个 article-ready 资产经精确身份去重后为 7 cases，Provider 现有 Quick `assets=6` 粗截断实际发送前 6 个、10,310 字。它已覆盖全部 3 个子问题，但每题恰好各有 2 个案例；每 case 仅一个分支和 2 条逐字 evidence。Quick 明确不要求横向比较，因此最小稳定边界是沿现有 rank 顺序为每个计划子问题选择第一个完整案例，去重同一 case，总量 3；Balanced/Deep 的既有 12/18 上限不变。
- 2026-07-18：M50 的 Quick-only 选择规则已用封存 M49 做零 Provider 实证：按计划子问题顺序分别选择现有 rank 中首个匹配案例，并按 asset ID 去重，7 个精确去重 cases 收敛为 3 个，依次覆盖 `program_insertion`、`circulation_zoning`、`sectional_daylight`，wire payload 为 5,330 字。Balanced/Deep 继续使用原有档位资产切片；没有改综合输出合同、timeout、token、retry、爬取或图片逻辑。
- 2026-07-18：M50 完整离线门禁通过：280 API、76 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48 文件格式、strict Mypy 18 source files、两端 lint/typecheck/build 和 30/108 evaluation 全绿。`git diff --check` 通过，仅既有 LF→CRLF 提示；全程没有 Provider、Firecrawl、Live Run、Deep、截图、暂存或提交。
- 2026-07-18：M51 唯一真实 Quick `a2cf2e20-af1f-4ffb-a2c2-9498c48ffe73` attempt 0 完成 `completed/coverage_satisfied`：21 usable、7 projects、21 verified/partial、3/3、gaps 0、enrichment gaps 0，11 次正文分析全部 completed，48 Trace、Firecrawl 0。唯一 synthesis 事件成功，gap-check 至完成约 32.3 秒，证明 M50 在 45 秒边界内真实通过。
- 2026-07-18：M51 精确去重后的正式输入有 9 cases；Quick 按计划顺序分别为 `program-insertion`、`circulation-separation`、`sectional-daylight` 选择首个完整 case，每题都有 2 条逐字 evidence。功能与剖面恰好选择同一 asset，因此 asset-ID 去重后实际发送 2 cases、5,662 字；这是规则预期结果，不应为凑足 3 个而重复同一案例。综合只引用这两个允许的资产 ID。
- 2026-07-18：M52 初步边界盘点确认相对 `98a9a01` 有 83 个 tracked 路径和 22 个 untracked 文件。22 个未跟踪项可明确分成三类：7 个产品/测试/文档候选，10 张独立作品集证据 PNG，以及 5 个可再生成的本地工具输出（两份重复 pytest XML、两份 Playwright CLI 日志/快照、一个 Impeccable live 配置）。现有 `.gitignore` 未覆盖这些本地产物，因此不能使用笼统 `git add -A`。
- 2026-07-18：M52 核对确认 7 个未跟踪候选都有明确归属：`run_gate.py` 被 API/main 直接导入且有独立回归；持续加载 HTML 被 packaged E2E 三处使用；`accept-xhs.py` 是零 Provider 的只读验收脚本；Extension `PRODUCT.md` 是扩展产品边界；release evidence 清单绑定 10 张 PNG；`HANDOFF.md` 是恢复契约。删除其中任一源码/fixture 都会使当前 tracked 改动不完整，因此这些候选应与对应实现一并纳入，而不是作为杂项忽略。
- 2026-07-18：变更主体高度交叉：API source 14 文件约 +3,832/-548、API tests 15 文件约 +4,517/-621；Extension source/tests 约 +2,156/-305；Board 约 +1,217/-327。`run_gate`、浏览器协议、正文研究、Board 展示和文档由同一完整门禁共同验证，强行按里程碑回溯拆成许多提交会产生难以验证的中间状态。稳定提交边界应按“完整产品源码+测试+运行文档”“独立发布证据 PNG”分层，而不是按 M41–M51 逐段猜拆。
- 2026-07-18：M52 发布安全复核通过：release evidence 中 10 张 PNG 的 byte 数与 SHA-256 全部匹配；tracked additions 与 7 个未跟踪候选中没有高置信 Provider key、Bearer token、私钥或签名 URL 命中；生产源码、配置、Board、Extension、脚本及当前用户文档中 Firecrawl 引用为 0。两份同尺寸 pytest XML 的内容哈希不同，说明它们是不同测试时刻的临时报告，更不应作为发布源码或证据提交。
- 2026-07-18：M52 最终集合审计把全部 105 个路径无遗漏、无重叠分为：84 个产品源码/测试/依赖/用户文档路径，5 个工程记录（`AGENTS.md`、`HANDOFF.md`、`task_plan.md`、`findings.md`、`progress.md`），11 个发布证据路径（manifest + 10 PNG），5 个本地工具输出。建议提交顺序为：先完整产品提交，再工程记录提交；只有用户明确选择把二进制证据纳入 Git 时，才单独提交 manifest + PNG。提交前应先小幅补 `.gitignore` 排除 XML、`.playwright-cli`、API artifacts 和 Impeccable live 配置，同时保持 `.artifacts/portfolio` 可见。
- 2026-07-18：M53 当前规则 Balanced `d995bed5-8c40-4df9-80e4-4fe6256d3628` 在唯一 attempt 0 真实通过：`completed/coverage_satisfied`，34 usable、6 projects、4/4、gaps 0、enrichment gaps 0。9 个精确去重 cases 全部进入 Balanced 综合，payload 19,726 字；唯一 synthesis 从 gap-check 到完成约 34.6 秒，结构精确为 2 causal / 2 comparisons / 1 conflict / 2 boundaries / 2 recommendations。12 次 page analysis completed、4 次 failed，60 Trace、Firecrawl 0。
- 2026-07-18：M53 Deep `6e255a32-2859-49d4-a5d9-a26d4d511ca2` 在唯一 attempt 0 封存 `blocked/research_synthesis_incomplete`：正文研究已达 55 usable、8 projects、6/6，24 page analyses completed / 4 failed、169 Trace、Firecrawl 0。失败不是 Provider synthesis timeout：Trace 中 synthesis 事件为 0。Run 用时 1,833.3 秒；6/6 已在 30 分钟前形成，但 workflow 为补 `insufficient_subquestion_assets` 继续 enrichment，最终检查 `deadline - clock() >= provider_call_reserve` 不成立，直接跳过综合。
- 2026-07-18：Deep 调度存在两个相连的全局边界错误：研究循环只保护通用 `worst_case_call_seconds=45`，没有保护 Deep synthesis 的 90 秒模式预算；而已完成 coverage 后仍允许可选 enrichment 启动公共搜索和多页解析，单次查询可跨 deadline。稳定修复应在 coverage 已完整时把模式综合预算作为不可消费保留时间，停止可选 enrichment并进入综合；不能增加总时限、retry Provider或放宽证据门。
- 2026-07-18：Deep 综合 reserve 已完成红绿最小修复。行为红测在总预算 250 秒、已完整 6/6、剩余 95 秒且仍有 enrichment gap 时，旧 workflow 仍发起 6 次公共搜索；生产现从研究 deadline 预扣档位综合预算，Deep 保留 90 秒、Balanced 60 秒、Quick 45 秒，研究阶段所有搜索、解析、正文分析和远程批次统一使用该 research deadline，最终综合仍使用总 deadline。目标与原“模型调用不够时间时仍允许较短公共搜索”回归均绿；总时限、证据门和 retry 未改变。
- 2026-07-18：Deep reserve 修复通过完整离线门禁：281 API、76 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48 文件格式、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全绿。首次静态检查只要求机械格式化本轮 `workflow.py` 表达式，格式化后从最终源码重跑 9 项聚焦回归和完整门禁均通过；没有 Provider、Run、Firecrawl、Deep 或截图调用。
- 2026-07-18：替代 Deep `fa03effa-5939-407c-a35c-c051fbbd9dca` 真实证明 synthesis reserve 调度修复生效。Run 在最终 6/6 gap-check 后停止研究，并在总耗时 1,767.7 秒内产生恰好一条 `research_synthesis` 事件；与旧 Deep 的 synthesis events 0 不同，综合调用已实际获得预留窗口。该调用约 61 秒后以 Provider `InternalServerError` 失败，因此终态仍是 `blocked/research_synthesis_incomplete`，不得 retry。
- 2026-07-18：替代 Deep 的正文研究本身通过：57 usable、7 projects、6/6、12 article-ready assets、7 article-ready projects；六个子问题都有完整且 context/mechanism 均由同源非空逐字 excerpt 支持的条件—机制—转译链。逐分支可审计链为 program 1、flow 5、structure 3、section 4、daylight 4、public sequence 4；25 次 page analysis completed、2 failed、154 Trace、Firecrawl 0、`research_sources=[]`。由于唯一综合失败，没有 Deep 的 causal/comparison/conflict/boundary/recommendation 输出可供结构验收，M53 三档整体验收不能宣告完成。
- 2026-07-18：M54 执行层审计确认 `_try_research_synthesis()` 对 Provider 任意异常只记录 failed Trace 并返回 `None`。现有 `ResearchSynthesis` validator 能约束 Deep 至少 2 causal / 2 comparisons / 1 conflict / 2 boundaries / 2 recommendations，并限制 evidence asset IDs，但不会验证 statement 是否逐字或语义上来自对应案例。确定性 fallback 因而必须使用更窄的构造边界：只重排 `ResearchSynthesisCase.subquestion_analysis` 中已通过正文证据门的 context、mechanism、transfer、limitations，不自由补写事实，也不能把普通案例差异冒充来源冲突。
- 2026-07-18：封存 Deep 的零 Provider scratch 证明 deterministic synthesis 可行：生产 `_research_synthesis_cases()` 重建 11 cases，其中 12 个分支同时具备至少两段逐字 evidence、完整 transfer 与已验证分析，5 个子问题具备不同项目的同题比较对。只取原字段组成 2 causal / 2 comparisons / 1 evidence-boundary conflict / 2 applicability boundaries / 2 recommendations 后，现有 Deep validator 通过，全部 evidence asset IDs 均在允许集合。无需第二次模型调用或新增事实。
- 2026-07-18：M54 最终执行规则只在已知可恢复的 synthesis transport/server/rate-limit 错误，以及明确的 Provider 结构输出错误后启用；任意普通 `ValueError`、`AssertionError` 等编程异常仍记录 failed 并保持 Run 阻断。fallback 不做第二次 Provider 请求，Trace 只记录一条 completed `research_synthesis`，同时写明 `generation_mode=deterministic_fallback` 与原 Provider error type。
- 2026-07-18：fallback 的内容边界比现有 schema 更严格。只有 branch context 和 mechanism 都能在 `statement｜原文：excerpt` 中逐项匹配、且 transfer 非空的 case 才可参与；Quick 只输出 1 causal / 1 recommendation，Balanced/Deep 精确输出 2 causal / 2 comparisons / 1 evidence-uncertainty conflict / 2 boundaries / 2 recommendations。比较仅并列同题不同项目的已验证机制，冲突项只并列明确 limitations，不宣称来源互相矛盾或某方案更优；answer 以“【本地证据汇总】”向用户暴露降级路径。
- 2026-07-18：封存 Deep 用最终生产 builder 零写入重放为 11 cases、精确 2/2/1/2/2、全部引用 ID 合法、最长 statement 255 字。完整 workflow fixture 进一步证明唯一综合 `InternalServerError` 后 Run 可诚实完成为 `completed/completion_satisfied`，coverage 和唯一 synthesis Trace 均标明 deterministic fallback。最终全仓门禁通过 288 API、76 Board、165 Extension、8 packaged E2E，以及 Ruff/format、strict Mypy、两端构建和 30/108 evaluation；没有 Live Provider、Run、Firecrawl 或截图。
- 2026-07-18：用户授权三档验收主线持续自主修复，不再逐轮等待批准。今后每条失败 Run 仍永久封存且绝不 retry；Agent 可在同一既定范围内自动离线诊断、红绿修复、跑完整门禁，并创建一条全新 attempt 0 继续验收，直到通过。Firecrawl、视觉来源、截图、提交和范围外能力仍未获授权。
- 2026-07-18：M54 后全新 Deep `96e03065-a758-4c76-be33-49ccae9fbd8d` 已在唯一 attempt 0 封存 `blocked/research_synthesis_incomplete`，不得 retry。正文研究本身达到 47 usable、7 projects、6/6、gaps 0，24 次 `public_page_analysis` completed、140 Trace、Firecrawl 0、`research_sources=[]`；但 `research_synthesis` 事件仍为 0，因此 deterministic fallback 没有获得执行机会。Run 总耗时 1,743.7 秒。
- 2026-07-18：`96e...` 的只读时间线定位到新的调度缺口：sequence 129 在 Deep research deadline 前约 35 秒启动可选公共搜索，本地搜索结束后直接来源页循环连续解析 4 页并跨过 research deadline，最终只剩约 56 秒，小于 Deep synthesis reserve 90 秒。项目展开页路径已有逐页 `public_page_parser.worst_case_call_seconds` 守卫，直接 `source.url` 解析路径没有。正确的全局合同是保留“模型调用放不下时仍可执行较短公开搜索”，但每个直接来源页开始前都必须重新确认剩余研究时间足以覆盖 parser 最坏耗时；否则跳过该页并进入综合。
- 2026-07-18：M55 最小修复仅在直接来源页解析条件加入逐页重算的 parser reserve；没有改变搜索门、总时限、页面预算、Provider retry、正文证据门或综合 fallback。两页 Deep fixture 现在在剩余 40 秒时执行一次公开搜索，35 秒时解析第一页，25 秒时跳过第二页，并在总 deadline 尚余 115 秒时恰好调用一次综合；原“模型搜索已放不下但较短公开搜索仍执行”的合同也保持通过。
- 2026-07-18：M55 最终全仓门禁通过：288 API、76 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全绿；`git diff --check` 也通过。没有 Provider、真实来源、Run、retry、Firecrawl、视觉来源或截图调用。下一 Live 动作仅为加载当前代码并创建一条全新 Deep attempt 0。
- 2026-07-18：M55 后 Deep `e1a00ca9-ed48-4033-b64d-228a7996e300` 已在唯一 attempt 0 封存 `blocked/time_budget_exhausted`，不得 retry。终态 49 usable、12 projects、5/6，唯一缺 `adaptive_interface`；25 次 page analysis completed、2 failed、195 Trace、Firecrawl 0。M55 与 M54 均通过 Live 路径：sequence 194 实际产生唯一 completed `research_synthesis`，Provider `InternalServerError` 后 deterministic fallback 输出精确 2/2/1/2/2。
- 2026-07-18：`e1a...` 暴露的下一层缺陷是 coverage-first 调度而非来源或综合失败。第三轮末已是 5/6；`adaptive_interface` 三次查询仅有一次 relevance 1 的正文分析。固定 round-major 查询顺序随后在第四轮先执行已覆盖的 `program_insertion`，耗尽 research deadline，未把同轮剩余预算优先给唯一未覆盖题。当前 loop 只在 retry continuation 或 `round_number > normal_rounds` 时跳过已覆盖分支；正常轮即使尚未完整覆盖仍继续 enrichment。全局修复应在 precedent coverage 未完成时跳过已覆盖分支，把下一次查询机会留给未覆盖分支；coverage 完整后才恢复正常 enrichment。
- 2026-07-18：M56 最小调度修复已在 loop 现有 coverage 检查中增加 `covered_subquestions < subquestion_count` 条件，没有重排查询文本、增加轮次/总时限或改变来源。参数化行为覆盖两侧合同：C 未覆盖时第二轮从 `A,B,C,A,B,C` 变为 `A,B,C,C`；A/B/C 均已覆盖但 enrichment 未满时仍继续第二轮 A，不会把研究深度误缩成每题一次。
- 2026-07-18：M56 最终完整门禁通过：290 API、76 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation 全绿。首次全 API 只暴露旧恢复 fixture 仍要求覆盖题重复两次；更新为精确 coverage-first 序列后从最终源码重跑全绿。未创建或 retry Run、未调用 Provider/Firecrawl、未截图。
- 2026-07-18：用户纠正持续授权的执行方式：自主推进不等于每修一个错误就立即新建一条 Deep。今后失败 Run 仍永久封存且不 retry，但必须先合并所有已知失败链，完成封存 Run 形状的零 Provider 端到端重放和完整门禁；只有综合预检通过后才允许创建下一条 Live attempt 0。无需逐次等待批准，但要最小化 Live Run 数量。M56 后仅重启了服务，尚未 POST 新 Run。
- 2026-07-18：M57 封存形状联合预检通过。fixture 从 5/6 开始，只执行缺失 `branch-f` 的一条持久化 QueryAttempt；短公开搜索一次、模型搜索 0 次；两页直接来源中只解析时间足够的第一页，第二页因 parser reserve 跳过；随后 completed 6/6。唯一综合模拟 `InternalServerError`，M54 fallback 仍精确输出 2/2/1/2/2。M54/M55/M56 已在一条零 Live Provider 的完整 workflow 中共同验证。
- 2026-07-18：M57 最终完整门禁通过：290 API、76 Board、165 Extension、8 packaged Chrome E2E，Ruff lint/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build、30/108 evaluation 与 `git diff --check` 全绿。综合预检与完整门禁均满足用户纠正后的唯一 Live 前置条件；服务已加载相同生产代码，M57 仅增强测试，无需再次重启。
- 2026-07-18：必要 Deep `3ed657fd-ecd8-4bcc-899a-fe8207afbea2` 已在唯一 attempt 0 封存 `blocked/time_budget_exhausted`，不得 retry，也不立即创建下一条。终态 47 usable、8 projects、5/6，仅缺 `program_insertion`；25 次 page analysis、93 Trace、Firecrawl 0。唯一综合实际调用，Provider `InternalServerError` 后 deterministic fallback completed 2/2/1/2/2；M54/M55 正常。
- 2026-07-18：`3ed...` 证明 M56 没有获得第二正常轮机会。首轮六题依次耗时约 4:06、4:27、4:14、4:19、6:35、4:02，分别执行 4、4、4、5、5、3 次正文分析；program 第一题 1 failed + 3 completed 仍未过 article-ready 门，其他五题各覆盖一次。六题首轮本身几乎耗尽 research deadline，随后直接综合。下一层全局修复必须在 coverage 未齐时为每题单轮正文分析设置公平边界，先完成六题轮转并保留缺失题恢复窗口；coverage 齐全后再放开 enrichment，不能增加 1,800 秒总预算。
- 2026-07-18：封存 `3ed...` 的逐次结果支持每题单轮 3 次正文分析作为最小公平边界。五个已覆盖分支都在前三次内已有 article-ready 结果：public-service 为第 2/3 次、people-vehicle 为第 1/3 次、old-new 为第 2/3 次、daylight 为第 2/3 次、sectional 为第 2 次；第 4/5 次均只属额外 enrichment。把 25 次降到最多 18 次可在 45 秒最坏边界下释放约 315 秒，足够为未覆盖 program 启动一次新的搜索、解析和正文分析，而不降低本轮实际 5 个已覆盖分支。
- 2026-07-18：M58 六题缩放最坏耗时联合重放通过。每个 `(subquestion, query generation)` 最多 3 次正文分析；六题首轮后 M56 只执行缺失 branch-a 的第二次查询，七个查询代际总分析 19（理论上限 21），在 research deadline 内完成 6/6。随后唯一 synthesis 模拟 `InternalServerError`，M54 fallback 仍精确输出 2/2/1/2/2。coverage 完整 fixture 同时证明 enrichment 查询仍可分析第 4 页。
- 2026-07-18：M58 最终离线门禁确认公平边界未牺牲深度：未覆盖状态严格限制同题同查询代际最多 3 次正文分析，6/6 后第 4 次 enrichment 仍可执行；M54–M58 聚焦 9/9、完整 293 API / 76 Board / 165 Extension / 8 packaged E2E、静态检查、两端构建和 30/108 evaluation 全绿。该结论完全来自零 Live Provider 的 workflow 重放；下一次 Live 只承担一次必要的端到端验收，不再作为逐错误诊断工具。
- 2026-07-18：M58 后唯一 Live Deep `7b69bf9c-...` 的首个 `program_insertion` 查询代际真实执行 3 次正文分析后停止，并在 sequence 15 已以 1 个正式项目覆盖该分支；sequence 16 随即轮转到 `public_service_flows`，没有继续消费第 4/5 次分析窗口。M58 的公平限制已穿过真实 Direct Playwright + Provider 链路，当前 Firecrawl 0。
- 2026-07-18：Deep `7b69bf9c-86fe-4cb4-976e-1de92dbe69a7` 已永久封存为 `blocked/research_synthesis_incomplete`：33 usable、4 projects、4/6、19 次正文分析、117 Trace、Firecrawl 0、`research_sources=[]`。首轮从旧失败形状的 25 次分析降为 16 次并进入恢复轮；`old_new_structure` 在第 3 轮连续 3 次分析后补齐，但这耗尽后续缺失题的研究窗口，采光只来得及解析新页而未分析，空间序列没有获得第 3 轮。下一层公平缺口因此是“多个未覆盖题之间仍按题连续消费最多 3 次”，不是 M56 的已覆盖题跳过失效。
- 2026-07-18：该 Run 的综合实际调用一次并以 Provider `InternalServerError` 失败，但 4/6 证据形状没有生成 M54 deterministic fallback，最终加入 `research_synthesis_incomplete`。覆盖不足仍是主阻断；综合 fallback 的最小证据前置也需在离线审计中单独确认，不能把两者混成一次 Live 重试。
- 2026-07-18：封存 `7b69...` 的生产 cases 零写入 scratch 为 4 cases：program、flow、section 各只有 1 个可用项目，old-new 有 2 个，daylight/spatial 为 0；因此只有 1 个同题比较分支，而 Deep deterministic fallback 明确需要至少 2 个同题双项目比较分支。fallback 返回 `None` 是正确证据边界，不应放宽。M59 只修多个缺失题之间的后续轮分析切片：首轮 3 次不变，coverage 未齐时第 2 轮起每题每轮 1 次，6/6 后 enrichment 不限。
- 2026-07-18：M59 最终联合重放把六题首轮后的 A/E/F 三个缺口按后续轮各 1 次分析切片完成 6/6；旧实现会让前序缺失题各连续消费 3 次并阻断末题。第一轮最多 3 次、后续未覆盖题每轮 1 次、6/6 后不限三侧合同与 M54/M55/M56/M58 共同通过，唯一 synthesis `InternalServerError` 仍产生精确 2/2/1/2/2 fallback。完整门禁为 293 API、76 Board、165 Extension、8 packaged E2E，静态检查、两端构建和 30/108 evaluation 全绿。
- 2026-07-18：M59 后 Live `aa1923fd-...` 首轮以 16 次正文分析在约 16 分钟达到 4/6，六题均至少有 1 pass；仅 program/daylight 未 article-ready。round 2 只执行这两个缺失题，program 搜索 0 后立即轮到 daylight，证明 M56 跳过覆盖题与 M59 后续轮切片已共同进入真实链路；Firecrawl 0。
- 2026-07-18：Deep `aa1923fd-45cc-44f4-9a7c-edd6565b1994` 已永久封存为 `blocked/budget_exhausted`：35 usable、2 projects、4/6、22 次正文分析、170 Trace、Firecrawl 0、`research_sources=[]`。缺 `program_insertion` 与 `daylight_transformation`，两题各累计 7 passes；round 2–7 始终逐题轮转，未再发生前序缺失题连续消费 3 次而饿死后题，M59 Live 调度验收通过。
- 2026-07-18：该 Run 的唯一综合在 4/6 形状仍生成 `deterministic_fallback`，精确满足 Deep 2/2/1/2/2；与 `7b69...` 的 4 cases/仅 1 个比较分支不同，本轮已积累足够同题比较证据。当前主阻断已收敛为 program/daylight 的公开检索召回或正文资格，而非综合、parser reserve、coverage-first 或分析切片；不得再开 Live 探路。
- 2026-07-18：`aa1923...` 两个首轮 relevance-2/enriched-2 强页经持久字段审计均被正确拒绝：Emerson 只有“机构建筑改造”条件、没有功能植入机制/转译；De Nederlandsche Bank 明确没有天窗、高侧窗、庭院、光井等采光操作。失败的 Public Library in Labin 与 Non-Commercial Cultural Hub 又经 Direct Playwright 零 Provider scratch 读取为 4,797/4,524 字，前者相关内容仅分类/标题，后者没有 daylight/light/skylight/courtyard 等机制句；允许失败页重试只会重复付费，不应实现。
- 2026-07-18：两缺失题的 7 轮域序列固定为 ArchDaily EN、ArchDaily CN、Designboom、Dezeen、Divisare、ArchDaily EN、ArchDaily CN。两题在 CN/Divisare 合计出现 5 个零结果轮次，program 的第二次 ArchDaily EN 又发生 `TargetClosedError`；真正可提供长正文的 ArchDaily EN 机会过少。下一步先用生产短查询 scratch 验证“零结果时单次回退 ArchDaily EN”能否召回更强文字页，再决定红测，不直接改域表。
- 2026-07-18：两组零 Provider 查询 A/B 否决了搜索层猜测：ArchDaily EN fallback 复现首轮同一弱页，精简通用尾词也没有改善 daylight，program 只换入同样不匹配的 Fieldhouse。相反，Direct Playwright 读取模型打 0/1 的两张 Designboom 页得到 10,654/10,431 字强正文：工业遗产文化中心明确写出 minimum interventions、夹层植入和公共/办公分层；港口博物馆明确写出 courtyards and roof openings bring daylight into interior volumes。
- 2026-07-18：正文提示已明确部分机制可准入，`PublicPageAnalysis` schema 也不会因低 relevance 清空字段；真实执行缺口在 `_persist_public_page_analysis()` 先于逐字证据校验直接 `relevance < 2 -> return 0`。这会让“完整 context/mechanism/transfer + 合法 excerpt、但模型标量 relevance 自相矛盾为 0/1”的强链永久丢失。M60 只能在完整链通过现有逐字校验时把有效 relevance 设为最低 2；缺字段或 excerpt 仍拒绝，不能普遍抬分。
- 2026-07-18：七条当日 Deep 的 SQLite 只读时间审计推翻了“浏览器抓取困难”的假设：总计 202.8 分钟中，按相邻 Trace 时间戳计算的正文分析槽为 167.9 分钟（82.8%），公开搜索 17.4 分钟、页面抓取/展开约 7.7 分钟、综合 5.1 分钟。172 次正文分析中，48 次 relevance 0/1、24 次失败、5 次高分但未增加证据，合计 77 次/74.6 分钟没有持久化增益；Provider 对瞬时错误会在同一分析内再请求一次精简正文，执行层 worst-case 为 90 秒。固定域轮换中 ArchDaily CN 31 次为 30 次零结果加 1 次错误，Divisare 2/2 零结果。最新 Run 的两次图片批次增加 5 个资产但 coverage 保持 4/6。真正浪费来自过重的单篇文章准入、模型标量否决、隐藏的同页二次请求、固定失败来源和整条 Run 的全覆盖阻断。
- 2026-07-18：现有数据结构只能部分表达多来源：`EvidenceClaim` 自带 `source_url`，但 `_persist_public_page_analysis()` 只查找同一 `source.url` 的候选，`article_ready` 又要求证据 URL 等于候选主 URL，项目覆盖仅按 `project_name` 原字符串分组。因此“找到合适案例后联网补文字”不能只加一个搜索词；必须形成有边界的项目证据聚合，并显式确认补充页属于同一项目。
- 2026-07-18：用户批准新的全局准入规则。事实完整性是 claim-level 硬门；案例是 project-level 证据包。主来源已确认项目身份且至少提供一个具体机制时，才允许一次项目名/事务所/缺失主题定向补查，最多读取两个可信文字页；每条事实保留各自 URL 和逐字 excerpt，transfer 明确为研究分析，relevance 只排序。补查失败保留部分证据与原站链接；只要存在有用引用答案，局部分支缺案例不得把整条 Run 标为 blocked。图片在文字 coverage 之后才可选执行。
- 2026-07-18：M61 已把上述准入规则落为执行合同。部分 context/mechanism claim 可独立持久化并保留各自 URL/逐字 excerpt；同项目通过严格标准化标题合并，异项目保持隔离。每项目只允许一次定向补查且执行层硬截断两页，补页可跨子问题复用并与主来源组成项目文字包；article-ready、coverage 与 synthesis 均能消费同一项目的多来源证据。逐字证据完整时低 relevance 只影响排序，transfer 仍明确为 ArchResearch 分析。
- 2026-07-18：M61 同时收紧了成本与终态：正文分析超时不再对同页立即发起第二次模型请求，worst-case 从 90 秒降到 45 秒；有正式引用但局部未覆盖的 precedent Run 进入 `partial`，必需浏览器检视失败仍为 `blocked`；文字 coverage 未齐前远程图片批次为 0。8 项目标行为、完整 browser-inspection、27 项 Provider、相邻 workflow/results/schema、Ruff、strict Mypy 与 `git diff --check` 均通过。
- 2026-07-18：M61 完整门禁的 PowerShell 5 运行暴露了验证脚本兼容性异常：`$PSNativeCommandUseErrorActionPreference` 未能让中间根 `pnpm run check` 失败传到脚本退出码，故最终“全部通过”本身不可信。其余门禁已明确通过 301 API、165 Extension、8 packaged E2E，Board 单独为 76/76；随后用 bundled pnpm 和显式 native exit 守卫重跑根 check，Board 76/76、Extension 165/165、两端 lint/typecheck/build 全部 exit 0。M61 离线实现因此可判定完成，但未来不能在 Windows PowerShell 5 下只信 `scripts/verify.ps1` 的末行。
- 2026-07-19：M61 后唯一 Live Deep `0715ab7e-...` 已真实验证单页单请求合同。首个 program 查询中 Emerson 与 Public Library in Labin 分别产生一条 `public_page_analysis` `APITimeoutError`，`retry_count=0`，workflow 随即前进到后续来源；没有旧版同页第二请求。随后至少 4 次正文分析完成，当前 attempt 0、Firecrawl 0，尚未形成 coverage checkpoint。
- 2026-07-19：同一 Live Deep 的终态项目包校正了中途指标解释：De Nederlandsche Bank 在 `sectional_layers`/`daylight_strategy` 的模型原始 `relevance=1` 且事件 `enriched=2`，实际表示两个同项目资产各写入了部分 context，不表示形成完整 context/mechanism/transfer 链；终态对应分支均为 `mechanism=false/transfer=0`。Emerson relevance 1 仍为 enriched 0。M61 Live 证明了低分部分 claim 可保留，但尚未证明该 Run 通过多来源补成正式链；不得把 enriched 资产数当作 coverage。
- 2026-07-19：M61 Live Deep 首个 gap-check 为 38 usable、5 projects、4/6；覆盖 old-new/program-insertion/public-logistics/sectional，缺 program-sequence/daylight，两题各 7 passes。13 次正文分析 completed、11 次单次 failed，Firecrawl 0；文字 coverage 未齐时 remote image batch 仍为 0。M61 已阻止旧版无效图片消费，但本次检索/项目补证仍未把两条缺口补成正式链。
- 2026-07-19：终态合并审计确认项目补查并非完全未触发。Instituto 的 `public_logistics` 部分 context 后 sequence 21 执行了一次补查，但本地浏览搜索返回通用 `Error`；当前失败 Trace 未保留 `purpose`。更关键的确定性缺陷位于 `_try_project_text_supplement()`：它按 rank 选择同项目第一张资产后立即判断分支。封存 Run 中 De Nederlandsche Bank、GATE M、Shitang 分别有 7/3/10 张候选，第一张均无正文分析，而后续同项目资产已有完整其他机制或缺失题部分 context，因此补查被错误跳过。所有终态 evidence claim URL 仍为单来源。
- 2026-07-19：综合失败不是应放宽的第二缺陷。唯一 synthesis 为 `InternalServerError`；生产 cases 零写入重建为 7，只有 public-logistics 具备两个不同项目，Deep deterministic fallback 所需的至少两组同题双项目比较不满足，`fallback_available=false` 正确。M62 只修项目级补查资格聚合，不降低 2/2/1/2/2 或证据绑定门。
- 2026-07-19：M62 最小实现不再选同项目第一张资产，而是汇总该项目全部 verified/partial candidates：当前分支若已完整则不补查；当前分支有部分 context/mechanism，或项目任一已验证分支已有具体 mechanism 时，才允许既有的一次补查。新红绿 fixture 用第一张空资产、第二张完整 program-insertion 机制、当前 program-sequence 缺失的形状，补查从 0 变为 1，并把主/补来源的逐字 claim 分别绑定两个 URL。搜索次数、两页上限、错误项目隔离和综合门均未改变。
- 2026-07-19：M62 最终离线基线通过 302 API、完整 browser-inspection、Provider 27/27、Ruff、48 文件 format、strict Mypy 18 source files、Board 76/76、Extension 165/165、两端 production build、8 packaged E2E 与 30/108 evaluation。一次 PowerShell 7 全仓命令在并发 Board 测试无诊断退出 1；独立 Board 76/76 后只补齐中断的 build/E2E 尾部并全部通过。该现象与 M61 相同，属于并发资源抖动，不应靠重复全仓门禁掩盖；验证脚本此次正确返回非零。
- 2026-07-19：M62 Live Deep `eea9cf24-58d1-451d-828a-ec8aa7706a59` 已永久封存为 `partial/no_new_assets`：24 usable、6 projects、4/6，缺 `sectional_sequence` 与 `public_interface`，两题各 7 passes；23 次正文分析为 12 completed / 11 failed，缺失两题分别只有 1 completed/3 failed 与 1 completed/4 failed，且两题 enriched 均为 0。Firecrawl 0、视觉/图片批次 0。唯一 synthesis 遇 `APIConnectionError`，M54 fallback 成功生成 Deep 2/2/1/2/2，因此综合不是本次覆盖失败主因。
- 2026-07-19：M62 补查资格修复已真实穿过 Live 链路：`project_text_supplement` 在 sequence 8/48/60/70/167 共执行 5 次，但结果总数为 0。零 Provider 查询重建显示每次搜索都把唯一项目名与 `architecture case study` 及 5–6 个焦点词一次性送入四站点 RSS；`public_interface` 题未命中现有五类意图，退化为 `design mechanism adaptive reuse`。M63 只收紧查询形状为唯一项目名加少量分支焦点并补齐公共界面焦点，不增加搜索次数、两页上限、域范围、Provider retry 或证据门。
- 2026-07-19：M63 最小实现将 sectional 补查缩短为 `"project" section mezzanine`，公共界面补查缩短为 `"project" public interface shared lobby`；其他既有分支同样只保留 2–3 个焦点词。M61/M62 同项目合并、异项目隔离、一次两页补查、多候选资格和 partial/无图合同保持。完整仓库门禁全绿为 303 API、76 Board、165 Extension、8 packaged E2E、Ruff/48 文件 format、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation；M63 implementation complete，未调用真实来源或创建第二条 Run。
- 2026-07-19：M63 Live Deep `c553f2be-52fe-436e-8891-0d4fadcad5e0` 已永久封存为 `partial/research_synthesis_incomplete`：46 usable、5 projects、4/6，缺 program-insertion 与 old-new-structure，两题各 7 passes；26 次正文分析 14 completed / 12 failed，Firecrawl 0、视觉/图片批次 0。4 次项目补查仍全部 result_count 0；唯一 synthesis 为 `APIConnectionError`。生产 cases 零写入重建为 6，只有四个已覆盖分支且 fallback unavailable，综合门保持正确。
- 2026-07-19：M63 查询词已缩短，但零 Provider 查询重建暴露搜索名污染：四个相关 `source_pages.title` 分别以 `RENOVATION`、`PUBLIC SPACE`、`CULTURAL CENTER`、`NANJING` 开头，而同 URL 的 candidate project_name 均为干净项目名。当前补查优先采用 `page.title`，缓存 case reuse 也重新用 `page.title` 构造 source。M64 只让直接页优先 `source.title`、缓存复用优先 `case.project_name`，不改变身份键、调用次数、域、两页上限或证据门。
- 2026-07-19：M64 最终规则不使用脆弱的前缀黑名单。候选仅在 clean source identity 或同页 parsed-title identity 任一严格匹配时参与；首次搜索只用 clean source title，两种 identity 同时登记到一次 attempted/pages 缓存，后续 cached case 使用 `case.project_name` 并复用同一 supporting bundle。带 `RENOVATION` 的直接/完整 workflow 红绿测试均通过，搜索仍为每项目一次、最多两页。最终全仓门禁全绿为 303 API、76 Board、165 Extension、8 packaged E2E、Ruff/48 文件 format、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation。
- 2026-07-19：M64 Live 的真实检索调用链纠正了最初的多域假设：`allowed_domains=[]` 时 precedent research 每轮只轮换一个可信站点，因此走单站 `_compact_site_query()`。生产公共查询把英文 typology/intent 与中文原始子问题拼在一起，而压缩器只要见到任意中文就选择中文输出，导致 Designboom、Dezeen、Divisare 也收到 `工业改造 社区文化中心 公众后勤流线`。这与两个 relevance 0 的英文站结果及跨轮同词重复一致；M65 应按目标站点语言处理混合查询，而非修改多域 RSS 或 M64 精确项目补查。
- 2026-07-19：M65 红测证明旧实现对 Designboom、Dezeen 的混合 flow 查询均输出中文；ArchDaily China 的中文输出和多域 `GATE M West Bund Dream Center + section mezzanine` 项目补查保持正确。最小实现只在查询同时含中英文且目标是已知单站时使用目标站点语言：ArchDaily China 为中文，其余可信站为英文；纯中文/纯英文行为及多域路径不变。
- 2026-07-19：M65 完整离线门禁全绿：307 API、76 Board、165 Extension、8 packaged E2E、Ruff/48 文件 format、strict Mypy 18 source files、两端 lint/typecheck/build 与 30/108 evaluation。最终只读守卫确认 M64 Live 仍为 `partial/research_synthesis_incomplete` attempt 0、active Run 0、staged 0；运行服务仍是 M65 修改前加载的 M64 进程，因此任何后续 Live 验收必须先重载 M65。
- 2026-07-19：唯一 M65 Live Deep `76f52c79-5fd9-4738-837d-5576eb3a72cd` 在 attempt 0 完成 `completed/completion_satisfied`：51 usable、9 formal projects、6/6、gaps 0，六题 passes 为 3/4/4/3/3/4。21 条 QueryAttempt 全部 completed；37 次正文分析为 29 completed / 8 failed；Firecrawl、视觉与图片批次均为 0。M65 的目标站点语言修复穿过真实链路，最终不再缺 flow 分支。
- 2026-07-19：M65 Live 的唯一 run-level synthesis 遇 `APIConnectionError`，M54 未重试 Provider，而是从 14 个证据完备 cases 生成明确标注的 deterministic fallback：精确 2 causal / 2 comparisons / 1 evidence-boundary conflict / 2 applicability boundaries / 2 recommendations。Run 因此诚实完成而非掩盖错误。Quick M51、Balanced `d995bed5-...` 与该 Deep 共同完成 M53 当前规则三档验收；早期所有 partial/blocked Deep 继续永久封存。
- 2026-07-19：M66 启动时重新核对发现，M53 接受的 Quick、Balanced、Deep 虽然 subquestion 数和综合强度不同，但都来自同一个“旧建筑更新中如何植入功能并组织流线/剖面”的问题家族；现有 Live 证据不能外推为任意建筑研究问题都能跑通。应使用多类问题的离线确定性矩阵覆盖三种深度，避免再连续创建真实 Run。
- 2026-07-19：用户提供的 Board 截图与只读 DOM 一致：当前工作区下拉选中 `M6 真实研究验证`，但主工作区加载的是一条 32 张参考、4 个子问题、正文案例 0 的旧未完成 Run；刚完成的 M65 Deep `76f52c79-...` 没有成为当前记录。下一步核对工作区 ID、API 返回顺序以及 Board 的 recent-run/restore 选择逻辑，不能把问题归因于用户查找方式。
- 2026-07-19：API/前端联合审计确认 Run 未丢失。`M6 真实研究验证` 工作区的最新记录确实是 7/17 的旧 Balanced；M65 Deep `76f52c79-...` 位于第三个工作区 `作品集实测 · 旧厂房社区文化中心`，且该工作区 API 列表中排第 1。Board 的 `recentRuns` 明确只调用当前 `activeWorkspaceId` 的 `/workspaces/{id}/runs`，并在列表中显示前三条，因此当前截图符合实现，但跨工作区最新 Run 缺乏可发现性。先切换到正确工作区验证记录可见，再决定是否需要产品级全局最近任务入口。
- 2026-07-19：只读 Board 切换验证成功：选择 `作品集实测 · 旧厂房社区文化中心` 后，最新卡片立即显示“设计策略 · 深入 · 51 张参考 · 7/19 · 研究已完成”；打开后页面顶部显示完成状态、51 张参考和 6 个子问题。Run 记录本身完整可访问，当前用户页面已停在该 Deep 结果。
- 2026-07-19：最新 Deep 的 Board 详情同时暴露口径不一致：coverage 报告/Run 终态认定 9 个 formal projects、6/6，但 API 的 51 个结果 `result_tier` 全为 partial，Board 因而显示“0 个已完成正文分析的项目案例”。这会让成功 Run 看起来没有正式案例；M66 后续需区分是 workflow 落库 tier 未提升还是 Board 只按 tier 过滤过严，不能用完成状态掩盖展示缺陷。
- 2026-07-19：M66 多问题三档确定性矩阵最终沿完整 text-first 路径通过：可信 ArchDaily 数字项目页、`Project / Studio` 项目身份、页面正文、逐字 excerpts、branch context/mechanism/transfer 和 synthesis 均使用生产门。社区图书馆采光、山地校园高差/无障碍、滨水工业遗址防洪更新三个问题族分别在 Quick 3/3、Balanced 4/4、Deep 6/6 完成，共 9/9 `completed/completion_satisfied`；Quick 综合 1/0/0/0/1，Balanced/Deep 均 2/2/1/2/2。该矩阵证明离线编排与证据门可跨问题工作，但不等同于九次真实 Provider 成功率保证。
- 2026-07-19：Board 的 0-case 根因不是 `result_tier=partial`，而是 `toWorkResult.analysisReady` 额外要求 `observations.length`。M48 后 precedent 正文案例不需要图片观察；真实 M65 Deep 的 context/mechanism/transfer/逐字 claims 完整但 observations 为空，因而被 UI 隐藏。红测只清空 observations 即复现；最小修复移除该图片观察门，仍严格要求中文 context、中文 mechanism、非空 transfer 和至少一条 text excerpt。
- 2026-07-19：重载后真实 M65 Deep 的正文案例已从 0 恢复可见，但 Board 顶部写成“14 个已完成正文分析的项目案例”，与终态 coverage 的 9 个 formal projects 不一致。页面按 6 个子问题归组，同一项目可跨题重复出现；14 是可展示的正文案例关联条目数，不能冒充唯一项目数。最终统计必须按项目身份去重为 9，同时保留各子问题内的案例归组。
- 2026-07-19：用户明确三档共同语义：Quick、Balanced、Deep 都不应死磕单一页面，正式研究单位是项目案例；同项目允许由多个可信正文来源补齐条件或机制，但事实继续逐条绑定来源 URL 与逐字 excerpt。M66 收尾必须验证该 project-level multi-source 合同覆盖三档，而非只在 Deep fixture 中成立。
- 2026-07-19：现有同项目双来源完整 workflow 测试原先固定为 Quick；参数化为 Quick/Balanced/Deep 后 3/3 直接通过，分别形成 3/3、4/4、6/6 coverage。主 ArchDaily 页提供 context、补充 Designboom 页提供 mechanism/transfer，最终仍是一个 `Foundry Arts Center` 案例，claim URL 集合精确保留两个来源。生产 `_try_public_page_branch_analysis()` 对三档共用同一 `_try_project_text_supplement()` 路径，没有 mode 特判。
- 2026-07-19：最终真实 Board 复核显示“9 个已完成正文分析的项目案例按子问题归组 · 37 张网页图片仅作待核验预览”，与 M65 coverage 的 9 formal projects 一致。页面实际保留 13 个按子问题呈现的 project dossier；底层 14 条 analysis-ready assets 在同题同项目内继续合并，因此唯一项目摘要、分题关系和资产证据三种数量不再混称。
- 2026-07-19：用户截图暴露成品顶栏的信息架构泄漏：`M6 真实研究验证` 是内部里程碑/验收命名，却作为当前工作区主标签直接展示；相邻 `新建` 未说明对象，和选择器并排后更像后台管理控件。按 Board 的 product register，这属于功能性 polish：工作区应使用用户项目语义，动作应明确为“新建工作区”；不能只改圆角或颜色掩盖命名问题。
- 2026-07-19：代码与 API 联合检查确认 `M6 真实研究验证` / `M33 完整性验收` 不在前端 fixture 或硬编码中，而是 SQLite 持久化工作区名；API 已有 `PATCH /workspaces/{id}`，Board client/UI 却只暴露 list/create，没有 rename。`新建` 则直接硬编码在顶栏。稳定修复应补就地重命名能力、把动作改为“新建工作区”，并通过现有 PATCH 迁移本机已知内部验收名称；不得用前端字符串遮罩欺骗数据层。
- 2026-07-19：UTF-8 清单确认三条内部数据分别为 `M6 真实研究验证`（38 Runs）、`M33 完整性验收`（3 Runs）和 `作品集实测 · 旧厂房社区文化中心`（19 Runs）。按 simplicity 复核后不新增工作区管理/重命名弹窗：产品已允许创建时命名，现有 API 足以一次性清理本机内部数据。前端最小长期修复是明确“新建工作区/创建工作区”，并持久化上次选择，避免刷新总回到 API created_at 升序中的最老工作区。目标名称为 `旧厂房更新 · 历史研究`、`旧工业建筑 · 结构与功能植入`、`旧厂房社区文化中心`。
- 2026-07-19：M67 本地数据迁移在 active Run 0、三条 ID/旧名称精确匹配后执行；60 条历史 Run 数量保持。三条名称现为 `旧厂房更新 · 历史研究`、`旧工业建筑 · 结构与功能植入`、`旧厂房社区文化中心`。迁移只 PATCH `name`，没有修改 workspace ID、brief、Run、证据或终态。
- 2026-07-19：真实 Board 重载后的 DOM 已显示三条项目语义名称与明确的“新建工作区”，`M6`、`M33`、`真实研究验证`、`完整性验收`、`作品集实测` 均不再出现在选择器。该浏览器此前无选择键，因此首次仍按兼容 fallback 选中第一条 `旧厂房更新 · 历史研究`；下一步通过正常下拉选择 `旧厂房社区文化中心`，再刷新验证持久化。
- 2026-07-19：通过真实原生工作区选择器切换到 `旧厂房社区文化中心` 后，DOM 精确标记第三项 selected，最近研究第一条为 M65 Deep“深入 · 51 张参考 · 7/19 · 研究已完成”。这证明新名称没有破坏工作区 ID、历史 Run 映射或最近任务加载；下一步刷新同一页面验证 selection persistence。
- 2026-07-19：同一真实 tab 刷新后，在最近任务仍处于加载态时，工作区选择器已直接恢复 `旧厂房社区文化中心` selected；没有先显示或回退到历史工作区。持久化选择的到达时序因此通过真实页面验证，不只依赖单测。
- 2026-07-19：390×844 viewport 的无截图 DOM QA 通过：浏览内容宽与 `documentElement/body.scrollWidth` 均为 375px，无页面级横向溢出；工作区 select 为 233.6×44px，“新建工作区”按钮为 101.6×44px，二者同一行且满足 44px 触控目标。selected workspace 仍是 `旧厂房社区文化中心`。
- 2026-07-19：用户进一步否决把“当前研究项目”做成与“最近研究”相似的独立板块。两者语义是父子关系：项目决定加载哪组历史 Run，最近研究是该项目内的具体任务。M68 因此把既有选择/新建控件压入首页“最近研究”标题行作为轻量上下文，不新增同权卡片；全局顶栏和结果页均不展示项目管理控件。
- 2026-07-19：用户提供的局部图中，工作区 select 与“新建工作区”占据 sticky `.app-header` 的独立主区，视觉上像全局导航功能；源码也确认 `.workspace-switcher` 与品牌、当前任务动作是同级兄弟。首页已有 `.recent-panel` 标题与说明，最小且清晰的插入点就是该标题 header 的右侧工具位，复用同一 DOM/状态而非复制第二套控件。
- 2026-07-19：现有首页是 starter/recent 两列，`.recent-panel` 本身不是卡片而是带顶部分隔线的列表区，适合把项目选择作为标题行右侧工具；无需新增容器。创建表单已相对 `.workspace-switcher` 绝对定位，可随 DOM 一起移动；窄屏现有 fixed `top:112px` 是顶栏两行布局遗留，M68 需改为相对首页控件的安全定位，并保持 44px 触控目标。
- 2026-07-19：PRODUCT 要求首页只保留问题入口与任务级历史摘要，最近研究保持 Flat；M68 与此一致。DESIGN 中“顶栏含工作区”的旧规则已被用户的成品反馈明确推翻，后续需同步该一条规范。对外文案采用“研究项目 / 新建项目”，内部 Workspace schema、API 和 storage key 不改，以便让项目容器与下方研究记录的父子关系更直观。
- 2026-07-19：项目创建浮层移到仅首页挂载后，进入结果时还需关闭 `workspaceCreateOpen`，否则返回首页会恢复一个此前不可见的旧浮层状态。所有进入结果的路径最终通过 `composerOpen=false`，但最小安全点是在 `openRun` 与成功提交/重跑路径关闭创建态；不改变结果加载或 Run 状态。
- 2026-07-19：`>1380px` 时最近研究列只有最小 360px，标题、长项目名和新建按钮不能可靠挤在同一水平线；因此控件作为同一 `<header>` 的第二行工具条，比强行同排更稳。`≤860px` 首页已单列，工具条可占满宽度；旧 `≤620px` fixed/top=112 创建浮层依赖顶栏位置，必须撤销为对项目工具条的绝对定位。
- 2026-07-19：M68 源码残留扫描在 Board/App/DESIGN 中已找不到“新建工作区 / 创建工作区 / 工作区名称”或旧的 accessible name `工作区`；内部变量、API 路径和测试 fixture 名称仍按 Workspace 保留。focused diff 显示本轮只触及 App DOM/两处结果进入状态、相关测试、workspace CSS 选择器和 DESIGN 的两条壳层规范。
- 2026-07-19：完整 App 回归的 3 个失败均是旧测试从 active result 直接操作全局 workspace selector；新 DOM 正确不存在该控件。仍可达的首页竞态覆盖包括 POST 未返回时切项目、POST 失败后切项目、terminal hydration 未完成时切项目；active result 下 poll/cancel 尚未完成时切项目已因入口移除而不可达，不应为测试恢复顶栏控件。
- 2026-07-19：无截图真实桌面 DOM QA 已通过：首页 `.app-header` 不含 select 或“新建项目”，只显示品牌和“查看上次结果”；`.recent-panel` 同时包含“最近研究”、说明、研究项目 select、新建项目和 3 条历史记录。当前 selected 为 `旧厂房社区文化中心`，viewport/scrollWidth 同为 1265px，无横向溢出；桌面控件高 34px，移动端仍待 44px 验证。
- 2026-07-19：从真实首页唯一“查看上次结果”进入 M65 Deep 后，新 DOM 顶部只有品牌与“发起新研究”；整个结果快照没有“研究项目”或“新建项目”，但结果自身的“图纸类型”筛选仍正常存在。该边界证明隐藏的是项目管理而非误删结果工具，且没有创建/重试任何 Run。
- 2026-07-19：390×844 无截图 DOM 几何通过：浏览器内容宽、`documentElement.scrollWidth`、`body.scrollWidth` 均为 375px；项目工具条位于最近研究内且不在顶栏，select 为 253.6×44px，新建项目为 89.6×44px，同排、均未越界。selected 项目仍为 `旧厂房社区文化中心`。
- 2026-07-19：390px 下打开“新建项目”只展开最近研究内的项目创建浮层，不提交请求；浮层宽 351.2px、左右均在 375px 内容区内，项目名 input 与禁用的“创建项目”按钮均高 44px，页面 scrollWidth 仍为 375px。旧 fixed/top=112 顶栏依赖已清除。
- 2026-07-19：取消创建并 reset viewport 后，真实页面回到默认首页，项目创建浮层消失、第三个项目与 3 条最近任务保持，浏览器 error log 为 0。浏览器 QA 到此完成，不再需要视觉采集或交互。
- 2026-07-19：M68 最终完整门禁一次通过：310 API、80 Board、165 Extension、8 packaged E2E，以及 Ruff/48-file format、strict Mypy、两端 lint/typecheck/build 和 30/108 evaluation。只读 API 守卫确认 3 个项目、60 条历史 Run、active 0；M65 Deep 仍 completed/attempt 0，封存 M61 Deep `0715ab7e-...` 仍 partial/attempt 0。
- 2026-07-19：用户截图确认 M69 的核心不是整体页面太宽，而是 dossier 内 `.dossier-analysis-grid` 的 2×2 网格按行共享高度：长“怎么转译”把同一行的短“适用边界”撑出大块空白，项目预览又另起整行，阅读节奏变成四个等权分析格。用户同时要求每个子问题在案例之后有问题级总结，避免结果退化为逐项目分析合集。
- 2026-07-19：Impeccable product/layout 规则与截图诊断一致：高密度研究工具不应把四种长度差异巨大的文本强制放进同尺寸网格；应让相关内容紧密分组、各列按自身内容自然结束，并用区段级留白而非等高单元格表达层级。M69 不以缩小字号或简单减 padding 掩盖结构问题。
- 2026-07-19：CollectUI 根地址的独立 HEAD 检查返回 HTTP 500，和 in-app Browser 导航超时互相印证；当前不能把该站不可访问误判成页面加载慢，也不能连续重试。按浏览器规则下一步只允许一次聚焦索引搜索寻找具体可读条目，失败则以用户截图、现有产品数据和可验证的通用详情模式完成布局，而不伪造 CollectUI 观察。
- 2026-07-19：唯一 Google 索引查询 `site:collectui.com case study dashboard details UI` 正常打开，但普通结果区没有任何 CollectUI 条目，说明当前既无法直读站点，也没有可点击的文本详情缓存。保留同一查询的“图片”结果作为最后一个可见索引面；不改写关键词继续搜索。
- 2026-07-19：M69 复核 ArchResearch 的 PRODUCT/DESIGN 与 Impeccable product/layout 规范后，确定案例档案必须继续是平面结构区段：条件/机制属于来源支持层，转译/边界属于研究判断层，应以自然高度的紧密分组和分隔线建立层级，禁止嵌套卡片、四格等高墙或靠缩小字号掩盖空白。题目级“问题小结”应位于该子问题全部案例之后，用现有正文案例归纳跨案例判断；无正文案例时不生成。
- 2026-07-19：CollectUI 的最后一个可见索引面（同一 Google 查询的“图片”入口）在导航/DOM 返回前超时并重置浏览连接，没有取得可核验标题、缩略图或来源。因此 M69 不声称看过 CollectUI 内容，也不再重试或改写搜索；外部研究封存为不可用，布局判断只绑定用户截图、现有真实 dossier DOM 和项目 DESIGN/PRODUCT 规则。
- 2026-07-19：Board 源码确认空白的精确边界：`App.tsx` 把项目条件、空间机制、怎么转译、适用边界作为 `.dossier-analysis-grid` 的四个同级 section；CSS 宽屏四列、≤1180px 两列，并用同一 grid row 对齐，因此 2×2 时长转译必然撑高短边界。M69 最小结构改为两条互不共享内部行高的内容列：条件→机制、转译→边界；移动端按同一 DOM 顺序折叠。题目小结的数据只取每个 dossier 已通过正文门的 `designMechanism`、`transferStrategy`、`limitation`，明确排除 `observation` 和预览图。
- 2026-07-19：M69 两条行为红测有效锁定目标：完成 Run 的 dossier 仍命中旧四格 class；同一子问题放入两个不同正文项目后，章节能正确渲染两份 scoped analysis，但不存在题目级 summary region。fixture 已证明机制、转译、边界和图片观察可被分别断言，生产实现无需扩展 API。
- 2026-07-19：M69 focused 结构/小结/空案例测试 3/3 green。完整 Board 首轮为 81 pass / 1 fail，唯一失败是既有跨题关联测试在整个 chapter 用单数 `getByText` 读取机制；现在该机制按设计同时出现在 dossier 与其后的题目小结。生产的 scoped analysis 没有串题，旧断言应缩到对应 dossier，不应删掉 summary 内容来迎合单数查询。
- 2026-07-19：旧跨题关联测试收窄到对应 dossier 后，完整 Board 82/82 通过；ESLint、TypeScript typecheck 与 Vite production build 也全部 exit 0。问题小结作为 dossier list 的直接后继保留，最多选择 3 条去重机制、4 个转译步骤和 3 条边界，保持正文原句顺序且不读取 observation；无案例与待归组章节不生成。
- 2026-07-19：localhost 已在重新连接的 in-app Browser 中明确 reload 到 M69；首页 DOM 正常，当前项目仍为 `旧厂房社区文化中心`，研究表单和最近研究父级结构未回退。浏览器共有两个既有 localhost tab，M69 只认领最近使用的一页，最终将清理重复页并保留单一成品页。
- 2026-07-19：已通过首页唯一“查看上次结果”打开真实 M65 Deep，确认服务已加载 M69：仍是 completed/51 张可用/9 个唯一项目/6 个子问题，并在每个已读章节的 dossier list 后出现 `问题小结`，内容直接复用该题正文机制、转译步骤和适用边界。首题小结没有采用其项目预览中的“当前记录未保留中文图面观察”，证明图片观察未混入归纳。
- 2026-07-19：真实 M65 Deep 的 1265px 聚合几何验证通过：document scrollWidth=viewportWidth=1265；旧 `.dossier-analysis-grid` 数量 0；首份 dossier 两条独立列高度约 255.4px / 540.2px，短列不再随长转译列共享内部行高；有 dossier 的 chapter 6 个、问题小结 6 个，且每个 summary 都是 `.dossier-list` 的直接后继。
- 2026-07-19：390×844 首轮无截图几何同样无溢出（document/body scrollWidth=viewportWidth=375），分析列按 274.6px + 675.4px 顺序折叠，summary 全部在视口内且仍为 6 个。但首题单项目 summary 高约 906.6px，原因不是 CSS 空白，而是 `flatMap` 先拿同一 dossier 的前四个长转译步骤，未形成跨案例抽样。问题小结应改为“每个项目最多一个首要转译动作，整体最多三条”，避免重新复制案例全文。
- 2026-07-19：双项目 summary 新增简洁性断言后按预期 red：旧输出包含第一个项目的第二个转译步骤。该失败证明问题在聚合取样而非样式；生产只需把 flatMap 全步骤改为每个 dossier 的首个非空 transfer，既保留跨项目覆盖，又让完整步骤继续只存在于案例正文。
- 2026-07-19：summary 投影收紧后 focused 与完整 Board 均通过（82/82），lint/typecheck/build 再次全绿。真实 M65 Deep 的 390px 复核中首题 summary 从约 906.6px 降到 529.8px（约减少 42%），首题只保留 1 个项目的 1 个首要转译动作，第二步不再重复；6 个 summary 仍全部存在，分析列仍顺序折叠，document/body 仍无横向溢出。
- 2026-07-19：Impeccable detector 对最终 `App.tsx` / `styles.css` 返回空数组，未发现嵌套卡片、同尺寸卡墙、侧色条、任意层级等规则问题。focused diff 中 M69 只新增 summary helper/DOM/测试、替换 dossier analysis 结构和对应 CSS；其余大量 diff 为用户既有 M35–M68 工作树，保持不动。
- 2026-07-19：首次完整 `scripts/verify.ps1` 运行可见通过 dev-common 与 Provider configuration security contract，随后监控会话在返回最终 output/exit code 前关闭；底层进程已不存在，app terminal 也无附着记录，因此该轮结果不能计为通过或失败。下一轮保持同一离线门禁但改用长 outer yield + 50 秒 bounded poll 捕获最终退出码。
- 2026-07-19：改用长 outer yield + bounded poll 后，M69 最终完整 `scripts/verify.ps1` 明确 exit 0：310 API、82 Board、165 Extension、8 packaged E2E，Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、dev-common/Provider security/process lifecycle 与 30/108 evaluation 全绿。全程无 Provider、Firecrawl、Live Run 或截图调用。
- 2026-07-19：首轮最终 Git/API 合并守卫中，`git diff --check` exit 0、staged 0 与完整 dirty status 均有效；PowerShell workspace-runs 枚举在某个 GET 返回 404，未据此推断 active 数。下一步只读取得 `/v1/workspaces` 原始 JSON，再改用 Node 标准 JSON 显式枚举，避免重复旧的 PowerShell 展平/路径错误。
- 2026-07-19：最终 Node 标准 JSON 守卫有效枚举 3 个项目与 60 条历史 Run，active 为空；M65 Deep `76f52c79-...` 保持 `completed/attempt 0/completion_satisfied`，封存 Deep `0715ab7e-...` 保持 `partial/attempt 0/research_synthesis_incomplete`。结合 staged 0、`git diff --check` exit 0，M69 可标记 complete。
- 2026-07-19：用户对 M69 真实页面复核后指出“还是很空”。截图证明剩余问题有两层：两条独立分析列虽然不再共享内部行高，但外层仍由右侧长 transfer 列决定总高，左列下方形成大片空面；单项项目预览仍放在三列 gallery 的第一格，右侧约三分之二为空。下一轮必须改结构，不再调整 padding/字号。
- 2026-07-19：原始 1449×1129 截图进一步确认：分析区左半在短“空间机制”结束后一直空到右侧第 5 条 transfer 与边界结束；项目预览卡约 444px 宽，父区约 1400px，右侧两格完全空置。源码对应 `.dossier-analysis` 固定两列与 `.dossier-gallery` 固定三列。最小正确拓扑是四行全宽 ledger（标签轨 + 正文）以及 `data-layout=single` 的左图右文 evidence sheet；2–3 项仍保留普通网格。
- 2026-07-19：M70 三条行为红测准确命中旧结构：analysis 仍存在 `.dossier-analysis-column`，单项 gallery 没有 `data-layout=single`，同项目双资产 gallery 也没有 `data-layout=grid`。案例聚合仍为一个 dossier，证明只需修改展示拓扑，不改数据、API 或 summary。
- 2026-07-19：M70 最小 JSX/CSS 已把四段 analysis 展平为 112px 标签轨 + 72ch 正文的四个自然高度行，并给 gallery 确定性标记 `single/grid`。桌面 single evidence sheet 使用左图右文、全宽 footer；≤860px 恢复图上文下。focused 结构/单图/多图测试 3/3 green。
- 2026-07-19：用户第二张真实页面截图指出全宽 ledger 右侧仍像“养鱼”。根因不是 grid，而是沿用 `max-width: 72ch`：CSS `ch` 按西文数字宽度计算，在 12px 中文中正文只到约页面中线。宽屏技术清单应取消该窄栏上限，并让 transfer 的有序列表按 DOM 顺序使用两栏连续流；≤860px 必须回到单栏。不能用装饰或伪信息填空。
- 2026-07-19：transfer wide-flow 行为红测按预期失败，第三行尚无 `.dossier-analysis-transfer`。该 class 只作为语义化布局钩子，不改变数据或文案；CSS multicol 将保持有序列表 DOM/读屏顺序，并以 `break-inside: avoid` 避免单条步骤跨栏。
- 2026-07-19：M70 wide-flow 实现后，focused 3/3、完整 Board 83/83、lint、typecheck、production build 全绿，Impeccable detector 0 findings。分析 p/ol 已不再受 72ch 限制；只有 transfer ol 在 >860px 使用两栏，单条步骤禁止跨栏，≤860px 恢复一栏。
- 2026-07-19：真实 M65 当前滚动位置的 1235px desktop 聚合几何证明“鱼塘”已消失：analysis 总宽 1146px，transfer 正文从 x204.6 延伸到 x1166.6、宽 962px，右侧只剩 24px padding；5 条步骤以 3+2 分布在 x222.6 / x718.6 两栏。首个 single gallery 与 sheet 同宽 1098px，左图 466px、右文 630.5px，确为横向全宽而非三列第一格；页面无横向溢出。
- 2026-07-19：390×844 无截图几何也通过：document/body scrollWidth=viewportWidth=375；transfer `column-count=1`，五条步骤同一 x=46.8，行 grid 为单列 317.6px；single gallery/sheet 同宽 317.6px，图片 316×210.9 在上、caption 316×138 在下；6 个问题小结保持。响应式没有用横向布局挤窄移动端。
- 2026-07-19：M70 最终完整 `scripts/verify.ps1` 明确 exit 0：310 API、83 Board、165 Extension、8 packaged E2E，Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、process/security contracts 与 30/108 evaluation 全绿。没有 Provider、Firecrawl、Live Run 或截图调用。
- 2026-07-19：M70 最终只读守卫确认 3 项目 / 60 Runs / active 0；M65 Deep 仍 `completed/attempt 0/completion_satisfied`，封存 `0715ab7e-...` 仍 `partial/attempt 0/research_synthesis_incomplete`；`git diff --check` exit 0、staged 0。M70 可标记 complete。
- 2026-07-19：M71 启动为只读认知负担审计，不修改产品代码。审计覆盖首页、运行/恢复状态、完整结果页、来源/Trace/方法/导出工具、空错状态和 390px，重点核验术语门槛、重复结论、选择数量、信息层级和下一步可理解性。Product Design audit 因其强制截图流程与项目禁截图约束冲突而不采用；使用 planning-with-files 与 Impeccable critique，后者要求两个相互隔离的 Assessment。
- 2026-07-19：恢复时误读了不存在的 `C:/Users/76384/.codex/skills/impeccable/critique.md`，命令只返回路径错误、未改项目状态；已定位并完整读取正确的 `reference/critique.md`，不得重复错误路径。
- 2026-07-19：M71 双 Assessment 结论：视觉系统本身没有典型 AI 模板感，主要问题是内容组织像模型中间产物。真实 M65 Deep 有 6 个子问题、51 条结果、13 个项目；综合标题 207 字，单项目最多 16 个预览、17 条转译和 18 条边界，两条落地建议各重复 4 次。结果页形成“研究越完整越难读”的反向质量曲线。
- 2026-07-19：M71 Nielsen 23/40，认知 checklist 失败 7/8。四个 P1 依次为：综合标题不可直接理解、证据库存默认全展开、五项后处理工具位于答案之前、深度/证据术语要求用户理解系统架构；P2 为来源检视器重复 dossier 分析、真正原文证据靠后。首页核心输入、partial/blocked 的结果保留说明、证据与权利分离仍是明确优点。
- 2026-07-19：Impeccable detector 对 `App.tsx` 与 `apps/board/src` 两次均 exit 0 / 0 findings，证明规则性 anti-slop 未命中，但无法覆盖语义重复和决策密度。两个 Assessment 的 Browser runtime 均返回 `No browser is available`，因此没有新 tab、实时 DOM、390px 实测、mutation/injection、overlay 或临时 detector server；报告明确使用源码/CSS/只读 Run 数据 fallback。
- 2026-07-19：等待 sub-agent 时数次误调用 `functions.wait` 并传入不存在的 `noop` cell，均立即返回 `exec cell noop not found`，未运行命令或改项目状态；后续改用 agent mailbox/final result，不得重复该无效等待方式。
- 2026-07-19：用户批准按 M71 优先级逐项修复。M72 只处理第一项“结果顶部不可直接理解”，不同时重排工具区、不折叠案例库存、不做全局术语改写。目标用户是正在等待研究结论的建筑学生/青年设计师；其首要问题是“研究发现什么、现在先做什么”，而不是 fallback 来源或系统如何综合。
- 2026-07-19：现有 Board 在 `App.tsx:2114` 无条件把 `activeSynthesis.answer.statement` 作为 `<h2>`，正常简短结论可读，但真实 M65 fallback 的 207 字 `【本地证据汇总】；项目...` 也被原样提升为主标题。现有测试只覆盖简短正常 statement，尚无 fallback/UI 可读性边界。M72 应在 Board 展示投影层修复，不改 API synthesis、证据 ID 或已封存 Run 数据。
- 2026-07-19：API fallback 已自带 `generation_mode: deterministic_fallback`，其 `answer` 是项目名+机制的汇总串，`causal_chains` 则稳定含“条件：…；机制：…；转译：…”，`recommendations` 含“转译步骤（项目）：…”。因此 Board 可无新推理地把首个 evidence-bound `机制` 投影成可读标题，把 recommendation 去掉系统标签后投影成动作；原始 answer 和所有证据 ID 保持不变并下沉到折叠详情。
- 2026-07-19：M72 展示策略确定：简短正常 answer 不动；`generation_mode` 为 fallback、`【…】` 前缀或异常超长的 machine-shaped answer 才启用概览投影。标题限制为单条简洁判断；动作按清理后文本去重并最多显示三条；若标题被投影，折叠区必须保留“完整综合原文”和本地整理身份。TypeScript contract 只补 API 已存在的可选 `generation_mode`，不改后端 schema/data。
- 2026-07-19：M72 新行为测试在旧实现上按预期 red：84 项 Board 中仅该项失败，DOM 仍以完整 fallback 串为 heading，并显示 4 条带项目包装的 recommendation。首次命令多传一个 `--`，Vitest 因此跑了全套而不是只过滤；失败证据仍有效，后续使用 `pnpm --filter @archresearch/board test src/App.test.tsx -t ...` 精确执行，不重复错误命令。
- 2026-07-19：最小实现新增纯展示 helper：从 fallback causal chain 的既有“机制”字段提炼标题，异常超长内容以 84 字为上限；recommendation 只删除确定性“转译步骤（项目）”包装、去重并取前三；完整 answer 进入折叠的“完整综合原文”。正常短 synthesis 不走投影。focused fallback test 已 1/1 green。
- 2026-07-19：M72 正常简短 synthesis 回归与 fallback focused 各自 1/1 green，完整 Board 84/84 green。新 helper 只做纯函数展示投影，原 `activeSynthesis`、证据数量、causal/comparison/conflict/boundary 数组和封存数据均未改；标题投影后 audit summary 改为“查看完整综合、比较与适用边界”。
- 2026-07-19：M72 Board lint、TypeScript typecheck、Vite production build 全绿，Impeccable detector 对 `App.tsx` 为 0 findings。in-app Browser 本轮恢复可用；只读打开真实 M65 Deep 后，标题由 207 字 fallback 串变为 54 字 evidence-bound 机制，fallback marker 0、heading overflow false，次级说明为“已核验正文的本地整理 · 1 条正文证据直接支撑”，立即动作 2 条且均已去掉项目包装，完整原文仍在 closed audit details。
- 2026-07-19：M72 390×844 无截图 DOM 几何通过：实际 clientWidth/scrollWidth 均 375，无页面级横向溢出；综合区宽 351.2px、标题高 134.4px且无自身 overflow，primary grid 单列 351.2px。viewport 已 reset。该验证只读切换到既有第三项目并打开既有 completed Run，未 POST、未创建/retry Run。
- 2026-07-19：M72 最终完整 `scripts/verify.ps1` exit 0：310 API、84 Board、165 Extension、8 packaged E2E，Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。最终只读守卫为 3 projects / 60 Runs / active 0，M65 completed/attempt 0，封存 `0715ab7e-...` partial/attempt 0；`git diff --check` exit 0、staged 0。
- 2026-07-19：用户启动 M73，范围严格为 M71 P1 #2 的证据库存渐进披露，不移动顶部工具、不改三档深度或证据术语。核心用户任务是先判断本题案例给出什么方法，再决定是否查完整档案；默认不应要求读完全部项目条件、五步转译、预览和来源。
- 2026-07-19：M73 预定合同：有正文案例的章节先显示现有 evidence-bound 问题小结，再按既有 dossier 顺序显示最多两个代表案例摘要，每个只含项目名、机制、首个转译和首个边界；全部完整 dossiers（包括代表案例）统一进入一个原生 `<details>`。展开后不得丢项目、顺序、预览、逐字证据或来源；无 dossier 分支保持原空状态，不生成假摘要。
- 2026-07-19：`caseGroups` 的 dossier 顺序由 API `results`/rank 顺序第一次出现的项目决定，同项目资产随后合并，因此 `dossiers.slice(0, 2)` 可作为不新增评分的确定性代表案例投影。现有 questionSummary 同样按该顺序从全部 dossier 提取最多三条机制/动作/边界，但默认九项仍与代表摘要重复；M73 将其默认层收敛为首条机制判断，其余方法完整保留在代表摘要与展开档案。
- 2026-07-19：现有完整 dossier JSX 是单一连续映射，包含四行分析、全部预览、来源和操作；最小改法是原样移入一个 native `<details>`，不拆数据、不复制 full dossier。默认层新增平面 ledger 式代表案例，不做嵌套卡片：项目身份 + 机制 + 首要动作 + 边界横向排布，≤860px 单列。既有详细 dossier 测试需先展开再断言，不能用 hidden 查询冒充用户可见性。
- 2026-07-19：M73 实现已落地并经续接复核：问题小结位于章节标题后且只保留首条正文机制，前两个既有 rank dossier 投影为代表案例，全部原 dossier 原样保留在 native closed `<details>`。JSDOM/Testing Library 仍能用 role 查询命中 closed details 内的 article，因此“默认折叠”必须用 `toBeVisible()` 验证可见性，不能断言节点不在 DOM；展开后再验证完整顺序与来源。
- 2026-07-19：完整 Board 首轮的 22 个失败已离线归为三类，而非 22 个独立产品缺陷：`findByText(项目名)` 因代表标题与完整 dossier 标题同名而歧义；详细图片/来源/操作断言没有先展开 inventory；原 M69 小结测试仍要求默认重复全部机制、动作、边界并位于 dossier 后，与 M73 的“判断在前、只保留首条机制”合同相冲突。修复应集中在测试 helper、可见代表标题和显式展开，不改生产结构。
- 2026-07-19：Board 测试可用一个最小 `openCaseInventories(user)` helper 模拟真实披露操作：结果加载后遍历当前 closed `details.case-inventory` 并点击各自 summary。仅等待完成/轮询的测试不应展开，可改为等待可见的 `代表案例 <项目>` article；需要图片、来源钉、收藏、对照、导出或档案可见性的测试才调用 helper，避免把所有回归都耦合到完整库存。
- 2026-07-19：真实 M65 completed 结果加载 M73 后的无截图 DOM 证据：6 个案例章节全部以问题小结紧跟章节标题，代表案例数依次为 `[1,2,2,1,2,2]`，6 个 native inventory 全部 closed；完整 dossier/source 数依次保留 `[1,2,3,1,2,4]`。当前自然 viewport 796px 下 document width 与 viewport 同为 796，无页面横向溢出。
- 2026-07-19：真实 M65 第三题的 3-project inventory 经唯一 summary 点击后 `open=true`，完整项目顺序保持 De Nederlandsche Bank → GATE M West Bund Dream Center → rooftop garden house；三条“打开项目来源”分别仍指向原 ArchDaily、ArchDaily、Designboom URL。首次用不存在的 `.dossier-footer` 范围读取返回 0，仅是 QA selector 错误；改为 inventory 内 bounded `a[href]` 后三条来源完整，无产品缺陷。
- 2026-07-19：M73 响应式无截图几何通过。1440 override 的实际 clientWidth 1425、scrollWidth 1425，代表条为约 285/1019px 两段，方法为三列；390 override 的实际 clientWidth/scrollWidth/bodyScrollWidth 均 375，代表条与方法都折成 351.2px 单列。两档均保持 6/6 小结紧随标题、代表数 `[1,2,2,1,2,2]`、6/6 inventory closed。
- 2026-07-19：M73 最终基线为 310 API / 85 Board / 165 Extension / 8 packaged E2E 全绿；只读持久状态仍为 3 projects / 60 Runs / active 0，M65 `76f52c79-...` completed/attempt 0，封存 Deep `0715ab7e-...` partial/attempt 0。全树 whitespace exit 0、staged 0，既有 dirty/untracked 资产保持。
- 2026-07-19：用户明确“继续”后启动 M74，只处理 M71 P1 #3 的 tool-first ordering。目标用户进入结果页时先读任务、研究结论、问题拆解和案例证据，再在内容之后使用研究过程、方法对照、表达规范、个人导出和分享导出；五项能力、前置条件与交互不删不改，深度/证据术语留给独立后续项。
- 2026-07-19：M71 原报告还建议“未选图纸时隐藏 disabled 工具矩阵并在首次选中后出现上下文条”，但获批 M74 验收明确要求五项工具、禁用前置条件和交互保持。按 surgical scope，本轮只改变结果正文与 `.result-workbench` 的 DOM/阅读顺序，不新增选择触发状态、不重构工具矩阵；这已直接解决“用户看答案前先理解五项后处理”的根因。
- 2026-07-19：源码确认 `.result-workbench` 与 `.results-section[aria-label=研究结果]` 是同层、同条件的相邻 sibling，workbench 当前正好排在 results-section 前。最小实现是把整段 workbench JSX 原样移到 results-section 之后；所有 state/handler/dialog/export 仍在同一 App scope。样式只需把现有 bottom separation 改为 content-to-tool 的 top separation，无需包装或新组件。
- 2026-07-19：`results-section` 结束后还有一个仅在图纸筛选为 0 时出现的 `.empty-filter`，再后面才是选图后出现的 comparison dock 与各 overlay。workbench 的正确稳定插入点是 empty-filter 之后、comparison dock 之前：任务/答案/案例和筛选反馈都先读，工具仍早于其 dialogs，选图后 dock 逻辑不受影响。
- 2026-07-19：真实 M65 M74 几何验证：1440 override 实际 1425px，section order 为 `[results-section, result-workbench]`、间隔 48px、workbench 两列约 379/948px；390 override 实际 375px，顺序不变、间隔 32px、workbench/actions 均单列 349.6px，document/body scrollWidth 均等于 viewport。两档都有且只有 5 个工具，初始 disabled 仍准确为对照、个人导出、分享导出。
- 2026-07-19：M74 最终基线为 310 API / 85 Board / 165 Extension / 8 packaged E2E 全绿；持久状态仍为 3 projects / 60 Runs / active 0，M65 completed/attempt 0，封存 Deep partial/attempt 0。全树 whitespace exit 0、staged 0，既有 dirty/untracked 资产保持。
- 2026-07-19：M75 对外术语审计确认四类首次接触问题：三档的“核心判断/因果推演/综合论证”描述了系统产物而不是用户收获；“正文案例/部分核验/问题匹配 3/4/精确裁图”都要求用户先理解实现术语。最小展示映射定为：三档共享“都以多个项目案例为依据”，用户收获分别为“快速找方向/形成方案依据/做跨案例论证”；结果统一说“案例证据”并说明已核对项目原文；`verified/partial/visual_lead` 展示为“原文依据完整/原文依据待补充/只作视觉参考”；数值 relevance 确定性映射为自然语言相关度；“精确裁图”改为“读取当前页面高清图纸”。内部 Quick/Balanced/Deep、evidence tier、relevance 数值和 Chrome 协议保持。
- 2026-07-19：对 API tier 语义复核后，M75 最终的用户标签收紧为“来源与内容已核对/已有依据，仍需核对/只作视觉参考”，避免把 `partial` 错说成原文缺失或把权利状态与证据状态混合。relevance 4/3/2/1/0 仅展示为“直接支撑本题/与本题高度相关/可补充本题/关联较弱/无直接关系”，排序和准入阈值不变。
- 2026-07-19：真实页无截图 QA 确认 M75 不增加新的认知块：桌面 1425px 下三档各约 416px 且等高 92px；390 override 的实际 375px 下三档均为 305.6px 单列，子文本 `scrollWidth <= clientWidth`，高清图纸读取按钮宽 149.6px 无溢出。真实 M65 结果页在 1425/375px 下 document/body 均与 viewport 同宽，10 个默认代表案例无溢出；唯一 `scrollWidth` 告警来自有意隐藏的 `.visually-hidden`。Browser error log 0，视口已 reset，既有 M65 结果页已保留。
- 2026-07-19：M75 最终术语残留审计通过。`apps/board/src` 的生产 UI 已不再显示“正文案例 / 部分核验 / 问题匹配 N/4 / 精确裁图 / 核心判断 / 因果推演 / 综合论证”等旧首见用语；仅 `DESIGN.md` 的禁用示例和 `App.test.tsx` 的负断言保留旧词，二者都不是对外展示。三档 POST 仍发送 `quick/balanced/deep`，`verified/partial/visual_lead` 与 0–4 relevance 仍是内部数据；PRODUCT/DESIGN/demo-flows/failure-cases/architecture 与当前展示映射一致。
- 2026-07-19：M75 最终 Run/Git 守卫确认 3 projects / 60 Runs / active 0；M65 `76f52c79-5fd9-4738-837d-5576eb3a72cd` 仍为 `completed/attempt 0/completion_satisfied`，封存 Deep `0715ab7e-6580-42ae-9d84-77a0c772c760` 仍为 `partial/attempt 0/research_synthesis_incomplete`。staged 为 0，全树 whitespace exit 0，所有既有 dirty/untracked 资产保持；M75 可标记 complete。
- 2026-07-20：M76 的产品边界确定为两条同时成立的路径：`precedent_research` 可在项目级正文案例之外带回小红书制图灵感，但小红书始终是 `aggregator/visual_lead`，不得证明项目事实、进入 dossier 或计入正式项目数；`visual_reference_search` 则以小红书视觉内容为主路径，严格检查与子问题/图纸意图的视觉相关性。两者共用“每子问题一篇高相关笔记、轮播最多四图、四图一次批量分类、原笔记来源、权利状态独立”合同。
- 2026-07-20：只读 API 历史审计确认现有 60 条 Run 全部是 `precedent_research`，`visual_reference_search` 为 0，因此用户所说“图纸灵感对应的小红书还没跑过”为真。同时，建筑案例路径历史上已有多条 `research_sources=["xiaohongshu"]`，Quick `42668844-497e-453f-bcd4-38c9f25cbb25` 曾在 attempt 0 completed，所以不能误说成 precedent + XHS 从未打通；它缺的是 M61–M65 项目级规则后的当前版本联合验收。
- 2026-07-20：`_coverage()` 存在阻断独立图纸灵感终态的全局缺陷：非 precedent 的 `coverage_assets` 正确取 relevance ≥2 且有本地/图像内容的 `usable`，但 `subquestion_asset_ids` 仍从 `evidence_backed` 填充，后者只包含 `verified/partial`。小红书必须保持 `visual_lead`，因此 `visual_reference_search` 即使每题都有高相关图，`covered_subquestions` 仍永远为 0，只能 partial。正确修复应仅让非 precedent 从已有 `coverage_assets` 的 `subquestion_ids` 计算视觉覆盖，不得改动 precedent 正文证据门、tier 或权利状态。
- 2026-07-20：XHS 每题一笔记边界在双浏览器后端时尚未完整闭合。OpenCLI 路径会正确只下载排序后第一篇笔记并将 URL 加入 `inspected_urls`，但同次搜索的其他 XHS source 仍继续进入通用 `can_inspect` 分支；如果 ArchResearch 扩展同时连接，它们可被再次裁图并持久化。正确全局合同是每子问题只允许排序后首篇 XHS 笔记进入任何图像检视路径；OpenCLI 失败/空结果时仍保留既有扩展搜索回退。
- 2026-07-20：M76 Live 预检的本地服务面正常：3 projects / 60 Runs / active 0，API health `ok`，Provider 为梭子蟹 API / `gpt-5.6-sol`，Board HTTP 200。`pnpm opencli -- doctor` 确认 daemon 1.8.6 在 19825 端口运行，但 Browser Bridge extension 未连接，connectivity 失败。因此当前不得创建 XHS Live Run；先区分 Chrome 未运行、扩展未安装或 daemon/扩展断线。
- 2026-07-20：Browser Bridge 阻断已进一步收敛。当前 `chrome/msedge` 进程为 0；Chrome Default profile 已安装且启用 OpenCLI extension `ildkmabpimmkaediidaifkhjpohdnifk` v1.0.22，`disable_reasons=[]`，同 profile 也安装当前 ArchResearch unpacked extension。因此 doctor 失败的最小原因是 Chrome 未运行，不是扩展缺失或被禁用；下一步只启动现有 Default profile 的空白页并复核 doctor。
- 2026-07-20：使用已解析的 `C:\Program Files\Google\Chrome\Application\chrome.exe` 隐藏启动 Default profile 后，OpenCLI doctor 全绿：daemon 1.8.6、extension 1.0.22、profile `2btxrgkn` connected，connectivity 0.2s。未重启 daemon、未重装/修改扩展。下一步仅用 limit=1 的读操作验证小红书登录态和结构化搜索，避免为登录问题浪费 Run。
- 2026-07-20：唯一 limit=1 OpenCLI XHS 搜索预检成功，返回 1 篇真实笔记，title 为“6⃣步搞定灰红配色线稿风爆炸图”，host 为 `www.xiaohongshu.com`，path 为 `/search_result/...`；预检输出未保留或显示 query token。这证明当前 Default profile 的登录态、Bridge 和 OpenCLI 结构化解析可用，允许进入服务重载前守卫。
- 2026-07-20：M76 Live `e525ca77-...` 首个正确 SSE 快照为 `inspecting`、55 Trace；三个视觉子问题已达 3/3 coverage、11 usable，恰好出现 3 次 `xiaohongshu_search` 与 3 次 `xiaohongshu_assets`。但 gap-check 同时报告 `verified_or_partial=0` 和 enrichment gap `insufficient_verified_or_partial`，随后进入第二轮通用网页搜索/检视；视觉目标的合格资产本应保持 `visual_lead`，因此这可能是 coverage 修复后仍残留的目标不匹配 enrichment 调度缺口。先让唯一 attempt 0 自行终止并合并审计，不创建下一条 Run。
- 2026-07-20：M76 Live `e525ca77-...` 已自行终止 `completed`，但当前版本成品验收不通过。小红书主链本身合格：三个子问题各对应一个唯一 `/search_result/...` 笔记，分别保留 4/4/3 张本地资产，11/11 均为 `aggregator / visual_lead / rights unknown`，Trace 中敏感 token 标记为 0。覆盖完成后的错误 enrichment 又为 `axon-style` 加入 Wikipedia 与学校站各一张 `unknown / visual_lead / rights unknown` 资产，使最终 13 张不再全部来自批准的小红书边界。该 completed Run 保留原样且不 retry；先离线修复视觉目标仍追逐 `verified/partial` enrichment 的调度缺口，完整预检前不创建下一条 Live Run。
- 2026-07-20：调度缺口可在现有 `_coverage()` 局部修复，无需 GitHub 研究或新架构。视觉目标仍应如实报告 `verified_or_partial=0`，但 enrichment 的第三个数量目标必须用其高相关 usable visual assets，而不是 precedent 专属的 verified/partial 层级；这样第三题完成后 `_enrichment_satisfied()` 可立即退出，precedent、source lookup、来源层级、权利状态和一笔记边界均不变。红测将把 M76 fixture 扩为两轮并要求只执行首轮 3 次 Provider 查询、`coverage_satisfied` 和空 enrichment gaps。
- 2026-07-20：视觉三档的默认问题数为 Quick 3、Balanced 4、Deep 6；每题一篇最多四图分别消耗 12/16/24 次视觉调用，均在各档 12/36/72 上限内。M76 回归改为三档参数矩阵，并用确定性不同感知哈希的本地图样满足各档 4/6/9 usable 目标；每档都要求首轮末题后停止，不能以单一 Quick 问题特判。
- 2026-07-20：按用户要求启动聚焦 GitHub 对比，当前主样本为 26.9k-star Apache-2.0 `jackwener/OpenCLI` 和 14.7k-star `xpzouying/xiaohongshu-mcp`，边界样本为两个单笔记图片下载器。初步共同模式是把“搜索列表”与“选定笔记的详情/图片”分成独立步骤；下载器只处理当前单篇笔记。ArchResearch 不采纳发布、评论、养号、账号矩阵或整篇无限下载能力；候选适配是：视觉目标一旦为某子问题选中首篇 XHS 笔记，就完成该题的来源选择，不再并行混入通用网页候选，并继续保留最多四图和一次批分类。
- 2026-07-20：GitHub 具体实现对比完成。Apache-2.0 `jackwener/OpenCLI` 的 `search` 只输出按 rank 截断的笔记列表并保留 signed URL，`download` 必须接收一个明确 note URL，只从当前 note ID 的结构化 `imageList` 按轮播顺序取图并在单笔记内去重；`xpzouying/xiaohongshu-mcp` 同样把 `/feeds/search` 与 `/feeds/detail` 分开，详情必须使用搜索返回的 `feed_id + xsec_token`，图片属于详情 `imageList`。`w3nry/xhs-image-downloader-edge` 有“当前 note root”作用域但默认可选整篇所有图；`codyshen0000/xhs-image-downloader` 会跨很多笔记滚动凑图片数量，明确不适合本产品。后两者未检测到许可证且不会复制代码；只采纳主样本共同的控制流：ranked search → explicit selected note → current-note images → bounded batch。
- 2026-07-20：修复后第二条 Live `36889c78-...` 已封存 `blocked/no_usable_assets`：3 次 XHS search 只有 1 次 `xiaohongshu_assets`，0 usable、0/3，6 次通用 `local_browser_search`，Trace token marker 0。首轮通用网页检视与 XHS 共用 Quick 12-call 视觉预算，先消耗了后续两题的笔记批处理机会；这证明“仅修 coverage 后第二轮停止”不够。离线合同应在某题 XHS 搜索返回候选后立即关闭同题 public/model search，只让显式选中的首篇笔记进入最多四图批次；只有 XHS 搜索整体为空/失败时才保留原有后备路径。
- 2026-07-20：用户明确取消此前把“一题一篇”和只读产品边界作为优化目标的要求，唯一目标改为提升性能。M76 将性能定义为研究成功率与结果相关性优先、耗时和调用成本其次；安全协议、凭据保护和禁社交动作仍受仓库硬约束。候选策略从“首篇固定”升级为“首篇快路径 + 同次 ranked results 内的有界 note fallback”，只有首篇无 usable asset 才继续下一篇，覆盖后立即停止；不会通过连续 Live Run 调参。
- 2026-07-20：性能方案选择保留现有 OpenCLI 而不新增 XHS MCP 服务。OpenCLI 已提供同等关键能力且已在本地验证；新增 Go/浏览器进程会增加启动、状态与部署开销，没有可验证的成功率或延迟收益。采纳的组合是：XHS 成功时独占本题检视；同次 search 的 top-2 notes 按 rank 依次下载/分类；首篇有 usable 立即停止，首篇 0 usable 才试第二篇；两篇都无 usable 后才结束该题。Quick 最坏 3×2×4=24 图，Balanced 4×2×4=32 仍低于 36，Deep 6×2×4=48 仍低于 72。
- 2026-07-20：用户进一步明确旧 UI 虽已有“视觉参考”小按钮，但它仍嵌在建筑研究方式中并继承三档，不算独立功能。新信息架构用同一任务启动器顶部的双选分段入口区分“建筑设计研究 / 图纸灵感”：建筑设计保留三档和项目案例语义；图纸灵感隐藏全部深度控件、提交固定内部 Quick 兼容值、历史列表不显示深度。来源反查保留为建筑研究的辅助动作，不成为第三个同级主入口。
- 2026-07-20：用户截图中的眼睛图标“视觉参考”确认是 M76 改版前的小入口，不是应与“图纸灵感”并存的第三项。当前两个 localhost 页面均只显示“建筑设计研究 / 图纸灵感”，旧“视觉参考”按钮为 0；结果中的“只作视觉参考”仅表示 `visual_lead` 证据用途，不能作为功能入口。后续保持一个独立灵感入口，不新增同义入口。
- 2026-07-20：M76 最终离线基线确认双主入口、建筑设计独有三档、图纸灵感固定 24 calls / 48 MiB、XHS ranked top-2、首篇成功即停、首篇 0 usable 才回退、XHS 候选存在时不并行同题通用 Provider。两条既有 Live 只证明修复前缺陷形状，均保持 attempt 0 且绝不 retry；最终性能版本未经新的 Live 验收，不得自行重载后创建 Run。
- 2026-07-20：M76 post-fix Live `f5be3f17-2698-4aa6-8f03-155a04e4c5a2` 验证首篇成功快路径：attempt 0 completed/coverage_satisfied，5 usable、3/3、3 个唯一 XHS note path、每题各一次 search/assets；5/5 均为 `aggregator/visual_lead/unknown rights` 且 relevance 2–4。三次同题通用 `openai` 搜索均明确 `skipped`，reason 为 `selected_xiaohongshu_note`，local-browser tools 0、Trace token marker 0。真实链未触发第二篇，因此 top-2 fallback 仍以三档和扩展详情离线红绿为验收证据，不为强行命中 fallback 再开 Run。
- 2026-07-20：用户截图确认 M77 的根因不是单句文案，而是视觉目标沿用了 precedent 的信息架构。真实用户通常只会问“找剖面图的风格”或“效果图怎么出”，此时系统应提供可比较的表达方向，例如线稿/拼贴/材质渲染或叙事氛围，而不是把一句需求扩成长篇项目条件并显示“问题拆解、读取项目原文”。正确边界是 visual goal 使用“灵感方向 → 不同风格类型 → 图片观察”，precedent goal 继续使用“问题拆解 → 项目案例 → 原文证据”。
- 2026-07-20：M77 源码审计确认双层根因。`OpenAIResearchProvider.plan()` 对 visual 只要求 `distinct visible-reference issues`，通用提示仍叫 architecture research subquestions；Mock visual 默认项也按可见特征/构图/图纸语言等分析维度拆分，不是互斥风格类型。Board 又无条件显示“Agent 会把问题拆开并读取项目原文”“问题拆解”“N 个子问题”和“0 个方案项目”，运行中还写“证据问题”。现有 DOM/CSS 可复用，正确修复是 goal-conditional semantics，不新增布局或卡片。
- 2026-07-20：M77 首轮实现后的残留审计发现运行状态详情仍无条件使用 precedent 的“读取项目/分析正文”，`partialDiagnosis()` 也无条件输出“子问题、项目原文、方案项目”。visual mental model 必须覆盖 active/completed/partial 三态；应复用同一 goal 条件切换，未归组 visual rationale 同步改为“灵感方向”。案例档案、legacy analysis 和案例证据区只服务非视觉结果，不需要重构。
- 2026-07-20：旧 M76 图纸灵感 Run 已持久化 precedent 形状的长问句，不能靠 M77 新 planner 自动改写。兼容策略只在 Board 展示层识别问句形状，并按该分支实际资产类型投影为“轴测图表达参考”或“平面图 / 分析图表达参考”；数据库、subquestion ID、资产关联与新 M77 短风格标签全部保持。该策略比显示虚假风格或迁移历史数据更符合可审计边界。
- 2026-07-20：用户用真实截图否定了上一条兼容投影：把旧分支改成“轴测图/平面图/剖面图表达参考”仍是按图纸类型拆分，不是按风格找灵感。新的全局规则是“类型是约束，风格是方向”：用户点名轴测图时，所有方向都必须是轴测图，只比较线稿、拼贴、材质、图解等表达；只有未点名图纸类型时才可选择适合的类型。旧数据没有足够依据重建风格，必须明确显示为旧版分组，不能伪造当前方向。
- 2026-07-20：M77 最终合同已由行为和全仓门禁共同验证。OpenAI 若在“轴测图”请求下返回其他图纸类型，展示方向会按同一序位回退为精细线稿/拼贴叙事/材质渲染轴测图；符合指定类型的模型方向保持。旧问句 Run 只改显示为旧版分组，资产关联仍按原 subquestion ID。最终持久状态仍为 3 projects / 63 Runs / active 0，证明离线实现没有写入历史研究。
- 2026-07-20：用户最新截图显示三个“旧版灵感分组”，证明 Board 已加载 M77 兼容展示，也说明旧 M76 Run 没有可恢复的风格方向数据。旧记录不应继续优化或改写；唯一能验证新体验的方式是重载 M77 后，用自然的单图纸类型问题创建一条全新 Run，并直接检查持久化 plan 是否同类型、不同风格。
- 2026-07-20：M78 Live planner 已真实通过最关键合同。自然问题“我想出一张轴测图，帮我找风格”持久化为 `精细线稿轴测图 / 拼贴叙事轴测图 / 材质渲染轴测图`，rationale 分别聚焦线宽留白、色块材质、光影纹理；三个 id 虽由模型自由命名，但标题全部固定轴测图且风格互异，后端没有生成平面、剖面、功能或流线分支。
- 2026-07-20：M78 唯一 Live 暴露了 planner 之后的第二层类型准入缺口。Run 完成 10 usable / 3/3，其中 8 张分类为 axonometric；拼贴方向选中的同一篇 XHS 笔记还保留了 1 张 analysis_diagram 和 1 张 site_plan（relevance 4/3），虽然该笔记另有一张真正的拼贴轴测图并足以覆盖方向。根因不是搜索或 plan，而是单笔记批分类后仍按一般视觉 relevance 保留同批所有 usable，没有把用户明确的轴测类型作为逐图准入门。正确修复应只过滤明确指定类型的 visual task，不能改变未指定类型、precedent、XHS top-2 或同笔记早停。
- 2026-07-20：M78 最终类型门放在远程四图批分类之后、持久化之前。每个 visual direction 从自己的短标题解析明确图纸类型；有明确类型时只保留对应 `ArchitectureAssetType`，被拒候选的本地文件即时删除，Trace 分别记录 raw candidate、accepted type 和 mismatch 数。未指定类型时不设门，仍允许系统探索多类图纸。这样一篇笔记只要有至少一张正确类型仍走首篇成功快路径，只有 0 张正确类型才尝试 ranked 第二篇。
- 2026-07-20：用户明确批准处理 M71 延后的来源检视器 P2。M79 范围只包含信息层级去重：逐字、URL 绑定的来源证据成为检视器主内容，案例条件/机制/转译/边界不再在抽屉内重复占据主阅读路径；底层证据数据、来源入口、provenance、rights、完整项目档案和结果页顺序保持。
- 2026-07-20：M79 源码审计确认 P2 是确定的 DOM 顺序问题：检视器右栏先重复项目条件、空间机制、完整转译、图面观察、方法推断，再把逐字 `evidenceClaims.text_excerpt` 放进后置的“来源与核验”；适用边界又在末尾重复一次。检视器的唯一主任务应是核对“这条判断由原文哪句话支持”，因此项目条件/机制/转译/边界继续只由完整 dossier 承载，抽屉主栏先展示 URL 绑定的逐字证据与核验元数据；图面观察/方法推断是否保留需按其是否已在单图证据条重复继续核对。
- 2026-07-20：单图证据条已经显示本分支的 `observation`，完整 dossier 已显示项目条件、机制、转译和边界；检视器重复展示这些字段没有独立任务价值。M79 的最小结构确定为：项目身份后立即进入“逐字原文证据”，每条保留 statement、verbatim excerpt、PDF 页码和 URL；随后常显“核验与权利”四项元数据，再保留收藏/拒绝和备注。`fact` 只在无 `evidenceClaims` 的旧记录中作为摘要 fallback，避免与正式 claim 重复。
- 2026-07-20：`selectedAnalysis` 只为旧检视器重复区块计算，移除这些区块后可连同该派生值删除；`selectedSubquestionId` 仍用于高亮当前证据条，不受影响。旧 `.inspector-analysis-summary/.inspector-highlight/.inspector-*-context` 样式没有其他调用方，可随结构一并清理；`evidence-locator` 与 `evidence-matrix` 继续复用现有设计 token。
- 2026-07-20：M79 新结构已通过 focused green。抽屉标题改为“核对原文证据”；右栏项目身份后依次为逐字 claim（statement → excerpt → 页码/定位链接）与常显核验/权利矩阵。旧案例分析派生值和专用 CSS 已删除，claim 行改为平面分隔而非卡片堆叠；收藏、拒绝、备注和左侧原始来源入口保持。
- 2026-07-20：M79 完整 Board 93/93、lint、typecheck、production build 均通过，Impeccable detector 对 `App.tsx/styles.css` 为 0 findings。完整回归只发现并迁移一条已被新职责明确取代的旧 inspector-as-dossier 断言；跨子问题机制仍在原 dossier 中验证，未丢失研究分析。
- 2026-07-20：文档审计发现 `DESIGN.md` 的 Evidence drawer 仍停留在 M43 前 500px/五类分析并列合同，与当前 1040px 双区及 M79 职责冲突；需同步为“左侧项目预览与原站入口、右侧逐字证据优先、核验/权利常显、案例分析不重复”。来源检视器只从 article-ready dossier 证据条进入，现行正式路径已有 `evidenceClaims` 准入，因此不为不可达的零-claim 状态增加专用 UI 分支。
- 2026-07-20：PRODUCT/DESIGN 已同步 M79，旧 inspector 结构与样式引用审计为 0。最终局部门禁再次通过 Board 93/93、lint、typecheck 和 production build；CSS 产物从 69.73 kB 收敛到 69.55 kB，JS 从 330.80 kB 收敛到 330.53 kB，符合去重目标且没有新增组件或依赖。
- 2026-07-20：M79 最终完整 `scripts/verify.ps1` 明确 exit 0：319 API、93 Board、165 Extension、8 packaged E2E，Ruff/48-file format、strict Mypy 18 source、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。来源检视器 P2 已完成，M71 审计中四项 P1 与该 P2 均已关闭；没有 Provider、Firecrawl、Live Run 或截图调用。
- 2026-07-20：用户明确批准 M80 无截图体验收口。范围仅为既有 M65 结果的来源检视器真实 DOM、响应式与键盘验收；禁止创建/retry Run、Provider/Firecrawl、截图和 Git 写操作。若真实页发现缺陷，必须先记录证据并转为离线行为红测，不能在页面里试错修改。
- 2026-07-20：M80 本地服务启动边界已核对。`scripts/start.ps1` 只以隐藏进程启动当前工作区 API 与 Board，在 8000/5173 可用端口等待 health/HTTP ready，并记录工作区 listener；`run-board.ps1` 只注入本地 API/WebSocket 地址后启动 Vite。启动不会创建 Run、调用 Provider 或操作 Chrome/OpenCLI，符合既有结果只读验收范围。
- 2026-07-20：M80 服务已在 8000/5173 ready，fresh in-app Browser tab 成功加载首页且环境显示就绪。当前项目仍是 `旧厂房社区文化中心`，但“最近研究”只列最新三条 M76–M78 图纸灵感记录，M65 Deep 不在可见列表；顶部“查看上次结果”也只指向最新记录。不能把视觉 Run 冒充来源检视器验收，下一步只读检查 Board 是否支持 run-id 深链或完整历史入口。
- 2026-07-20：Board 源码确认没有 run-id 深链，历史 UI 固定 `recentRuns.slice(0, 3)`。临时切到 `旧工业建筑 · 结构与功能植入` 后，最新正文研究虽有 3 条可用参考，但在当前 text-first 规则下显示 0 个正式项目、0 个 dossier，因此没有来源检视器入口；它不能证明 M79。下一步回到首页并检查另一个既有正文项目，不修改历史可见性产品范围。
- 2026-07-20：M80 已通过标准“发起新研究”动作返回首页，并临时选择 `旧厂房更新 · 历史研究`，没有创建任务。当前仅等待该项目既有历史加载；完成验收后会恢复原始 `旧厂房社区文化中心` 选择。
- 2026-07-20：`旧厂房更新 · 历史研究` 的最新既有 Balanced 检查点可用于 M80：32 usable、2 个当前规则 article-ready 项目，前两题分别保留 6/3 条案例证据，并提供默认关闭的“查看完整案例与证据”。它不是 M65，但使用同一 M48+ text-first dossier/claim UI，且比无入口的视觉/旧 partial Run 更符合检视器真实验收；不会把其研究质量当作 M65 验收结论。
- 2026-07-20：M80 1280×720 真实 DOM 验收通过。来源检视器为 1040×720，document/body clientWidth 与 scrollWidth 均为 1280；右栏顺序是“逐字原文证据”后“核验与权利”，8 条 claim 均有 blockquote 和定位链接，原始来源入口 1、收藏/拒绝 2、备注 1。六类旧 dossier 标题计数 0，打开后 activeElement 为“关闭”按钮。
- 2026-07-20：M80 键盘合同通过：在“关闭”按钮上按 Escape 后检视器 count 立即为 0，焦点准确返回触发它的 `查看 De Nederlandsche Bank ... 剖面图证据` 按钮，没有落到 body 或其他同名证据条。viewport capability 已按文档加载，下一步可安全做临时 390×844 override，结束前必须 reset。
- 2026-07-20：M80 390×844 首轮真实 DOM 中，单列与核心内容均通过：dialog 390.4×844、content display block、预览在上证据在下，document/body scrollWidth=390，8/8 引文与链接、四项核验、重复标题 0、打开焦点均正确。但交互目标违反现有 DESIGN 合同：关闭/收藏/拒绝高度 36px，原始来源 37.6px，均低于紧凑屏 44px；这是唯一新发现，需先离线红绿修复后再复验。
- 2026-07-20：浏览器已用 Escape 关闭检视器并 reset 回 1280×720，焦点仍返回原 trigger。Board 测试目录没有现成的 `styles.css` 文本断言；最相邻的设计约束套件是 `src/design-system.test.ts`，应先检查并在该文件增加窄屏 inspector 44px 合同，而不是把样式细节塞进组件行为测试。
- 2026-07-20：`design-system.test.ts` 已使用 `styles.css?raw` 分割 860/620px media rules 并验证触摸尺寸，M80 可沿用同一模式。最小修复应放在 `@media (max-width: 860px)`：只给 `.inspector-heading button`、`.inspector-preview-pane .source-link`、`.inspector-actions button` 设置 `min-height: 44px`；不扩大所有按钮、不改桌面 36px 密度、不动抽屉布局。
- 2026-07-20：M80 post-fix 真实 390×844 DOM 证明最小 media-query 修复准确生效：关闭、原始来源、收藏、拒绝实际高度均从 36–37.6px 提升到 44px；drawer 仍为单列，preview/evidence 与 evidence/verification 顺序正确，8 条 claim/8 个定位链接、四项核验、备注与打开焦点完整，页面无横向溢出且未重新出现 dossier 分析标题。无需扩大到其他控件或改动信息架构。
- 2026-07-20：用户截图确认 M81 有两个相互关联的根因：visual workflow 沿用 M76 的“首篇帖子产出 usable 即停止”，导致图纸灵感仍像项目案例一样少而精；Board 又把同一帖子内的每张图摊平成独立卡片，重复标题、画面观察、原帖链接和对照动作。正确单位应拆开：帖子负责信息、来源、权利和一次整体说明；图片负责逐图类型、去重、明确类型过滤与独立选择。目标是在现有 24 calls / 48 MiB 内每方向累计 3–4 篇高相关帖子、每帖最多 4 图，而不是取消质量门来机械凑数。
- 2026-07-20：首轮源码索引确认改动横跨真实执行与展示，不能只改 CSS。后端已有 XHS ranked-note、`xiaohongshu_assets`、selected/skipped trace 和 M78 type-mismatch 合同；Board 当前在灵感区逐 `result` 渲染图片、观察、原帖入口与独立 comparison action。最小方向是保留 AssetCandidate/API 逐图结构，在 workflow 调整 note accumulation，在 Board 仅用现有 `sourceUrl`/帖子 identity 派生 note groups，避免数据库迁移和丢失逐图审计。
- 2026-07-20：精确根因已定位。workflow 用 `xiaohongshu_satisfied_subquestions: set[str]`，任一 note `added > 0` 就把方向加入 satisfied；后续 source 在进入 downloader/browser inspection 前直接 skip，因此 `XIAOHONGSHU_VISUAL_NOTE_LIMIT` 只承担“首篇为 0 时最多尝试 top-2”，不能累计多个成功帖子。Board 的 `inspirationGroups` 只有 `typeGroups`，渲染再 `flatMap(...).map(result)`，每个 asset 都复制 `result.project`、observation、source link 和 footer。M81 应把后端 satisfied 条件改为达到成功-note target 或尝试上限，把前端派生层增加按 canonical `sourceUrl` 的 note groups；不改 comparison ID 与 inspector 的逐图入口。
- 2026-07-20：现有常量为每帖 `REMOTE_VISUAL_BATCH_LIMIT=4`、visual note attempt limit=2；搜索本身已返回最多 4 个 ranked notes。M81 无需增加搜索次数，可把同一次搜索的尝试上限改为 4，并以 3 个 `added > 0` 的 usable notes 作为方向目标：首三篇成功即停，失败时最多看第四篇。这样三个方向上限为 12 个分类批次/48 图，仍在 24-call / 48-MiB 预算内。现有 AssetCandidate 已含 source URL、note title、逐图 observation/type/relevance/rights，Board 可用首项作为帖子元数据、用唯一 observation 首项作一次摘要，不生成新事实。
- 2026-07-20：extension browser fallback 与 OpenCLI downloader 都复用同一 satisfied set，因此 usable-note 计数必须同时接入两条路径；否则主路径会累计、fallback 仍首篇停止。Board 已有 `uniqueSummaryItems()` 可从同帖逐图观察中只取首个唯一句作为帖子摘要；现有 1180/860/620 断点只围绕 `.inspiration-gallery/.inspiration-sheet-actions`，M81 可替换为两列 note list + 帖子内两列 image grid，并在 ≤860px 保持 note list 单列、每图选择目标 44px。
- 2026-07-20：extension 迁移 fixture 的第二层失败来自测试媒体 identity，而非生产预算或 note target。`RecordingBrowser` 对每个页面都返回同一组 `https://images.example/drawing-N.png`；即使截图像素不同，后续帖子仍按 image URL 去重为 `added=0`，所以无法累计 usable notes。fixture 必须让 enumerate_media URL 随 note slug 变化，生产的跨帖/逐图去重不可放宽。
- 2026-07-20：媒体 identity 修正后，extension fallback 实际按顺序处理 branch-1 rank 1–3 和 branch-2 rank 1，然后达到 `VISUAL_REFERENCE_INSPECTION_LIMIT=(24 calls, 48 MiB)`；原因是 extension 的 `inspect_source_page` 对每帖最多 6 张截图逐张分类，而 OpenCLI `inspect_local_images` 对每帖最多 4 图走一次批分类入口。M81 的“三帖/方向”应作为默认 OpenCLI 主路径合同；extension 保持同一 24-call 上限并尽量累计，预算耗尽时诚实 partial，不能为了对称提高额度或降低逐图检查。
- 2026-07-20：M81 真实旧封存视觉结果加载了新 Board 分组结构：3 个方向各有 1 篇帖子，共 3 个具名 `article` / 10 张图；每篇只显示一次标题、观察、原笔记链接和权利说明，10 张图各自保留检视入口与“加入对照”。封存 M78 Run 的拼贴帖仍如实显示历史 1 analysis diagram + 1 axonometric + 1 site plan，不把旧数据伪装成 post-fix 类型过滤结果。
- 2026-07-20：M81 桌面 bounded DOM 几何精确通过：1265px viewport 下 document/body scrollWidth 都为 1265；帖子列表为两列 579.6px，每帖内图片网格保持两列 269px。三篇帖子分别含 4/3/3 图，且每篇标题/观察/来源链接/权利行都是 1，逐图预览与对照分别为 4/3/3，总计 10 个独立对照按钮。
- 2026-07-20：M81 390×844 无截图几何通过：实际 clientWidth/document/body scrollWidth 均为 375，帖子列表单列 351.2px，三帖内部图片网格都保持两列 158.8px；10 个逐图对照控件全部 44×44px，三条原笔记链接也均为 44px 高。
- 2026-07-20：M81 浏览器运行时 error-level log 为 `[]`。临时移动视口已 reset，最终页恢复 1265px 且 scrollWidth 同为 1265；单一已验证结果页作为 deliverable 保留。
- 2026-07-20：M81 最终 `scripts/verify.ps1` 明确 exit 0：319 API、94 Board、165 Extension、8 packaged E2E；Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。仅保留既有 TestClient/httpx2 deprecation warning。
- 2026-07-20：M81 首次最终 API 计数守卫复现已知 PowerShell `Invoke-RestMethod` JSON 数组保留语义，工作区对象被当作单项并以 null id 请求，所得 1/0/0 已丢弃；同一命令中的 `git diff --check` exit 0、staged 0 与完整 dirty status 有效。下一步使用 Node 标准 JSON 显式聚合一次，不再重复 PowerShell 计数。
- 2026-07-20：M81 最终只读持久状态守卫通过：3 projects / 64 Runs / active 0；封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍为 completed / attempt 0 / coverage_satisfied，未 retry。结合最终全仓门禁、无截图真实 QA、whitespace exit 0 与 staged 0，M81 可标记 complete。
- 2026-07-20：用户在明确得知“尚未重新搜索新数据”后批准开始实测，授权边界是唯一一条 post-fix Live Run。预检基线为 3 projects / 64 Runs / active 0；封存 `10d31b4c-...` 保持 completed / attempt 0 / coverage_satisfied，绝不 retry。新验收复用同一 workspace 与自然问题，但创建全新 attempt 0，不是重试。
- 2026-07-20：M82 唯一 Live Run 已创建：`cbf8053b-f0c6-4408-b0d7-4b0ffff13b16`，workspace `fd3b06cd-...`，question `我想出一张轴测图，帮我找风格`，visual_reference_search / quick / XHS-only，attempt 0。创建前 active 0，POST 仅执行一次；后续只监控该 ID。
- 2026-07-20：M82 唯一 Live Run `cbf8053b-...` 已终止 `partial/composing`，当前读取到 11 个结果但 0 个 XHS note；来源实际为 ArchDaily、Wikipedia、Archcollege，类型含 photograph/analysis_diagram/elevation/render/section，明显不满足自然轴测 + XHS-only + 逐图 axonometric gate。该 Run 立即永久封存、绝不 retry；不得创建第二条，先合并离线诊断 OpenCLI/extension fallback 与通用 Provider 抑制链。
- 2026-07-20：M82 62 条 Trace 已定位首个失败链：sequence 4 `xiaohongshu_search` 为 `skipped/OpenCliCommandError`；随后 extension `local_browser_search` 六次都返回 4 个通用网页候选，OpenAI 通用搜索虽因 `local_browser_search` 被 skipped，但 workflow 仍解析并持久化这些 ArchDaily/Wikipedia/Archcollege 页面。最终 coverage 仅 3 usable、2 projects、3/3，gaps=`browser_inspection_incomplete`、enrichment=`insufficient_usable_assets`；这不是 M81 多帖累计失败，而是 OpenCLI 失败后的 extension fallback 未保持 XHS-only 来源边界。
- 2026-07-20：源码合并诊断确认具体控制流：`_try_xiaohongshu_search` 的 OpenCLI 搜索失败后，只有当 workflow 启动时 extension `browser_client.connected` 为真才有第二个 XHS searcher；本次 Trace 没有 extension XHS backend，说明当时只注册了 OpenCLI。失败返回空 sources 后，visual flow 没有关闭 `can_search_publicly`，因此转入 Direct public `local_browser_search`，把通用网页结果当成视觉候选。现有“有 XHS candidates 时抑制通用搜索”合同成立，但“XHS-only 且所有 XHS backend 失败时不得降级通用网页”缺少合同。
- 2026-07-20：相邻离线测试只覆盖两类成功路径：OpenCLI 返回 ranked XHS notes 时累计三帖；已获得 XHS note URLs 后由 extension 逐页检查并在固定预算内 partial。没有覆盖 `research_sources=['xiaohongshu']`、OpenCLI 抛错、extension XHS searcher 不可用、Direct public search 可用的组合，因此本次通用网页降级未被 319 API 门禁捕获。API stdout/stderr 无额外 OpenCLI 错误细节；Trace 仅安全记录 `OpenCliCommandError`。
- 2026-07-20：M82 发现第二个独立前置缺陷：当前 API 进程 `2026-07-20T07:05:03Z` 启动，`workflow.py` 的 M81 修改时间为 `07:59:02Z`，而 `scripts/start.ps1` 启动 uvicorn 时没有 `--reload`。因此 M82 实际运行的是 M81 之前的后端，不能作为多帖子累计实现的有效 Live 验收；Board HMR 更新不等于 API 已重载。即使重载，当前源码仍存在 XHS-only 全后端失败后降级通用网页的独立缺口。
- 2026-07-20：用户批准 M83，范围是 XHS-only fail-closed 修复、离线门禁和受控服务重载；授权不包含新的 Live Run。最小合同必须模拟 XHS searcher 抛错且 Direct public/generic provider 可用，断言两者均未调用、通用资产为 0，并保持既有 OpenCLI 三帖累计与 extension 预算 partial 测试。
- 2026-07-20：M83 最小实现点确定在每轮搜索能力计算：当 `goal=visual_reference_search` 且 `research_sources` 精确为 `{xiaohongshu}` 时，通用 Direct public 与模型搜索从一开始都应为 false，唯一可用搜索面是 XHS searcher 链；OpenCLI→extension fallback 仍由 `_try_xiaohongshu_search` 内部顺序完成。全 XHS 失败后 Run 无资产并诚实 blocked/no_usable_assets，coverage 保留 browser_inspection_incomplete。
- 2026-07-20：红测采用一个双参数行为合同分别暴露两条旧降级路径：有 `RecordingPublicSearchParser` 时断言 `queries=[]`，无 parser 时断言 generic provider `calls=0`；两种情况下 SourcePage/AssetCandidate 均为 0。这样不把修复只绑定到 M82 恰好出现的 Direct public 配置。
- 2026-07-20：M83 参数化红测有效命中两条生产旧行为：local case 实际执行 3 次 Direct public query；model case 实际执行 generic provider 3 次。失败发生在预期的调用计数断言，没有 fixture 或终态形状错误，证明 fail-closed 缺陷可稳定离线复现。
- 2026-07-20：M83 最小生产修复只新增 `xiaohongshu_only_visual` 判定，并在每轮能力计算时把 Direct public/model search 设为 false；XHS searcher 链、四帖尝试、三帖目标、逐图类型门和 24-call/48-MiB 均未改。focused 5/5 green：fail-closed local/model 两例、OpenCLI 三帖累计、extension 预算 partial、显式轴测类型过滤全部通过。
- 2026-07-20：M83 扩大 API 门禁通过：完整 browser-inspection suite 全绿；Ruff lint、两文件 format check、strict Mypy 18 source files 全绿。现有 README/architecture 仍写 M76 top-2，PRODUCT/DESIGN 已写 M81 四帖/三 usable 但缺 fail-closed，需同步这四处当前合同后再跑全仓门禁。
- 2026-07-20：M83 首次全仓门禁在 API 结束为 319 pass / 2 fail。仅 `test_board_exports.py` 两例在读取 `results[0]` 时为空；它们使用的 XHS-only visual Run 过去由 generic mock provider 填充资产，正是新 fail-closed 合同禁止的旧路径。browser-inspection 与静态门禁仍全绿，失败不是生产 guard 误触普通 precedent 流程。
- 2026-07-20：两条 export 失败共用 `_run_with_results`，该 helper 未显式传 `research_sources`，因此采用产品默认 XHS-only，却把 generic mock provider 当导出数据生成器。导出测试并不验证小红书来源，最小正确迁移是显式 `research_sources: []`，把 fixture 声明为通用视觉 mock；不应放宽生产 fail-closed。
- 2026-07-20：export fixture 迁移后 focused 10/10 green：完整 board-export 行为、XHS fail-closed local/model、OpenCLI 三帖累计、extension 固定预算 partial 与显式类型门共同通过。首次 full-gate 两个失败已合并解决，允许执行修复后的最终全仓门禁。
- 2026-07-20：M83 修复后最终 `scripts/verify.ps1` 明确 exit 0：321 API、94 Board、165 Extension、8 packaged E2E；Ruff lint/48-file format、strict Mypy 18 source files、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。仅既有 TestClient/httpx2 deprecation warning。
- 2026-07-20：M83 标准 stop/start 已在预检 3 projects / 65 Runs / active 0 下成功完成；API/Board 新 PID ready、health ok / HTTP 200，未创建 Run。首次 post-reload 时间守卫错误地用 PowerShell `DateTime.Parse(...).ToUniversalTime()` 对 `Z` 时间二次换区，所得 false 无效；改用 DateTimeOffset 重算，其他 ready/count 结果不受影响。
- 2026-07-20：第二次 timestamp 比较仍无效：`ConvertFrom-Json` 已在读取时把 ISO `Z` 字符串转换成 PowerShell DateTime，之后 DateTimeOffset 无法恢复原 offset。停止使用 PowerShell JSON 日期链，改用 Node `JSON.parse` + filesystem mtime 做一次标准 UTC 比较；不影响已验证的服务 ready 与持久状态。
- 2026-07-20：Node 原始 JSON/mtime 最终时间守卫有效：API started `10:58:17Z`，workflow last-write `10:47:25Z`，`api_loaded_after_workflow_write=true`。M81 多帖累计与 M83 fail-closed 已由当前服务进程加载；本轮没有 Live Run。
- 2026-07-20：用户在看见 M82 历史失败卡并确认 M83 已加载后明确“继续”，批准 M84 新 post-fix Live 验收。与 M82 不同，M84 必须先读 `/v1/browser/status` 和服务/active 状态；只有至少一条 XHS search surface ready 才允许唯一一次 POST，避免在已知 OpenCLI/extension 都不可用时机械消耗 Run。
- 2026-07-20：M84 预检基线有效：3 projects / 65 Runs / active 0，API started 晚于 workflow write；`/v1/browser/status` 为 ArchResearch extension `connected=false`、OpenCLI adapter `available=true`。`pnpm opencli -- doctor` 进一步证明 adapter 可发现不等于可执行：daemon v1.8.6 正在 19825，但 Browser Bridge extension 未连接，connectivity FAIL。
- 2026-07-20：M84 单次 `opencli daemon restart` 成功重启 v1.8.6，但输出立即提示 Bridge 未连接；等待 3 秒后的第二次 doctor 仍为 Extension MISSING / Connectivity FAIL。继续 POST 会只得到 M83 正确的 fail-closed blocked，不能验证累计，因此 Run 数保持 65。
- 2026-07-20：M84 环境根因进一步确认：系统当前 Chrome process count=0，而 OpenCLI daemon 19825 正在监听；Bridge 无法连接是因为用户 Chrome 未运行，不是 daemon 未启动。可以安全启动既有默认 Chrome 会话一次，等待已安装 Bridge 自动连接；不安装或修改扩展。
- 2026-07-20：启动既有默认 Chrome 后，`opencli doctor` 全绿：daemon v1.8.6、Bridge extension v1.0.22、profile `2btxrgkn`、connectivity 0.2s。没有安装或修改扩展；M84 的至少一条 XHS search surface 已真实 ready，可在再次确认 active 0 后创建唯一 Run。
- 2026-07-20：M84 唯一 Live Run 已创建：`965fde3c-3a75-41b2-a119-381ee1f21c44`，workspace `fd3b06cd-...`，natural axis prompt、visual_reference_search / quick / XHS-only、attempt 0。创建前 3 projects / 65 Runs / active 0 且 doctor 全绿；POST 仅执行一次。
- 2026-07-20：M84 active Trace 已真实证明 M81+M83 当前服务生效：OpenCLI search completed/result_count 4/fallback false；前三帖中 note 1 接受 4/4 axonometric，note 2 因 4/4 type mismatch 全拒，note 3 接受 4/4 axonometric。当前持久化 2 篇 XHS-only帖子 / 8 张，全部 axonometric；usable_note_count=2、note_attempt=3，正在继续尝试第 4 帖，通用来源为 0。
- 2026-07-20：M84 唯一 Run 已 terminal partial。Live 路径正确性通过：12 persisted images 全为 `axonometric`、全部 XHS-only、0 generic sources；第一方向四次 note attempt 为 4 accepted / 4 mismatched / 4 accepted / 3 accepted，真实达到 3 usable notes。随后第二方向只获得 1 usable note，并在第二帖后达到 visual_calls_used=24；第三方向虽完成 ranked search，但无剩余分类预算。最终为 4 unique posts / 12 images，数量只略高于旧 3 posts / 10 images。
- 2026-07-20：M84 最终 run shape：partial / attempt 0 / stop_reason=`time_budget_exhausted`；coverage 12 usable、4 unique posts、2/3 directions，只有 technical-linework 与 collage-texture，atmospheric-cutaway uncovered。30 Trace 中仅 workflow/xiaohongshu_search/openai-skipped/xiaohongshu_assets，generic events 0。OpenCLI 三次 ranked search 都 completed/result_count 4/fallback false。
- 2026-07-20：预算根因由源码确认：`VISUAL_REFERENCE_INSPECTION_LIMIT=(24,48 MiB)` 的 “calls” 实际在 `inspect_local_images` 准备每张本地图时由 `reserve_capture()` 逐图递增；同帖四图虽然只发一次 `classify_remote_batch` Provider 请求，仍消耗 4 slots。因此“三方向 × 三 usable 帖 × 每帖最多四图”在无失败时就需 36 slots，遇到错类型重试可到 48 slots；当前 24 slots 与 M81 目标结构性不相容。M84 仅用约 190 秒，`time_budget_exhausted` 还是对 inspection-slot exhaustion 的误标。
- 2026-07-20：用户已批准 M85 采用最小容量修复：视觉检查上限提高到 48 个逐图槽位，字节上限保持 48 MiB；现有每方向最多四帖的上限使最坏三方向各四帖恰好封顶 48，因此无需新增调度器即可避免前两个方向结构性饿死第三方向。若扩展后备因单帖暴露更多图片仍耗尽，继续诚实 partial，不放宽逐图类型门。停止原因需明确为 `visual_budget_exhausted`，不能再借用真实时长语义。
- 2026-07-20：M85 有效红测确认旧实现的两个精确行为：三方向最坏 fixture 在 `visual_calls_used=24` 截断；从已耗尽逐图预算恢复时终态原因是泛化的 `no_usable_assets`，Board 对目标新原因也只能显示通用 partial 文案。fixture 本身已通过三方向 schema 门，失败不再来自测试设置。
- 2026-07-20：48 槽下 extension fixture 暴露一条既有完成语义缺口：前两方向各 3 篇、第三方向仅 2 篇 usable 帖子时，逐图资产 coverage 已满足，旧终态会误报 `completed/coverage_satisfied`。正确边界不是把三帖变成所有视觉 Run 的硬完成门，而是仅当 XHS-only visual 已耗尽 inspection budget 时，若任一方向尚未达到三帖目标就保持 partial；未耗尽但来源本身不足的既有完成语义和普通视觉/precedent 均不改变。
- 2026-07-20：M85 最终实现边界已验证：OpenCLI 最坏三方向各四帖恰好 48 slots，第三方向不再被前两方向结构性饿死；extension 每帖可检查 6 图，48 slots 下为 3/3/2 usable notes，因此诚实 `partial/visual_budget_exhausted`。48 MiB、逐图显式类型过滤、XHS-only fail-closed、来源/权利和帖子分组均未放宽。历史 M84 Run 保留其原始旧原因，不做数据迁移。
- 2026-07-20：用户明确批准 M86 单次 loaded-M85 Live 验收，并强调“修不完就一直修，不要坏了就又新开一条”。授权边界是只创建一条全新 attempt 0；若失败，该 Run 永久封存，后续持续离线红绿修复与全仓验证，绝不用新增 Live Run 逐错误试探。成功也不自动追加第二条。
- 2026-07-20：M86 preflight 全部有效：API health ok 且 started_at 晚于 workflow last-write，3 projects / 66 Runs / active 0；`/v1/browser/status` 为 connected=true、XHS search available=true；OpenCLI doctor 为 daemon 1.8.6、Bridge 1.0.22、profile connected、0.1s。已满足唯一 Live Run 的创建前置条件。
- 2026-07-20：M86 首个 active checkpoint 已证明 loaded 路径：OpenCLI ranked search completed/result_count 4/fallback false，generic OpenAI skipped；第一篇 XHS note 下载 4、候选 4、轴测类型接受 4、mismatch 0、added 4，`visual_calls_used=4`。持久结果为 1 帖 / 4 图，全部 axonometric 与 xiaohongshu.com。
- 2026-07-20：M86 已越过 M84 的旧 24-slot 停止点。第一方向四次尝试形成 3 usable posts / 11 axonometric；第二方向首帖接受 1 图，run 在 `visual_calls_used=24` 后仍继续 inspecting。当前 4 posts / 12 images，全 XHS-only、类型全 axonometric，generic OpenAI 两方向均 skipped。这是真实服务已加载 48-slot 的直接证据。
- 2026-07-20：M86 唯一 Live Run 已 terminal `completed/coverage_satisfied`：25 usable images、8 unique XHS posts、3/3 directions、gaps/enrichment_gaps 均空，`visual_calls_used=40`、preview bytes 8,559,520。第三方向三帖均 usable，最后一帖 4 candidates 中接受 3 axonometric、拒绝 1 mismatch；全量持久类型仅 axonometric。M85 容量与停止边界无需追加修复，也绝不创建第二条 Run。
- 2026-07-20：M86 全量只读审计通过：方向维度分别为 technical 3 posts/11 images、collage 3/8、rendered 3/9；跨方向共 8 unique posts / 25 stored assets。所有结果均 xiaohongshu.com、axonometric、aggregator、visual_lead、unknown rights/project/association。34 Trace 仅 workflow 18、XHS search 3、XHS assets 10、OpenAI skipped 3；generic events 0、OpenAI non-skipped 0。当前 3 projects / 67 Runs / active 0。
- 2026-07-20：M86 无截图真实 Board 验收通过。结果页状态为已完成/25 条可用参考，方向摘要为 11/8/9 张；视觉板按方向渲染 3/3/3 个帖子 article、11/8/9 个图片与独立对照、每方向 3 个原笔记链接。9 个方向级 article 对应 8 个唯一帖子，唯一共享帖子在两个关联方向各出现一次，但每个 article 内标题、观察、来源/权利、原帖只一次。页面 all image alt 均轴测图、document/body overflow 0、error log 0。
- 2026-07-20：M87 根因确认：`result-workbench` 只判断 result view 与结果数量，未按 `activeRun.goal` 分支，因此两种模式共享标题、设计方法、表达规范和导出文案；截图中的模式错配是生产结构而非数据问题。用户明确要求不同模式不同功能，并且两种模式都不显示研究过程。最小产品边界是保留 comparison/export 的既有后端行为但按模式重命名/取舍，表达规范仅属于图纸灵感；Trace 持久化/API 保留，前台入口与抽屉移除。
- 2026-07-20：M87 具体源码边界已确认：共享工作台位于 `App.tsx` 结果区，当前不读取 `activeRun.goal`；用户 Trace 抽屉由独立 `traceOpen` 状态、入口按钮与 drawer 组成。`traceEvents` 同时服务浏览器不可用等结果诊断，因此只移除前台 drawer/state/helper，保留事件获取与诊断数据。现有工作台 CSS 依赖固定双列与位置选择器，需改为自然响应式网格以容纳建筑设计 3 项与图纸灵感 4 项动作。
- 2026-07-20：M87 测试迁移边界已确定：旧共享工作台测试同时断言 5 个按钮并打开 Trace，将改为两种 goal 的互斥工具合同；建筑设计只显示案例策略/案例研究板/证据板，图纸灵感只显示图纸表达/表达规范/灵感板/来源板，两种模式都不得出现“查看研究过程”或“研究过程记录”。既有表达规范行为 fixture 迁移到 visual goal，不改变数据模型或 handler。
- 2026-07-20：M87 有效红测在旧生产上得到 6 条预期失败，覆盖新模式标签、Trace 移除、导出命名与表达规范归属；最小生产修改后 focused 6/6 green。`traceEvents` 仍获取并供来源诊断使用，只有 `traceOpen`、Activity/演示 Trace import、summary helper、drawer JSX 与无调用 CSS 被删除。工作台动作网格改用 goal class + 1px gap，不再依赖按钮序号画分隔线。
- 2026-07-20：M87 完整 Board 首轮门禁为 96/96、lint、typecheck、production build 全绿；Impeccable detector 对 `App.tsx/styles.css` 为 0 findings，`git diff --check` exit 0。静态审计发现并删除仅由旧前台抽屉使用的 demo `TraceItem/traceItems`；真实 `TraceEvent` client、SSE 获取、持久化/API 和来源失败诊断完全保留。
- 2026-07-20：M87 无截图真实 DOM 验收通过。M86 图纸灵感结果工作台精确为“对照图纸表达 / 编辑图纸表达规范 / 导出个人灵感板 / 生成可分享来源板”，建筑历史结果精确为“对照案例策略 / 导出案例研究板 / 生成可分享证据板”；交叉模式标签、研究过程按钮和过程 dialog 均为 0。桌面两页 document/body overflow 0，390×844 override 下两套工作台均为单列、按钮高 88px、overflow 0，error logs 0；视口和原 `旧厂房社区文化中心` 项目选择已恢复。
- 2026-07-20：M87 最终全仓门禁 exit 0：323 API / 96 Board / 165 Extension / 8 packaged E2E，加 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 evaluation 全绿。只读服务守卫为 3 projects / 67 Runs / active 0；封存 `10d31b4c-...` 仍 completed/attempt 0，未 retry。whitespace exit 0、staged 0，完整 dirty/untracked 工作树保留。
- 2026-07-20：用户批准 M88 删除两种模式的低价值对照区。产品解释采用最小且不虚构功能的边界：移除工作台对照入口、对照 dialog 与底部 dock；既有逐图选择仍服务私有/分享导出，但前台改为“加入/移出导出板”语义。图纸灵感删除对照后恰为表达规范、个人灵感板、分享来源板三项一行三列；建筑设计只剩两个真实导出动作，保持一行两列，不新增占位功能。
- 2026-07-20：M88 源码审计确认当前 `comparisonIds` 同时承担“方法对照选择”和两个导出的选中资产合同，不能连选择状态一起删除，否则两个导出会永久 disabled。正确拆除边界是：删除 `comparisonOpen`、workbench compare button、comparison dock、dialog、`selectedComparisonResults` 与专用 CSS；把保留状态/handler/逐图按钮改名为 export-board selection，并继续 PATCH 同一 board `selected_asset_ids` API。PRODUCT/DESIGN 中全部对照职责同步删除。
- 2026-07-20：布局可直接复用 M87 的 gap-grid：visual 从 4 项减为 3 项后把 desktop class 改为三列；precedent 从 3 项减为 2 项后改为两列。≤620px 现有统一单列规则继续生效，无需新增断点或视觉组件。
- 2026-07-20：用户随后明确修正 M88：建筑设计保留案例策略对照，只删除图纸灵感对照。前两条关于“双模式删除/建筑两列”的推演不再执行。最终两套工作台都为桌面一行三列：precedent 继续 compare/private/share，visual 为 style/private/share；comparison dock/dialog 只对非视觉结果可达，视觉逐图按钮改为“选择此图用于导出 / 取消导出选择”。
- 2026-07-20：M88 相邻 JSX 确认最小生产差异只有三处：workbench compare button 用 `!isVisualResearch` 包裹；visual 图片按钮把 `compared` 文案改为导出选择；comparison dock/dialog 都加 `!isVisualResearch` 可见门。现有 design demo 对照测试应保留，证明建筑功能未被误删；visual 行为测试需从 4 按钮改为 3 并主动选择图片，断言导出启用而 dock/dialog/所有对照文案仍为 0。
- 2026-07-20：M88 focused 红测 2/2 有效：旧生产仍渲染 5 个“加入对照”视觉按钮，visual workbench 仍为 4 项而非 3 项。失败点精确来自待改生产行为，没有 fixture 或异步噪声。
- 2026-07-20：M88 核心红绿完成，focused 4/4：visual workbench 仅 style/private/share 三项，desktop class 为 visual 三列；逐图按钮为“选择此图用于导出 / 取消导出选择”，选择后两种导出均启用且 comparison dock/dialog 仍为 0。precedent workbench 仍为 compare/private/share 三项，demo 方法对照矩阵完整通过。
- 2026-07-20：M88 完整 Board 离线门禁通过：96/96 tests、lint、typecheck、production build 全绿；Impeccable detector 对 `App.tsx/styles.css` 为 0 findings。构建产物 CSS 68.91 kB、JS 329.61 kB，较 M87 继续小幅收敛；design comparison CSS/JS 因仍有正式调用方而保留。
- 2026-07-20：M88 真实 M86 visual desktop DOM 已加载最新代码：结果工作台按钮精确为表达规范/个人灵感板/分享来源板三项，computed grid 为 3 个等宽 278.763px 列；28 个逐图选择均为导出语义，visual compare button、comparison dock/dialog 全部为 0，document overflow 0。
- 2026-07-20：M88 visual 390×844 override 下工作台为单列 349.6px，三按钮均高 88px、document overflow 0；viewport 已 reset。随后 reload 返回首页以恢复验收前的项目选择，未触发任何研究或 board PATCH。
- 2026-07-20：M88 真实 design desktop DOM 为对照案例策略/案例研究板/分享证据板三项，computed grid 同为 3 个等宽 278.763px 列，design compare button 1、visual compare button 0、overflow 0。390×844 下恢复单列 349.6px，三按钮均高 88px、overflow 0；viewport 已 reset。建筑对照的可交互矩阵继续由 focused demo 与完整 Board 测试证明，未写入 live board 选择。
- 2026-07-20：M88 最终全仓门禁 exit 0：323 API / 96 Board / 165 Extension / 8 packaged E2E，加 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 evaluation 全绿。3 projects / 67 Runs / active 0 保持；封存 `10d31b4c-...` 仍 attempt 0、未 retry。whitespace exit 0、staged 0，工作树全部保留。
- 2026-07-20：用户批准 M89：当前结果工具区像雏形且在整体页面中过重，需要参考 GitHub 上相关成熟项目并轻量化；建筑设计与图纸灵感必须继续区分。M89 先做 GitHub 只读模式对比，再把可验证模式映射回现有 Board，不新增后端/基础设施、不复制代码、不创建或 retry Run。
## M89 GitHub reference scan (2026-07-20)

只读查看了几类成熟开源项目的 README/入口说明，结论只用于提炼交互模式，不复制代码或引入基础设施：

- `siyuan-note/siyuan`：把内容块、双向引用、数据库视图与导出作为工作内容本身；工具围绕内容组织，而不是用一组重型“分析卡片”包住结果。可迁移点：结果区先展示内容，再把保存/导出收进轻量动作带。
- `OCA/knowledge`：以文档页、附件预览、项目/标签/引用等小模块组合知识工作流；每个模块职责单一。可迁移点：建筑设计的“对照”应是唯一的判断型工具，来源板/研究板应是独立的收集与分享动作。
- `Zlvsky/React-Konva-moodboard`：视觉板以画布和素材为主，图片拖入、背景、导出是直接动作，工具不抢主视觉。可迁移点：图纸灵感应该突出图片网格与帖子级来源，工具只保留规范、个人板、来源板三项。
- `dibyasom/moodboard`：基本能力收敛为创建、添加素材、分享、协作；没有把每一张图片重复套上完整文案。可迁移点：图纸灵感应按帖子承载标题/观察/来源，图片只承担类型与选择状态。
- `inlineresearch/Inline-Studio`：单进程工作台但用 canvas/planning/generation 分区，且强调不可破坏的 take 历史；复杂过程留在工作流内部。可迁移点：ArchResearch 前台只给用户结果和下一步动作，研究过程/Trace 继续留在诊断层。

暂未采用：Foam README 路径在 connector 中返回 404；空仓库 `winniesss/design-inspiration-board` 不作为参考依据。

M89 取舍：不新增画布、拖拽或协作系统；只把现有结果工具区改成低权重 action rail。建筑设计保留 `案例策略对照 / 案例研究板 / 分享证据板`，图纸灵感固定为 `表达规范 / 个人灵感板 / 分享来源板`，并继续按帖子显示一次标题、观察、来源和权利信息。

## M89 产物实现决策

- 导出按 `ResearchRun.goal` 分成两个 artifact kind：`case_research_board` 与 `visual_inspiration_board`，manifest 记录该类型，避免下游把两种板当成同一产品。
- 建筑设计导出是项目证据台账：每个案例包含项目条件、设计机制、怎么转译、适用边界和逐字证据；私有/分享仍沿用原有权利门禁。
- 图纸灵感导出按 `source_url` 聚合帖子；帖子标题、整体观察、来源链接只出现一次，组内图片只出现图纸类型和权利状态。保存的表达规范（主色、线型层级、字体、纹理、版式备注）展示在板首，并同步写入 manifest。
- Board 的表达规范编辑器新增纹理与版式备注，保存后会进入视觉灵感导出；建筑设计不显示这些视觉字段，视觉灵感不显示项目条件/设计机制/边界。

## M90 export visibility and history/memory research

- 真实导出页的三张 `<img>` 都 `complete=true / naturalWidth=0`。页面使用 `/v1/results/{id}/content`，三个请求均返回 404；API 实际只注册 `/v1/assets/{asset_id}/content`。这是确定性路由拼写缺陷，不是 CSP、跨域或图片本身损坏。
- 已做最小修复并让 board-export 8/8 通过：本地持久化图片的导出地址改为 `/v1/assets/{id}/content`；不需要创建或 retry Run，旧导出 HTML 本身仍是不可变文件，需要重新导出或对旧文件做兼容路由才能恢复。
- GitHub 初筛选择 `open-webui/open-webui`、`danny-avila/LibreChat`、`mem0ai/mem0` 作为三类参照：会话历史、可部署聊天历史、长期语义记忆。第一轮代码搜索显示 LibreChat 有 history/retention 配置入口，Mem0 的 memory store 支持显式 history/expiration 语义；Open WebUI 未在初始关键词中暴露固定 TTL。
- 暂不把“最近展示条数”与“底层删除天数”绑定：成熟本地优先产品通常分页/搜索全部历史，首页只显示最近若干；真正的长期记忆需要显式提炼、可查看/删除，不能把全部聊天或 Run 自动当作记忆。

### GitHub implementation comparison

- Open WebUI 的 chat router 将“最近/列表”和“永久保存”分开：普通列表分页默认每页 50，另一列表页每页 60，导出批次每页 10；聊天支持 pinned / archived / search filter，没有发现默认 TTL 自动清理。这说明 UI 首屏数量是展示策略，不应等于数据库保留上限。
- LibreChat 的配置区分 temporary chat retention 与普通会话；`temporaryChatRetention` 接受 1–8760 小时（最多 365 天），并有 `retentionMode`。这类 TTL 适用于明确标记的临时会话，不适合默认删除用户的正式研究成果。
- Mem0 将“会话最近消息”和“长期记忆”分层：提取记忆时只读取最近 10 条 session messages；长期 memory 可选 `expiration_date`，不设置即不过期；过期记忆默认从 search/get_all 隐藏，但仍可按 ID 读取。其关键模式是“少量最近上下文 + 显式长期记忆 + 可选过期”，不是无限回放全部历史。
- ArchResearch 现状：首页已经硬编码只显示最近 4 条，但 API 返回该项目全部 Run；三个项目分别有 3 / 38 / 26 条 Run，底层没有 Run TTL。这个 4 条限制过窄且没有“查看全部历史”入口，用户无法组织已经保存的 67 条研究。
- 市面产品官方规则补充（2026-07-21）：ChatGPT 普通聊天与 Saved Memory 都默认保留到用户主动删除，Temporary Chat 才在 30 天内自动删除；Slack 付费工作区也默认保留到工作区生命周期结束，免费版才提供 90 天或 1 年的限期选择。共同模式是把“首屏显示多少”与“何时永久删除”分开。
- M90 最终取舍：正式 ArchResearch Run 是项目记录，默认永久保存到用户删除项目；首页展示最近 10 条，其余按需展开。现阶段不增加自动删除，也不凭空建立新的 memory 数据库。未来若明确批准临时记忆，默认 30 天并与正式历史隔离；用户主动保存的项目记忆仍应保留到主动删除。
- 旧导出不改写原 HTML；新增 schema-hidden 的 `/v1/results/{asset_id}/content` 兼容读取，并与 canonical `/v1/assets/{asset_id}/content` 共享同一个受限本地文件 handler。新导出只生成 canonical 地址，因此当前旧链接可恢复、未来产物不再依赖旧路径。

## M91 page-matched history and actual memory architecture

- 用户截图确认 M90 把右侧最近研究默认铺成 10 行，直接把两栏 grid 的整行高度拉到接近一屏，左侧四个问题起点下方形成大块无意义空白。问题不是数据保留，而是把“展示窗口 10”误当成首页布局需求。
- M91 的最小修正是：首页只显示最近 3 条，完整历史仍全部保留；“全部历史”打开固定视口内的 overlay，不再把主页继续向下撑长。首页不再展示系统保留策略说明。
- 当前真实架构只有四层持久状态：`Workspace` 项目；`ResearchRun` 及 checkpoint/Trace；Run 下的结果/证据；用户对结果的收藏、拒绝、备注、Board/style/export。前端 `localStorage` 只记当前 workspace 选择。现在没有跨 Run 的 memory entity、自动记忆提炼、记忆检索或独立 TTL；把正式 Run 历史称作“记忆系统”是不准确的。

## M92 strategy matrix and retention policy

- 用户截图确认当前案例研究板只输出单个项目的一张图和与结果页重复的长段条件/机制/转译文字，缺少跨案例关系、选择判断或可直接带回设计的结构；它不能以当前形态继续存在。
- 保留该入口的唯一合理产品形态是“跨案例策略矩阵”：以子问题为行组、案例为列，单元格只承载机制、首要迁移动作、适用边界和证据入口；另给去重后的设计动作清单。它与结果页的阅读型 dossier 明确分工，不再复制图片和长文。
- 当前持久数据盘点为 67 Runs：12 completed、38 blocked、15 partial、2 cancelled、active 0。用户明确要求删除未完成记录，因此清理集合固定为 55 条 `partial/blocked/failed/cancelled`；completed `10d31b4c-...` 不在集合内。
- Retention 语义按 Run 而非 Workspace：新 Run 创建即 `retention_expires_at = created_at + 14 days`；永久保留令 expiry 为 null/flag true；取消永久后从操作时重新获得 14 天。既有 completed Run 在迁移时统一获得 14 天 adoption grace，避免旧记录在用户尚未选择时立即消失。
- 2026-07-21：真实导出暴露了最后一个产品边界：同一项目选两张资产仍只能形成单列，不能称为“跨案例策略矩阵”。最终合同在 Board 与 API 双端统一为至少两个标准化后的不同项目名；同项目请求返回 422，视觉灵感私有导出与分享证据板不受影响。
- 2026-07-21：加载后的真实双项目产物以 Canal Hub 1958 与 De Nederlandsche Bank 为列，包含 6 个设计问题、8 条去重设计动作、0 图片、0 旧 `.case/.case-copy` 长文块且无横向溢出。它与研究结果页的差别成立：结果页用于逐案阅读，矩阵用于跨案对齐机制、动作、适用边界和来源。

## M93 personal collections

- 用户把“个人灵感板”重新定义为产品内的“个人收藏”：它代表用户明确认为可复用的建筑案例或图纸，而不是另一个导出文件标题。收藏必须有主页入口，并按建筑方案 / 图纸灵感、原研究题目两级组织。
- 选择资产只是临时草稿，不应继续同时承担“对照、导出、收藏”三种含义。结果页在至少选中一项后显示底部停靠条，明确给出已选数量、清空和“添加到个人收藏”；只有提交该动作后才写入收藏。
- M93 不建设 memory table、自动提炼或跨 Run 推理。优先复用现有 `SavedReference` 用户策展状态；实现前必须确认其 API、级联删除和 14 天清理边界，并让显式收藏免于普通历史过期。
- 源码审计确认 `SavedReference.asset_candidate_id` 没有外键，且已有 `snapshot` JSON；它可以独立于 `AssetCandidate/ResearchRun` 生存。当前 M92 `delete_runs` 仍手动删除 SavedReference，现有 snapshot 也缺少 run question/goal，并只引用可能随 Run 删除或过期的图片地址。
- 最小持久方案是不建新表：保存时把 question、goal 与结果展示字段写入 snapshot，并把受控本地图复制到 `data_dir/collections/{saved_id}`；收藏内容使用独立安全 endpoint。Run 清理不再删除 SavedReference 或收藏副本，主页用 workspace-scoped 聚合读取；删除收藏使用 saved-reference ID，因此原 Run 已过期也能完成。
- 用户进一步明确收藏是与主页、研究结果同级的完整页面，不是 overlay/dialog。收藏页与研究结果页都必须提供明确的“返回主页”动作；底部收藏条仍留在结果页，只负责提交当前选择。现有 `collectionOpen + floating-panel` 实现因此只是被测试取代的中间状态，不能作为 M93 验收结果。
- 收藏提交后的停靠条不应继续显示“已选 N 项 + 已加入个人收藏 + disabled 添加按钮”的操作形态；它应收敛为“加入成功”确认。只有用户又选中新的未收藏项时，操作形态才恢复。用户已纠正同题语义：一次提交可包含多项，这一整批必须共同保留；以后同一研究题目的新批次覆盖旧批次，而不是让多个历史批次长期并存。
- 加载后真实收藏页首次 DOM 检查发现全局顶栏和页内标题各有一个“返回主页”。这是同一页面内的重复命令，不增加导航能力；最终只保留所有非首页页面共用的顶栏返回动作，并以每页恰好一个按钮固定合同。

## M94 remove Pinterest product surface

- 用户明确决定当前产品只保留小红书，删除 Pinterest。范围是当前 schema/TypeScript 来源契约、Board 来源控件与域名标签、workflow 提示/路由、相关测试和当前产品文档；历史 HANDOFF/findings/progress 与既有 Run 数据保留为事实记录。
- 全仓初扫确认没有 Pinterest SDK、凭据、API client 或直抓实现。现存生产路径只有 `ResearchSource.pinterest`、Board 的 Pinterest 复选框与 URL 标签、以及通用 Provider 搜索提示允许返回 Pinterest 原 Pin 链接；因此可做一次小范围彻底删除，不引入替代抽象。
- 不能只删除枚举与提示：通用 Provider 仍可能自行返回 Pinterest/`pin.it` URL，而当前 `_is_sparse_visual_platform_url` 会为它们保留特殊低信任语义。若直接删掉识别，反而可能把 Pinterest 当普通项目来源。M94 对新 Provider 结果采用 fail-closed 丢弃；既有数据库记录不迁移、不删除。
- Loaded API 只读盘点：当前 12 条 completed Runs 中 8 条记录 `xiaohongshu`，0 条含 `pinterest`。因此可直接收窄 Pydantic/TypeScript 枚举，不需要历史数据迁移或读取兼容层。
- M94 生产复扫后仅保留两类有意 Pinterest 字符串：workflow denylist 的域名匹配，以及 Board/API 行为测试中的拒绝/缺席断言。所有用户可选能力、对外来源契约、Provider 偏好提示与当前产品文档均已删除；denylist 防止模型绕过来源合同意外带回 Pinterest。
- M94 loaded 验收：API `/health` 与 Board 均为 200，OpenAPI `ResearchSource` enum 仅为 `["xiaohongshu"]`，Live Board 模块不含 Pinterest 文案；结构化数据仍为 3 projects / 12 completed Runs / active 0。完整仓库门禁保持 331 API / 100 Board / 165 Extension / 8 packaged E2E 全绿。

## M95 launch-setting distillation and case-source audit

- 用户截图显示展开区同时呈现参考网页、文件上传、XHS 来源、三档深度和运行前检查；它把补充输入、来源策略、核心决策与技术状态放在同一层，明显压过主任务。核心动作应是选择建筑研究深度并开始，其他内容按需出现。
- 现有产品信息架构已把“建筑设计研究 / 图纸灵感”分开，因此建筑模式继续显示“小红书灵感”复选框既冗余又误导：XHS 不能证明正式案例；图纸灵感模式应自动使用 XHS-only，不再让用户理解内部来源路由。
- M95 先只读核对最近 completed Runs 的来源域名和 Trace，以及后端可信站点/查询路由。只有证据显示实际被锁定 ArchDaily 时才修改检索；若只是展示偏差，则只修界面与来源解释。
- 12 条 completed Runs 的结果域名显示：最新 Deep `76f52c79-...` 为 ArchDaily 31 / Designboom 20；最近 Balanced `d995bed5-...` 和 Quick `a2cf2e20-...` 的保留资产全部来自 ArchDaily；更早三档曾同时出现 Designboom、ArchDaily China 和 XHS。因此用户的近期观感真实，但系统并非全局 ArchDaily-only。
- 后端可信站点固定为 ArchDaily、ArchDaily China、Designboom、Dezeen、Divisare；未指定域名时 `_public_search_domains` 按 round 循环五站。需要继续核对 SourcePage/QueryAttempt，判断非 ArchDaily 候选是未被搜索、抓取失败还是没有通过正文证据门，不能用弱来源机械凑多样性。
- 只读 SQLite 审计定位真实根因：Quick `a2cf2e20-...` 的 3 个 QueryAttempt 与 Balanced `d995bed5-...` 的 4 个 QueryAttempt 全部停在 round 1，SourcePage 分别为 11/11 个 ArchDaily；当前域名函数仅按 round 取模，因此同一轮所有分支都固定 ArchDaily。Deep 进入 round 1–4 后才得到 ArchDaily 15 / Designboom 14 / Dezeen 6。应在同一轮按查询序号分散站点，而不是降低证据门或机械保留弱案例。
- Board 当前 `researchSources` 对所有 goal 默认 `xiaohongshu`，提交建筑设计时也据此要求 XHS access 并发送 XHS 来源。M95 应删掉用户可见来源选择，按模式派生：`visual_reference_search → [xiaohongshu]`，其他 goal → `[]`；重试继续尊重历史 Run 自身来源。
- M95 UI 合同采用 Impeccable `distill`：建筑设计的核心决定“研究方式”移出补充区并压成紧凑三选一；只显示每档用户收获和当前档的一句差异。补充区只保留 URL/文件，XHS 选择消失；建筑/来源反查不常显 browser preflight，图纸灵感常显紧凑“研究环境”。
- 建筑提交此前即使 `requireConnected=false`，只要 API browser 状态为 connected 仍会请求页面扩展权限并在未授权时阻塞。M95 将提交前 Chrome/XHS 硬门严格限定到 `visual_reference_search`；建筑案例研究继续走本地公开网页链，页面高清图纸读取不再成为隐藏前置条件。
- 单纯按全局 query index 轮换虽能解决首轮单站，但会让某些固定分支跨恢复轮永远错过 ArchDaily China，破坏目标站点语言路径。最终 selector 使用 `round_number + round_query_index - 2`：同轮不同查询分散站点，同一分支跨轮继续偏移，五站语言规则保持。
- 加载后的无截图 DOM 验收确认最终信息架构成立：建筑模式常显研究方式，不含 XHS 或研究环境，补充资料只有 URL/文件；图纸灵感不显示研究方式，只显示小红书研究环境。390px 与桌面宽度均无文档级横向溢出，说明减重没有把复杂度转移到窄屏。

## M96 collection mode entrances

- 用户截图确认当前收藏页虽然按“建筑方案 / 图纸灵感”分区，但两个分区仍在同一长页面里同时展开，视觉上仍是一份混合资料清单；用户希望收藏像主页启动器一样先做两个明确入口，再进入单一模式内容。
- 建筑收藏的长期价值是项目身份与研究判断，不是缩略图：模式页应保留原研究问题、项目名、项目/图纸类型之外的研究文字和来源入口，移除图片缩略图。图纸灵感的长期价值是视觉回看：模式页只显示原比例图片，帖子标题、类型与观察文字不再铺在网格下方。
- 删除与打开来源仍是收藏管理必需动作，但在图纸灵感中应作为图片边缘的低干扰操作，不能重新形成文本卡；建筑与图纸模式都需要返回收藏分类页，顶栏继续提供唯一返回主页动作。
- Impeccable product/distill 约束把本次核心目的收敛为“先选择要回看的资料类型”。收藏分类页只承担一次模式选择；进入后建筑采用平面文字台账，图纸采用原比例图片网格。两种模式不共享同一种收藏卡，也不重复另一模式所需的信息。
- 当前 Board 只有 `collectionOpen` 布尔状态，`collectionSections` 会把两种模式和题目组同时渲染；现有行为测试也把“双标题同时可见”写成旧合同。M96 只需增加一个收藏子视图状态并替换这段渲染，不改 API 或 SavedReference 数据形状。
- 现有收藏 CSS 把两类内容统一成 `preview + copy + delete` 横向行，这正是截图里的混合视觉。应分别定义建筑文字行与视觉图片格，复用现有 Lucide 来源/删除图标和全局按钮语汇，不引入新依赖或通用卡片抽象。
- 现有收藏页顶栏已有唯一“返回主页”，因此模式页再增加的返回动作必须命名为“返回收藏分类”，放在页内标题前；它不能复用顶栏动作或制造第二个“返回主页”。加载和全空状态仍留在分类层，单模式空状态进入对应模式页显示。
- 主页双入口已经提供可复用的布局、图标与响应式语汇；收藏分类不需要新视觉组件，只需同样的二列入口结构和模式计数。收藏快照已有 `question / project_name / design_mechanism / transfer_strategy / limitations / collection_file`，M96 无需扩展后端。
- 图纸模式应移除题目分组、项目名、类型和 observation 的可见重复，只在图片 `alt` 与操作 `aria-label/title` 中保留必要语义；建筑模式继续按原问题分组，并显示项目名和 `design_mechanism || note`，不读取或渲染 `collection_file/image_url`。
- 实现保持分类页在零收藏时也显示两个 0 项入口，让空状态先教会信息架构；进入空模式再显示对应的“去建筑研究选择案例 / 去图纸灵感选择图片”下一步。这样空状态与有数据状态共享同一导航，不会因第一次收藏而改变页面结构。
- Loaded 真实收藏分类页已显示且只显示两个入口：`建筑方案 2 项 · 项目与研究文字`、`图纸灵感 1 项 · 收藏图片`；DOM 中没有题目、项目或图片提前展开，顶栏仍只有一个“返回主页”。
- 1440px 真实建筑收藏页显示一个原研究问题、两个项目名、两段研究机制及各自来源/删除动作，DOM 图片数为 0；分类页与建筑页均无文档级横向溢出。该形态直接保留用户需要的“项目 + 研究文字”，没有用资产类型或缩略图重新增加噪声。
- 1440px 真实图纸收藏页精确为 1 张图片、1 个来源图标、1 个删除图标；`h2/h3`、copy block、原研究题目和 observation 文案均为 0，页面无横向溢出。项目名只作为图片 alt 与操作可访问名称存在，不形成可见文字卡。
- 390×844 override 下实际 390px client：分类入口各 176×69px，图纸来源/删除均为 44×44px，建筑来源/删除高度均为 44px；分类、建筑、图纸三页 scrollWidth 都等于 viewport，建筑内容元素无越界。图纸图片宽 365px，保持单列原比例而未被裁成固定卡片。
- 用户在首次实现后纠正交互：不要进入收藏后只看到一层空白模式选择。最终结构应默认展示建筑收藏，并把同样的建筑 / 灵感双入口常驻为页内切换器；点击只替换下方内容，不存在“返回收藏分类”。此前 index 子视图及其返回层级全部撤销。
- 修正后的 loaded 默认 DOM 符合用户描述：打开收藏即显示 2 个建筑项目与研究文字；顶部建筑按钮为 pressed、图纸按钮未选，二者同时可见；页面没有“返回收藏分类”，只保留顶栏唯一“返回主页”。
- 修正后的双向切换在 desktop/390px 都保持原位：切到图纸后两个 switch 仍在且图纸 pressed，下方为 1 图片 / 0 copy / 0 题目；切回建筑后建筑重新 pressed、0 图片。390px 两个 switch 各 176×54px，图面操作 44×44px，两个模式均无溢出。
- M96 最终没有改变 SavedReference、收藏副本、同题批次替换或删除 API；它只改变查看层。服务重载后 API/Board 200，真实数据仍为 2 个建筑收藏 / 1 个图纸收藏，说明界面分流没有迁移或重写用户收藏。

## M97 direct high-resolution collection images

- 用户截图显示图纸收藏目前只有图片和右上来源/删除图标，但点击图面本身没有高清动作。最自然的产品语义是：图面打开收藏的本地高清副本，外链图标继续打开原始小红书帖子；两者目的不同，不能共用一个链接。
- SavedReference 已优先保存 `collection_file`，Board 也已用 `/v1/collections/{id}/content` 渲染图像；因此高清入口不需要新 API、下载逻辑或站内 modal。把现有 `<img>` 包进新标签链接即可保留原始分辨率、浏览器缩放与键盘访问，缺本地副本时沿用 `image_url`。
- 当前视觉 item 的 `previewUrl` 已精确实现本地副本优先，但 JSX 只是裸 `<img>`；右上来源链接与删除是 media 容器的独立兄弟。最小修改是仅给图片增加 `collection-visual-open` 链接，不嵌套现有操作，也不改变其层级、URL 或事件。
- Loaded 真实 DOM 现在同时暴露两个不同链接：图面 `打开高清图片… → /v1/collections/317488a1-.../content`，右上来源 `打开来源… → xiaohongshu.com/search_result/...`；图片是高清链接的子元素，删除仍是独立按钮，语义没有串用。
- 真实高清链接属性为 `target=_blank / rel=noreferrer / title=打开高清图片`，cursor 为 `zoom-in`；1440px 与 390px 下 link 和 image 宽度分别完全相等（254/254、365/365），无横向溢出，页面 error logs 0。本地 Board 路径直接响应 200 `image/png`、1,547,533 bytes，证明不是缩略图占位或失效链接。
- M97 最终重载前只读守卫确认 API/Board 均为 200，持久状态精确为 3 projects / 12 Runs，12 条全部 `completed`、active 0；永久封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍为 `completed` / attempt 0 / `coverage_satisfied`。因此可以安全执行一次标准 stop/start，不需要也不得创建或 retry Run。

## M98 case-subquestion architecture collections

- 用户提供的真实收藏截图证明当前建筑视图只有“原研究题目 → 项目名 → 一段机制”，无法看出每个案例具体回答了哪个研究子问题，也没有把已收藏的项目条件、转译动作、边界或证据上下文组织成可复用资料。
- 本阶段将“相关内容”解释为收藏快照或其原始证据中已经存在、且能绑定到该案例子问题的内容；不使用模型补写，也不为了填满卡片制造泛化文字。目标结构为“原研究题目 → 案例子问题 → 项目案例”，图纸灵感高清入口与图片-only 视图不变。
- Impeccable product register 要求这类工具界面优先使用熟悉的平面层级、标准切换与一致动作，而不是嵌套卡片、装饰性动效或 modal。M98 可以提高研究信息密度，但应把层级限定为题目、子问题、案例三层，并复用现有来源/删除按钮、色彩与排版语汇。
- 源码与真实数据审计确认根因在收藏快照：`AssetCandidate` 已持有 `subquestion_ids`、逐题 `subquestion_analysis`、`project_context` 与 `evidence_claims`，Run 也保留每个子问题的完整题目；但 `save_result` 只把项目级 `design_mechanism / transfer_strategy / limitations` 写入 snapshot，Board 因此只能按原研究题目列项目名和一段机制。
- 当前两个建筑收藏仍能通过各自 `asset_candidate_id` 找到原始 completed Run 资产，因此可在不改 Run 的前提下为旧 snapshot 兼容性投影子问题包；新保存则应把每个关联子问题的题目、条件、机制、转译、边界与可绑定的逐字摘录直接持久化进 snapshot。底层 Run 后续过期时，新收藏仍保持自包含，旧收藏在原资产仍可用时获得完整显示。
- 真实关联结果精确为三个案例子问题组：De Nederlandsche Bank 关联 `public_logistics`；Instituto 同时关联 `shell_insertion` 与 `program_zoning`。每个逐题 analysis 都有独立条件、机制、5 条转译、5–6 条边界，并能找到与条件/机制逐字匹配的正式 EvidenceClaim，因此收藏无需生成或猜测任何内容。
- 默认把每个案例的条件、机制、前两条转译和第一条边界放在平面台账中，剩余转译、边界与一条机制优先的逐字原文置于原生 `details`。这既解决“只有一段话”的简陋，也避免 Instituto 两个分支一次铺开约二十条长段落；来源与删除仍在案例标题行，建筑收藏继续不显示图片。
- 实现审查否决了“精确匹配失败后取同资产任意首条 claim”的回退，因为一个资产可同时关联多个子问题，这会把另一分支证据错挂到当前分支。最终证据投影严格要求 claim statement 等于逐题 `design_mechanism` 或 `project_context`；没有精确匹配就不显示逐字证据。当前三个真实分支均有匹配，不受影响。
- Loaded 真实数据实际形成 3 个子问题 / 4 个案例关联：公众与后勤流线下有 De Nederlandsche Bank 与 Instituto，后者只有项目条件、适用边界和精确条件原文，没有机制或转译，界面按事实留空而未回退到它的外墙雨幕机制；另两个子问题分别呈现 Instituto 的壳体介入和功能共享分析。
- 桌面 1099px viewport 下四个案例台账均宽约 1007px、0 图片、4 个 details 默认关闭且无横向溢出；展开 De Nederlandsche Bank 后精确显示 3 条其余转译、4 条更多边界、原文摘录及同一 ArchDaily 证据 URL。390×844 override 下实际 clientWidth 375px，四个案例均宽 351px，来源/删除最小 44px且无溢出；唯一发现是 details summary 高 40px，需补到 44px 后复验。
- 40px summary 缺陷已由 CSS contract 红绿修复，真实 390px 复验为 summary/actions 均最小 44px、clientWidth=scrollWidth=375、details 默认关闭。切换图纸灵感仍精确为 1 图、0 建筑案例/题目，高清链接和图片均宽 365px，本地 content 与 XHS 原帖链接分离；最终切回建筑为 3 个子问题 / 4 个案例、0 图片、0 展开项，页面 error logs 0。
- 最终服务加载后，workspace 收藏 GET 已执行一次兼容升级；随后直接只读 SQLite 确认 De Nederlandsche Bank snapshot 持久保存 1 个 `case_subquestions`，Instituto 持久保存 3 个。该结果不再依赖原 AssetCandidate/Run 继续存在。最终仍为 3 projects / 12 completed Runs / active 0，封存 Run 未变化。

## 2026-07-23 local service availability recovery

- 用户提供的浏览器错误页显示 `127.0.0.1` 拒绝连接；这首先是本地监听进程缺失或退出的运维故障，不是 M98 收藏界面的应用内错误。
- 恢复边界仅是只读核对持久状态并执行一次标准 `scripts/stop.ps1` / `scripts/start.ps1`。验收必须同时满足 API/Board 200、3 projects / 12 completed Runs / active 0，以及封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍为 completed / attempt 0；不得借恢复动作创建或 retry Run。
- 只读诊断确认 5173 与 8000 均无监听，`.archresearch/dev-processes.json` 仍指向已经退出的 API pid 55764 和 Board pid 54584，因此根因是旧开发进程退出后留下了过期进程记录。
- 重启前 SQLite `mode=ro` 守卫通过：3 个 workspace、12 条 Run 全部为 completed、active 0；永久封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍为 completed / attempt 0 / coverage_satisfied。可以安全执行一次标准 stop/start，不需要也不允许 retry。
- 一次标准 stop/start 后，API 与 Board 均返回 HTTP 200；5173 由新 Board pid 45640 监听，8000 由新 API pid 45428 监听，两个进程启动时间都晚于各自最新源码。重启后 SQLite 状态仍为 3 projects / 12 completed Runs / active 0，封存 Run 完全未变。
- 最终 Git 只读守卫通过：`git diff --check` exit 0、staged 0；105 个既有 dirty/untracked 条目原样保留。恢复收口时 API/Board 仍为 200，因此用户只需重新加载原错误页即可回到应用。

## M99 architecture-collection hierarchy

- 用户截图中的内容不是缺数据，而是四个语义层主要共用同一平面、相近的粗体和连续横线：原研究题目像普通页标题，案例子问题像一条工具栏，项目名与来源操作漂在台账上方，四个字段又全部等权，因此视觉上容易被读成一段连续问题与答案。
- 本阶段不增加嵌套卡片、彩色侧条、阴影或更多徽章。Impeccable product register 与现有 ArchResearch 设计系统都要求平面结构区段；最小方向是用正确 heading 层级、子问题的完整分组面、项目标题纸头和字段标签轨的字阶/留白差异，让层次来自结构而非装饰。
- 需要特别验证同一子问题下两个项目的归属边界；截图当前只完整显示第一个项目，如果只强化单项目标题而不包住整个子问题组，第二个项目仍可能看起来属于下一个问题。
- 当前 JSX 的 heading 顺序其实已经是 `h2 → h3 → h4 → h5`，问题主要不在 HTML 标题级别，而在缺少可见的“原研究题目 / 项目案例”层级标签、question/subquestion 没有命名分组，以及 CSS 中四级内容共享相近字号、边线和连续纵向节奏。
- M99 最小结构决定：外层研究题目和每个案例子问题成为有可访问名称的平面 region；补齐可见“原研究题目”和“项目案例”标签；外层只使用一张问题工作纸，子问题用整行浅色章节头，项目仍是无圆角、无阴影的平面区段。字段台账保留 112px 标签轨，但正文提高到主墨色并用独立标签轨背景/结构线分开，不增加嵌套卡片。
- M99 行为红测有效：旧 DOM 虽有 h2/h3/h4/h5，但找不到名为“原研究题目：…”的 region，失败输出也确认没有可见“原研究题目 / 项目案例”标签。测试在任何生产代码改动前失败，准确锁定结构层级缺口。
- M99 核心红绿已转绿：研究题目和案例子问题现在是命名 region，可见标签补齐“原研究题目 / 项目案例”；视觉上只增加一个外层问题工作纸，子问题使用整行浅色章节头，项目标题和来源动作位于独立纸头，字段台账通过 112px 浅色标签轨与主墨色正文分开。图纸收藏 JSX/CSS 未改。
- 扩大 Board 门禁通过：101/101 tests、ESLint、TypeScript、production build 全绿，Impeccable detector 返回空数组。真实桌面 DOM 为 1 个原题 region / 3 个子问题 region / 4 个项目，字号形成 24px 原题、20px 子题、16px 项目、12px 字段标签与 14px 正文的清晰阶梯；唯一外层工作纸宽 1007px，页面 clientWidth=scrollWidth=1084，无横向溢出。
- 390px 无截图复核通过：外层工作纸宽 351px，题目/子题/项目和字段轨均折为单列；来源、删除与 details summary 实测最小 44px，页面 clientWidth=scrollWidth。图纸态仍精确为 1 图、0 建筑 region，本地高清 href 不变；切回建筑恢复 1/3/4 层级与 0 图片。
- M99 权威完整门禁通过 331 API / 101 Board / 165 Extension / 8 packaged E2E，全部静态、构建、安全与 evaluation 检查全绿。active 0 下最终标准重载后，新进程晚于对应源码，API/Board 200，3 projects / 12 completed Runs / active 0 与封存 Run 均未变化。
- 最终 1280×720 loaded DOM 保持 1 原题 / 3 子题 / 4 项目、0 建筑图片、0 默认展开详情与 0 error logs；clientWidth=scrollWidth=1265。M99 没有为了强化层级增加弹窗、嵌套卡片、彩色侧条、阴影或新数据字段。

## M100 continuous case collection with project images

- 用户对 M99 的真实 loaded 截图给出明确否决：外层边框、浅色子问题带、四行带底色的字段轨、summary 和展开后的第二组字段共同形成连续矩形块，虽然层级明确，却像“一块一块的布丁”，阅读被容器切断。
- 用户同时推翻两个先前决定：建筑收藏不再是 0 图片；“查看完整相关内容”也不再需要。M100 必须把全部转译、全部边界和逐字证据直接整合到项目档案正文，并恢复少量项目案例图片。
- 最小且诚实的图片规则暂定为每个项目最多三张已有正式案例图片，只用于识别项目与回看，不证明机制；不得混用 XHS visual lead，也不得为凑图启动新检索。实现前先只读核对当前 SavedReference 本地副本、snapshot image 字段和原 Run 同项目资产，决定是否需要耐久 `case_images` 快照扩展。
- Impeccable layout/product register 要求此次用留白、行宽和少量细分隔线形成连续叙事，不再用工作纸包工作纸、字段表格或视觉 badge。项目档案可以采用桌面“图片轨 + 正文”结构，窄屏改为图片在前、正文在后；缺图时正文自然全宽。
- 真实数据审计证明可以满足“几个案例图片”而不新抓取：De Nederlandsche Bank 在同一 completed Run、同一精确 `project_name` 下有 7 张正式项目资产（剖面、立面、总平面），Instituto 有 1 张 photograph；全部已有 `image_url/source_url/asset_type`，均非 XHS，但当前 snapshot 只保存被收藏候选的一张 `image_url`，没有 `case_images` 列表。
- 两个真实建筑收藏当前都没有 `collection_file`，相关资产也没有 `storage_path`，所以不能伪称本地副本；最小耐久边界是把最多三张现有同 Run/同项目图片的 URL、来源、类型与 asset ID 写入 snapshot。选图先保留用户实际收藏的资产，再按 rank 补充不同图纸类型，最后才补同类型，严格去重 URL。
- M98 legacy upgrade 只在缺 `case_subquestions` 时读取原资产；M100 必须把 `case_images` 升级判定独立出来，使已经拥有逐题包的旧收藏也能一次性补图并持久化。Run 过期后 snapshot 仍保留图片引用；当前没有本地图时诚实使用远程图片 URL。
- M100 两端红测有效：API 在 `snapshot["case_images"]` 处明确 `KeyError`；Board 在第三条转译仍位于关闭的 `<details>` 中时 `toBeVisible` 失败。失败 DOM/响应保留现有数据，分别锁定“没有多图快照”和“完整内容仍被折叠”两个真实缺口。
- M100 核心红绿已转绿：API 保存与旧收藏读取都持久返回最多三张 `case_images`，当前收藏图优先、不同图纸类型优先且 URL 去重；Board 使用本地首图副本优先、其余已有远程图回退，并在同一连续 story 中直接显示全部条件、机制、转译、边界和精确原文，DOM 不再有 details/summary。
- M100 扩大门禁全绿：101 Board tests、27 个 API collection/schema 邻接测试、Ruff lint/format、strict Mypy 18 source files、ESLint、TypeScript、production build 与 Impeccable detector 0。响应式合同已从已删除的 summary 触控目标改为 ≤860px 图片轨/正文单列。
- 真实收藏 GET 已把 `case_images` 独立懒升级并持久化：De Nederlandsche Bank 为总平面图、剖面图、立面图 3 张，Instituto 只有来源实际具备的 1 张项目照片；直接只读 SQLite 得到相同计数。没有新抓取、没有用 XHS 补图，也没有把远程 ArchDaily URL 误称为本地副本。
- 无截图桌面 DOM 验收为 1 个原题 / 4 个案例关联 / 6 个实际图片实例；所有图片 `naturalWidth/naturalHeight > 0`，0 个 `details`/summary、“查看完整相关内容”与“更多”标题均不存在。问题与子问题容器的计算背景为透明、border 为 0，图片/正文使用 340px + 剩余宽度双区，页面无横向溢出、error logs 0。
- 390×844 override 下四个案例双区全部折为 351.2px 单列，图片轨与案例容器同宽，6 张图全部加载；来源、删除和证据来源操作均为 44px，clientWidth=scrollWidth=375。图纸灵感回归仍为 1 图 / 0 建筑 region，本地高清 href、`target=_blank` 与 `rel=noreferrer` 未变；最终已切回建筑并 reset viewport。
- PRODUCT、DESIGN 与 architecture 已从 M99 的工作纸/112px 字段轨/disclosure 合同更新为连续文档流、全部内容默认可见、最多三张 typed 正式案例图片及“图片不证明机制”的证据边界。
- M100 权威完整门禁明确通过 331 API / 101 Board / 165 Extension / 8 packaged E2E，连同 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 和 30/108 evaluation 全绿。active 0 下最终标准重载后 API/Board 均为 200，新进程晚于对应源码，最终 loaded 收藏页仍是 1 原题 / 3 子题 / 4 案例 / 6 图片实例、0 disclosure、0 error logs。

## M101 solution-first architecture collections

- 用户对 M100 loaded 截图的否决是信息任务不聚焦：340px 左侧多图像附件栏，右侧同时显示项目条件、空间机制、5 条转译、完整边界与证据，导致“怎么解决当前案例子问题”仍埋在研究档案里。问题不只是样式，而是收藏页继续承担了证据审计页的职责。
- 本阶段唯一核心目的固定为让用户一眼看懂该案例如何解决当前子问题。保留项目身份、来源、删除和几张已有案例图片；收藏表面只显示明确的核心解法与可执行步骤。项目条件、长边界清单和逐字证据继续保留在 durable snapshot 与原来源中，但不在收藏页重复展示。
- Impeccable distill/product register 指向扁平、全宽、线性阅读流：移除 340px 固定侧栏与研究字段堆叠，图片改为紧凑的上下文图带，核心解法成为唯一正文重点，步骤使用真实有序序列。不得新增卡片、折叠控件、弹窗或装饰层。
- 现有 M100 DOM 顺序是图片侧栏在前、完整研究 story 在后；CSS 把所有图片锁在 240–340px 左列，首图再跨两列，因此截图形成一块高而重的附件墙。最小结构应改为 answer-first DOM：`核心解法` → 最多三条 `怎么做` → 紧凑案例图带；来源/删除仍在项目头。
- 收藏行为 fixture 已有完整 project context、mechanism、3 条 transfer、2 条 limitation 和精确 evidence，适合直接固定 M101 合同：机制与步骤继续可见，项目条件/边界/逐字证据均不进入收藏 DOM，三张图片和图纸灵感回归不变。
- M101 最小实现不碰 grouping、snapshot 或 API：每个已有案例关联仍可被删除和打开来源；有 mechanism/transfer 时显示命名解法 region，无可复用解法时只显示诚实短提示。步骤使用已有 `uniqueSummaryItems(..., 3)` 去重并封顶三条，不生成或改写研究内容。
- 图片从 answer 之前的 340px 固定侧栏移到 answer 之后，使用 160–220px auto-fit 图带；三个真实图在宽屏不再撑成高附件墙，窄屏可自然折行。图片链接、类型 caption 与本地首图回退均未改变。
- Loaded desktop 验收确认首案例解法宽 672.75px、高 320px，位于图片区之前；三张图各约 218×164px 横排，整段图片高约 185px。真实页面为 4 个案例关联，其中 3 个有核心解法且各 3 步，1 个只有诚实缺失提示；0 旧 story/evidence/details、0 overflow、0 error logs。
- 390px 首次验收发现 auto-fit 按 220px 最大轨计算为单列，三张图因此形成约 570px 高图带，虽已在答案之后但仍偏长。应在 ≤620px 明确使用两列等宽缩略图，让三图变成两行；来源/删除已实测全部 44px，解法宽 351.2px、无溢出。
- 两列移动图带修复后，390px 实测为 171.6px + 171.6px 两轨，三张图各约 170×127.5px、图带总高 305px，较单列减少约 265px；全部图片加载且 overflow 0。图纸灵感切换仍为 1 图 / 0 建筑案例，本地高清 href 未变。
- M101 权威完整门禁明确通过 331 API / 101 Board / 165 Extension / 8 packaged E2E，连同 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。最终 active-0 重载后的真实收藏页仍为 1 原题 / 3 子题 / 4 案例 / 3 解法 region / 每项 3 步 / 6 图，旧 story/evidence/details、overflow 与 error logs 均为 0。

## M102 enlarge collection imagery

- 用户要求建筑收藏和图纸灵感图片都再大一点；M101 的解法优先顺序、最多三步和审计字段隐藏均保持。本阶段只调整图片网格，不改 JSX、快照、来源、删除或高清链接。
- 目标尺寸先按现有工作面推导：建筑桌面三张图从约 220px 增至约 300px，仍可在 1007px 内容宽内完整横排；390px 采用首图全宽、其余两图并排，在显著放大主图的同时避免三张全部纵向堆叠。图纸灵感桌面通过提高列最小宽度减少列数，移动端单图全宽。
- Impeccable layout/product register 要求这是可预测的结构性 breakpoint 变化，不使用图片缩放动效、横向滚动、复杂 masonry 或新容器。
- 当前 CSS 精确基线为建筑 `auto-fit minmax(160px, 220px)`、图纸灵感 `auto-fill minmax(220px, 1fr)`，手机建筑固定两等列、图纸灵感单列；因此只需改四条网格规则，无需 JSX。
- Loaded desktop 建筑收藏实测为精确 300px × 3 轨，图片内容各约 298.4×223.8px；相比 M101 的 218.4×163.8px，宽高各增加约 37%，单图面积约增加 88%。6 图全部加载，解法/三步合同、overflow 0 与 error logs 0 保持。
- 图纸灵感桌面网格已从约四个 220px 起步列收敛为两条 503.1px 轨，单个真实收藏 media 宽 503.1px，本地高清 href/target/rel 保持。切换后的首个同步读发生在图片解码完成前，img 高 0/loaded false；需要等待该唯一图片变为 visible/naturalWidth>0 后再记录最终尺寸，不能把瞬时状态判为产品失败。
- 等待唯一图片可见后，图纸灵感真实尺寸为 image 501.5×520px、media 503.1×521.6px，源图自然尺寸 1080×1440，loaded true、overflow 0。本地高清链接继续指向 `/v1/collections/317488a1-27a9-4d5f-80fc-cf678bbc10fe/content`。
- 390px viewport override 后界面按默认行为回到建筑收藏；基于 fresh snapshot 实测首图约 349.6×262.2px 跨两列，后两图各 170×127.5px 并排，6 图全部加载、solution regions 3、overflow 0。相比 M101 手机所有图 170px，主图宽度约翻倍。
- 390px 图纸灵感保持单列全宽：grid 366.4px，image 约 364.8×486.4px，源图加载成功且高清本地 href 不变，overflow 0。两种收藏完成后已切回默认建筑并 reset viewport。
- M102 权威完整门禁明确通过 331 API / 102 Board / 165 Extension / 8 packaged E2E，连同 Ruff/format、strict Mypy、两端 lint/typecheck/build、process/security 与 30/108 evaluation 全绿。active-0 最终重载后桌面仍为三条 300px 建筑图轨、首图约 298×224px，6 图 loaded、3 solution regions、overflow/error logs 0。

## M103 architecture collection question directory

- 用户要求建筑收藏默认先进入问题目录，再点击查看具体案例子问题详情。最小解释是目录项以 `case_subquestions` 为单位，而不是再增加一层原研究题目选择；每项同时显示所属原研究题目和已收藏案例数，避免丢失上下文。
- 详情页只渲染被选中的一个子问题及其现有 solution-first 案例；提供明确“返回问题目录”。顶部建筑方案 / 图纸灵感切换继续常驻，切换类型、离开收藏或重新进入时清空选择并回到目录。
- 不采用 modal、URL 路由、accordion 或持久化选择；这是收藏页面内部的一层可逆本地状态。M101/M102 图片与解法布局完整复用。
- 行为红测准确命中旧 DOM：建筑收藏仍直接渲染两个 fixture 子问题和两套案例内容，找不到“建筑问题目录”。最小实现只增加 `{ collectionQuestion, subquestionId }` 本地选择状态，并从现有 `collectionCaseGroups` 展平目录；没有改变 grouping、snapshot、API 或删除/来源行为。
- 真实数据目录为 3 项，分别保留原研究题目与 2/1/1 个已收藏案例计数。桌面目录按钮约 146px 高、1099px 页面无溢出；390px 下长中文问题自然增高到约 230px，三行均宽 351px、初始 0 案例详情/0 图片且无页面溢出。
- 点入真实首题后只存在 1 个子问题 region、2 个案例和 4 张现有案例图；390px 返回按钮为 44px，首图约 350px、次图约 170px，scrollWidth 等于 clientWidth。切到图纸灵感时本地高清 `/v1/collections/317488a1-27a9-4d5f-80fc-cf678bbc10fe/content` 仍在，切回建筑后详情选择清空并恢复目录。

## M104 clarify supplementary inputs

- 用户截图显示“补充资料”展开后只有“参考网页”与“上传草图、图片或 PDF”两个名词标签，没有解释何时使用、系统如何使用、不填是否影响研究。用户正准备发起建筑研究，属于有具体问题但不确定是否需要准备资料的首次任务心智；缺失的是决策帮助，不是更多功能。
- 当前真实数据流比初始假设更窄：提交前，网页和文件先保存为 workspace `InputArtifact`；所有历史 artifact 都进入后续研究上下文。URL 只以 `Reference URL` 字符串影响 planner/query，不保证优先访问或核验该页；PDF 最多抽取 2,000 字文本进入上下文；普通图片只以文件名进入上下文。只有 `source_lookup` 会取 workspace 最新上传图片做反向搜索。因此界面必须称 URL 为“研究线索”，不能承诺“优先核对”；上传说明应区分 PDF 补充条件、JPG/PNG 用于来源反查。
- 两者均可不填。普通建筑研究仍根据问题和既有 workspace context 规划/检索；当前 UI 的“补充资料”与字段标签没有说明 optional、持久 workspace 范围或具体用途，是截图困惑的直接原因。
- 最小界面方案应保留现有折叠与两个字段，只把入口和字段文案改成可行动语言，并在输入前给出短说明。禁止新增 tooltip、modal、设置卡、上传分类或第三个入口。
- Loaded desktop 可访问树已把入口、总说明、两个标签与两条帮助文本完整暴露；URL 和 file input 分别通过 `aria-describedby` 绑定对应说明。当前 1084px viewport 下资料区宽 941px，两字段各 463px，intro 跨两列，页面 scrollWidth 等于 clientWidth；没有增加卡片或额外控制。
- 390px fresh DOM 保持完整语义，资料区正确折为 306px 单列，总说明约两行 38px，URL 帮助一行、文件帮助两行，页面无横向溢出。入口按钮已为 44px，但 URL/file input 实测仅 39/36px，低于既有 compact-screen 44px 触控合同；需补一个窄屏 CSS 行为红测并只提高这两个输入。
- Compact 修复后同一真实 390px 页面两个 input 均精确为 44px，资料区仍是 306px 单列，document scrollWidth 375px 等于 clientWidth；viewport override 已 reset。文案和触控修复都不影响主问题、研究方式、来源反查或开始研究。

## M105 scenario-mapped supplementary inputs

- 用户截图证明 M104 只把通用资料说明写清楚，却在“来源反查”已选中时仍显示“添加资料（可选）”和混合的 PDF/JPG/PNG 字段。当前模式、输入用途和主动作没有形成一一对应关系，这是新的直接困惑来源。
- 已审计的真实能力边界决定三个场景必须分开：建筑研究可消费 PDF 文本和 URL 研究线索；来源反查需要 workspace 中最新上传的 JPG/PNG，并可把疑似来源 URL 作为核对线索；图纸灵感当前不会把用户上传图片作为视觉参考，因此不应展示这组资料输入。
- 最小方案不改 API、artifact 或 workflow，只让启动器按 `goal` 渲染场景专属字段与 CTA。建筑资料仍可折叠且明确可选；来源反查资料常显并以图片为必需任务输入；图纸灵感隐藏整组资料。
- 为防止 UI 隐藏而请求仍携带旧值，研究场景实际变化时必须同时清空 `referenceUrl`、`files` 并收起建筑资料区。这是场景隔离，不是新增持久化状态。
- Loaded desktop 验收确认建筑资料仍是 941px 两列、每列约 463px，PDF accept 与两项 `aria-describedby` 正确，0 overflow。切换来源反查后建筑 URL 清空、建筑 region/按钮消失，反查图片区常显且 `required=true`、`multiple=false`、accept 仅 JPG/PNG，疑似 URL 为空，CTA 为“开始反查”。
- 图纸灵感真实 DOM 为 0 file input、0 建筑/反查资料 region、0 项目资料按钮，同时仍保留“来源反查”辅助入口和“查找灵感”主动作。390px 下建筑与反查资料都折为 305.6px 单列，两个输入均 44px，页面 overflow 0；viewport 已恢复到 1099×986。

## M106 project brief as research boundary

- 用户进一步校正“项目任务书”的心智模型：它不是普通附件或条件补充，而是研究开始前的边界上下文。系统阅读任务书中的场地、功能与限制后，应以这些内容收束问题拆解和案例检索范围，再结合用户的研究问题展开。
- 当前真实链路已支持这一语义：PDF 文本进入 workspace research context 并参与 planner/query；但界面只写“补充场地、功能、面积等项目条件”，没有说明这些条件如何改变后续思考范围。
- 最小且诚实的文案方案是把入口具体化为“添加任务书或案例页（可选）”，展开说明区分两种用途；任务书帮助文本明确“读取 → 作为拆题与检索边界”，案例 URL 继续只称研究线索。不能承诺上传后先生成独立分析、替用户改写问题或展示中间思考成果，因为当前请求仍在点击开始后一次执行。
- Loaded 用户页签已经暴露完整的可访问因果链：折叠入口直接点名任务书/案例页；展开首句区分“收束研究范围 / 补充参考线索”；PDF 的 `aria-describedby` 明确系统读取场地、功能与限制，并把它们作为问题拆解和案例检索边界。入口、标签与说明没有使用“AI 思考成果”等无法验证的内部过程词。
- 真实切换隔离保持：来源反查常显自己的图片 region 和“开始反查”，0 任务书收束文案；图纸灵感为 0 建筑/反查资料 region、0 任务书文案，保留“查找灵感”且页面 overflow 0。

## Completion roadmap assessment

- 当前功能实现已经足够支撑本地单用户 Beta；下一阶段的最高价值不是继续增加结果页模块，而是让任务书真正形成“读取项目条件 → 用户确认研究问题 → 再创建 Run”的可见闭环。M106 目前只把任务书文本送入同一次研究请求，没有独立的问题确认阶段。
- 真实能力的最大证据缺口是来源反查：保留的 12 条 Run 中没有 `source_lookup`，TinEye 的正式能力仍需要用户自己的 Key 和一次明确批准的 Live acceptance。缺少 Key 时只能诚实显示 fallback/partial，不能声称来源已被验证。
- 外部验证应先做 8–10 题小样本和六名目标学生测试，再决定是否执行完整 30 题付费矩阵；108 个分类样本仍是 deterministic synthetic fixtures，不能替代 100+ 权利清晰的真实图片集。
- 工程风险集中在大文件和交付状态，而不是现有回归门禁：`App.tsx`、`styles.css`、`workflow.py` 已成为维护热点；发布证据停留在 2026-07-16，当前最终源码和 331/103/165/8 门禁尚未形成可复现的版本化发布。
- 完善顺序固定为任务书问题确认、真实来源反查、目标用户验证、无行为重构与覆盖率、发布收口。工作区删除、自动 StyleProfile 与真实图片大样本属于后置项，不应混入首个产品阶段。

## M107 real brief fixture: Gengzhi Tu translation

- PDF 共 3 页。任务对象是正在实施中的苏州科技馆，设计目标为长江流域蚕桑丝织文化的智慧博物馆；成果强调虚实空间结合、空间模式创新、感知与交互方式、关键细节，并最终提交 A3 横版图册和 VR 体验短视频。
- 任务书对《耕织图》的定义不是单纯装饰画：它同时是科技、说明、叙述和场景图像，具体包含室内陈设、庭院景观、生产行为、生活场景和祀谢仪式。宋摹卷以长廊式连房和廊柱分割单元，把环环相扣的劳动流程嵌入空间；展开长卷本身被比作观看展览。
- 因此用户问题的关键不是“二维图案怎样立体化”，而是“二维图像中哪些关系具有空间生成能力”。可转译层级应从低到高区分：视觉元素（线、色、密度）、构成规则（分段、连续、重复）、空间原型（廊、院、室、节点）、行为流程（工序与人物移动）、时间/交互（展开、触发、反馈）。
- 适合本任务的主策略是关系转译而非形似复制：先把图像拆成空间—人物—器具—动作—工序图谱，再从长卷连续性、廊柱节奏、单元串联和生产/生活叠合中抽象规则，生成可行走、可触发、可验证的三维空间序列。
- M107 应先展示“任务书提取的项目边界”，再生成可编辑的问题目录。首版目录可为：工序如何成为参观序列；长廊分段如何成为空间语法；人物/器具/场所如何形成互动节点；二维叙事如何加入时间形成四维体验；转译结果如何通过模型、动线和交互原型验证。
- 该真实样本暴露了一个必要产品边界：系统必须把“直接提取图案”“提取形式构成”“提取行为与关系”作为不同深度，明确推荐后两者，避免输出贴图、仿古构件或任意造型类比。

## M107 implementation architecture

- 当前 `apiClient.startResearch()` 会顺序注册 URL、上传文件并立即 POST `/runs`；Board 的 `handleResearchSubmit()` 只有这一个提交出口。因此 M107 必须在客户端启动器中引入独立的 brief-review 状态，不能只改文案。
- 最小且不污染持久数据的边界是新增一个非持久化 multipart `brief-review` endpoint：接收 PDF、主问题和研究档位，在内存中提取文本并调用现有 planning provider，返回项目摘要、项目边界和 3/4/6 个子问题；该请求不注册 artifact，也不创建 Run。用户确认后才沿用现有 input upload + Run POST。
- 为让用户编辑后的问题真正进入研究，`ResearchSpec` 需要接受可选且数量与档位一致的 typed `subquestions`；`create_run()` 将其写入 `ResearchRun.subquestions`。现有 workflow 已优先使用 checkpoint/existing subquestions，因此不需要新增工作流分支，只需保留其规范化合同。
- `ResearchPlan` 可向后兼容地增加默认空的 `project_summary` 与 `project_boundaries`。OpenAI planner 在有建筑任务书上下文时结构化返回两者；现有 Run 规划继续只消费 subquestions。Mock provider 对真实《耕织图》fixture 返回确定性摘要、边界与问题目录，普通旧测试保持原计划。
- 前端使用启动器内联第二步而非 modal：展示摘要、边界和可编辑问题行；用户可编辑、删除、补充，确认按钮仅在问题数量符合当前档位且内容非空时可用。预分析失败保留原输入并提供“跳过整理，直接研究”。
- 最终实现保持该边界：`/brief-review` 最多读取 12,000 字且只在内存中处理；确认时 `ResearchSpec` 校验问题数与唯一 id，现有 workflow 的 checkpoint-first planner 原样消费已确认目录。没有新表、列或迁移。
- Loaded 实机未触发 preview Provider：真实 PDF 和主问题只装入本地表单，用于验证 CTA 与响应式输入，没有点击“读取任务书并整理问题”，因此没有网络上传、Provider 消耗、artifact 或 Run。桌面资料区 941px/两列、文件输入 463×36、CTA 162×44；390px override 下资料区和 CTA 306px 单列、文件输入/CTA 均 44px、页面 overflow 0。viewport 已恢复，error logs 0。

## M107 corrected task-brief flow and live evidence

- 用户否决了可见的任务书摘要、边界与问题编辑步骤：这些属于研究内部推理，不是一个新功能界面。该决定覆盖本文件此前“先展示边界、再确认问题”的产品建议；正确交互是同一条普通研究、一次“开始研究”、最后显示既有结果页。
- 后端非持久化 `/brief-review` 仍是合适的内部边界：它必须先成功生成与档位一致的 typed subquestions，随后同一提交才上传 PDF 并创建 Run。这样既能让任务书约束研究，又不会把内部拆题暴露为用户工作流；失败时停止创建 Run并保留输入，不能静默绕过任务书。
- 真实 Standard Run `ff16988d-ebbd-4da4-b5e2-3cd080dcf0be` 证明链路成立：3 页 PDF 只持久化一次，4 个子问题覆盖空间原型、工序序列、元素语法和时间性交互，Trace 明确记录 `planner: checkpoint`。最终为 28 个 usable assets、8 个项目、4/4 覆盖、107 条证据声明，并以 `completed / completion_satisfied / attempt 0` 结束。
- 结果诚实指出案例不是《耕织图》的版本证据，只能支持受限的空间转译机制；交互触发也仍是设计推断。这说明任务书确实改变了研究边界，而非只作为附件保存。

## M112 global project-preview copy deduplication

- 用户截图显示同一项目的两张剖面图各自重复完整空间机制，以及同一句“当前记录未保留这张图的中文图面观察”缺省说明。重复来自建筑结果的项目预览模板，不是 M107 数据特例，因此必须在全局结果投影层修复。
- 正确信息所有权是“项目组承载共同机制与共同缺省说明，单图承载图纸类型、独有观察、相关度和操作”。不能用隐藏第二张图的方式规避，也不能删除 Inspector、来源、对照或收藏所需的逐图数据。
- Impeccable distill/product 规则支持该收束：重复信息只说一次，项目档案保持平面结构，图片组仍按原始顺序和比例显示；不新增卡片、折叠层、字段或数据迁移。
- 全局投影规则现以项目档案为信息所有者：多图标题若已由本题项目分析表达则省略，其他重复标题与重复观察上提到一条平面式共同说明；不同图纸类型产生的 legacy 缺省观察统一为一句“这些图”说明。只有唯一且不与项目分析重复的标题/观察留在对应图片，空 caption 不渲染；单图走原样分支。
- 真实 M107 Standard 结果包含 11 个项目档案，其中 5 个多图、6 个单图。多图的 2/3/4 张证据均保留逐图检视、相关度与对照操作，具体缺省观察为 0、项目级缺省说明各 1；全部单图仍有原标题和观察。桌面与 390px 均无横向溢出，移动操作 44px，error logs 0。

## Post-M112 completion audit

- 当前没有 `in_progress` 阶段。数据库只有 9 条 completed 建筑研究和 4 条 completed 图纸灵感研究，`source_lookup` 为 0 条，因此来源反查虽有 TinEye/fallback 实现、Mock 和失败边界测试，仍没有真实功能验收证据。
- 当前工程门禁很强但维护性风险集中：`App.tsx` 3,662 行、`styles.css` 4,045 行、`workflow.py` 4,846 行；`pytest-cov` 已是开发依赖，但默认 `scripts/verify.ps1` 没有输出或约束覆盖率。重构必须先做行为 characterization 和覆盖基线，不能直接大拆。
- 发布复现仍是明确缺口：仓库没有 `.github` CI；README 只有当前机器的 setup/start/verify 步骤，没有干净 Windows/Chrome 环境的安装、更新和回滚验收记录。最终发布证据仍指向历史阶段而非 M112 源码状态。
- 更新后的完成度判断为：本地单用户 V2.1 约 87%，作品/受监督演示约 92%，小范围内测约 83%，公开可复现发布约 60%。增长来自真实任务书 Standard 闭环和结果多图收束；公开发布比例未提高，因为 CI、clean-machine 与授权版本仍未完成。

## M113 source removal, case selection, and history controls

- 用户明确删除来源反查，因此 M108 真实 TinEye 验收提案被产品决策取代；当前 0 条 source-lookup Run，无需迁移或删除历史数据。实现必须移除当前启动器、Pydantic/TypeScript goal contract、workflow 路由、TinEye 配置/客户端、测试和现行文档，不能只隐藏按钮留下不可达功能。
- 建筑收藏的持久语义已是“原研究题目 → 案例子问题 → 项目案例”，而结果页仍把选择/收藏绑定到 `AssetCandidate` 图纸。正确选择单位应是当前子问题中的项目 dossier：同一项目在不同子问题可分别选择；一次选择保存一条代表 asset 作为现有 API 锚点，同时 snapshot 继续携带该子问题研究包和最多三张同项目正式图片。图纸灵感仍按单图选择。
- 用户截图显示的是首页“研究项目”原生 `<select>` 的展开态，而不是“查看全部历史” dialog。系统蓝色选中条、过大的菜单宽度和紧邻最近研究标题使层级混淆；修复应使用当前产品控件语汇提供有界项目菜单，并同时回归完整历史弹层的视口高度、焦点返回、永久保留动作与 390px 无溢出。
- M113 已完成产品级删除：当前代码、配置、文档和 25 条评测任务不再接受来源反查或 TinEye；仅保留 API 拒绝旧 goal 与 Board 不显示旧入口的防回归断言。OpenAPI `ResearchGoal` 只含 `precedent_research` 与 `visual_reference_search`。
- 建筑结果现在在项目档案标题选择代表 asset，一次选择保存该项目现有的全部案例子问题包与最多三张正式案例图；图组内逐图选择按钮为 0，但每张图的证据检视入口保持。真实 M107 选择后显示“已选 1 个项目案例”；图纸灵感真实结果仍有 22 个逐图选择入口。
- 首页项目控件改为 3 项 `menuitemradio` 的有界菜单，支持当前项、点击外部和 Escape 关闭；完整历史弹层显示具体项目名与 11 条当前项目记录，关闭按钮有明确 accessible name。390px 下弹层左右边界为 16px、列表独立滚动、页面无横向溢出。

## M114 project taxonomy audit

- 首页菜单展示的是人工创建的 Workspace 名称，不是系统按研究内容生成的分类。把它们视觉上并列为“研究项目”会让用户误以为三项具有一致、可解释的项目边界。
- `旧厂房更新 · 历史研究` 只有 2 条 precedent Run，brief 明写“验证……研究闭环”，本质是历史验收容器；`旧工业建筑 · 结构与功能植入` 为 0 Run 空 Workspace；二者都不应与真实设计项目等权出现。
- `旧厂房社区文化中心` 有 11 条 Run（7 precedent、4 visual），但最新的《耕织图》任务书研究实际属于“苏州科技馆 · 蚕桑丝织智慧博物馆”。当前项目边界因此已被混用，菜单名称无法解释全部内容。
- 正确的信息单位应是“真实任务书/设计委托对应一个项目，研究问题作为项目内 Run”。建议保留旧厂房项目，单独建立苏州科技馆项目并迁移 M107 Run 与任务书；历史验证 Workspace 应进入归档视图，空 Workspace 是否删除需用户明确批准。

## M115 project reorganization and M116 collection root cause

- M115 的显式 Workspace 归档状态、活动/归档项目菜单分组和归档只读视图已经实现；迁移/API/Board 聚焦测试、Ruff、strict Mypy、Board 108 tests、lint 和 typecheck 已通过。真实数据事务创建 `苏州科技馆 · 蚕桑丝织智慧博物馆`（`56d1ab66-2cad-44a6-9382-bec37664f8aa`），迁入 M107 Run、1 份任务书和 2 条关联收藏；原验证 Workspace 改名 `历史研究归档` 并归档，零内容 Workspace 已删除。当前为 2 个活动项目 + 1 个历史归档、13 completed Runs、active 0，封存 Run 未改变。
- 当前 `comparisonIds: string[]` 同时承担方法对照、Board 持久选择和批量收藏。建筑 dossier 用代表 asset ID 作为键，因此同一 asset 出现在多个子问题时会联动勾选、两个子问题被去重成一个计数，并且 `saveResult(assetId)` 会把该 asset 的全部 `case_subquestions` 写入快照。
- 正确合同是“收藏选择按关联、方法对照按唯一 asset”：建筑键必须包含 `subquestionId + assetId`，图纸灵感仍按 asset；保存前按 resultId 分组并显式传递所选 `subquestion_ids`。同 asset 的两个子问题应显示 2 个选择，但方法对照只保留一个 asset 列。保存成功后同时清空关联选择、图片选择和持久 Board selection。
- 收藏详情截图的问题是布局而非内容缺失：长问题标题使用 24px 且项目正文纵向铺排，宽屏形成左侧窄正文、右侧大片空白，图片被压到下方。应使用现有固定字阶和间距，把案例正文与 1–3 张图片在宽屏组织为两列，≤1180px 恢复单列，不新增卡片或折叠层。
- M115 loaded QA 证实项目菜单为 2 个“当前项目”加 1 个“历史归档”；归档视图显示 2 条 Run、0 个“新建研究” region，834px 默认视口 overflow 0。苏州项目显示唯一 M107 Standard Run 和 2 条建筑收藏。
- M116 真实 M107 结果中，香港大学项目同时出现在第 1、2 子问题。两个按钮初始 `aria-pressed=false/false`，点击第 1 题后为 `true/false`，底部显示 1 个项目案例，证明选择键已按子问题关联分离；没有点击保存收藏。
- 收藏详情在 1440px 的 grid 列宽为 815.5px / 489.3px、间距 24px，单张案例图宽 488px、问题标题 20px、overflow 0；390px 恢复 351.2px 单列，图片宽 350px，来源和删除命中区均 44px、overflow 0。
- M116 后端 `SaveCreate.subquestion_ids` 为可选兼容字段：显式提供时严格校验属于当前 asset 的案例关联并精确替换快照子集；省略时首次收藏保持旧的全关联行为，已有子集的备注更新保持原子集，避免 Inspector/备注路径扩回全部问题。
- M116 前端建筑选择以关联键计数、按 resultId 分组发送一次；方法对照和 Board 仍只持久唯一 asset ID。成功保存后建筑关联、图纸 asset 选择和 Board selected IDs 全部清空；同题替换不会删除本轮刚更新的同 ID SavedReference。

## M117 question-derived history record names

- M115 的 `archived_at` 是通用数据库字段，但当前唯一入口是无业务规则的通用 Workspace PATCH；它接受客户端任意时间戳，也不检查活动 Run 或是否会把全部项目归档。前端只按字段分组，没有归档/恢复动作，因此真实使用仍是一次性数据整理。
- 正确归档单位仍是 Workspace/设计项目，不是完成 Run 的 14 天保留策略。归档必须是显式、可逆、不删除内容的状态迁移：活动 Run 阻止归档；至少保留一个当前项目；恢复不改 Run、artifact、collection、evidence 或 retention。
- UI 应在每个项目菜单行提供独立的归档或恢复命令；归档当前项目成功后切换到另一个当前项目，归档页显示真实项目名并提供明显的恢复动作。无需确认 modal，因为归档可逆且不删除数据。
- 用户进一步澄清“历史记录归档”指 Run 记录命名，不是 Workspace 归档/恢复。正确单位是每次研究：记录名只根据 `question` 生成简短概括，完整问题继续保存在 Run 和结果页；现有记录与未来记录使用同一派生规则。M117 先前尚未收口的项目归档命令、菜单动作和测试已撤回，M115 的既有只读历史 Workspace 展示保持不变。
- `ResearchRunRead.title` 现在是从 `question` 计算的响应字段而非新数据库列：因此 13 条旧 Run 和未来 Run 自动共用规则，无迁移、模型调用或持久数据改写。规则去口语前缀，以“如何/怎样/怎么”组织主题与动作，统一冒号并在 28 字边界去掉悬空连接词；无法拆分时只截取问题开头。完整 `question` 始终不变。
- 真实苏州 Run 显示为“耕织图：转译提取元素”。旧厂房完整历史的 10/10 标题均通过实页核对；QA 额外捕获并修复自带冒号造成的 `：：`，以及 390px 历史弹窗关闭按钮 36px 的触控回归。最终 390px 无溢出，关闭/保留按钮均 44px，页面 error log 0。

## M118 idempotent local-service recovery

- 当前无法访问不是数据或前端渲染故障：`.archresearch/dev-processes.json` 仍记录 2026-07-24 启动的 API 39488 / Board 43308 和固定端口 8000 / 5173，日志证明二者当时正常启动，但当前两个端口均无监听。根因形状是陈旧运行态记录没有在下一次启动前自动识别和收敛。
- 现有 `start.ps1` 无条件选择端口并新建进程，不读取已有状态；重复启动会产生第二套服务，陈旧/半活状态也没有统一恢复路径。`stop.ps1` 已具备按工作区命令行验证监听者的安全停止边界，可复用该边界，不能信任状态文件中的 PID，也不能误杀占用同端口的其他程序。
## 2026-07-25 M118 implementation findings

- 健康复用不能只检查状态文件或 PID：`Test-WorkspaceDevelopmentServicesReady` 现在要求 API/Board 端口均有效且不同、各自恰有一个命令行引用当前 workspace 的监听进程，并且对应 HTTP endpoint 在 1 秒内返回 2xx-4xx。
- 陈旧或半活状态的收敛继续沿用既有安全边界：只对状态中仍合法的端口调用 `Stop-WorkspaceTcpListeners`，该函数会重新核对监听进程命令行；无关监听者不会被停止，状态文件 PID 不参与终止决策。
- 健康实例复用发生在 runtime 解析和可用端口选择之前，因此重复执行 `start.ps1` 不会因为 5173/8000 已占用而另起一组服务；失效状态会先删除，再选择当前可用端口启动。
- 启动失败路径现在先输出 API/Board 四份日志各最多 20 行，再清理本次已验证的工作区监听者和 launcher，便于诊断而不扩大终止范围。
- 真实陈旧状态恢复后重新使用首选端口 API 8000 / Board 5173，二者均为 HTTP 200；紧接着第二次 `start.ps1` 输出 `already running`，API/Board 的 listener PID 与 launcher PID 四项均未变化，证明重复启动已幂等。
- 恢复后的只读基线仍为 3 projects / 13 completed Runs / active 0；封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍为 completed / attempt 0 / coverage_satisfied。
- M118 最终权威门禁在最终控制流补丁后 exit 0：336 API / 110 Board / 165 Extension / 8 packaged E2E，Ruff/49-file format、strict Mypy、两端 lint/typecheck/build、process/security 与 25/108 evaluation 全绿。最终 API/Board 仍为 200，`git diff --check` 通过，staged 0。

## M119 verified workspace backup and safe restore

- 用户批准的数据安全主线是本地用户工作区的搬迁/灾难恢复，不是源码或整个 Git 工作树备份。必须覆盖 SQLite、收藏本地图、上传任务书/附件和导出文件；排除凭据、日志、依赖、构建产物和 `.archresearch/dev-processes.json`。
- 备份包需要自描述 manifest（格式版本、创建时间、相对路径、类别、字节数、SHA-256）。恢复必须拆成只读预检与显式确认两步；任何 archive、manifest、hash、路径或 SQLite 校验失败都不得写入现有数据。
- 正式恢复仍需先在独立 staging 中完整展开和复检，再保存当前数据的 rollback copy，最后执行原子替换；测试必须用临时工作区证明失败前后当前数据逐字节不变。
- 当前数据根为 `Settings.data_dir`（默认 `.archresearch`），SQLite URL 默认指向 `.archresearch/archresearch.db`。完整可恢复白名单应为数据库文件与 `runs/`、`collections/`、`workspaces/`、`exports/`：数据库中的 `AssetCandidate.storage_path` 仍引用 `runs/` 本地案例资产，`SavedReference.snapshot.collection_file` 引用 `collections/`，`InputArtifact.storage_path` 引用 `workspaces/`，Board 导出位于 `exports/`。
- 当前真实体量约为数据库 17.6MB、runs 62.9MB、collections 1.55MB、workspaces 6.62MB、exports 0.14MB，适合单一 ZIP。必须明确排除 `tool-bench/`（约 2.5GB）、logs、runtime、screenshots、xhs acceptance，以及根目录的 browser token、provider/firecrawl/live-smoke 配置和 `dev-processes.json`。
- 恢复数据库后绝对 `storage_path` 会继续指向原 `data_dir`。同机原位恢复可直接成立；跨机器恢复必须在 staging SQLite 中把白名单内旧数据根路径重写到目标 `Settings.data_dir`，并再次验证所有引用落在目标白名单内。该迁移应限定于 `InputArtifact.storage_path` 与 `AssetCandidate.storage_path`，收藏文件只保存 basename、导出路径不持久化到数据库。
- `Database` 持有 engine 与 sessionmaker，普通依赖每次从 `request.app.state.database` 取当前实例。恢复 endpoint 不应打开普通 Session；在确认 active Run 为 0 后，先完成 staging 检验，再 dispose 当前 engine、执行带回滚的 allowlist entry swap，最后构造/绑定新的 `Database` 并复检。若交换或 rebind 失败，必须先恢复旧 entries，再恢复旧 Database 对象。
- ZIP 采用 `manifest.json` + `data/archresearch.db` + `data/{runs,collections,workspaces,exports}/...`。manifest 固定格式 ID/version、创建时间、源 data root、数据库 schema revision、每文件 category/size/SHA-256 与汇总。预检拒绝绝对路径、`..`、反斜杠/盘符、symlink、重复/额外 entry、超限文件数/解压大小、缺项、hash/size 不符、schema 不兼容和 SQLite quick/foreign-key check 失败。
- UI 按现有 product register 放成非 modal 的“数据管理”完整页面：主页顶栏提供低权重入口；备份直接下载；恢复先选 ZIP 并显示只读预检摘要，预检通过后才显示危险确认。成功恢复后重新 hydrate workspaces；不把低频数据操作塞进研究启动器或最近研究区。
- 后端红绿确认 Python `sqlite3.Connection` 的 `with` 只管理事务、不关闭 Windows 文件句柄；所有 snapshot、rewrite、quick-check 和 summary 连接必须用 `contextlib.closing`，否则临时数据库无法删除且真实恢复交换也可能被 WinError 32 阻塞。
- 前端产品合同已固定：`POST /v1/data-backups` 下载 ZIP；`POST /preflight` 只读检查；`POST /restore` 必须携带固定 confirmation 并再次检查同一上传文件。Board 预检前不渲染恢复按钮，通过后显示项目/Run/收藏/附件数量和回滚说明。
- 最终 Impeccable product register 复核确认数据管理页沿用现有系统字体、语义色、结构线和 44px 移动控件；备份与恢复是两个平面区段，标准文件输入先做只读预检，危险恢复动作只在预检成功后出现。对 `App.tsx` 与 `styles.css` 的 detector 返回 `[]`，无需追加视觉改动。
- 最终安全复核确认 durable 数据、portable path、manifest 路径/类别、逐文件 size/hash、SQLite quick/foreign-key/schema 和额外/缺失 entry 均有边界；但 `manifest.json` 在读取前尚无独立未压缩大小限制，且 ZIP entry 数量上限应在遍历所有 info 前执行。应先用恶意 oversized manifest 行为测试固定拒绝，再补小型 manifest/entry 早期上限，避免预检在正式校验前消耗无界内存或遍历量。
- 上述缺口已关闭：ZIP info 数量超过 `MAX_ARCHIVE_FILES + manifest` 时立即拒绝；manifest 同时以 `ZipInfo.file_size` 和 bounded read 双重限制为 1 MiB，再进入 Pydantic JSON 解析。恶意高压缩超大清单测试从缺常量红测转为 422“清单大小”绿测，M119 聚焦安全/round-trip/rollback 现为 5/5。
- 最终真实备份 `archresearch-backup-20260725T200720.zip` 为 70,993,145 bytes，archive SHA-256 `2fad30316f8c01979b4b4248ebf04b209b25db450bd59ec6f96ad384ff61b4ee`。API 只读预检为 ready/version 1/schema `d0f1a2b3c4d5`、66 files、73,830,230 uncompressed bytes、3 projects / 13 Runs / 5 collections / 1 InputArtifact。
- 独立检查器未调用 M119 service：逐一复算 66 个 size/hash，验证 exact entry set、白名单 category/path、SQLite quick/foreign-key/schema/counts 和 52 条 portable storage paths；`tool-bench`、logs、runtime、process state、browser/provider/Firecrawl config、screenshots 等禁入项为 0。真实恢复未执行；临时工作区 round-trip 与 mid-swap byte-identical rollback 测试已覆盖恢复成功/失败合同。
- M119 可收口为 `implementation_complete`：最终服务加载后 API/Board 200，真实数据仍为 3 projects / 13 completed Runs / active 0，封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 为 completed / attempt 0 / coverage_satisfied；没有生成真实 rollback archive，证明未调用 restore。当前没有已批准的自动下一步。

## M120 conservative workspace cleanup

- 用户批准删除无用文件，但当前工作树的大量修改与未跟踪项是 M77–M119 产品源码、测试、迁移、规划、发布证据和真实备份，不能用 Git 状态是否 dirty 判断为垃圾。清理边界固定为可再生成且与产品/验收证据无关的缓存、构建产物、临时日志和测试 XML；作品集 PNG、Impeccable critique、HANDOFF、`.archresearch` durable 数据、迁移/源码/测试和 M119 ZIP 均保留。
- 进一步审计确认 `.archresearch/tool-bench` 为 50,930 files / 2,495,562,275 bytes，内容是 2026-07-16 的 browser-act/browser-use/crawl4ai/opencli/playwright 临时虚拟环境、下载包和认证测试 profile；当前源码无引用，M119 也明确把它排除在 durable 数据外。`.archresearch/screenshots` 的 12 张旧 M5 runtime 图不属于冻结的 `.artifacts/portfolio` 10 图发布证据包；残留 `.archresearch/firecrawl.json` 与已删除 Firecrawl 产品决策冲突。三者可安全删除。
- 其余明确生成物为两份 pytest XML、根 `.playwright-cli`、Mypy/Ruff/Pytest/Python caches、Board/Extension `dist`、空 test-results/tsbuildinfo、extension Impeccable live config 和空 artifact 子目录，合计约 46 MB。`node_modules` 是当前运行依赖；`work/` 含 XHS 验收脚本/JSON 和 20 页审计渲染；`.impeccable/critique` 是工程记录，均保留。
- M120 最终删除 19 个固定路径、释放 2,543,468,519 bytes（2.369 GiB）。`.archresearch` 只剩 durable DB/runs/collections/workspaces/exports 与当前运行所需 logs/runtime/provider/browser token/dev-processes，加少量历史 live-smoke/XHS acceptance；`.artifacts` 只剩经过哈希冻结的 10 张 portfolio PNG 和 M119 已验证备份。精确 `.gitignore` 只新增 `.playwright-cli/`、两处 test artifact 与 extension live config 路径，不隐藏作品集、备份、critique 或产品文件。
- 最终文档补丁后的轻量复核仍通过：`git diff --check` exit 0（仅既有 LF→CRLF 提示）、staged 0、`task_plan.md` 中 `in_progress` 0、API/Board 200。只读 API 聚合再次确认 3 projects / 13 completed Runs / active 0，封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍为 completed / attempt 0 / coverage_satisfied。

## M121 next-step planning

- `task_plan.md` 的 completion roadmap 仍以 M113 和 327/107/165/8 为基线，未吸收 M118 幂等启动恢复、M119 完整工作区备份/预检/失败回滚和 M120 保守清理。当前权威基线是 341 API / 112 Board / 165 Extension / 8 packaged E2E，API/Board 200，3 projects / 13 completed Runs / active 0。
- 可继承但未批准的产品方向只剩三类：M109 目标用户验证、M110 维护性/稳定性收敛、M111 可重复发布。M107 任务书流程已完成，M108 来源反查已由 M113 删除，收藏、备份和工作区清理均不应再被包装成新功能。
- 当前三个主要维护热点仍高度集中：`apps/board/src/App.tsx` 3,930 行 / 189,568 bytes，`apps/board/src/styles.css` 4,325 行 / 103,839 bytes，`apps/api/src/archresearch_api/workflow.py` 4,716 行 / 203,751 bytes。拆分前必须先建立行为字符化和覆盖率基线，不应在无保护时一次性重构。
- 仓库当前没有 `.github` CI 文件。README 只记录 Windows 11 / Chrome / Python 3.12 / Node 24 / pnpm 11 的手工 setup/start；`scripts/setup.ps1` 会创建根 `.venv`、安装 Python/Node 依赖并构建扩展，但尚无 clean-machine/update 验收与 CI 复现。`pytest-cov` 已是 dev dependency，但尚无一致的 API/Board/Extension 覆盖率报告或门限。
- M119 备份只覆盖 SQLite 与 durable 用户数据，明确不包含源码。当前 104 个 tracked diff 路径与多个 untracked 产品/测试/迁移文件仍只存在工作树；在继续大规模实施前，最高优先级是经用户单独授权后冻结可恢复的源码基线，不能把数据备份误当成源码备份。

## M121 Xiaohongshu-derived proxy test

- OpenCLI 1.8.6 的 `xiaohongshu` 适配器提供只读 `search` / `note` / `comments` 命令，足以保留公开笔记 URL 并提取问法；不需要也不允许使用 ask、follow、publish 等写操作。
- 当前 OpenCLI daemon 1.8.6 在 19825 正常运行，但 Browser Bridge extension 未连接，doctor 为 `Extension: not connected / Connectivity: failed`。因此专用 CLI 路径尚未访问小红书；按 browser skill 可改用用户 Chrome 已有登录态，但如 Chrome 也未登录，必须请用户在该浏览器登录，不得换站点伪造样本。
- browser runtime 首先选择 in-app Browser，直连小红书聚焦搜索页在 30 秒内未完成并导致控制会话重置；未输入、点击、下载或创建 Run。改用 Chrome 后，`agent.browsers.get("extension")` 报 browser unavailable，连续两次轻量列表均只发现 in-app Browser，确认 ChatGPT Chrome 控制链当前不可用。
- 技能规定的四项诊断进一步确认：Google Chrome 150.0.7871.182 已安装，但当前 `running=false`；Default profile 中 ChatGPT Chrome Extension 1.2.27221.15725 已安装且 enabled；native host manifest/registry/origin 全部正确。唯一阻塞是 Chrome 未运行；按技能要求，必须先得到用户确认才能执行 `open-chrome-window.js`。
- 用户批准后，启动脚本实际打开 Chrome，尽管脚本本身未在 30 秒内返回并使控制内核重置。之后只读检查确认 `running=true`，Chrome browser binding 出现且可用；没有重复启动。
- Chrome 的聚焦搜索 `建筑学课程设计 求助` 成功加载，页面显示已登录的“我”入口。首轮候选笔记包括 `6a2a5e74` 建筑排布课设求助、`69498a11` 建筑设计困惑、`69f856bb` 展板纳入求职作品集、`69dcf0dc` 建筑成图指导、`6a250dab` 方案设不出来、`69eac2b8` 课程作业如何完成、`68a00785` 医疗建筑设计、`6a2e5c42` 求助、`692d7be3` 设计求助。候选只是标题层，必须逐篇读正文才能形成测试问题；代做、付费服务和纯情绪帖排除。
- 样本 XHS-P01（`https://www.xiaohongshu.com/explore/69498a11000000001e03a63c`）正文询问“什么样的设计是好设计、大学各阶段应完成什么目标”，作者评论进一步明确“拿到任务书后如何分析并确定核心理念、怎么进行案例分析、再开始做图”。可去个人化为两条高价值建筑研究测试问题：A）拿到任务书后，如何从场地、功能与限制中确定核心理念，并用案例研究推进方案；B）建筑学生应如何建立判断方案好坏的标准，避免只追随老师偏好。
- 样本 XHS-N01（`https://www.xiaohongshu.com/explore/6a2a5e7400000000080265a0`）正文只是“给了第一个图，第二个图怎么画”，图与评论表明它是天正给排水/45° 轴测图生成的软件操作问题。这应作为负样本：不属于建筑案例研究，也不是“找图纸风格灵感”；产品应识别边界，而不是用案例研究假装回答具体 CAD 命令。
- 样本 XHS-B01（`https://www.xiaohongshu.com/explore/69f856bb000000003700c3e7`）想把原有 100+ 页 A3 / 7 张 A0 课设压缩为作品集中约 5 页，具体矛盾是“保留哪些效果图、平面、草图与分析，是否为信息量重画大图”。去个人化问法为：“如何把一个信息量很大的建筑课设压缩成 5 页作品集，保留完整设计逻辑而不只是堆图？”这是高价值用户需求，但属于“学生自有项目内容编辑”，当前 ArchResearch 只会研究外部案例与导出研究成果，不能假装已读取整套学生图纸并完成作品集编辑。
- 样本 XHS-N02（`https://www.xiaohongshu.com/explore/6a250dab000000001603ff77`）只表达“设不出来”、拖延和临近截止，没有场地、功能、空间矛盾或输出类型。这不能直接成为研究题；正确产品行为应是要求用户补充“什么建筑/哪个设计冲突/希望得到案例还是图纸灵感”，而不是启动昂贵且泛化的 Live Run。
- 样本 XHS-N03（`https://www.xiaohongshu.com/explore/692d7be3000000001e027f13`）描述次日提交 4 张 A2 图前的焦虑、害怕失败与无法开始。它没有可研究的建筑类型或空间冲突，应作为需要温和分流的支持边界；产品不能把心理压力伪装成案例研究问题，也不应据此创建 Live Run。
- 下一候选 `6a2e5c42000000001503dfed` 在详情页加载期间超时，尚未取得可验证正文，不能把标题“求助”计入样本。Chrome 控制内核被自动重置后，应重新绑定现有 Chrome，而不是重复启动窗口。
- `6a2e5c42000000001503dfed` 随后已在原 Chrome 标签中显示标题“求助”，但整页文本读取与结构化内容导出分别再次超时。没有取得正文就没有问题证据；该候选正式跳过，不再重试，也不计入 8–10 条语料。
- 样本 XHS-B02（`https://www.xiaohongshu.com/explore/668fa4a9000000002500570f`）明确描述三合一合用前室为改善入户体验而增设隔墙与普通弹簧门，并追问双向疏散和“前室套前室”的合规风险。去个人化问法为：“高层住宅三合一合用前室中，增设隔墙和普通弹簧门以兼顾入户体验与双向疏散是否合规，会不会形成前室套前室？”这是具体且真实的设计冲突，但结论需要现行消防规范与审图口径；当前 ArchResearch 的案例研究不能替代规范核验，应明确边界而非给出伪合规结论。
- 候选 `69d0f72e000000002202897c` 的公开页只返回标题“逼仄的夹缝空间就这样被激活了”，服务端初始数据没有可验证正文；不能仅凭标题改写成学生问题，跳过且不计入语料。
- 样本 XHS-P02（`https://www.xiaohongshu.com/explore/684d3bb8000000002102e97a`）说明连廊可连接分散建筑、改善流线，并通过坡道/台阶处理高差和提供全天候通行。去个人化学生式问法为：“既有建筑改造中，如何用连廊连接分散且有高差的体量，同时改善日常流线、全天候通行和新旧建筑关系？”它适合建筑案例研究；产品应以多个正式案例比较连接位置、结构独立性、无障碍坡度、气候边界与新旧界面，而不是照抄帖子的泛化结论。
- 样本 XHS-P03（`https://www.xiaohongshu.com/explore/6a44a817000000002100b389`）提出天窗自然采光、挑空中庭、交错楼梯、回游走廊与多材质公共空间。去个人化学生式问法为：“封闭且采光不足的公共建筑，如何通过中庭、天窗和回游动线改善自然采光并形成可停留的公共核心？”它适合建筑案例研究，但需要案例证据说明采光机制、剖面关系和适用边界，不能把“都能套用”当成事实。
- M121 代理语料已达到 9 条：4 条建筑研究正向输入（XHS-P01 两问、XHS-P02、XHS-P03）与 5 条边界/负向输入（XHS-N01、XHS-B01、XHS-N02、XHS-N03、XHS-B02）。这足以进入产品适配筛选；继续采集只会增加相似样本，不应替代真实学生观察。
- 启动器适配筛选：XHS-P01A（任务书→核心理念）可直接进入建筑设计研究，有真实任务书时应作为可选 PDF 收束范围；XHS-P01B（好设计标准）只在补充项目阶段与建筑类型后适合研究，否则过宽；XHS-P02（既有建筑高差连廊）和 XHS-P03（中庭采光/回游动线）可直接进入建筑设计研究。四者都不是图纸灵感任务。
- 边界分流：XHS-N01 是 CAD/天正命令；XHS-B01 需要读取并编辑学生整套作品集；XHS-N02 信息不足；XHS-N03 是情绪支持；XHS-B02 需要规范与审图口径。当前产品应分别拒绝伪装成案例研究、要求补充、或明确不能给出合规/心理支持结论，不能因为文本可提交就机械创建 Run。
- 唯一 Quick Live 选择 XHS-P02：“既有建筑改造中，如何用连廊连接分散且有高差的体量，同时改善日常流线、全天候通行和新旧建筑关系？”它具有清晰建筑对象、三个可验证机制维度和案例研究适配性，且归属于现有“旧厂房社区文化中心”项目。XHS-P03 保留为离线筛选样本，不再为凑数量创建第二条 Run。
- 唯一 M121 Quick `5e4184cf-c3e7-47ce-bc5d-8761bcc632a1` 在 attempt 0 以 `completed / completion_satisfied` 结束：26 usable、3/3 coverage、gaps 0、Firecrawl 未恢复。规划的三题准确覆盖高差与流线分离、全天候连廊复合使用、新旧结构/材料节点，说明问题入口和拆解对真实学生式空间冲突是适配的。
- 研究质量没有达到“已解决学生问题”的标准：`coverage_report.project_count = 1`，且 enrichment 明确缺 `insufficient_project_diversity` 与 `insufficient_multi_asset_projects`。结果列表虽有 4 个项目/26 资产，但另外 3 个只提供图纸列举或有限正文，26 个结果全部为 `partial`；唯一正式覆盖来自 Designboom 的再利用集装箱孵化器。
- 该唯一正式项目能证明碎片化体量、连续互联空间、通透界面、日光与交流，但不能证明被连接对象是既有高差体量，不能证明公众/后勤流线分离、全天候围护，也不能证明新介入与原有砖混外壳的结构独立性或连接节点。综合对这些缺口标注诚实，4 条建议也明确区分证据事实与目标项目推断，这是证据边界优点；但主要问题本身没有获得直接案例回答。
- 当前完成语义存在代理测试暴露的 P1 产品风险：`_completion_satisfied()` 只检查 coverage gaps，不检查 enrichment gaps，因此一个项目跨三题提供有限机制就能显示 `completed/completion_satisfied`。真实学生会把“完成”理解为已有足够案例依据，而不是“每题至少有一条部分证据”。在新增功能前，应先决定是否让直接性/项目多样性不足进入 partial 状态或至少在结果首屏显著显示“依据不足”。
- Loaded Board 无截图核对确认最新记录在“旧厂房社区文化中心”正确显示并可打开，完整结果页、3 个子问题、案例证据和后续工具均能渲染。页面同时显示“研究已完成”“每个子问题都已找到方案证据”，但案例区明确只有“1 个项目已核对项目原文”、23 张网页图片只作预览，且每题唯一项目都标“已有依据，仍需核对”。这不是隐藏证据问题，而是顶层完成文案与下层真实证据强度不一致。
- 第一题摘要还显示“4 个方案项目 · 12 条案例证据”，实际正式 dossier 仍只有 1 个项目；它把保留的 partial/图片线索数量混入用户理解的“方案项目”。代理测试因此给出两个相互关联的 P1：完成状态过强、问题拆解计数口径易误导。修复前不应通过再开 Run 来寻找更好样本。
## 2026-07-26 M121 proxy-result repair evaluation

- The M121 Run is not a failed workflow. `workflow._completion_satisfied()` deliberately means that `coverage_report.gaps` is empty; `_enrichment_satisfied()` separately requires no `enrichment_gaps`. This preserves a useful terminal answer after all three subquestions have at least one source-bound result, even when depth targets are not met.
- The misleading completion claim is created in the Board projection. `runAnnouncement()` maps every architectural `completed` Run to `研究已完成`, although the loaded Run already carries `completion_satisfied` and two enrichment gaps. The same projection is reused in recent history, so fixing only the result-page heading would leave inconsistent language.
- The status-strip count has the same semantic problem: `resultCountLabel` uses backend `usable_assets`, so the tested result can read as 26 usable references even though only one project passes the Board's existing text-evidence gate. Pipeline-usable assets and formal case evidence are not interchangeable user concepts.
- Subquestion summaries currently count every non-XHS result as a case asset/project, including results whose `analysisReady` is false. The formal case section already applies the correct gate (`non-visual-platform && analysisReady`) and therefore reports one evidence-qualified project. Reusing this established gate is a smaller and safer fix than adding a schema or parallel evidence classifier.
- Recommended repair surface, if authorized, is Board-only: derive an evidence-strength presentation state from `coverage_report.gaps` and `enrichment_gaps`, use it in loaded and recent status copy, and make the status/subquestion counts distinguish formal projects from public-page/image leads. No workflow, API schema, database, source policy or Run lifecycle change is required.
- Changing backend `completed` to `partial` was rejected for this finding. It would turn a quality-depth shortfall into an execution-state regression, expose retry UI, affect accepted historical Runs, and conflict with the policy that useful incomplete research remains available. Such a change would require a separate impact audit and stronger evidence than this one proxy Run.
- Required characterization shape is the exact observed contradiction: 3/3 coverage, one formal project, several partial lead projects, project-diversity and multi-asset enrichment gaps, plus synthesis limitations that deny direct proof of key target conditions. The negative control is an enrichment-complete architectural Run; visual inspiration and current partial/blocked retry behavior are out-of-scope regressions to guard.

## 2026-07-26 M124 approved scope

- The user explicitly superseded the prior Board-only recommendation: architectural research must reach all configured depth/enrichment targets before it may be called complete, and result content must not appear while the Run is active. Bounded provider failure still cannot be fabricated as success; exhaustion must terminate honestly as partial/blocked while preserving checkpoints internally.
- The first screenshot was initially misread as a request to remove the complete Run-history dialog. The user's clarification screenshot identifies the actual target: the workspace-selection popover that contains “当前项目 / 历史归档”. The complete Run-history dialog and compact latest-three list are not requested changes and must be restored.
- Impeccable product guidance still supports the corrected inline direction: workspace context should be visible without a transient dropdown, using the existing flat work-surface vocabulary rather than cards. Current and archived workspaces can become a directly visible labelled selection list; selecting one continues to filter its Run records.
- Active Board content currently arrives through three paths: `openRun()` hydrates an active Run immediately, startup restoration hydrates the latest active Run, and the poll loop calls `syncProvisionalResults()` every second. All three must be gated; hiding only the result section would still spend requests and retain stale provisional state.
- The workflow's normal-round stop condition emits `completion_satisfied` as soon as minimum subquestion coverage is present, while the query loop skips already covered subquestions during recovery rounds. Strict full-depth delivery therefore requires both removing the minimum-completion break and allowing bounded recovery rounds to continue enrichment; changing only final status would not make another attempt to reach the full target.
- The user's final clarification removes the workspace taxonomy from this surface entirely: no workspace popover, no inline “当前项目 / 历史归档” replacement, and no extra project-name layer before records. The direct objects are question-derived Run titles. Workspace IDs still remain in Run data so opening a record can restore its background project context and new research still saves to an active workspace.
- Recovery search confirms the visible home implementation already follows that final direction. Remaining matches are either explicit negative assertions, unrelated collection copy, or stale tests/docs that still describe the removed menu and latest-three/history-dialog model. Those stale contracts must be updated; the flat cross-workspace list itself should not be redesigned again.
- Loaded no-screenshot DOM inspection confirms the current Board renders all 14 Runs as one question-title list. There is no visible workspace selector, “当前项目 / 历史归档” taxonomy, archive region or history dialog; “新建项目” is the only explicit project action. The remaining user-facing mismatch was documentation that still described the superseded current-project/latest-three/history-dialog model.

## 2026-07-26 M124 completion findings

- The final information hierarchy is deliberately flat: the direct object is a research question record, not a Workspace category. Project ownership still exists in data and is restored when a record opens, but it does not create a visible navigation layer on the home surface.
- Strict completion and flat history are one coherent contract. Architectural Runs only announce `completed` when coverage and enrichment both pass; useful under-depth Runs become `partial`, while legacy completed records with enrichment gaps use the honest presentation “研究已形成初步依据”. Active Runs keep checkpoints internally but do not fetch or expose provisional result content.
- The visually hidden workspace-name marker remains only as asynchronous test synchronization. It is `aria-hidden`, provides no visible or navigable project taxonomy, and does not reintroduce the removed selector.
- Impeccable distillation confirms the cut removes an obstacle rather than functionality: question records, retention actions, opening results and “新建项目” remain directly available; the workspace selector, project/archive grouping, separate archive region and history dialog do not earn a place and are absent.
- Final authoritative verification passed at 341 API / 113 Board / 165 Extension / 8 packaged E2E plus Ruff/format, strict Mypy, ESLint, TypeScript, production builds, process/security tests and 25/108 evaluation. Loaded no-screenshot DOM verification found 14 flat question records, no visible project/archive categories, no workspace selector, no history dialog, exactly one “新建项目”, no horizontal overflow and no page errors.

## 2026-07-26 M125 recent-history density finding

- The user screenshot shows M124's flat history semantics are correct but its page density is not: all 14 records extend the home page, while the grid-only header places “新建项目” on a separate row after the title and description, creating roughly one record-row of avoidable empty space.
- The appropriate reduction is a bounded native scroll viewport, not another category, modal or hidden “view all” branch. All question records remain immediately available in one list, but only about four are visible at once; keyboard users need a named focusable scroll region.
- The header can remain in the existing markup and become a two-column grid: title/description at left and the existing project action at top right. No new card, navigation layer, copy or data contract is required.
- The completed implementation caps the viewport at `min(400px, 55dvh)` and keeps native vertical scrolling, `overscroll-behavior: contain`, stable scrollbar space and a visible focus outline. This shows roughly four records in the user's compact view while preserving every record and retention action in one accessible list.
- Loaded no-screenshot geometry at 445×736 reports header height 52.8px, history client height 400px versus 1,871px scroll height, 14 record rows, page overflow 0 and page errors 0. At the default 1030×986 viewport the same header is 52.8px and the 400px history contains 1,162px of scroll content.

## 2026-07-26 M126 shorter recent-history viewport

- The user requested a second, narrower visible history length. The smallest scoped response is to reduce only the existing native scroll viewport from `min(400px, 55dvh)` to `min(320px, 45dvh)`; all 14 cross-workspace question records, keyboard focus, native scrolling, opening and retention remain unchanged.
- The focused CSS contract was changed first and failed against the old production cap as intended. Production CSS and the Board product/design contracts now agree on roughly three visible records rather than four.
- Loaded no-screenshot geometry under the 445×736 viewport override reports a 320px history client/rect height over 1,871px scroll content, 14 record rows, page overflow 0 and page errors 0. The browser reported 446px inner width under the 445px host override; this does not cross the 620px/860px responsive boundaries or affect the acceptance result.
- The narrower viewport does not reintroduce a preview, modal or taxonomy. It remains the same named, focusable native scroll region, so the density improvement does not hide records behind a second interaction model.

## 2026-07-26 M127 restart-safe local availability

- M118 made `scripts/start.ps1` restart stale/partial services and reuse a healthy pair, but no Windows login/startup registration invokes that script. A machine restart therefore always terminates API and Vite, leaving `127.0.0.1:5173` unavailable until a manual start. This is an activation gap, not another listener-health bug.
- The smallest durable fix is a current-user, non-admin login startup entry that launches the existing verified start script invisibly. The startup mechanism must point to an absolute workspace path, be idempotent and have an explicit removal path; process ownership and health remain delegated to M118 rather than duplicated.
- The real pre-fix state reproduced the report: both `/health` and Board timed out while `.archresearch/dev-processes.json` still named listeners from the prior login. Running the existing start script replaced those stale listeners and restored 200/200, confirming that automatic invocation is the missing link.
- The installed current-user shortcut lives at `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\ArchResearch.lnk`, targets the resolved PowerShell 7 runtime, invokes the absolute workspace `scripts/start.ps1` with a hidden window and uses the workspace as its working directory. No administrator or machine-wide task is required.
- Registration is safely idempotent and removable. It refuses to overwrite or delete a same-name shortcut unless its PowerShell target, exact start-script arguments and workspace directory identify this installation.
- The full repository gate includes the new autostart test and passes at 341 API / 114 Board / 165 Extension / 8 packaged E2E, plus Ruff, format, strict Mypy, ESLint, TypeScript and production builds. The change does not depend on a provider key, Firecrawl, a Live Run or browser automation.

## 2026-07-26 recovery verification

- The latest authoritative recovery state is 3 projects / 14 Runs / active 0. Older HANDOFF entries with larger historical Run counts are superseded by the later retention and product-work milestones.
- M77, M78, M93, M126 and M127 are all `implementation_complete`. M121 is the only `in_progress` phase and cannot complete without observing 2–3 real target users.
- Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` remains completed / attempt 0 / coverage_satisfied, permanently sealed and never eligible for retry.
- `HANDOFF.md`, the active phase table, recent findings/progress and `git status --short --branch` all agree that there is no approved automatic next product step. The full dirty and untracked worktree is user-owned state and must be preserved.

## 2026-07-26 next-step recommendation

- The highest-value next step is not another UI feature or workflow optimization. M124–M127 closed the known completion, history-density and restart-availability problems; the remaining product-confidence gap is still M121's lack of direct observation from 2–3 real target users.
- The completed proxy round is useful for question-shape and result-strength diagnosis, but it cannot measure task completion, time, navigation, result comprehension, collection retrieval or recovery expectations. Repeating proxy Runs would add cost without closing the M121 gate.
- The pilot should cover 8–10 de-identified tasks across architectural research, one project-brief path, visual inspiration, result-state comprehension, history reopening and collection retrieval. Participants must initiate their own tasks; Codex must not auto-create Live Runs.
- Findings should trigger implementation only when severity is P0/P1 or the same P2 pattern repeats across at least two participants. This prevents another redesign from a single comment while still escalating safety, trust and core-task failures immediately.
- M122 modularization remains valuable but should follow the pilot and a separate source-baseline decision. The current 104-path tracked diff plus untracked product files are not protected by the M119 data backup, so broad refactoring before that decision increases recovery risk without user evidence.

## 2026-07-26 M121 plan terminology correction

- “来源核验” was an incorrect summary label for the new pilot plan. Source lookup/TinEye was removed by M113 and is not a current product flow, pilot task or roadmap candidate.
- The intended pilot coverage is result-state comprehension: whether users understand completed versus initial-basis output, formal project cases versus preview leads, and visual-inspiration-only material. It does not ask users to perform source lookup or reverse-image verification.

## 2026-07-26 M128 answer-first interface redesign

- User-provided screenshots identify the architectural result page and architectural collection detail as the current readability failures. Both fragment one answer into many small labels, columns, metadata rows and nested disclosures; the result page also hides useful case content behind “查看完整案例与证据”.
- The product goal for both surfaces is now explicit: users want the research result, not how the system researched it or where it came from. The primary UI should expose conclusion, case mechanism, actions, boundary and necessary project images by default; source/provenance/status/verbatim/audit material must not compete on these two surfaces.
- M128 is limited to Board information architecture, rendering, styles, tests and current product/design documentation. Evidence binding and provenance remain in backend/durable data; visual inspiration, APIs, schemas, exports, selection semantics and collection persistence remain unchanged.
- Screenshot audit: every result question repeats a large numbered heading, a separate white summary band, a four-column representative-case ledger, source/status metadata and a closed disclosure. The same answer is split across five reading stops, and the only fuller case content is hidden behind the disclosure.
- Screenshot audit: the collection detail uses abundant width but separates the project title, solution text, numbered moves, images and detached top-right actions into distant islands. The visible project title also mixes project, architect and publication identity, while “打开来源” competes with the actual saved result.
- The shared redesign unit should be one continuous case answer: project identity; a concise mechanism statement; two or three actionable moves; a short applicability boundary; and only the images needed for recognition. Question-level synthesis should precede these units once, not repeat as labels inside each case.
- A bounded HTTP read confirmed CollectUI is reachable, but its category page exposes no useful server-rendered article/list/detail examples to inspect without resuming browser automation. The applicable pattern is therefore the editorial principle behind those references rather than a copied component: a strong chapter lead, flat ruled entries, generous reading width, and one recognition image aligned with each answer.
- The minimal implementation should delete the result-page representative ledger plus hidden full dossier split and render every evidence-qualified dossier once, in source order, as a visible answer unit. Collection detail should remove the original-question repetition and architecture source action, keep deletion, and present the saved mechanism as the lead followed by at most three moves, one non-audit applicability boundary and the existing recognition images.
- The implemented architectural result surface now has one chapter lead and one flat answer unit per evidence-qualified project. It no longer renders the separate question-summary band, representative ledger, project-condition ledger, evidence gallery, source footer, source inspector trigger, completed-run progress disclosure or unassigned lead group. Each project keeps collection selection, up to three transfer moves, one boundary and at most one available recognition image.
- The collection detail now suppresses the repeated original research heading and architecture source link, keeps delete and all persistence behavior, filters audit-like limitation strings from the visible boundary, and uses a constrained editorial text/image split with one dominant image. Drawing collection markup and actions are unchanged.
- Loaded legacy Runs exposed display residue that the deterministic fixtures had not covered: completed architectural results still showed a status strip; synthesis recommendations retained “转译建议 / 该建议转译自” wrappers; boundaries included `drawing_ids`, “源网站入口”, “页面仅支持” and “未覆盖研究子问题”; and some project names ended with `| ArchDaily`. The Board now removes only those presentation artifacts while preserving the original synthesis, limitations and project names in durable data.
- Architectural result and collection project titles strip only recognized publication suffixes (`ArchDaily`, `ArchDaily China`, `Dezeen`, `Designboom`, `Divisare`). Architect or studio identity before the suffix remains visible, and visual-inspiration naming is unchanged.
- No-screenshot loaded QA passes at 1440×900 and 390×844. The architectural result shows 3/3 chapters and 3/3 visible cases, no `<details>`, status strip, source/audit text, hidden content, page overflow, clipped text or unloaded recognition images. The architectural collection detail shows 2/2 visible cases, 3/1 loaded images, no source/audit copy or publisher suffixes, a stable desktop two-column split, a mobile single column and 44×44 delete controls. The mobile result filter is now 44px high. Console errors are 0 and the temporary viewport override was reset.
- The authoritative M128 gate is 341 API / 115 Board / 165 Extension / 8 packaged E2E with every static, type, build, process, security and evaluation check passing. Durable state remains 3 projects / 14 completed Runs / active 0; the sealed Run is unchanged and no runtime data was written.

## 2026-07-26 完成度检查与运行保留期 P0

- 只读复验确认记录属实：3 workspaces / 14 research_runs（全部 `completed`，active 0）、158 asset candidates、304 source pages、302 evidence claims、1,486 trace events、14 reference boards、5 saved references、1 input artifact。API 测试用独立 `pytest --collect-only` 实测 341，与 M128 门禁数字一致；Board 115 / Extension 165 / 8 packaged E2E 沿用 M128 那次全绿门禁，本轮未重跑。
- 新发现 P0：14 条 Run 的 `keep_forever` 全部为 0，其中 12 条在 `2026-08-03T17:56:37Z` 同时到期（M92 采纳宽限期 + 14 天），`ff16988d` 与 `5e4184cf` 分别在 08-06 与 08-08 到期。`main.py:131` 在每次 API 启动的 lifespan 中无条件调用 `cleanup_expired_data`；`lifecycle.py:45-54` 选出到期 Run，`lifecycle.py:113-153` 的 `delete_runs` 对 Run 及其级联 assets/sources/claims/traces、磁盘 `runs/<id>` 与 `exports/<board_id>` 做硬删除。M127 的开机自启会在每次 Windows 登录时触发该启动路径。
- 后果链闭合：2026-08-03 之后的首次无人值守登录即会删除 12 条记录，包含封存 Run `10d31b4c-…` 与全部三档验收 Run（`a2cf2e20`、`d995bed5`、`76f52c79`、`f5be3f17`、`23b6f84c`）及旧发布证据引用的 `7d8faa53`/`b4c314a6`/`d13bdc67`；8 月 8 日后 14 条全失。HANDOFF 的"永久封存"此前只约束不得 retry，不构成删除保护——语义与代码脱节。
- 备份不能替代保留标记：`.artifacts/archresearch-backup-20260725T200720.zip`（70,993,145 B、manifest + 66 数据文件、含 `data/archresearch.db`）确实留存，但其中的 Run 携带**已过期**的 `retention_expires_at`，恢复后下一次 API 启动会把同一批 Run 再删一遍。
- 可见性判断修正：Board 最近研究的每条记录本就有"还剩 N 天 / 设为永久"控件（`App.tsx:585-594`），倒计时并非不可见。真正缺失的是不可替代的验收证据与普通 Run 之间没有任何区分、没有跨记录的集中到期提示、删除后没有撤销。
- 处置：经用户批准，用产品既有 `PATCH /v1/runs/{run_id}/retention` 把 14 条 Run 全部标为永久保留，未改代码、schema 或迁移，可逐条撤销。独立重开只读 SQLite 复读为 `keep_forever=1` / `retention_expires_at` 为空 14/14，按 `lifecycle.py` 原始谓词实测当前可删除 Run 为 0、仍挂到期时钟的 Run 为 0。
- 记录中的 M122 文件规模已过时：实测 `App.tsx` 3,881 行、`styles.css` 5,132 行、`workflow.py` 4,971 行，样式表比旧记录多 807 行。工作树现为 104 个已跟踪改动路径 / +27,400 −4,788 加 16 个未跟踪条目，仍不在 M119 数据备份范围内。

## 2026-07-26 M129 源码基线保护（已批准的 A→B→C 顺序中的 A）

- 边界重算：`98a9a01` 之后的全部实现共 130 个路径（104 个已跟踪改动 = 95 modified + 5 deleted + 4 工程记录，加 26 个未跟踪文件）。无缺口、无重叠地分为 112 个产品路径、5 个工程记录（`AGENTS.md`、`HANDOFF.md`、`task_plan.md`、`findings.md`、`progress.md`）、11 个发布证据路径（manifest + 10 张 PNG，约 2.0 MiB）和 2 个排除的本地工具输出。
- 两个本地输出此前完全没有被 ignore，笼统 `git add -A` 会把它们提交进去：`.artifacts/archresearch-backup-20260725T200720.zip`（70,993,145 B，内含真实 SQLite 数据库）与根 `.impeccable/critique/`。`.gitignore` 已精确补上 `.impeccable/` 与 `.artifacts/*.zip`，同时保留 `.artifacts/portfolio/` 可见，使发布证据仍是显式选择而不是被静默忽略。
- 凭据扫描对“确实要提交的内容”执行，而不是全仓：全部已跟踪 diff 新增行加 14 个未跟踪候选文件，按 provider key、`sk-`/`sk-ant-`、AWS key、GitHub/Slack token、Bearer、私钥头、`X-Amz-Signature`、`api_key=`/`secret=`/`password=` 字面量匹配，命中 0。产品源码、Board、Extension、脚本、`.env.example` 与 README 中 Firecrawl/TinEye 引用 0，绝对用户路径 `76384` 泄漏 0。
- 用户决定：发布证据层暂不纳入 Git。10 张 PNG 是 M46 之前旧运行的截图，`task_plan.md` 已记录它们不能证明当前三档质量；仓库先保持无二进制资产，等 M123 刷新证据时再决定。文件仍保留在磁盘上。
- 门禁在提交前重跑并全绿：341 API / 115 Board / 165 Extension / 8 packaged Chrome E2E，加 dev-common 测试、Provider 配置安全契约、autostart 测试、评测夹具（25 research tasks + 108 classification samples）、Ruff check、Ruff format 51 files、strict Mypy 19 source files 与根 `pnpm run check`（lint/typecheck/test/build）。
- 唯一无法执行的门禁步骤是 `scripts/tests/process-lifecycle.tests.ps1`。本会话 shell 的 CIM/WMI 运行时已损坏：`Get-CimInstance Win32_Process` 直接抛 `Microsoft.Management.Infrastructure.Native.ApplicationMethods` 类型初始化异常，禁用沙箱后同样失败，因此这是环境限制而非代码回归；M127/M128 曾在 Codex 环境下通过该脚本。
- 共享工作区并发写入已被实测确认，必须作为长期约束记录：本 Codex 会话在我提交后仍在写同一工作树，`scripts/tests/process-lifecycle.tests.ps1` 与 `scripts/dev-common.ps1` 的 mtime 分别为 20:03:14 与 20:03:44（提交发生在约 20:02–20:03），`dev-common.ps1` 从提交内的 269 行增长到工作树 431 行，改动方向正是把监听进程检测改为不依赖 WMI。结论：本工作区的 `git add` 只能保证“逐文件读取时刻”的快照，任何跨文件一致性都必须与并发写入方协调，提交前后都要重新读 `git status`。

## 2026-07-26 M130 首页“已完成 / 已形成初步依据”混排诊断

- 用户提出的规则本身没有失效。M124 之后的终态判定在 `workflow.py:1373` 只有 `_enrichment_satisfied(coverage)` 为真才写 `completed`，即 `gaps` 与 `enrichment_gaps` 同时为空，`stop_reason` 恒为 `coverage_satisfied`；达不到就是 `partial`。按当前代码不可能再产生“completed 但深度不足”的记录。
- 混排的成因完全是历史数据。14 条 Run 创建于 2026-07-11 至 2026-07-25，全部早于今天落地的 M124。旧规则只要求 `gaps` 为空（`completion_satisfied`），不检查 enrichment，因此 7 条 Run 持久状态是 `completed`，`coverage_report.enrichment_gaps` 却非空，且带着当前代码已不可能再产生的 `stop_reason=completion_satisfied`。M124 没有改写这批数据，只在 Board 增加了诚实降级标签。
- 实测每条记录的显示标签：6 条建筑 Run 显示“研究已形成初步依据”（`7d8faa53`、`b4c314a6`、`42668844`、`76f52c79`、`ff16988d`、`5e4184cf`），4 条建筑 Run 显示“研究已完成”（`58f4b9f9`、`d13bdc67`、`a2cf2e20`、`d995bed5`），4 条图纸灵感 Run 显示“已完成”。用户截图中的两条“初步依据”加一条“已完成”与此一致。
- 发现一个当前代码的真实不一致，与历史数据无关：`App.tsx:407-414` 的 enrichment 降级条件写死了 `run.goal === 'precedent_research'`，图纸灵感 Run 走 `visualLabels` 直接显示“已完成”。`e525ca77` 正是 `completed` 且 `enrichment_gaps=['insufficient_verified_or_partial']`，与被降级的建筑 Run 条件相同却仍自称已完成。同一条件两种诚实标准，是这份列表看起来随意的直接原因。
- `58f4b9f9`（2026-07-11）的 `coverage_report` 根本没有 `enrichment_gaps` 键，Board 的 `?? 0` 把它当作无缺口。这是比 enrichment 规则更早的记录，不能与真正达标的 Run 混为一谈，但也不应假装它被评估过。
- 不采用回填持久状态的修法：这 7 条里包含 M53/M65 的三档验收 Run 与旧发布证据引用的 `7d8faa53`/`b4c314a6`，改写它们的 `status` 会使既有验收记录与发布证据失真。修复只在展示层进行，durable 数据不动。

## 2026-07-26 M131 旧深度不足 Run 的定向删除

- 用户判定这批旧记录属于失败记录并要求删除，后续会有新的实际测试。核对后指出其中两条不是失败记录而是记录正在引用的验收证据，用户随即确认删 5 保 2。这条边界值得固化：**“深度不足”不等于“失败”，删除前必须逐条对照它在 `HANDOFF.md`/`task_plan.md` 里的身份**。
- 已删除 5 条：`7d8faa53`（M46 之前的 Balanced 发布证据）、`b4c314a6`（同期 Deep 发布证据）、`42668844`（HANDOFF 第 9 条的 Quick 链路验收）、`e525ca77`（M76 图纸灵感 Live）、`5e4184cf`（M121 代理轮 Quick）。保留 `76f52c79`（M53/M65 被接受的 Deep）与 `ff16988d`（M107 唯一真实任务书 Standard Run）直到新的实际测试产生替代证据。
- 删除前先用产品自带的 `POST /v1/data-backups` 生成 70,957,655 字节完整备份并通过 `POST /v1/data-backups/preflight`：`ready=true`、66 文件、14 Runs、5 收藏、1 份任务书，SHA-256 `2B8D692BB01C257F7B345B52FC9D0D46C39FE9CCC21C6AF35FE563D4B7956A4C`。因此这次删除可整体回滚，不是不可逆操作。
- 删除走产品既有的 `lifecycle.delete_runs` 安全 helper，不写裸 SQL：显式 run_id 列表、目标必须全部存在、目标与保留名单不得重叠、保留名单在删除前必须都在。执行前停服、执行后重启，避免与运行中的 API 争抢 SQLite。
- 个人收藏未受影响，这验证了 M93/M100 的快照设计：`ff16988d` 上挂着 2 条收藏，但收藏保存的是独立 snapshot 与本地图副本，删除前后 `saved_references` 均为 5。删除前已确认另有 2 条收藏本就指向早已不存在的 Run 仍正常。
- 删除后的持久基线为 3 workspaces / 9 Runs（全部 completed）/ active 0 / `keep_forever=1` 9/9 / 5 收藏 / 1 份任务书；磁盘 `runs/` 目录从 6 个减为 3 个。封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 未在删除名单内，仍为 completed / attempt 0 / coverage_satisfied。
- 遗留的证据不一致必须记录，不能装作没有：`docs/release-evidence-2026-07-16.md` 冻结的三个 accepted run 里有两个（`7d8faa53`、`b4c314a6`）的底层数据已被删除，`.artifacts/portfolio/` 中对应的 8 张 PNG 因此只剩截图而无可核对的 Run。该文档与 10 张 PNG 都未纳入 Git，处置留到 M123 刷新发布证据时一并决定。

## 2026-07-26 M132 案例研究页与收藏页的版式实测

- 在 1920×1080 真实页面上实测，**根因不是留白多少，而是同一页有两条互相冲突的左边界**。`.results-section` 与 `.collection-page` 是 `max-width: var(--layout-stage-max)`（1600px）居中，左边界 x=153；而内部的 `.case-chapter`（`styles.css:2975`）和 `.collection-architecture`（`styles.css:1699`）是 `max-width: 1180px; margin: 0 auto`，在 1600px 里再次居中，左边界 x=**363**。于是章节标题、研究结论、`个人收藏` h1 全在 153，案例正文和收藏案例全在 363，相差 210px。
- 同一原因造成控件“飞到右边”：`图纸类型` select 贴的是 1600px 外框右缘（x=1597），`选择案例` 贴的是 1180px 内框右缘（x=1445），两者与正文右缘（约 1160）分别相距 264px 和 285px。截图里那种“标签在最左、控件在最右、中间一大片空”的观感就来自这两层不同宽度的框。
- 阅读列宽在一页里跳了四次：研究结论 header 673px → `.synthesis-primary` 920px → `.case-chapter` 1180px → `.case-answer-copy` 808px。左缘固定而右缘反复移动，是这两页最强的“排版没做完”信号。
- 行长实测超标的正是没有 measure 的块：`.synthesis-primary li` 896px / 112ch，`.synthesis-boundary` 值列 828px / 153ch，收藏页 `.collection-question-heading h2` 1081px（24px 字号下每行约 45 个汉字）。反之写了 `max-width: 58ch/64ch` 的 `.case-answer-mechanism`、`.case-answer-actions ol` 实测 63–76ch，本来就是舒适的，说明问题是覆盖不全而不是数值错。
- 三处“标签轨”是 M99 已被 M100 否决过的做法的残留：`.synthesis-boundary` `80px minmax(0,1fr)`、`.case-answer-boundary` `80px minmax(0,1fr)`、`.collection-case-boundary` `84px minmax(0,1fr)`。12px 的“适用条件”只占 48px，轨道却是 80–84px，再加 12px gap，必然留出可见空档；正文里还重复一次“适用边界：”“页面不是…”之类的自我标注。
- 这三处适用条件同时是全页最小的字（`--font-sm` 12px），却承载用户必须遵守的使用边界；`优先做法`/`核心解法`/`怎么做` 反而是 14–16px。层级与重要性相反。
- CollectUI 本轮可以正常打开（此前两次会话超时），但它是 Dribbble 作品图聚合站，正文全是图片、无可读结构，且以 landing/branding/motion 类营销版式为主。对一个中文密集研究阅读面没有可直接迁移的结构；照搬只会得到 impeccable 明确列为 slop 的通用卡片墙。真正适用的成熟范式是文档/编辑型阅读版式：单一文档栏 + 固定 measure + 贴着内容的操作，而不是画廊式卡片。
- 第一版修复只做到“统一左缘”是不够的，用户当场指出问题：内容全部贴左、右侧留下约 600px 空白，反而更像没做完。教训是**对齐一致 ≠ 版式成立**；页面级规则必须同时决定内容栏的宽度、位置和两侧余量，而不是只消除内部差异。
- 因此确立全局文档规则并写进 `design-system.test.ts`：`--layout-doc-max: 1180px`，`.result-task-heading`、`.research-synthesis`、`.case-analysis > .results-header`、`.case-chapter`、`.collection-page > .panel-heading`、`.collection-page > .collection-entry-switch`、`.collection-architecture`、`.collection-question-directory` 共用一条 `max-width + margin-inline: auto` 规则。任何新增的结果/收藏区块只要加进这一条选择器就自动符合版式，不需要各自设宽度。
- 实测结果：1920 下左右边距 362 / 378，1440 下 122 / 138，两页所有区块只有一个左缘和一个右缘；`图纸类型` 筛选器与 `选择案例`、收藏页删除按钮的右缘都精确落在 1542（1920 时）文档右缘上，不再分别停在 1600px 外框和 1180px 内框。
- 行长同时收口：案例标题从 148ch 降到 46ch，`优先做法` 从 112ch 降到 69ch，适用条件从 12px/153ch 变为 14px/69ch。收藏页与结果页均无超过 80ch 的文本块。
- 另修两处“标签自我重复”：`userFacingRecommendation` 原本只剥离 `【转译建议…】` 方括号形式，真实数据是裸的 `转译建议：`，因此界面上每条做法都以标签开头；适用条件同样带 `适用边界：` 前缀。现在两者都在展示层剥离，durable 数据不变。

## 2026-07-26 M133 全触点白话文案走查

- 逐块走查主页、建筑结果页、收藏页、备份页的全部可见文案后，最大的问题不是个别词难懂，而是**同一个概念在不同页面有三个名字**：案例的做法在结果页叫“可直接采用”、在收藏页叫“怎么做”；适用边界在结果页/综合区叫“适用条件”、在收藏页叫“适用时注意”。用户在两个页面看同一个案例会以为是不同的信息。已统一为“怎么做 / 适用条件”。
- 主页来源说明“只有正文证据完整的项目进入结果”暴露了内部术语“正文证据”；改为“只收录文章内容完整的项目”。“数据管理”改为“备份与恢复”、“预检备份”改为“检查备份包”——按钮和页面只做这两件事，名字就该说这两件事。
- “研究已形成初步依据 / 已形成初步灵感”是用户在本会话早些时候亲自困惑过的状态；不改标签本身（已批准的措辞），但补了 title 解释（“已回答全部研究问题，但案例数量或深度未达完整标准，可作初步参考”）。“取消永久 / 设为永久”补了后果说明 title（“取消后改为保留 14 天，到期自动删除”）。
- 尝试过抑制“章节小结与首个案例机制逐字重复”，被 7 条既有 M128 契约测试挡下——这些测试明确要求每个案例块自带机制句。已回退。结构性根因记录在案：`App.tsx:2177-2180` 的题目级小结就是第一个案例机制的原文复制，所以每章开头的结论句必然在第一个案例里逐字再现一次。要消除这个重复只有两个方向（小结注明出处案例，或首个案例不重复机制句），都属于对已验收布局的改动，留给用户决定，不在文案轮里顺手改。
- 教训：0×0 视口下 `scrollWidth - clientWidth` 会给出假溢出（本轮测得 320）。浏览器面板收起后必须先显式 resize 再量测，不能把面板状态当页面回归。

## 2026-07-26 M134 章节结论不再在首案例复读

- 用户选择方向 B。设计决定：章节小结保留其“答案优先”位置，首个案例在机制句与小结逐字相同时省略该句；这不是删除信息，而是把 M73 折叠时代“结论只说一次”的本意带进 M128 的全摊开布局。
- 抑制规则刻意收窄到 `dossierIndex === 0 && designMechanism.trim() === questionSummary.statement`：小结取自首个非空机制并经过 trim，所以相等判断必须同样 trim；后续案例机制即使碰巧相同也不受影响。若首案例机制为空、小结来自第二个案例（罕见形状），不做任何抑制，宁可保留重复也不误删非来源案例的机制。
- 迁移的 7 条契约不是被削弱而是换位：机制的可见性仍被断言（章节层），新增“首案例内不存在”的负向断言，多案例测试继续要求 Warehouse Forum 等后续案例自带机制句。跨章节测试的原意（每章用自己的逐题分析、不塌缩进首章）用“章包含自己的机制 + 首案例不复读”表达。
- 真实耕织图 Run 复核证明规则按预期工作：四章案例数 5/1/1/4，案例机制段 4/0/0/3，结论句复读 0。单案例章节的阅读流最能说明收益：问题 → 结论句 → 项目名 → 怎么做 → 适用条件，没有任何一句读两遍。

## 2026-07-26 M135 剩余触点文案收口

- 三个未走查面（图纸灵感结果、运行/失败状态、工具与导出）用"三个并行审计 agent + 每条发现一个对抗核验 agent"的多智能体流程收口：45 条原始发现，43 条核验存活、2 条被驳回。核验层真实拦下了审计层的错误：有替换文案会让 UI 撒谎（就绪状态声称小红书可搜索，而该分支恰好是不可用时才渲染）、有的指向不存在的按钮、有的把方向搞反（"下方结果"实际在上方）——证明对抗核验不是走过场。
- 编排教训：第一轮核验的 prompt 忘了插入发现载荷，45 个核验 agent 全部以"无法核验原文存在，keep=false"正确拒绝。失败安全设计有效，但也说明**编排脚本的模板插值必须先自测一条**再放量；修复后从缓存恢复，审计层零重跑。
- 词汇层面的系统性发现：同一功能最多有三个名字（对照入口"对照案例策略"/"对照方法与边界"、对话框"方法对照"）；"档位""容量""配对码""精确提取""聚合来源""可嵌入"是内部概念直译；"分享版权利检查"存在真实的中文切分歧义（版权/权利抢词）；"权利 权利未知"是模板拼接出的显示级重复。修复统一为一词一概念，并新增 `copy-glossary.test.ts` 在源码层封禁 18 个废弃词，防止词汇漂移回流。
- 死胡同状态是比措辞更重的问题：扩展已连接但未授权时，界面只说"需授权"却没有任何按钮能完成授权（唯一说明藏在提交研究后的错误里）；连接状态读不到时只说"请检查本地服务"。这类状态现在都给出具体修复路径。收藏上限 6 此前完全不可见，第 7 次点击静默失败；现在计数处直接标注"（最多 6 张/个）"。
- 需要向后端同步的观察（本轮未改后端）：`visualLabels.blocked` 的诚实文案依赖 stop_reason 区分"环境不可用"与"没找到图"，当前展示层统一为"暂未找到可用图纸"，若试点中用户对环境类失败困惑，应把 stop_reason 映射进该状态。
- M121 观察工具包已写入 `docs/m121-pilot-kit.md`：10 个按序任务（含任务书路径与状态理解）、去身份化记录表、会后 5 问、P0-P3 处置阈值与硬边界。待用户招募 2–3 名真实用户后执行。

## 2026-07-27 M141 第二次保留期 P0：永久 Run 的证据被独立时钟清空

- 用户两次报告"点开历史研究是空的"，第一次被我误判为热更新伪影——因为我抽测的 `ff16988d` 恰好完好。API 逐条核查后坐实：`76f52c79`（M53/M65 验收 Deep）、`d995bed5`、`a2cf2e20`、`d13bdc67`、`58f4b9f9` 五条的 results 全部为 0。教训与 M136 呼应：**抽样验证会漏掉系统性缺陷，用户复报同一现象时必须全量核查，不能用一次成功样本关闭问题**。
- 根因：`lifecycle.py` 的清扫对 asset_candidates（7 天 TTL）、source_pages（30 天）、evidence_claims（7 天）按各自 `expires_at` 硬删，**完全不看所属 Run 的 `keep_forever`**。7/26 的 P0 修复只把 Run 行标记为永久，子数据的时钟照走——"永久封存"的语义第二次与代码脱节，这次是在子表层。
- 实测危险边缘：修复落地时 `10d31b4c`（封存 Run）与 `f5be3f17` 的资产已过期十余小时、`23b6f84c` 数小时后到期，全靠"API 恰好没重启"才幸存；下一次 Windows 登录的自启动就会触发清扫。修复（三个过期查询豁免 keep_forever Run 的子数据）在下一次启动加载，先于清扫执行。
- 可恢复性：7/25 备份含 `76f52c79` 的 51 资产 + 136 逐字证据（建筑资产为文本行，无磁盘文件依赖），已写外科恢复脚本；`d995bed5`/`a2cf2e20`/`d13bdc67`/`58f4b9f9` 的资产在更早的启动清扫中丢失且两份备份都没有，**不可恢复**。这四条 Run 行仍在、来源页仍在（30 天 TTL 未到），但作为"已验证"引用的底层证据已残缺，需要用户决定去留。
- 结构性教训：**同一保护语义必须覆盖对象的全部组成部分**。"保留 Run"在数据模型里横跨五张表，任何按表独立设计的 TTL 都会在语义上撕开缺口。新增的红绿契约把"永久 Run 的资产/来源/证据不过期"固定为行为测试。

- 参考站的可迁移信号在**品类分布**而不是具体页面：siteinspire 2026 策展前五是 Typographic/Design&Art/Portfolio/Web&Interactive/Minimal，加上 Grid Layout 742——证实"排版主导 + 网格节奏 + 单一强调色"仍是高级感的主流语法，我们的制图桌方向不需要转向，需要的是动效词汇量补齐（盘点仅 2 keyframes / 5 transitions）。
- 三设计师 + 逐组对抗核验的 workflow 第二次证明核验层的真实价值：6 条被拒的理由全部站得住——整页入场 stagger 编排正是 DESIGN.md 明令禁止的"千篇一律入场"（核验员正确指出 drawer-in/research-options-reveal 的先例只动单元素、从不编排兄弟）；两条用了非 token 色值字面量；一条给不存在的 DOM 写动画（.evidence-sheet-actions 内根本没有 aria-pressed 按钮）；Material 印圈庆祝动效被识别为通用套路而非制图语汇。**没有这层核验，这 6 条大概率会被我当作"看起来合理"落地。**
- 我自己预备的红灯也被这次裁决推翻：入场 stagger 契约（reading-rise + nth-child 封顶）写在产品自己的设计法禁区里。教训：**红灯测试要在方案裁决之后写，不要在构思之前押注具体机制**。
- 合成时发现两位设计师对同一选择器（.collection-dock-success）提了不同动画——多智能体输出必须由单一整合者裁决冲突（取 sheet-settle fast，保持"内容更替=fast 档"的语义一一对应），否则两条"各自合理"的规则叠加就是抖动。
- 落地时踩了一个结构约定坑：向文件中部插入独立 @media (max-width: 620px) 块会打破 design-system 测试的"860/620 各一块、按序在尾部"切片约定，5 条无关测试瞬间全红。样式表的媒体查询结构本身是被测契约，新增响应式规则只能并入既有块。

## 2026-07-26 M136 模拟 persona 走查（诚实替代，不冒充真人）

- 用户要求"编几个用户开测"。伪造真人观察被拒绝——规划里外部验收门明写人类参与者"intentionally not fabricated"；改为执行诚实等价物：3 个**无任何产品上下文**的 persona 子代理读真实页面文字做首见理解测试，结果明确标注"模拟"，M121 保持 in_progress。方法学上的关键点：子代理真的没见过产品，它们的误读是真实的首见误读，比主对话假装无知可信。
- 最有价值的信号是**三个 persona 独立撞上同样的词**：'轮流检索'（3/3 逐字引用为最不确定的词之一）、'研究已形成初步依据'（2/3 读成"还没跑完"）、保留期恐惧（3/3，包括"我会退回截图存相册"这种信任流失表述）。跨 persona 重复是比单条意见强得多的证据，工具包的 ≥2 复现阈值在模拟里同样有效。
- 一个此前所有代码审计都没抓到的数据形状问题被 persona 抓到了：'适用条件' 槽位里装着"页面不是《耕织图》或其版本出处"这类**来源免责句**。S1 的原话"既然不能用，为什么还列出来给我看"和 S3 的"适用条件应该跟着案例走，而不是跟着当初那个问题走"指向同一根因——`auditBoundaryPattern` 没把来源型免责句算作审计句。这类问题只有"带着目的读内容"的人能发现，纯词汇审计发现不了。
- S2 验证了 M135 的修复真实有效：'转载合集（非首发）· 权利 未注明' 被首见者准确理解为"搬运号发的、版权归属不明"，并能正确推理对自己使用场景的影响。文案修复的效果可以用同方法回归。
- 最重的未修发现是 S3 的出处链接冲突：M128 按用户要求删除了结果与收藏页的全部来源链接，而专业 persona 认为"没有出处的案例一条都不敢放进汇报"，收藏页（最接近产出的页面）恰恰最需要出处。这是答案纯净度与专业信任的真实矛盾，已在 `docs/m121-simulated-walkthrough.md` 列为需用户裁决的第一项。
- 模拟的边界同样得到确认：S1/S3 对"选择案例点了会怎样"的不确定部分源于纯文字模拟看不到点击反馈——这类发现必须降权，留给真人试点。模拟能测文字可理解性，测不了操作行为。

## 2026-07-26 M129 开发服务启停的 WMI 依赖

- 现象与最初判断的偏差：`Get-NetTCPConnection -ErrorAction SilentlyContinue` 在本机返回空并不代表端口空闲，它的 CIM 层此时已经坏了，`-ErrorAction SilentlyContinue` 把失败吞掉。真正可信的空闲证据是 `Get-AvailableTcpPort` 的绑定成功和 `netstat -ano`，两者都不经 WMI。
- 根因定位：MSIX 版 pwsh 7.6.4（`C:\Program Files\WindowsApps\Microsoft.PowerShell_7.6.4.0_x64__8wekyb3d8bbwe`）加载 `Microsoft.Management.Infrastructure.Native.Unmanaged.DLL` 抛 `DllNotFoundException … E_ACCESSDENIED`。Winmgmt 服务本身 Running，Windows PowerShell 5.1 的 `Get-WmiObject` 正常返回，用 `Start-Process` 另起的独立 pwsh 同样失败——所以坏的是这个 PowerShell 安装，不是 WMI，也不是调用它的 shell 环境。
- 失败链闭合：`start.ps1` 先成功拉起 uvicorn 与 vite（日志里有 `VITE v8.1.4 ready`），随后 `Get-WorkspaceListeningProcessIds` 抛错进入 catch，`Stop-Process -Force` 把两个刚起来的进程杀掉。表现是"连接被拒绝且日志无错误"，与用户看到的一致；M127 开机自启走的正是这条路径，每次登录都会重演。
- 该缺陷此前一直被掩盖：12:36 那次成功启动来自 Codex 运行时（自带 pnpm 与可用 CIM 的 pwsh），所以同一份脚本在同一天既能成功也能失败。pnpm 从 PATH 上"消失"和 CIM 失效是同一件事的两个侧面。
- 替代原语的可用性已实测：`netstat -ano` 在 zh-CN 上仍输出英文 `LISTENING`，但实现不依赖该词，只用环回本地端点加未连接对端地址判定；PEB 读法对 `node.exe`（可执行文件在 `C:\Program Files\nodejs`，工作区路径只出现在命令行里）和 uvicorn 都取到了与 WMI 完全一致的命令行。
- 仅凭可执行文件路径判定工作区归属是不够的：vite 监听进程是系统 Node，`apps/api/.venv/Scripts/python.exe` 又是 uv trampoline，实际进程显示为 `~/.cache/codex-runtimes/...python.exe`。两者都只有命令行里带工作区路径，所以必须保留命令行归属语义。
