# ArchResearch 新会话交接

## 新会话启动顺序

1. 先完整阅读本文件。
2. 阅读 `task_plan.md` 中状态为 `in_progress` 或 `implementation_complete` 的阶段。
3. 阅读 `findings.md` 和 `progress.md` 的末尾；只有追溯原因时才搜索更早记录。
4. 运行 `git status --short --branch`，保留所有既有修改和未跟踪文件，不得 reset、checkout 或批量清理。
5. 开始动作前，用两三句话复述“当前状态、下一步、验证标准”。

## 产品与不可逆决策

- 产品是面向建筑学生与青年设计师的本地优先实时研究 Agent，不建设案例库或全局向量索引。
- 公开建筑网站使用进程内 Direct Playwright；登录态小红书默认使用 `@jackwener/opencli@1.8.6` Browser Bridge。
- 不再使用 Firecrawl，不要恢复任何 Firecrawl 配置、Key、积分或余额界面。
- ArchDaily、事务所官网等负责落地案例和方案证据；小红书负责制图、配色、形体、分析图和视觉灵感，始终按 `aggregator / visual_lead` 处理，不能单独证明项目事实。
- 所有模型统一为 `gpt-5.6-sol`，推理强度统一为 `medium`。
- 图纸灵感固定使用小红书；OpenCLI 搜索异常或返回空结果时回退 ArchResearch 扩展。建筑设计研究不携带小红书来源，也不以 Chrome/页面高清图纸权限作为启动前置条件。
- 图纸灵感的每个方向按排名最多尝试 4 篇小红书笔记，在 48 个逐图检查槽位 / 48 MiB 总预算内目标累计 3 篇 usable 帖子；每帖仍等距取最多 4 图并合并做一次视觉批量分类，扩展后备在固定预算结束时诚实保留 partial。XHS-only 的 OpenCLI 与扩展路径都失败或为空时必须诚实终止，绝不降级为通用网页素材。
- 正式方案案例按项目而不是按单篇文章组织。每条事实必须分别绑定来源 URL 和逐字引文；一篇主文章确认项目身份并提供至少一个具体设计机制后，缺失背景或机制可经一次定向联网搜索、最多两个可信文字来源补齐。同项目证据可合并，不同项目不得合并；`transfer` 是明确标注的 ArchResearch 设计转译，不要求原文直接回答用户问题；模型 `relevance` 只排序，不能推翻已有逐字证据。补查后仍不完整的案例保留为部分证据和原站入口，只要 Run 已形成有用的引用答案，就不得仅因局部分支缺正式案例而整体 `blocked`。Quick、Balanced、Deep 的差异是综合强度：Quick 核心因果与直接建议，Balanced 条件—操作—结果、比较与边界，Deep 跨案例综合、冲突/不确定性和失效边界。图片只作可选预览和原站入口，不证明机制、不参与案例准入；文字研究未完成时不执行图片批次。严格视觉相关性只属于 `visual_reference_search` 或明确图片/图纸任务。
- 产品界面不得展示供应商余额、作品集预留额度或”网页研究额度”。
- “永久封存”同时意味着不得 retry **和**不得被保留期清理删除：正式验收 Run 与全部封存 Run 必须保持 `keep_forever=1`。新建 Run 默认仍是 14 天保留，任何被当作验收证据的 Run 在接受后都要显式标记永久，否则会在下一次 API 启动时被 `cleanup_expired_data` 硬删除。
- 用户已授权当前三档验收主线持续自主推进，但明确禁止“每修一个错误就立即新建一条 Deep”。失败 Run 永久封存且不 retry；先合并诊断全部已知失败链，完成封存 Run 形状的零 Provider 端到端重放与完整门禁，只有综合预检通过后才创建一条必要的全新 attempt 0。无需逐轮等待批准，但必须最小化 Live Run 数量；授权仍不扩大到 Firecrawl、视觉来源、截图、提交或其他产品范围。

## 已完成与验证

- M42 release-candidate closeout 已完成：范围、安全、证据、发布风险和提交边界均已复核；没有暂存或提交。
- M41 已完成：OpenCLI 固定无 shell 调用、严格 JSON、签名 URL 脱敏、`/search_result/` 支持、多图下载、四图单批分类、异常/空结果回退和运行时自动发现均已接入。
- 正式依赖已写入根 `package.json`/`pnpm-lock.yaml`，许可证写入 `THIRD_PARTY_NOTICES.md`。
- M43 与 M44 已完成：运行前检查已减重；来源检视器已由 500px 文字抽屉改为 1040px 桌面图纸/分析双区和窄屏图片优先单列，桌面与 390px 真实页面复核通过。
- M45 发布前最终全仓 `scripts/verify.ps1` 已覆盖上述 UI 增量并输出全部检查通过：226 API、75 Board、165 Extension、8 packaged Chrome E2E；Ruff、Mypy、ESLint、TypeScript、生产构建与 30/108 evaluation 全部通过。
- M46 实现已完成并通过新的全仓门禁：228 API、76 Board、165 Extension、8 packaged Chrome E2E。正文优先、逐字证据约束、文章分析覆盖门、运行级资产绑定综合和三档强度下限均已落地；Board 只把分析完成的项目编入案例，综合结论采用内联平面区段和渐进披露。
- `git diff --check` 已通过，仅有 Windows LF→CRLF 提示。
- M47 最新离线门禁通过：完整 browser-inspection、Ruff lint/format、strict Mypy 18 source files、277 API tests 与 `git diff --check` 全绿。
- M48 text-first precedent research 已完成并通过完整仓库门禁：279 API、76 Board、165 Extension、8 packaged Chrome E2E，Ruff、strict Mypy、两端 lint/typecheck/build 和 30/108 evaluation 全绿；没有 Live Provider、Firecrawl、Deep 或截图调用。
- M49 唯一真实 Quick `c8a67738-a827-462f-8914-760e519d1201` attempt 0 已封存：正文主链 3/3、5 projects、8 article-ready、8/8 逐字绑定、14/14 page analyses completed、Firecrawl 0；最终唯一 synthesis 调用 `APITimeoutError`，终态 `blocked/research_synthesis_incomplete`，不得 retry。
- M50 已完成：Quick 综合按计划子问题顺序各取现有 rank 中首个完整 case，并按 asset ID 去重；Balanced/Deep 原切片不变。封存 M49 的零 Provider scratch 从 7 cases 收敛为 3、三题全覆盖、payload 5,330 字；完整离线门禁通过 280 API、76 Board、165 Extension、8 packaged E2E。
- M51 已完成：唯一新 Quick `a2cf2e20-af1f-4ffb-a2c2-9498c48ffe73` attempt 0 终态 `completed/coverage_satisfied`，21 usable、7 projects、3/3、gaps 0、11/11 page analyses completed、48 Trace、Firecrawl 0。三题首个完整 case 按 asset ID 去重后实际发送 2 cases / 5,662 chars，唯一综合约 32.3 秒成功。
- M52 只读发布边界审计已完成：105 个路径无遗漏分为 84 product、5 engineering records、11 release evidence、5 local outputs；10 张 PNG 哈希全匹配，新增内容高置信凭据扫描与生产 Firecrawl 扫描均为 0。未 stage、commit、清理、移动或删除文件。
- M53 当前规则三档验收已完成：Quick M51、Balanced `d995bed5-8c40-4df9-80e4-4fe6256d3628` 与 M65 Deep `76f52c79-5fd9-4738-837d-5576eb3a72cd` 均已接受。最终 Deep 在 attempt 0 完成 `completed/completion_satisfied`：51 usable、9 formal projects、6/6、29/37 次正文分析 completed、Firecrawl/视觉/图片批次 0；唯一 synthesis `APIConnectionError` 由 M54 deterministic fallback 以精确 2/2/1/2/2 完成。所有早期失败 Deep 仍永久封存且不得 retry。
- M54 deterministic synthesis failure fallback 已离线实现完成。可恢复的唯一综合失败不再重试 Provider，而是只用逐字证据绑定的 branch context/mechanism/transfer/limitations 生成明确标注的“本地证据汇总”；Quick 为 1/0/0/0/1，Balanced/Deep 为 2/2/1/2/2，普通编程异常仍失败。封存 Deep 零写入重放与完整 Run 失败 fixture 均通过；最终完整门禁为 288 API、76 Board、165 Extension、8 packaged E2E，静态检查、构建和 30/108 evaluation 全绿。未创建或 retry Live Run。
- M55 direct-page parser synthesis reserve 已离线实现完成。短公开搜索仍可在模型调用放不下时执行，但每个直接来源页开始前都重新检查 parser worst-case reserve；两页 Deep fixture 只解析时间足够的第一页、跳过第二页并恰好综合一次。最终完整门禁为 288 API、76 Board、165 Extension、8 packaged E2E，静态检查、构建和 30/108 evaluation 全绿；没有创建或 retry Run。
- M56 coverage-first normal-round scheduling 已离线实现完成。precedent coverage 未齐时，正常轮跳过已覆盖分支，把下一次查询机会留给未覆盖题；coverage 齐全后正常 enrichment 保持。最终完整门禁为 290 API、76 Board、165 Extension、8 packaged E2E，静态检查、构建和 30/108 evaluation 全绿；没有创建或 retry Run。
- M58 uncovered-branch analysis fairness 已离线实现完成。coverage 未齐时，每个 `(subquestion, query generation)` 最多执行 3 次公共页正文分析；6/6 后解除该限制并保留第 4 次 enrichment。六题最坏耗时重放只恢复缺失 branch-a、在 research deadline 内完成 6/6，唯一综合错误由 M54 fallback 精确生成 2/2/1/2/2。最终完整门禁为 293 API、76 Board、165 Extension、8 packaged E2E，静态检查、两端构建与 30/108 evaluation 全绿；实现阶段没有 Live Run、retry、Firecrawl、视觉来源或截图。
- M59 uncovered follow-up analysis slicing 已离线实现完成。首轮上限 3 保持；coverage 未齐时第 2 轮起每个缺失题每轮只给 1 次正文分析，6/6 后 enrichment 不限。多缺口六题重放按 A/E/F 各 1 次完成 6/6，唯一综合错误由 M54 fallback 精确生成 2/2/1/2/2；完整门禁为 293 API、76 Board、165 Extension、8 packaged E2E，全部静态、构建和 30/108 evaluation 全绿。
- M61 project-level multi-source text evidence 已离线实现完成。同项目多来源按严格项目身份合并，每条事实保留自己的 URL 与逐字引文；每项目一次定向补查、执行层最多两页，低 relevance 只排序，transfer 明示为分析，有用不完整结果为 partial，必需浏览器失败仍 blocked，文字 coverage 前不运行图片批次。最终基线为 301 API、76 Board、165 Extension、8 packaged E2E，Ruff、strict Mypy、两端 lint/typecheck/build 与 `git diff --check` 全绿；没有创建或 retry Run。
- M62 multi-asset project supplement eligibility 已离线实现完成；同项目第一张空资产不再遮蔽后续候选的机制或部分分支证据。最终基线为 302 API、76 Board、165 Extension、8 packaged E2E；唯一 Live Deep 证明资格路径触发 5 次，但补查全部返回 0，Run 因两题未覆盖而 partial。
- M63 concise project supplement query 已离线实现完成。补查仍严格每项目一次、最多两页、可信域不变，但查询从“项目名 + architecture case study + 5–6 个焦点词”缩短为“项目名 + 2–3 个分支焦点”，并补齐公共界面题。最终完整门禁为 303 API、76 Board、165 Extension、8 packaged E2E，Ruff/format、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation 全绿；没有创建第二条 Run。
- M64 clean project name for supplement search 已实现并完成唯一 Live 验收。clean source identity 与同页 parsed-title identity 共同保持严格同项目匹配，搜索只用 clean source/case name，两种 identity 共享一次 attempted/pages 缓存；M64 Live 已封存为上述 5/6 partial，绝不 retry。
- M65 target-site-language query compaction 已完成离线与 Live 验收。混合中英文的单站查询按目标站点语言压缩：ArchDaily China 用中文，Designboom/Dezeen/Divisare 等英文站用英文；纯语言查询与 M64 多域精确项目补查不变。完整离线门禁为 307 API、76 Board、165 Extension、8 packaged E2E，加 Ruff、48-file format、strict Mypy、两端构建与 30/108 evaluation；唯一 Live Deep 随后完成 6/6。
- M66 跨问题三档与记录可见性审计已完成。社区图书馆采光/通风、山地校园高差/无障碍/流线、滨水工业遗址防洪/开放/结构保留三类问题分别运行 Quick、Balanced、Deep，共 9/9 沿完整 text-first 证据门完成；三档均以项目案例包为单位，同项目可合并多个可信正文页，每条事实仍绑定自己的 URL 与逐字引文。最新 Deep `76f52c79-...` 位于 `作品集实测 · 旧厂房社区文化中心`，不是 `M6 真实研究验证`；Board 已修复无图片观察时隐藏正文案例及按资产误报项目数的问题，真实页面显示 9 个唯一正式项目。最终完整门禁为 310 API、78 Board、165 Extension、8 packaged E2E，加 Ruff、48-file format、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation；没有创建 Live Run 或截图。
- M67 成品工作区命名 polish 已完成。持久化的 `M6 真实研究验证`、`M33 完整性验收`、`作品集实测 · 旧厂房社区文化中心` 已在 active Run 0 和名称精确守卫下改为 `旧厂房更新 · 历史研究`、`旧工业建筑 · 结构与功能植入`、`旧厂房社区文化中心`；60 条历史 Run、workspace ID、brief 与证据均未改变。Board 动作改为“新建工作区 / 创建工作区”，并记住上次选择，刷新后不再回到最老工作区。无截图真实页面与 390px DOM QA 通过；最终门禁为 310 API、80 Board、165 Extension、8 packaged E2E，加全部静态、构建和 30/108 evaluation。
- M68 项目上下文信息架构已完成。项目选择与新建从全局顶栏移入首页“最近研究”标题区，作为下方历史任务的父级筛选，而不是相似的第二个模块；对外文案为“研究项目 / 新建项目 / 创建项目”。结果页完全不显示项目管理，顶栏只保留品牌与当前任务动作。Workspace schema/API/ID、选择记忆、创建逻辑与 60 条历史 Run 均未改变。无截图桌面、结果页与 390px DOM QA 通过；最终门禁仍为 310 API、80 Board、165 Extension、8 packaged E2E，加全部静态、构建和 30/108 evaluation。
- M69 案例密度与问题小结已完成。CollectUI 根站和唯一索引图片回退均不可用，因此没有把不可验证模式归因给该站；项目档案已从共享行高四格改为两条自然高度分析列，390px 顺序折叠。每个有正文案例的子问题在全部 dossier 后只生成一次题目级小结，只复用本题机制、每项目一个首要转译动作和适用边界，不读取图片观察、无案例不生成。真实 M65 Deep 显示 6/6 小结、桌面/390px 无溢出，首题移动小结从约 907px 收敛到约 530px；最终门禁为 310 API、82 Board、165 Extension、8 packaged E2E，加全部静态、构建和 30/108 evaluation。没有 Provider、Firecrawl、Live Run 或截图调用。
- M70 案例全宽密度 refinement 已完成。用户真实截图证明 M69 两列外壳和中文 `72ch` 仍留下空面；现已改为 112px 标签轨 + 全宽正文的四行台账，宽屏长转译按 DOM 顺序以 3+2 两栏落位、≤860px 单栏。单项项目预览铺满 gallery 为左图右文，移动端图上文下；多项仍为网格。真实 M65 在 1235px 下 transfer 宽 962px、右侧仅 24px padding，single sheet 宽 1098px；390px 无溢出且正确折叠，6/6 问题小结保持。最终门禁为 310 API、83 Board、165 Extension、8 packaged E2E，加全部静态、构建和 30/108 evaluation；没有 Provider、Firecrawl、Live Run 或截图调用。
- M71 全界面认知负担审计已完成，Nielsen 23/40。首页基本可用，四个 P1 集中于结果概览不可读、证据库存默认全展开、工具位于答案之前、深度/证据术语偏内部；完整报告归档于 `.impeccable/critique/2026-07-19T13-47-08Z__apps-board-src-app-tsx.md`。
- M72 已按用户“一项一项来”完成第一个 P1。正常短综合不变；fallback、系统前缀或异常超长 answer 只从既有 causal mechanism 投影为一条可读判断，recommendation 去掉系统包装、去重并最多三条，完整 answer 和本地整理身份仍在折叠审计层。真实 M65 标题从 207 字机器串变为 54 字机制，2 条直接动作、desktop/390px 无溢出、error log 0；完整门禁为 310 API、84 Board、165 Extension、8 packaged E2E，active Run 0。
- M73 已按用户“一项一项来”完成第二个 P1。每个有正文案例的子问题先显示一条既有证据机制判断和最多两个按原 rank 选择的代表案例；全部完整 dossier、预览、逐字证据、来源与操作仍在一个默认关闭的原生展开内。真实 M65 为 6/6 小结在前、代表案例数 `[1,2,2,1,2,2]`、6/6 inventory closed，展开第三题后 3 个项目与 3 条原站来源完整；desktop/390px 无溢出、error log 0。完整门禁为 310 API、85 Board、165 Extension、8 packaged E2E，active Run 0。
- M74 已按用户“一项一项来”完成第三个 P1。结果页现在按“任务 → 结论 → 问题拆解 → 案例证据 → 继续使用工具”展开；研究过程、方法对照、表达规范、个人导出和分享导出五项能力原样保留在答案之后，三项初始 disabled 条件未改变。真实 M65 desktop/390px DOM 顺序、两列/单列布局、无溢出与 error log 0 均通过；完整门禁为 310 API、85 Board、165 Extension、8 packaged E2E，active Run 0。
- M75 已按用户“一项一项来”完成第四个 P1。概览/标准/深入都明示以多个项目案例为依据，对外收获为“快速找方向 / 形成方案依据 / 做跨案例论证”；内部 `quick/balanced/deep` 请求值不变。`verified/partial/visual_lead` 对外固定为“来源与内容已核对 / 已有依据，仍需核对 / 只作视觉参考”，0–4 relevance 仅作内部排序/门限并映射成自然语言。真实 M65 desktop/390px 无截图 QA 已通过；最终唯一一次完整门禁为 310 API、89 Board、165 Extension、8 packaged E2E，active Run 0。未调用 Provider/Firecrawl，未创建或 retry Run，未截图，未处理来源检视器 P2。
- M76 图纸灵感独立入口已完成。首页只保留“建筑设计研究 / 图纸灵感”两个同级主入口；建筑设计保留概览/标准/深入，图纸灵感隐藏深度并使用固定 24 calls / 48 MiB。XHS 每题一次 ranked search，最多按 rank 处理前两篇；第一篇有 usable 即停，只有 0 usable 才处理第二篇；有 XHS 候选时不并行同题通用 Provider，OpenCLI 失败/空结果才走扩展后备。最终门禁为 313 API、90 Board、165 Extension、8 packaged E2E，加 Ruff/48-file format、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation。唯一 post-fix Live `f5be3f17-...` attempt 0 completed 3/3，保留 5 个 XHS-only visual assets，三次同题通用搜索均 skipped，Trace token marker 0；真实证明首篇成功快路径，top-2 fallback 保持离线红绿证明，不为强行触发 fallback 再开 Run。
- M77 图纸灵感风格方向已离线实现完成。视觉任务不再拆功能、流线、项目条件或来源核验问题；用户点名轴测图、剖面图、效果图等类型时，所有方向固定该类型，只变化线稿、拼贴、材质、图解等表达风格。OpenAI 擅自切换图纸类型时由同序确定性方向纠正；旧问句形状的 M76 Run 只在 Board 标为“旧版灵感分组”，不伪造当前风格、不迁移历史数据。最终门禁为 317 API、93 Board、165 Extension、8 packaged E2E，加 Ruff/48-file format、strict Mypy、两端 lint/typecheck/build 与 30/108 evaluation；未重载服务、未创建/retry Run、未截图。
- M78 单一 Live 风格验收与失败后离线修复已完成。唯一 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` attempt 0 completed/coverage_satisfied：plan 为精细线稿/拼贴叙事/材质渲染轴测图，10 usable、3/3、3 篇 XHS-only、全部 aggregator/visual_lead/unknown rights，三次通用搜索 skipped、Trace token marker 0。逐图审计发现其中 8 张 axonometric，拼贴笔记另混入 1 analysis_diagram + 1 site_plan；该 Run 永久封存、绝不 retry。最终实现会在视觉批分类后、持久化前按方向明确类型过滤，0 正确类型才尝试第二篇，并让 workflow deterministic fallback 同样生成风格方向。封存形状红绿和最终 319 API / 93 Board / 165 Extension / 8 E2E 全绿；最终代码已重载，当前 3 projects / 64 Runs / active 0，未创建第二条 Live、未截图。
- M79 来源检视器层级精简已完成。检视器不再重复项目档案的条件、机制、转译、观察、推断和边界；右栏先显示 URL 绑定的逐字原文 claim、页码和定位链接，再常显发布来源、项目身份、图片归属和权利状态。左侧预览/原站入口、收藏/拒绝和备注保持。最终完整门禁为 319 API、93 Board、165 Extension、8 packaged E2E，加全部静态、构建与 30/108 evaluation；Impeccable detector 0 findings。未创建或 retry Run、未调用 Provider/Firecrawl、未截图。
- M80 无截图来源检视器体验收口已完成。既有真实正文结果在 1280×720 与 390×844 均通过逐字证据优先、零重复 dossier 分析、完整来源/核验/动作/备注、无横向溢出与 Escape 精确焦点返回；唯一发现的紧凑屏触控缺陷已按行为红绿把关闭、原始来源、收藏、拒绝全部提升到实测 44px。最终 Board 94/94、Impeccable detector 0、完整 319 API / 94 Board / 165 Extension / 8 E2E 门禁全绿。浏览器已恢复 `旧厂房社区文化中心` 与默认视口；用户随后因无法访问明确要求恢复服务，API/Board 现已在 8000/5173 ready。当前仍为 3 projects / 64 Runs / active 0。未创建/retry Run、未调用 Provider/Firecrawl、未截图、未执行 Git 写操作。
- M81 高容量帖子分组图纸灵感已完成。默认 OpenCLI 每方向最多尝试 4 帖并累计 3 篇 usable notes，每帖最多 4 图；extension fallback 在固定 24 calls / 48 MiB 内诚实结束 partial。Board 以帖子为标题/观察/来源/权利单位，以图片为逐图类型、检视器和独立对照单位。desktop/390px 无截图 QA、319 API / 94 Board / 165 Extension / 8 packaged E2E 与全部静态/构建门禁通过；当前仍为 3 projects / 64 Runs / active 0，未创建 Live Run。
- M83 XHS-only fail-closed 已离线实现并加载当前服务。OpenCLI→extension 两条 XHS 路径都失败时 Direct public/model search 与 generic assets 均为 0；focused 红绿、M78/M81 回归和最终 321 API / 94 Board / 165 Extension / 8 packaged E2E 全绿。API 于 workflow 修改后标准重载；当前 3 projects / 65 Runs / active 0，本轮未创建 Live Run。
- M85 48-slot 视觉检查容量已达 `implementation_complete` 并加载当前服务。图纸灵感现为 48 个逐图槽位 / 48 MiB；OpenCLI 三方向最坏各四帖可恰好完成，extension 不足三帖时诚实 `partial/visual_budget_exhausted`，真实截止时间才使用 `time_budget_exhausted`。最终 323 API / 95 Board / 165 Extension / 8 packaged E2E 与全部静态/构建门禁全绿；当前 3 projects / 66 Runs / active 0，未创建或 retry Live Run。
- M86 loaded-M85 单次 Live 验收已完成。唯一 Run `23b6f84c-8658-43c7-9317-32c0696ca583` attempt 0 completed/coverage_satisfied：25 张 XHS-only axonometric、8 个唯一帖子、三方向各 3 usable posts、generic 0、40/48 slots。无截图真实 Board 为 3/3/3 帖子组、11/8/9 图片与独立对照，overflow/error 0；没有 retry 或第二条 Run。当前 3 projects / 67 Runs / active 0。
- M61 完整门禁曾由 Windows PowerShell 5 错误吞掉中间根 TypeScript 失败，因此脚本末行不能单独作为证明；Board 单独重跑 76/76、随后根 `pnpm run check` 以显式退出守卫完整通过。未来在 PowerShell 5 下不得只信 `scripts/verify.ps1` 的最终成功文案。
- API Key 保存在 Windows 凭据管理器，不在项目文件；不得打印或迁移 Key。
- 发布证据冻结在 `docs/release-evidence-2026-07-16.md`，包括三个 accepted run、Board QA 和 10 张截图的 SHA-256。

## 当前唯一主线

1. M46 已把“深度”从候选数量改成研究强度。正文阅读是所有正式案例的共同准入；只有逐字引文支持的项目条件、设计机制和转译步骤才可覆盖子问题。Quick / Balanced / Deep 的运行级综合有不同结构化最低要求。
2. 旧 Quick `d13bdc67-...`、Balanced `7d8faa53-...`、Deep `b4c314a6-...` 都发生在 M46 之前，只能作为历史运行与旧发布证据，不能再证明当前三档研究质量；现阶段也不制作或更新截图证据包。
3. M46 最终 `scripts/verify.ps1` 已输出全部通过：228 API、76 Board、165 Extension、8 packaged E2E，静态检查、生产构建与 30/108 evaluation 全绿；没有 Provider、真实来源、小红书或 Firecrawl 调用。
4. 缓存直接项目页的分支重分析已完成全局红绿修复并在新 Quick `065d4a8a-aa83-42f2-b983-983037a1b5b1` 的唯一 attempt 0 中真实验证：页面正文/图纸仅解析一次，新 `(source_url, subquestion_id)` 各自执行证据门分析，并以分支自有条件/机制/转译/逐字证据决定覆盖。该 Run 诚实终止 `blocked/research_synthesis_incomplete`：16 usable、3 formal projects、2/3，缺失 `flow-separation`；最终综合另外出现一次 `APITimeoutError`，106 条 Trace 中 Firecrawl 为 0。不得 retry 该 Run。
5. 站内搜索压缩器的全局红绿修复已完成并通过真实验收。新 Quick `b7c91d0d-42c9-429a-ae4f-11368c9caa0d` 的 12 条实际站内短查询全部保留工业类型和正确功能/流线/剖面主意图，包括第 4 轮恢复词噪声。该 Run 仍在唯一 attempt 0 终止 `blocked/research_synthesis_incomplete`：20 usable、1 formal project、12 verified/partial、1/3，仅 `flow_separation` 覆盖；唯一正式项目 De Nederlandsche Bank 有 6 段逐字流线证据，功能植入与剖面仍无 article-ready 公共图纸，最终综合另外以 `APIConnectionError` 失败，Trace 中 Firecrawl 为 0。不得 retry 该 Run。
6. 该失败的下一层全局缺陷已经红绿修复：首个可信但仅含照片的项目页不再独占子问题唯一远程批次；同一查询内的具体项目页先解析，再按页轮转组成一次最多 4 图的跨页批次，分类结果回写各自来源，获得图纸的可信页才进入文章证据门。新两页行为测试和既有批次/缓存/正文门均通过。随后新 Quick `fe0aea3a-1908-4409-9aa6-013f440312df` 在唯一 attempt 0 终止 `blocked/no_new_assets`：16 usable、9 verified/partial、1 formal project、1/3，仅功能植入覆盖；跨页批次已真实覆盖 4/3/2 页，资产绑定综合成功，功能与剖面批次各有一次上游 `InternalServerError`。不得 retry。
7. `fe0aea3a-...` 的确定性恢复召回缺陷也已全局红绿修复：站内搜索与 workflow 共用工业改造类型同义词和功能/流线/采光/剖面意图相关性评分，只重排不删除；`architecture/projects/work` 等路径标记必须后接具体 slug，频道根页不能冒充项目展开。四类矩阵、workflow 页槽和链接边界测试通过。随后新 Quick `080d60e9-714a-4f87-b4e0-49bad1394fac` 在唯一 attempt 0 到达 12 usable、12 verified/partial、2 formal projects、2/3；Designboom 已先解析钢厂博物馆与铁路仓库，证明排序修复真实生效，但功能唯一远程批次仍在首轮消费，恢复强页无法分类；最终综合另有 `APIConnectionError`。不得 retry。
8. 跨轮批次缺陷现已红绿修复：文章研究的公共页候选按子问题跨所有计划轮累计，到该分支最后一个查询才用共享类型/意图相关性和正文/图纸信号选优，仍只执行一次最多 4 图的批次；其他研究目标保持即时行为。随后新 Quick `98a0b853-3531-479e-a308-dc3341ffd787` 在唯一 attempt 0 终止 `blocked/budget_exhausted`：14 usable、14 verified/partial、2 formal projects、2/3，跨轮流线批次确实到第 5 轮才执行 4 图/4 页，但唯一调用 `InternalServerError`；资产绑定综合成功。不得 retry。
9. 全新 Quick `42668844-497e-453f-bcd4-38c9f25cbb25` 已在唯一 attempt 0 通过：终态 `completed/completion_satisfied`，18 usable、3 formal projects、3/3、gaps 0；资产绑定综合完成（4 cases），30 条逐字证据声明可审计，78 条 Trace 中 Firecrawl 为 0。稳定正文分析焦点与此前的公共页召回、图库准入、跨页抽样和统一 45 秒请求边界共同通过了真实链路验收。
10. M47 最终 Balanced `08e0ad17-cac5-4918-9d57-bf4c6f32f237` 已在唯一 attempt 0 封存为 `blocked/budget_exhausted`，不得 retry：48 usable、6 projects、3/4，仅缺 `program_insertion`；综合成功且满足 2/2/1/2/2，20 次正文分析全部 completed、Firecrawl 0。它证明旧 drawing gate 会丢弃强正文，不能再作为当前规则的验收结论。
11. M48 已全局撤销 precedent 的 drawing gate：完整逐字正文链可以绑定意图匹配预览、普通同源照片或无图文本案例；无图缓存页也能跨子问题复用。Provider 提示、持久化、coverage、Board 和文档已统一；明确视觉任务的分类与相关性门保持不变。完整离线门禁通过。
12. M49 单一 Quick `c8a67738-...` 已真实验证 text-first 正文主链，但整体验收因最终 synthesis `APITimeoutError` 失败；该 Run 封存，不得 retry。所有正式案例本轮恰好有同源预览，完全无图路径仍只由离线红绿测试证明。
13. M50 已完成 Quick 综合输入的离线确定性限界：每个计划子问题保留首个完整正文 case，同一 asset 只发送一次；封存 M49 重放为 3 cases / 3 branches / 5,330 chars，完整门禁全绿。Balanced/Deep、爬取、图片和综合输出强度均未改变。
14. M51 已真实通过 M50：三题各绑定首个完整正文 case，同一 asset 覆盖多题时只发送一次；唯一综合在 45 秒内成功，终态 completed，Firecrawl 0。不得为机械凑数重复同一 case。
15. M52 建议提交边界为三层：84 个产品源码/测试/依赖/用户文档路径；5 个工程记录；11 个可选发布证据路径。5 个本地工具输出始终排除，禁止 `git add -A`。产品改动跨后端、扩展、Board 和文档共同门禁，不应按旧里程碑强拆出不可验证的中间提交。
16. M54–M57 已共同解决综合可恢复失败、直接页 parser reserve、coverage-first 调度与封存 5/6 形状预检；M58 进一步限制 coverage 未齐时每个 `(subquestion, query generation)` 最多 3 次正文分析，并在 6/6 后恢复 enrichment。六题零 Provider 重放与完整门禁已经共同通过。
17. M59 后 Deep `aa1923fd-45cc-44f4-9a7c-edd6565b1994` 已永久封存为 4/6，绝不 retry；program/daylight 各获得 7 passes，M59 与 M54 均通过。七条 2026-07-18 Deep 的只读时间审计确认 202.8 分钟中 167.9 分钟位于正文分析槽，抓取解析不是主瓶颈；单篇文章准入、低 relevance 否决、即时同页二次请求和局部缺口导致整条 Run blocked 才是根因。M60 的低分证据修复已被 M61 吸收。
18. M61 已离线完成项目级多来源文字证据、严格补查上限、错误项目隔离、有用部分结果终态、文字优先图片延后和单次 45 秒正文分析合同。Live Deep `0715ab7e-6580-42ae-9d84-77a0c772c760` 已永久封存为 `partial/research_synthesis_incomplete`：38 usable、5 projects、4/6、13 次正文分析 completed / 11 failed、Firecrawl 0、图片批次 0。其 coverage 与 synthesis 合并审计已经完成，不得 retry。
19. M62 已红绿修复项目补查资格并由 Live Deep `eea9cf24-58d1-451d-828a-ec8aa7706a59` 验证路径真实触发。该 Run 永久封存为 `partial/no_new_assets`：24 usable、6 projects、4/6、sectional/public 各 7 passes、23 次正文分析、Firecrawl 0、图片批次 0；5 次项目文字补查全部 result_count 0。唯一 synthesis `APIConnectionError` 由 M54 fallback 成功输出 2/2/1/2/2，因此失败主因不是综合。
20. M63 已完成短查询实现，但 Live Deep `c553f2be-52fe-436e-8891-0d4fadcad5e0` 仍封存为 4/6。四次补查查询的项目名分别带 `RENOVATION`、`PUBLIC SPACE`、`CULTURAL CENTER`、`NANJING` 等解析页栏目/地点前缀；26 次正文分析为 14 completed / 12 failed，唯一 synthesis 为 `APIConnectionError`，Firecrawl 与图片批次均为 0。失败后的 coverage/synthesis 合并审计和零写入 fallback 重建均已完成。
21. M64 已让补查匹配同时接受 clean source identity 与同页 parsed-title identity，搜索只用 clean source/case name，并把两种 identity 绑定到同一一次补查缓存。唯一 M64 Live `8d12e14b-...` 已封存为 5/6 partial，绝不 retry；失败后已先完成 flow 召回、正文和综合的合并离线诊断，没有连续 POST。
22. M65 已修复真实缺陷并由唯一 Live Deep `76f52c79-...` 验证完成：51 usable、9 formal projects、6/6、completion_satisfied，active Run 0。M53、M65、M66、M67、M68、M69、M70、M71、M72、M73、M74 与 M75 均 complete；三档共同采用项目级多来源案例包，跨问题矩阵 9/9。成品当前项目为 `旧厂房社区文化中心`，刷新会保持选择；项目切换/新建只位于首页“最近研究”，结果页不显示。结果页先给任务与可读判断，再按问题展示代表案例和按需完整证据；继续使用工具位于研究内容之后并按建筑设计/图纸灵感分流。三档现在用成品收获区分深度，证据状态与相关度不再暴露内部术语或分数。
23. M76 已 complete。既有 Live `e525ca77-22b6-4751-8062-3730f933ba13` 保持 completed/attempt 0/completion_satisfied，但混入两条通用网页资产；`36889c78-880e-4b1f-8757-3050d1100cf2` 保持 blocked/attempt 0/no_usable_assets；两条永久封存、绝不 retry。最终 Live `f5be3f17-2698-4aa6-8f03-155a04e4c5a2` completed/attempt 0/coverage_satisfied：5 usable、3/3、全部 XHS-only，三次同题通用搜索均 skipped，token marker 0。当前为 3 projects / 63 Runs / active 0；不得为强行触发 top-2 fallback 再开 Run。
24. M77 与 M78 均为 `implementation_complete`。自然轴测输入的同类型多风格 planner 已 Live 证明；逐图同类型过滤由唯一封存 Run 的真实 8+2 形状重放和完整门禁证明，最终服务已加载。当前为 3 projects / 64 Runs / active 0；`10d31b4c-...` 永久封存、绝不 retry。不得自动创建下一条 Live；若用户以后明确要求 post-fix Live，必须先复核当前状态，并在失败后继续先离线合并诊断，不能连续 POST。
25. M79 已关闭 M71 报告中延后的来源检视器 P2；四项 P1 与该 P2 现均完成。检视器的主任务固定为核对逐字原文与来源，不再作为第二份案例分析。
26. M81 已 complete。当前唯一已批准下一步是 M82：复用自然轴测问题创建一条全新 post-fix attempt 0，验证多帖子累计、逐图类型过滤与帖子级 Board。不得 retry 或创建第二条；任何失败先离线合并诊断。
27. M82 唯一 Run `cbf8053b-f0c6-4408-b0d7-4b0ffff13b16` 已终止 partial 并永久封存：11 个通用网页结果、0 个 XHS note，绝不 retry 或补开第二条。
28. M82 合并诊断已确认双根因：API 进程早于 M81 workflow 修改且无 auto-reload，所以本次不是 M81 累计逻辑的有效 Live 测试；同时 OpenCLI 失败、extension XHS backend 未连接时，当前源码仍会把 XHS-only 降级为 Direct public search。
29. M83 为 `implementation_complete`，当前服务已加载 M81+M83。
30. 当前唯一已批准下一步是 M84：先预检 3 projects / 65 Runs / active 0 与 XHS search surface；只有 OpenCLI 或 ArchResearch extension XHS fallback ready 才创建唯一一条全新 attempt 0。不得 retry `cbf8053b-...` 或其他封存 Run，失败后不得补开第二条。
31. M84 预检由 `opencli doctor` 证明 daemon/Bridge/connectivity 全绿；唯一 Run `965fde3c-3a75-41b2-a119-381ee1f21c44` 已终止 partial 并永久封存，绝不 retry 或创建第二条。
32. M84 真实产出 4 篇 XHS-only帖子 / 12 张全部 axonometric，generic 0；首方向达到 3 usable notes，但固定 24 slots 在第二方向耗尽，第三方向未分类。源码确认预算按图片而非按批量 Provider 请求计数，三方向 × 三帖的无失败下限已是 36 slots；`time_budget_exhausted` 还误标了 inspection-slot exhaustion。
33. M87 已达 `implementation_complete`。建筑设计结果只显示案例策略对照、案例研究板与证据板；图纸灵感只显示图纸表达对照、表达规范、个人灵感板与来源板；两种模式都没有研究过程入口/抽屉，Trace 持久化/API 与内部诊断仍保留。最终 323 API / 96 Board / 165 Extension / 8 packaged E2E、detector 与无截图真实 DOM/390px QA 全绿。当前 3 projects / 67 Runs / active 0，所有封存 Run 未 retry。
34. M88 已达 `implementation_complete`：建筑设计保留案例策略对照；图纸灵感已删除对照入口，并确保视觉结果没有对照 dock/dialog。视觉逐图选择改成导出语义。两种工作台都为桌面一行三列：design 为 compare/private/share，visual 为 style/private/share。最终 323 API / 96 Board / 165 Extension / 8 packaged E2E、detector、无截图 desktop/390px QA 全绿；当前 3 projects / 67 Runs / active 0，所有封存 Run 未 retry。
35. M89 已达 `implementation_complete`：GitHub 只读参考已记录（SiYuan、OCA Knowledge、React-Konva Moodboard、Dibyasom Moodboard、Inline Studio）。建筑设计导出按案例生成条件/机制/转译/边界/逐字证据台账；图纸灵感导出按帖子聚合标题/观察/来源，图片只保留图纸类型，并展示保存的表达规范。manifest 使用 `case_research_board` / `visual_inspiration_board` 两种 artifact kind；Board 表达规范新增纹理与版式备注。完整门禁 326 API / 96 Board / 165 Extension / 8 packaged E2E，加根 lint/typecheck/test/build、Ruff、strict Mypy、detector 0、无截图 E2E 全绿；API/Board 已重载，health 200，当前 3 projects / 67 Runs / active 0，`10d31b4c-...` attempt 0 永久封存，staged 0。没有创建或 retry Run。
36. M90 已达 `implementation_complete`：新导出使用 canonical `/v1/assets/{id}/content`，旧导出由同一安全 handler 的 schema-hidden `/v1/results/{id}/content` 兼容，真实旧导出三图自然尺寸均非 0。Board 默认显示最近 10 条、按需展开完整历史，正式 Run 不自动过期；临时记忆未来若批准才采用独立 30 天规则。最终 326 API / 97 Board / 165 Extension / 8 packaged E2E、全部静态/构建/detector、无截图 QA 全绿。当前 3 projects / 67 Runs / active 0；封存 Run 未 retry。当前没有已批准下一步，等待用户指定产品工作。
37. M91 已达 `implementation_complete`：首页最近研究固定 3 条，真实页面 starter/recent 高度均为 398.8px；全部 26 条进入低于 viewport 的 bounded overlay，Escape 焦点返回、error 0。现有架构只有 durable Run history、30 天诊断元数据、7/30 天临时资产/来源和 per-Run/user-curated state，没有独立 memory store。最终 326 API / 97 Board / 165 Extension / 8 packaged E2E 全绿，当前 3 projects / 67 Runs / active 0。当前没有已批准下一步，等待用户指定是否建设真正的项目记忆层。
38. M92 已达 `implementation_complete`，且不建记忆层。案例私有导出是至少两个不同项目才能生成的无图片策略矩阵，按研究问题对齐机制、可迁移动作、边界与来源，并附去重动作清单；图纸灵感导出不变。Run 默认保留 14 天，可逐条永久保留或取消永久；55 条 blocked/partial/cancelled 及关联本地状态已由安全 helper 删除，现为 3 projects / 12 completed Runs / active 0。最终 330 API / 97 Board / 165 Extension / 8 packaged E2E、真实双项目无截图 QA 与 loaded service 均通过；`10d31b4c-...` completed/attempt 0 保留且绝不 retry。当前没有已批准的自动下一步，等待用户指定产品工作。
39. M93 已达 `implementation_complete`：个人收藏复用 `SavedReference` 快照与独立本地图，普通 14 天 Run 清理不删除收藏；主页入口进入完整非弹窗页面，先分建筑方案 / 图纸灵感，再按研究题目分组，收藏页与结果页各有唯一“返回主页”。底部条只计未收藏项，保存后只显示“加入成功”，新勾选后恢复；一次提交的多项先全部保存，再删除同模式同题旧批。最终 331 API / 100 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、loaded desktop/390px 无截图 QA 全绿；当前 3 projects / 12 Runs / active 0，`10d31b4c-...` 永久封存且未 retry，staged 0。当前没有已批准的自动下一步，等待用户指定产品工作。
40. M94 已达 `implementation_complete`：小红书是当前唯一视觉灵感来源；Pinterest 已从 API/Board 来源契约、界面、prompt/routing 和当前产品文档删除，通用 Provider 意外返回的 Pinterest/`pin.it` 结果在持久化前直接丢弃。完整 331 API / 100 Board / 165 Extension / 8 packaged E2E 与全部静态/构建/评测门禁通过，loaded OpenAPI 仅含 `xiaohongshu` 且 Board 无 Pinterest 文案。当前仍为 3 projects / 12 completed Runs / active 0，封存 Run 未 retry；当前没有已批准的自动下一步，等待用户指定产品工作。
41. M95 已达 `implementation_complete`：建筑启动器常显紧凑三档“研究方式”，补充资料只保留 URL/文件；建筑模式不再显示或提交 XHS，也不显示研究环境，图纸灵感则自动使用 XHS 并独占紧凑研究环境。普通建筑查询按 `round + per-round query position` 在 ArchDaily、Designboom、Dezeen、Divisare、ArchDaily China 间轮换，首轮不同问题不再全部锁定 ArchDaily，补查顺序与站点语言路径保持。最终 331 API / 100 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、loaded desktop/390px 无截图 QA 全绿；服务已标准重载，当前 3 projects / 12 completed Runs / active 0，`10d31b4c-...` attempt 0 永久封存且未 retry，staged 0。当前没有已批准的自动下一步，等待用户指定产品工作。
42. M96 已达 `implementation_complete`：个人收藏打开后默认建筑方案，顶部常驻建筑方案 / 图纸灵感双向切换，下方原位替换内容，不增加空白分类层或返回层级。建筑收藏按原题目显示项目与研究文字、0 图片；图纸灵感为图片-only 网格，0 题目/标题/观察 copy，来源/删除退到图面图标。最终 331 API / 100 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、loaded desktop/390px 无截图 QA 全绿；服务已标准重载，当前 3 projects / 12 completed Runs / active 0，`10d31b4c-...` attempt 0 永久封存且未 retry，staged 0。当前没有已批准的自动下一步，等待用户指定产品工作。
43. M97 已达 `implementation_complete`：图纸收藏图片本身可用键盘在新标签打开本地高清副本，缺副本沿用保存的原图 URL；原帖来源与删除保持独立。最终 331 API / 100 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、loaded desktop/390px 无截图 QA 与真实 200 image/png 响应全绿；服务已在 active 0 下标准重载，当前 3 projects / 12 completed Runs / active 0，`10d31b4c-...` attempt 0 永久封存且未 retry，staged 0。
44. M98 已达 `implementation_complete`：建筑收藏按“原研究题目 → 案例子问题 → 项目案例”组织，每个平面案例台账默认显示逐题条件、机制、两步转译与首要边界，剩余内容和精确 statement 绑定的逐字原文按需展开；缺失机制的部分分支诚实留空。新收藏持久保存 Pydantic/TypeScript 对齐的 `case_subquestions`，旧收藏首次读取时只增升级并写回 snapshot；当前两个旧收藏已由 SQLite 确认为 1+3 条持久逐题数据。最终 331 API / 101 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、detector 0、loaded desktop/390px 无截图 QA 与 Git 守卫全绿；图纸灵感图片-only 和高清/原帖双链接保持。当前仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存且未 retry，staged 0。
45. 2026-07-23 用户报告 localhost `ERR_CONNECTION_REFUSED`；只读诊断确认 5173/8000 无监听且进程记录已过期，SQLite 仍为 3 projects / 12 completed Runs / active 0。active 0 下执行一次标准 stop/start 后 API/Board 均恢复 200，新进程晚于当前源码，封存 Run `10d31b4c-...` 仍 completed / attempt 0 / coverage_satisfied；未创建或 retry Run、未恢复 Firecrawl、未截图。
46. M99 已达 `implementation_complete`：建筑收藏使用命名的原研究题目 / 案例子问题 region，并补齐可见“原研究题目 / 项目案例”标签；一个原题只使用一张平面工作纸，子问题为整行浅色章节头，项目为无阴影纸头，字段以 112px 标签轨与主墨色正文形成 24/20/16/12/14px 层级。101 Board、detector 0、完整 331 API / 101 Board / 165 Extension / 8 packaged E2E、无截图 desktop/390px QA、active-0 重载与 Git 守卫全绿；图纸 image-only、高清/原帖入口、来源/删除和数据均未改变。当前 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存且未 retry。
47. M100 已达 `implementation_complete`：建筑收藏已取消 M99 的外层工作纸、浅色问题带、112px 字段轨和“查看完整相关内容”，改为以留白与细分隔线组织的连续案例档案；项目条件、空间机制、全部转译、全部边界和精确逐字证据默认直接显示。`SavedReferenceSnapshot` 新增 typed `case_images`，新收藏保存、旧收藏独立懒升级均最多保留三张同 Run/同项目正式图片，优先当前资产和类型多样性；图片只用于识别回看，不证明机制，不用 XHS 或新抓取补数。当前真实收藏持久为 De Nederlandsche Bank 3 图、Instituto 1 图。最终 331 API / 101 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、detector 0、loaded desktop/390px 无截图 QA、active-0 重载与 Git 守卫全绿；图纸灵感本地高清入口不变。当前仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存、completed / attempt 0 / coverage_satisfied 且绝不 retry。
48. M101 已达 `implementation_complete`：建筑收藏不再重复研究审计，只回答案例如何解决当前子问题。每个有依据的案例先显示逐题“核心解法”，再显示去重后的最多三条“怎么做”；项目条件、完整边界和逐字证据继续保存在 durable snapshot/原来源，但不进入收藏表面。最多三张已有正式案例图移到答案之后，宽屏为约 220px 紧凑图带，≤620px 为两列；无解法关联只显示诚实短提示。最终 331 API / 101 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、detector 0、loaded desktop/390px 无截图 QA、图纸灵感高清回归、active-0 重载与 Git 守卫全绿。当前仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存、completed / attempt 0 / coverage_satisfied 且绝不 retry。
49. M102 已达 `implementation_complete`：两类收藏图片只通过 CSS 网格放大，不改 M101 解法优先结构或任何数据/链接。建筑桌面为三条 300px 图轨，≤620px 首图全宽、后两图并排；图纸灵感桌面最小列宽 340px，当前工作面实测约 502×520px，手机单图约 365×486px 全宽。本地高清入口、来源/删除和 caption 均保持。最终 331 API / 102 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、detector 0、loaded desktop/390px 无截图 QA、active-0 重载与 Git 守卫全绿。当前仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存、completed / attempt 0 / coverage_satisfied 且绝不 retry。
50. M103 已达 `implementation_complete`：个人收藏的建筑方案默认先显示“问题目录”，每个平铺入口对应一个案例子问题，并同时显示原研究题目与已收藏案例数；点击后只渲染该子问题的 solution-first 案例详情，可明确“返回问题目录”。切换建筑/图纸类型、返回主页、重新打开收藏或所选子问题失去最后案例时都会回到目录。M101/M102 解法与大图布局、图纸灵感图片-only 和本地高清入口均保持，不新增 API、快照字段、路由或持久化状态。最终 331 API / 102 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、detector 0、loaded desktop/390px 无截图 QA、active-0 重载与 Git 守卫均通过；真实默认目录为 3 项、0 详情/案例/图片且无横向溢出。当前仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存、completed / attempt 0 / coverage_satisfied 且绝不 retry；staged 0。
51. M104 已达 `implementation_complete`：“补充资料”入口改为“添加资料（可选）”，展开后先明确已有案例页、项目任务书或待查图片时再添加，不填也能研究。URL 改为“案例或项目网页”，只承诺作为研究线索；文件改为“项目文件或待查图片”，明确 PDF 补充项目条件、JPG/PNG 用于来源反查。两项说明均与输入建立 accessible description，≤860px 输入为 44px；请求、workspace artifact、workflow 与支持格式不变。最终 331 API / 103 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、detector 0、loaded desktop/390px 无截图 QA、active-0 重载与 Git 守卫均通过。当前仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存、completed / attempt 0 / coverage_satisfied 且绝不 retry；staged 0。
52. M105 已达 `implementation_complete`：建筑研究只显示可选的项目任务书 PDF 与指定案例/项目 URL；来源反查常显一张必需待查 JPG/PNG 与可选疑似来源 URL，主按钮为“开始反查”；图纸灵感不显示这组资料。切换场景会清空上一场景尚未提交的 URL/文件并收起建筑资料区，隐藏资料不会误提交。最终 331 API / 103 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、loaded desktop/390px 无截图 QA、active-0 重载与 Git 守卫全绿；当前仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存且未 retry，staged 0。
53. M106 已达 `implementation_complete`：建筑资料入口改为“添加任务书或案例页（可选）”；任务书明确作为上游研究边界，系统读取场地、功能与限制后用它收束问题拆解和案例检索，指定案例/项目 URL 仍只是独立线索。界面不承诺独立预分析或自动改写问题。最终 331 API / 103 Board / 165 Extension / 8 packaged E2E、全部静态/构建/评测、loaded 无截图 QA、active-0 重载与 Run/Git 守卫全绿；当前仍为 3 projects / 12 completed Runs / active 0，`10d31b4c-...` 永久封存且未 retry。
54. M107 已达 `implementation_complete`，真实流程验收 complete：任务书只作为同一研究的后台边界，Board 保持唯一“开始研究”，不显示摘要/边界/问题编辑中间页，也不重复文件名。内部非持久化 `/brief-review` 先生成 3/4/6 条 typed subquestions，再由同一提交上传 PDF 并创建普通 Run；失败时保留输入且不静默绕过。最终 334 API / 105 Board / 165 Extension / 8 packaged E2E、全部静态/构建/安全/评测全绿。
55. 唯一真实 Standard Run `ff16988d-ebbd-4da4-b5e2-3cd080dcf0be` 已 `completed / completion_satisfied / attempt 0`：真实 3 页任务书只保存一次，4 条 checkpoint 问题、28 usable assets、8 projects、4/4 coverage、107 EvidenceClaims；结果直接进入正常标准研究页。当前为 3 projects / 13 completed Runs / active 0 / 1 InputArtifact。
56. `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍 completed / attempt 0 / coverage_satisfied，永久封存且绝不 retry。当前没有已批准的自动下一步，等待用户指定产品工作。
57. M112 已达 `implementation_complete`：建筑结果的多图项目将重复推断、相同观察与共同缺省说明提升到一条平面式“共同图面说明”，每张图只保留图纸类型、真正独有文案、相关度与既有操作；无独有文案时不渲染空 caption，单图标题与观察不变。真实 M107 结果的 5 个多图 / 6 个单图在 desktop/390px 无截图 QA 中通过，最终完整门禁为 334 API / 106 Board / 165 Extension / 8 packaged E2E，detector 0。当前 API/Board 200，3 projects / 13 completed Runs / active 0 / 1 InputArtifact，封存 Run 不变，staged 0。当前没有已批准的自动下一步，等待用户指定产品工作。
58. M113 已达 `implementation_complete`：来源反查/TinEye 已从启动器、Pydantic/TypeScript goal、workflow/provider 注入、配置、现行文档和评测夹具删除；OpenAPI 只接受建筑研究与图纸灵感。建筑结果在项目档案选择一次案例，逐图证据仍可打开但不再勾选；图纸灵感继续逐图选择。项目菜单与完整历史弹层通过键盘、desktop/390px 无截图 QA。最终完整门禁为 327 API / 107 Board / 165 Extension / 8 packaged E2E、25/108 evaluation，detector 0。active-0 标准重载后 API/Board 200，仍为 3 projects / 13 completed Runs / active 0；`10d31b4c-...` 永久封存、completed / attempt 0 / coverage_satisfied 且绝不 retry。当前没有已批准的自动下一步，等待用户指定产品工作。
59. M115 已达 `implementation_complete`：Workspace 新增显式归档状态，项目菜单分为 2 个当前项目与 1 个历史归档，归档视图只读。真实事务新建 `苏州科技馆 · 蚕桑丝织智慧博物馆`，迁入唯一 M107 Run、1 份任务书和 2 条关联收藏；历史验证 Workspace 改名并归档，零内容 Workspace 删除。当前为 3 projects / 13 completed Runs / active 0，全部证据、retention 和封存 Run 保持。
60. M116 已达 `implementation_complete`：建筑收藏选择按“案例子问题 + 代表 asset”独立计数，同 asset 跨题选择不会联动，两个关联显示 2 并只 POST 一次精确 `subquestion_ids`；成功后建筑/图纸勾选和持久 Board selection 全部清空，下次从 1 重新选择。收藏详情在 >1180px 使用解法/大图双列，≤1180px 单列；1440/390 无截图 QA、detector 0、完整 329 API / 109 Board / 165 Extension / 8 packaged E2E 门禁全绿。服务已在 active 0 下标准重载，API/Board 200，封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍 completed / attempt 0 / coverage_satisfied，绝不 retry。当前没有已批准的自动下一步，等待用户指定产品工作。
61. M117 已达 `implementation_complete`：历史记录统一为 Run 级标题，不是新的 Workspace 归档生命周期。API 为全部现有与未来 Run 从完整 `question` 确定性派生最多 28 字的 `title`，最近研究、全部历史、打开与保留操作统一使用；完整问题不改写、无数据库迁移和模型调用。实页苏州记录为“耕织图：转译提取元素”，旧厂房 10/10 标题无双冒号；390px 无溢出且历史关闭/保留均 44px。最终为 335 API / 110 Board / 165 Extension / 8 packaged E2E、detector 0，API/Board 200，3 projects（2 active + 1 archived）/ 13 completed Runs / active 0；封存 Run 不变且绝不 retry。当前没有已批准的自动下一步，等待用户指定产品工作。
62. M118 已达 `implementation_complete`：`scripts/start.ps1` 现在会先验证状态文件记录的 API/Board 是否各有一个当前 workspace listener 且 HTTP 可用；完整健康时直接复用相同端口和 listener/launcher PID，陈旧、半活或无效状态只清理重新验证为当前 workspace 的监听者并重新启动，绝不信任记录 PID 或停止无关进程。真实昨日陈旧状态已恢复为 API 8000 / Board 5173 且均 200，紧接的第二次启动输出 `already running` 并保持四项 PID 不变；启动失败会输出四份日志各最多 20 行。最终权威门禁为 336 API / 110 Board / 165 Extension / 8 packaged E2E，加全部静态/构建/process/security/25/108 evaluation 全绿。当前仍为 3 projects / 13 completed Runs / active 0，封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 保持 completed / attempt 0 / coverage_satisfied 且绝不 retry；staged 0。当前没有已批准的自动下一步，等待用户指定产品工作。
63. M119 已达 `implementation_complete`：完整数据备份覆盖 SQLite、`runs/collections/workspaces/exports`，包括收藏本地图、任务书/附件和导出；数据库路径跨机器便携化，versioned manifest 记录类别、大小和 SHA-256。预检在写入前拒绝损坏/超大清单、不兼容版本、危险/额外/缺失路径、symlink、hash/size 不符和无效 SQLite；恢复先 staging 复检、生成 rollback ZIP，再只交换白名单数据，模拟中途失败可逐字节恢复当前数据。源码、凭据、日志、依赖、构建、runtime/process state、截图与 `tool-bench` 均排除。最终 341 API / 112 Board / 165 Extension / 8 packaged E2E、全部静态/构建/process/security/evaluation 和 detector 0 全绿；真实 70,993,145-byte ZIP 已通过 API 预检和独立 66-file/hash/SQLite 校验，真实 restore 未执行。当前 API/Board 200，3 projects / 13 completed Runs / active 0，封存 Run保持 completed / attempt 0 / coverage_satisfied 且绝不 retry，staged 0。当前没有已批准的自动下一步，等待用户指定产品工作。
64. M120 已达 `implementation_complete`：保守清理删除 19 个已验证的可再生成/废弃路径，主要是 `.archresearch/tool-bench` 的旧浏览器工具虚拟环境与测试 profile，并包含缓存、dist、测试 XML、Playwright 临时输出、旧 runtime screenshots 和残留 Firecrawl 配置，共释放 2,543,468,519 bytes（2.369 GiB）。所有源码、测试、迁移、规划、发布证据、10 张 portfolio PNG、Impeccable critique、`work/`、node_modules、durable `.archresearch` 数据与 M119 真实备份均保留；四类本地工具输出加入精确 ignore。最终 API/Board 200，3 projects / 13 completed Runs / active 0，封存 Run completed / attempt 0 / coverage_satisfied，`git diff --check` 通过、staged 0。未使用 git clean/reset/checkout，未 stage/commit/push，未创建或 retry Run、未截图、未恢复 Firecrawl。当前没有已批准的自动下一步，等待用户指定产品工作。
65. M121 仍为 `in_progress`，但已批准的小红书代理轮已完成：9 条去身份化公开问法完成模式筛选，唯一 Quick `5e4184cf-c3e7-47ce-bc5d-8761bcc632a1` attempt 0 为 `completed/completion_satisfied`、26 usable、3/3、正式项目 1、enrichment 缺项目多样性与多图项目。M124 已吸收其结果强度和完成语义修复；不得 retry 或创建第二条代理 Run。M121 只有观察 2–3 名真实目标用户后才能 complete。
66. M124 已达 `implementation_complete`：建筑研究只有 coverage 与 enrichment 目标同时满足才进入 `completed`，有用但深度不足的结果诚实终止为 `partial`；active Run 不请求或渲染临时结果。主页历史取消项目选择器、“当前项目 / 历史归档”分类、独立归档区和历史弹窗，3 个项目中的 14 条 Run 直接按问题标题平铺；打开记录只在后台同步所属项目，“新建项目”保留。历史 completed 但 enrichment 不足的记录显示“研究已形成初步依据”。最终 341 API / 113 Board / 165 Extension / 8 packaged E2E 与全部静态、构建、进程、安全和评测门禁通过；loaded 无截图 DOM 为 14 条平铺问题、无分类/选择器/弹窗、无溢出或页面错误。当前 3 projects / 14 completed Runs / active 0，`10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍 completed / attempt 0 / coverage_satisfied，永久封存且绝不 retry。当前没有已批准的自动下一步，等待用户指定产品工作。
67. M125 已达 `implementation_complete`：最近研究仍为跨项目问题平铺，但完整 14 条记录收进具名、可键盘聚焦的原生滚动窗口，最大高度 400px / 55dvh，不再把主页无限拉长；标题/说明与“新建项目”改为紧凑双列头部。114 Board、ESLint、TypeScript、production build 和 detector 0 全绿；loaded 无截图 445×736 几何为头部 52.8px、历史窗口 400px / scroll 1,871px、14 条记录、横向溢出 0、页面错误 0。未改变记录、保留、打开或后台项目同步，未截图、未改 durable 数据。当前没有已批准的自动下一步，等待用户指定产品工作。
68. M126 已达 `implementation_complete`：最近研究原生滚动窗口进一步缩短为最大 320px / 45dvh，默认约露出三条；14 条跨项目问题记录、键盘聚焦、打开、保留动作和紧凑头部全部保持。focused red/green、114 Board、ESLint、TypeScript、production build 与 detector 0 全绿；loaded 无截图 445×736 override 几何为历史窗口 320px / scroll 1,871px、14 条记录、横向溢出 0、页面错误 0。当前仍为 3 projects / 14 completed Runs / active 0；`10d31b4c-94dd-4442-b24a-fc1b241e658e` 永久封存且绝不 retry。未截图、未改 durable 数据、未执行任何禁止的 Git 动作。当前没有已批准的自动下一步，等待用户指定产品工作。
69. M127 已达 `implementation_complete`：重启后打不开不是 M118 健康检查失效，而是 Windows 登录后从未自动调用启动脚本。当前用户启动目录现已安装受管 `ArchResearch.lnk`，以隐藏窗口和绝对工作区路径运行 `scripts/start.ps1`，不需管理员权限；注册可重复、可安全移除且拒绝覆盖/删除外部同名快捷方式。真实陈旧状态冷启动恢复 API/Board 200，第二次启动复用 listener PID 33660/26036；完整 341 API / 114 Board / 165 Extension / 8 packaged E2E 与全部静态/构建门禁通过。当前 3 projects / 14 completed Runs / active 0；`10d31b4c-94dd-4442-b24a-fc1b241e658e` 永久封存、绝不 retry。未截图、未改 durable 数据、未执行禁止的 Git 动作。当前没有已批准的自动下一步，等待用户指定产品工作。
70. M128 已达 `implementation_complete`：建筑结果与建筑收藏改为答案优先的连续阅读流。每个问题先给结论，再把全部合格案例各显示一次；案例只显示去除发布媒体后缀的项目名、逐题机制、最多三条做法、第一条非审计适用条件和必要识别图，不再显示折叠详情、完成状态条、研究过程、来源/证据/核验文案、来源链接或检视器入口。收藏保留删除、持久化和最多三图；图纸灵感、API、schema、export 与 Run 数据不变。最终 Board 115/115、ESLint、TypeScript、production build、detector `[]`，loaded 1440/390 无截图 QA 无溢出/截断/隐藏案例/页面错误，完整门禁为 341 API / 115 Board / 165 Extension / 8 packaged E2E。当前 3 projects / 14 completed Runs / active 0；`10d31b4c-94dd-4442-b24a-fc1b241e658e` 仍 completed / attempt 0 / coverage_satisfied，永久封存且绝不 retry。未创建 Live Run、未截图、未改 durable 数据、未执行禁止的 Git 动作。当前没有已批准的自动下一步，等待用户指定产品工作。
71. 2026-07-26 完成度检查已完成，并修复了一个新发现的 P0。只读复验确认 3 projects / 14 completed Runs / active 0，341 API 测试独立收集后与 M128 门禁一致。审计发现 14 条 Run 的 `keep_forever` 全为 0、其中 12 条在 `2026-08-03T17:56:37Z` 同时到期，而 `cleanup_expired_data` 在每次 API 启动无条件硬删除到期 Run 及其 assets/sources/claims/traces 与磁盘目录，M127 开机自启会在每次登录触发该路径——8 天后的首次无人值守登录即会删除封存 Run `10d31b4c-…` 与全部三档验收证据；留存的 70,993,145 字节 M119 备份因保存已过期时间戳而无法保护。经用户批准，已用既有 `PATCH /v1/runs/{id}/retention` 把 14 条 Run 全部标为永久保留；独立只读 SQLite 复读为 `keep_forever=1` / 到期时间为空 14/14，按 `lifecycle.py` 原谓词实测可删除 Run 为 0。未改代码、schema 或迁移，未创建/重试/取消 Run，未截图，未执行禁止的 Git 动作，staged 仍为 0。当前没有已批准的自动下一步，等待用户指定产品工作。
72. M129 源码基线保护已完成，这是本文件“已验证基线”的实质变化：`98a9a01` 之后的全部产品实现不再只存在于工作树，已按 M52 三层边界提交到本地分支。130 个路径分为 112 产品 / 5 工程记录 / 11 发布证据 / 2 排除的本地输出；产品基线提交 `d772902` 精确包含那 112 个路径（12 A / 5 D / 95 M），无 `.artifacts`、`.impeccable`、记录或证据泄漏，全程未用 `git add -A`、未 push。`.gitignore` 已补 `.impeccable/` 与 `.artifacts/*.zip`，防止 70,993,145 字节的数据备份 ZIP（内含真实 SQLite）被误提交，同时保留 `.artifacts/portfolio/` 可见。经用户决定，发布证据层（manifest + 10 张 PNG）暂不纳入 Git，文件仍在磁盘上。提交前门禁为 341 API / 115 Board / 165 Extension / 8 packaged E2E 加全部静态/构建/安全/评测检查。
73. 本地 pwsh 的 WMI 缺陷已由并发 Codex 会话红绿修复并提交为 `06f3424`：MSIX 版 pwsh 7.6.4 无法加载 MMI 原生 DLL，`start.ps1` 的归属校验抛 `DllNotFoundException … E_ACCESSDENIED`，catch 分支随即杀掉它自己刚拉起的 API 与 Vite——这正是“登录后打不开且日志无错误”的真因，M127 开机自启每次登录都会重演。监听发现改用 `netstat -ano`（按环回端点与未连接对端判定，不依赖本地化状态词），命令行改用 `NtQueryInformationProcess` 读 PEB；函数签名、工作区归属语义与 `start.ps1`/`stop.ps1` 调用点不变。`scripts/dev-common.ps1`、`start.ps1`、`stop.ps1` 中 WMI 原语命中为 0，`process-lifecycle.tests.ps1` 已在两个独立会话中通过。
74. 本工作区由多个 agent 会话并发写入，这是长期约束而非偶发情况。已实测：产品基线提交完成后，同一工作树的 `scripts/dev-common.ps1` 与 `scripts/tests/process-lifecycle.tests.ps1` 仍在被写，`dev-common.ps1` 由 269 行增长到 431 行，`task_plan.md`/`findings.md`/`progress.md` 也在被并发追加。任何 `git add` 只保证“逐文件读取时刻”的快照，不保证跨文件一致性；提交前后都必须重新读 `git status`，并在另一方仍在写记录文件时暂停提交。
75. 提交与推送的当前边界：本地提交已获授权并已执行，`git push` 仍未获授权且从未执行。禁止 `git add -A`、reset、checkout、clean。10 张 PNG 证据包保持未跟踪，等 M123 刷新证据时再决定是否纳入。
76. M130 已把完整性诚实标签统一到两种模式。M124 的严格规则本身没有失效——`workflow.py:1373` 只有 `gaps` 与 `enrichment_gaps` 同时为空才写 `completed`；首页混排完全来自 M124 之前创建的历史数据。真实缺陷是 `App.tsx` 的降级条件写死 `precedent_research`，图纸灵感 Run 在相同条件下仍自称“已完成”；现改为两种 goal 同时生效，分别输出“已形成初步灵感”与“研究已形成初步依据”。门禁 341 API / 116 Board / 165 Extension / 8 packaged E2E 全绿。
77. **当前持久基线已变为 3 projects / 9 Runs / 9 completed / active 0 / `keep_forever=1` 9/9 / 5 收藏 / 1 份任务书。** M131 经用户逐条确认后删除了 5 条 M124 之前的深度不足 Run：`7d8faa53`、`b4c314a6`、`42668844`、`e525ca77`、`5e4184cf`。删除前已生成并预检通过 70,957,655 字节完整备份，走产品既有 `lifecycle.delete_runs` 安全 helper，停服执行、重启后 API/Board 200。封存 Run `10d31b4c-94dd-4442-b24a-fc1b241e658e` 不在删除名单内，仍 completed / attempt 0 / coverage_satisfied，永久封存且绝不 retry。
78. `76f52c79`（M53/M65 被接受的 Deep）与 `ff16988d`（M107 唯一真实任务书 Standard Run）**被明确保留**，因为它们是现有“已验证”声明的底层数据，不是失败记录。它们在首页显示为“研究已形成初步依据”，这是诚实的旧规则标签，不是缺陷。只有当新的实际测试产生替代证据后才可考虑删除。
79. 遗留证据不一致：`docs/release-evidence-2026-07-16.md` 冻结的三个 accepted run 里，`7d8faa53` 与 `b4c314a6` 的底层数据已删除，`.artifacts/portfolio/` 对应的 8 张 PNG 只剩截图。该文档与 10 张 PNG 均未纳入 Git，处置推迟到 M123 刷新发布证据时一并决定，不要当作当前有效证据引用。

80. M130–M135 已完成结果与收藏的答案优先阅读收口、旧深度不足 Run 的定向删除（当前基线 3 projects / 9 Runs / 9 completed / active 0 / `keep_forever` 9/9）、单文档栏全局版式（`--layout-doc-max` 共享规则 + design-system 契约）、全触点白话文案（词汇统一 + `copy-glossary.test.ts` 源码级守卫封禁 18 个废弃词）与章节结论不复读规则。当前权威门禁为 **341 API / 123 Board / 165 Extension / 8 packaged E2E**。文案词汇表已定稿：怎么做 / 适用条件 / 核心解法 / 对照案例策略 / 连接 Chrome 读取高清图纸 / 转载合集（非首发）/ 备份与恢复；废弃词见 `apps/board/src/copy-glossary.test.ts`。
81. M121 观察工具包已提交至 `docs/pilot/m121-observation-kit.md`（10 个按序任务、去身份化记录表、会后 5 问、P0–P3 处置阈值、硬边界）。**当前唯一下一步：用户招募 2–3 名真实目标用户后按该工具包执行 M121 试点观察**；观察期间不得自动创建 Live Run、不得截图。M122 模块化按既定顺序排在试点之后。

## 工作区保护

- 当前分支：`codex/archresearch-v2-1`（未 push）。M129 之后基线为：`98a9a01` → `d772902` 产品基线 → `06f3424` WMI-free listener 修复 → 工程记录提交。
- 工作树仍是用户资产。未跟踪的 `.artifacts/`（含 10 张 PNG 与数据备份 ZIP）和 `docs/release-evidence-2026-07-16.md` 是有意保留在 Git 之外的，不要重置、覆盖或顺手清理。
- 重要决策写 `findings.md`；阶段和验收写 `task_plan.md`；进展、错误和恢复点写 `progress.md`。不要写逐命令流水账。

## 给新对话的第一句话

> 继续 ArchResearch。先完整读取 HANDOFF.md，再按它读取 task_plan.md、findings.md、progress.md；不要重做已完成工作，不要恢复 Firecrawl，先汇报当前状态和唯一下一步，然后继续执行。
